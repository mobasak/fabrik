"""Vendored fabrik-lib `health-probe` (D-082) — `from libs.health_probe.health_probe import compare`.

Vendor, don't depend: the two modules beside this file are byte-identical to fabrik-lib at the sha in
their VENDORED-FROM header. A bump is a re-vendor (replace both files + the sha), never an edit here.
"""
