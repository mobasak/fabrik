## ⚠️ Question bar — decide, don't drip

Ask the user ONLY when a question clears BOTH: (1) the answer **materially changes {{CHANGES_WHAT}}**, AND (2) you **cannot resolve it** from {{RESOLVE_FROM}}. Otherwise **decide it, apply the house rules, and note it in one line** the user can override. **Never interrupt for** {{NEVER_FOR}}. **Do** raise: {{DO_RAISE}}.
