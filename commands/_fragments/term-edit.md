## ⚠️ Termination contract — READ FIRST (the rule agents skip)

This is a LOOP, not a one-shot. It ends — and you may {{DONE_ACT}} — **only when a delta round that carried a fresh non-authoring seat CONFIRMED zero defects — confirmed by EXECUTION (a pinned read quoted beside the sentence, a count re-derived from its primary source, a `path:line` resolved at the pinned SHA, a gate line run, a URL status probed by the orchestrator) — with nothing left unexecuted (`confirmed: 0, fixed: 0, unexecuted: 0`) and the md5 unchanged (D-206, D-212)**. Fixes open new gaps, so **the round in which you edited the {{ARTIFACT}} is NEVER the last round** — it MUST be followed by another delta round. **The pass SHAPE:** round 1 is the ONLY full pass — it reads the WHOLE {{ARTIFACT}}, cut into DISJOINT slices: by SECTION for the spec and plan reviews (Opus on the rule/grammar sections, Sonnet on the rest — one seat each for a small artifact, more slices when `dispatch_headroom.py --slices opus=N,sonnet=N` sizes them; `fabrik-researcher` seats only for the external facts the {{ARTIFACT}} cites, counted inside `opus=`/`sonnet=` by their model token; no Haiku seat — D-207, D-212, D-218); every other consumer by its own independent units, in the mix the `⚠️ Floor` line of its `## Subagents` section states where it has one (a Sonnet seat per unit, a Haiku mechanical seat per grep-able class, the Opus authoritative seat) and the D-208 three-seat floor otherwise — a minimum, never the count; under a partition the three-seat floor stands down, satisfied by construction (D-208, D-218); no slice's text read by two seats, and the union IS the pass — unless `dispatch_headroom.py` printed a `TRIMMED partition` line: then the union is NOT the full pass, the ledger names the unread slices and the next round sweeps them first; every later round is a DELTA over the previous round's fix diff plus one hop — the sections whose cross-reference tokens (`I#`, `D#`, `G#`, `T##`, a `§` heading, a `path:line`) cite the edited section — plus any sibling commit on the {{ARTIFACT}} since the last round (a round that edited nothing — a quiet round 1, or a middle round whose confirmed defect was routed to a backlog row — leaves an EMPTY fix diff: the next round's delta is then the load-bearing claims the previous round's ledger names — its counts, enumerations, anchors and executable rules — re-derived by the fresh seat; a bounded re-derivation, never a second full read) — sized by the fix: one Opus seat for a rule or grammar paragraph, one Sonnet seat for wording, plus the hygiene script; the CLOSING round is a delta round with a fresh non-authoring seat (`62-using-subagents.md` § Role separation defines independent) whose Method cell leads with `method: re-derivation` and names the standing-clean classes it did not re-read (the ONE sanctioned exception, `62-using-subagents.md` § Role separation: a solo self-convergence loop — a command whose loop dispatches NO finders anywhere — closes on its own re-derivation delta round, and its independence is deferred to the paired review command that follows it, never skipped). **A dispatched finder that DIES (stream watchdog, API stall, null return) has read NOTHING — its slice stays un-swept until re-dispatched or covered by you, and the round row records dispatched vs returned** (web-ecommerce-factory 2026-08-29: 2 of 3 finders died at the harness's 600s watchdog and a converged-looking round had silently lost two-thirds of its recall). ⚠️ **THE CLOSING ROUND IS A RE-DERIVATION ROUND — a different METHOD, not just a fresh read, and the
ledger must SAY so.** RE-DERIVE every factual claim inside the delta — every count, enumeration, and line anchor —
from its primary source; do not re-verify citations (those were verified in earlier rounds, and
re-running the same method reaches the same blind spots). **When the {{ARTIFACT}} carries an EXECUTABLE rule — a verdict algebra, a state machine, a truth table, a comparator — the closing round EXECUTES it against the artifact's own cases (a throwaway script is enough) and records the run; text-only re-derivation is blind to a rule that is internally consistent and wrong** (the deployment-verification spec: five text-only CONVERGED stamps each void within hours, the sixth held because it RAN the algebra — 01M1GMTZ). Three classes are invisible to
citation-checking BY CONSTRUCTION: **count-re-derivation** (the cite proves the example, never the
number), **bounded-search-as-total** (a `-N`/`head`/`-A` window read as the population), and
**data-integrity** (a constraint the text asserts and the store does not hold). Two live proofs one
week apart: trade-intelligence (two false claims through TWO quiet-round stamps) and tryton-crm (SIX
defects through ELEVEN rounds — the sentence prescribing this round was IN the command and applied
in zero rounds, because nothing detected its absence). So the **Pass Ledger carries a `method`
column** (cells self-label: `method: citation — <scope>` | `method: re-derivation — <scope>` | `method: gate` — the LABEL leads the cell and is what the graders anchor on; `method: delta re-derivation` matches nothing), the exit requires **at least one
row naming `method: re-derivation`** (D-053 amended), and BOTH graders demand it: `check_convergence.py` REFUSES a plan's CONVERGED
flip without one, and `check_review_coverage.py` fails any changed review artifact lacking one; the form is machine-checked; running the round honestly is what the token attests.
Convergence under one method is method-stability, not truth. **Minimum two rounds, ALWAYS** — even a quiet round 1 must be confirmed by an independent delta round 2 (`command_run.py` prints TERMINAL only after ≥2 rounds, D-211); accuracy outranks round-count.

Anti-cheat (mechanical, not vibes): record the `md5sum` of the WORKING-TREE {{ARTIFACT}} at the pin of every round (`md5(start)`) and after its fixes (`md5(end)`) — never hash the pin itself, which is frozen by construction; residue fixes are the previous round's edits and precede its `md5(end)`, so the closing round's `md5(start)` equals that hash. Identical hash AND `confirmed: 0, fixed: 0, unexecuted: 0` on a delta round with a fresh seat = the quiet round → {{DONE_WORD}}. A different hash means you edited → the next delta round. A quiet round asserted without matching hashes does not count.{{EXEMPT_NOTE}}

**Before Pass 1 and before every pin — three mechanical obligations, in order:** (1) **RESIDUE, before any pin after pass 1:** re-read every edited paragraph WHOLE on the working tree, grep the {{ARTIFACT}} for the old wording, count the edited tables' pipes, and run the hygiene script on the WORKING TREE before the `cp` — the orchestrator's own fix residue is caught by the script, never by a seat (measured: 8 of 13 plan-review rounds were fix residue, and 35 of 44 spec-review passes changed ≤6 lines); these fixes are the PREVIOUS round's edits — count them in its `edits:` and take its `md5(end)` after them. (2) **PIN** the {{ARTIFACT}} once PER ROUND — `cp` it to scratch, put its md5 in every brief (that hash is the round's `md5(start)`), and never touch the pin while its seats run (the seats read the pin; you edit the working tree). (3) **SCRIPTS, then the flip gates:** at EVERY pin run `python3 scripts/enforcement/check_review_hygiene.py --surface <pin> --phrase "<old wording>"` on the pin (the phrases: at pass 1 the retired wording your brief names, if any; from round 2 the old wording of every sentence the last round edited — with no `--phrase` the run asserts nothing about wording) and `python3 scripts/enforcement/check_citations_resolve.py --changed` in the REAL tree — advisory, and BOUNDED: it grades only the docs git reports as CHANGED under `docs/superpowers/specs/`, `docs/development/plans/`, `docs/development/reviews/` and `docs/reference/` (its `SOURCE_GLOBS`) — a DATED name (a spec, plan or review) only inside its 30-day window (`DEFAULT_SINCE_DAYS`), an undated doc under `docs/reference/` always: `git add` (or `git add -N`) a fresh DRAFT before the first pin — an untracked file is invisible to it — and for an artifact outside those roots, or a dated one older than the window, it examines NOTHING, so its anchors stay the seats' duty (a `--pin` root and wider globs are a backlog EXTEND); at a scratch root every repo path is missing and it ticks green over 0 examined anchors. Then run the {{ARTIFACT}}'s flip gates — where it HAS them, per the flip-gate invocation matrix (the `MATRIX` table in `tests/enforcement/test_flip_gate_matrix.py`, documented in `docs/workflows/FINAL_GATE_WORKFLOW.md`): a spec's gate is the `check_spec_convergence` row; a plan SET's are the other four; a MONOLITH plan's are `check_rule_grounding`, `check_convergence` and `check_plan_quality` (`check_plan_tickets` refuses a file); a FROZEN-chain artifact (flows, data contract, UI design, design system) has `check_frozen_chain.py` at its flip and the data contract `check_schema_sync.py` too — outside the matrix, invoked as `final_gate.py --check` names them; every other artifact has no flip gate today — its ledger SAYS so rather than recording a gate that did not run — at pass 1 AND again at the close, because a gate run COUNTS only when you can show the {{ARTIFACT}} was in the gate's examined set (measured 2026-09-10: a committed-clean scratch copy, a scratch-root anchor check and `check_plan_quality` outside its cwd-bound `PLAN_DIR` all return green over NOTHING, and a non-dated set directory is REFUSED aloud); the working-tree {{ARTIFACT}} is never flipped for a gate.

Maintain a numbered **Pass Ledger** and reproduce it verbatim in your report — each row names what that round actually RE-CHECKED (a verification-only look is labelled `VERIFY`, never numbered as a pass; a `confirmed: 0` row for a round that never ran is a FABRICATED row, worse than an honest unfinished ledger). You are done **only when the last row reads `confirmed: 0`, `fixed: 0` and `unexecuted: 0`** — the shape both graders read as quiet (`_confirmed_quiet` in `check_review_coverage.py` on a receipt's LAST row; `QUIET_PASS` in `check_convergence.py` wherever the row sits): a closing round that parked a candidate unexecuted has not executed its verdicts — with `md5(start) == md5(end)` on a delta round that carried a fresh seat. Any last row with `confirmed > 0` or `unexecuted > 0` means you owe the next delta round — **run it UNPROMPTED, inside THIS invocation**, the moment it is owed; the obligation predates any challenge. Never end the turn on a non-zero row for the operator to re-invoke — **you return control EXACTLY ONCE: at the quiet delta round.** Three thoughts that each mean *run the next round now*: "it was already done," "the edit was trivial," "it's obviously clean." **Verdicts are EXECUTED, never argued (D-206, D-212):** a seat PROPOSES a candidate with a concrete failure scenario and the command that would prove it; YOU run it. A candidate is CONFIRMED only when you executed it and it held; otherwise it is `RECORDED — unexecuted (<why>)` · `RECORDED — by design (D-nnn)` / `(I#, round k)` · `RECORDED — measured (<why>)` · `RECORDED — hygiene false positive (<why>)`; REFUTED carries the executed disproof (the quoted line, the re-derived count, the status code); a wording nit is `RECORDED — measured (wording; changes no behaviour)`, never edited in a closing round. RECORDED and REFUTED rows never reopen the loop and never count in `confirmed:`; a re-raise of a RECORDED candidate that carries evidence its row does not cover is a NEW candidate and is executed (the RECORDED rows in the ledger replace the retired `Standing:` line — D-219); a `RECORDED — unexecuted` candidate is executed in the NEXT round or its row says why it cannot be (`measured`, `by design`), which is how the closing row reaches `unexecuted: 0`. Each row carries ONE counters cell — the receipt grammar's five counters in `check_review_coverage.py`'s own order, `found: F, new: N, confirmed: C, fixed: X, unexecuted: U`, plus `edits: E` for the document loops (the receipts have no `edits:`; `unexecuted:` counts the round's RECORDED — unexecuted rows) — a seats cell naming every seat by model token (`opus×1`, `sonnet×2`, `(fresh)` on the closing round's), a method cell, and the md5 pair; stamp every round on the run record with `python3 scripts/command_run.py round --seats <n> --findings <found> --confirmed <confirmed> --classes-swept <every class of the ledger — cite the last full pass for the classes the delta did not touch> --classes-new …`, and call `done` only when it prints TERMINAL AND the closing row reads `unexecuted: 0` with a fresh seat named — the record carries no `unexecuted` counter and its verdict ignores `--seats`, so TERMINAL is necessary, never sufficient. **Stall circuit-breaker:** 3 consecutive delta rounds with non-decreasing **and nonzero** `confirmed:` counts → STOP with `## BLOCKED: NON-CONVERGENCE` naming the suspected foundation error as ONE operator question with a recommended answer — measured non-progress means the defect is upstream of the {{ARTIFACT}}'s text (a design claim, a contract); a candidate that asks for NEW content (a mechanism the design never chose, a section the {{ARTIFACT}} never had) is a plan-time or spec-time item routed as that question or a backlog row, never grammar designed by review — a review edit CORRECTS, it does not extend. Not a ceiling: rounds stay unbounded while `confirmed:` falls, a refute-everything round never trips it, and an all-zero `confirmed:` streak is convergence, never a stall — the breaker just doesn't fire; DONE still requires the quiet delta round above. **Probe duty (pass 1: every probe; every later round: every probe inside its delta and every probe whose text cites an edited section):** RUN each probe in that scope — a probe is a fenced block whose first line begins `$ ` (illustrative fences without that prefix are exempt) — and compare output verbatim; a later round re-runs its scope because an edit can silently stale a probe that was true at pass 1. A LOAD-BEARING claim (one some step or verdict would change were it false) carrying NO probe is itself a standing finding: "ungrounded claim". **There is NO round ceiling** — while anything is still being CONFIRMED or edited, the next delta round is owed. The non-quiet stops are exactly THREE — the stall circuit-breaker above; a per-axis **BLOCKED escalation**: an axis stuck after 3 consecutive reconcile attempts → pause it, surface it to the operator in the report's `## BLOCKED` section (axis + the 3 attempts), keep converging the rest; and **no seat to dispatch** — `dispatch_headroom.py` printing `SEATS: 0` (a loaded box, a failed quota probe) is re-run once a minute until a seat frees, and the fleet-quota HOLD (`62` § Dispatch policy: it dispatches nothing) is `blocked` (missing infra) with the round's state committed — a round that cannot carry its fresh seat is never closed solo. Never self-exit with an "accepted risk". **Context is never a reason to stop:** the harness AUTO-COMPACTS long conversations and the run continues in the same invocation — keep durable artifacts current and keep going; "low context" filed as BLOCKED is still the named violation, and a heavy remainder is dispatched to fresh subagents, never deferred. Post-compact, the SessionStart hook auto-injects a recent-context recap, and the `session-recall` MCP (`search_chats` / `get_chat`) recovers any detail the summary dropped — use it instead of re-deriving or guessing.

| Pass | seats · axes re-checked ({{AXES}}) | counters | method | {{ARTIFACT}} md5 (start → end) |
|-----:|---|---|---|---|
| Pass 1 | opus×1 + sonnet×1 (the round-1 slices) · all axes | found: 9, new: 9, confirmed: 6, fixed: 6, unexecuted: 2, edits: 7 | method: citation — full pass (6 confirmed + 2 unexecuted + 1 refuted) | a1b2… → 9f8e… |
| Pass 2 | sonnet×1 (fresh) · delta over § 3, § 7 + one hop | found: 1, new: 1, **confirmed: 0**, fixed: 0, unexecuted: 0, edits: 0 | method: re-derivation — delta over § 3, § 7; standing clean since pass 1: every other class (1 refuted) | 9f8e… → 9f8e… ✓ → **{{DONE_WORD}}** |
