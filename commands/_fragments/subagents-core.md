## Subagents — {{HEADLINE}}

Per `62-using-subagents.md` § Dispatch policy (the canonical detail), this command's gradeable fan-out defaults to the **OpenRouter pool** via `fanout({{TASK_TYPE}}, units, repo=REPO, project="{{PROJECT}}", …)` — the hand-picked, flywheel-scored roster (family-diverse; no default price cap). `fanout` **auto-records each unit UNSCORED** at dispatch → after you merge + refute, **back-fill your verdict** with `set_quality(r.agent_id, score, project="{{PROJECT}}", task_type={{TASK_TYPE}}, model=r.model)` — a row left unscored teaches the flywheel nothing. ⚠️ Never hand-roll `run_agents`+`record_run` — it no-ops. `mode="read_only"` = single-shot + inline content (`web_tools`/`mcp_servers` still callable); `mode="write"` = real file reads with **disjoint `owned_paths`** per unit or it serializes (`62` § Parallelism). Guard the import: `try: from libs.subagents import fanout, set_quality` / `except ImportError: fanout = None`. Native subagents produce no `AgentResult` → they record nothing (by nature); `scripts/enforcement/check_subagent_flywheel.py` WARNs on an unrecorded pool run.{{FLOOR}}{{EXTRA}}
