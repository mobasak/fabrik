## Subagents — {{HEADLINE}}

**⚠️ POOL OFF — D-181 (operator, 2026-09-07).** The OpenRouter subagent pool is OFF by operator ruling (D-181; mechanism revised by D-182 — the provider credentials stay provisioned, so a `fanout` would still dispatch and SPEND: this text is the control, and intel monitors `fabrik_analytics.subagent_runs` for any dispatch after it landed). Until the operator re-enables it, **this command's every fan-out runs NATIVE** — Claude Task subagents (`fabrik-reviewer` · `fabrik-researcher` · `fabrik-gui` · general-purpose) with the SAME unit split, the SAME author-blind/independence rule (`62` § Role separation) and the SAME decide/refute/merge you own. Skip every `fanout` / `pick_models` / `set_quality` / `record_agent_run` / `results_table` step named anywhere in this command — a native seat has no `AgentResult`, records nothing, and owes no score. **Never write `NO-POOL:` for it:** `scripts/enforcement/check_subagent_flywheel.py`'s pool-or-declare layer stands down by the same ruling (`_POOL_POLICY_ON = False`, D-182). Size the native fan-out to the SURFACE, never to a fixed small number: partition it into INDEPENDENT units (failure class · file · screen · doc · pack · journey · fact · behaviour) — the units are the PARTITION and the box is the CEILING (D-191): every unit gets one Sonnet breadth seat AND one Haiku mechanical seat (none on a judgement surface — grounding and adjudication units have no grep-able angle; this command's floor below says which it is), plus the Opus authoritative seat(s) (one per risky unit, at least one), all dispatched **in a single message** so they run in parallel — a 3-unit surface on an idle box is 7 seats (4 on a judgement surface), never a token 1–2. The mechanical angle is grep-shaped, so it is GLOBAL: `--mechanical <M>` is the number of grep-able classes the surface HAS; trimmed below one per unit, each Haiku seat sweeps ONE class across every unit; a grounding or adjudication surface has none (`--mechanical 0`). The CAP is independence OF THE SURFACE, not of the reader: partition so no unit's ground truth is another's, and a unit you cannot brief distinctly is not a seat. ⚠️ **NEVER solo, never two — the FLOOR is three seats.** A surface with fewer than three units still dispatches THREE seats on DIFFERENT angles over it: measured, not assumed (1 seat found 0; 3 over the same surface found 0/5/0, and the 5 held a real fail-open two self-sweeps had read past). Those three are the same technique `/fabrik-review-scoped` § floor measures — duplicate briefs buy sampling variance, not coverage, so they are budgeted as ONE unit. **THE DISPATCH STEP (D-191, binding on every fan-out):** run `python3 /opt/fabrik/scripts/sysadmin/dispatch_headroom.py --units <N> [--heavy] [--risky <R>] [--mechanical <M>]` and dispatch **exactly the `SEATS:` and the mix it prints, all in ONE message** (when the COST line says TRIMMED, the trimmed mix — never the per-unit sentence), each seat a distinct unit × angle brief with its model token (`model: "sonnet"|"haiku"|"opus"`). The number it prints is min(units × angles + the Opus seat(s) · the CLI cap of 20 · box capacity — every seat's tools load the box, `--heavy` for pytest/build/render seats — minus seats live in sibling sessions · quota headroom), trimmed for maximum unit coverage when a cap binds (Haiku, then the extra Opus seats, then Sonnet); the floor never overrides a hard cap (D-189). **Record the dispatch BEFORE you send it:** `python3 scripts/command_run.py dispatch --seats <n>` — that stamp is what sibling sessions subtract for 15 minutes (a `round --seats` written at the round's close reserved nothing while the seats ran); then close the round with `python3 scripts/command_run.py round --seats <n> --findings <n> --classes-swept … --classes-new …` — the tripwire on D-186 is blind without it. Model by ROLE, and the MECHANISM is the one-token per-dispatch override `Agent(subagent_type=…, model="opus"|"sonnet"|"haiku"|"fable")` — the agent definitions default to Sonnet or inherit, so a role without the token is Sonnet: Fable orchestrates/adjudicates (METERED usage credits, not subscription quota — a Fable seat that refuses falls back to Opus by name, recorded), Opus is the authoritative pass (`model: "opus"`), Sonnet is breadth, Haiku is every unit's mechanical seat (grep-able classes, format, inventory). Price (D-190): haiku 1× · sonnet 2× · opus 5× · fable 10× — cost = Σ seats × multiplier, the script prints it; breadth on Opus is a 2.5× overspend. Dispatch them together and keep working while they run — blocking on one seat is where the wall-clock goes (D-186). The unit count is what the SURFACE HAS, so the rule scales itself and is not a quota to spend: a one-file diff yields one or two units (3–5 seats; a judgement surface pads to the floor of three), not eight, and only the authoritative seat is Opus — the cost is bounded by the UNIT count, not by how idle the box looks.{{FLOOR}} The pool contract is kept below, commented, so re-enabling is an uncomment, not a rewrite.

<!-- POOL OFF (D-181, 2026-09-07) — the pool-default contract, kept verbatim for re-enable; restore by uncommenting when the operator turns the credential back on:
Per `62-using-subagents.md` § Dispatch policy (the canonical detail), this command's gradeable fan-out defaults to the **OpenRouter pool** via `fanout({{TASK_TYPE}}, units, repo=REPO, project="{{PROJECT}}", …)` — the hand-picked, flywheel-scored roster (family-diverse; no default price cap). `fanout` **auto-records each unit UNSCORED** at dispatch → after you merge + refute, **back-fill your verdict** with `set_quality(r.agent_id, score, project="{{PROJECT}}", task_type={{TASK_TYPE}}, model=r.model)` — a row left unscored teaches the flywheel nothing. ⚠️ Never hand-roll `run_agents`+`record_run` — it no-ops. `mode="read_only"` = single-shot + inline content (`web_tools`/`mcp_servers` still callable); `mode="write"` = real file reads with **disjoint `owned_paths`** per unit or it serializes (`62` § Parallelism). ⚠️ **Secret-material content never enters a pool unit's task text** (`.env`/`.env.*` VALUES — `.env.example` is exempt, names only — `secrets/`, key/cert material, live credentials): inlining ships the literal secret to a third-party API; a unit whose surface is secret-bearing runs NATIVE instead, and a dispatch that would be entirely secret-material declares `NO-POOL: secrets-only surface` (the corpus-wide carve-out family; write-mode workers read committed HEAD — which is exactly where a LEAKED secret lives, so the carve-out binds BOTH modes). Guard the import: `try: from libs.subagents import fanout, set_quality` / `except ImportError: fanout = None`. Native subagents produce no `AgentResult` → they record nothing (by nature); `scripts/enforcement/check_subagent_flywheel.py` WARNs on an unrecorded pool run.{{FLOOR}}{{EXTRA}} **A STANDING operator/plan NO-POOL directive supersedes this pool-default as POLICY** — run native-only and carry the `NO-POOL: <reason>` declaration **in an in-cycle COMMIT MESSAGE (or set `FABRIK_NO_POOL`)** — that is where `scripts/enforcement/check_subagent_flywheel.py` reads the waiver; a declaration only in chat output is invisible to it; that is distinct from the transient pool-exhaustion exception and needs no apology per run.
-->

⚠️ **PIN the subject before you dispatch, or the pass is not reproducible.** A read-only finder cannot pin what it reads, and the dispatching session keeps editing — the two obligations are in tension by construction ("converge in THIS invocation" keeps the author editing; "the author's own re-read never counts" requires a concurrent independent reader). Measured twice in two days: a finder watched its subject's md5 change mid-review and had to reclassify four findings as *superseded by the edit* (`01M1KN0H6JRPXVFKH911425K2Y`), and an author-blind reviewer watched a spine go 320 → 514 → 552 lines and a ticket split underneath it — briefed on 8 tickets, it graded 9 (`01M1KYMG27FMEQXA89A2A4B2ZD`). Both survived only because the finder SAID so. So: **`cp` the artifact to a stable scratch path at dispatch (`<scratchpad>/<artifact>.pass<N>.md`) and brief finders on THAT path**, carry its **md5 in the brief**, and require the finder to **state the hash it actually read**. A hash mismatch is a re-dispatch, not a judgement call; a finding against an unpinned subject is dismissible as stale, which is the same as not having run the pass. Record the snapshot md5 in the pass-ledger row.

<!-- POOL OFF (D-181) — pool-grounder and flywheel-sink notes, kept for re-enable:
**A single-shot pool grounder fetches only when TOLD to.** `read_only` + `web_tools` advertises the tool schemas, but a unit that is not instructed to call them asserts from memory and returns dead URLs (measured: $0.03 spent, 38% dead URLs — 01M1GSR9, intel). Every research brief carries the line *"you MUST call web_search before asserting any vendor fact"* and enough `max_turns` for the round-trip; a cell whose unit never fetched is UNVERIFIED, never a fact.

**The flywheel sink needs `psycopg` (v3) in the interpreter that dispatches.** `libs/subagents/pg_ledger.py` imports `psycopg`; without it `record_agent_run`/`set_quality` return cleanly and write NOTHING (brand-identity-creator 2026-09-02: 569 runs unrecordable, zero errors at the call site — 01M1H9YH; a project shipping `psycopg2-binary` has the wrong driver — 01M1H7ZQ). Before the first pool dispatch: `python -c "import psycopg"` in the venv you run from; missing ⇒ `uv pip install 'psycopg[binary]'` (dev dependency — the ledger is a dev-time surface) and say so in the run. `check_subagent_flywheel.py` names this exactly; read it before dispatching.
-->
