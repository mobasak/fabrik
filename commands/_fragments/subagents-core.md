## Subagents — {{HEADLINE}}

Per `62-using-subagents.md` § Dispatch policy (the canonical detail), this command's gradeable fan-out defaults to the **OpenRouter pool** via `fanout({{TASK_TYPE}}, units, repo=REPO, project="{{PROJECT}}", …)` — the hand-picked, flywheel-scored roster (family-diverse; no default price cap). `fanout` **auto-records each unit UNSCORED** at dispatch → after you merge + refute, **back-fill your verdict** with `set_quality(r.agent_id, score, project="{{PROJECT}}", task_type={{TASK_TYPE}}, model=r.model)` — a row left unscored teaches the flywheel nothing. ⚠️ Never hand-roll `run_agents`+`record_run` — it no-ops. `mode="read_only"` = single-shot + inline content (`web_tools`/`mcp_servers` still callable); `mode="write"` = real file reads with **disjoint `owned_paths`** per unit or it serializes (`62` § Parallelism). ⚠️ **Secret-material content never enters a pool unit's task text** (`.env`/`.env.*` VALUES — `.env.example` is exempt, names only — `secrets/`, key/cert material, live credentials): inlining ships the literal secret to a third-party API; a unit whose surface is secret-bearing runs NATIVE instead, and a dispatch that would be entirely secret-material declares `NO-POOL: secrets-only surface` (the corpus-wide carve-out family; write-mode workers read committed HEAD — which is exactly where a LEAKED secret lives, so the carve-out binds BOTH modes). Guard the import: `try: from libs.subagents import fanout, set_quality` / `except ImportError: fanout = None`. Native subagents produce no `AgentResult` → they record nothing (by nature); `scripts/enforcement/check_subagent_flywheel.py` WARNs on an unrecorded pool run.{{FLOOR}}{{EXTRA}} **A STANDING operator/plan NO-POOL directive supersedes this pool-default as POLICY** — run native-only and carry the `NO-POOL: <reason>` declaration **in an in-cycle COMMIT MESSAGE (or set `FABRIK_NO_POOL`)** — that is where `scripts/enforcement/check_subagent_flywheel.py` reads the waiver; a declaration only in chat output is invisible to it; that is distinct from the transient pool-exhaustion exception and needs no apology per run.

**A single-shot pool grounder fetches only when TOLD to.** `read_only` + `web_tools` advertises the tool schemas, but a unit that is not instructed to call them asserts from memory and returns dead URLs (measured: $0.03 spent, 38% dead URLs — 01M1GSR9, intel). Every research brief carries the line *"you MUST call web_search before asserting any vendor fact"* and enough `max_turns` for the round-trip; a cell whose unit never fetched is UNVERIFIED, never a fact.

**The flywheel sink needs `psycopg` (v3) in the interpreter that dispatches.** `libs/subagents/pg_ledger.py` imports `psycopg`; without it `record_agent_run`/`set_quality` return cleanly and write NOTHING (brand-identity-creator 2026-09-02: 569 runs unrecordable, zero errors at the call site — 01M1H9YH; a project shipping `psycopg2-binary` has the wrong driver — 01M1H7ZQ). Before the first pool dispatch: `python -c "import psycopg"` in the venv you run from; missing ⇒ `uv pip install 'psycopg[binary]'` (dev dependency — the ledger is a dev-time surface) and say so in the run. `check_subagent_flywheel.py` names this exactly; read it before dispatching.
