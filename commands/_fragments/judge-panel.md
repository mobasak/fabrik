**A judge panel ranks the approaches, never the lead alone (D-363).** Before you recommend, dispatch THREE
`general-purpose` seats on Sonnet in ONE message, each handed the approaches WITHOUT your recommendation — in
alphabetical order at equal depth, no `recommended` marker; you form your recommendation only after the panel returns — the
grounding they rest on (anchored `path:line` and cited URLs) and the scoring rules of this phase; each returns,
per approach, a verdict per rule with its one-line reason and the approach it ranks first. You adjudicate: a
recommendation the panel's majority does not rank first states why in the artifact, and every split verdict is
carried to the operator's approval as an open question. Stamp the three seats (`command_run.py dispatch --seats
3`). Cobra: a panel handed your recommendation agrees with it — the brief never names it.
