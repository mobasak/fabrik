id: test-static-site
template: static-site
kind: service
domain: test-static-site.example.com

secrets:
  from_env:
    - NAMECHEAP_API_KEY
    - CLOUDFLARE_API_TOKEN
  from_file:
    GOOGLE_SERVICE_ACCOUNT_CREDENTIALS_JSON: /tmp/test-static-site-creds.json

env:
  LOG_LEVEL: INFO
