id: test-desktop-app
template: desktop-app
kind: service
domain: test-desktop-app.example.com

secrets:
  from_env:
    - NAMECHEAP_API_KEY
    - CLOUDFLARE_API_TOKEN
  from_file:
    GOOGLE_SERVICE_ACCOUNT_CREDENTIALS_JSON: /tmp/test-desktop-app-creds.json

env:
  LOG_LEVEL: INFO
