id: test-saas-skeleton
template: saas-skeleton
kind: service
domain: test-saas-skeleton.example.com

secrets:
  from_env:
    - NAMECHEAP_API_KEY
    - CLOUDFLARE_API_TOKEN
  from_file:
    GOOGLE_SERVICE_ACCOUNT_CREDENTIALS_JSON: /tmp/test-saas-skeleton-creds.json

env:
  LOG_LEVEL: INFO
