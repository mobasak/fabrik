# Required environment variables

# DATABASE_URL=postgresql://postgres:password@localhost:5432/main
# Substituted into the DATABASE_URL fallback in compose.yaml, so it is REQUIRED
# whenever DATABASE_URL itself is not set — an unset value yields an empty password.
POSTGRES_PASSWORD=
API_KEY=
# Application environment variables
LOG_LEVEL=INFO
DEBUG=false
