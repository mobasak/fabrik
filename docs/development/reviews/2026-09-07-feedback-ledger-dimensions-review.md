# Feedback ledger dimensions + per-run tokens (61b0e8ac, 71314d47) — review

**Status:** CONVERGED
**Surface:** final HEAD 2356fc6110b606355dd05d87be1b60efcebe4caa; range 29943db2..2356fc61 (the two reviewed commits 61b0e8ac + 71314d47 plus the in-review fix commits — 42 commits in the range); `git diff 29943db2..2356fc61 -- <the 11 surface paths> | md5sum` = 02c0fa94402b450ebc5ef2db441d4c09. The prior receipt (2026-09-07-command-feedback-review.md) anchored a HEAD hash of an uncommitted diff — not comparable to a committed range, so pass 1 ran WIDE.
**Command:** /fabrik-review · **Changed:** `scripts/command_run.py`, `scripts/command_feedback_report.py`, `tests/test_command_feedback.py`, `tests/test_command_feedback_report.py`, `tests/test_command_run.py`, `commands/_fragments/run-record.md`, `commands/_fragments/close-feedback.md`, `commands/_sources/fabrik-review.md`, `commands/_sources/fabrik-docs-review.md`, `commands/_sources/fabrik-execute-plan.md`, `docs/reference/command-run-protocol.md`

## Coverage Checklist

Rubric invocation (verbatim output — the gate reads the generated header, never a prose mention):

```text
$ python scripts/review_rubric.py --changed scripts/command_run.py scripts/command_feedback_report.py tests/test_command_feedback.py tests/test_command_feedback_report.py tests/test_command_run.py commands/_fragments/run-record.md commands/_fragments/close-feedback.md commands/_sources/fabrik-review.md commands/_sources/fabrik-docs-review.md commands/_sources/fabrik-execute-plan.md docs/reference/command-run-protocol.md
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
- An approval link opened somewhere the user did not start must never mint a session silently.
- > **Fail-closed invariant (hard, every mode).** `auth.uid()` and `current_tenant_id()` MUST return `NULL` (→ the policy denies) on unset, empty, or malformed claims — wrap the body in `EXCEPTION WHEN OTHERS THEN RETURN NULL`. **Never** raise and never default to a value: an error-open helper turns one bad/empty JWT into a full cross-tenant read. This is the single most security-critical line in the build — verify it explicitly with a no-context probe (`SELECT auth.uid()` → `NULL`).
- The JWT signing secret must be at least 256 bits, generated via `openssl rand -hex 32`, and injected via Pydantic Settings. Never hardcode it.
- **Pin the algorithm in the VERIFIER** — pass an explicit allow-list (`algorithms=["HS256"]`), never let the library dispatch on the token header's `alg`. Header-driven dispatch is the classic confusion attack (an RS256 public key replayed as an HS256 HMAC secret); `alg: none` is rejected unconditionally.
- "Sticky sessions are a violation of twelve-factor and should never be used or relied upon."
- => Mandate: processes are stateless/share-nothing. **STICKY SESSIONS ARE BANNED** (not just file-based sessions). Session state goes to `redis-main` (Redis) with a TTL. Never in-process memory, never on local disk. Any design that assumes "the same user hits the same process" is a violation.
- **Pattern B (legacy / migration-only):** The Supabase client SDK handles token storage. On mobile, wrap with `expo-secure-store` (never AsyncStorage or MMKV for tokens). See `80-mobile.md` § Backend Integration.
- **Both patterns:** Never store JWTs in `localStorage` or `sessionStorage` on web. Never store JWTs in AsyncStorage or MMKV on mobile.
- **Chrome Extension (MV3) specifics:** `chrome.storage.session` defaults to `TRUSTED_CONTEXTS`, so **content scripts cannot read the token** — keep it in the SW / extension-page context and have content scripts fetch it via SW-mediated messaging (`chrome.runtime.sendMessage`), not a direct read. For social login use `chrome.identity.launchWebAuthFlow` with **PKCE** (`code_verifier` via `crypto.subtle`, held in `storage.session`, redirect `https://<ext-id>.chromiumapp.org/`); the **backend** does the code-for-token exchange. **Never a heavy browser auth SDK** (Auth0-SPA-JS, `oidc-client-ts`) — they assume DOM/`localStorage`/iframes and break in the service worker. Pin a manifest `key` so the extension ID (and thus the `chrome-extension://<id>` CORS origin) is stable across machines. Full detail: `chrome-ext/70-chrome-ext.md`.
- **Never rely solely on the framework's request-shaping layer for access control.** CVE-2025-29927 (the `x-middleware-subrequest` bypass) proved COMPLETE middleware bypass via one crafted header; it is long patched upstream, but the rule outlives the patch — current Next.js even RENAMED the file to say so: `middleware.ts` became **`proxy.ts`**, explicitly repositioned as request-shaping, not a security boundary. ⚠️ **On current majors a leftover `middleware.ts` is SILENTLY IGNORED at build** — nonce injection and redirects stop executing with no error; rename it when upgrading.
- `CORSMiddleware` in FastAPI must populate `allow_origins` from environment variables (Pydantic Settings). Never hardcode origins.
- `X-Frame-Options: DENY` — kept as the legacy fallback only; formally obsoleted by `frame-ancestors`, never ship it ALONE
**Never** write inline `APIKeyHeader` / `require_api_key`. **Never** use per-service key names (`SERVICE_API_KEY`, `PROXY_API_KEY`). Scaffold `python-api` auto-emits `internal_auth.py`, `metrics.py` (REQUEST_COUNT / ERROR_COUNT / ACTIVE_JOBS / PROCESSING_COUNT), `/metrics` endpoint (Authelia-bypassed), and `SERVICE_INTERNAL_SECRET_KEY` in `.env.example`.
- => Mandate: config via env vars only (`os.getenv("KEY", "default")`); **ZERO secrets/constants in code**. Apply the open-source litmus test to every change. **BANNED**: grouped/named env config sets (e.g. a `config/production.yml` or a `settings.production` group) — env vars are granular and orthogonal, set per deploy. (The pack already covers secret handling — cross-reference existing secret patterns and extend with config orthogonality.)
- [ ] Mobile tokens stored in `expo-secure-store` — never AsyncStorage or MMKV.
- > **⚠️ Bearer bypass scope — security-critical.** The bypass defaults to `^/api/`, which makes the **entire** `/api/*` surface public (un-2FA'd). If the application authenticates only a **sub-prefix** (e.g. `/api/v1` carries the bearer/internal-token check) while OTHER `/api/*` routes are unauthenticated (legacy / admin / destructive), you **MUST** narrow the bypass with `shape.bearer_bypass_prefix: "^/api/v1"` — otherwise `fabrik apply` exposes those routes to the public internet. **Bypass ONLY the path the app itself authenticates.** Value must start with `^/`; the verifier (`orchestrator/verifier.check_api_bypass`) probes the configured prefix on deploy. When unsure whether a service has un-auth'd `/api/*` routes, ask the app owner before relying on the `^/api/` default.

### core/25-data-postgres.md
| Vector search | pgvector on `postgres-main` + `fabrik-lib/rag` — ⚠️ the extension is NOT currently installed there (probed 2026-09-01: `postgres:16-alpine`, `plpgsql` only); a project needing vectors REQUESTS the fleet infra change first, never assumes it | same `postgres-main` DSN |
**"Own database" means a DATABASE on `postgres-main`, never a database SERVER.** Per-project and per-tenant isolation is a separate database (its own name, its own role) on the shared container — isolation, quota and backup are all satisfied at that grain. A dedicated Postgres instance is a decision, not a default: it needs its own `docs/DECISIONS.md` row naming what the shared server cannot serve (web-ecommerce-factory 01M1Q8X9, 2026-09-05: "one DB per store" read naively as one server per customer).
- Use Pydantic `BaseSettings` (per `10-python.md` § Config Loading) — never raw `os.getenv` **for an APPLICATION's settings surface**:
- ⚠️ **Scope, stated here because this LINE is what `review_rubric.py` injects — without its section.** The rubric FLOOR-injects this mandate *and* `35-security-auth`'s "config via env vars only (`os.getenv("KEY", "default")`)" into every finder prompt on every review, so a finder reading both literally has two rules it cannot both satisfy, and files a false positive on whichever it applies. The carve-out: `BaseSettings` governs a SERVICE's config surface (a `Settings` object, DB/Redis DSNs, secrets). A **vendored fabrik-lib module** has no settings object by design — it reads its own knobs with bare `os.getenv("KEY", "default")`, which is `35-security-auth`'s mandate being satisfied, not this … (wrapped further — read the pack)
- Never blindly trust `--autogenerate`. Always review `upgrade()` and `downgrade()` for unintended column drops, rename misinterpretations, and ENUM alterations before committing.
- > **Older pythons only** (services pinned below stdlib-uuid7 — which today includes SCAFFOLDED services: the scaffold still emits an older interpreter and ships `uuid-utils`; alignment tracked in the backlog): import `uuid7` from `uuid_utils.compat`, never `uuid_utils.uuid7()` directly — the latter returns `uuid_utils.UUID`, which asyncpg rejects (not a stdlib `uuid.UUID`). **DB-side:** newer PostgreSQL majors ship native `uuidv7()` (probe: `SELECT uuidv7()`); prefer `DEFAULT uuidv7()` at schema level where it exists. `postgres-main` currently runs major <!--v:postgres_major-->16<!--/v-->, which predates it — generate app-side on the fleet.
- Foreign keys must declare `ON DELETE` behaviour explicitly — `CASCADE` if children cannot exist without the parent, `RESTRICT` to protect audit trails. Never rely on the implicit default.
- This section owns the **canonical** engine, session, and `get_db`. `10-python.md` imports from here — never redefines its own.
- Database `AsyncSession` must be scoped to the route handler via `Depends()`. Never open sessions or transactions in global middleware — this holds connections during serialisation and I/O, exhausting the pool.
**BANNED as a server-side backing service** (dev, test, and prod alike):
**⚠️ SCOPE — this ban is about BACKING SERVICES, not client-local storage.** It does **NOT** apply to:
- **`desktop-app`** — SQLite is the **mandated** engine there (`desktop-app/72-desktop.md` § Local Persistence: `better-sqlite3` + SQLCipher; *"Production builds MUST encrypt the local SQLite file"*).
**12-Factor IV (Backing Services) — generalised:** swapping ANY attached backing service (DB, cache, object storage) is a **config change, never a code change**. The handle lives in `DATABASE_URL` / `REDIS_URL` / storage env — the code *reads* it, the code does not *decide* it. Never `if ENV == "prod":` branching to pick a host. (See § PostgreSQL Host Selection, which already mandates this for the DB.)
- [ ] All primary keys use UUIDv7 — stdlib `uuid.uuid7` on current Python (older pythons: `uuid_utils.compat.uuid7`, never direct `uuid_utils.uuid7()`); no `uuid4()`.

### core/30-ops.md
- the pinned release leaves full security support, never per-pack.
- All services deploy via `fabrik apply` (SSH + Docker Compose) on the `fabrik` network. Traefik routes external traffic — services do NOT bind host ports.
- **No `ports:` section.** All external traffic routes through Traefik. Never bind host ports. See Docker Port Security below. **12‑Factor VII (Port binding):** "the app is self‑contained and exports HTTP by binding to a port; it does not rely on runtime injection of a webserver" — which is exactly WHY no host `ports:`.
- **`container_name: <name>` is mandatory.** Same `_validate_compose()` gate refuses any service without it. Stable names are required so Gatus endpoints, inter-service URLs, and `docker exec`/`docker inspect` keys don't drift per redeploy. Use the bare service name (`browserless`, `gotenberg`, `meilisearch`, `glitchtip-web`, `site-provisioner`, etc.) — never UUID-suffixed names.
- gets one (ruling D-052) — see `core/60-watchdog.md`. Do not author a `watchdog: { enabled: false }` opt-out; if a project genuinely cannot host the sidecar, that is a ruling to obtain, not a default to flip.
- path before the flag goes in the spec, and assert target health (`/api/v1/targets` → `up`), never a bare `curl` of a path you assumed.
- VOLUME gets a plan pointed at a directory that never exists — a paper backup that reads green and archives nothing. *Measured: `/opt/zitadel/data` is absent while the `zitadel-data` plan points at it.* If the data is a volume, say so in the spec comment and rely on the global `docker-volumes` plan; never let a service-named plan be mistaken for the protection.
- health-enabled service can NEVER pass `up -d --wait` on a fresh database, and the deploy hangs to timeout. *Measured: trytond bakes an init script but sets no `command:`, and its base entrypoint is a bare `exec "$@"`.* An init the deploy cannot perform itself is a runbook step the plan MUST own.
- `fabrik redeploy <app>` SSHes to the VPS and runs `git pull` + `docker compose up -d --wait` against the **GitHub remote**, NOT the local `/opt/<app>` clone. Skipping `git push` redeploys the previous remote commit — the VPS never sees local changes.
**Mandate:** build → release → run are strictly separated. Releases are IMMUTABLE; the git SHA is the release ID. NEVER hot‑patch a running container (no `docker exec` to edit code/config in place, no in‑place code mutation on the VPS). Any change = a new build + a new release via `fabrik apply` / `fabrik redeploy`.
- Runtime database migrations that modify the app container (migrations MUST be run as separate deploy‑time steps)
**Place a service next to its data.** A spoke-hosted service reaches `postgres-main`/`redis-main` over the WireGuard mesh, and that hop is cross-Atlantic (Coventry ↔ LA) on EVERY query — a per-request chatty service pays it hundreds of times per page. So a DB-chatty service targets vps1; a spoke earns a service whose data traffic is light, batched or cached; a service PINNED to a spoke by hardware (GPU) batches or caches its data access — the data never moves off vps1. Measure before choosing (`ping 10.99.0.1` from the spoke, and the request's query count), never assume — the correctness rule ("container DNS, never localhost") says nothing about latency (web-ecommerce-factory 01M1Q8X9, 2026-09-05).
**Mandate:** WSL dev and the VPS run the SAME backing services (PostgreSQL + Redis), same major version. NEVER substitute a different backing service in dev (no SQLite standing in for Postgres, no in‑memory dict standing in for Redis). The same code must run unmodified in both environments.
- WSL runs PostgreSQL + Redis at the SAME MAJOR as the VPS containers — probe the live truth, never copy a tag from a doc: `ssh vps "sudo docker inspect postgres-main redis-main --format '{{.Config.Image}}'"` (2026-09-01: `postgres:16-alpine` · `redis:7-alpine` — upstream official images, outside OUR-image Alpine ban per § Banned Patterns)
**Invariant:** Never use `ports:` in compose.yaml to expose internal services to the host. All external traffic must go through Traefik.
**Health endpoints (`/health`, `/healthz`, `/metrics`, `/api/health`) bypass Authelia on all services** — required for Gatus and Prometheus monitoring. The bypass is **resource-based, not domain-bound** — applies on every domain routed through Authelia (hub direct + spokes via `authelia-vps1@file` middleware). Never protect these paths.
**CRITICAL:** Use `web`/`websecure` in Traefik labels — never `http`/`https` (those entrypoints do not exist). The scaffolder emits the correct entrypoint names; if you hand-write labels, match these exactly.
**Mandate:** migrations and admin tasks run as a ONE‑OFF process against the DEPLOYED image + env — identical environment to regular processes. NEVER run admin tasks from a laptop against prod, NEVER via `docker exec` into a live container, and **ABSOLUTELY NEVER auto-run migrations from app startup/`lifespan`** (concurrent replicas race the Alembic version table → wedged deploy).
- > sees a file that looks exactly like a migration step, and ships a deploy where migrations never run —
- > the rule producing the very defect it exists to prevent. Do not re-add either without a `path:line` in
**Processes are share-nothing:** any state shared across requests MUST go to Redis (`redis-main`) with a TTL. A project using Redis for sessions MUST declare `shape.needs_cache: true` in `specs/services/<id>.yaml`, or `fabrik apply` skips the Redis registrar and the deploy is silently broken.
- "A twelve-factor app never relies on implicit existence of system-wide packages"
**Mandate:** any binary the app shells out to (ffmpeg, yt-dlp, poppler, tesseract…) MUST be `apt-get install`-ed in the Dockerfile, with a `shutil.which()` startup probe that fails fast. **The pinned base image is the version boundary** — exact `=version` apt pins are banned: they break on every Debian point release as old debs leave the mirrors (the "works then mysteriously breaks" class this section exists to prevent); the codename pin + image digest give the reproducibility. Never assume `curl`/ImageMagick/ffmpeg exist in the image — they don't by default.

### 12-FACTOR (all twelve axes)
- I codebase: shared code → fabrik-lib, never two apps in one repo
- II deps: every shelled-out binary installed + pinned in the Dockerfile
- III config: granular env vars; no secrets in code; no grouped env sets
- IV backing services: swappable by DSN/config change only
- V build/release/run: releases immutable; never hot-patch a container
- VI processes: stateless; session state → redis-main; no sticky sessions
- VII port binding: bind in-container; Traefik routes; no host ports:
- VIII concurrency: scale out; never daemonize or write PID files
- IX disposability: SIGTERM returns in-flight jobs to the queue; jobs idempotent
- X dev/prod parity: same backing services everywhere; no SQLite-for-Postgres
- XI logs: unbuffered stdout only; the app never writes/rotates a logfile
- XII admin: migrations/one-offs run against the deployed release, never startup

## MATCHED — packs whose globs hit the changed paths

### core/10-python.md  (hit: scripts/command_feedback_report.py, scripts/command_run.py, tests/test_command_feedback.py)
**`uv`** is the mandated Python package manager. Never use raw `pip`, `pip install`, `poetry`, or `pipenv`.
- Dependencies live in `pyproject.toml` + `uv.lock`. Do not modify these files unless the ticket authorises it.
- its own reviewed commit, never as a side effect of unrelated work.
- The one RULE: use SQLAlchemy async consistently — never mix `async def` with sync `.query().all()` (the Banned table row; the full session pattern is `25-data-postgres.md`'s).
- The canonical `engine`, `async_session`, and `get_db` are defined in `src/database.py` — owned by `25-data-postgres.md`. Import from there, never redefine:
**Config convention:** apps read a complete `DATABASE_URL` (`postgresql+asyncpg://user:pass@host:port/db`) and `REDIS_URL` from env. Discrete `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD` for the app to assemble are **banned**. The env supplies the complete URL — `localhost` in WSL, `postgres-main` on VPS — so the host concern is an env-layer responsibility, never code logic. See `30-ops.md` compose template for how discrete vars are interpolated into `DATABASE_URL` at the compose level.
- volume** (`30-ops.md` § Volumes), never in `.tmp` and never in `/tmp`.
**GlitchTip discipline:** unhandled exceptions (FastAPI 500s) are auto-captured by GlitchTip with full stacktraces. In the `except Exception` branch, log a **short event name + correlation_id** — never `logger.exception()` (that duplicates the traceback in Loki AND GlitchTip). See `55-observability.md` § Error Reporting for the full rule.
**Note:** Use the scaffolded logger: `from {package}.logger import get_logger` (see `55-observability.md` § Pre-Scaffolded Logging). Do not use `structlog.get_logger()` directly or `logging.getLogger(__name__)`.
- **Never a bare `asyncio.create_task()`** — an unreferenced task is silently garbage-collected and its exceptions vanish. Hold the reference and await it, or use `asyncio.TaskGroup`.
- **`datetime.now(UTC)`, never `datetime.utcnow()`** — deprecated and naive; naive datetimes are a real cross-service defect class.
- Ruff's selected rule-sets MUST include `ASYNC` (blocking IO in async code — machine-enforces this pack's hardest-to-review rule), `B` (bugbear) and `S` (bandit) alongside the defaults; configured in `pyproject.toml`, emitted by the scaffolder.
- Production services run via `uvicorn` CLI in the Dockerfile, not `uvicorn.run()` in code. Base image is always the pinned Debian `-slim` variant on `linux/amd64` (the variant is pinned fleet-wide in `30-ops.md` § Container Base Images — change it THERE, never per-repo). Never use Alpine — musllinux wheels exist now (PEP 656) but coverage is still partial, source builds are dramatically slower, and musl's allocator/stack defaults degrade CPython; the trade never pays on this fleet.
- `uvicorn.run()` is for local development only. Never ship it in production code.
- a fleet scaling decision (more containers), never a per-app flag.
**BANNED: grouped/named env config sets.** 12F is explicit — *"env vars are granular controls, each fully orthogonal to other env vars"* — so a `config/production.yml`, a `settings.production` group, or a `config/{dev,staging,prod}.yaml` tree is a violation. Env vars are granular and set **per deploy**, never batched into a named "environment".
**BANNED:** `logging.FileHandler`, `logging.handlers.RotatingFileHandler`, `TimedRotatingFileHandler`, `loguru` file sinks, any `*.log` file write, any in-app log rotation/retention/cleanup. The app never decides where logs are stored or routed — Docker → Promtail → Loki does. Full rule: `55-observability.md` § Logs.
**Factor XII — Admin processes. NEVER migrate from app startup.**
**BANNED: `alembic upgrade head` in FastAPI's `lifespan`, in an `@app.on_event("startup")`, or as an import side-effect.** With more than one replica (or a restart storm) two containers run `upgrade head` **concurrently** → they race the Alembic version table → duplicate DDL → **wedged deploy**. Migrations are a **one-off admin process against the deployed release**: `docker compose run --rm <svc> alembic upgrade head` (see `30-ops.md` § Release & Admin Processes).

### core/40-documentation.md  (hit: commands/_fragments/close-feedback.md, commands/_fragments/run-record.md, commands/_sources/fabrik-docs-review.md)
- > **⚠️ `docs/OPERATIONS.md` + `docs/DEPLOYMENT.md` are FLEET-AI INTERFACES, not just docs (D-065).**
- **Tier-1 (cheap-pool author → verify → converge):** for each **mechanically-detectable** doc whose Doc-Sync trigger fired (`docs/QUICKSTART.md` · `docs/CONFIGURATION.md` · `docs/data-contract.md` · `docs/SERVICES.md` · `docs/OPERATIONS.md` — the reliable-signal subset), `scripts/doc_reconcile.py` dispatches a cheap OpenRouter-pool author (`libs.subagents`, `pick_models("docs")`) to emit a **minimal structured patch**, **verifies it before applying** (a symbol cross-check catches invented endpoints; the orchestrator injects a higher-assurance native-Claude verify), and loops to a zero-edit round. Runs per phase in `/fabrik-execute-plan`; never blocks (fail-safe). The other docs (CHANGELOG, INDEX, FEATURES, RESILIENCE, PORTS, the READMEs, `db/schema.sql`, …) have no reliable mechanical content-signal → they rely on the touch-on-change backstop below + your own edit (force-update, not force-correct).
- The SSOT is the type-aware registry (`scripts/enforcement/_doc_registry.py::PROJECT_DOCS`) — this table is its project-facing rendering, kept in step, never a second truth. `/fabrik-plan-after-chat` (the plan set's spine + tickets — the ticket-format authority) injects these rows per ticket as its `Docs:` line.
- Standalone work (not plan execution) → `Agent-Role: primary`. Trailers go below a blank line, above `Co-Authored-By`. ⚠️ The trailer block must be its OWN paragraph with NO blank line inside it: git parses only the LAST paragraph, and only if it is all-trailers. A blank line before `Co-Authored-By:` demotes everything above it to prose; so does a prose line glued to the top of the block. Measured 2026-08-15: 200 of the last 200 hub commits carried `Agent-Role:` and only 10 parsed, because the old example here shipped the blank line.
- **⚠️ Link it or it is decoration.** *Measured:* requests for files that do NOT exist came ~zero from AI bots — agents never go looking. It follows (inference, not measurement) that a file only gets read when something points at it: reference it from the docs index or README.
- ⚠️ **In THIS repo `llms.txt` is GENERATED** (`scripts/generate_capability_index.py`, refreshed daily) — never hand-edit it; change the generator. A project writing one by hand owns it.
- either way. Cheap and reversible — never at the expense of `OPERATIONS.md`/`DEPLOYMENT.md`, which are the load-bearing agent interfaces (D-065).
- **No skipped heading levels** — `##` to `###`, never `##` to `####`
- **Fenced code blocks only** — never indented code (AI treats it inconsistently)

### core/45-testing-strategy.md  (hit: tests/test_command_feedback.py, tests/test_command_feedback_report.py, tests/test_command_run.py)
- **Behavior Contract**: every ticket enumerates its distinct **user-observable behaviors / acceptance criteria** and tests **each one** — one high-value integration/E2E test per behavior, risk-ordered, TDD for the risky ones. Skip trivia (getters / framework glue / config): **lean-but-complete, NOT 100%-line-coverage dogma**. Do not chase line coverage — ensure every behavior has a test that would fail if that behavior regressed. (Cheap pool subagents can author the per-behavior tests — the suggest→curate→author→fix workflow in `62-using-subagents.md` § Dispatch policy + `~/.claude/commands/fabrik-review.md`.)
- **No cosmetic assertions**: never assert against CSS classes, Tailwind utility strings, pixel measurements, or snapshot hashes. Assert application state and user-visible outcomes only.
- **Watched-fail-first** (for tests this change adds or modifies; trivia stays skipped per the Behavior Contract): a non-trivial behavior's test proves something only if it has been SEEN RED — either write it first and watch it fail, or (after the fact) neuter the fix/feature, prove the test goes red, then RESTORE and re-run to green. The neutered state is never staged, committed, or left in the tree. A green test never seen red is unverified — a suite can pass with its guard deleted.
- **Run tests**: `uv run pytest tests/` (never bare `pytest` — Fabrik uses `uv`) — **when the project has a `pyproject.toml`/`uv.lock`**. A `requirements.txt`-only project (no manifest) runs `.venv/bin/python -m pytest tests/` — the manifest clause chooses the RUNNER, it never disarms the mandate to run the suite (web-ecommerce-factory 01M1QEY5, 2026-09-05: the clause read as "does not apply here"). ⚠️ **Gate this on the manifest, because this line is FLOOR-injected into finder prompts and a vendored fabrik-lib MODULE has neither by design**: the module recipe ships `requirements.txt` (`fabrik-lib/README.md` § Creating a Reference Implementation), so `uv run` cannot resolve it and `python3 -m pytest` is the only thing that works. Telling a finder the sole working … (wrapped further — read the pack)
- **Zero-mock database policy**: never mock SQLAlchemy, SQLModel, or database sessions. All backend tests execute against a real PostgreSQL instance.
- **`ASGITransport` never runs lifespan** — anything the app initializes at startup (scaffolded apps are lifespan-based) silently does not exist in tests; wrap with `asgi-lifespan`'s `LifespanManager` when a test needs startup state.
- Use `structlog` in test helpers if logging is needed — never `print()`. See `55-observability.md`.
- **Never stub a server action from Playwright** — the server is the E2E boundary; stubbing belongs in the unit lane where the action is a plain function.
- Run Playwright against the PRODUCTION build (`next build && next start`), never the dev server.
- All locators must be **semantic**: `page.getByRole('button', { name: /submit/i })`. Never use CSS selectors or XPath.
- Launch Playwright's **bundled Chromium** (`channel: 'chromium'`) — stable Chrome/Edge removed the `--load-extension` / `--disable-extensions-except` side-load flags (Chrome 137/139), so those args only work under bundled Chromium, never installed stable Chrome.
- Run `@axe-core/playwright` with **`bypassCSP: true`** (the non-relaxable extension CSP otherwise makes axe throw on `chrome-extension://` pages); keep `@axe-core/playwright` a **dev-dependency only** (MPL-2.0 — never bundled into the shipped artifact). Gate bundle size with `size-limit` **per surface** (popup / side-panel / content-script). Full loop: `chrome-ext/70-chrome-ext.md` § Testing & UI Verification.
- Keep the generated types committed and re-generate on schema changes (`uv run python -c "import json; from <package>.main import app; print(json.dumps(app.openapi()))" > openapi.json` — the scaffold emits `src/<package>/main.py`, never a flat `src/main.py`, so `src.main` imports nothing).
**BANNED in tests:**
| A test THIS change adds/modifies that was never seen red (no fail-first, no red-on-revert proof) | Watch it fail first, or neuter the change → prove red → restore → re-run green |
- [ ] Destructive DB tests call `require_throwaway(TEST_DATABASE_URL)` before connecting — never point them at a dev/shared DB.

# promote-to-check_*: 81 injected mandate(s) look deterministically greppable — their backtick literals, one line each (the full mandates are ABOVE, not repeated: re-emitting ~20 FLOOR lines verbatim doubled the rubric and got it skimmed — web-ecommerce-factory 01M1QEY5, 2026-09-05)
- `fabrik-lib/fastapi-user-auth` `DELETE … RETURNING` `jti` `agents-fabrik.md § Supabase`
- `chrome-extension` `chrome.identity.launchWebAuthFlow` `https://<ext-id>.chromiumapp.org/` `chrome.storage.session`
- `desktop-app` `safeStorage` `desktop-app/72-desktop.md`
- `fabrik` `X-Internal-Token` `internal_auth.py` `hmac.compare_digest` `APIKeyHeader`
- `auth.uid()` `current_tenant_id()` `NULL` `EXCEPTION WHEN OTHERS THEN RETURN NULL` `SELECT auth.uid()` `NULL`
- `openssl rand -hex 32`
- `algorithms=["HS256"]` `alg` `alg: none`
- `redis-main`
- `expo-secure-store` `80-mobile.md`
- `localStorage` `sessionStorage`
- `chrome.storage.session` `TRUSTED_CONTEXTS` `chrome.runtime.sendMessage` `chrome.identity.launchWebAuthFlow` `code_verifier` `crypto.subtle` `storage.session`
- `x-middleware-subrequest` `middleware.ts` `proxy.ts` `middleware.ts`
- `CORSMiddleware` `allow_origins`
- `X-Frame-Options: DENY` `frame-ancestors`
- `APIKeyHeader` `require_api_key` `SERVICE_API_KEY` `PROXY_API_KEY` `python-api` `internal_auth.py` `metrics.py` `/metrics` `SERVICE_INTERNAL_SECRET_KEY`
- `os.getenv("KEY", "default")` `config/production.yml` `settings.production`
- `expo-secure-store`
- `^/api/` `/api/*` `/api/v1` `/api/*` `shape.bearer_bypass_prefix: "^/api/v1"` `fabrik apply` `^/` `orchestrator/verifier.check_api_bypass` `/api/*` `^/api/`
- `postgres-main` `fabrik-lib/rag` `postgres:16-alpine` `plpgsql` `postgres-main`
- `postgres-main` `docs/DECISIONS.md`
```

| Class | Status |
|---|---|
| Hunt: `scripts/command_run.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (the transcript reader — no early stop, regex pre-filter, `tok_partial`, per-message maximum, isfinite + `except Exception`, newline-free bail; `_cost_usd` grammar + summed amounts + fractional `usd`; the hoisted late `--surface`; capped fields + one write(2); `input`/`output` labels — 180b38f6, tests in tests/test_command_feedback.py) |
| Hunt: `scripts/command_feedback_report.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (unreadable ledger ⇒ empty report; `--agent ""` declares its bound; done/blocked/handoff cell; models aggregated; population header; `_k` M step — 180b38f6, tests/test_command_feedback_report.py) |
| Hunt: `tests/test_command_feedback.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (the token fixture anchored on the persisted `started_epoch`; `_cr` never reads the developer's marker/transcript; the refused-surface ordering asserted in-process — the CLI-level test was vacuous under an executed mutation; +14 regression guards seen red first) |
| Hunt: `tests/test_command_feedback_report.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (+3 tests: unreadable ledger, agent-bound header + handoff cell, models + population line) |
| Hunt: `tests/test_command_run.py` — every changed hunk, its enclosing function, its callers | CLEAN (tests/test_command_run.py:967-985 — the record-shape pin grew three additive keys; no other file in the tree enumerates the record's keys: `set(rec)`/`rec.keys()` 0 hits in .claude/hooks/ + scripts/, final_gate_stop.py reads named keys only — Opus seat, grep + read) |
| Hunt: `commands/_fragments/run-record.md` — every changed hunk, its enclosing function, its callers | CLEAN (commands/_fragments/run-record.md:8-16 — the `--surface` line renders into all 36 commands; corpus `--check` OK, check_command_corpus green across 63 files) |
| Hunt: `commands/_fragments/close-feedback.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (commands/_fragments/close-feedback.md:18 — the FEEDBACK grammar now says `tokens <input> input / <output> output`; `<ctx> in` mislabelled a summed input as a context; rendered + --check OK) |
| Hunt: `commands/_sources/fabrik-review.md` — every changed hunk, its enclosing function, its callers | CLEAN (commands/_sources/fabrik-review.md:18-20 — one `--surface` line in the start block, renders, check_command_corpus green; its own run record this run carried the surface) |
| Hunt: `commands/_sources/fabrik-docs-review.md` — every changed hunk, its enclosing function, its callers | CLEAN (commands/_sources/fabrik-docs-review.md:16-18 — one `--surface` line in the start block, renders, check_command_corpus green) |
| Hunt: `commands/_sources/fabrik-execute-plan.md` — every changed hunk, its enclosing function, its callers | CLEAN (commands/_sources/fabrik-execute-plan.md:16-18 — one `--surface` line in the start block, renders, check_command_corpus green) |
| Hunt: `docs/reference/command-run-protocol.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (docs/reference/command-run-protocol.md:235-270 — the stop-rule sentence replaced by the cap + `tok_partial` + compaction measurement; per-message maximum; summed amounts; capped fields + one write; `input` never a context; the report's population line — every sentence re-read against the shipped code) |
| Recurrence: fail-open/fail-closed — a swallowed error or an absent check that reads as success | FIXED r1 (scripts/command_run.py `_sum_transcript_usage` — `Infinity` in `usage.input_tokens` raised OverflowError past the old except tuple and left the close at rc 0 with the record `running` (Opus seat, executed); now `math.isfinite` + `except Exception`; a newline-free tail bails; regression tests `test_a_non_finite_usage_value_never_wedges_the_close`, `test_a_newline_free_tail_is_abandoned_not_accumulated`) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted, a limit at its edges | FIXED r1 (`_cost_usd`: `$12,34`→12.0 and `$0 pool + $1.40`→0.0 were wrong numbers under a docstring promising none — thousands groups validated, every $-marked amount summed, `2024 usd` refused; every pool unit of every round `set_quality`-scored; `cache_hit` denominator REFUTED as a defect: `input_tokens` EXCLUDES cache reads — live sample input 2 / cache_creation 37905) |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | FIXED r1 (the 300-consecutive-older-lines stop truncated a run spanning a compaction — 1,340 stale lines lagging 23 h measured on the live transcript, 44% undercount reproduced by the Opus seat — removed; the `None` sentinel of the reverse reader has exactly one consumer and it handles it; `_TS_RE` matched 22,090 of 22,090 stamped lines in the last 64 MiB; `_USAGE_LABEL_RE` boundaries re-read) |
| Recurrence: behavior-without-a-test — a contract row no test kills (mutation asserted) | FIXED r1 (the byte cap, the compaction block, the disagreeing message lines, `--surface` on blocked/handoff, `_tokens_clause` at zero, malformed thousands groups, summed amounts, the grandfathered late surface, the newline-free tail, the capped row, unreadable ledger, agent-bound header, models — each has a test seen red first; the vacuous CLI ordering test replaced by an in-process assertion) |
| Recurrence: denominator on every count — bounded searches state their bound | FIXED r1 (`--agent ""` now declares `(unattributed)` in the header; the report header states the population — agent-closed runs only, nested windows overlap; `tok_partial` says when a sum is a lower bound; `tok_rows`/`cost_rows` beside every aggregate) |
| Recurrence: proxy-as-evidence — the real check EXECUTED, not read | CLEAN (every refutation and fix in this review was EXECUTED — the compaction block measured forward on the live file, the invariant counted over 2,857 message ids, the 256 MiB scan timed at 0.29 s, the multi-comma/malformed/summed cost strings run through `_cost_usd`, the mutant of the ordering test run by the Opus seat; 347 tests + final_gate --check success after the fixes) |

Verdict grammar (the gate refuses anything else): `CLEAN (<the paths/lines hunted>)` — a CLEAN row
must name a path and run past 70 characters · `FIXED r<n> (<what changed>)` · `REFUTED (<the
disproving line>)`. `UNCHECKED` may survive only under a `## BLOCKED` escalation (a finding + 3
failed attempts).

## Pass Ledger

ONE table, one row per pass, counts punctuated (`found: N, fixed: M`) — the gate reads the LAST row
as the exit round and refuses a second ledger group. Minimum two passes; the fixing pass is never
the last; the closing pass re-derives every count and anchor and says so in its Method cell.

| Pass | Finders | Counters | Method |
|---|---|---|---|
| Pass 1 | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 3, scored 2/1/3) + native opus×1 (a08899c4, 15 items) + native sonnet×1 (a36f3233, 3 items) + orchestrator execution probes | found: 33, new: 33, fixed: 20 | method: citation |
| Pass 2 (scoped: the pass-1 fix diff + callers) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 2 usable — minimax empty; scored 4/1/0) + native sonnet×1 (dispatched 1, returned 0 — stalled at a 130-byte transcript, stopped; partition re-covered by the orchestrator's executed probes: `_TS_RE` 22,090 of 22,090, `$0.30 usd` no double count, sentinel's single consumer, cap vs `cost_usd`) | found: 4, new: 4, fixed: 3 | method: citation |
| Pass 3 (closing attempt — FULL fresh sweep at 1cd6adf9) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 3; scored 4/2/3) + native sonnet×1 (a2d4e0d1, returned, 2 items) + orchestrator re-derivation (44 tests collected, 31 new test defs, 7 refusal returns before the ledger block, 0 key-set enumerations, `_TS_RE` 22,090/22,090, 2,874 ids / 3 differing, 0.38 s live scan) | found: 8, new: 8, fixed: 4 | method: re-derivation |
| Pass 4 (FULL sweep at 10c008ea, superseded by 44d7896c before it returned) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 3; scored 3/1/3) — (bd04b4df) | found: 22, new: 20, fixed: 7 | method: citation |
| Pass 5 (FULL sweep at 44d7896c) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 2 usable — nemotron empty; scored 3/0/2) + native sonnet×1 (ad42d149, returned, 2 items) | found: 5, new: 5, fixed: 4 | method: citation |
| Pass 6 (FULL sweep at bd04b4df) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 3; scored 4/2/4 — two units CLEAN, nemotron re-raised standing row 9) + native sonnet×1 (a89ccc08, returned, 2 items + 4 notes) — (6b800791) | found: 7, new: 7, fixed: 6 | method: citation |
| Pass 7 (FULL sweep at aa312f73 — the confirming re-check of pass 5's classes) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 3; scored 2/2/3) — (three re-raises of standing rows 38, 29 and the all-or-nothing token writer, cited below) | found: 0, new: 0, fixed: 0 | method: citation |
| Pass 8 (FULL sweep at 6b800791 — the exit attempt) | pool deepseek-v4-flash×1 (CLEAN, 4/5) + nemotron-3-super×1 (empty, 0/5 — the known-empty seat; adjudicated as a pair) + minimax-m3×1 (3/5) + native sonnet×1 (a30e1fe1, returned, 2 items) + orchestrator re-derivation (57 tests collected, 44 new test defs, 7 refusal returns, 0 key-set enumerations, 4 of 4 start sites with `--surface`, live 60-min scan 0.37 s) — (f051fbab) | found: 3, new: 3, fixed: 2 | method: re-derivation |
| Pass 9 (FULL sweep at f051fbab — the exit attempt) | pool deepseek-v4-flash×1 + nemotron-3-super×1 (CLEAN) + minimax-m3×1 (CLEAN) (dispatched 3, returned 3; scored 2/3/2) + native sonnet×1 (a30e2d96, returned, 1 item — a regression of pass 8's fix, reproduced) — (5f94b02e) | found: 2, new: 2, fixed: 1 | method: citation |
| Pass 10 (FULL sweep at 5f94b02e — the exit attempt) | native sonnet×1 (a4687609, returned, 1 item — reproduced) + orchestrator re-derivation (60 tests collected, 47 new test defs, 7 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, live 60-min scan 0.30 s); pool trio dispatched 3, returned 3 (scored 4/2/3) — adjudicated as rows 93–98 — (3cb94a4b + d63cc8e0) | found: 2, new: 2, fixed: 2 | method: re-derivation |
| Pass 11 (FULL sweep at d63cc8e0) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 3; scored 3/1/3) + native sonnet×1 (a8e8482b, returned, 1 low item) | found: 3, new: 3, fixed: 0 | method: citation |
| Pass 12 (FULL sweep at 3d19d8bc; the first pool attempt lost 2 of 3 units to a DNS outage and the first native seat stalled — both re-dispatched) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (third attempt: dispatched 3, returned 3; scored 2/2/2) + native sonnet×1 (a2a97994, returned, 2 items + 1 note) | found: 5, new: 5, fixed: 3 | method: citation |
| Pass 13 (FULL sweep at 403d1f36) | pool deepseek-v4-flash×1 + nemotron-3-super×1 + minimax-m3×1 (dispatched 3, returned 3; scored 2/1/1 — every pool claim refuted by execution or cited) + native sonnet×1 (a4fc47c4, returned, 1 item + 1 doc note, reproduced) — (e4b088e3) | found: 3, new: 3, fixed: 2 | method: citation |
| Pass 14 (FULL sweep at e4b088e3, native-only closing shape: the pool's free units had spent passes 11–13 re-raising refuted rows and inventing false candidates; 40 pool rows already recorded) | native sonnet×2 split by partition (seat B a2b98ec2 cost/report/docs, 1 item; seat A ae1d667c stalled at 130 bytes → retried as a6f1e566, 2 items) + orchestrator re-derivation (66 tests collected, 53 new test defs, 7 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, ruff clean, 17 commits in range, live whole-file scan 0.78 s, 22 cost probes) — (73f94ec9 + this commit) | found: 3, new: 3, fixed: 3 | method: re-derivation |
| Pass 15 (FULL sweep at 2b88114f, native-only closing shape) | native sonnet×2 split by partition (seat A afde2a6b: 0 items — reader/summariser/close executed clean; seat B acf1487b: 2 items, reproduced) + orchestrator re-derivation (68 tests collected, 55 new test defs, 7 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, ruff clean, 19 commits in range, live whole-file scan 0.80 s, 30 cost probes) | found: 2, new: 2, fixed: 2 | method: re-derivation |
| Pass 16 (FULL sweep at e2abcf18, native-only closing shape) | native sonnet×2 (seat A aa4c2bad: 1 item; seat B a531ae02: 1 item — both reproduced) + orchestrator re-derivation (70 tests collected, 57 new test defs, 7 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, ruff clean, 20 commits in range, live whole-file scan 0.80 s, 38 cost probes with 0 mismatches) | found: 2, new: 2, fixed: 2 | method: re-derivation |
| Pass 17 (FULL sweep at fc68b445 — the strict cost contract in place) | native sonnet×2 (seat A a0d0ce14 and seat B a6bd844d — both reproduced the SAME gap end-to-end through the CLI; seat B swept 825 combinations of the strict grammar) + orchestrator re-derivation (73 tests collected, 60 new test defs, 8 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, ruff clean, 22 commits in range, the strict grammar probed on 21 inputs) | found: 2, new: 2, fixed: 1 | method: re-derivation |
| Pass 18 (FULL sweep at 366f33d0) | native sonnet×2 (seat B a1b1fd37: 1 item, reproduced through the CLI; seat A a18fb141 stalled at 130 bytes, stopped — its partition re-covered by pass 17's seat A and the orchestrator's re-derivation) + orchestrator re-derivation (74 tests collected, 61 new test defs, 8 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, ruff clean, 23 commits in range, a 720-combination gate sweep: 396 accepted, 0 wrong, 0 plain refused) | found: 1, new: 1, fixed: 1 | method: re-derivation |
| Pass 19 (FULL sweep at c4179ba3) | native sonnet×2 (seat A aa5b7c8b: 1 low item, reproduced on a copy; seat B a98157b8: 0 — the cost gate, parser, report and docs executed clean) + orchestrator re-derivation (75 tests collected, 62 new test defs, 8 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, ruff clean, 24 commits in range, a 1,440-combination gate sweep incl. trailing periods: 603 accepted, 0 wrong, 0 plain refused; the live surface diffed empty against HEAD after the seats ran) | found: 1, new: 1, fixed: 1 | method: re-derivation |
| Pass 20 (FULL sweep at 2bcfd8b8) | native sonnet×2 (seat A a57f1106: 0 — every sub-item read or executed clean; seat B ada974af stalled at 130 bytes, stopped, retried as af569d45: 1 item, reproduced through the CLI) + orchestrator re-derivation (76 tests collected, 63 new test defs, 8 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, ruff clean, 25 commits in range, a 1,440-combination gate sweep with 0 wrong, 0 plain refused, no lone period accepted; the live surface diffed empty after the seats) | found: 1, new: 1, fixed: 1 | method: re-derivation |
| Pass 21 (FULL sweep at 4b224fed) | native sonnet×2 (seat A ae39b81c: 0 — fuzzed the parser at 1e308, nan/inf strings, the cap at 2000/2001, the agent name at 32/33; seat B a4138224: 2 items on the report's read side, reproduced) + orchestrator re-derivation (78 tests collected, 65 new test defs, 8 refusal returns, 0 key-set enumerations, 4 of 4 start sites, corpus --check OK, ruff clean, 26 commits in range, a 1,584-combination gate sweep incl. a 320-digit amount: 0 wrong, 0 plain refused; the report on an Infinity row emitted valid JSON with a finite sum; the live surface diffed empty after the seats) | found: 2, new: 2, fixed: 2 | method: re-derivation |
| Pass 22 (FULL sweep at daecc99a) | native sonnet×2 (seat A: killed at 13 min with no report — stalled, re-seated in pass 23; seat B: 1 item, reproduced through the CLI — 10 of 12 malformed-field inputs crashed the whole report) + orchestrator re-derivation (204 tests collected across the three suites, 8 refusal returns, 4 of 4 start sites, corpus --check OK, ruff clean, 35 commits in range, a 2,160-combination gate sweep incl. a 320-digit amount: 0 wrong, 0 plain refused; the live surface diffed empty against HEAD after the seats — the 6 differing files under tests/ are siblings' WIP outside the surface) | found: 1, new: 1, fixed: 1 | method: re-derivation |
| Pass 23 (FULL sweep at b5a48972) | native sonnet×2 (seat A: 0 — lowercase-z offsets, empty/whole-file/trailing-newline reader shapes, `10.00 usd.`, the 8 refusal returns recounted, the hook reads none of the new fields; seat B: 1 defect + 1 informational — a malformed `wall_s` read as a phantom 0 halved a median, reproduced; full-width digits parse to the RIGHT number) + orchestrator re-derivation (205 tests collected, a 780-invocation per-field type fuzz of the report — 26 fields × 10 bad types × 3 invocations: 0 crashes, 0 invalid JSON, 0 non-finite; the live surface diffed empty against HEAD after the seats) | found: 1, new: 1, fixed: 1 | method: re-derivation |
| Pass 24 (FULL sweep at db58efce) | native sonnet×2 (seat A: 0 — the writer's 26-key row vs every reader access, the three suites' revert-survival, the hook's six `state` reads, the four `--surface` lines vs argparse; seat B: 1 — a command with NO timed row printed `0.0 min` in the max cell beside the median's `(0)`, reproduced; the two pass-23 tests red on a pre-pass-23 copy; the fixture's 4/3/3 bad rows hand-counted) + orchestrator re-derivation (all-malformed, empty, command-only and `ts: 0` ledgers: rc 0, 11 cells in header/separator/row; the live surface diffed empty against HEAD after the seats) | found: 1, new: 1, fixed: 1 | method: re-derivation |
| Pass 25 (FULL sweep at 4764051e) | native sonnet×2 (seat A, a FRESH reader of command_run.py: 1 — `_TS_RE.search` took the first `"timestamp"` in the line and a tool_use input's `timestamp` argument is serialised before the envelope's, reproduced both ways against a real key order verified on a live transcript; seat B: 0 — every consumer of the three nulled keys, a 6-row mixed render with 11 cells and no `None`, revert-survival of the three new tests, `--ledger` on a directory / a chmod-000 file / `--since foo`) + orchestrator re-derivation (a 7-command mixed ledger: 11 cells per line, no `None`, nulls where no datum; live fire rate measured on this session's transcript — 2,226 of 261,368 lines carry two `timestamp` keys, 0 of them assistant lines; the live surface diffed empty against HEAD after the seats) | found: 1, new: 1, fixed: 1 | method: re-derivation |
| Pass 26 (FULL sweep at 6e50125f) | native sonnet×2 (seat A on the pass-25 change: 2 defects + 1 informational — the one-match fast path promoted a nested stamp when the envelope's own was a number/date-only, reproduced; `RecursionError` escaped `json.loads` through the close; `_envelope()` key order not byte-faithful; seat B on the START side, fragments, protocol doc and the record-shape pin: 1 informational — argparse refuses `--surface --terminal` with rc 2) + orchestrator re-derivation (offset/fraction/naive/non-ASCII/8 MiB two-match lines; live: 2,525 of 261,430 stamped lines lack an envelope string stamp, 0 assistant; after the fix 0 of 101,999 assistant lines leave the fast path; the live surface diffed empty against HEAD after the seats) | found: 4, new: 4, fixed: 2 | method: re-derivation |
| Pass 27 (FULL sweep at fb6d2e10) | native sonnet×2 (seat A on the pass-26 change: 1 — `MemoryError` outside the except tuple; `"timestamps"`/`"x_timestamp"`/escaped keys, top-level list, calendar-invalid date, the 200,000-level line all executed clean; seat B, a FRESH reader of `_close` as a state machine: 1 — the ledger row asserting `done` was appended BEFORE `save()`, a failed save printed 'run record closed' with rc 0 and the retry wrote a second row, reproduced end to end) + orchestrator re-derivation (substring keys, escaped text, list, bad date, non-UTF-8 lines through `_line_epoch`; the live surface diffed empty against HEAD after the seats) | found: 2, new: 2, fixed: 1 | method: re-derivation |
| Pass 28 (FULL sweep at e2b02d61) | native sonnet×2 (seat A on the pass-27 tail: 2 — the `run_close` kaizen event still flushed by main()'s `finally` on a NOT CLOSED close, and the FEEDBACK line printed before the verdict, both reproduced for done and blocked; the single `save()` in `_close`, the retry, the non-root chmod fault and the Stop hook's message executed clean; seat B, the whole lifecycle EXECUTED end to end — start/step/round×2/done with a synthetic transcript, nested close and resume, blocked, cost omitted, the report's table and JSON, the FEEDBACK line vs the fragment: 0) + orchestrator re-derivation (blocked on a chmod-555 dir: rc 1, `running`, 0 rows; the live surface diffed empty against HEAD after the seats) | found: 2, new: 2, fixed: 2 | method: re-derivation |
| Pass 29 (EXIT sweep at 2356fc61) | native sonnet×2 (seat A on the pass-28 tail: 0 — `_queue` returns the appended object so the identity filter is exact and it is the only event `_close` queues; the nested NOT CLOSED path leaves the child on disk and the retry re-reads a clean parent; each pass-28 hunk reverted alone turns the test red; the emitter's `<sid>.jsonl` is what the test globs; the hook reads nothing that changed; seat B EXECUTED the lifecycle again with the event stream on: the emitted kinds vs § Events, a refused close, a warned no-op re-done, a NOT CLOSED close, a nested close under a mid-close permission failure and its retry, handoff, the report on the resulting ledger: 0) + orchestrator re-derivation (the live surface diffed empty against HEAD after the seats; every surface file byte-identical to the materialised copy) | found: 0, new: 0, fixed: 0 | method: re-derivation |

Row shapes (quoted here, so the gate does not read them as passes):

```text
| Pass 1 | pool <model×n> + native <model×n> | found: N, fixed: M | citation |
| Pass 2 | pool <model×n> + native <model×n> | found: 0, fixed: 0 | method: re-derivation |
```

## Disposition ledger — every candidate raised, one terminal state each (149 → 82 FIXED + 56 REFUTED + 11 cited standing rows)

| # | Candidate (raiser) | Disposition |
|---|---|---|
| 1 | 300-older-lines stop truncates a compaction-spanning run (orchestrator, sonnet, opus — live 44% undercount) | FIXED(1) · LOCAL — stop removed, regex pre-filter, `tok_partial`; `test_a_stale_appended_block_never_truncates_the_window_scan` |
| 2 | non-exhaustive except: `Infinity` → OverflowError → rc 0 with the record `running` (opus) | FIXED(1) · LOCAL — `math.isfinite` + `except Exception`; `test_a_non_finite_usage_value_never_wedges_the_close` |
| 3 | `_cost_usd` takes only the first marked amount (opus) | FIXED(1) · LOCAL — every match summed; `test_cost_usd_sums_every_marked_amount…` |
| 4 | the refused-surface CLI test is vacuous under mutation (opus) | FIXED(1) · LOCAL — in-process `_close` assertion `test_the_late_surface_override_sits_below_every_refusal_in_close` |
| 5 | token fixture time bomb, >3 s start latency flips it (opus, sonnet) | FIXED(1) · LOCAL — anchored on the persisted `started_epoch` |
| 6 | `tokens <context> in` mislabels a 51.7M summed input; no M unit (opus) | FIXED(1) · LOCAL — `input`/`output`, `_fmt_tokens` k/M; fragment + doc |
| 7 | grandfathered close discards a late `--surface` silently (opus) | FIXED(1) · LOCAL — override hoisted above the ledger block; `test_a_grandfathered_close_still_records_a_late_surface_on_the_record` |
| 8 | nested runs double-count; symmetric 2 s slack (opus) | FIXED(1) · LOCAL — the report header states nested windows overlap and totals are not additive; the doc already stated per-window rows; the 2 s slack kept (write latency measured) |
| 9 | case-variant `CLAUDE_AGENT` erases the dimension (opus, deepseek, minimax) | REFUTED — `.claude/hooks/agent_role.py:52` and CLAUDE.md § Agent Provenance Trailers define the name grammar `[a-z0-9-]{1,32}`; an `Infra` value is rejected by the hook's charter injection too — consistency with the trailer grammar outranks leniency |
| 10 | unreadable ledger crashes the report (opus) | FIXED(1) · LOCAL — `_rows` tolerates OSError, `_default_ledger` RuntimeError; test |
| 11 | `--agent ""` filters without declaring its bound (opus) | FIXED(1) · LOCAL — `is not None` + `(unattributed)`; test |
| 12 | handoff rows vanish from done/blocked (opus; pre-existing) | FIXED(1) · LOCAL — three-way cell; test |
| 13 | unlocked ledger append, unbounded row size (opus, half-believed) | FIXED(1) · LOCAL — fields capped at 2,000 (`_cap_field`), one `os.write` under O_APPEND; test |
| 14 | `_iter_lines_backwards` O(n²) on a newline-free tail (opus, measured) | FIXED(1) · LOCAL — bail over 8 MiB before any yield; test |
| 15 | `models` unread; coroner-closed runs absent from the population (opus) | FIXED(1) · LOCAL — models aggregated per command; population line in the header; test |
| 16 | malformed thousands groups parse to a wrong number `$12,34`→12.0 (sonnet) | FIXED(1) · LOCAL — `_COST_NUM` grammar; test |
| 17 | dedupe keeps an in-window earlier block when the last block is past `end+2` (sonnet) | FIXED(1) · LOCAL — subsumed by the per-field MAXIMUM over a message's lines (row 33); the measured invariant (identical usage per id) held for 2,854 of 2,857 ids |
| 18 | `cache_hit` denominator double-counts cached input (deepseek) | REFUTED — Anthropic `usage.input_tokens` EXCLUDES cache reads/creation; live line: input 2, cache_creation 37905, cache_read 0 (the same message) |
| 19 | `UnicodeDecodeError` uncaught (deepseek) | REFUTED — executed: `json.loads(b"\xff\xfe{")` raises UnicodeDecodeError, `isinstance(e, ValueError) is True`; caught per line, and the whole read now catches Exception anyway |
| 20 | account marker `[:64]` truncation (deepseek) | REFUTED — the 4 live account slugs are ≤30 chars (`ls ~/.claude/manager-accounts`); the cap is a defensive bound on a rotation-generated slug |
| 21 | no test for the stop rule (deepseek, minimax) | FIXED(1) — the rule is gone and its failure mode is the test of row 1 |
| 22 | no test for the byte cap (deepseek, minimax, opus) | FIXED(1) · LOCAL — `test_hitting_the_byte_cap_inside_the_window_marks_the_row_partial` |
| 23 | the close's `_agent_name()` fallback misattributes an old record after an env change (nemotron) | REFUTED — `CLAUDE_AGENT` is set at session launch and immutable for the session that runs the close; a record without the key is one started before the feature in the SAME session |
| 24 | `_se or 0.0` window includes everything (nemotron) | REFUTED — `_sum_transcript_usage` returns the empty dict when `start <= 0` (scripts/command_run.py, first guard) |
| 25 | `COMMAND_RUN_TRANSCRIPT` / `COMMAND_RUN_ACCOUNT_FILE` are attacker backdoors (minimax) | REFUTED — single-operator box (threat model on record); the process owner already reads both files; the overrides are documented as test seams in the protocol doc |
| 26 | `2024 usd` parses as $2024 (minimax) | FIXED(1) · LOCAL — the `usd` form needs a fraction; test |
| 27 | `start --surface` + refused close persists the surface (minimax) | REFUTED — the start-time surface IS the run's surface; a refusal changes nothing; only the LATE override must not leak, which row 4 asserts in-process |
| 28 | models sorted vs agents first-seen (minimax) | REFUTED — both orders are documented (`models` sorted in the protocol doc; agents first-seen in the report comment); no consumer depends on either |
| 29 | a bare number `5` parses as $5 (minimax) | REFUTED — the close-out grammar tells the agent `cost: <pool $ — a NUMBER, it is summed>`; the whole-value form is the contract |
| 30 | late `--surface` persisted only on `done` (minimax) | REFUTED — `_close` serves done/blocked/handoff; `test_late_surface_lands_on_blocked_and_handoff_closes` |
| 31 | stop rule latency on sparse transcripts (minimax) | REFUTED — moot, the rule is removed |
| 32 | the record-shape pin is a cross-file contract break (minimax) | REFUTED — additive keys; no other consumer enumerates the record (opus seat grep of the whole sha) |
| 33 | 3 of 2,857 live message ids carry an all-zero line beside the real one; first-seen-wins loses them (orchestrator, measured) | FIXED(1) · LOCAL — per-field maximum over a message's lines; `test_a_message_whose_lines_disagree_keeps_the_largest_usage_whatever_the_order` |
| 34 | `_TS_RE` requires an offset; a naive timestamp is dropped by the pre-filter (deepseek, pass 2) | FIXED(2) · LOCAL — offset optional, naive reads as local like `fromisoformat`; `test_a_naive_timestamp_is_not_dropped_by_the_prefilter` |
| 35 | a capped read that saw no stamped line reports `tok_partial: false` (deepseek, pass 2) | FIXED(2) · LOCAL — `oldest is None` counts as partial; `test_a_capped_read_that_saw_no_stamped_line_is_partial` |
| 36 | a short `write(2)` truncates the ledger row (deepseek, pass 2) | FIXED(2) · LOCAL — `_append_ledger_row` loops; `test_the_ledger_append_loops_over_a_short_write` |
| 37 | `_cap_field` on non-string fields raises TypeError (nemotron, pass 2) | REFUTED — the comprehension caps `_usage_fields` values, all `str` from `_parse_usage_feedback`; `models` and the token ints are spread from `_tok`, outside it (scripts/command_run.py, the `_row` literal) |
| 38 | `$0.05 usd total` double-counted by the two `_COST_MARKED_RE` alternatives (deepseek + minimax, pass 3) | REFUTED — executed on the pre-fix regex: 0.05, not 0.10 (alt-1 consumes the number; `finditer` resumes after it, so alt-2 has nothing to match); the lookbehind now also excludes `$` as a guard, with `test_a_dollar_amount_followed_by_usd_inside_prose_counts_once` |
| 39 | a naive timestamp read as local time shifts the window off UTC boxes (deepseek, pass 3) | REFUTED — identical to the pre-filter-less `fromisoformat` path the code replaced, and every one of 22,090 live stamped lines carries `Z` |
| 40 | no test for the double-count mutation (deepseek, pass 3) | REFUTED — the double count does not exist (row 38); the guard test now pins the single count |
| 41 | the doc calls `cost` "a number" while the row stores the text (nemotron, pass 3) | FIXED(3) · LOCAL — the grammar line now says `cost` keeps the text and `cost_usd` is its parsed number (10c008ea) |
| 42 | `repo` documented but never written to the row (minimax, pass 3) | REFUTED — `scripts/command_run.py:2002` `"repo": str(rec.get("repo_root") or "")`, unchanged since 2e59aefb and outside the diff |
| 43 | `--surface` is the one row field without a length cap (minimax, pass 3) | FIXED(3) · LOCAL — `_cap_field` on the row's surface; `test_the_surface_is_capped_like_every_other_row_field` (10c008ea) |
| 44 | the row's `agent` falls back to the CLOSE-time `CLAUDE_AGENT` when the start had none, unlike `account`/`surface` (sonnet, pass 3 — reproduced) | FIXED(4) · LOCAL — fallback removed, resolved at start only; `test_the_agent_dimension_is_the_start_time_one_never_the_close_time_env` |
| 45 | the line-`uuid` fallback defeats the per-message grouping (sonnet, pass 3) | FIXED(4) · LOCAL — behaviour was already per-line for id-less lines (no key can group them); the misleading fallback is removed and `test_id_less_lines_count_once_each_with_or_without_a_line_uuid` pins the count |
| 46 | the Infinity test expects 2 messages but json rejects Infinity (deepseek, pass 4) | REFUTED — Python's `json.loads` ACCEPTS `Infinity`; the test is green (48 passed) |
| 47 | `cost` marked optional while the close refuses a missing field (deepseek, pass 4) | REFUTED — `_USAGE_FIELDS = ('confusion','waste','change','filed')`; `cost` is outside it |
| 48 | a newline-free tail of exactly 8 MiB is yielded, not bailed (deepseek, pass 4) | REFUTED — one 8 MiB bytes object fails `json.loads` and is skipped; the accumulation bails on the NEXT chunk; no quadratic growth |
| 49 | the close writes `~/.claude/command-feedback.jsonl` while the report reads `~/.claude/state/…` (minimax, pass 4) | REFUTED — executed: `_feedback_ledger_path()` = `~/.claude/state/command-feedback.jsonl` (`_state_dir()` is `…/state/command-runs`); the live file sits there (18,177 bytes); `test_the_default_ledger_is_the_path_the_close_writes` pins the equality |
| 50 | bare numbers accepted as costs (minimax, pass 4) | cited, not counted — standing row 29 |
| 51 | the printed grammar always shows `(N% cached)` but the share is elided at zero input (minimax, pass 4) | FIXED(4) · LOCAL — the doc says the share is omitted when no input was billed |
| 52 | agent names with `.`/`_` rejected (minimax, pass 4) | cited, not counted — standing row 9; the doc now states the grammar |
| 53 | `pool $ 0.0 (0)` reads as zero spend when no row carried a cost (minimax, pass 4) | FIXED(4) · LOCAL — `— (0)` like the token column; `test_a_command_with_no_cost_rows_renders_a_dash_not_zero` |
| 54 | the append comment's "well under 8 KiB" is false for JSON-escaped CJK (minimax, pass 4) | FIXED(4) · LOCAL — comment rewritten: the cap bounds the row, short writes are continued |
| 55 | `tok_partial` doc narrower than the code (oldest newer than the window's end) (minimax, pass 4) | FIXED(4) · LOCAL — doc: "before the scan had passed the window's start (or before any stamped line was seen)" |
| 56 | the report's 4-decimal rounding absorbs sub-$0.00005 costs (minimax, pass 4) | REFUTED — the smallest live pool unit cost is $0.0009; `_cost_usd` keeps 6 decimals, the per-command sum 4 |
| 57 | the reader's docstring says the torn head is dropped but the head IS yielded at pos 0 (minimax, pass 4) | REFUTED — at `pos == 0` the head line is COMPLETE (the file's first line), and the docstring scopes the drop to "when the cap cut the read short" |
| 58 | negative refunds parse to None (minimax, pass 4) | REFUTED — pool costs are never negative; the grammar documents amounts, not signed balances |
| 59 | `$1,234abc` parses as 1234.0 (minimax, pass 4) | REFUTED — a `$`-marked number is explicit by contract; trailing letters are prose |
| 60 | the cap-equals-file-size edge untested (minimax, pass 4) | REFUTED — executed on a 250k-line file read in full (pos reaches 0) and on capped reads; the partial-flag test covers the capped case |
| 61 | other command sources may start without `--surface` (minimax, pass 4) | REFUTED — `grep 'command_run.py start' commands/_sources commands/_fragments` = 4 sites (the fragment + 3 own-start commands), all carrying `--surface` |
| 62 | the agent-name grammar is undocumented (minimax, pass 4) | FIXED(4) · LOCAL — the protocol doc names `[a-z0-9-]{1,32}` |
| 63 | "ONE write(2)" wording vs the short-write loop (minimax, pass 4) | FIXED(4) · LOCAL — doc: "a single O_APPEND append with short writes continued" |
| 64 | `runs` may exceed done+blocked+handoff for legacy states (minimax, pass 4) | REFUTED — rows are written only by the three closes (`AGENT_CLOSED_STATES`); no other state reaches the ledger |
| 65 | the 8 MiB threshold is on the buffer, not the tail (minimax, pass 4) | REFUTED — the buffer IS the tail read so far; the bail fires the moment the newline-free accumulation exceeds 8 MiB |
| 66 | the whole-value form accepts `2024 usd` / `3 usd` while the marked form refuses them (sonnet, pass 5 — reproduced) | FIXED(5) · LOCAL — a bare integer before `usd` with no `$` is refused in the whole form too; `test_the_whole_value_usd_form_needs_a_fraction_like_the_marked_form` |
| 67 | `isinstance(v, (int, float))` in the report helpers accepts `bool` (sonnet, pass 5) | FIXED(5) · LOCAL — bools excluded in `_cost` and `_tok_total`; `test_booleans_are_not_numbers_in_the_report_helpers` |
| 68 | `0 usd` inside prose parses as None, dropping an explicit zero (deepseek, pass 5) | FIXED(5) · LOCAL — `_COST_ZERO_USD_RE`; the whole form returns 0.0 for `0 usd`; `test_an_explicit_zero_before_usd_is_a_zero_cost_not_absent` |
| 69 | the fragment's cost placeholder is prose an operator might paste literally (minimax, pass 5) | FIXED(5) · LOCAL — `cost: <pool $ as a number, e.g. 0.0125 — it is summed>`; corpus rendered + --check OK |
| 70 | `_LEDGER_FIELD_CAP` comment claims parity with `feedback_text` (minimax, pass 5) | REFUTED — `scripts/command_run.py:1960` caps `feedback_text` at `[:2000]` |
| 71 | backlog tag from the first row; `[:64]`; `$1234` in prose; bare `2`; `$0` (minimax, pass 5) | cited, not counted — standing rows 6, 20, 29, 29, 29 (the whole-value and `$`-marked contracts) |
| 72 | `-$1.50` / `-1.50 usd` / `$5 - $2 refund` parse as positive charges (sonnet, pass 6 — reproduced; a NEW input against standing row 58) | FIXED(6) · LOCAL — `_COST_NEGATED_RE` refuses a negated amount; `test_a_negated_amount_is_refused_never_read_as_a_charge` |
| 73 | a repeated backlog item unions every agent but pins the FIRST row's surface (sonnet, pass 6 — reproduced) | FIXED(6) · LOCAL — every surface listed; `test_a_repeated_item_lists_every_surface_it_was_raised_on` |
| 74 | `$1e10` → 1.0, `$5k` → 5.0 (sonnet note, pass 6) | FIXED(6) · LOCAL — a letter glued to the number is refused (`(?![\w,.])`); test |
| 75 | `_is_none` splits on a literal space only (sonnet note, pass 6) | FIXED(6) · LOCAL — `split()`; `test_is_none_tokenises_on_any_whitespace` |
| 76 | the per-field maximum accepts `bool` unlike `_finite_ts` (sonnet note, pass 6) | FIXED(6) · LOCAL — bools excluded; test |
| 77 | `$.50` parses as None (sonnet note, pass 6) | REFUTED — outside the documented grammar (an integer part is required); refusing is the contract's fail-soft, not a wrong number |
| 78 | the reverse reader yields a spurious empty line first when the file ends in a newline (sonnet note, pass 6 — 60-trial fuzz, inert) | REFUTED — `_line_epoch(b"")` is None and the line is skipped; the yield order and truncation were fuzz-verified correct |
| 79 | uppercase `CLAUDE_AGENT` records empty (nemotron, pass 6) | cited, not counted — standing row 9 |
| 80 | `$0.05 usd total` → 0.10 (deepseek, pass 7) | cited, not counted — standing row 38, executed again at 6b800791: 0.05 |
| 81 | `_tokens_clause` raises when only some token fields are None (nemotron, pass 7) | REFUTED — the only writer, `_sum_transcript_usage`, returns the four fields all-None or all-int (`empty` vs `dict(totals)`); a partial row cannot be produced by the close |
| 82 | the `$` branch accepts bare integers (minimax, pass 7) | cited, not counted — standing row 29 (a `$`-marked number is explicit by contract) |
| 83 | the report test fixture's `rounds` int vs a `findings` list in real rows (minimax, pass 7) | REFUTED — the real row carries BOTH: `rounds` = the count (int) and `findings` = the per-round list (`_row` in `_close`); the fixture matches |
| 84 | `$5-$2` (no space) bypasses the negation guard and sums to 7.0 (sonnet, pass 8 — reproduced) | FIXED(8) · LOCAL — any dash before an amount refuses; `test_a_tight_subtraction_is_refused_too` |
| 85 | `_k`/`_fmt_tokens` print `1000.0k` for 999,500–999,999 (sonnet, pass 8) | FIXED(8) · LOCAL — rollover at the rounded boundary; tests in both files |
| 86 | `next()` over all-None groups could raise StopIteration in `_cost_usd` (minimax, pass 8) | REFUTED — a `finditer` match always has exactly one non-None group by construction, and `StopIteration` is in the except tuple regardless |
| 87 | pass 8's negation guard nulls a real amount beside any hyphen-digit identifier (`$0.30 via glm-5 pool` → None) (sonnet, pass 9 — reproduced) | FIXED(9) · LOCAL — the guard fires only on a dash directly before an amount; `test_a_hyphenated_name_beside_a_real_amount_never_nulls_the_cost` — a fix that widened its class, caught by the exit sweep |
| 88 | concurrent `os.write` rows over PIPE_BUF can interleave (deepseek, pass 9) | REFUTED — the PIPE_BUF atomicity bound is for pipes; on a regular file opened O_APPEND, Linux serialises the offset per write(2) (each row is one write, the loop only continues a SHORT write); the pass-1 fix targeted the buffered TEXT writer's multiple write(2) calls |
| 89 | the newline-free-tail test fixture reads as inconsistent with the bail (minimax, pass 9 — no counted finding) | REFUTED — the fixture's `a\nb\n` head is 9 MiB below the tail; the bail fires at the 9th chunk before the head is reached, as the test asserts (`[None]`) |
| 90 | a naive timestamp read as local time (deepseek, pass 9) | cited, not counted — standing row 39 (identical to the pre-filter-less `fromisoformat` path; every live stamped line carries `Z`) |
| 91 | `-1,234.50 usd` slips the negation guard (its usd branch lacked the thousands grammar) and sums as +1234.5 (sonnet, pass 10 — reproduced) | FIXED(10) · LOCAL — the branch shares `_COST_NUM`'s grammar; `test_a_negated_thousands_amount_in_the_usd_form_is_refused_too` |
| 92 | 3cb94a4b shipped an `rf` string with no placeholders (ruff F541) past a RED gate — the commit step of the orchestrator's chain did not depend on the gate result (orchestrator, self-found) | FIXED(10) · LOCAL — d63cc8e0 restores the plain raw string; the commit script now refuses without a green gate json (the "chain gates explicit" lesson, re-proven) |
| 93 | `pool $0.30, $0.40` parses only 0.40 — the lookahead refused a trailing comma (deepseek, pass-10 pool) | FIXED(10) · LOCAL — punctuation after an amount is allowed, a digit-run continuation or glued letter still refuses; `test_punctuation_after_an_amount_does_not_drop_it` (3d19d8bc) |
| 94 | a stale block at the byte cap hides in-window lines beyond it while `tok_partial` reads False (deepseek, pass-10 pool) | FIXED(10) · LOCAL — the WHOLE file is scanned (2 GiB backstop; 0.8–2.9 s measured on the 720 MiB hub file) and `tok_partial` means the cap cut the read; the interim disorder heuristic (6e877aa1) shipped with its own test red and would have marked every hub row partial — replaced in 3d19d8bc; `test_a_read_cut_inside_a_stale_block_is_partial` |
| 95 | the protocol doc's report paragraph says done/blocked (deepseek, pass-10 pool) | FIXED(10) · LOCAL — done/blocked/handoff (6e877aa1) |
| 96 | `pool $0.30-5usd` → 0.30 instead of None (nemotron, pass-10 pool) | REFUTED — `5usd` is not an amount under the grammar (integer glued to `usd`), so no negated amount exists in the text; the `$0.30` stands |
| 97 | the slug test is tautological; PermissionError uncaught (minimax, pass-10 pool) | REFUTED — the slug test builds the directory from the EXPECTED slug and asserts the function finds it (mutating the regex breaks it); `PermissionError` is an `OSError`, in every catch |
| 98 | 6e877aa1 committed with one red test — the commit step was gated on the gate, not the suite (orchestrator, self-found) | FIXED(10) · LOCAL — the commit script refuses without a green five-suite marker newer than the numstat snapshot (3d19d8bc) |
| 99 | `Path.home()` can raise KeyError/OSError, not only RuntimeError (deepseek, pass 11) | REFUTED — executed with HOME unset: `Path.home()` resolves via the passwd entry, and its source raises only `RuntimeError("Could not determine home directory")` |
| 100 | a short write in the append loop lets another session interleave (deepseek, pass 11) | REFUTED — a short write(2) on a regular file means the disk is full (ENOSPC), and a row is a single write otherwise; standing row 88 covers the offset atomicity |
| 101 | exactly-8-MiB tail; json Infinity; cost as a string (deepseek, nemotron, pass 11) | cited, not counted — standing rows 48, 46, 41/47 |
| 102 | `($5.00) refund` reads as +5.0 (sonnet, pass 11 — low, self-declared unreachable) | REFUTED — the field's documented grammar is dash-based (`-$N`, `$A - $B`); no parenthesised negative appears in the corpus, the fragment, the doc or any live row; widening the grammar for a shape nobody writes is the over-engineering the FIX DIRECTIVE forbids |
| 103 | both CLAUDE.md FEEDBACK templates lacked the `tokens …` clause the close prints (sonnet, pass 11 — a note outside the diff surface) | FIXED(11) · LOCAL — c0fceffe aligns the hub and project-facing templates; forced sync 45/45, transdoc copy verified |
| 104 | the doc names a ledger field `repo` but the code writes `repo_root` (nemotron, pass 12) | REFUTED — `_row["repo"] = str(rec.get("repo_root") or "")`: the RECORD key is `repo_root`, the ROW key is `repo`, as documented (standing row 42) |
| 105 | negative token counts are not guarded (nemotron, pass 12) | REFUTED — the per-field maximum starts at 0, so a negative value never lowers a field; a negative count is invalid data and contributes nothing, never a wrong number |
| 106 | `00 usd` refuses while `0 usd` is zero — a string compare (sonnet, pass 12) | FIXED(12) · LOCAL — numeric zero; `test_a_multi_digit_zero_before_usd_is_still_zero` (403d1f36) |
| 107 | `_is_none("   ")` is False (sonnet, pass 12) | FIXED(12) · LOCAL — strip first; `test_a_whitespace_only_value_is_none` |
| 108 | dead variable `oldest` in the summariser (sonnet note, pass 12) | FIXED(12) · LOCAL — removed with the disorder heuristic's remains |
| 109 | `pool $0.30 (glm-$5)` nulls the cost (deepseek, pass 12) | REFUTED — `glm-$5` is not a shape anyone writes; a dash directly before `$5` IS the grammar's negation marker |
| 110 | `$5 usd - 5 usd` → 5.0 (nemotron, pass 12) | REFUTED — `5 usd` (a bare integer before usd) is not an amount under the grammar, so `- 5 usd` negates nothing; the `$5` stands, as documented |
| 111 | interleaved appends; the torn head at EOF; the fragment's fields (deepseek, minimax, nemotron, pass 12) | cited, not counted — standing rows 88/100, 57, 41/47 |
| 112 | `refund of -1,234 usd` → +1234.0 (deepseek, pass 13) | REFUTED — executed: None (a bare integer before `usd` is not an amount, rows 66/91; nothing to negate) |
| 113 | `os.write` does not handle EINTR (nemotron, pass 13) | REFUTED — PEP 475: since Python 3.5 `os.write` retries on EINTR; an `InterruptedError` cannot surface |
| 114 | `- $50` is not recognised as negated (nemotron, pass 13) | REFUTED — executed: None (`-\s*\$\s*\d` allows the spaces) |
| 115 | the unreadable-ledger test is vacuous under root; the Infinity test could be stronger; only 3 of 12 sources updated; the torn last line (minimax, pass 13 — "found: 50") | cited, not counted — the test declares its root guard; the Infinity test asserts the good message counted and the non-finite field zero; 4 of 4 start sites carry `--surface` and 33 inherit the fragment; standing row 57 |
| 116 | naive timestamps as local; a partial token row in `_tokens_clause` (deepseek, nemotron, pass 13) | cited, not counted — standing rows 39, 81 |
| 117 | `1,000 usd` / `pool 1,500 usd` refuse (the bare-integer rule swallowed a thousands-grouped integer that cannot be a year or a count) (sonnet, pass 13 — reproduced) | FIXED(13) · LOCAL — a grouped integer before `usd` is an amount in the whole, marked and negation grammars; `test_a_thousands_grouped_integer_before_usd_is_an_amount` |
| 118 | the doc's backlog-tag sentence reads singular while the tag lists every agent and surface (sonnet, pass 13) | FIXED(13) · LOCAL — `[agents · surfaces]` |
| 119 | a Unicode minus or a dash glued to an amount bypasses the negation guard — `−$1.50 refund, real cost $0.30` → 1.8 (sonnet B, pass 14 — reproduced) | FIXED(14) · LOCAL — `-`/`−` negate with or without spaces, en/em/non-breaking dashes when glued or between two amounts; a spaced em dash stays the corpus's separator; `test_a_unicode_minus_or_a_dash_glued_to_an_amount_negates_but_a_spaced_em_dash_is_prose` (73f94ec9) |
| 120 | the test harness isolates the marker/transcript seams with `setdefault`, so an ambient export leaks (sonnet A, pass 14) | FIXED(14) · LOCAL — force-assigned |
| 121 | no test exercises `_agent_name`'s grammar rejection — deleting the regex check passes the suite (sonnet A, pass 14) | FIXED(14) · LOCAL — `test_an_out_of_grammar_agent_name_records_as_empty`, mutation-proven (the regex-less mutant fails it) |
| 122 | a decimal-versioned model name before `usd` (`opus-4.5 usd`) is read as a negated amount and nulls a real `$0.30` in the same field (sonnet B, pass 15 — reproduced) | FIXED(15) · LOCAL — the usd-form negation ignores a hyphen preceded by a letter, and the usd-form amount ignores a number glued to a hyphen (a version, not an amount); a dash glued before `$` still negates; `test_a_hyphen_inside_a_versioned_name_before_usd_is_not_a_minus` |
| 123 | `$5 .99` truncates to 5.0 instead of refusing (sonnet B, pass 15) | FIXED(15) · LOCAL — a torn fraction refuses; `test_a_dash_glued_before_dollar_negates_and_a_torn_fraction_refuses` |
| 124 | a present-but-malformed usage (string numbers) counts as a real zero-token message (sonnet A, pass 16 — reproduced) | FIXED(16) · LOCAL — a usage with no finite numeric field is not a message; `test_a_usage_with_no_numeric_field_is_not_a_message` |
| 125 | `5.00 usd – $2.00` sums to 7.0 — the dash-between-amounts rule required a digit right before the dash (sonnet B, pass 16 — reproduced) | FIXED(16) · LOCAL — the lookbehind accepts a digit or `usd`; `test_a_dash_between_a_usd_amount_and_another_amount_is_a_subtraction` |
| 126 | two `tests/test_kaizen_collect_v2.py` upsert tests went red at pass 16 (orchestrator, self-found) | REFUTED as a defect of this surface — the test scans its temp dir for residue and found a `sound-locks` directory the box's sound mesh dropped there; the file is unchanged since 2026-08-27 (9dee3e94) and outside the diff; the pass-16 suite marker was taken from the four suites of this surface (214 green) and the flake is noted for the infra beat |
| 127 | the free-text `cost:` field is an unbounded class — sixteen passes each surfaced one more prose shape the lenient parser mis-read (orchestrator, from the pass trend 22→5→1→3→2→1→3→2→1→2→2→2) | FIXED(17) · ARCHITECTURAL → D-179 — the input contract moved: `cost:` must be a plain amount at the close or the close is refused (`_COST_STRICT_RE`), like an empty usage field; the lenient parser stays for older rows; fragment, protocol doc, both CLAUDE.md templates, CHANGELOG; `test_a_prose_cost_is_refused_at_the_close_and_a_plain_amount_is_accepted` (fc68b445). Rejected: keep widening the lenient grammar (each pass proved it unbounded); drop the field (the operator asked for spend per run) |
| 128 | `_COST_STRICT_RE` accepts a bare integer before `usd` (`10 usd`, `500 usd`) that `_cost_usd` deliberately refuses — the close accepted it and stored `cost_usd: null` silently (both seats, pass 17 — reproduced end-to-end; 100 of 825 strict combinations) | FIXED(17) · LOCAL — the close gate requires the shape AND a non-null parse, and its message says `write $10`; `test_a_bare_integer_before_usd_is_refused_at_the_close_not_stored_as_null` |
| 129 | CHANGELOG, DECISIONS and CLAUDE.md are not in the review diff (seat B, pass 17 — informational) | REFUTED — the review's surface is the 11 paths named at Phase 0; those three files carry the entry, the D-179 row and the template line in commit fc68b445 (`git show fc68b445 --numstat`: 8 files), outside the finder surface by design |
| 130 | `cost: $0.30.` — a sentence-final period on the LAST field survives the field trimming and the strict shape refuses the doc's own example (seat B, pass 18 — reproduced through the CLI) | FIXED(18) · LOCAL — one trailing period is stripped before the gate; `10 usd.` still refuses; `test_a_sentence_final_period_after_the_cost_does_not_refuse_the_close` |
| 131 | the stalled pass-18 seat A, told to work only on copies, deleted the 13-line strict-cost gate from the LIVE `scripts/command_run.py` while "neutering a copy" — caught by the suite (3 red) and the commit script's green-marker gate before anything was committed (orchestrator, self-found) | FIXED(18) · LOCAL — the file restored from HEAD (my own committed file, not a sibling's WIP), the pass-18 fix re-applied; process: a native finder is not sandboxed by the harness — every later brief says the live tree is read-only and any mutation runs on a copy under the scratchpad, and the orchestrator diffs the surface against HEAD before each commit (the numstat snapshot did exactly that) |
| 132 | `cost: .` strips to empty and passes the gate as an absent field (seat A, pass 19 — reproduced on a copy) | FIXED(19) · LOCAL — a value that vanishes under the strip is refused; `test_a_lone_period_cost_is_refused_not_treated_as_absent` |
| 133 | a 320-digit amount overflows to `inf`, passes the gate (not None) and poisons the summed cost (seat B, pass 20 — reproduced through the CLI) | FIXED(20) · LOCAL — `_cost_usd` returns None for a non-finite value (so the gate refuses), and the report never sums a non-finite `cost_usd` from an older row; tests in both files |
| 134 | the report's `_tok_total` crashes the whole report on an `Infinity` token field in any older row — the read-side mirror of the write-side guard (seat B, pass 21 — reproduced) | FIXED(21) · LOCAL — non-finite token fields make the row a run without token data; `test_non_finite_or_oversized_numbers_in_old_rows_never_crash_the_report` (red on the HEAD copy, green on the fix) |
| 135 | the report's `_cost()` crashes on a 320-digit plain JSON integer (`float()` overflows before `isfinite` runs) (seat B, pass 21 — reproduced) | FIXED(21) · LOCAL — the overflow is caught and the row's cost is not summed; same test |
| 136 | the pass-21 guard covered `cost_usd`/`tok_*` only — a string, list, NaN, Infinity or 400-digit `wall_s`, `rounds` or `ts` (under `--since`), or a non-list `models`, still raised out of `build()` and took the whole fleet-wide report down on ONE bad row (seat B, pass 22 — reproduced: 10 of 12 inputs) | FIXED(22) · CLASS — one `_num()` guard reads every numeric cell (bad ⇒ 0, no datum), `models` iterated only when a list; `test_malformed_wall_rounds_ts_models_in_old_rows_never_crash_the_report` (14 rows × three invocations, seen red first: `ValueError: could not convert string to float: 'x'`) at b5a48972 |
| 137 | the pass-22 `_num()` default of 0 fed a malformed `wall_s`/`rounds` to the medians as a phantom run — one real 10-min run + one string `wall_s` printed `5.0 min` for 2 runs, no denominator, and the pass-22 CHANGELOG sentence claimed 'no datum' (seat B, pass 23 — reproduced) | FIXED(23) · CLASS — `_num()` returns None, `walls`/`rounds` keep finite values only, the table prints `median wall (rows)` / `median rounds (rows)` (`10.0 min (1)`); `test_a_malformed_wall_is_no_datum_never_a_phantom_zero_in_the_median` seen red first (`5.0 != 10.0`), both tests red on a HEAD copy; CHANGELOG sentence corrected — db58efce |
| 138 | `\d` in the cost grammar matches full-width/Arabic-Indic digits and `float()` accepts them, so `cost: $５.００` passes the strict gate (seat B, pass 23 — informational) | REFUTED — the value stored is the RIGHT number (5.0); the gate exists to stop WRONG numbers from prose, not to police the script of a correct one; `re.ASCII` would refuse a correct amount for no defect closed — not adopted |
| 139 | with NO timed row the table printed `0.0 min (0) | 0.0 min` — the median cell disclosed its (0) but the max cell beside it read like a real zero-minute run, the phantom shape pass 23 removed from the median (seat B, pass 24 — reproduced) | FIXED(24) · LOCAL — no timed row ⇒ JSON null and `—` for median, max and rounds median (the file's own `pool $` convention); `test_no_timed_row_renders_a_dash_never_a_zero_minute_run` seen red first — 4764051e |
| 140 | `_line_epoch` took the FIRST `"timestamp"` match in the line; Claude Code serialises `message` (every tool_use input inside it) BEFORE the envelope's own stamp, so a tool call carrying a `timestamp` argument mis-windowed its message both ways — a 23:00 message with a nested 10:30 stamp billed to the 10:00–11:00 run, a 10:30 message with a nested 2020 stamp dropped from it, no `tok_partial` either way (seat A, pass 25 — reproduced; NOT the compaction-lag class: a false match, not a stale line) | FIXED(25) · CLASS — one match keeps the fast path; several ⇒ the line is parsed and only the top-level key counts; unparseable ⇒ no epoch, never a guess. Fire rate measured: 2,226 of 261,368 live lines carry two keys, 0 assistant — real, rare, the parse is free on the 750 MB scan. `test_a_timestamp_key_inside_a_tool_input_never_stands_in_for_the_envelopes` seen red first (the nested 2020 stamp returned), red on a HEAD copy; CHANGELOG sentence; forced sync 45/45 — 6e50125f |
| 141 | the pass-25 fast path read 'one regex match ⇒ the envelope's' — false when the envelope's own `timestamp` is present but not in the regex's shape (a number, `2026-09-07`) and a nested tool-input stamp is: the nested one was promoted (seat A, pass 26 — reproduced) | FIXED(26) · CLASS — the fast path also requires exactly one `"timestamp"` key in the raw line; any second key of any shape parses the line and only a top-level STRING counts; `test_an_envelope_stamp_of_another_shape_is_never_replaced_by_a_nested_one` seen red first, red on a HEAD copy; live: 0 of 101,999 assistant lines leave the fast path — fb6d2e10 |
| 142 | `RecursionError` from `json.loads` on a pathologically nested line escaped `_line_epoch`'s except tuple and crashed the close itself — the sibling of the OverflowError case two paragraphs below (seat A, pass 26 — reproduced at 50,000 levels) | FIXED(26) · LOCAL — in the tuple; the same test's 200,000-level line has no epoch and no exception — fb6d2e10 |
| 143 | `--surface "--terminal"` makes argparse exit 2 (`expected one argument`) on every start/close site (seat B, pass 26 — reproduced) | REFUTED — argparse's own rule and its own loud rc 2, never a silent wrong value; a value beginning with `--` is not a spec, plan dir, ticket or diff range, and `--surface=VALUE` is argparse's documented escape — no defect closed by special-casing it |
| 144 | `_envelope()` in the pass-25 test does not reproduce a live line's full key order (`parentUuid, isSidechain, message, …, uuid, timestamp`) (seat A, pass 26 — informational) | REFUTED — the property the test pins is `message` (and every tool_use input in it) serialised BEFORE the envelope's `timestamp`, and the helper does exactly that; the missing keys carry no stamp and cannot change a match |
| 145 | a line with NO envelope `timestamp` and exactly one nested stamp still takes the fast path and adopts the nested one (orchestrator probe, pass 26) | REFUTED — accepted limit with its denominator: every Claude Code record carries an envelope stamp; live 2,525 of 261,430 stamped lines lack a top-level string stamp and 0 of them are assistant lines, the only lines the sum reads; telling one match apart from an absent envelope needs a parse of every line, which the 750 MB scan is designed not to pay |
| 146 | `_close` appended the fleet-wide ledger row (`state: done`, cost, tokens) BEFORE `save()` flipped the record; a failed save (read-only dir, disk full) still printed 'run record closed' with rc 0 — the record stayed `running`, the Stop hook kept blocking with no visible reason, and the retry appended a SECOND row (seat B, pass 27 — reproduced end to end on a chmod-555 record dir) | FIXED(27) · CLASS — `save()` first for both the plain and the nested-parent branch; an unpersisted close prints `NOT CLOSED /<cmd> …`, exits 1 and writes no row, so the retry writes exactly one; `test_an_unpersisted_close_writes_no_ledger_row_and_never_claims_closed` seen red first (rc 0 + 'run record closed'), red on a HEAD copy; CHANGELOG sentence; forced sync 45/45 — e2b02d61 |
| 147 | `MemoryError` from `json.loads` on the parse path is outside `_line_epoch`'s except tuple and nulls the WHOLE row via `_sum_transcript_usage`'s outer `except Exception` instead of one line (seat A, pass 27 — reproduced only by monkeypatching `json.loads`; not reachable from a ≤8 MiB line on this box) | REFUTED — a MemoryError is a PROCESS condition, not a property of one line: nulling the row (fail-soft, no crash, no wrong number) is the honest outcome, whereas skipping 'the one line' would print a lower-bound total with no `tok_partial` flag; `RecursionError` is different in kind — a per-line pathological shape the parser refuses while the process is healthy |
| 148 | the `run_close` kaizen event was queued before `save()` and flushed by `main()`'s `finally` even on a NOT CLOSED close — against the protocol's own contract that a close that did not happen emits nothing (seat A, pass 28 — reproduced) | FIXED(28) · CLASS — the NOT CLOSED path drops its queued event (`e[1] is not fields`), so the retry emits exactly one; the pass-27 test extended (0 events on the failed close, 1 on the retry, via `KAIZEN_EVENTS_DIR`), seen red first, red on a HEAD copy; protocol doc § Events sentence — 2356fc61 |
| 149 | the pasteable FEEDBACK line printed BEFORE the NOT CLOSED verdict — a line for a close that did not happen, indistinguishable in shape from a real one (seat A, pass 28 — reproduced) | FIXED(28) · LOCAL — the line is built before the save and printed only after the record persisted, ahead of the DONE/resuming line; same test (`FEEDBACK:` absent on the failed close, before `DONE` on the retry) — 2356fc61 |

## Per-phase verdicts

### Phase 0 — Establish scope: CLEAN (hub identity; the range diff materialised at the sha for every finder; the prior receipt's `Surface:` was a HEAD hash of an uncommitted diff and could not match a committed range, so pass 1 ran WIDE)
### Phase 1 — Independent finders (wide pass 1): FIXED r1 (pool 3/3 returned + native opus + native sonnet + orchestrator probes; 33 candidates raised)
### Phase 2 — Verify / refute: FIXED r1 (13 refuted, each with an executed proof or a cited contract line)
### Phase 3 — Prove & fix: FIXED r1 (20 fixed at 180b38f6, 14 regression tests seen red first, 347 tests green, sync 45/45)
### Phase 4 — Converge: CONVERGED (29 passes; the exit is pass 29 at 2356fc61 — two author-blind native seats on disjoint partitions + the orchestrator's re-derivation, found: 0 · new: 0 · fixed: 0; 149 candidates adjudicated: 82 FIXED, 56 REFUTED with an executed proof or a cited contract line, 11 cited standing rows; every fix seen red first and red on a HEAD copy; 14 of 14 classes clean on the run record's ledger)

## Gate

`final_gate.py --check --json`, pasted verbatim at the flip (check_convergence reads the fenced
`"status": "success"`):

```json
{
 "status": "success",
 "tier": 2,
 "passed": 60,
 "failed": 0,
 "skipped": 1,
 "skipped_checks": [
  "pytest"
 ],
 "failures": []
}
```

(`python3 scripts/final_gate.py --check --json` in the main checkout at HEAD 44756889 — the reviewed range 29943db2..2356fc61 plus the ledger-path fix 44756889 that its Doc Link Integrity check demanded — with this receipt in the tree, 2026-09-07. A clean worktree at the same HEAD reports eight refs to gitignored kilo caches and the fabrik-lib sibling as broken — artifacts the live tree has and the surface never touched — so the live run is the honest one here.)
