# Review — session_orient.py gains the ledger-first line (hook surface → full review per sizing rule)

Surface: working-tree diff, one file — .claude/hooks/session_orient.py (a 4-line prose addition to
the ORIENT text block + one parenthetical on the session-recall line), pre-commit atop 278125f8.
Mode: NO-POOL — solo native.
Why: the operator's "are you sure you have built what we needed?" audit found the orient hook still
steering every fresh/post-compact session to session-recall with no ledger mention — the Lesson-143
stale-twin class (a second copy of guidance keeps teaching the retired order). The duty text in the
48 CLAUDE.md files was already live; this closes the one remaining steering surface.

## Rubric (review_rubric.py --changed .claude/hooks/session_orient.py — verbatim section heads)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: .claude/hooks/session_orient.py)
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | security-auth floor (35) | CLEAN | prose string constants only; no auth/token surface in the diff |
| 2 | data-postgres floor (25) | CLEAN | no DB; grep of the diff for sql/engine: 0 |
| 3 | ops floor (30) | CLEAN | grep of session_orient.py's diff for compose/ports/docker/traefik: 0 — a stdout text block, no deploy artifact touched |
| 4 | 12-Factor | CLEAN | a hook emitting stdout text; no process/config/log change |
| 5 | python discipline (10) | CLEAN | string-concatenation style matches the surrounding block byte-for-byte; ast.parse clean |
| 6 | hook-safety (the escalation reason) | CLEAN | the edit touches ONLY the ORIENT text constant — no control flow, no exit codes, no cause logic; the hook's failure modes are unchanged by construction (diff is entirely inside one string expression) |
| 7 | steering consistency | CLEAN | the new line matches the CLAUDE.md duty verbatim in substance (ledger → recall → hunt); the session-recall line now says "(AFTER the ledger)" so the two lines cannot be read as competing orders — the exact mismatch this fix removes |
| 8 | fail-open/fail-closed | CLEAN | no gate/guard semantics in the diff |
| 9 | boundary/sentinel/prefix | CLEAN | no markers/regexes; plain prose lines inside the existing bullet list |
| 10 | behavior-without-a-test | CLEAN | orient text is prose consumed by agents; its mechanical guard is syntax (ast.parse run) + the hook exercising itself at every SessionStart; no testable behavior added |
| 11 | cost/quota | CLEAN | none |
| 12 | fleet lens | CLEAN | .claude/hooks is a governance-sync trigger — the line is correct for all 12 types (the ledger is universal); project copies get it on this commit's sync |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — all 12 classes over the 1-file diff | citation | solo native (NO-POOL; dispatched: 0) | 0 | 0 | 0 | |
| Pass 2 | CLOSING — re-derivation: ast.parse re-run; the emitted ORIENT text rendered by importing the module constant and grepping both lines present in order (ledger line BEFORE recall line); gate 55/0 fresh | method: re-derivation | 0 | 0 | 0 | TERMINAL |

## Step verdict

Step verdict: PASS — the last steering surface now orders ledger-first, everywhere, from the next
SessionStart onward.

```json
{"status": "success", "tier": 2, "passed": 55, "failed": 0}
```
