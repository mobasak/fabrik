# T01 acceptance review — `decisions.py --merge-owner` (plan 2026-09-06-plan-2-multi-agent-adoption)

**Status:** CONVERGED
**Surface:** `git diff ca4ec9ed..worktree-agent-aae9de1c5861d11c6 -- scripts/decisions.py tests/test_decisions_helper.py` (10,709 B; +136/−27 over 2 files; coder chain: pool attempt 1 deepseek-v3.2-exp died idle after 7 turns → pool attempt 2 gpt-5.6-luna returned an untested 15 KB diff whose tests referenced undefined names (the write sandbox cannot run pytest — filed to intel 01M1V98VN3QNS73GCR3GYF9FXH) → native Sonnet fixup ffd123f4 restored the 20 pre-existing tests the pool diff had deleted and re-authored the three merge-owner tests).

## Coverage Checklist

Rubric invocation (verbatim head of the output):

```text
$ python scripts/review_rubric.py --changed scripts/decisions.py tests/test_decisions_helper.py
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
```

Summary: FLOOR core/35-security-auth.md · core/25-data-postgres.md · core/30-ops.md · 12-FACTOR; MATCHED core/10-python.md.

| Class | Status |
|---|---|
| core/10-python.md — uv only / `datetime.now(UTC)` / no logfile / ruff sets | CLEAN (stdlib; no datetime; ruff + format clean in the worktree) |
| core/40-documentation.md — trailers, one CHANGELOG entry (orchestrator-applied at merge) | CLEAN (trailers parse: `Agent-Role: subagent`, `Agent-Task: T01`) |
| FLOOR 35/25/30 — no auth, no DB, no service; secrets never read | CLEAN (no surface) |
| 12-FACTOR — XI stdout only; III no constants beyond the regex | CLEAN |
| Recurrence: Behavior Contract row without a killing test (mutation probe) | FIXED r1 → r2 (the short-row guard at :181 had no killing test — native (2)) |
| Recurrence: Touches discipline / governance files / spine / lock untouched | CLEAN (`--name-only` = the ticket's Touches) |
| Recurrence: fleet blast radius of a synced surface (single-session repo prints/writes nothing) | CLEAN (a new CLI flag; `_rows/_query/_check/_next_id` have zero diff hunks — native (7)) |
| Recurrence: proxy-as-evidence — the ticket gate RUN in the coder's worktree by the orchestrator | CLEAN (20 passed; ruff check + format clean; CLI probed on a fixture ledger: `beta` / `UNDECLARED` rc 3 / unreadable rc 1) |
| Recurrence: denominator on every count | CLEAN — every count in this artifact carries its population: diff bytes/lines (`git diff --numstat`), tests passed of collected (`pytest -q` tail), finders usable of dispatched (the trio table), pids scanned (859) |
| Recurrence: fail-open/fail-closed — a swallowed error that reads as success | CLEAN (an unreadable ledger exits 1 with the stderr line — executed; a 3-cell row is guarded and now tested) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted | CLEAN (pool r1 4/4/0, r2 1/3/1 scored; native opus×1 r1; r2 closing layer = the orchestrator's executed verification) |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | CLEAN (`^` + `.match` double-anchored; `len(cells) > 3` guard tested r2) |
| Recurrence: behavior-without-a-test — a contract row no test kills | FIXED r2 (the guard's test) |

## Round 1 — over `ca4ec9ed..ffd123f4`
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max (error, empty) + native opus×1 — round 1.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): CLEAN — the three rows → their tests named; Touches verified. Accepted.
- gemini-3-flash (4/5): CLEAN with per-row `path:line` (:183 last-match + strip, :185 anchored match, :175 unreadable exit 1, :34 regex sync + AFTER-EDIT). Accepted.
- qwen3-max (0/5): returned an error payload — no coverage; the batch is PARTIAL (2 of 3 usable).

### Native finder (opus) — 8 hunt items executed on a scratch copy; 2 raised
- (1) mutation probes (a) first-match, (b) no `*` strip, (c) exit 0 for UNDECLARED → each killed by its test; (d) `.match`→`.search` is an EQUIVALENT mutant (`^` still anchors; dropping both fails the test) — CLEAN.
- (2) **DEFECT M — the `len(cells) > 3` guard at `scripts/decisions.py:181` has no test**: removing it leaves 19 passed while a real 3-cell row above a valid `MERGE OWNER:` row raises IndexError (proven on the copy). → FIX routed to the coder: a short-row test. (Escaped `\|` in an EARLIER cell shifts `what` to cells[4] → silent UNDECLARED — inherited `_rows` behaviour the DO-NOT forbids changing; contract-conformant, noted.)
- (3) unreadable path: `:160` and `:177` byte-identical (`cat -A`); exit 1 proven — CLEAN. (4) path shape: a ledger FILE path works like `--next-id` — CLEAN. (6) AFTER-EDIT header names docs_updater.py — CLEAN. (7) ruff/format/mypy clean, stdlib only, 20 passed — CLEAN. (8) red-first: all three new tests fail on the base script (argparse `SystemExit: 2` — a weak red; the behavioural reds are the mutation probes above).
- (5) **DEFECT L — Touches discipline by hunk**: `--numstat` on the test file is 107/26 because ~50 lines are a voluntary `ruff format` reflow of five pre-existing tests (tests/ is outside `final_gate.py:2216 _RUFF_ROOTS`, so not gate-induced). → FIX routed: revert the unrelated hunks.

Verdict r1: 2 defects → fixup dispatched to the same coder (session alive, D2 routing). Board T01 🟡 → 🔵.

## Round 2 — over `ca4ec9ed..b7ffb88a` (6,088 B; +29/−1 script, +62/−0 tests — purely additive over the base test file)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own EXECUTED verification (the round-1 fixes were prescribed verbatim; the closing layer re-ran them rather than re-deriving a brief) — round 2.

### Adjudication (pool layer)
- deepseek-v4-flash (1/5): three H-confidence rows — "the last match is not stored" (REFUTED: `test_merge_owner_prints_last_matching_owner_and_strips_leading_stars` proves `beta` over an earlier `alpha`), "missing-file crash" (REFUTED by execution below), "`read_text` result discarded" (NOTED, cosmetic: the read is the probe that mirrors `_next_id`'s error path byte-for-byte; `_rows` re-reads by design).
- gemini-3-flash (3/5): CLEAN. Accepted.
- qwen3-max (1/5): "falls through to `_rows` and crashes on a missing ledger" — REFUTED by execution: `--merge-owner <repo-without-ledger>` → stderr `decisions: cannot read …`, rc 1, no traceback.

### Orchestrator execution (the closing layer)
- `pytest tests/test_decisions_helper.py -q` in the coder's worktree → **21 passed**; `ruff check` clean; `git diff ca4ec9ed..b7ffb88a --numstat -- tests/test_decisions_helper.py` → `62 0` (the round-1 reflow reverted; the file is the base plus the T01 block).
- r1 (2): `scripts/decisions.py:181 if len(cells) > 3:` present and `test_merge_owner_skips_a_short_row_above_the_matching_one` kills its removal (the coder quoted the IndexError from the removed-guard run).
- r1 (5): additive tests, zero deletions — CLEAN.
- The CLI probed on a fixture ledger: `beta` (last, bold-stripped) rc 0 · `UNDECLARED` rc 3 · unreadable rc 1 with the stderr line.
- `ruff format --check` on the test file: pre-existing drift at ca4ec9ed (the five compact-call tests), no new class — out of scope, stated.

Verdict r2: found: 0, fixed: 0 — **CONVERGED**. Merge target: `worktree-agent-aae9de1c5861d11c6` @ b7ffb88a.

## Pass Ledger
| Pass | Layer | Method | Findings → fixed |
|---|---|---|---|
| Pass 1 | pool ×3 (2 usable) + native opus×1 | method: re-derivation (mutation probes on a copy; anchors executed) | 2 → 2 |
| Pass 2 | pool ×3 + orchestrator execution | method: re-derivation (tests re-run, numstat re-counted, CLI re-probed) | 0 → 0 |
