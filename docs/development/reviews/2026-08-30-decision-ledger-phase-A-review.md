# Review — plan-1 decision-ledger, Phase A (helper + hub ledger + registry + template)

Surface: working-tree Phase-A diff — scripts/decisions.py (new) · tests/test_decisions_helper.py (new) ·
docs/DECISIONS.md (new, 11 seed rows) · scripts/enforcement/_doc_registry.py (+1 DocRow) ·
templates/scaffold/docs/DECISIONS_TEMPLATE.md (new) · INDEX.md (2 rows) · CHANGELOG.md (whole-plan entry,
moved from Phase C by the ERROR-tier same-change law) — pre-commit, baseline dd6e6172.
Mode: NO-POOL (operator standing directive) — solo native; closing pass re-derivation.
Prior-report anchor: none for this surface (all-new files).

## Rubric (review_rubric.py --changed <the 7 paths> — verbatim section heads)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: scripts/decisions.py, scripts/enforcement/_doc_registry.py, tests/…)
### core/40-documentation.md  (hit: docs/DECISIONS.md, templates/…, INDEX.md, CHANGELOG.md)
### core/45-testing-strategy.md  (hit: tests/test_decisions_helper.py)
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | security-auth floor (35) | CLEAN | read-only stdlib CLI, no auth/token/secret surface; grep of the diff for jwt/secret/token: 0 |
| 2 | data-postgres floor (25) | CLEAN | no DB — the ledger's whole design point; grep for sql/engine/psycopg in the new files: 0 DB-API uses |
| 3 | ops floor (30) | CLEAN | no compose/ports/deploy surface |
| 4 | 12-Factor | CLEAN | helper: stdout-only (XI), no daemon (VIII), no config groups (III); positional `--root` for tests, `/opt` default is a path arg not config |
| 5 | python discipline (10) | CLEAN | full type hints, `list[str]`/`| None` forms, module-level compiled regexes, AFTER-EDIT header; print via the ASCII `_say` guard (enforcement-CLI convention, not service code) |
| 6 | testing (45) | CLEAN | 5 behaviors red-first (collection red WATCHED, then 5/5); factory-free tmp fixtures; standalone-CLI behavior pinned via subprocess; no print in tests |
| 7 | fail-open vs fail-closed | CLEAN | query path always exits 0 (a missing ledger is rolling adoption, silently skipped — by design and TESTED); only `--check` has a failing exit, and it fails CLOSED on a dangling pointer |
| 8 | boundary/sentinel/prefix | CLEAN | ROW_RE anchors `^\| D-\d+ \|` — header/separator rows carry no id and never match (exercised by fixtures); SUPERSEDES_RE case-insensitive; no-match query prints the honest denominator line instead of silence |
| 9 | data-integrity (the seed rows) | CLEAN | every `where` sha re-verified in git log this session (74ad8a06 · 025cbb20 · 7862d8a2 · 5f07370a · 21f37809 · 730a1af5 · 8760e000 · 65c50e23 · 4b97f5e4 · 188d8fc9 — the last grepped from history for D-001); D-010..D-000 ids monotonic, no supersede pointers yet so `--check` green is trivially true and SAYS so here |
| 10 | registry coupling | CLEAN | DocRow mirrors the LESSONS row shape exactly; template file exists (test-enforced — tests/test_doc_registry.py 14/14); allowlist 16 → 17 probed |
| 11 | behavior-without-a-test | CLEAN | 4 planned behaviors + the standalone-CLI run all have rows; the registry add is pinned by the existing hub-side suite |
| 12 | cost/limit edges | CLEAN | no spend, no subprocess beyond the test's own |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — all 12 classes, every new file read line-by-line | citation | solo native (NO-POOL; dispatched: 0) | 1 | 1 | 1 | the where-cell print used `padded[5]` which mis-selects on a 7-cell row — verified live output instead: cells are exactly 6 by ROW_RE construction, but the CHANGELOG-in-A move (doc-law override of the plan's C placement) was recorded as a deviation note in the plan file rather than silent |
| Pass 2 | CLOSING — re-derivation: suite re-run 5/5 + 14/14; live query re-run (`context7` → the D-003 row, fleet root); `--check` exit 0; allowlist re-probed 17/DECISIONS; INDEX rows grepped present; gate 55/0 fresh | method: re-derivation | 0 | 0 | 0 | TERMINAL |

Pass 2 = the no-op: `new: 0`, everything adjudicated.

## Phase A verdict

Phase A verdict: PASS — clean. The ledger answers a real operator-shaped question
(`decisions.py context7`) on its first day, and the one mechanical check is live.

```json
{"status": "success", "tier": 2, "passed": 55, "failed": 0}
```
