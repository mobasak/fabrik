# T02b acceptance review — `--adopt` backlog tagging (plan 2026-09-06-plan-2-multi-agent-adoption)

**Status:** CONVERGED
**Surface:** `git diff c2631de2..worktree-agent-aa2502f9469ff9223 -- scripts/docs_updater.py tests/test_docs_updater_adopt.py` (r1: +195/−10 script, +233 tests; native Sonnet coder cdebb51c, NO-POOL — the write sandbox cannot run pytest). Base = c2631de2 (T02a merged).

## Coverage Checklist

Rubric invocation (verbatim head of the output):

```text
$ python scripts/review_rubric.py --changed scripts/docs_updater.py tests/test_docs_updater_adopt.py
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
```

| Class | Status |
|---|---|
| core/10-python.md — uv only / stdlib / no logfile / ruff sets | CLEAN (stdlib; ruff + format clean in the worktree) |
| core/40-documentation.md — trailers, one CHANGELOG entry (orchestrator-applied) | CLEAN (trailers parse; Deltas block carries the entry) |
| FLOOR 35/25/30 · 12-FACTOR | CLEAN (no surface) |
| Recurrence: Behavior Contract row without a killing test (mutation probe) | FIXED r1 → r2 (native: 2 of 10 mutants survived — the struck-row skip and the untagged-legend guard) |
| Recurrence: Touches discipline / governance files untouched | CLEAN (`--name-only` = the two Touches; the real backlogs were only COPIED into fixtures) |
| Recurrence: fleet blast radius of a synced surface | CLEAN (native (8): a repo with no backlog → rc/stdout/tree byte-identical to c2631de2; native (2): 129 of 129 changes on real hub/transdoc/seo copies are pure tag insertions — 0 legend/header/fence/struck rows touched) |
| Recurrence: proxy-as-evidence — the ticket gate RUN in the worktree by the orchestrator | CLEAN (71 passed; ruff check + format clean) |
| Recurrence: denominator on every count | CLEAN — every count carries its population: 129/1383 hub lines, 25/65 transdoc, 18/78 seo, 1314/8589 fleet-wide, 4 of 54 compound-tag bullets, mutants 8 of 10 killed |
| Recurrence: fail-open/fail-closed | CLEAN (a missing backlog is silently nothing by contract; the skip grammar is the fail-closed side — widened r2 for compound tags) |
| Recurrence: cost/quota accounting | CLEAN (pool r1 4/1/2 scored; native opus×1 r1) |
| Recurrence: boundary/sentinel/prefix | FIXED r1 → r2 (native D3: `_BACKLOG_ALREADY_TAGGED_RE` too narrow for `[fleet+infra]`-style tags; the coder's own D5: the hub header's tag cell is named `Owner`, not `Tag` — the ticket text was wrong, recorded as a plan defect) |
| Recurrence: behavior-without-a-test | FIXED r1 → r2 (native D1/D2; pool D6 the shared round-robin counter) |

## Round 1 — over `c2631de2..cdebb51c`
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): CLEAN — the four rows → their tests; the already-tagged regex read. Accepted.
- gemini-3-flash (1/5): a one-line CLEAN with no evidence — no coverage credited.
- qwen3-max (2/5): (1) "`[ ]` matches the already-tagged regex" — REFUTED (a space is outside `[a-z0-9-]{1,32}`; `[X]` likewise); (2) the round-robin counter could reset per shape and the fixture would not notice — ACCEPTED (D6, an interleaved fixture).

### Native finder (opus) — 10 mutation probes on a copy (8 killed), real-corpus runs on hub/transdoc/seo copies; 4 raised
- **D1 H** — `:1010` the struck-row skip: `if False` leaves 47 passed; 16 of 16 struck bullets fleet-wide depend on it. **D2 H** — `:988` the legend guard: never exercised because every legend fixture row is already tagged. **D3 M** — `:951` the skip grammar rejects compound tags (`[infra/docs]`, `[fleet+infra]`, `[intel→fabrik-lib]`: 4 of 54 hub bullets would get a second owner). **D4 H** — the BC1 docstring quotes an `AttributeError` red that cannot occur (the real red is an AssertionError at :813).
- CLEAN by execution: idempotency on the three real copies (3); report rows 1:1 with changes, excerpts ≤60 (4); the interface signature (5); Touches/lint/71 passed (6); blast radius vs c2631de2 (8).
- Plus the coder's own grounding note, accepted as **D5**: the hub table header is `| Effort | Owner | Item | … |` — the ticket's `Tag` column name exists only in the legend; the tag cell is detected by `Tag` OR `Owner` from r2. Recorded as a plan-text defect (the T02b Scope paraphrased the header).

Verdict r1: 6 items (4 native + the coder's D5 + the pool's D6) → fixup dispatched to the same coder. Board T02b stays 🔵.

## Round 2 — over `c2631de2..3ba09e23` (the round-1 fixup 3ba09e23 by a FRESH coder: the first coder died on a rate limit mid-round then was stopped; its r1 branch cdebb51c merged in, its uncommitted test edits salvaged to `.fabrik/plan-locks/<stem>-salvage-T02b.diff` (82 lines) and applied cleanly — the D6 salvage procedure; script +231/−10, tests +345 over the ticket)
Finders: pool deepseek/deepseek-v4-flash (google/gemini-3-flash-preview + qwen/qwen3-max returned errors — the batch is PARTIAL, 1 of 3 usable) + native opus×1 — round 2.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): the four rows → their tests with line anchors (`:766-811`, `:827-845`, `:858-877`, `:890-907`), each stated to fail on regression. Accepted.
- gemini-3-flash (0/5), qwen3-max (0/5): error payloads, no coverage.

### Orchestrator execution
- In the coder's worktree: `pytest tests/test_docs_updater_adopt.py tests/test_docs_updater.py -q` → **83 passed**; ruff check + format clean; `_BACKLOG_ALREADY_TAGGED_RE` at `:960` is the widened form; `_BACKLOG_TAG_HEADER_NAMES = {"tag", "owner"}` at `:972` (D5).
- Fleet state after the T02a/T04 merges (T06 evidence, measured now): 45 of 47 project copies of `scripts/docs_updater.py` carry `run_adopt`; 45 project hooks carry `_sessions_line`; the rendered `/fabrik-vision` names `docs/development/PLANS.md` 4 times.

### Native finder (opus) — the six round-1 mutants re-applied on a copy: ALL KILLED by their named tests (D1 struck bullet; D2 untagged legend; D3 the four compound params + the three checkbox variants; D4 the quoted red re-run verbatim against c2631de2; D5 the real hub header → `table-tag`; D6 the interleaved counter). Corpus re-run on copies: hub 82/1383, transdoc 20/65, seo 18/78 lines changed, 0 header/legend/separator/fence/struck rows, the four compound-tag bullets untouched, second run byte-identical 3/3. **2 raised:**
- **DEFECT-1 H** — `:960` (used at `:1013`): the WIDENED regex — the round-2 prescription's own defect — is `search()`ed over the whole row, so prose brackets read as "already tagged": fleet-wide over 25 backlogs / 1618 candidate rows, 58 rows lost vs the round-1 regex, only 4 of them the intended compound tags — 54 false skips across 15 repos (`[key: string]`, `[tool.ruff]`, `[0.831, 0.940]`, a leading `[see the docs](x.md)` link, a `[2026-09-06]` date). → FIX at the mechanism: decide only at the row's tag POSITION with a beat-shaped head; dates and uppercase heads are not tags.
- **DEFECT-2 low** — three line-number citations drifted with the docstring growth (`:813`→`:821`, `STRATEGIC_BACKLOG.md:33`→`:34`).
- CLEAN: `tests/test_docs_updater.py` byte-unmodified (23 passed); the salvage applied with no duplicate names or orphan halves; Touches exact; ruff clean; stdlib only.

Verdict r2: 2 defects (one a regression of the round-2 brief itself, recorded as such) → fixup dispatched to the same coder. Board T02b stays 🔵.

## Round 3 — over `a1720dd8..7a4526e0` (the round-2 fixup 7a4526e0: script +246/−10, tests +451 over the ticket; 109 passed)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own EXECUTED verification (the native Opus seat dispatched for this round stalled at a 130-byte transcript for 15 minutes and was stopped; two native Opus rounds preceded — r1 and r2 — and this round's decisive check, the fleet re-measurement, was RUN by the orchestrator) — round 3.

### Adjudication (pool layer)
- deepseek-v4-flash (3/5), gemini-3-flash (4/5 — read the anchored mechanism and its negative lookaheads), qwen3-max (3/5): all CLEAN. Accepted.

### Orchestrator execution (the closing layer)
- In the coder's worktree: `pytest tests/test_docs_updater_adopt.py tests/test_docs_updater.py -q` → **109 passed**; ruff check + format clean; `--name-only` = the two Touches.
- DEFECT-1 by execution — `classify_backlog_row` on 14 shapes: `[see the docs](x.md)`, `[prometheus-app-metrics-setup.md](y.md)`, `[2026-09-06]`, `[key: string]`, `[WIP]`, `[x]`, `[ ]`, `[X]`, a `[key: string]` in prose → **bullet** (tagged); `[infra]`, `**[fleet+infra]**`, `[intel→fabrik-lib]`, `[infra/T16 decision]` → **skip**; a hub-shaped row with `[0.831, 0.940]` in prose → **table-tag**. 14 of 14 as the contract wants.
- **Fleet re-measurement (the decider):** the round-1, round-2 and round-3 taggers run on copies of all 25 backlogs (8,637 lines): tagged **1318 · 1238 · 1303**. Round 3 vs round 1: 26 rows lost, 11 gained. The 26: the 4 hub compound-tag bullets (intended) + 22 table rows whose `Owner` cell already holds a bold value (`| … | **iie1** |`, `| Rank | ID | Why | Owner |` rows) — round 1 had DOUBLE-tagged an owned cell, round 3 respects it (correct; headers verified). The 11 gained are rows round 1 skipped for a bracket in PROSE (`[0-9]+`, `[\w.:/-]`, `[id]`) — the round-2 false-skip class, now tagged. Second run byte-identical on **25 of 25**.
- DEFECT-2: the three citations replaced by grep-by-content (`grep '^| Effort \| Owner \| Item'`, the test method's name) — no line numbers to drift.

Verdict r3: found: 0, fixed: 0 — **CONVERGED**. Merge target: `worktree-agent-a1465c43f1a445a7f` @ 7a4526e0 (carries the r1 branch cdebb51c by merge). Fleet-synced RUN_SCRIPT → `sync_enforcement_to_projects.py --force` after the merge.

## Pass Ledger
| Pass | Layer | Method | Findings → fixed |
|---|---|---|---|
| Pass 1 | pool ×3 + native opus×1 | method: re-derivation (10 mutation probes; real-corpus runs on hub/transdoc/seo copies: 129/129 pure insertions) | 6 → 6 |
| Pass 2 | pool ×1 usable + native opus×1 | method: re-derivation (six mutants re-applied and killed; fleet-wide 1618 candidate rows counted: 54 false skips found) | 2 → 2 |
| Pass 3 | pool ×3 + orchestrator execution | method: re-derivation (14 shapes classified; 25 backlogs re-tagged under three tagger versions and diffed; idempotency 25/25) | 0 → 0 |
