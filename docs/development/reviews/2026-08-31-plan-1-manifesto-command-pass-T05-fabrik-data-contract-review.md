# T05 — /fabrik-data-contract: 63b manifesto conformance

Status: CONVERGED — 1 source fix + 4 anchor corrections + 2 cells re-argued; closing round new: 0

Surface: commands/_sources/fabrik-data-contract.md (221 lines, read in full; SOURCE-relative citations) + rendered composition (run-record + term-edit + questionbar + subagents-core + close-feedback — all swept by T01, questionbar and subagents-core carrying this pass's T01/T04 fixes at next render).
Outcome: 1 FIX + artifact corrections — fifth consecutive falsified stamp. Substantive: a FREEZE is a Status flip → the ledger row is owed SAME-change (source now says so). Method correction adopted for T06+: verdict anchors are grep-derived, never estimated.

## 63b Verdict Table

| intersection | verdict (SOURCE-relative citations) |
|---|---|
| (a) checkable gates | CONFORMS — term-edit's md5-verified edit-free no-op governs the FROZEN flip (:162, :189-191); the FREEZE CHECKLIST must be all-ticked (:140); the header-flip-after-no-op exemption is stated precisely so the anti-cheat cannot be gamed (:166-171) |
| (b) ledger routing + one-way field block | FIXED — the initial cell rested on a Phase-5 non-sequitur (verifier: :70 is about disposable implementations, not ledger exemptions); the sound mechanism is the ledger duty's own "approval/Status flip" trigger — and a FREEZE IS that flip, so Phase 4 now mints the docs/DECISIONS.md row in the SAME change (T03/T04's atomicity precedent); the re-freeze close-out (:206-224 post-fix) still routes downstream impact |
| (c) rigor scales with irreversibility | CONFORMS (cell re-argued per the verifier — modes select the field SOURCE, not a rigor tier): the correct argument is classification, not tiering — a data contract is a binding artifact every downstream agent builds against, one-way-by-nature, so the full md5-verified loop applied in EVERY mode IS the manifesto-correct depth (there is no legitimate fast-path freeze); Mode-B grandfathering (:30-31) remains the reversible handling of legacy deviation |
| (d) labeled verified/assumption evidence | CONFORMS — "Treat every field as unproven until read from the real source — a column name is not its type" (:58-59; anchors grep-corrected); external standards grounded LIVE with URL + date, injected-directive discard (:199); the ⚠ audit: annotation names the omission-reads-as-present trap (:109-112) |
| (e) captured disorder | CONFORMS — R1/R2 reconciliation notes "must reach the frozen file, not evaporate at emit" (:117); fleet-wide grandfather factoring (:113); Downstream impact table with "Zero hits is a stated result, never an omitted one" (:210-214) |
| (f) most-reversible default under ambiguity | CONFORMS — questionbar renders with the Invariant-3 default (T01); the ONE sanctioned stop-and-ask is precisely gated (already-FROZEN file only, :52-53); DRAFT-stub-is-not-a-STOP is the explicit reversibility carve-out to check-before-create (:48-52) |

6/6 adjudicated after verification: 5 CONFORMS · 1 FIXED. Fifth consecutive falsified stamp.

## Scoped verification review

| pass | finders | found | new | fixed | verdict |
|---|---|---|---|---|---|
| Pass 1 | 1 native author-blind verifier: every anchor grep-re-derived, ledger-exemption + kill-criterion + stall hunts | 6 | 6 | 6 | Y1/Y2/Y5/Y6 anchor imprecisions FIXED · Y3 (b) non-sequitur FIXED (source: freeze mints its ledger row SAME-change) · Y4 (c) category mismatch FIXED (cell re-argued: one-way-by-nature) · declined-ask default examined-sound (a legitimate irreversible-action gate) |
| Pass 2 (closing, method: gate) | mechanical: source clause greps clean; corrected anchors re-derived; diff prose-only | 0 | 0 | 0 | → EXIT |

## Per-finding disposition ledger

| # | finding | state |
|---|---|---|
| Y1/Y2/Y5/Y6 | four verdict anchors off-target (estimated, not derived) | FIXED — corrected to the verifier's grep lines; anchor-derivation discipline adopted for T06+ |
| Y3 | (b) rested on a Phase-5 misquote; the freeze Status-flip had no ledger instruction anywhere in the source | FIXED — Phase 4 now mints the row SAME-change, classified at mint |
| Y4 | (c) credited source-selection as rigor-scaling | FIXED — cell re-argued via one-way-by-nature classification |
