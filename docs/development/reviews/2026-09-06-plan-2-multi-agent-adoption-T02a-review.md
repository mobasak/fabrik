# T02a acceptance review — `docs_updater.py --adopt` (plan 2026-09-06-plan-2-multi-agent-adoption)

**Status:** CONVERGED
**Surface:** `git diff ca4ec9ed..worktree-agent-a8f1d9418d61c2357 -- scripts/docs_updater.py tests/test_docs_updater_adopt.py` (33,248 B; +658/−21 over 2 files; native Sonnet coder 8c839125 after the pool attempt (gpt-5.6-luna) died on a connection timeout — NO-POOL declared, the write sandbox cannot run pytest: 01M1V98VN3QNS73GCR3GYF9FXH). Fleet-synced RUN_SCRIPT — blast radius is the first-class class.

## Coverage Checklist

Rubric invocation (verbatim head of the output):

```text
$ python scripts/review_rubric.py --changed scripts/docs_updater.py tests/test_docs_updater_adopt.py
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
```

| Class | Status |
|---|---|
| core/10-python.md — uv only / `datetime.now(UTC)` / no logfile / ruff sets | CLEAN (stdlib; `datetime.now(UTC)` for the row date; `utcnow` absent — native (4)(10); ruff + format clean in the worktree) |
| core/40-documentation.md — trailers, one CHANGELOG entry (orchestrator-applied at merge) | CLEAN (trailers parse; the Deltas block carries the entry + one INDEX row) |
| FLOOR 35/25/30 — no auth, no DB, no service; secrets never read | CLEAN (the scan reads `comm` + `cwd` only — native (5)) |
| 12-FACTOR — XI stdout only; III no constants beyond the regex | CLEAN |
| Recurrence: Behavior Contract row without a killing test (mutation probe) | FIXED r1 → r2 (native D3 last-match, D4 placement, D5 seam literal, D6 scan exactness — five survivors of 12 probes) |
| Recurrence: Touches discipline / governance files / spine / lock untouched | CLEAN (`--name-only` = the two Touches; `tests/test_docs_updater.py` unmodified and green — native (11)) |
| Recurrence: fleet blast radius of a synced surface (single-session repo prints/writes nothing) | CLEAN (native (2): base vs new `--check`/`--sync` on bare + 4 fixtures — identical rc/stdout/files; an absent, directory, chmod-000 or binary DECISIONS.md → `None`, no crash) |
| Recurrence: proxy-as-evidence — the ticket gate RUN in the coder's worktree by the orchestrator | CLEAN (34 passed; ruff check + format clean) |
| Recurrence: denominator on every count | CLEAN — every count carries its population: diff bytes/lines (`git diff --numstat`), tests passed of collected, finders usable of dispatched, mutants killed of probed |
| Recurrence: fail-open/fail-closed — a swallowed error that reads as success | FIXED r1 → r2 (native D9: a REFUSED `epic_order` printed as an ordinary row with rc 0) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted | CLEAN (pool r1 4/4/3 scored; native opus×1 r1; the dead pool coder scored 0) |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | FIXED r1 → r2 (native D2 name validation, D6 `startswith` survivor, D8 `--adopt ""` falls through) |
| Recurrence: behavior-without-a-test — a contract row no test kills | FIXED r1 → r2 (native D1 idempotency with epics present is a blind spot of the BC2 fixture; D7 the phase-note reword) |

## Round 1 — over `ca4ec9ed..8c839125`
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): CLEAN — 7 rows → tests; blast radius reasoned. Accepted.
- gemini-3-flash (4/5): CLEAN — 7/7 + a stdlib inventory. Accepted.
- qwen3-max (3/5): (1) `comm` trailing newline → REFUTED (stripped; native (5) probed prefix/newline); (2) `_insert_owner_line` inside fences → REFUTED by the native probe (8 H1 shapes incl. fence-before-H1 all land after the H1); (3) id regex → self-refuted; (4) `--adopt` writes DECISIONS.md in a bare repo → self-refuted (an explicit act); (5) `sync_plans_index()` always called → NOTED, idempotent by `replace_block`'s guard; (6) BC7 test passes on old code → REFUTED (the coder's red run lists it failing on the base); (7) the AFTER-EDIT header edit as a Touches violation → REFUTED (the ticket mandates it); (8) self-refuted.

### Native finder (opus) — 12 mutation probes on a scratch copy (baseline 34 passed), 11 hunt items; 9 raised
- Killed: terminal-filter drop → BC1; `replace_block` guard drop → 2 tests; unconditional ledger write → BC2+BC3; refusal drop → BC4; header dropped from `empty` → BC3+BC7.
- **D1 H** — idempotency breaks whenever `epics/*.md` exists: `:1638` appends an `epic_order` row unconditionally (run 2 prints a table, not `(nothing to adopt)`; bytes unchanged). **D2 H** — no name validation: `'al|pha'` / `'-lead'` write an unparsable row, so every run appends another and the header stays UNDECLARED. **D3 H** — LAST-match-wins unguarded (`:962`; the T01→T02a seam fixture missing). **D4 H** — Owner placement unguarded (`:1512`; behaviour correct across 8 H1 shapes). **D5 H** — the T02a→T06 seam literal unguarded (`:1577`). **D6 low** — scan exactness/realpath unguarded (`:1484`). **D7 low** — `_PLANS_PHASE_NOTE` reword untested; the `NO_OWNER` comment at `:834` still says "tail sweep". **D8 low** — `if args.adopt:` lets `--adopt ""` fall through. **D9 advisory** — a REFUSED delegation prints as an ordinary row with rc 0.
- CLEAN by execution: blast radius (2), ledger ids and cells (4), the scan (5), the seed literal today (6), the subprocess call (7), the header (9), Touches + stdlib + `utcnow` absent (10), `tests/test_docs_updater.py` unmodified, 34 passed (11).

Verdict r1: 9 defects → fixup dispatched to the same coder (session alive, D2 routing). Board T02a 🟡 → 🔵.

## Round 2 — over `ca4ec9ed..e7429d43` (the round-1 fixup e7429d43: script +327/−22, tests +623 — 47 passed)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 2.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): CLEAN, naming the one real fleet edge — `scripts/epic_order.py` absent in projects when an epics dir exists — which the native finder measured live (3a). Accepted.
- gemini-3-flash (2/5): "`os.listdir(proc_root)` might raise" — REFUTED by its own reading (handled); "`--adopt` pollutes an empty project" — REFUTED, an explicit act on the operator's word.
- qwen3-max (3/5): CLEAN. Accepted.

### Native finder (opus) — the nine round-1 mutants re-applied on a scratch copy (baseline 47 passed): all nine KILLED by their named tests (D1 BC2 idempotency with epics; D2 `[al|pha]`/`[-lead]`; D3 last-match; D4 all four placement params; D5 the seeded suffix; D6 `claude-foo` + the aliased symlink; D7 no "tail sweep"; D8 `--adopt ""`; D9 the rc-blind row). Regressions CLEAN: `tests/test_docs_updater.py` unmodified and green; base-vs-new `--check`/`--sync` on a bare fixture identical in stdout/stderr/exit/file tree/md5. Touches + lint CLEAN. The coder's red evidence re-run against 8c839125: 8 failed / 16 passed as claimed. **2 raised:**
- **(3a) DEFECT H** — `:1675/:1682`: `epic_order.py` is hub-only (0 of 41 projects carry it; web-ecommerce-factory has an epics dir), so a successful adoption there returns 3 forever and reruns print the failure row — introduced by the D9 fixup. → FIX: skip the delegation when the script is absent (the file's own `:1026-1028` pattern).
- **(3b) DEFECT H** — `:944`: `_ADOPT_NAME_RE` accepts `Alpha`, `a.b`, `x@y`, `alpha_1`, 40×`a` while `epic_order.py:748` requires `^[a-z0-9-]{1,32}$` → a half-adopted repo with an immutable ledger row and zero epics owned. → FIX: validate against epic_order's grammar before any file I/O.

Verdict r2: 2 defects → fixup dispatched to the same coder. Board T02a stays 🔵.

## Round 3 — over `ca4ec9ed..9607384f` (the round-2 fixup 9607384f: script +340/−22, tests +728 — 55 passed)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own EXECUTED verification (the two round-2 fixes were prescribed verbatim; two native Opus rounds preceded this one) — round 3.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): CLEAN, naming the guarded delegation. Accepted.
- gemini-3-flash (2/5): "`--adopt` writes PLANS.md/DECISIONS.md in a bare repo" — REFUTED a third time: an explicit act on the operator's word (I11 binds the passive surfaces), and `docs/DECISIONS.md` is already sync-seeded in 41 of 41 projects.
- qwen3-max (3/5): CLEAN. Accepted.

### Orchestrator execution (the closing layer)
- In the coder's worktree: `pytest tests/test_docs_updater_adopt.py tests/test_docs_updater.py -q` → **55 passed**; ruff check + format clean; `--name-only` = the two Touches.
- (3a): `epic_order_script.is_file()` guard present (`:1582` docstring names the hub-side path); `TestD3aAbsentEpicOrderIsSkippedNotFailed` asserts rc 0, no row, second run `(nothing to adopt)` — the coder's red `assert 3 == 0` against e7429d43.
- (3b): `_ADOPT_NAME_RE = re.compile(r"^[a-z0-9-]{1,32}$")` at `:947` — byte-identical to `epic_order.py:748`; the orchestrator's own probe: `Alpha` · `a.b` · `x@y` · `alpha_1` · 40×`a` · `al|pha` · `""` REFUSED, `n-1` · `1st` · `-lead` valid (a leading `-` is legal under the hub's own grammar — the round-2 brief listed it as bad by mistake; the coder caught and documented it).
- Grammar consequence noted for the spine's T01→T02a seam: the ledger READ regex (`MERGE_OWNER_RE`) stays the wider `[A-Za-z0-9][A-Za-z0-9_.@-]*` (it reads hand-minted rows), the WRITE grammar is epic_order's — deliberate asymmetry, stated.

Verdict r3: found: 0, fixed: 0 — **CONVERGED**. Merge target: `worktree-agent-a8f1d9418d61c2357` @ 9607384f. Fleet-synced RUN_SCRIPT → `sync_enforcement_to_projects.py --force` after the merge.

## Pass Ledger
| Pass | Layer | Method | Findings → fixed |
|---|---|---|---|
| Pass 1 | pool ×3 + native opus×1 | method: re-derivation (12 mutation probes on a copy; base-vs-new differential on 5 fixtures; ledger ids/cells re-derived) | 9 → 9 |
| Pass 2 | pool ×3 + native opus×1 | method: re-derivation (the nine mutants re-applied and killed; live fleet shape measured: epic_order.py in 0 of 41 projects; the name grammar measured against epic_order.py:748) | 2 → 2 |
| Pass 3 | pool ×3 + orchestrator execution | method: re-derivation (55 tests re-run; the regex and the guard re-read; 10 name shapes probed) | 0 → 0 |
