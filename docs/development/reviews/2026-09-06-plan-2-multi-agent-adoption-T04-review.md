# T04 acceptance review — `session_orient.py` sessions advisory (plan 2026-09-06-plan-2-multi-agent-adoption)

**Status:** CONVERGED
**Surface:** `git diff ca4ec9ed..worktree-agent-ae398d23a2b677726 -- .claude/hooks/session_orient.py tests/test_session_orient_hook.py docs/workstation/hooks-index.md` (14,790 B; +201/−1 over 3 files; native Sonnet coder a5ee18bd). The `ruff format` drift in both Python files (51 + 98 lines) is PRE-EXISTING at ca4ec9ed (measured by the orchestrator: `ruff format --diff` on the base copies = the same line counts as on the branch) and out of scope.

## Coverage Checklist

Rubric invocation (verbatim head of the output):

```text
$ python scripts/review_rubric.py --changed .claude/hooks/session_orient.py tests/test_session_orient_hook.py docs/workstation/hooks-index.md
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
```

Summary: FLOOR core/35-security-auth.md · core/25-data-postgres.md · core/30-ops.md · 12-FACTOR; MATCHED core/10-python.md.

| Class | Status |
|---|---|
| core/10-python.md — uv only / `datetime.now(UTC)` / no logfile / ruff sets | CLEAN (stdlib; stdout only; ruff check clean; the format drift is inherited, measured, out of scope) |
| core/40-documentation.md — trailers, one CHANGELOG entry (orchestrator-applied at merge) | CLEAN (trailers parse: `Agent-Role: subagent`, `Agent-Task: T04`) |
| FLOOR 35/25/30 — no auth, no DB, no service; secrets never read | CLEAN (no surface) |
| 12-FACTOR — XI stdout only; III no constants beyond the regex | CLEAN |
| Recurrence: Behavior Contract row without a killing test (mutation probe) | FIXED r1 → r2 (three unguarded behaviours — native (3a)(3d)(3e)) |
| Recurrence: Touches discipline / governance files / spine / lock untouched | CLEAN (`--name-only` = the ticket's Touches) |
| Recurrence: fleet blast radius of a synced surface (single-session repo prints/writes nothing) | CLEAN (native (1): base vs new hook byte-identical, 3,192 B, on a one-session fake proc tree; hub + worktree suppression proven (2)) |
| Recurrence: proxy-as-evidence — the ticket gate RUN in the coder's worktree by the orchestrator | CLEAN (28 passed; ruff check clean) |
| Recurrence: denominator on every count | CLEAN — every count carries its population: diff bytes/lines (`git diff --numstat`), tests passed of collected, finders usable of dispatched, pids scanned (859) |
| Recurrence: fail-open/fail-closed — a swallowed error that reads as success | CLEAN (per-entry OSError skipped; the `__main__` guard swallows — which is why the dangling test had to assert the ADVISORY, r2) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted | CLEAN (pool r1 4/2/2 scored; native opus×1 r1) |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | FIXED r1 → r2 (the `< 2` boundary and the exact-`comm` match now have killing tests) |
| Recurrence: behavior-without-a-test — a contract row no test kills | FIXED r1 → r2 (three rows) |

## Round 1 — over `ca4ec9ed..a5ee18bd`
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): CLEAN — the four rows → their assertions named; one fragility note (the byte-identity test vs the real `/proc`) — REFUTED: a real `claude` session in a pytest tmp cwd is impossible.
- gemini-3-flash (2/5): three "defects" — (1) an `is_dir` check on `scripts/` "inferred from codebase standards" — REFUTED, `_identity_line` at :139 uses exactly `is_file()` on the full path; (2) self-refuted in its own text; (3) the 200 ms test ceiling vs the Scope's ≤50 ms target — NOTED: the Behavior Contract row is the test's bar (200 ms) and the native finder measured 16–29 ms on this box; kept.
- qwen3-max (2/5): (1) "idempotency on disk" — REFUTED, the hook writes nothing; (2) a main checkout deliberately cloned under a path containing `/.claude/worktrees/` — REFUTED, outside any Fabrik layout and the spec's own D5 wording is the substring; (3) realpath "attack" (L) — REFUTED, `realpath` is non-strict and OS-bounded.

### Native finder (opus) — 10 hunt items executed on scratch copies; 3 raised
- (1) one-session blast radius: base vs new hook byte-identical (`cmp -s`, 3,192 B) — CLEAN; the hub marker is absent from real projects so the advisory is live fleet-wide (an accepted property of D5's trigger). (2) hub + worktree suppression empirically silent; both mutations kill their tests — CLEAN.
- **(3a) DEFECT H — the `< 2` boundary is unguarded** (`session_orient.py:194`): `< 3` leaves 27 passed; a refactor would mute every 2-session repo with a green suite. → FIX: a 2-entry test asserting `**2 sessions`.
- **(3d) DEFECT H — `test_sessions_advisory_dangling_cwd_never_raises` (:487-497) is vacuous**: the `__main__` guard (:393) swallows every exception and exits 0, and `return ""` passes it. → FIX: dangling entry beside two live entries, assert the advisory still fires.
- **(3e) DEFECT M — exact `comm` match unguarded** (:186): `startswith("claude")` leaves 27 passed. → FIX: a `claude-foo` fixture entry.
- (4) `FABRIK_PROC_ROOT` falls back to `/proc` when missing or a file; no `environ` read — CLEAN. (5) placement: def :153, the one call :346 right after `_identity_line`; headless/compact still print the block — CLEAN. (6) fail-open per entry; `realpath` non-strict — CLEAN. (7) hooks-index.md `--numstat` 1/1, the session_orient row only — CLEAN. (8) Touches = the three files — CLEAN. (9) timing test KEEP (859 pids, 16.5–29.2 ms vs 200 ms). (10) 28 passed — CONFIRMED.

Verdict r1: 3 defects → fixup dispatched to the same coder (session alive, D2 routing). Board T04 🟡 → 🔵.

## Round 2 — over `ca4ec9ed..d92888e8` (the round-1 fixup d92888e8: tests only, +46/−2; hook unchanged — the probe showed the hook needed no change)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own EXECUTED verification (the three round-1 fixes were prescribed verbatim; the closing layer re-ran them) — round 2.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): CLEAN — the seven tests named against their rows; single-session byte-identity restated. Accepted.
- gemini-3-flash (3/5): CLEAN with two self-refuted L rows (the `realpath` try/except covers the "never raises" contract, as its own note says). Accepted.
- qwen3-max (2/5): the live-scan timing test "cannot detect a mid-scan vanish" — REFUTED as the wrong test: the vanish class is pinned by the REWRITTEN dangling test (a dangling `cwd` beside two live entries → the advisory still fires with `**2 sessions`, and the coder proved its red by making the dangling branch raise); the timing test bounds latency only, by design.

### Orchestrator execution (the closing layer)
- `pytest tests/test_session_orient_hook.py -q` in the coder's worktree → **29 passed** (28 + the exactly-two test; 3d and 3e were in-place strengthenings, so 29 not 30 — the coder counted rather than manufactured); `ruff check` clean; `grep -c 'claude-foo\|exactly_two'` → 2.
- r1 (3a): `test_sessions_advisory_fires_at_exactly_two_shared_sessions` present; the coder's `< 3` mutant run: `1 failed, 28 passed`.
- r1 (3d): the dangling test rewritten (dangling beside two live entries, asserts `**2 sessions`); its red proven with a raising resolution (`assert 0 == 1`).
- r1 (3e): `("106","claude-foo",…)` in the fires-at-three fixture; the `startswith` mutant miscounted 3→4 and failed.
- `git diff ca4ec9ed..d92888e8 --numstat` → hook 52/0, hooks-index 1/1, tests 192/0; `--name-only` = the three Touches.
- `ruff format --check`: the same pre-existing drift as at ca4ec9ed, no new hunks in touched lines (coder's `--diff` inspection + the orchestrator's base-copy measurement in round 1).

Verdict r2: found: 0, fixed: 0 — **CONVERGED**. Merge target: `worktree-agent-ae398d23a2b677726` @ d92888e8. Fleet-synced hook → `sync_enforcement_to_projects.py --force` runs after the merge.

## Pass Ledger
| Pass | Layer | Method | Findings → fixed |
|---|---|---|---|
| Pass 1 | pool ×3 + native opus×1 | method: re-derivation (base-vs-new hook byte-compared on a fake proc tree; 5 mutation probes; 859 live pids timed) | 3 → 3 |
| Pass 2 | pool ×3 + orchestrator execution | method: re-derivation (29 tests re-run, numstat re-counted, fixture tokens grep-counted) | 0 → 0 |
