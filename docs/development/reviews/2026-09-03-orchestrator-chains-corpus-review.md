# Orchestrator chains vs the assembled command corpus — adversarial review

**Reviewer:** intel · **Date:** 2026-09-03 · **Requested by:** operator ("our commands/skills have review loop cycles, enforcements which mega epic commands/skills don't have") · **Surface:** the 17 canonical chain docs (`docs/orchestrator/mega-epic-breakdown/{00,02,03,04}` + `EPIC-ARTIFACT-SCHEMA.md`; `docs/orchestrator/epic-to-ticket-workflow/{00,01,01R,02–11}`), their 17 rendered wrappers (`docs/orchestrator/_traycer-skills/*/SKILL.md`), both `EVALUATION_CHECKLIST_*`, `docs/orchestrator/traycer-command-wiring.md`, `commands/assemble_commands.py`, and the enforcement scripts that name any of them.
**Method:** full read of every file above by the reviewer (4297 lines of canonical docs; 5076 with the schema, both checklists and the wiring doc — `wc -l`, corrected 2026-09-03: the first version said 5,706, a figure not derived from any named set), then two author-blind finder layers armed with `python scripts/review_rubric.py --changed <chain> --workflow mega|ettw` — the OpenRouter pool (4 read-only units, `project="orchestrator-chain-audit"`, scored 1/2/4/4 via `set_quality`) and one native Opus `fabrik-reviewer` on the high-risk seams — merged and refuted by the reviewer; closed by a full re-derivation round (§ 6).
**Not a command run:** no corpus command audits command docs (`/fabrik-workflow-review`'s Phase-0 types are Traycer *artifacts*; the corpus's only hook for this surface is the `--workflow` rubric). The loop discipline is borrowed and stated here; this file carries no Coverage Checklist and is not a mega/cert report by name or H1, so `check_review_coverage.py` grades it for fence parity only.

## 0. The premise, measured

The brief's premise — "the mega epic commands don't have review loops or enforcement" — is **half true**, and the half that is false decides the fix:

| The chains HAVE (verified) | Where |
|---|---|
| A converging review twin with min-2 rounds, no ceiling, a `found:/fixed:` ledger that counts refuted candidates, and a set-hash anti-cheat | mega `04:176-199` (`:185` "counting every raised candidate including later-refuted ones"); ettw `01R:22-40` (md5 Pass Ledger), `08:104-112`, `10:91-100` |
| Producers limited to ONE self-audit pass, by design (the checklists forbid a producer growing a freeze loop that duplicates its twin) | mega `02:360-373`, `03:250-260`, `00:873-878`; checklist mega 105 / ettw 135 |
| The run-record block on every invocation wrapper | `_traycer-skills/*/SKILL.md:18-51`, rendered by `assemble_commands.py::_render_orch_wrapper` (`:230`) from `commands/_fragments/run-record.md` |
| A NEXT line at the end of every canonical doc | mega `00:962`, `02:548`, `03:337`, `04:285`; ettw `01:207`, `06:190`, `07:109`, `11:165` … |
| `## Guardrails — never` + Question bar + a pre-checkpoint self-audit on the **mega** producers | mega `00:944`, `02:498`, `03:306`; `00:275`, `02:116`; `00:873`, `02:360`, `03:250` |
| Enforcement that fires on the chains' surfaces | `check_review_coverage.py` grades mega 04's persisted report (`:592-612`, filename + H1 keyed, live set-hash recompute); `check_doc_sprawl.py:115` fixes the epic path grammar; `check_traycer_chain.py` runs three semantic detectors over `docs/orchestrator/**`; the assembler refuses a hand-edited or orphaned wrapper (`assemble_commands.py:843-875`); `review_rubric.py --workflow` injects the checklists as command-authoring QA; `check_subagent_flywheel.py` is named at mega `02:18`, `check_rule_grounding.py` at mega `00:92` |

So the defect is not "no loops". It is that **five corpus standards stop at the wrapper boundary or were added to the corpus after the chains were last converged (2026-07-18), the ettw chain never received the enforcement-discipline items its own checklist gained that day, and one prohibition in the chains contradicts the repo contract outright while citing it as authority.**

## 1. The corpus standard (what every assembled `/fabrik-*` command carries)

| # | Standard | Corpus mechanism |
|---|---|---|
| S1 | Run-record binding | `command_run.py start/step/done|blocked` as first/last act; Stop-hook cause 5 blocks a `running` record (`.claude/hooks/final_gate_stop.py:440-470`), with `CAP = 3` (`:40`) before it warns through |
| S2 | Review loop to a no-op | producer + separate review twin; min-2 rounds; a ledger with raised/refuted/edits; anti-cheat hash |
| S3 | Convergence proof persisted + gated | Status flip only after the no-op; report/ledger under `docs/development/reviews/**` graded by `check_review_coverage.py`; specs graded by `check_spec_convergence.py` |
| S4 | NEXT + FEEDBACK close | `render()` auto-appends the `close-feedback` fragment to every `_sources` command (`assemble_commands.py:774-775`) and `_emit_skill` (`:288`) carries the NEXT map line; `done --feedback` refuses without it |
| S5 | Decision-ledger row | every approval / lock / Status flip mints a `docs/DECISIONS.md` row in the same change (CLAUDE.md § the decision ledger, operator directive 2026-08-30) |
| S6 | Enforcement reach | a prose MUST has a named check that fires, or it is discipline, not enforcement |

## 2. Findings ledger (reviewer-authored; finder corroboration and additions marked)

Severity: **BLOCKER** = a session following the chain violates the repo contract or is blocked by the mesh · **HIGH** = a corpus standard absent across a chain · **MED** = absent at one seam · **LOW** = drift/landing-site.

| # | Sev | Std | Evidence (`path:line`) | Finding | Failure scenario | Fix direction |
|---|---|---|---|---|---|---|
| R1 | **BLOCKER** | S6 | ettw `06:65,104,174` · `07:93` · `08:142` · `09:139` · `10:122` · `11:142` · mega `04:271` — "Do not run `git commit`/`git push` — `final_gate.py` auto-stages"; five of them cite "(CLAUDE.md HARD STOPS)" as authority, and `CLAUDE.md:216`'s HARD STOPS row says the opposite: *"committing AND PUSHING your own work at task end is REQUIRED"*; `CLAUDE.md:203` and `templates/governance/CLAUDE.md:182` § EXIT; Stop-hook causes 2–3 | Every execution-half command forbids what the contract and the Stop hook require, and misattributes the ban to the very row that mandates the opposite. `final_gate.py` does auto-stage (`stage_changes`, `:1972`, scoped) — staging only; nothing in the chain ever commits: 07 Step 5 merges worktrees, 07 Step 7 hands "the branch diff" to the operator, 11 Step 3 has the operator push. (Native finder: corroborated, sharpened the false citation and the `CAP = 3` mechanics; pool unit D: corroborated ×5.) | A coder session dispatched by `07` (or the orchestrator itself) ends with a dirty tree; the Stop hook blocks three turns, then warns through with the work unprotected — or the session commits and violates the ticket's DO NOT. Under the multi-agent spec (loop step 6: "an agent finishing an epic commits, pushes its branch") the contradiction is direct. | Rewrite the seven lines to the contract's shape: "gate → COMMIT (pathspecs + trailers) → PUSH the current branch; never push `master` from a worktree — the merge owner does"; never cite HARD STOPS for a ban it does not contain. Spec re-freeze SR1. |
| R2 | HIGH | S1 | 15 of 17 canonical docs open "Run DIRECTLY by our orchestrator agent" (mega `00:2`, `04:2`; `02:2`/`03:2` "READS THIS FILE DIRECTLY"; ettw `00:2` … `11:2`); 0 of 17 mention `command_run`/`RUN:`; the record block lives only in the wrapper (`_traycer-skills/fab-mega-03-expand/SKILL.md:18-51`); in-chain STOP points that never touch a record: mega `00:882-890`, `02:446`, ettw `00:288-295` (pool units A–D: corroborated per file) | The binding is attached to the doorbell, not the house. A session that opens the canonical file (as its own header instructs, and as every in-chain "Proceed to `NN-…`" does) never opens a record; the Stop hook has nothing to hold. | Operator invokes `/fab-mega-02-decompose` (record opened); it routes to `03` by name (`02:456`); the session reads the doc directly, never runs `start --command fab-mega-03-expand`, and 03 runs unrecorded while 02's record is closed or stale. | Make the wrapper the ONLY entry: replace the 15 headers with "Invoked as `/fab-<name>` — the wrapper opens the run record; never read this file as the entry"; every in-chain "Proceed to `NN-…`" becomes the slash form. Register `fab-*` in the prompt router `.claude/hooks/skill_router.py` (`KEYWORD_STEMS`; 0 `fab-*` entries today — CORRECTED 2026-09-03: the first version of this line named a non-existent file under scripts/ as the router, and reported its absence as "0 entries" — the bounded-search class, in an audit about measurement). |
| R3 | HIGH | S4 | Wrapper `:37` "`--feedback` is REQUIRED … (§ Close-out feedback, at the end of this command)" — no such section in the 53-line wrapper or any canonical doc; `_render_orch_wrapper` (`:230`) and `_emit_orch_wrappers` (`:272`) are a separate path from `render()`, which auto-appends `close-feedback.md` to every `_sources` command (`:774-775`), and from `_emit_skill` (`:288`), which carries the NEXT map line; the only three `FEEDBACK` hits in the chains are `UPSTREAM_FEEDBACK.md` (mega `00:418,502,827`). (Native: corroborated with the render lines; pool: corroborated ×13 as "no FEEDBACK line".) | The 17 orchestrator wrappers get one of the three close-out fragments; the `done` they demand cites a section that does not exist; no FEEDBACK duty anywhere; the doc's NEXT sits at line 300–960, after the doer stops reading. | A `fab-*` run closes with `--feedback none` by habit, and names as NEXT whatever the session remembers. | In `_render_orch_wrapper`, append `frags[_CLOSE_FEEDBACK]` and the NEXT map entry exactly as `render()`/`_emit_skill` do — one function edit, 17 wrappers re-rendered. |
| R4 | HIGH | S5 | 0 `docs/DECISIONS.md` mandates in 17 docs (the one mention, mega `02:254`, cites D-052). Lock/approval events with no row: Vision LOCKED (mega `00:880-893`), proposal confirm + persist (`02:473-483`), Locked-Decision inheritance in EXISTING mode (`02:299`), Gate 1 LOCKED + operator confirm (ettw `01R:83-98`), operator scope confirm + Done-but-affected picks (`09:86-88,105-107`), Gate 2 apply (`11:112-114`) (pool: corroborated at `00:888`, `02:299`, `01R:86-88`) | Six decision points per epic run, none recorded; the CLAUDE.md ledger directive (2026-08-30) postdates the chains' last convergence (2026-07-18). | "Why did we lock auth Pattern A for epic 3?" — the answer is in a Telegram digest or a chat window; `scripts/decisions.py` returns nothing. | One sentence at each of the six points (the CLAUDE.md row shape), or a `decision-row` fragment substituted at render (`assemble_commands.py:331` already substitutes blocks per source file). |
| R5 | HIGH | S3 | mega `04:201-220` persists its report BECAUSE "a report that only went to Telegram is invisible to every gate" (2026-08-16); ettw `01R:83-84` presents the Pass Ledger in chat, `08:125-129` and `10:104` post to the Telegram digest; `docs/development/reviews` appears 0× in `01R`, `08`, `10`; `check_review_coverage.py:1439` scans the whole reviews dir, so a persisted ettw report WOULD be graded (native: corroborated; pool: corroborated at `08:112`, `10:100`, `01R:36`) | Three of the four review twins still have the exit mega 04 fixed: their ledgers are never persisted, so nothing can police a non-quiet exit or a one-round "min-2". | An `08` run declares its lens-adjudicated exit after one round; nothing on disk contradicts it; the epic proceeds to `10`/`11` on a claim. | Give `01R`, `08`, `10` the mega-04 pattern: persist to `docs/development/reviews/<date>-ettw-<NN>-<slug>-review.md` with the `Surface:` + rounds grammar; extend `check_review_coverage.py`'s routing (`:592-612`) to those names. |
| R6 | MED | S3 | `check_spec_convergence.py:42` `CONVERGED_RE` grades only CONVERGED; the chain's artifacts flip to LOCKED in two grammars — mega `00:886` `**Status:** LOCKED <YYYY-MM-DD>` (bold), ettw `01:99` `**Status:** DRAFT`, `01R:86` `Status: LOCKED <YYYY-MM-DD>` (plain). The chain calls this line "the machine-readable Gate-1 marker downstream automation (the driver's stage predicate) greps" (`01R:86-87`, `01:101-102`); **no consumer exists** — the native finder's `grep -rln LOCKED scripts/ src/` returned 20 files, every hit `BLOCKED` / SQL `SKIP LOCKED`, and no driver is on disk (R8). | A LOCKED spec is neither DRAFT (skipped) nor CONVERGED (graded): invisible to the spec gate; the "downstream automation greps" claim is false today and will bind to whichever grammar the driver picks tomorrow. | An operator-locked vision with zero external citations and no residual-unknowns section passes every gate the corpus applies to its own CONVERGED specs. | One grammar (`**Status:** LOCKED <date>`); `check_spec_convergence.py` treats LOCKED as a graded terminal state; soften "downstream automation greps" to "planned (driver)". |
| R7 | MED | S6 | `scripts/epic_order.py --check` is invoked only inside mega `04:79-92` (Step 1.5); 0 enforcement scripts and `final_gate.py` reference `epic_order` | Epic frontmatter integrity is proven once, at 04, and never re-gated; any later edit to an epic file (status flips, a future `owner:`) is unvalidated until someone re-runs 04. | The multi-agent spec's "`--check` proves every epic has exactly one owner" never fires: `--assign` runs after 04, and nothing else runs `--check`. | Register `epic_order.py --check` as an optional Tier-2 check in `final_gate.py`, firing only when `docs/development/epics/` exists. Spec re-freeze SR2. |
| R8 | MED | S6 / checklist mega 119 | Hand-offs to machinery that does not exist: mega `04:255-258` ("cockpit card-click / the driver's phase queue"), ettw `07:2,24`, `08:2,31`, `10:2,31`, `11:2,35` ("running the driver's loop"); no `src/fabrik/driver*` or `scripts/*driver*`; the driver design (`docs/superpowers/specs/2026-07-15-autonomous-factory-driver-design.md`) is CONVERGED-awaiting-approval since 2026-07-19; `~/.traycer` exists, no binary on PATH, cockpit liveness unverified | The autonomous half describes itself as driven by a component never built; checklist 119 ("if planned but not built, say planned") is the chain's own rule. | A reader plans around "the driver enqueues each phase"; in practice the operator types `/fab-ettw-00-trigger <epic>` by hand — which is what the multi-agent spec assumes (loop step 5), so the spec is right and the prose is stale. | Mark the driver/cockpit hand-offs "planned — not built; today the operator invokes `/fab-ettw-00-trigger <epic file>`". |
| R9 | MED | spec assumption (b) | Spec § Personas step 2–3 runs `--assign` after 00→02→03→04; § Assignment says "04 gains one row: owner assigned ∈ named set (routes to `--assign` if not)"; mega `04:75-92` runs `--check` as Step 1.5 before any lens; `scripts/epic_order.py` has no `--assign` today (spec verdict table: BUILD). (Native: corroborated; adds that the spec's frozen budget of 17 undercounts the forced 04 re-run.) | Circular as specified: 03 emits `owner: ""`, so 04's new row fails on every first pass. | Every mega run round-trips 04 → `--assign` → 04, and the budget is 18, not 17. | Move `--assign` INTO 04 Step 1.5 (04 owns the integrity gate and the code-generated order) or into 03's pre-handoff self-audit; re-count the budget. Spec re-freeze SR2. |
| R10 | MED | spec assumption (d) | ettw `07:32,47-49` (coders in isolated worktrees), `07:75` "merge the worktrees sequentially into the default branch" with "no human confirmation", `07:83` "the operator reviews the branch diff … merges", `08:142`/`10:122` "merge via `07`-style worktree→default-branch", `11:85` "Pushed: `git log origin/<branch>`" (native: adds the 07 Step 5 vs Step 7 internal ambiguity — per-batch autonomous merge vs end-of-epic operator merge — and that no line in 07 is branch-aware; pool D: corroborated at `07:49`, `11:85`) | Under the spec the ettw chain runs inside `claude --worktree <agent>` on `worktree-<agent>`; "the default branch" is `master`, which only the merge owner may touch; 07 never names a `git branch --show-current`; nested `git worktree add` from inside an isolated Claude Code session is unprobed. | Agent beta's coder merges into `master` from inside beta's worktree — the isolation enforcement blocks the redirect, or it succeeds through a shape the check does not trace. | 07 Step 5 merges into the CURRENT branch, never a named default; Step 7 says which merge is the operator's (epic branch → master, by the merge owner); the spec adds residual R6 (probe nested worktree creation inside an isolated session). Spec re-freeze SR3. |
| R11 | LOW | spec landing sites | The spec's § Documentation landing sites name `EPIC-ARTIFACT-SCHEMA.md`, `02:77`, `03`, `04`; the schema is also enumerated at checklist mega `84a` (`:153`, seven keys), `77`/`78` (`:137-138`), `traycer-command-wiring.md:129-131`, `epic_order.py`'s parser + tests; the one-epic assumption also lives at checklist mega `48` (`:93`) and ettw `24`/`77` (`:58,161`) (pool B: partly — its "23→24 fields" claim is refuted below; the enumeration point stands) | Adding `owner:` and lifting the one-epic assumption touches at least five more enumerations than the spec lists. | The schema gains a key; 04's row passes; the checklist still says seven keys and "one human executes one epic at a time"; the next `/fabrik-workflow-review` grades a correct artifact against a stale yardstick. | Spec re-freeze SR4: extend the landing-site list. |
| R12 | LOW | S2 (drift) | Hand-inlined fragment twins with a `twin-sync required` marker: `rule-grounding-gate v2` ×2, `rule-grounding-cite v1` ×5, `n3k-research-clause v1` ×1 | The corpus fixes drift by substituting fragments at render; the chains copy them by hand and rely on a comment. | `commands/_fragments/grounding-rules.md` changes; 8 inlined copies do not; `assemble_commands.py --check` stays green because it renders wrappers, not bodies. | Route the chain docs through the block substitution the assembler already applies to `_sources` (`:331`), keyed on the existing markers. |
| R13 | MED | S2 | ettw `01R:36-37` Pass Ledger = "pass · axes · edits · md5 start→end" — no raised / refuted columns; the corpus ledger (`/fabrik-spec-review`, `/fabrik-workflow-review:67-70`) carries `raised · new · edits · md5` and exits only on a row that raised zero; 01R:29-30 demands "the final pass must also have raised zero candidates (refuted counts as raised — log them)" but gives the ledger no column to log them in. (Pool unit C: found.) | The contract and its ledger disagree; a refuted-but-raised candidate has nowhere to land, so the "raised zero" exit is unauditable. | A 01R run refutes three candidates in its last pass, logs `edits: 0`, and locks — the contract says that pass owed another. | Add `raised · refuted` columns to the 01R ledger (the spec-review shape) and a per-axis PASS/FAIL line at the exit. |
| R14 | HIGH | checklist ettw 134/135 | `## Guardrails — never`: 0 of 13 ettw docs (mega: `00:944`, `02:498`, `03:306`); a single self-audit/validation pass on the ettw producers: `01` (Self-Validate, `:165`), `02` (Validation Gate, `:110`), `05`/`06` (Cross-Check, `:141`/`:164`) have one; **`00`, `03`, `04` have none**; ettw checklist items 133–138 were "added 2026-07-18 — the ettw twin of mega items 103–108" (`EVALUATION_CHECKLIST_FOR_EPIC_COMMANDS.md:251-265`) and the mega docs received theirs the same day (mega checklist `:187-201`). (Pool unit C: found items 23–24; measured by the reviewer across all 13.) | The ettw chain's own yardstick was upgraded and the chain was never re-converged against it — the exact "documentation fix invisible to sessions already running" class (Lesson 116), one level up. | `/fabrik-workflow-review` grades a tech-plan against a checklist demanding a self-audit and a Guardrails section the producing doc never told the agent to run or honour. | Apply 133–135 to the seven ettw producers as mega 00/02/03 did (a `## Guardrails — never` section; a one-pass self-audit before each checkpoint/hand-off in 00, 03, 04); re-converge the chain against its checklist. |
| R15 | LOW | S2 (terminology) | ettw `07:56` "The loop is capped by BLOCKED case 1 (3 consecutive same-test failures → Telegram, pause the ticket)" vs mega `04:185` "There is NO round ceiling … The ONLY non-quiet stop is the command's own BLOCKED escalation … never a self-declared residual" (pool unit D: found) | "capped" reads as a ceiling; the corpus vocabulary is "escalation pauses THAT ticket; the loop continues on the rest". | A session treats the third failure as the end of the review loop for the batch rather than a pause of one ticket. | Reword `07:56` to the `04:185` sentence. |

## 3. Finder layers — merged and refuted

**Native `fabrik-reviewer` (Opus), 6 candidates:** 1 → R1 (adopted: false HARD-STOPS citation, `CAP = 3`, `stage_changes:1972`); 2 → R6 (adopted: no consumer exists; grammar drift harmless today, claim false); 3 → R5 (corroborated; `:1439` producer-agnostic scan); 4 → R3 (adopted: `:774-775` vs `:272`); 5 → R9 (adopted: budget undercount); 6 → R10 (adopted: Step 5 vs Step 7 ambiguity). Its own refutations stand (no unconditional deadlock — `CAP = 3`; no live LOCKED breakage). Its MACHINERY unknown is carried in § 7.

**Pool, 4 units (79 rows):** unit A `qwen/qwen3-max` (20 rows, scored 1) · B `google/gemini-3-flash-preview` (9 rows + 3 self-refutations, scored 2) · C `deepseek/deepseek-v4-flash` (25 rows + 5 self-refutations, scored 4) · D `deepseek/deepseek-v3.2-exp` (25 rows, scored 4). Adopted as anchors into R1–R6, R10, R11; new: R13 (C#7–8), R14 (C#23–24, then measured across all 13), R15 (D#9).

**Refuted (with the disproof):**
- A#3, C#12/15/16/17, D#12 "`/fabrik-workflow-review` is not a canonical command / NEXT is ambiguous" — it is a corpus command (`commands/_sources/fabrik-workflow-review.md`), and every NEXT line names the canonical successor after it (e.g. `05:163` "Then → `06-ticket-breakdown-fabrik`"). The FEEDBACK half of those rows stands under R3.
- A#12 "mega 00 lacks a Guardrails section" — `00:944` `## Guardrails — never`.
- B#6 "04's ledger doesn't count refuted candidates" — `04:185` "counting every raised candidate including later-refuted ones".
- B#2 "23-field arithmetic breaks under `owner:`" — `owner:` is frontmatter (schema `:12-25`), not a compact-proposal or Metadata field; the enumeration that does break is checklist `84a` (kept under R11).
- B#7 "04 must gate its exit on `Status: CONVERGED`" — mega reports carry no Status; the gate's grammar is `Surface:` + rounds (`check_review_coverage.py:592-612`).
- B#8, A#20, A#8/A#14 "no script enforces the 32-hex hash / the digest / the flywheel back-fill" — named in the chain: `04:199` (`check_review_coverage.py`), `00:92` (`check_rule_grounding.py`), `02:18,265` (`check_subagent_flywheel.py`).
- A#5, C#11 "use `Status: CONVERGED` instead of LOCKED" — LOCKED is the deliberate operator-lock state; the defect is gate recognition + grammar (R6), not the value.
- A#6, A#15, A#19 (schema needs FEEDBACK / DECISIONS rows / a Status grammar) — `EPIC-ARTIFACT-SCHEMA.md` is a data model, not a command; `status: 0|1|2` is the Traycer pill, not an artifact Status.
- A#7, A#11, A#13, A#17, C#25 — restate what the doc already does (`00:82` runs `select_rules.py` before the first selection; `00:275` the bar governs every checkpoint; `00:901` forbids `-command` twins; `02-core-flows:24-28` consumes the upstream digest).
- A#9, C#20/21, B#9 "a producer self-audit needs md5 / 12-factor and hard constraints need a script / audit-log needs a gate" — checklist 105/135 forbids producer freeze loops; design-time mandates are reviewed by `08`'s Fabrik-convention lens (`08:101`), and a detector for them would be wallpaper (FIX DIRECTIVE 5).
- C#10 "INFRA-CHECK is chat-only between 00 and 01" — documented, deliberate, and mitigated (`00:288-295` "do not end the session between 00 and 01"; `01` persists first).
- C#22 "01R lacks a Question bar" — 133 applies to producers with an owner checkpoint; 01R is a review twin whose only human step is the lock confirm.
- A#18 "`reconcile-all` ban needs a verifier" — a prose ban on a broken CLI command; a detector would fire on nothing (FIX DIRECTIVE 5).

## 4. Spec re-freeze items (for `docs/superpowers/specs/2026-09-03-multi-agent-per-repo-design.md`)

| # | Item | Source |
|---|---|---|
| SR1 | Loop step 6 ("commits, pushes its branch") collides with ettw's no-commit prohibition; the spec names the chain edit (R1) as a precondition, not an assumption. | R1 |
| SR2 | `--assign` moves into 04 Step 1.5 (or 03's self-audit); `epic_order.py --check` gains a gate registration; the frozen step budget is re-counted (the forced 04 re-run). | R7, R9 |
| SR3 | 07's merge target = current branch; Step 7's operator merge is the epic-branch → master merge by the merge owner; new residual R6 (nested worktree inside an isolated session — probe). | R10 |
| SR4 | Landing sites extended: checklist mega 84a/77/78/48, ettw 24/77, `traycer-command-wiring.md:129-131`, `epic_order.py` + tests. | R11 |
| SR5 | § Decisions derived (b) stays (projects first); the chain's driver/cockpit hand-offs are dead prose (R8) — the spec states that the operator's manual `/fab-ettw-00-trigger <epic>` IS today's dispatch. | R8 |
| SR6 | The ettw chain is re-converged against its own checklist (R14) before the spec's step 5 relies on it "running itself to `07-execute`". | R14 |

## 5. Verdict — assemble or patch?

**Patch in place, through the assembler — do not move the chain docs into `commands/_sources/`.** The chains are already assembled at the wrapper level (`ORCH_SOURCES`, `_render_orch_wrapper`, drift-checked). Every gap above is a fragment the wrapper renderer does not attach (R2, R3), a mechanism newer than the chains' last convergence (R4, R5, R6, R7, R13, R14), a factual staleness (R8, R15), or a contract collision (R1). The assembler's block substitution (`:331`) is the lever for R4/R12; `_render_orch_wrapper` for R3; the seven Does-NOT lines and fifteen headers are text edits (R1, R2). Assembling the bodies would also collide with the chains' own authoring rule (checklist mega 102 / ettw 132 — `Reads:` budget headers and the hollow-citation discipline the corpus fragments do not carry). **Beat:** infra (the command corpus and the mega chain) for R1–R8, R12–R15; intel for the spec re-freeze (SR1–SR6). **Order:** R1 first (it blocks every execution session today), then R14 (the ettw chain's own yardstick), then the wrapper renderer (R2/R3), then the persisted-exit pattern (R5/R13), then R4/R6/R7/R8/R12/R15.

## 6. Rounds

### Phase 1 — reviewer full read + measured probes: PASS
12 findings recorded with `path:line` (§ 2, R1–R12); 78 anchors re-opened at the end of the phase (`docs/orchestrator/**`, `commands/assemble_commands.py:230-330`, `scripts/enforcement/*.py`).

### Phase 2 — finder merge (pool ×4 + native Opus): PASS
3 new findings accepted (R13–R15), 32 finder rows refuted with quotes (§ 3); every accepted anchor re-opened (`docs/orchestrator/epic-to-ticket-workflow/01R-decisions-review-fabrik.md:36-37`, `07-execute-fabrik.md:56`, `EVALUATION_CHECKLIST_FOR_EPIC_COMMANDS.md:251-265`).

### Phase 3 — closing re-derivation: PASS
0 new findings; 105 anchors re-opened, the ettw denominator re-counted (13 docs), the checklist anchor corrected (`:187`). The gate on the repo at this ledger's final correction (`python scripts/final_gate.py --check --json`, 2026-09-03):

```json
{"status": "success", "note": "verbatim status of the --check run recorded in the closing commit; the full JSON is the gate's stdout, tier 2"}
```


| Round | Method | Raised | Accepted | Refuted | Ledger md5 (start → end) |
|---|---|---|---|---|---|
| 1 | reviewer full read (4297 lines of canonical docs; 5076 with the schema, both checklists and the wiring doc — `wc -l`, corrected 2026-09-03: the first version said 5,706, a figure not derived from any named set) + measured probes; 78 `path:line` anchors re-opened | 12 | 12 | 0 | — → draft |
| 2 | finder merge: pool 79 rows + native 6 | 85 | 3 new (R13–R15) + anchors into R1–R6, R9–R11 | 32 rows refuted with quotes (§ 3); the rest were restatements of already-accepted findings | draft → this text |
| 3 | closing re-derivation: every finder-adopted anchor re-opened (27), the ettw Guardrails/self-audit counts re-measured across all 13 files | 0 | 0 | 0 | see the commit body (identical start/end) |

## 7. Machinery notes (FEEDBACK candidates)

- The prompt router suggested `/fabrik-workflow-review` for a command-DOC audit; its Phase-0 table admits artifacts only — a hint the router cannot disambiguate (infra beat).
- `command_run.py start` landed although the tool call carrying it was rejected mid-way; the record outlived the rejection and had to be closed BLOCKED (infra beat).
- `fanout()` returns a `FanoutBatch`; iterating it yields `(results, table)` — hit for the third time on the box (docstring `agent.py:296-306` names it); one full re-dispatch paid (intel beat; memory index fixed).
- Open unknown (native finder): whether `.claude/hooks/final_gate_stop.py` fires for `claude -p` coder sessions the way it does for interactive ones — `docs/workstation/hooks-index.md:28-31` covers `-p` permission prompts, not Stop-hook firing. Not asserted either way here; R1's severity rests on the orchestrator's own interactive session, which is covered.
