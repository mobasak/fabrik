# Review — sysadmin way-of-working audit fixes (bc7caaae + 24c92a98 → 5efba721)

**Surface:** HEAD 24c92a98 · diff-md5 7720b7f8350bc06b66a381b8f9886ecd (bc7caaae^..24c92a98, scripts/sysadmin/); fixes landed as 5efba721 + ff58f7f9.
**Scope:** 5 audit fixes (reversal-detector revival, wake-slot accounting, model policy, dead-domain, system-prompt reality) reviewed adversarially; the review round found 13 more, all confirmed ones fixed in 5efba721.
**Status:** DONE — closing re-derivation raised new: 0.

## Rubric

Armed with `python3 scripts/review_rubric.py --changed scripts/sysadmin/detect_reversals.py scripts/sysadmin/proactive-check.sh scripts/sysadmin/morning-report.sh` (verbatim output header):

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all axes)
```

## Finders

- **Pool breadth** (`fanout("review", …, mode="read_only")` ×2, flywheel-recorded + `set_quality`): raised naive-ISO tz semantics, bool-subclass, TOCTOU rate race, model-env quoting.
- **Native Opus** (`fabrik-reviewer`): the authoritative pass — 13 findings, incl. a regression in the fix under review (hour-rollover stale counter, reproduced by simulation), the daily-digest class instances, test non-collection, and a mutation the tests survived.

## Pass Ledger

- **Pass 1** (pool ×2 + native Opus — ts semantics, wake accounting, model params, prompt truthfulness, adjacent-class): found: 17 | new: 17 | fixed: 13 → not done (changed code).
- **Pass 2** (method: **re-derivation** over the post-fix surface — re-grepped the ts-compare class repo-dir-wide [0 raw comparisons remain in scripts/sysadmin, denominator: every .sh+.py], re-ran both suites [32 pass], re-ran the hour-rollover simulation against the committed logic [shed no longer eats the new hour: WAKE 1/5, 2/5], `bash -n` on all 5 changed shells, ruff clean after one autofix, mutation re-verified [naive-as-UTC mutant fails the positive control, restore green]): found: 0 | new: 0 | fixed: 0 → EXIT.

## Coverage Checklist

| Class | Verdict |
|---|---|
| ts-parse correctness | FIXED — `_entry_epoch` (float/naive-local-ISO/tz-aware; bool excluded); positive control pins LOCAL semantics (mutation-proven); unparseable ts WARNs once per run (f9). Same-host writer/reader invariant makes local-naive correct. |
| ts class extinction (adjacent files) | FIXED (f2) — 5 identical sites in daily-digest.sh (3 masked-to-zero metrics + 2 dead alarm paths) class-fixed with the same `_ep` shape; closing grep: 0 raw ts comparisons remain in scripts/sysadmin. |
| wake-slot accounting | FIXED — increment post-call gated on rc≠75 (original fix) + the review-found rollover regression closed (on-disk reset at hour rotation, f1, sim-reproduced then sim-proven). TOCTOU race REFUTED as in-scope: pre-existing shape, cron cadence (15 min) cannot self-overlap the ≤300s stage-2. |
| model-param quoting/injection | CLEAN — `"${VAR:-default}"` single-token, argv (no shell) downstream; env is root-cron-owned. Opus verified all 4 sites inside intended commands, continuations intact. |
| override operability + docs | FIXED (f6) — morning/weekly/monthly now source `.env.sysadmin` (cron env is minimal; the documented override was inoperative); both vars documented in the governance runbook config table. |
| system-prompt truthfulness | FIXED (f5, f5b) — "shed to the pool" overclaim corrected (a shed run's output is DROPPED; no shell caller routes to a pool); capped-incident path honest; cap arming attributed to `claude_rotate.py::run_claude`. |
| least-authority (f7) | FIXED — `bypassPermissions` dropped from the morning-report formatting job (needs no tools; carried full root-docker authority on a weaker model). |
| fail-open/fail-closed (standing) | FIXED where found — cert probe failures now LOUD in morning-report (the fail-silent that hid dead coolify.vps1 for 3 months, f8); proactive-check's documented-deliberate silent skip left as designed. |
| behavior-without-a-test (standing) | FIXED (f3+f4) — the grader moved to `tests/` (pytest testpaths; it was collected by NOTHING at scripts/sysadmin/), positive control added that kills the naive-as-UTC mutation all prior tests survived. |
| cost/quota/limit edges (standing) | FIXED — the rollover bug WAS the limit-edge; sim proves 5 wakes are available after a new-hour shed. |
| boundary/sentinel/prefix (standing) | CLEAN — exit-75 sentinel handling verified end-to-end by Opus (no path lets a wake escape counting; `RESULT=$()` → `GOV_RC=$?` bash semantics verified empirically). |
| deferred (reasoned) | f10 cross-source TZ skew — inert on a UTC fleet; verified at deploy (`timedatectl`). f11 DST fold — annual 1h edge at a 10-min lookback, comment-scope. f12 FIXED (yesterday-actions dual-format). f13 FIXED (CHANGELOG + AFTER-EDIT + tmp cleanup). |

## Refuted (proof)

- **"writer/parser tz mismatch if bot runs UTC and parser local"** (pool) — REFUTED: both processes run on the SAME host with the same /etc/localtime; naive-local in, naive-local out; cutoff is absolute epoch both sides.
- **"bool exclusion broken"** (pool) — REFUTED: works as written (finder itself conceded); style-only.
- **"TOCTOU rate race is a regression"** (pool) — REFUTED as in-scope: the read-then-write shape predates the change; 15-min cron cannot overlap a ≤300s stage-2; manual+cron overlap is bounded (+1 wake).

## Gate

```
$ pytest tests/test_detect_reversals.py tests/test_quota_governor.py  →  32 passed
$ bash -n <all 5 changed .sh>  →  clean
$ ruff check <changed .py>  →  clean (1 autofix, ff58f7f9)
$ grep -rn 'get(.ts., 0) *[<>]' scripts/sysadmin/  →  0 raw comparisons remain
```

Exit: 17 candidates → 13 FIXED + 2 DEFERRED-with-reason (f10 verified-at-deploy, f11 comment-scope) + 2 REFUTED-hard (tz-mismatch, bool) with the TOCTOU refuted-as-in-scope; Pass-2 re-derivation raised new: 0.
