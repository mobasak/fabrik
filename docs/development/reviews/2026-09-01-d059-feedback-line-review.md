# Review — D-059 feedback-line + MCP-probe landing (2c984e99 + fix rounds)

Status: CLOSED
Surface: commit 2c984e99 (FINAL OUTPUT 7-line contract + _FINAL_BLOCK_KEYS + _mcp_probe_advisory)
and the fix rounds it triggered (8f6e609b, d899a224). Compact: 1 native finder + authority pass
(pool skipped — libs/subagents still sibling WIP, standing disposition).

## Rubric (verbatim head of `review_rubric.py --changed` over the surface — armed the finder)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
```

## Coverage Checklist

| Class | Verdict | Evidence |
|---|---|---|
| enforcement-was-metric-only (headline) | FIXED | the landing's "Enforced, not asked" claim was FALSE — _FINAL_BLOCK_KEYS fed only the final_block_emitted kaizen metric, no blocking decision read it (self-inflicted read-it-don't-recall-it, in the governing file). A `feedback-line` stall cause now BLOCKS a GATE:+DONE:+NEXT: block missing FEEDBACK:, riding the stall lane's 3-attempt warn-through; pinned both directions at the subprocess level (`test_six_key_block_without_feedback_line_blocks` / `test_seven_key_block_passes_the_feedback_gate`) |
| d059-overclaim | FIXED | the D-059 row's enforcement claim is TRUE as of d899a224; its mint-time falsity is recorded HERE (rows immutable; the WHERE files now match the claim) |
| kaizen-test-regression | FIXED | 3 tests shipped red (6-key fixture vs 7-key tuple, never run at landing — the hub gate's pytest is marker-gated and never executed them); fixture now 7 keys, 28/28 |
| probe-test-hang | FIXED | the probe spawned LIVE MCP stdio servers inside every fleet repo's test suite (finder killed a hung run, /proc-verified) — COMMAND_RUN_DIR/PYTEST_CURRENT_TEST/FABRIK_MCP_PROBE=0 seams; 105 command_run tests now pass in 20s |
| probe-fail-silent | FIXED | empty-stdout + nonzero-rc printed nothing (the check silently disabling itself); crashed-probe stderr tail now surfaces with the D-033 pointer |
| probe-repo-pinning | FIXED | `--repo` now pinned via `_repo_root()` (mcp_health's default "." does no upward walk — a subdir invocation silently reported "no .mcp.json") |
| stale-synced-docs | FIXED | AGENTS-compact.md (incl. the restated block BODY gaining its FEEDBACK line), session_orient.py's per-session ORIENT text, command-evaluation-checklist.md, agent-command-routing-and-gates.md, 2 command sources, hook comments — repo-wide `6-line` grep now 0 |
| block-seen-consumers / transition | CLEAN | _final_block_seen feeds only the metric; old-block sessions were never hard-blocked by the tuple — the new stall cause is the deliberate enforcement, with warn-through |
| footer-misread | CLEAN | the STATE footer lacks GATE:/DONE: — cannot trip the new cause; pre-existing loose matching unchanged |
| fail-open/fail-closed | FIXED | the probe's fail-silent path closed (row above); the stall cause fails open by warn-through design |
| behavior-without-a-test | FIXED | both directions of the enforcement + the probe seam pinned; kaizen fixture repaired |
| cost/quota accounting | CLEAN | probe capped at 30s, skipped in every harness context; no other cost surface |
| boundary/sentinel/prefix | FIXED | the ⚠-vs-WARN prefix and the GATE:/DONE:/NEXT: key-presence boundary are this surface's sentinels — the missing-FEEDBACK boundary now detected line-anchored; the probe's rc/empty-stdout boundary surfaced |

## Pass Ledger

| Pass 1 | method: gate | found: 2 | new: 2 | fixed: 2 | finders: d059-finder(native, dispatched) · authority(self: block-seen consumers traced, 4 stale corpus refs + hook comment fixed 8f6e609b, corpus re-rendered from master MAIN) |
| Pass 2 | method: re-derivation | found: 0 | new: 0 | fixed: 0 | finders: authority re-sweep — repo grep for stale refs returned 0 at that scope |
| Pass 3 | method: citation | found: 11 | new: 7 | fixed: 11 | finders: d059-finder(returned: 7 CONFIRMED + 3 PLAUSIBLE candidates + 5 clean, incl. the false-enforcement headline proven by live pytest + /proc) — all adjudicated real and fixed in d899a224 |
| Pass 4 | method: re-derivation | found: 0 | new: 0 | fixed: 0 | finders: authority closing sweep — 139 tests green across the three affected suites, zero `6-line` refs repo-wide, both enforcement pins green |

## Gate

`final_gate --lean --check --json` → `success` earlier this session; the landing's own gate
could not have caught the test regression (the hub gate's pytest row is marker-gated OFF — the
finder's machinery note; already covered by the backlog's final_gate-honesty cluster).

## Terminal

The exit round is the ledger's last row (Pass 4 — the quiet round). Totals: 13 raised, 13 fixed,
0 refuted. The headline: the commit that added a "we enforce feedback now" contract did not
enforce it — the finder proved it by reading every consumer; the enforcement now exists and is
pinned at the real subprocess boundary.
