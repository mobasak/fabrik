# T11 — /fabrik-doc-converge: 63b manifesto conformance

Status: CONVERGED — 3 source fixes; closing round new: 0

Surface: commands/_sources/fabrik-doc-converge.md (79 lines, read in full; grep-derived anchors) + rendered composition (run-record + term-edit + grounding-artifact + subagents-core + close-feedback, T01/T04-swept).
Outcome: 3 FIXES — eleventh consecutive falsification. The pre-flagged ripple question resolved AGAINST the stamp: the same-run branch was structurally impossible (the one-doc guardrail), forcing every keyed ripple into deferral vs the SAME-change floor — and the verifier proved the gate never fires on doc↔doc pairs, so the law was pure prose. Fixed via the mechanical-ripple distinction. The old-claims sample is now fraction-stated; STRATEGIC_BACKLOG's 'consciously dropped' now cites the drop.

## 63b Verdict Table

| intersection | verdict (grep-derived SOURCE anchors) |
|---|---|
| (a) checkable gates | CONFORMS — term-edit's md5 no-op governs (:12); each doc row carries a bidirectional "Complete when" contract (:27-40) — three cells are deliberately COST-BOUNDED checks (spot-open · reason-the-destructive · what-is-executable), adjudicated as the manifesto's own Pareto scaling for a utility sweep and now stated as such here rather than passed off as fully mechanical; "the contract row's ground-truth column is the denominator" (:71); the old-claims re-check now carries its k/N fraction in the pass ledger (post-fix) |
| (b) ledger routing + one-way field block | CONFORMS — doc convergence is the NOT-a-decision carve-out's own class (routine doc edits, CHANGELOG's beat); close-feedback's decision line rides the render for anything ruling-shaped |
| (c) rigor scales with irreversibility | CONFORMS-via-delegation (T03 precedent) — a single-doc utility whose per-row contracts size each sweep; rows with executable truth are RUN, not inspected (:47-48) |
| (d) labeled verified/assumption evidence | CONFORMS — "Enumerate what you READ (files × surfaces), not what you remember" (:44-46); "output beats inspection" (:47-48); "a path that looks right is not grounding — open it" (:53-54) |
| (e) captured disorder | CONFORMS — dead claims deleted WITH the removal stated (:54-55); KEYED ripples land as minimal mechanical edits in this run's commit (SAME-change law; not a second convergence); only a full-converge-worthy ripple defers, with staleness stated in the commit body and the gate's doc↔doc blindness named as WHY the statement is mandatory (post-fix) |
| (f) most-reversible default under ambiguity | CONFORMS — a doc with no contract row STOPs with its named route (:17-20); the append-only ledgers are excluded by design with stated reasons — no silent scope creep |

6/6 adjudicated after verification: 4 CONFORMS · 2 FIXED (a-sample, e-ripple).

## Scoped verification review

| pass | finders | found | new | fixed | verdict |
|---|---|---|---|---|---|
| Pass 1 | 1 native author-blind verifier: the pre-flagged question adjudicated with a full check_doc_sync.py read (gate is doc↔doc-blind — the follow-up branch had NO backstop), guardrail cross-read, contract-cell checkability audit, anchor re-derivation | 5 | 5 | 4 | E1 ripple contradiction FIXED (mechanical-ripple distinction) · E2 gate blindness NAMED in-source as the why · E3 sample bounded (k/N in the ledger) · E4 'consciously dropped' now cited; the three cost-bounded cells adjudicated as deliberate Pareto scaling, stated · E5 off-by-one anchors corrected |
| Pass 2 (closing, method: gate) | mechanical: all three clauses grep clean; no same-run-or-follow-up residue | 0 | 0 | 0 | → EXIT |

## Per-finding disposition ledger

| # | finding | state |
|---|---|---|
| E1 | the ripple's same-run branch was impossible (one-doc guardrail) → every keyed ripple deferred, against the SAME-change floor | FIXED — a keyed ripple is a minimal mechanical edit in this commit; only full-converge ripples defer, stated in the commit body |
| E2 | check_doc_sync never fires on doc↔doc — the law had zero mechanical backstop | FIXED-by-naming — the source now states the gate blindness as why the commit-body statement is mandatory (checker candidate stays under the rollout law) |
| E3 | "a sample of old ones" unbounded/unstated in a termination criterion | FIXED — k/N in the pass ledger; the closing pass re-opens all edit-touched claims |
| E4 | four self-graded contract cells | FIXED(1)+ADJUDICATED(3) — 'consciously dropped' now cites the drop; spot-open/reason-destructive/what-is-executable stated as deliberate cost-bounded checks (Pareto), not passed off as mechanical |
| E5 | off-by-one anchor pattern | FIXED |
