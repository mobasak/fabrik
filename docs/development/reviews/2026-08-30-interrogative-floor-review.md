# Review — 730a1af5 "the interrogative floor" (commands + enforcement + dogfooded spec)

Surface: 730a1af536a2d1f5cbaa79638af55b50b927f6d2 + working-tree md5 4610a829610d1e4a19f17492e37f467e
Scope: commands/_sources/fabrik-spec.md · commands/_sources/fabrik-spec-review.md ·
scripts/enforcement/check_spec_convergence.py · tests/enforcement/test_spec_convergence.py ·
docs/superpowers/specs/2026-08-30-decision-ledger-v2-design.md
Mode: NO-POOL (operator standing directive) — solo native; closing pass in a fresh non-author frame
(re-derivation method, every claim re-read from the live file, not from authoring memory).
Prior-report anchor: newest review for this scope is `2026-08-30-1c-floor-batch-review.md` — its
`Surface:` hash does not match (different commit), so this is a full WIDE pass 1, not a re-adjudication.

## Rubric (verbatim, review_rubric.py --changed <the 5 paths>)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.
## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: scripts/enforcement/check_spec_convergence.py, tests/enforcement/test_spec_convergence.py)
### core/40-documentation.md  (hit: commands/_sources/fabrik-spec-review.md, commands/_sources/fabrik-spec.md, docs/superpowers/specs/2026-08-30-decision-ledger-v2-design.md)
### core/45-testing-strategy.md  (hit: tests/enforcement/test_spec_convergence.py)
```
(Full 129-line output retained in session scratchpad `rubric.txt`; section heads reproduced above —
the FLOOR packs' mandates were hunted as classes below, not re-pasted.)

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | security-auth floor (35) | CLEAN | no auth/token/session surface in the diff — docs + a read-only advisory check; grep of diff for auth/jwt/secret: 0 relevant |
| 2 | data-postgres floor (25) | CLEAN | no DB surface; the design under spec explicitly rejects a DB |
| 3 | ops/deploy floor (30) | CLEAN | no compose/ports/deploy surface |
| 4 | 12-Factor axes | CLEAN | no service/process surface; check remains a stdlib CLI |
| 5 | python discipline (10) | CLEAN | check edit: module-level compiled regexes, no new deps, ASCII-safe output path unchanged; tests mirror existing helper style |
| 6 | documentation rules (40) | FIXED(2) | two stale cross-refs left by the trigger move into § Lifecycle: `## Out of scope` still said triggers live in § Storage decision; intake I9 `Where` still pointed at the hybrid para. Both re-pointed at § Lifecycle |
| 7 | testing strategy (45) / behavior-without-a-test | CLEAN | NO-PERSONAS fires (red-first, watched), NO-LIFECYCLE fires (red-first, watched), date gate pinned by pre-cutoff test; each heading's presence clearing its own finding asserted in the opposite test; warn_only exit-0 pinned by pre-existing `test_no_module_constant_is_dead`/CLI tests — 26/26 green |
| 8 | fail-open vs fail-closed | CLEAN | check stays `warn_only` + always-exit-0 (`main`'s SystemExit/Exception guards untouched); new findings raised inside `_audit` under the same try — a regex error cannot red the gate; date-gate fails toward NOT grading (no match → no finding), the correct advisory direction |
| 9 | boundary/sentinel/prefix | CLEAN | `>=` FLOOR_CUTOFF boundary tested at exactly 2026-08-30 (fires) and 2026-08-27 (silent); `^##\s+Personas\b` anchors at h2 exactly as the command mandates the literal heading (an h3 or renamed heading is a genuine miss the mandate text now prevents); labels NO-PERSONAS/NO-LIFECYCLE collide with no existing label (grep: unique) |
| 10 | enforcement-overclaim (last batch's live class) | CLEAN | check comment says heading-presence-only, review owns the answers; NO-* finding texts claim only "mandated section"; command texts never claim mechanical enforcement of the six answers — D-axis walk is explicitly the reviewer's duty |
| 11 | corpus internal consistency (spec ↔ review) | CLEAN | six-interrogative mapping identical on both sides (WHO=Personas incl. automated · WHEN=## Lifecycle · WHERE incl. landing sites); A0b BLOCKING-finding language and the check's advisory NO-PERSONAS coexist without contradiction (review blocks, check warns) |
| 12 | fleet lens (46 repos, 12 types) | CLEAN | new mandates are type-independent (every design has consumers + a lifecycle); date-gated so zero historical churn; advisory-only mechanically; live fire rate measured: exactly 1 spec (the dogfooded one), corpus otherwise untouched |
| 13 | dogfood completeness (the spec vs the new floor it shipped under) | FIXED(2) | the v2 spec lacked the newly-mandated `Documentation landing sites` section — the operator had LITERALLY asked "where are we writing where to document this implementation?" and the answer lived only in chat; section added, and that operator question folded in as intake row I11 (11/11 IN) |
| 14 | cost/quota/limit accounting | CLEAN | no spend surface; advisory output budget already charges its truncation marker up front (pre-existing) |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — all 14 classes, all 5 files | citation | solo native (NO-POOL declared; dispatched: 0, orchestrator covered all partitions) | 4 | 4 | 4 | F1 stale § Out-of-scope ref · F2 stale I9 Where · F3 missing Documentation-landing-sites section · F4 unlogged operator question → I11 |
| Pass 2 | SCOPED — the fix diff + its cross-refs (intake table, Lifecycle, Out of scope, landing sites) | citation | solo native | 0 | 0 | 0 | grader re-run: v2 spec 0 findings; 26/26 tests |
| Pass 3 | CLOSING — full fresh sweep; every count/enumeration/anchor re-derived from the live files (intake 11 counted, trigger refs grepped 0, landing-sites section grepped present, grader re-run NONE) | method: re-derivation | solo native, non-author frame | 0 | 0 | 0 | TERMINAL |

Pass 3 = the no-op: `new: 0`, every candidate adjudicated, gate green.

## Verdict

All 14 classes CLEAN/FIXED. 4 findings, all fixed in-run, all in the dogfooded spec (the command +
check text survived the overclaim hunt intact this time). `final_gate --check`: success 54/0.
