# /fabrik-review — `/fabrik-rivals` audit fixes (commit 15bcec7a)

Status: CLOSED — coverage-adjudicated exit, final round `found: 0, fixed: 0`

**Surface:** `HEAD 15bcec7a1a906b3aba157bea00fed07eb30398db` · diff md5 `560d7c8b70ee3703c23a1535275fafbe`

**Scope.** Command 1 of 31 in a command-corpus audit. The reviewed surface is commit `15bcec7a`
plus the working-tree fixes this review produced: `scripts/rivals_run.py` (the `--rediscover`
machinery), `scripts/enforcement/check_rivals_dossier.py` (NEW, fleet-synced, `warn_only`),
`scripts/final_gate.py` (registration), both test files, the command source, the reference doc,
`INDEX.md`, `CHANGELOG.md`, `docs/LESSONS_LEARNT.md`.

**Predecessor.** `docs/development/reviews/2026-08-27-fabrik-rivals-surface-review.md` closed at
`found: 0` on surface `bd20e79d`/`84794389`. The surface has since changed, so this is a NEW
adjudication that inherits that report's twelve classes and adds four for the new code.

**Finders.** Native orchestrator only. `NO-POOL: operator standing directive — no subagent or pool
dispatch for hub work (2026-08-26, reaffirmed this session).` Every finding below was reproduced by
executing the real artifact, not by reading it.

## Rubric

Generated by `python scripts/review_rubric.py --changed <the 7 code/doc paths>`; FLOOR
(`core/35-security-auth`, `core/25-data-postgres`, `core/30-ops`, all twelve 12-Factor axes) plus
MATCHED (`core/10-python`, `core/40-documentation`, `core/45-testing-strategy`).

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | **fail-open vs fail-closed** (standing) | FIXED (1) | **F1** — `argparse` exits 2 on a bad flag; `SystemExit` is a `BaseException`, so `except Exception` missed it. Reproduced: `--bogus-flag` → exit 2. On a `warn_only` check that is a blocking red in ~46 repos, and the docstring claimed "always exits 0". Fixed with `parse_known_args` + a `SystemExit` guard; re-probed all five argv shapes → exit 0. |
| 2 | **behavior-without-a-test** (standing) | FIXED (3) | F1, F2, F4 each landed with a test watched RED first. New: `_comp_slug`, `_union_sidecar`, `_stash_prior`, `_durable_prior`, `_dedup_cards` all now covered. |
| 3 | **boundary / sentinel / prefix** (standing) | FIXED (2) | **F4** — `line[:MAX_LINE-1] + "..."` yields `MAX_LINE + 2` (measured 222 vs a declared 220), so the constant did not mean what it says. **F5** — `_comp_slug` collapses punctuation, so `crm/eu` and `crm-eu` share a sidecar filename; the in-file `job_id` guard makes that safe, proven by red-on-revert. |
| 4 | **cost / quota / limit** (standing) | FIXED (1) | **F2** — the accumulated union lived only in a local variable across `await run(...)`; the engine overwrites `competitors` at `orchestrator.py:585` and its `_persist()` is a literal dict that drops driver keys, so a raise or a Ctrl-C between those points destroyed every rival PAID for in prior rounds. Fixed with a durable sidecar written before the run. |
| 5 | Python discipline (`core/10-python`) | CLEAN | `ruff` clean via the gate; config read via `os.getenv` only; `print` is CLI UX in an enforcement check, matching `check_plan_lock_release.py`'s precedent. |
| 6 | Documentation truth (`core/40-documentation`) | FIXED (1) | **F3** — the loop shape is stated in three places; verified command source `:107-128`, reference doc `:111-119, :174-176` and the CHANGELOG agree with each other AND with the code (`--rediscover` on rounds 1..N-1, final round without). |
| 7 | Secrets / config in code (FLOOR 35) | CLEAN | No key, host, or token in the new code; the check reads only the filesystem. |
| 8 | Postgres / migrations (FLOOR 25) | REFUTED | No DB in this surface. |
| 9 | Docker / compose / ports (FLOOR 30) | REFUTED | Ships no service, container, or port. |
| 10 | 12-Factor | CLEAN | III env-only config; XI stdout only, no logfile; VI no shared in-process state. |
| 11 | Fleet-render safety | CLEAN | `assemble_commands.py --check` → installed == rendered; rendered from the MAIN checkout (`git rev-parse --git-dir` == `--git-common-dir`). |
| 12 | Fleet blast radius (synced enforcement) | FIXED (1) | F1. `scripts/enforcement/` syncs as a whole DIRECTORY (`fabrik_synced_manifest.py:105`), so this check auto-enrols in ~46 repos; verified silent when `docs/reference/rivals/` is absent, which is most of the fleet. |
| 13 | Advisory output budget | FIXED (2) | **F4** (off-by-two) and **F9** (zero margin). Swept {1,3,6,12} dossiers x {short, 200-char} names: worst was 499/500 — the invariant held with NO headroom, so `MAX_LINE` dropped 220 → 200 and the worst case is now 479/500. Remedy intact and the withheld-findings marker present in every case. Pinned by two tests. |
| 14 | Checkpoint state safety | FIXED (1) | F2. Also verified: `_write_progress`/`_stash_prior` are atomic (`tmp.replace`), every path verifies `job_id` before mutating, and a foreign or corrupt checkpoint degrades to a no-op rather than a traceback. |
| 15 | Test vacuity (the U+2028 class) | CLEAN | All new tests watched RED before their fix. The collision guard additionally proven by a red-on-revert whose mutation was asserted APPLIED before its verdict was read (Lesson 134). No module constant is dead (introspection test). |
| 16 | Testing discipline (`core/45-testing-strategy`) | CLEAN | Watched-fail-first honoured; no cosmetic assertions; no mocked DB (none present). |

## Pass Ledger

| Pass | finders | found | new | fixed |
|---|---|---|---|---|
| Pass 1 (WIDE) | native orchestrator, all 16 classes in one round | 5 | 5 | 5 |
| Pass 2 (SCOPED) | native, the fix diff + its callers | 2 | 2 | 2 |
| Pass 3 (SCOPED) | native, behavior-without-a-test + vacuity over the pass-2 fixes | 1 | 1 | 1 |
| Pass 4 (SCOPED) | native, mechanical: dead symbols · budget re-sweep · render parity · registration | 1 | 1 | 1 |
| **Pass 5 (closing FULL sweep)** | native, all 16 classes re-adjudicated against the final code | **0** | **0** | **0** |

Pass 2 findings: **F6** (a failed re-arm was indistinguishable from "round 1", so a VOIDED round read
as a dry one and would have counted toward convergence) and **F7** (that same path computed
`added = total - 0`, reporting already-known rivals as NEW). Pass 3: **F8** — the `--preflight-only`
ordering was asserted only in a comment; the test written for it initially anchored on the function
DEFINITION rather than the call site and failed against correct code, which is the vacuity class this
review is watching for, so it was re-anchored and then proven by red-on-revert. Pass 4: **F9** — the
advisory output's measured worst case was 499/500, an invariant holding with ZERO margin; `MAX_LINE`
lowered 220 → 200 for 21 chars of headroom (re-measured worst 479/500).

## Phase verdicts

- **Phase 0 (scope + rubric + persist)** — PASS. Surface hashed, rubric generated and applied, report
  created before Pass 1.
- **Phase 1 (finders, recall)** — PASS. All 16 classes assigned in one wide round; 5 raised.
- **Phase 2 (refute)** — PASS. F3 and the data-leak consequence of F5 REFUTED with proof; the rest
  confirmed by execution.
- **Phase 3 (prove & fix + regression guards)** — PASS. 6 fixes, each with a test watched RED first
  or proven by an asserted-applied red-on-revert.
- **Phase 4 (loop to no-op)** — PASS. 5 passes; the closing full sweep raised nothing.

## Closing evidence

Every exit path of the fleet-synced check, re-probed against the FINAL code:

```
  rc=0  argv=''
  rc=0  argv='--bogus'
  rc=0  argv='--root'
  rc=0  argv='--help'
  rc=0  argv='--root /nonexistent'
  rc=0  argv='--root scripts/final_gate.py'
```

Advisory budget re-swept over {1,3,6,12} dossiers x {short, 200-char} names after the F9 fix:

```
  worst total now: 479 /500  (margin: 21 chars)
```

Scoped suites (the surface's own tests):

```
118 passed
```

⚠️ `tests/test_kilo_review_validation.py` and `tests/test_kilo_strictness_scenarios.py` fail to
COLLECT (`ModuleNotFoundError: scripts.kilo_code_review`). Pre-existing and unrelated: Kilo was
retired, the module is gone, and neither file is in this diff. Reported, not touched.

Mechanical gates, this run:

```
$ python -m pytest tests/test_rivals_run.py tests/enforcement/test_rivals_dossier.py -q
118 passed

$ python commands/assemble_commands.py --check
check OK — installed commands + skills match rendered sources

$ python scripts/final_gate.py --json
"status": "success"  (41 blocking / 0 failures)
```

## Findings

- **F1 — `argparse` SystemExit escapes the guard → exit 2 on a fleet-synced `warn_only` check.**
  CONFIRMED by execution. The reference implementation had already solved this
  (`check_plan_lock_release.py:454-456`) and said why; this check copied its guard shape but not its
  parser. Fixed + 4 parametrized tests.
- **F2 — the competitor union had a data-loss window across the engine call.** CONFIRMED by control
  flow (`orchestrator.py:585` overwrite · literal-dict `_persist` · merge at `rivals_run.py:852`).
  Fixed with a durable sidecar + 3 tests.
- **F3 — doc/code parity of the loop shape.** Verified consistent across all three statements; no
  edit required. Raised and REFUTED.
- **F4 — `MAX_LINE` off-by-two.** CONFIRMED (222 vs 220). Fixed + a test.
- **F5 — sidecar filename collision across job_ids.** CONFIRMED collision; the in-file `job_id`
  guard REFUTES the data-leak consequence. Pinned by a red-on-revert test.

## Upstream observation (out of this surface's scope)

`check_plan_lock_release.py:607-608` carries the same `MAX_LINE` off-by-two as F4
(`line[: _MAX_LINE - 1] + "..."` → `_MAX_LINE + 2`). It is not in this review's diff and is not
edited here; recorded so it is not lost.
