# Review — plan-1 decision-ledger, Phase C (corpus wiring + reference doc + docs convergence)

Surface: working-tree Phase-C diff — commands/_fragments/chat-intake.md (ledger joins the ASK-bar
derivation sources) · commands/_sources/fabrik-spec.md (ledger-first episodic step) ·
commands/_fragments/close-feedback.md (the decision line before done) · docs/reference/decision-ledger.md
(new) · INDEX.md (2 rows) · templates/scaffold/docs/DECISIONS_TEMPLATE.md (docs-review fix) — pre-commit.
Mode: NO-POOL — solo native; closing pass re-derivation. Corpus rendered from MAIN (--check first).
Prior-report anchors: phase-A and phase-B reviews cover their surfaces; no overlap re-swept.

## Rubric (review_rubric.py --changed <the 6 paths> — verbatim section heads)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/40-documentation.md  (hit: commands/_fragments/…, commands/_sources/fabrik-spec.md, docs/reference/decision-ledger.md, INDEX.md)
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | security-auth floor (35) | CLEAN | prose-only diff; grep for token/jwt/secret across the 6 files: 0 new statements |
| 2 | data-postgres floor (25) | CLEAN | grep for sql/engine/psycopg across the 6 files: 0 — governance/docs text only |
| 3 | ops floor (30) | CLEAN | no compose/deploy statements; the reference doc's one infra claim (the /opt single-filesystem note) is labeled box-topology explicitly |
| 4 | 12-Factor | CLEAN | no code, no service — the 6 files are md; no process/config/log statements introduced |
| 5 | documentation rules (40) | CLEAN | reference doc: one H1, no skipped levels, fenced-none (no code blocks needed), `-` lists; INDEX tree + table rows both added; commands-are-rules: all three corpus edits are present-tense duties, zero changelog prose |
| 6 | corpus wiring correctness | CLEAN | ledger placed FIRST in both consult orders (ASK-bar sources list; episodic-memory step) matching the spec's "structured beats lexical"; close-feedback decision line names the exact write-trigger classes from the spec and offers the honest `no decisions this run` verdict; render green from MAIN, --check first (32+32+4) |
| 7 | docs-truthfulness (the C6 convergence) | CLEAN | /fabrik-docs-review ran to a no-op: 8 claims reconciled (48 seeds counted; --force-survival test grepped; both mail ids on disk; helper output shape matched live; template headers diffed identical; drift marker; 11 hub rows; query-order consistency across all three texts); 1 finding FIXED — the scaffold template's {{date}} had no substitution mechanism in scaffold.py (grepped), now `(scaffold)` |
| 8 | fail-open vs fail-closed | CLEAN | no gate semantics changed; the close-feedback decision line is a stated duty (advisory-first per the spec's enforcement posture), never claimed mechanical |
| 9 | enforcement-overclaim | CLEAN | the reference doc names the one mechanical check (--check pointers) and the advisory-first posture explicitly; nothing claims gate enforcement of the write duty |
| 10 | boundary/sentinel/prefix | CLEAN | no new labels/regexes; the fragment edits keep their surrounding list/sentence structure (read in context before editing) |
| 11 | behavior-without-a-test | CLEAN | docs/corpus phase — no user-observable code behavior added; the corpus render + drift hook are the mechanical guards |
| 12 | cost/limit edges | CLEAN | no spend |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — all 12 classes over the 6-file diff + the C6 docs-review reconciliation (its 8 claims + 1 fix folded in) | citation | solo native (NO-POOL; dispatched: 0) | 1 | 1 | 1 | the {{date}} placeholder (via docs-review) |
| Pass 2 | CLOSING — re-derivation: render re-verified green; `grep -c "{{" DECISIONS_TEMPLATE` → 0; seed count re-counted 48; doc_sync clean; INDEX rows re-grepped present; ledger-first wording re-read in both rendered commands | method: re-derivation | 0 | 0 | 0 | TERMINAL |

Pass 2 = the no-op: `new: 0`, everything adjudicated.

## Phase C verdict

Phase C verdict: PASS — clean. The query duty is now wired into the moments that matter: spec intake
(ledger before session-recall), the ASK bar (a prior ruling decides an item outright), and every
command close-out (row appended, or "no decisions this run").

```json
{"status": "success", "tier": 2, "passed": 55, "failed": 0}
```
