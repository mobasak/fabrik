# Review — the post-MCP-implementation work (05da7aa9..8917df88, operator-invoked /fabrik-review)

Surface: 5 commits (da96aa0e scaffold emission · 095a1bb7 D-031/D-032 · 7cf35828 D-033/D-034 ·
aed5d45d D-035 · 8917df88 D-036); 3 interleaved sibling commits excluded with attribution
(f04904d0/bea54c0c/786c6f0e — sysadmin/quota beat, Agent-Context verified).
Mode: NO-POOL for work; TWO NATIVE AUTHOR-BLIND fabrik-reviewer finders (partitioned), per the
author-blind law — the finders record nothing (native), the adjudication and every fix are mine.

## Rubric (verbatim output — review_rubric.py --changed scripts/command_run.py scripts/mail.py scripts/sysadmin/emit_mcp_project_config.py .claude/hooks/session_orient.py)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)

## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: the four python surfaces)
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | defs self-consumption (BLOCKER) | FIXED | the hub's DERIVED .mcp.json was also the default defs source — after D-031 stripped postgres-pro from it, a default re-emission deleted the server from every qualifying repo (finder proved live: 4× UPDATE). Fix: static hand-curated scripts/sysadmin/mcp_defs.json (16 servers, retired pruned) heads the chain; regression test on the REAL default chain (the old suite only ever tested --defs fixtures) |
| 2 | scheme normalization (MAJOR) | FIXED | postgresql+asyncpg:// DSNs failed psycopg's parser → 3 live, reachable DBs wrongly denied. Fix: +driver suffix normalized for probe AND emission; re-emission verified: 7 repos now carry postgres-pro (was 4); roster row corrected with the denominator |
| 3 | orphan processes (MAJOR) | FIXED | mcp_health leaked reparented stdio servers (6 measured); fix: start_new_session + killpg in finally; live re-run: 0 orphans after a full probe |
| 4 | negated filing verb (MAJOR) | FIXED | "none — not filed anywhere" classified as filed with a beat attribution that never happened; fix: negation window (3 tokens); telemetry classes preserved |
| 5 | bare-filed floor (MAJOR) | FIXED | a vacuous "filed" passed the floor a vacuous "none" failed; fix: 12-char floor on the filed path |
| 6 | stopword none (MAJOR) | FIXED | "none — nothing to report" passed the byte-count; fix: vacuous-word strip before the 6-char floor |
| 7 | empty/quoted/fenced headers (MAJOR) | FIXED | an empty "WHY:", a quoted forward, and a code-fence template all satisfied D-035; fix: authored-lines-only (fence+blockquote excluded) + ≥2 chars content per key |
| 8 | advisory ordering (MINOR) | FIXED | "Sent anyway" printed before guards that then refused; moved after the last unconditional refusal — refused-secret repro now prints nothing false |
| 9 | case compare (MINOR) | FIXED | Agent-Name vs CLAUDE_AGENT compare now case-normalized both sides |
| 10 | malformed-vs-absent .mcp.json (MINOR) | FIXED | a broken file no longer reads as "no file here" — distinct fix-first message, proven on a broken fixture |
| 11 | name-list cap (MINOR) | FIXED | ORIENT MCP line capped at 40 names + count |
| 12 | docker probe (MINOR) | FIXED | docker-run entries SKIPPED honestly (a probe would launch a live container the kill can never reach) |
| 13 | verified non-defects | CLEAN | finder-verified: commented-out DATABASE_URL unmatched; argv-visible URI acceptable under the single-operator model; probe wall-clock 0.057s measured; no redis rewrite gap; exit paths of the trailer warn untouchable; hub-detection heuristic consistent with its two pre-existing siblings; _STRUCTURED_KINDS ⊂ KINDS deliberate |
| 14 | behavior-without-a-test | FIXED→CLEAN | every one of the 15 fixes carries its red-first regression test (7 new tests added across 3 suites — incl. the FIRST test of the real default defs chain, which the old suite's fixture-only design structurally missed) |
| 15 | cost/quota accounting | CLEAN | zero paid calls; two native finders on subscription; probes local (measured 0.057s wall for the DB sweep) |
| 16 | boundary/sentinel/prefix | CLEAN | the new regexes re-checked: negation window is token-bounded, the +driver rewrite anchors on scheme start, fence-state toggles on line-start ``` only, the none-token strip keeps its separator class |
| 17 | pre-existing, out of scope | CLEAN (noted) | replaying()/CHERRY_PICK_HEAD suppression (ee85d2e9) and _feedback_is_required's fail-open started_at — both documented deliberate choices predating this surface |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — 2 author-blind native finders, partitioned (enforcement/refusal · emitter/health/scaffold), 46 combined verified claims (14+32 denominators) | execution-backed finding | 2 native (NO-POOL for work; native finders per the author-blind law) | 15 | 15 | 15 | 1 BLOCKER + 6 MAJOR + 6 MINOR fixed; 2 noted pre-existing |
| Pass 2 | CLOSING (method: re-derivation) — every finder repro RE-EXECUTED against the fixed code and every count re-derived from primary source: default-chain test green, the 7-repo postgres-pro count re-derived live (`grep -l` = 7, roster row matches), orphans re-counted 0 post-probe, refused-send stderr re-captured empty of the false line, malformed-file message re-rendered distinct; 329 tests across 7 suites; gate 55/0 | method: re-derivation | 0 | 0 | 0 | TERMINAL |

Quiet final pass: found: 0 · fixed: 0 — TERMINAL.

## Step verdict

Step verdict: PASS — the operator-invoked review earned itself a BLOCKER: my own solo scoped
reviews had stamped all five commits clean, and the author-blind pair found 15 real defects in
them, including one that would have silently unwound a shipped feature on the next routine run.

```json
{"status": "success", "tier": 2, "passed": 55, "failed": 0}
```
