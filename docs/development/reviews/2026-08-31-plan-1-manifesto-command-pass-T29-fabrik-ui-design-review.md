# T29 — /fabrik-ui-design: 63b manifesto conformance

Status: DONE

Surface: commands/_sources/fabrik-ui-design.md (282 lines post-fix, wc-derived, read via targeted sections + full grep sweep; the freeze/converge/close-out sections read closely) + the RENDERED command `~/.claude/commands/fabrik-ui-design.md` (429 lines at evaluation: run-record · term-edit · questionbar · subagents-core-adjacent § + close-feedback; re-rendered at merge).
Outcome: 3 source fixes (freeze-ledger mint T05/T15 class; the T16 freeze-law in-review exception; the secrets carve-out — FOURTH occurrence of the T21 class) + D-051 minted (the unrecorded 2026-08-29 design-system-ladder ruling, the D-050 treatment).

## 63b Verdict Table

| intersection | verdict (grep-derived SOURCE anchors, post-fix) |
|---|---|
| (a) checkable gates | CONFORMS — FROZEN is earned only by "the no-op, md5-verified round" (:204); the Build Verification Loop ends only on an edit-free, gate-green, review-clean no-op pass — "a screen shipped without reaching that no-op is not done" (:195-199); "the pass in which you changed the UI is NEVER the last pass" (:163-164); the frozen-chain pin gate (check_frozen_chain, per the flows sibling) covers the stale-PIN mechanically |
| (b) ledger routing + one-way field block | FIXED (+1 ledger mint) — the freeze mints classified-at-mint, STAGED same-commit, the T05/T15 law mirrored (:133-135); AND the command's own Phase-1 hard law — the 2026-08-29 design-system LADDER ruling — had ZERO ledger rows despite living in a commit body + rule pack: D-051 minted with that provenance (the D-050 treatment; checker exit-0). One-way field block N/A — versioned supersede is the unwind path, the T15 adjudication |
| (c) rigor scales with irreversibility | FIXED — the secrets carve-out was missing from the design-time pool fan-out (a route/component file can carry an inline Stripe/Maps/test key that fanout would ship to a pool model — the T21/T25/T26 class, FOURTH occurrence): screens whose files carry secret material now get the NATIVE read or a redacted excerpt (:230-234). Otherwise conformed: DESIGN SYSTEM FIRST (:2, :17); FROZEN + independent attestation both required before planning (:23-26, :216-218, :256-258); the Build Verification Loop is "the UI analogue of the per-phase /fabrik-review gate" (:150, :212) |
| (d) labeled verified/assumption evidence | CONFORMS — md5-verified no-op as the freeze proof (:204); the re-freeze close-out demands the Downstream impact table with zero-hits-stated (the flows-mirror close-out, :258-275); commit-not-approve distinction — "committing a DRAFT/FROZEN artifact is not approving it" (:142) |
| (e) captured disorder | CONFORMS — the re-freeze close-out names what changed and the owed downstream re-freezes as the NEXT line, "the impact list is the part only you know" (:271-275); the design-system adopted-vs-created state is carried in the header (:132); close-feedback rides |
| (f) most-reversible default under ambiguity | FIXED — the freeze law reproduced the exact T16 gap: it routed re-freezes through /fabrik-ui-design ONLY while the review sibling's in-review re-freeze edits the FROZEN artifact directly (ui-design-review :141-143); the law now names the sanctioned exception, mirroring the T16 flows fix verbatim (:132). Handing off on an unattested FROZEN contract stays banned (:216-218); questionbar governs; COMMIT-superseded clause holds (:142-145) |

6/6 adjudicated: 3 CONFORMS, 3 FIXED (+1 ledger mint).

## Scoped verification review (nested /fabrik-review)

| round | findings | disposition |
|---|---|---|
| 1 — author-blind fabrik-reviewer verifier | 6 candidates: **3 CONFIRMED** (the freeze law reproduced the T16 in-review-re-freeze gap verbatim — ui-design-review edits the FROZEN artifact directly at :141-143 while the law routed through this command only → exception clause mirrored from the T16 flows fix; cell (c)'s two anchors off-target — the real quotes at :150 and :212; cell (a)'s :160-163 anchor started on a blank line) · **2 PLAUSIBLE adopted** (the secrets carve-out missing from design-time pool fan-out — FOURTH occurrence of the T21 class → native-or-redacted rule :230-234; the design-system LADDER ruling unminted despite being Phase-1 hard law → D-051 with commit-body + rule-pack provenance, decisions.py --check exit-0) · **1 observational** (render staleness — expected mid-ticket, per T25 precedent). Angles CLEAN: both counts exact, check_frozen_chain wires ui-design.md + design-system.md, the T15 one-way adjudication carried without regression, mint wording verbatim-consistent | 3 source edits + 1 ledger mint + artifact re-grounding |
| 2 — closing re-derivation sweep | found: 0, fixed: 0 — the exception clause, carve-out, and mint re-read post-edit; D-051 verified in-ledger; anchors re-derived against the 282-line source | TERMINAL no-op |

Verifier falsification streak: 29-for-29 — headline: the T16 class reproduced verbatim on the sibling freeze surface, and the T21 secrets class fell for a FOURTH time.

## Per-finding disposition ledger

1. Freeze-law exception gap (CONFIRMED, T16 class) → the sanctioned-exception clause mirrored (:132).
2. Anchor drift ×2 in (c) (CONFIRMED) → :150, :212.
3. (a) blank-line anchor (CONFIRMED minor) → :163-164.
4. Secrets carve-out (PLAUSIBLE→REAL, 4th T21 occurrence) → native-or-redacted rule (:230-234).
5. Unminted LADDER ruling (PLAUSIBLE→REAL, D-050 class) → D-051 minted; checker exit-0.
6. Render staleness (observational) → merge render carries all fixes.
