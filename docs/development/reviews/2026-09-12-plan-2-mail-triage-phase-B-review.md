# Review — 2026-09-12-plan-2-mail-triage-phase-B

**Status:** IN-PROGRESS
**Surface:** `git rev-parse HEAD` = 702b4c2f8919f8b3f4c0190934ccf359d7fa217f; range tip 702b4c2f8919f8b3f4c0190934ccf359d7fa217f; `git diff fb8e86e3~1..HEAD -- commands/_fragments/term-edit.md docs/workflows/FINAL_GATE_WORKFLOW.md scripts/command_run.py scripts/enforcement/check_convergence.py scripts/enforcement/check_doc_index.py scripts/enforcement/check_hooks_index.py scripts/enforcement/check_plan_tickets.py scripts/enforcement/check_review_coverage.py scripts/enforcement/check_review_hygiene.py scripts/enforcement/check_ticket_breadth.py scripts/final_gate.py scripts/review_rubric.py tests/enforcement/test_check_doc_index.py tests/enforcement/test_check_hooks_index.py tests/enforcement/test_check_plan_tickets.py tests/enforcement/test_check_review_hygiene.py tests/enforcement/test_flip_gate_matrix.py tests/enforcement/test_plan_tickets_epic_scope.py tests/enforcement/test_review_exit_contract.py tests/test_check_convergence.py tests/test_check_review_coverage_precommit.py tests/test_command_run_feedback_required.py tests/test_command_run.py tests/test_final_gate_advisory_display.py tests/test_review_rubric.py` md5 1f4d05aa28cf5cb538fbcd8f5eecd074 (80950 bytes)
**Command:** /fabrik-review · **Changed:** `commands/_fragments/term-edit.md`, `docs/workflows/FINAL_GATE_WORKFLOW.md`, `scripts/command_run.py`, `scripts/enforcement/check_convergence.py`, `scripts/enforcement/check_doc_index.py`, `scripts/enforcement/check_hooks_index.py`, `scripts/enforcement/check_plan_tickets.py`, `scripts/enforcement/check_review_coverage.py`, `scripts/enforcement/check_review_hygiene.py`, `scripts/enforcement/check_ticket_breadth.py`, `scripts/final_gate.py`, `scripts/review_rubric.py`, `tests/enforcement/test_check_doc_index.py`, `tests/enforcement/test_check_hooks_index.py`, `tests/enforcement/test_check_plan_tickets.py`, `tests/enforcement/test_check_review_hygiene.py`, `tests/enforcement/test_flip_gate_matrix.py`, `tests/enforcement/test_plan_tickets_epic_scope.py`, `tests/enforcement/test_review_exit_contract.py`, `tests/test_check_convergence.py`, `tests/test_check_review_coverage_precommit.py`, `tests/test_command_run_feedback_required.py`, `tests/test_command_run.py`, `tests/test_final_gate_advisory_display.py`, `tests/test_review_rubric.py`
**Plan:** `docs/development/plans/2026-09-12-plan-2-mail-triage-command-machinery.md`
**Anchor:** the newest receipt for this scope is `docs/development/reviews/2026-09-12-plan-2-mail-triage-phase-A-review.md`, whose `Surface:` is a commit RANGE (`b9981f45~1..219ebe6a`) — a range cannot match a `HEAD + md5` digest by construction, so this run does NOT inherit its checklist and round 1 is the full WIDE partitioned pass over the 25 files (7 native seats: opus×4 on scripts/command_run.py · check_convergence.py + check_review_coverage.py · check_plan_tickets.py + check_ticket_breadth.py + check_hooks_index.py + check_doc_index.py · check_review_hygiene.py + final_gate.py + review_rubric.py; sonnet×3 on the four command_run/convergence/coverage test files · the nine enforcement/rubric/gate test files · term-edit.md + FINAL_GATE_WORKFLOW.md + plan↔code parity), every file read once. Pin: `git archive 702b4c2f` at the scratchpad `execB/pinB/`, range diff md5 `1f4d05aa28cf5cb538fbcd8f5eecd074`. Hygiene at the round's start: 4 hits over 25 files, all four template-residue at `docs/workflows/FINAL_GATE_WORKFLOW.md:164` and `:267` where the doc DESCRIBES the residue class — see § Residual.

## Coverage Checklist

> The rows below carry the verdict of the round that adjudicated them (round 1 for the files the delta rounds kept touching); they are re-adjudicated to their FINAL state at the close, one leading verdict each — until then the per-round disposition ledgers are the current record (round 11, R13). **Re-adjudicated at the close against the commit chain** (§ Re-adjudication basis): 24 of the 25 rows already carried their final verdict; ONE changed — `check_ticket_breadth.py` from FIXED to CLEAN, because no round's commit touches that file and F28's fix landed in its three readers, each of which has its own row.

Rubric invocation (verbatim output — the gate reads the generated header, never a prose mention):

```text
$ python scripts/review_rubric.py --changed commands/_fragments/term-edit.md docs/workflows/FINAL_GATE_WORKFLOW.md scripts/command_run.py scripts/enforcement/check_convergence.py scripts/enforcement/check_doc_index.py scripts/enforcement/check_hooks_index.py scripts/enforcement/check_plan_tickets.py scripts/enforcement/check_review_coverage.py scripts/enforcement/check_review_hygiene.py scripts/enforcement/check_ticket_breadth.py scripts/final_gate.py scripts/review_rubric.py tests/enforcement/test_check_doc_index.py tests/enforcement/test_check_hooks_index.py tests/enforcement/test_check_plan_tickets.py tests/enforcement/test_check_review_hygiene.py tests/enforcement/test_flip_gate_matrix.py tests/enforcement/test_plan_tickets_epic_scope.py tests/enforcement/test_review_exit_contract.py tests/test_check_convergence.py tests/test_check_review_coverage_precommit.py tests/test_command_run_feedback_required.py tests/test_command_run.py tests/test_final_gate_advisory_display.py tests/test_review_rubric.py
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3; SERVICE surface)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
- An approval link opened somewhere the user did not start must never mint a session silently.
- > **Fail-closed invariant (hard, every mode).** `auth.uid()` and `current_tenant_id()` MUST return `NULL` (→ the policy denies) on unset, empty, or malformed claims — wrap the body in `EXCEPTION WHEN OTHERS THEN RETURN NULL`. **Never** raise and never default to a value: an error-open helper turns one bad/empty JWT into a full cross-tenant read. This is the single most security-critical line in the build — verify it explicitly with a no-context probe (`SELECT auth.uid()` → `NULL`).
- The JWT signing secret must be at least 256 bits, generated via `openssl rand -hex 32`, and injected via Pydantic Settings. Never hardcode it.
- **Pin the algorithm in the VERIFIER** — pass an explicit allow-list (`algorithms=["HS256"]`), never let the library dispatch on the token header's `alg`. Header-driven dispatch is the classic confusion attack (an RS256 public key replayed as an HS256 HMAC secret); `alg: none` is rejected unconditionally.
- "Sticky sessions are a violation of twelve-factor and should never be used or relied upon."
- => Mandate: processes are stateless/share-nothing. **STICKY SESSIONS ARE BANNED** (not just file-based sessions). Session state goes to `redis-main` (Redis) with a TTL. Never in-process memory, never on local disk. Any design that assumes "the same user hits the same process" is a violation.
- **Pattern B (legacy / migration-only):** The Supabase client SDK handles token storage. On mobile, wrap with `expo-secure-store` (never AsyncStorage or MMKV for tokens). See `80-mobile.md` § Backend Integration.
- **Both patterns:** Never store JWTs in `localStorage` or `sessionStorage` on web. Never store JWTs in AsyncStorage or MMKV on mobile.
- **Chrome Extension (MV3) specifics:** `chrome.storage.session` defaults to `TRUSTED_CONTEXTS`, so **content scripts cannot read the token** — keep it in the SW / extension-page context and have content scripts fetch it via SW-mediated messaging (`chrome.runtime.sendMessage`), not a direct read. For social login use `chrome.identity.launchWebAuthFlow` with **PKCE** (`code_verifier` via `crypto.subtle`, held in `storage.session`, redirect `https://<ext-id>.chromiumapp.org/`); the **backend** does the code-for-token exchange. **Never a heavy browser auth SDK** (Auth0-SPA-JS, `oidc-client-ts`) — they assume DOM/`localStorage`/iframes and break in the service worker. Pin a manifest `key` so the extension ID (and thus the `chrome-extension://<id>` CORS origin) is stable across machines. Full detail: `chrome-ext/70-chrome-ext.md`.
- **Never rely solely on the framework's request-shaping layer for access control.** CVE-2025-29927 (the `x-middleware-subrequest` bypass) proved COMPLETE middleware bypass via one crafted header; it is long patched upstream, but the rule outlives the patch — current Next.js even RENAMED the file to say so: `middleware.ts` became **`proxy.ts`**, explicitly repositioned as request-shaping, not a security boundary. ⚠️ **On current majors a leftover `middleware.ts` is SILENTLY IGNORED at build** — nonce injection and redirects stop executing with no error; rename it when upgrading.
- `CORSMiddleware` in FastAPI must populate `allow_origins` from environment variables (Pydantic Settings). Never hardcode origins.
- `X-Frame-Options: DENY` — kept as the legacy fallback only; formally obsoleted by `frame-ancestors`, never ship it ALONE
**Never** write inline `APIKeyHeader` / `require_api_key`. **Never** use per-service key names (`SERVICE_API_KEY`, `PROXY_API_KEY`). Scaffold `python-api` auto-emits `internal_auth.py`, `metrics.py` (REQUEST_COUNT / ERROR_COUNT / ACTIVE_JOBS / PROCESSING_COUNT), `/metrics` endpoint (Authelia-bypassed), and `SERVICE_INTERNAL_SECRET_KEY` in `.env.example`.
- => Mandate: config via env vars only (`os.getenv("KEY", "default")`); **ZERO secrets/constants in code**. Apply the open-source litmus test to every change. **BANNED**: grouped/named env config sets (e.g. a `config/production.yml` or a `settings.production` group) — env vars are granular and orthogonal, set per deploy. (The pack already covers secret handling — cross-reference existing secret patterns and extend with config orthogonality.)
- [ ] Mobile tokens stored in `expo-secure-store` — never AsyncStorage or MMKV.
- > **⚠️ Bearer bypass scope — security-critical.** The bypass defaults to `^/api/`, which makes the **entire** `/api/*` surface public (un-2FA'd). If the application authenticates only a **sub-prefix** (e.g. `/api/v1` carries the bearer/internal-token check) while OTHER `/api/*` routes are unauthenticated (legacy / admin / destructive), you **MUST** narrow the bypass with `shape.bearer_bypass_prefix: "^/api/v1"` — otherwise `fabrik apply` exposes those routes to the public internet. **Bypass ONLY the path the app itself authenticates.** Value must start with `^/`; the verifier (`orchestrator/verifier.check_api_bypass`) probes the configured prefix on deploy. When unsure whether a service has un-auth'd `/api/*` routes, ask the app owner before relying on the `^/api/` default.

### core/25-data-postgres.md
| Vector search | pgvector on `postgres-main` + `fabrik-lib/rag` — ⚠️ the extension is NOT currently installed there (probed 2026-09-01: `postgres:16-alpine`, `plpgsql` only); a project needing vectors REQUESTS the fleet infra change first, never assumes it | same `postgres-main` DSN |
**"Own database" means a DATABASE on `postgres-main`, never a database SERVER.** Per-project and per-tenant isolation is a separate database (its own name, its own role) on the shared container — isolation, quota and backup are all satisfied at that grain. A dedicated Postgres instance is a decision, not a default: it needs its own `docs/DECISIONS.md` row naming what the shared server cannot serve (web-ecommerce-factory 01M1Q8X9, 2026-09-05: "one DB per store" read naively as one server per customer).
- Use Pydantic `BaseSettings` (per `10-python.md` § Config Loading) — never raw `os.getenv` **for an APPLICATION's settings surface**:
- ⚠️ **Scope, stated here because this LINE is what `review_rubric.py` injects — without its section.** The rubric FLOOR-injects this mandate *and* `35-security-auth`'s "config via env vars only (`os.getenv("KEY", "default")`)" into every finder prompt on every review, so a finder reading both literally has two rules it cannot both satisfy, and files a false positive on whichever it applies. The carve-out: `BaseSettings` governs a SERVICE's config surface (a `Settings` object, DB/Redis DSNs, secrets). A **vendored fabrik-lib module** has no settings object by design — it reads its own knobs with bare `os.getenv("KEY", "default")`, which is `35-security-auth`'s mandate being satisfied, not this … (wrapped further — read the pack)
- Never blindly trust `--autogenerate`. Always review `upgrade()` and `downgrade()` for unintended column drops, rename misinterpretations, and ENUM alterations before committing.
- > **Older pythons only** (services pinned below stdlib-uuid7 — which today includes SCAFFOLDED services: the scaffold still emits an older interpreter and ships `uuid-utils`; alignment tracked in the backlog): import `uuid7` from `uuid_utils.compat`, never `uuid_utils.uuid7()` directly — the latter returns `uuid_utils.UUID`, which asyncpg rejects (not a stdlib `uuid.UUID`). **DB-side:** newer PostgreSQL majors ship native `uuidv7()` (probe: `SELECT uuidv7()`); prefer `DEFAULT uuidv7()` at schema level where it exists. `postgres-main` currently runs major <!--v:postgres_major-->16<!--/v-->, which predates it — generate app-side on the fleet.
- Foreign keys must declare `ON DELETE` behaviour explicitly — `CASCADE` if children cannot exist without the parent, `RESTRICT` to protect audit trails. Never rely on the implicit default.
- This section owns the **canonical** engine, session, and `get_db`. `10-python.md` imports from here — never redefines its own.
- Database `AsyncSession` must be scoped to the route handler via `Depends()`. Never open sessions or transactions in global middleware — this holds connections during serialisation and I/O, exhausting the pool.
**BANNED as a server-side backing service** (dev, test, and prod alike):
**⚠️ SCOPE — this ban is about BACKING SERVICES, not client-local storage.** It does **NOT** apply to:
- **`desktop-app`** — SQLite is the **mandated** engine there (`desktop-app/72-desktop.md` § Local Persistence: `better-sqlite3` + SQLCipher; *"Production builds MUST encrypt the local SQLite file"*).
**12-Factor IV (Backing Services) — generalised:** swapping ANY attached backing service (DB, cache, object storage) is a **config change, never a code change**. The handle lives in `DATABASE_URL` / `REDIS_URL` / storage env — the code *reads* it, the code does not *decide* it. Never `if ENV == "prod":` branching to pick a host. (See § PostgreSQL Host Selection, which already mandates this for the DB.)
- [ ] All primary keys use UUIDv7 — stdlib `uuid.uuid7` on current Python (older pythons: `uuid_utils.compat.uuid7`, never direct `uuid_utils.uuid7()`); no `uuid4()`.

### core/30-ops.md
- the pinned release leaves full security support, never per-pack.
- All services deploy via `fabrik apply` (SSH + Docker Compose) on the `fabrik` network. Traefik routes external traffic — services do NOT bind host ports.
- **No `ports:` section.** All external traffic routes through Traefik. Never bind host ports. See Docker Port Security below. **12‑Factor VII (Port binding):** "the app is self‑contained and exports HTTP by binding to a port; it does not rely on runtime injection of a webserver" — which is exactly WHY no host `ports:`.
- **`container_name: <name>` is mandatory.** Same `_validate_compose()` gate refuses any service without it. Stable names are required so Gatus endpoints, inter-service URLs, and `docker exec`/`docker inspect` keys don't drift per redeploy. Use the bare service name (`browserless`, `gotenberg`, `meilisearch`, `glitchtip-web`, `site-provisioner`, etc.) — never UUID-suffixed names.
- gets one (ruling D-052) — see `core/60-watchdog.md`. Do not author a `watchdog: { enabled: false }` opt-out; if a project genuinely cannot host the sidecar, that is a ruling to obtain, not a default to flip.
- path before the flag goes in the spec, and assert target health (`/api/v1/targets` → `up`), never a bare `curl` of a path you assumed.
- VOLUME gets a plan pointed at a directory that never exists — a paper backup that reads green and archives nothing. *Measured: `/opt/zitadel/data` is absent while the `zitadel-data` plan points at it.* If the data is a volume, say so in the spec comment and rely on the global `docker-volumes` plan; never let a service-named plan be mistaken for the protection.
- health-enabled service can NEVER pass `up -d --wait` on a fresh database, and the deploy hangs to timeout. *Measured: trytond bakes an init script but sets no `command:`, and its base entrypoint is a bare `exec "$@"`.* An init the deploy cannot perform itself is a runbook step the plan MUST own.
- `fabrik redeploy <app>` SSHes to the VPS and runs `git pull` + `docker compose up -d --wait` against the **GitHub remote**, NOT the local `/opt/<app>` clone. Skipping `git push` redeploys the previous remote commit — the VPS never sees local changes.
**Mandate:** build → release → run are strictly separated. Releases are IMMUTABLE; the git SHA is the release ID. NEVER hot‑patch a running container (no `docker exec` to edit code/config in place, no in‑place code mutation on the VPS). Any change = a new build + a new release via `fabrik apply` / `fabrik redeploy`.
- Runtime database migrations that modify the app container (migrations MUST be run as separate deploy‑time steps)
**Place a service next to its data.** A spoke-hosted service reaches `postgres-main`/`redis-main` over the WireGuard mesh, and that hop is cross-Atlantic (Coventry ↔ LA) on EVERY query — a per-request chatty service pays it hundreds of times per page. So a DB-chatty service targets vps1; a spoke earns a service whose data traffic is light, batched or cached; a service PINNED to a spoke by hardware (GPU) batches or caches its data access — the data never moves off vps1. Measure before choosing (`ping 10.99.0.1` from the spoke, and the request's query count), never assume — the correctness rule ("container DNS, never localhost") says nothing about latency (web-ecommerce-factory 01M1Q8X9, 2026-09-05).
**Mandate:** WSL dev and the VPS run the SAME backing services (PostgreSQL + Redis), same major version. NEVER substitute a different backing service in dev (no SQLite standing in for Postgres, no in‑memory dict standing in for Redis). The same code must run unmodified in both environments.
- WSL runs PostgreSQL + Redis at the SAME MAJOR as the VPS containers — probe the live truth, never copy a tag from a doc: `ssh vps "sudo docker inspect postgres-main redis-main --format '{{.Config.Image}}'"` (2026-09-01: `postgres:16-alpine` · `redis:7-alpine` — upstream official images, outside OUR-image Alpine ban per § Banned Patterns)
**Invariant:** Never use `ports:` in compose.yaml to expose internal services to the host. All external traffic must go through Traefik.
**Health endpoints (`/health`, `/healthz`, `/metrics`, `/api/health`) bypass Authelia on all services** — required for Gatus and Prometheus monitoring. The bypass is **resource-based, not domain-bound** — applies on every domain routed through Authelia (hub direct + spokes via `authelia-vps1@file` middleware). Never protect these paths.
**CRITICAL:** Use `web`/`websecure` in Traefik labels — never `http`/`https` (those entrypoints do not exist). The scaffolder emits the correct entrypoint names; if you hand-write labels, match these exactly.
**Mandate:** migrations and admin tasks run as a ONE‑OFF process against the DEPLOYED image + env — identical environment to regular processes. NEVER run admin tasks from a laptop against prod, NEVER via `docker exec` into a live container, and **ABSOLUTELY NEVER auto-run migrations from app startup/`lifespan`** (concurrent replicas race the Alembic version table → wedged deploy).
- > sees a file that looks exactly like a migration step, and ships a deploy where migrations never run —
- > the rule producing the very defect it exists to prevent. Do not re-add either without a `path:line` in
**Processes are share-nothing:** any state shared across requests MUST go to Redis (`redis-main`) with a TTL. A project using Redis for sessions MUST declare `shape.needs_cache: true` in `specs/services/<id>.yaml`, or `fabrik apply` skips the Redis registrar and the deploy is silently broken.
- "A twelve-factor app never relies on implicit existence of system-wide packages"
**Mandate:** any binary the app shells out to (ffmpeg, yt-dlp, poppler, tesseract…) MUST be `apt-get install`-ed in the Dockerfile, with a `shutil.which()` startup probe that fails fast. **The pinned base image is the version boundary** — exact `=version` apt pins are banned: they break on every Debian point release as old debs leave the mirrors (the "works then mysteriously breaks" class this section exists to prevent); the codename pin + image digest give the reproducibility. Never assume `curl`/ImageMagick/ffmpeg exist in the image — they don't by default.

### 12-FACTOR (all twelve axes)
- I codebase: shared code → fabrik-lib, never two apps in one repo
- II deps: every shelled-out binary installed + pinned in the Dockerfile
- III config: granular env vars; no secrets in code; no grouped env sets
- IV backing services: swappable by DSN/config change only
- V build/release/run: releases immutable; never hot-patch a container
- VI processes: stateless; session state → redis-main; no sticky sessions
- VII port binding: bind in-container; Traefik routes; no host ports:
- VIII concurrency: scale out; never daemonize or write PID files
- IX disposability: SIGTERM returns in-flight jobs to the queue; jobs idempotent
- X dev/prod parity: same backing services everywhere; no SQLite-for-Postgres
- XI logs: unbuffered stdout only; the app never writes/rotates a logfile
- XII admin: migrations/one-offs run against the deployed release, never startup

## MATCHED — packs whose globs hit the changed paths

### core/10-python.md  (hit: scripts/command_run.py, scripts/enforcement/check_convergence.py, scripts/enforcement/check_doc_index.py)
**`uv`** is the mandated Python package manager. Never use raw `pip`, `pip install`, `poetry`, or `pipenv`.
- Dependencies live in `pyproject.toml` + `uv.lock`. Do not modify these files unless the ticket authorises it.
- its own reviewed commit, never as a side effect of unrelated work.
- The one RULE: use SQLAlchemy async consistently — never mix `async def` with sync `.query().all()` (the Banned table row; the full session pattern is `25-data-postgres.md`'s).
- The canonical `engine`, `async_session`, and `get_db` are defined in `src/database.py` — owned by `25-data-postgres.md`. Import from there, never redefine:
**Config convention:** apps read a complete `DATABASE_URL` (`postgresql+asyncpg://user:pass@host:port/db`) and `REDIS_URL` from env. Discrete `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD` for the app to assemble are **banned**. The env supplies the complete URL — `localhost` in WSL, `postgres-main` on VPS — so the host concern is an env-layer responsibility, never code logic. See `30-ops.md` compose template for how discrete vars are interpolated into `DATABASE_URL` at the compose level.
- volume** (`30-ops.md` § Volumes), never in `.tmp` and never in `/tmp`.
**GlitchTip discipline:** unhandled exceptions (FastAPI 500s) are auto-captured by GlitchTip with full stacktraces. In the `except Exception` branch, log a **short event name + correlation_id** — never `logger.exception()` (that duplicates the traceback in Loki AND GlitchTip). See `55-observability.md` § Error Reporting for the full rule.
**Note:** Use the scaffolded logger: `from {package}.logger import get_logger` (see `55-observability.md` § Pre-Scaffolded Logging). Do not use `structlog.get_logger()` directly or `logging.getLogger(__name__)`.
- **Never a bare `asyncio.create_task()`** — an unreferenced task is silently garbage-collected and its exceptions vanish. Hold the reference and await it, or use `asyncio.TaskGroup`.
- **`datetime.now(UTC)`, never `datetime.utcnow()`** — deprecated and naive; naive datetimes are a real cross-service defect class.
- Ruff's selected rule-sets MUST include `ASYNC` (blocking IO in async code — machine-enforces this pack's hardest-to-review rule), `B` (bugbear) and `S` (bandit) alongside the defaults; configured in `pyproject.toml`, emitted by the scaffolder.
- Production services run via `uvicorn` CLI in the Dockerfile, not `uvicorn.run()` in code. Base image is always the pinned Debian `-slim` variant on `linux/amd64` (the variant is pinned fleet-wide in `30-ops.md` § Container Base Images — change it THERE, never per-repo). Never use Alpine — musllinux wheels exist now (PEP 656) but coverage is still partial, source builds are dramatically slower, and musl's allocator/stack defaults degrade CPython; the trade never pays on this fleet.
- `uvicorn.run()` is for local development only. Never ship it in production code.
- a fleet scaling decision (more containers), never a per-app flag.
**BANNED: grouped/named env config sets.** 12F is explicit — *"env vars are granular controls, each fully orthogonal to other env vars"* — so a `config/production.yml`, a `settings.production` group, or a `config/{dev,staging,prod}.yaml` tree is a violation. Env vars are granular and set **per deploy**, never batched into a named "environment".
**BANNED:** `logging.FileHandler`, `logging.handlers.RotatingFileHandler`, `TimedRotatingFileHandler`, `loguru` file sinks, any `*.log` file write, any in-app log rotation/retention/cleanup. The app never decides where logs are stored or routed — Docker → Promtail → Loki does. Full rule: `55-observability.md` § Logs.
**Factor XII — Admin processes. NEVER migrate from app startup.**
**BANNED: `alembic upgrade head` in FastAPI's `lifespan`, in an `@app.on_event("startup")`, or as an import side-effect.** With more than one replica (or a restart storm) two containers run `upgrade head` **concurrently** → they race the Alembic version table → duplicate DDL → **wedged deploy**. Migrations are a **one-off admin process against the deployed release**: `docker compose run --rm <svc> alembic upgrade head` (see `30-ops.md` § Release & Admin Processes).

### core/40-documentation.md  (hit: commands/_fragments/term-edit.md, docs/workflows/FINAL_GATE_WORKFLOW.md)
- > **⚠️ `docs/OPERATIONS.md` + `docs/DEPLOYMENT.md` are FLEET-AI INTERFACES, not just docs (D-065).**
- **Tier-1 (author → verify → converge; the author leg is NATIVE while the pool is OFF, D-181 — `scripts/doc_reconcile.py`'s pool author cannot dispatch):** for each **mechanically-detectable** doc whose Doc-Sync trigger fired (`docs/QUICKSTART.md` · `docs/CONFIGURATION.md` · `docs/data-contract.md` · `docs/SERVICES.md` · `docs/OPERATIONS.md` — the reliable-signal subset), `scripts/doc_reconcile.py` dispatches a cheap OpenRouter-pool author (`libs.subagents`, `pick_models("docs")`) to emit a **minimal structured patch**, **verifies it before applying** (a symbol cross-check catches invented endpoints; the orchestrator injects a higher-assurance native-Claude verify), and loops to a zero-edit round. Runs per phase in `/fabrik-execute-plan`; never blocks (fail-safe). The other docs (CHANGELOG, INDEX, FEATURES, RESILIENCE, PORTS, the READMEs, `db/schema.sql`, …) have no reliable mechanical content-signal → they rely on the touch-on-change backstop below + your own edit (force-update, not force-correct).
- The SSOT is the type-aware registry (`scripts/enforcement/_doc_registry.py::PROJECT_DOCS`) — this table is its project-facing rendering, kept in step, never a second truth. `/fabrik-plan-after-chat` (the plan set's spine + tickets — the ticket-format authority) injects these rows per ticket as its `Docs:` line.
- Standalone work (not plan execution) → `Agent-Role: primary`. Trailers go below a blank line, above `Co-Authored-By`. ⚠️ The trailer block must be its OWN paragraph with NO blank line inside it: git parses only the LAST paragraph, and only if it is all-trailers. A blank line before `Co-Authored-By:` demotes everything above it to prose; so does a prose line glued to the top of the block. Measured 2026-08-15: 200 of the last 200 hub commits carried `Agent-Role:` and only 10 parsed, because the old example here shipped the blank line.
- **⚠️ Link it or it is decoration.** *Measured:* requests for files that do NOT exist came ~zero from AI bots — agents never go looking. It follows (inference, not measurement) that a file only gets read when something points at it: reference it from the docs index or README.
- ⚠️ **In THIS repo `llms.txt` is GENERATED** (`scripts/generate_capability_index.py`, refreshed daily) — never hand-edit it; change the generator. A project writing one by hand owns it.
- either way. Cheap and reversible — never at the expense of `OPERATIONS.md`/`DEPLOYMENT.md`, which are the load-bearing agent interfaces (D-065).
- **No skipped heading levels** — `##` to `###`, never `##` to `####`
- **Fenced code blocks only** — never indented code (AI treats it inconsistently)

### core/45-testing-strategy.md  (hit: tests/enforcement/test_check_doc_index.py, tests/enforcement/test_check_hooks_index.py, tests/enforcement/test_check_plan_tickets.py)
- **Behavior Contract**: every ticket enumerates its distinct **user-observable behaviors / acceptance criteria** and tests **each one** — one high-value integration/E2E test per behavior, risk-ordered, TDD for the risky ones. Skip trivia (getters / framework glue / config): **lean-but-complete, NOT 100%-line-coverage dogma**. Do not chase line coverage — ensure every behavior has a test that would fail if that behavior regressed. (Cheap pool subagents can author the per-behavior tests — the suggest→curate→author→fix workflow in `62-using-subagents.md` § Dispatch policy + `~/.claude/commands/fabrik-review.md`.)
- **No cosmetic assertions**: never assert against CSS classes, Tailwind utility strings, pixel measurements, or snapshot hashes. Assert application state and user-visible outcomes only.
- **Watched-fail-first** (for tests this change adds or modifies; trivia stays skipped per the Behavior Contract): a non-trivial behavior's test proves something only if it has been SEEN RED — either write it first and watch it fail, or (after the fact) neuter the fix/feature, prove the test goes red, then RESTORE and re-run to green. The neutered state is never staged, committed, or left in the tree. A green test never seen red is unverified — a suite can pass with its guard deleted.
- **Run tests**: `uv run pytest tests/` (never bare `pytest` — Fabrik uses `uv`) — **when the project has a `pyproject.toml`/`uv.lock`**. A `requirements.txt`-only project (no manifest) runs `.venv/bin/python -m pytest tests/` — the manifest clause chooses the RUNNER, it never disarms the mandate to run the suite (web-ecommerce-factory 01M1QEY5, 2026-09-05: the clause read as "does not apply here"). ⚠️ **Gate this on the manifest, because this line is FLOOR-injected into finder prompts and a vendored fabrik-lib MODULE has neither by design**: the module recipe ships `requirements.txt` (`fabrik-lib/README.md` § Creating a Reference Implementation), so `uv run` cannot resolve it and `python3 -m pytest` is the only thing that works. Telling a finder the sole working … (wrapped further — read the pack)
- **Zero-mock database policy**: never mock SQLAlchemy, SQLModel, or database sessions. All backend tests execute against a real PostgreSQL instance.
- **`ASGITransport` never runs lifespan** — anything the app initializes at startup (scaffolded apps are lifespan-based) silently does not exist in tests; wrap with `asgi-lifespan`'s `LifespanManager` when a test needs startup state.
- Use `structlog` in test helpers if logging is needed — never `print()`. See `55-observability.md`.
- **Never stub a server action from Playwright** — the server is the E2E boundary; stubbing belongs in the unit lane where the action is a plain function.
- Run Playwright against the PRODUCTION build (`next build && next start`), never the dev server.
- All locators must be **semantic**: `page.getByRole('button', { name: /submit/i })`. Never use CSS selectors or XPath.
- Launch Playwright's **bundled Chromium** (`channel: 'chromium'`) — stable Chrome/Edge removed the `--load-extension` / `--disable-extensions-except` side-load flags (Chrome 137/139), so those args only work under bundled Chromium, never installed stable Chrome.
- Run `@axe-core/playwright` with **`bypassCSP: true`** (the non-relaxable extension CSP otherwise makes axe throw on `chrome-extension://` pages); keep `@axe-core/playwright` a **dev-dependency only** (MPL-2.0 — never bundled into the shipped artifact). Gate bundle size with `size-limit` **per surface** (popup / side-panel / content-script). Full loop: `chrome-ext/70-chrome-ext.md` § Testing & UI Verification.
- Keep the generated types committed and re-generate on schema changes (`uv run python -c "import json; from <package>.main import app; print(json.dumps(app.openapi()))" > openapi.json` — the scaffold emits `src/<package>/main.py`, never a flat `src/main.py`, so `src.main` imports nothing).
**BANNED in tests:**
| A test THIS change adds/modifies that was never seen red (no fail-first, no red-on-revert proof) | Watch it fail first, or neuter the change → prove red → restore → re-run green |
- [ ] Destructive DB tests call `require_throwaway(TEST_DATABASE_URL)` before connecting — never point them at a dev/shared DB.

# promote-to-check_*: 81 injected mandate(s) look deterministically greppable — their backtick literals, one line each (the full mandates are ABOVE, not repeated: re-emitting ~20 FLOOR lines verbatim doubled the rubric and got it skimmed — web-ecommerce-factory 01M1QEY5, 2026-09-05)
- `fabrik-lib/fastapi-user-auth` `DELETE … RETURNING` `jti` `agents-fabrik.md § Supabase`
- `chrome-extension` `chrome.identity.launchWebAuthFlow` `https://<ext-id>.chromiumapp.org/` `chrome.storage.session`
- `desktop-app` `safeStorage` `desktop-app/72-desktop.md`
- `fabrik` `X-Internal-Token` `internal_auth.py` `hmac.compare_digest` `APIKeyHeader`
- `auth.uid()` `current_tenant_id()` `NULL` `EXCEPTION WHEN OTHERS THEN RETURN NULL` `SELECT auth.uid()` `NULL`
- `openssl rand -hex 32`
- `algorithms=["HS256"]` `alg` `alg: none`
- `redis-main`
- `expo-secure-store` `80-mobile.md`
- `localStorage` `sessionStorage`
- `chrome.storage.session` `TRUSTED_CONTEXTS` `chrome.runtime.sendMessage` `chrome.identity.launchWebAuthFlow` `code_verifier` `crypto.subtle` `storage.session`
- `x-middleware-subrequest` `middleware.ts` `proxy.ts` `middleware.ts`
- `CORSMiddleware` `allow_origins`
- `X-Frame-Options: DENY` `frame-ancestors`
- `APIKeyHeader` `require_api_key` `SERVICE_API_KEY` `PROXY_API_KEY` `python-api` `internal_auth.py` `metrics.py` `/metrics` `SERVICE_INTERNAL_SECRET_KEY`
- `os.getenv("KEY", "default")` `config/production.yml` `settings.production`
- `expo-secure-store`
- `^/api/` `/api/*` `/api/v1` `/api/*` `shape.bearer_bypass_prefix: "^/api/v1"` `fabrik apply` `^/` `orchestrator/verifier.check_api_bypass` `/api/*` `^/api/`
- `postgres-main` `fabrik-lib/rag` `postgres:16-alpine` `plpgsql` `postgres-main`
- `postgres-main` `docs/DECISIONS.md`
```

| Class | Status |
|---|---|
| Hunt: `commands/_fragments/term-edit.md` — every changed hunk, its enclosing function, its callers | CLEAN (B-S3 read the T4.14 paragraph whole in the pin and the 17 of 17 rendered term-edit consumers carry the sentence; no contradiction with term-coverage.md's md5/edits recipe) |
| Hunt: `docs/workflows/FINAL_GATE_WORKFLOW.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (F51 the ticket_breadth `never blocks` scoped; F52 the doc-index and hooks-index rows carry T4.6/T4.11; :169 restated for F9; B-S3 read every row naming a changed script) |
| Hunt: `scripts/command_run.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F1 the stem-bound ticket form without the time bound + the phase-form prefix escape, F2 the stem capture, F3 the negative `--delta`, F5 the placeholder regex; F6/F7 RECORDED; B-O1 read `_plan_stem`, `_phase_review_exists`, `convergence_warning`, `_round_report`, the `round`/`step`/`start` handlers, `_parse_usage_feedback` and probed all of them on a copy) |
| Hunt: `scripts/enforcement/check_convergence.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F9 untracked targets bound to the session's running record, F13/F14 the ledger-line grammar, F17 HEAD-blob advisory through `_head_texts`, F19 the advisory crash guard, F22 NOTE deferral; F18/F23 RECORDED measured, F20/F21 REFUTED; B-O2 read `_executed_targets`, `_converged_targets`, `_changed_md`, `_LEDGER_LINE`, `_committed_claims_advisory`, `_check_executed_plan`, `main`) |
| Hunt: `scripts/enforcement/check_doc_index.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F24 `--untracked-only` exits 0 as the warn_only row's contract, F30 `mode` on `_fail`; F45 RECORDED by design; B-O3 read `main` end to end and probed a scratch repo) |
| Hunt: `scripts/enforcement/check_hooks_index.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F29 `-z` + working-tree existence; B-O3 read `_tracked_hooks`, `main`'s hub marker and the `|=` widening) |
| Hunt: `scripts/enforcement/check_plan_tickets.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F25 the two bare NOTE prints, F26 the Touches-prose shapes, F27 the `./` gate path, F31 the `_norm` prefix; F32 RECORDED measured; B-O3 read `_note`, `_GATE_FILE_RE`, the READ-budget exemption, the ghost-gate and prose blocks, the discovery/allow-external NOTE sites) |
| Hunt: `scripts/enforcement/check_review_coverage.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F10/F11/F12 `_hunt_gaps` path-shaped, wrapped, fence-stripped; F15/F16 `_running_review_receipts` archive skip, receipt-writing command set, plan-stem filter; B-O2 read `_changed_md`, `_hunt_gaps`, `_running_review_receipts`, `_grade`, `main`'s changed-set join) |
| Hunt: `scripts/enforcement/check_review_hygiene.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F34 blanked read for `--symbol`, F35 `_until_heading` as a fence-aware heading match with a NOTE, F36 `--label` refusal, F37 docstring order, F38 module docstring, F39 `_dedupe` docstring, F40 `(×N)` on the line, F41 `_LABEL.clear()`, F42 `_line_occurrences`; B-O4 read `Hit`, `_display`, `_term_sites`, `_surface_hits`, `_dedupe`, `_until_heading`, `scan`, `main`) |
| Hunt: `scripts/enforcement/check_ticket_breadth.py` — every changed hunk, its enclosing function, its callers | CLEAN (re-adjudicated at the close: NO round's commit touches this file — F28's fix landed in its three READERS, each of which carries its own row; the rc-2 arm itself executed: `--plan-dir /tmp` → rc 2, the gate's no-argument path unchanged; B-O3 read `main`'s refusal and every caller — final_gate.py's registration, liveness_audit.py's `row_warn_only`, fabrik-plan-review.md's breadth bullet) |
| Hunt: `scripts/final_gate.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F28 the registration comment; the T4.6 row itself CLEAN — B-O4 read `run_optional_check` :347-395 and confirmed `*args` forwards `--untracked-only`, `warn_only` implies advisory, the row name does not collide with tier 2's; F24's exit contract fixed on the check's side) |
| Hunt: `scripts/review_rubric.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F33 hub-gated tooling floor, F44 doc paths do not vote; F43 FEATURES.md; F46 REFUTED; B-O4 read `TOOLING_PREFIXES`, `_floor_for`, `build_rubric`'s header and every consumer of the FLOOR header — tests/test_check_convergence.py:39 prefix, check_rule_grounding's MATCHED_LINE_RE, docs/FEATURES.md:91) |
| Hunt: `tests/enforcement/test_check_doc_index.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F49 the synthetic-repo rewrite of the `--untracked-only` test; B-S2 read the whole new test and its REPO hardcode) |
| Hunt: `tests/enforcement/test_check_hooks_index.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (the deleted-in-worktree case B-O3 named; B-S2 killed the `_tracked_hooks` mutant) |
| Hunt: `tests/enforcement/test_check_plan_tickets.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F25's NOTE test made real — the set outside the plans layout; the F26/F27 negative test; B-S2 read the T01/T02 fixture edits and found no older assertion weakened, killed the READ-budget mutant) |
| Hunt: `tests/enforcement/test_check_review_hygiene.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F48 the repeated-selector dedupe test, the stop-heading/symbol/fence test, the label refusal; B-S2 proved the old dedupe test never exercised `_dedupe`) |
| Hunt: `tests/enforcement/test_flip_gate_matrix.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F50 the module docstring, the row text and the untracked control under F9's running-record rule; B-S2 read the row-text-vs-assertion parity; B-S3 confirmed the assertion change is behavioural) |
| Hunt: `tests/enforcement/test_plan_tickets_epic_scope.py` — every changed hunk, its enclosing function, its callers | CLEAN (B-S2 read `_build`'s new gate files against every inventory/`git status` assertion in the file — none perturbed; the pre-existing `_find_fences` parity red is RECORDED in § Residual) |
| Hunt: `tests/enforcement/test_review_exit_contract.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F4 the DELTA_BUDGET mirror test; B-S2 confirmed the T4.14 needle is a text-presence check on the fragment — the render parity is `test_assemble_dispatch_step.py`'s) |
| Hunt: `tests/test_check_convergence.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F9's sibling-negative rewrite, F13's negation/glyph/blockquote test, F17/F22's advisory assertions; B-S1 killed all 11 mutants over the T4.1/T4.2/T4.3/T4.8/T4.10 tests) |
| Hunt: `tests/test_check_review_coverage_precommit.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F10–F12's Changed-list test, F15/F16's running-record test; B-S1 killed the T4.4/T4.5 mutants) |
| Hunt: `tests/test_command_run_feedback_required.py` — every changed hunk, its enclosing function, its callers | CLEAN (B-S1 killed the T3.4 mutant; carries the placeholder test B-O1 looked for in the other file) |
| Hunt: `tests/test_command_run.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F8 the foreign receipt `utime`d after start, F1/F2/F3/F5's four new tests; B-S1 killed the T3.1–T3.3 mutants) |
| Hunt: `tests/test_final_gate_advisory_display.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F47 the tier-1 containment; battery M21 kills the hoist) |
| Hunt: `tests/test_review_rubric.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (F33's project/tooling/mixed assertions; B-S2 killed the SERVICE revert) |
| Recurrence: fail-open/fail-closed — a swallowed error or an absent check that reads as success | FIXED r1 (F11, F13, F24, F27, F34 — every guard on the surface executed: `_hunt_gaps` wrapped list, `_LEDGER_LINE` prose, the warn_only row's exit, the `./` gate path, the `--symbol` count; F19's advisory guard added) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted, a limit at its edges | CLEAN (7 native seats stamped before dispatch and closed on `round --seats 7`; no pool; the only limit on the surface — `DELTA_BUDGET` 20 — probed at 20 and 21 by B-O1 and bound by F4) |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | FIXED r1 (F2 the stem's trailing punctuation, F5 the placeholder brackets, F10 path-shaped tokens, F31 `lstrip` vs `removeprefix`, F35 the heading prefix match) |
| Recurrence: behavior-without-a-test — a contract row no test kills (mutation asserted) | FIXED r1 (F3's CLI delta, F4's mirror, F8, F47, F48, F49; the round-zero battery on a copy asserts every FIXED row's grader red by name — `execB/battery.out`) |
| Recurrence: denominator on every count — bounded searches state their bound | CLEAN (every count in this receipt carries its population: 55 rows, 17 of 17 consumers, 8 of 296 receipts, 67 of 132 phase receipts, 317 plan files, 0 of 5 plan sets, 12 of 12 rules-match tests) |
| Recurrence: proxy-as-evidence — the real check EXECUTED, not read | CLEAN (every CONFIRMED row names the probe or grader that executed it; F20/F21/F46 were REFUTED by execution — a regex evaluated, a suite run — never by reading) |

Verdict grammar (the gate refuses anything else): `CLEAN (<the paths/lines hunted>)` — a CLEAN row
must name a path and run past 70 characters · `FIXED r<n> (<what changed>)` · `REFUTED (<the
disproving line>)` · `RECORDED — unexecuted (<why>)` · `RECORDED — by design`, parenthesising the
owning row's first-cell id and the EARLIER round that adjudicated it, or a `D-nnn` with no round
(the § Residual block below shows the shape) · `RECORDED — measured (<why>)` ·
`RECORDED — hygiene false positive (<why>)` — every RECORDED reason is PARENTHESISED, never
colon-delimited (a colon would spell the `unexecuted: N` counter the ledger refuses). `UNCHECKED`
may survive only under a `## BLOCKED` escalation (a finding + 3 failed attempts).

### Re-adjudication basis — which rounds touched which file (re-derived from the commit chain, not from this receipt's prose)

Every Phase B commit's own `--name-only`, mapped to the round in its subject (25 commits, 37 files;
`execB/phaseB_file_rounds.txt`). This is the denominator the closing re-adjudication reads: a Hunt
row's FINAL verdict is FIXED where some round's commit touched that file, CLEAN where none did.

The convergence signal is in the tail of that map. The ORIGINAL Phase B surface has been quiet for
five rounds: `command_run.py` and `check_convergence.py` were last touched in round 13,
`check_plan_tickets.py` in round 7, `review_rubric.py` in round 6, `check_doc_index.py` /
`check_hooks_index.py` / `final_gate.py` / the flip-gate and exit-contract tests in round 3 or
earlier. Rounds 14–18 touched exactly TWO source files — `check_review_hygiene.py` and
`check_review_coverage.py` — plus their two test files, and within those, one comment-reading model
and one docstring. Rounds 15, 16 and 17 each confirmed defects in the PREVIOUS round's own prose
rather than in the Phase B code, which is what triggered the D10 rule-3 class rewrite recorded in
the Pass 17 row.


## Disposition ledger — round 1

Every candidate any seat raised, one row, one terminal state (55 rows: 43 FIXED · 3 REFUTED · 9 RECORDED). Seats: B-O1 command_run.py · B-O2 check_convergence.py + check_review_coverage.py · B-O3 check_plan_tickets.py + check_ticket_breadth.py + check_hooks_index.py + check_doc_index.py · B-O4 check_review_hygiene.py + final_gate.py + review_rubric.py · B-S1 the command_run/convergence/coverage tests (11 of 11 mutants killed, no candidate) · B-S2 the enforcement/rubric/gate tests · B-S3 term-edit.md + FINAL_GATE_WORKFLOW.md + plan↔code parity (T4.14's sentence in 17 of 17 rendered consumers; T3.5, T4.7, T4.8's denominators re-derived). Every FIXED row's grader was asserted red by NAME on a copy of the fixed tree with the fix reverted (round-zero battery, `execB/battery.out`).

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| F1 | B-O1 | scripts/command_run.py `_phase_review_exists` `since` | removed-guard / multi-session | FIXED r1 · LOCAL (a stem-bound ticket receipt is evidence without the time bound — a plan resumed in a later session mints a fresh `started_epoch`; the phase form keeps the bound with the plan's `<date>-plan-<n>` prefix as an additive escape; the refusal text names the stem source and the window; grader: `test_the_phase_gate_binds_a_ticket_artifact_to_the_plan_stem_and_the_record_start`) |
| F2 | B-O1 | `_plan_stem` char class | boundary/sentinel | FIXED r1 · LOCAL (`)`, `,`, `:`, `;` excluded from the stem and the lookahead; grader `test_the_plan_stem_survives_trailing_punctuation_in_the_surface` — executed: `'foo.md)'`, `'foo.md:141'`, `'foo.md,'` before the fix) |
| F3 | B-O1 | `round --delta` unvalidated | fail-open | FIXED r1 · LOCAL (a negative `--delta` is refused, rc 2; the suppression keyed on the dispatch's self-reported delta is the plan's own T3.3 design — that half is `RECORDED — by design (D-229, the fragments' delta budget)`; grader `test_round_refuses_a_negative_delta_and_the_cli_delta_reaches_the_advisory`, which also drives `--delta` through the CLI — the two survivors the seat named) |
| F4 | B-O1 | `DELTA_BUDGET` mirror unguarded | cross-file contract | FIXED r1 · LOCAL (`test_command_run_mirrors_the_delta_budget_of_dispatch_headroom` binds the two constants; dispatch_headroom is hub-only by design — the constant in the ~46 synced copies is the fragments' number, not a mirror of a file they carry) |
| F5 | B-O1 | placeholder regex refuses `filed: <01M1RHJY>` | boundary | FIXED r1 · LOCAL (only bracket content carrying whitespace or a pipe is the grammar's own text; grader `test_a_real_id_inside_angle_brackets_is_not_a_placeholder`) |
| F6 | B-O1 | `new:` carries classes-opened or a candidate count | precision | RECORDED — by design (the plan's own T3.2 row: `new:` derived from the classes ledger, or the `--new` counter when given) |
| F7 | B-O1 | the PHASE form is not stem-bound; a surface-less record keeps the legacy ticket shape | fail-open residual | RECORDED — measured (a `<date>-plan-<n>` prefix rule would refuse 67 of 132 phase receipts on this box — `ls /opt/*/docs/development/reviews/*phase*-review.md` — so the prefix is an additive escape, never a requirement; a record with no parseable plan surface keeps the time bound, and the refusal text now says where the stem comes from; backlog row at Finish) |
| F8 | B-O1 | `test_the_phase_gate_binds…` wrote the foreign receipt BEFORE `start` | test-quality | FIXED r1 · LOCAL (the foreign receipt is `utime`d after the start so only the stem can refuse it; the T3.4 placeholder test the seat reported absent lives in tests/test_command_run_feedback_required.py — `command grep -c placeholder` there = 1, so that half was disproved) |
| F9 | B-O2 | `??` skip removed — a sibling's untracked draft reds every session's gate | removed-guard / shared tree | FIXED r1 · ARCHITECTURAL → no D-row (one correct form: an untracked plan is a target only when THIS session's running record names it — `_running_plan_stems`, the T4.5 shape; `_changed_md`'s standing rule holds; the flip-gate matrix row, its module docstring, its assertion and the FINAL_GATE_WORKFLOW bullet carry the same sentence; graders: `test_an_untracked_converged_plan_is_graded_before_it_is_staged` with the sibling negative, the matrix row) |
| F10 | B-O2 | `_hunt_gaps` demands a Hunt row for every backticked token | false positive / blocking | FIXED r1 · LOCAL (`_PATH_SHAPED`: a `/` or a file suffix; grader `test_the_changed_list_reads_paths_only_across_wrapped_lines_and_never_from_a_fence`) |
| F11 | B-O2 | `_CHANGED_LINE` reads one line | fail-open | FIXED r1 · LOCAL (the list runs to the next blank line or bold label; same grader) |
| F12 | B-O2 | `hunted` read from RAW text | raw-vs-stripped asymmetry | FIXED r1 · LOCAL (both sides read on the fence-stripped text; same grader) |
| F13 | B-O2 | `_LEDGER_LINE` accepts prose that starts with the label | fail-open | FIXED r1 · LOCAL (the label is followed by its number and a separator — `|`, `—`, `-`, `:`, `·`, `(` or end of line; a status glyph or a blockquote before the label is tolerated; grader `test_a_prose_line_starting_with_the_label_never_satisfies_the_executed_citation`; the bare `Pass N —` shape stays: 8 of 296 hub receipts write it) |
| F14 | B-O2 | `_LEDGER_LINE` refuses a numeric first column, a blockquote, a glyph | false negative | FIXED r1 for the glyph and blockquote shapes (same grader); the numeric first column `| 3 | closing |` is not a ledger row by the corpus's own mandate (a `Pass k` label on every ledger row — /fabrik-review § Reporting; `check_review_coverage._pass_counters` reads the same label), so that half is no defect |
| F15 | B-O2 | `_running_review_receipts` re-admits `-archive.md` and a sibling's receipt | removed-guard | FIXED r1 · LOCAL (archives skipped; when the record's surface names a plan only receipts naming that plan join; grader `test_a_running_record_pulls_only_its_own_receipts`) |
| F16 | B-O2 | `"review" in command` matches the artifact reviews | over-broad match | FIXED r1 · LOCAL (`_RECEIPT_WRITING_COMMANDS`: the six commands that write a docs/development/reviews/ receipt; same grader — `fabrik-spec-review` pulls nothing) |
| F17 | B-O2 | `_committed_claims_advisory` reads the working tree | correctness | FIXED r1 · LOCAL (HEAD blobs through one `git cat-file --batch` — `_head_texts` — and `_check_executed_plan` takes that text; grader: the advisory test's local-revert assertion) |
| F18 | B-O2 | the advisory walks every plan on every gate run | performance | RECORDED — measured (0.61 s over 317 plan files on the hub, 8 rows, before batching; one git process after) |
| F19 | B-O2 | `_check_executed_plan` can raise out of the advisory | robustness | FIXED r1 · LOCAL (a broad `except` turns the traceback into an advisory line) |
| F20 | B-O2 | `_GATE_FILE_RE` extension list diverges from `PROOF` | cross-file contract | REFUTED (different semantics: `_GATE_FILE_RE` names files a Gate line RUNS — py/ts/tsx/js/mjs/sh — while `PROOF` names files a citation POINTS AT; a `.vue` is cited, never run) |
| F21 | B-O2 | the re-derivation message's shape vs `_REDERIVATION_ROW` | style | REFUTED (`_REDERIVATION_ROW` = `\b(?:pass|round)\b[^\n]*\bmethod\W{0,4}\s*:?\s*\*{0,2}\s*re-?deriv`, satisfied by the quoted `| Pass N | … | method: re-derivation — … |`; "the label LEADING the method cell" is the `method:` label) |
| F22 | B-O2 | a NOTE from `_changed_md` prints ahead of the ⚠ advisory header | fail-silent (final_gate's ⚠-first opt-in) | FIXED r1 · LOCAL (NOTE lines are deferred to `_NOTES` and printed after the advisory block; grader: the advisory test's untracked-draft assertion — `out.startswith("⚠")`) |
| F23 | B-O2 | no test exercises the advisory's cost | behaviour-without-a-test | RECORDED — measured (F18's number; a timing assertion in a unit test is a flake, not a guard) |
| F24 | B-O3 | `final_gate` lean row is `warn_only` but the check exits 1 on findings | fail-closed by accident | FIXED r1 · LOCAL (`--untracked-only` always exits 0 — the row's contract; `mode` also rides the `_fail` payloads; grader `test_untracked_only_reports_the_creating_runs_docs_and_nothing_else`, now on a SYNTHETIC repo asserting rc 0) |
| F25 | B-O3 | two of five NOTE prints still bare under `--json` | incomplete fix | FIXED r1 · LOCAL (the two multi-line `print(` calls → `_note(`; grader `test_notes_go_to_stderr_under_json` now runs the set OUTSIDE the plans layout so `--allow-external` fires — the vacuous version the seat proved) |
| F26 | B-O3 | Touches-prose ERROR fires on a comment continuation, a wrapped bullet, a numbered item, a blockquote | false positive (fleet) | FIXED r1 · LOCAL (`_TOUCHES_LINE_OK` + an open `<!-- … -->` span + an indented continuation; grader `test_touches_shapes_that_are_not_prose_stay_quiet_and_a_dot_slash_ghost_gate_fires`) |
| F27 | B-O3 | `./`-prefixed gate path never matches `_GATE_FILE_RE` | fail-open | FIXED r1 · LOCAL (`(?:\./)?` before the capture; same grader) |
| F28 | B-O3 | rc 2 contradicts three readers | cross-file contract | FIXED r1 · LOCAL (commands/_sources/fabrik-plan-review.md `It always exits 0` → the two exits named; final_gate.py's registration comment; liveness_audit.py's `row_warn_only` rationale; rendered corpus re-rendered) |
| F29 | B-O3 | `_tracked_hooks` reads the index, splits on whitespace | stale-index false positive | FIXED r1 · LOCAL (`-z` + `(root / p).is_file()`; grader: the hooks test's deleted-in-worktree assertion; the nested-dir shape stays basename by the index's own convention) |
| F30 | B-O3 | `mode` absent on `_fail` payloads | return-shape | FIXED r1 · LOCAL (with F24) |
| F31 | B-O3 | `lstrip("./")` beside `removeprefix("./")` | normalisation asymmetry | FIXED r1 · LOCAL (`_norm` uses `removeprefix`; `.claude/` exemption not added — a scope question the ticket did not raise, backlog) |
| F32 | B-O3 | a table-shaped Touches would make every gate file a ghost | false positive shape | RECORDED — measured (0 of 5 dated hub plan sets carry a table under `## Touches`; the bullet collector never read tables, so the shape is pre-existing debt — backlog row at Finish) |
| F33 | B-O4 | `TOOLING_PREFIXES` drops the security floor for a project's `tests/`-only diff | removed-guard (fleet) | FIXED r1 · ARCHITECTURAL → no D-row (one correct form: the tooling floor applies only in the HUB — the `templates/governance/CLAUDE.md` marker — and doc paths do not vote; docs/FEATURES.md:91 says so; grader `test_the_floor_is_surface_aware_for_hub_tooling` with the project assertions the seat named) |
| F34 | B-O4 | `--symbol` counts the un-blanked text under `--stop-at-heading` | fail-open | FIXED r1 · LOCAL (`read` stores the blanked text; grader `test_stop_at_heading_matches_a_real_heading_and_blanks_the_symbol_count_too`) |
| F35 | B-O4 | `_until_heading` matches any line starting with the text, inside fences, `#` swallows all | silent truncation | FIXED r1 · LOCAL (a `#`-led heading, exact text, fence-aware, a NOTE names the blanked line count or `not found`; same grader) |
| F36 | B-O4 | `--label` silently discarded with ≠ 1 surface | fail-silent | FIXED r1 · LOCAL (`ap.error`, rc 2; grader `test_a_label_with_two_surfaces_is_refused_and_a_repeated_selector_dedupes`) |
| F37 | B-O4 | `_display`'s docstring demoted to dead code | style | FIXED r1 · LOCAL (the early return sits below the docstring; the grader asserts `_display.__doc__`) |
| F38 | B-O4 | module docstring names `SURFACE_SUFFIXES` as the class gate | doc↔code contradiction | FIXED r1 · LOCAL (names the `path.endswith(".md")` gates; `test_the_docstring_names_the_md_only_classes` asserts the real gate) |
| F39 | B-O4 | `_dedupe` docstring attributes 15-vs-13 to a repeated phrase on one line | false attribution | FIXED r1 · LOCAL (the producer is the same selector given twice or a phrase+claim collision; the grader gives the selector twice and reads `occurrences: 4`) |
| F40 | B-O4 | `occurrences` only in JSON | behaviour-without-a-surface | FIXED r1 · LOCAL (`line_out` appends `(×N)` when N > 1; asserted) |
| F41 | B-O4 | `_LABEL` global never cleared | idempotency | FIXED r1 · LOCAL (`_LABEL.clear()` at the top of `main`) |
| F42 | B-O4 | occurrences counted case-sensitively while the site matched case-insensitively | precision | FIXED r1 · LOCAL (`_line_occurrences` counts with the same pattern; the grader's `The Widget, the widget` reads 4 over two selectors) |
| F43 | B-O4 | FLOOR header consumers: docs/FEATURES.md:91 | doc mirror | FIXED r1 · LOCAL (the sentence names the hub's tooling floor) |
| F44 | B-O4 | `all()` over the changed set — two doc files flip a tooling partition to SERVICE | effectiveness | FIXED r1 · LOCAL (`.md`/`.txt`/`.rst` paths do not vote; the grader's tooling set carries CHANGELOG.md) |
| F45 | B-O4 | `"--untracked-only" in sys.argv` — a raw argv scan | style | RECORDED — by design (the file's own convention for `--json` at :219; an `=`-form flag is not one this script takes) |
| F46 | B-O4 | tests/test_rules_match.py:167 filters by `FLOOR_PACKS` | named-constant consumer | REFUTED (the suite passes — 12 of 12 — under the hub-gated floor, and the filter's invariant, FLOOR packs suppressed from MATCHED, holds for the packs the floor injected) |
| F47 | B-S2 | the tier-1 AST grader passes when the row is hoisted out of `if tier == 1:` | test-quality | FIXED r1 · LOCAL (the call must sit inside an `If` whose test is `tier == 1`; battery M21) |
| F48 | B-S2 | the dedupe test never exercised `_dedupe` | test-quality | FIXED r1 · LOCAL (with F39) |
| F49 | B-S2 | the `--untracked-only` test subprocesses the LIVE tree and passes vacuously on an empty drift | test-quality | FIXED r1 · LOCAL (with F24: a synthetic repo, the script copied in, rc and both drift shapes asserted) |
| F50 | B-S3 | test_flip_gate_matrix.py's module docstring still says `skips ??` | doc↔code contradiction | FIXED r1 · LOCAL (rewritten to the running-record rule with F9) |
| F51 | B-S3 | FINAL_GATE_WORKFLOW.md:467 `never blocks` vs rc 2 | doc mirror | FIXED r1 · LOCAL (scoped to the gate's no-argument form; with F28) |
| F52 | B-S3 | no FINAL_GATE_WORKFLOW rows for `--untracked-only`, the hooks-index derivation, the rubric floor | Doc Sync Matrix | FIXED r1 · LOCAL (the doc-index and hooks-index rows carry the T4.6/T4.11 sentences; the rubric is not a gate check — its floor sentence lives in docs/FEATURES.md:91) |
| F53 | B-S1 | `_round_report`'s `len(new)` fallback branch untested | coverage | RECORDED — measured (the pre-existing `--classes-new a,b` test reads that branch — `new: 2` — so the fallback is exercised; the seat's own table killed all 11 of its mutants) |
| F54 | B-S1 | the grammar round-trip test is coupled to `_USAGE_GRAMMAR`'s exact prose | fragility | RECORDED — measured (the coupling is the test's point — the grammar must parse to exactly its own labels; a reworded grammar that still parses passes) |
| F55 | hygiene | template-residue at docs/workflows/FINAL_GATE_WORKFLOW.md:164 and :267 | hygiene | RECORDED — hygiene false positive (the doc describes the residue class by its own two shapes) |

## Disposition ledger — round 2 (delta over bb995e60)

38 rows: 28 FIXED · 1 REFUTED · 9 RECORDED. Six fresh seats (B2-O1 command_run.py · B2-O2 check_convergence.py + check_review_coverage.py · B2-O3 check_plan_tickets.py + check_hooks_index.py + check_doc_index.py + the ticket_breadth readers · B2-O4 check_review_hygiene.py + review_rubric.py · B2-S1 the round-1 tests · B2-S2 the doc and comment mirrors) over bb995e60's 24-file fix diff plus one hop. Round 1's REFUTED and RECORDED rows were carried verbatim and none was re-raised.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| G1 | B2-O2 | `_LEDGER_LINE` refuses `| **Pass 23** |` | removed-guard regression | FIXED r2 · LOCAL (emphasis `[*_`]*` closes before the separator; a comma separates too; 2 of 8 bold shapes in the hub's receipts were refused — grader: the four emphasis/comma shapes in `test_a_prose_line_starting_with_the_label_never_satisfies_the_executed_citation`) |
| G2 | B2-O2 | `_running_plan_stems` reads only `CLAUDE_SESSION_ID`; the gate's shell carries `CLAUDE_CODE_SESSION_ID` | fail-open (the rule was dead where it runs) | FIXED r2 · LOCAL (command_run's own `_session_id` chain; grader: the `CLAUDE_CODE_SESSION_ID` assertion in the untracked-plan test) |
| G3 | B2-O2 | a nested review record parks the plan-naming parent in `stack` | ordering/nesting | FIXED r2 · LOCAL (surfaces read from the record AND its stack, in both graders; grader: the nested-record assertions in the untracked-plan and running-record tests) |
| G4 | B2-O2 | `_CHANGED_LINE` runs past the list into prose | boundary | FIXED r2 · LOCAL (the list continues only on lines starting with a backtick; grader: the prose-after-list assertion) |
| G5 | B2-O2 | `_PATH_SHAPED` drops `Makefile`/`Dockerfile` | fail-open | FIXED r2 · LOCAL (an extension-less allowlist; grader: the `Makefile` assertion) |
| G6 | B2-O2 | `_RECEIPT_WRITING_COMMANDS` omits `fabrik-execute-plan` | removed-guard | FIXED r2 · LOCAL (added — its Finish writes the whole-plan receipt; grader: `_rc("fabrik-execute-plan") == 1`) |
| G7 | B2-O2 | `_head_texts` `{}` on a git failure reads as no debt | fail-silent | FIXED r2 · LOCAL (a deferred NOTE names the unexamined count) |
| G8 | B2-O2 | `check_review_coverage._running_review_receipts` has the same session-id read | one hop out (Phase B batch 2 code, not bb995e60's hunk) | RECORDED — measured (fixed in-run with G2's chain and the stack union, graded by the running-record test's `CLAUDE_CODE_SESSION_ID` assertion; not counted under the delta bar — it is this phase's own code, so it did not wait) |
| G9 | B2-O2 | `_untracked_is_target`'s rename branch is dead for `??` lines | style | RECORDED — measured (git never emits a rename on an untracked line; harmless) |
| G10 | B2-O4 | the doc-path vote exclusion turns a docs-only tooling partition into SERVICE | regression | FIXED r2 · LOCAL (doc paths vote when they are the whole partition; grader: the command-source-only and pack-only assertions) |
| G11 | B2-O4 | `--stop-at-heading` is a no-op on the `--receipt` path | fail-open | FIXED r2 · LOCAL (`_note_stop` + blanking in both loops; grader: the receipt-path dead-symbol assertion) |
| G12 | B2-O4 | `ap.error` exits 2 on a script whose contract is exit 0 | contract break (fleet) | FIXED r2 · LOCAL (`_refuse` — exit 0, the refusal on stdout, in the envelope's notes under --json; the round-1 grader re-pointed at rc 0) |
| G13 | B2-O4 + B2-S2 | docs/FEATURES.md:91 enumerates five categories against six prefixes | doc↔code | FIXED r2 · LOCAL (names `TOOLING_PREFIXES` and the whole-partition rule) |
| G14 | B2-O4 | the fence toggle flips on a `~~~` inside a ``` block | silent truncation | FIXED r2 · LOCAL (the opening run's character closes the fence; grader: the tilde fixture) |
| G15 | B2-O4 | a wrapped occurrence contributes the floor of 1 | precision (docstring) | FIXED r2 · LOCAL (the docstring states the floor and its one wrong case) |
| G16 | B2-O4 | `TOOLING_PREFIXES` `tests/` covers the hub's CLI tests too | granularity | RECORDED — measured (pre-dates the hunk; backlog row at Finish: `tests/enforcement/` + `tests/sysadmin/` rather than bare `tests/`) |
| G17 | B2-O4 | a `.yaml` gate-config diff votes SERVICE | pre-existing | RECORDED — measured (`.pre-commit-config.yaml` sits under no tooling prefix; unchanged by the hunk) |
| G18 | B2-O4 | `_LABEL.clear()` and `re.I` in `_line_occurrences` had no in-process grader | test-quality | FIXED r2 · LOCAL (two in-process `main` calls assert the label does not leak; `_line_occurrences(["Foo foo"], 1, "foo") == 2`; `_floor_for(…, None)`) |
| G19 | B2-S1 | `<01M1AAA, 01M1BBB>` refused as a placeholder | false positive | FIXED r2 · LOCAL (`_is_placeholder`: a `|`, or several tokens with prose words; grader: the multi-id and prose cases) |
| G20 | B2-S1 | the running-record fixture repo could run a global git hook | test robustness | FIXED r2 · LOCAL (`core.hooksPath=/dev/null` on the fixture repo) |
| G21 | B2-S1 | `battery.out` showed the stale 3-survivor summary | machinery | RECORDED — measured (the reduced re-run's output was not captured into the file; the full battery is re-run after round 2 and its output replaces the file) |
| G22 | B2-O1 | the phase-form prefix escape is not delimited (plan-2 vs plan-20) | boundary/prefix | FIXED r2 · LOCAL (`startswith(prefix + "-")`; grader: the plan-20 stale receipt assertion; the case-insensitive assertion kills the `.lower()` mutants) |
| G23 | B2-O1 | a stem-bound ticket receipt satisfies every phase of a re-executed plan forever | stale evidence | RECORDED — by design (D-243; DISPATCHER mode's contract never binds a ticket receipt to a phase — /fabrik-execute-plan § D4; the cost is now STATED in the function's comment and in docs/reference/command-run-protocol.md) |
| G24 | B2-O1 | the refusal states a bound not in force and prints the literal `<plan>` | message accuracy | FIXED r2 · LOCAL (the time clause only with a finite start, zone named; the stem it read, or the sentence that the surface names none; grader: `test_the_phase_gate_refusal_names_the_stem_and_states_the_bound_only_when_in_force`) |
| G25 | B2-O1 | docs/reference/command-run-protocol.md § step and fabrik-execute-plan.md's artifact sentence describe only the loose phase form | Doc Sync (one hop out) | RECORDED — measured (fixed in-run: both name the two shapes, the stem bind, the time bound and the prefix escape; rendered; not counted under the delta bar — Phase B's T3.1 made them stale, not bb995e60) |
| G26 | B2-O3 | `+` bullets quiet but unparsed by `_list_paths` | silent invisibility | FIXED r2 · LOCAL (`_list_paths` parses `+`; grader: the `+` fixture asserts no ghost-gate error, i.e. the path was collected) |
| G27 | B2-O3 | `> path` quiet but invisible | silent invisibility | FIXED r2 · LOCAL (a blockquote is quiet only when its remainder is not path-shaped; grader: the quoted-path assertion) |
| G28 | B2-O3 | any indented line exempt | fail-open | FIXED r2 · LOCAL (only a continuation directly under a bullet; grader: the indented-paragraph assertion; 0 of 60 hub tickets carry the shape — measured) |
| G29 | B2-O3 | prose after `-->` on the closing line swallowed | fail-open | FIXED r2 · LOCAL (the tail after the close is re-tested; grader: `real prose after the close`) |
| G30 | B2-O3 | a comment opened mid-bullet reports its continuation | false positive | FIXED r2 · LOCAL (a span opened anywhere on a line is tracked; same grader) |
| G31 | B2-O3 | an orphan `-->` line reads as prose | false positive | FIXED r2 · LOCAL (`-->` joins the quiet shapes; grader: the orphan fixture) |
| G32 | B2-O3 | the discovery-mode `_note` has no grader | behaviour-without-a-test | FIXED r2 · LOCAL (`test_a_discovery_note_rides_stderr_under_json`) |
| G33 | B2-O3 | `-z` half of `_tracked_hooks` has no grader | behaviour-without-a-test | FIXED r2 · LOCAL (a tracked `my hook.py` is required whole) |
| G34 | B2-O3 | `is_file()` silently drops a path that cannot be examined | fail-open | FIXED r2 · LOCAL (tri-state: only a provably absent path drops) |
| G35 | B2-O3 | the lean row cannot tell clean from unexamined | observability | FIXED r2 · LOCAL (`examined` rides the payload, 0 on the `_fail` arms; grader: `examined == 1`) |
| G36 | B2-O3 | `fp = root / p` uses the raw token; `.//x` prefix; `././tests/x.py` | one hop out | RECORDED — measured (0 of 60 hub tickets; benign under pathlib; cited for the record) |
| G37 | B2-S2 | the `round 25` citation in check_convergence's comment | provenance | REFUTED (check_review_coverage.py's `_changed_md` docstring, in the pin, states it: "round 25 reproduced a NOTE printing before the ⚠-first advisory header") |
| G38 | orchestrator (executing G32's grader) | check_plan_tickets discovery mode prints `no plan directories in scope` on stdout under `--json` | one hop out (T4.8's class, not bb995e60's hunk) | RECORDED — measured (fixed in-run: `[]` under `--json`; graded by `test_a_discovery_note_rides_stderr_under_json`; not counted under the delta bar) |

## Disposition ledger — round 3 (delta over 7cb15def + 346e8a40)

30 rows by their leading verdict: 20 FIXED · 1 REFUTED · 9 RECORDED (of the RECORDED, four — H11, H18, H19, H20 — are one-hop rows fixed in-run and not counted, and H6/H17/H21/H24/H29 are measured or by-design). The run record's round 3 was closed with `--confirmed 25` before these rows were re-derived; the receipt's 20 is the corrected count (round 4, B4-S2). Six fresh seats (B3-O1 command_run.py and B3-S1 the round-2 tests were re-dispatched after API timeouts and returned; B3-O2 check_convergence.py + check_review_coverage.py · B3-O3 check_plan_tickets.py + check_hooks_index.py + check_doc_index.py · B3-O4 check_review_hygiene.py + review_rubric.py · B3-S2 the doc and comment mirrors) over 7cb15def's 24 files plus the 346e8a40 repair, each pointed at the two commits by sha (the range carried four sibling commits). Rounds 1–2's REFUTED and RECORDED rows were carried verbatim and none was re-raised.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| H1 | B3-O3 | `_present` no-op: `exists()` raised on EACCES first and dropped a dangling symlink | fail-open / crash | FIXED r3 · LOCAL (one `os.lstat` tri-state; graders: the dangling-symlink and mode-000-parent assertions) |
| H2 | B3-O3 | `_PATHISH` flags `and/or`, `24/7`, `Python 3.12`, `T4.8` at ERROR severity | false positive (fleet) | FIXED r3 · LOCAL (a real extension set; the message names the three quoted shapes; grader: the round-three quiet fixture) |
| H3 | B3-O3 | `_prev_bullet` reset by a blank line, a comment or a table row | removed-guard regression | FIXED r3 · LOCAL (a continuation survives those; same grader) |
| H4 | B3-O3 | `examined` counted untracked docs the loop skipped | denominator | FIXED r3 · LOCAL (counted inside the loop after the skips; grader: an excluded untracked doc in the fixture keeps `examined == 1`) |
| H5 | B3-O3 | the `examined` grader pinned the literal | test-quality | FIXED r3 · LOCAL (with H4) |
| H6 | B3-O3 | a second unterminated `<!--` on one line; the plain path prints no `examined` | one hop out | RECORDED — measured (0 of 123 hub tickets; backlog rows at Finish) |
| H7 | B3-O4 | `_until_heading`'s char-only fence tracker closes a 4-backtick block on a 3-backtick line | silent truncation (fail-open) | FIXED r3 · LOCAL (the file's own `_fence_step`; grader: the 4-backtick fixture) |
| H8 | B3-O4 | `or paths` fallback defeated by the mandated CHANGELOG entry beside a command source | regression | FIXED r3 · LOCAL (`_NON_VOTING_DOCS` and `docs/` never vote; the docstring and docs/FEATURES.md say so; graders: the three pairs) |
| H9 | B3-O4 | an all-prose tooling partition gets the Python floor | design | FIXED r3 · LOCAL (`FLOOR_PACKS_TOOLING_DOC` = `core/40-documentation.md`; label stays TOOLING; graders: the docs-only floor assertions) |
| H10 | B3-O4 | the plain `--label` refusal prints no summary line | observability | REFUTED (the graders `test_a_claim_term_lists_every_mirror_site…` and `test_a_claim_without_a_surface…` pin the plain refusal as the WHOLE stdout — the contract predates this review; a summary line was tried in-round and refused by them) |
| H11 | B3-O4 | `_dedupe` sums `occurrences` for the same selector given twice | one hop out (round-1 code) | RECORDED — measured (fixed in-run: `max`; the round-2 grader reads `(×2)`; not counted under the delta bar) |
| H12 | B3-O2 | the git-failure NOTE is unreachable through final_gate's ⚠-first opt-in | fail-open | FIXED r3 · LOCAL (an advisory ROW under the ⚠ header; grader `test_a_git_failure_in_the_advisory_is_a_warning_final_gate_can_see` asserts `startswith("⚠")` on a non-git root) |
| H13 | B3-O2 | `_head_texts` cannot tell a truncated batch from a complete one | fail-open | FIXED r3 · LOCAL (returns `(dict, complete)` from the exit code and the parse; the advisory row names `k of N`; same grader) |
| H14 | B3-O2 | `fabrik-execute-plan` in the set makes a seat's IN-PROGRESS draft red the orchestrator's gate through `_hunt_gaps` | fail-closed on a sibling's draft | FIXED r3 · LOCAL (the IN-PROGRESS carve-out gates every leg of `_grade`, not `check_file` alone; grader: the IN-PROGRESS receipt with a missing Hunt row passes) |
| H15 | B3-O2 | the comma separator admits prose that starts with the label | fail-open | FIXED r3 · LOCAL (comma dropped — the two bold rows needed `(`/`—`, none the comma; grader: the `Round 3, the sweep…` assertion) |
| H16 | B3-O2 | the command filter reads the top record while the stack union reads the parent | asymmetric nesting | FIXED r3 · LOCAL (the innermost receipt-writing frame owns the window and its start; grader: the nested data-contract assertion) |
| H17 | B3-O2 | stale stack frames widen the stem set | plausible | RECORDED — measured (`command_run.py` pops the parent on every close path — `stack.pop()` at the close — so a frame outlives its run only when the record itself stays `running`, which is the state the gate must honour) |
| H18 | B3-O2 | `_PASS_ROW` refuses the emphasis `_LEDGER_LINE` accepts | one hop out | RECORDED — measured (fixed in-run: `[*_`]*` on `_PASS_ROW`; asserted in the prose test; 0 of 1197 docs carried a row the two disagreed on) |
| H19 | B3-O2 | a bullet-list Changed block captures nothing | one hop out | RECORDED — measured (fixed in-run: a `- `-led backticked line continues the list; graded; 0 of 30 receipts regressed) |
| H20 | B3-O2 | the bare-name allowlist misses `Taskfile`, `Caddyfile`, `CODEOWNERS` | one hop out | RECORDED — measured (fixed in-run: the rule is inverted — a backticked token is a path unless it is an md5/sha, a count, a range or carries whitespace; graded) |
| H21 | B3-O2 | with no plan stem every fresh receipt joins the blocking set | design | RECORDED — by design (D-243; the round-1 fallback for a scoped review over commits — bounded now by H14's IN-PROGRESS carve-out) |
| H22 | B3-S2 | `67 of 132` and `2 of 8` did not re-derive | provenance | FIXED r3 · LOCAL (both comments and the protocol doc now name the POPULATION: the 132 files named `*phase*-review.md` under /opt/*/docs/development/reviews on 2026-09-13, and the two bold rows `**Pass 23**`/`**Pass 25**`) |
| H23 | B3-S2 | the protocol doc's `<id>` vs the code's `T\d{2}[a-z]?` | doc precision | FIXED r3 · LOCAL (`T##[a-z]?` stated) |
| H24 | B3-S2 | the pinned range diff carries four sibling commits | machinery | RECORDED — measured (every seat was pointed at the two commits by sha; the next round's pin diffs each commit against its own parent) |
| H25 | B3-O1 | `_is_placeholder` refuses the mandated `<none — surfaces exercised: mail.py>` and `<01M1AAA|01M1BBB>` | false refusal (contradicts its own docstring) | FIXED r3 · LOCAL (the grammar's ellipsis, a nested `<`, a PROSE alternation or the grammar's noun phrases are the placeholder; the honest shape at the head of the brackets is a value; graders: the three real shapes and four placeholder shapes) |
| H26 | B3-O1 | the `-` delimiter kills the escape for lettered plan numbers (`plan-2a`) | removed-behaviour regression | FIXED r3 · LOCAL (`plan-\d+[a-z]?` bounded by a non-alphanumeric — plan-2 is neither plan-20 nor plan-2a; 3 of 166 fleet stems carry a letter; graders: the plan-2a assertions) |
| H27 | B3-O1 | the refusal calls the stem-less ticket form unbound while the code time-binds it | message contradicts code | FIXED r3 · LOCAL (`bound only by this record's start` with the time clause; grader: the no-surface assertion) |
| H28 | B3-O1 | `_since_label`'s empty branch and `_is_placeholder`'s alternation had no grader | test-quality | FIXED r3 · LOCAL (`_since_label({}) == ""`, a string epoch, and the alternation shapes asserted directly) |
| H29 | B3-O1 | the 2 s grace and the prefix escape are not stated by the refusal; a numeric-string `started_epoch` fails open | one hop out | RECORDED — measured (`_finite_ts` predates the phase; the under-statement is in the safe direction; backlog row at Finish) |
| H30 | B3-S1 | the graders read a third piece of live box state (the session's own run record) with no conftest pin | test isolation (the conftest's own documented class) | FIXED r3 · LOCAL (an autouse pin: a scratch `COMMAND_RUN_DIR`, a constant fake `CLAUDE_SESSION_ID` — two older `done` tests key one record across a cwd change — and no `CLAUDE_CODE_SESSION_ID`; reproduced by the seat against the operator's live record; battery R3-20 kills its removal through `test_the_suite_never_reads_the_operators_live_run_record`, which asserts the pin's real invariant after round 4 re-pointed it) |

## Disposition ledger — round 4 (delta over 3d975da7)

31 rows by their leading verdict: 24 FIXED · 0 REFUTED · 7 RECORDED. Six fresh seats (B4-O1 command_run.py · B4-O2 check_convergence.py + check_review_coverage.py · B4-O3 check_plan_tickets.py + check_hooks_index.py + check_doc_index.py · B4-O4 check_review_hygiene.py + review_rubric.py · B4-S1 the round-3 tests and the conftest pin · B4-S2 the doc and comment mirrors, the commit message and this receipt) over 3d975da7's 19 files, pinned per commit. Rounds 1–3's REFUTED and RECORDED rows were carried verbatim and none was re-raised. The confirmed series reads 43 → 28 → 20 → 24.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| J1 | B4-O1 | the honest `filed:` shape refused when its surface list carries a grammar noun or a pipe | false refusal (fail-closed) | FIXED r4 · LOCAL (the honest head is decided before the noun and alternation rules; grader: `the mail id router` and `mail.py | command_run.py` shapes) |
| J2 | B4-O1 | the stem-less refusal claims a bound when the record has no finite start | message contradicts code | FIXED r4 · LOCAL (built from `_since_label`: `UNBOUND — this record carries no finite start`; grader: the string-epoch record in the refusal test) |
| J3 | B4-O1 | the timestamp clause printed twice | style | FIXED r4 · LOCAL (`the same bound`; grader counts the clause once) |
| J4 | B4-O1 | `[a-z]?` drops the escape for a two-letter plan number | fail-closed, latent (0 of 3 lettered stems) | FIXED r4 · LOCAL (`[a-z]*`; grader: `plan-2ab`) |
| J5 | B4-O1 | the honest-head regex under `re.I` admits any 6-letter word | fail-open, low | FIXED r4 · LOCAL (`(?i:none)` alone is case-blind; the id alternative is upper-case; grader: `<nothing filed, no surfaces exercised worth naming>` is a placeholder) |
| J6 | B4-O1 | the dead noun `surfaces exercised: …`; the refusal's trailing colon | one hop out | RECORDED — measured (the noun is deleted in-run — subsumed by the ellipsis rule; the colon predates the phase) |
| J7 | B4-O2 | the exclusion-only path rule counts every backticked symbol as a path | false positive (blocking, fleet) | FIXED r4 · LOCAL (a token is a path when path-shaped AND carrying `/` or `.`, or a well-known bare file; stated cost: `cafebabe`; grader: `_hunt_gaps`/`--json`/`IN-PROGRESS` in backticks pass) |
| J8 | B4-O2 | the inert backtick in `_PASS_ROW`; a code-span label defeats the closing-row rule | one hop out (masking design) | RECORDED — by design (D-243; the backtick is dropped in-run and the shape is out of grammar; the masking rule is the file's own — backlog row at Finish) |
| J9 | B4-O2 | numbered, bold and quoted bullets end the Changed list | fail-open, latent (0 of 1,303 docs) | FIXED r4 · LOCAL (`1.`/`)`/`>`/bold continuations; grader: the numbered+quoted fixture) |
| J10 | B4-O2 | the advisory row can say `N of N … the rest were NOT examined` | message accuracy | FIXED r4 · LOCAL (worded from the counts) |
| J11 | B4-O2 | the innermost writing frame without a start darkens the check | fail-open | FIXED r4 · LOCAL (the innermost writing frame WITH a usable start; grader: the two-frame stack, both directions) |
| J12 | B4-O2 | a comma inside the label cell `| Pass 2, closing |` no longer matches | tightening | RECORDED — measured (0 of 1,230 ledger lines under the pin's reviews carry it) |
| J13 | B4-O2 | `reversed()` and the `ValueError` limb of `_head_texts` had no grader | test-quality | FIXED r4 · LOCAL for `reversed()` (the two-frame stack with different starts); the `ValueError` limb's disposition is J13b's row |
| J13b | B4-O2 | the `ValueError` limb of `_head_texts` | behaviour-without-a-test | RECORDED — measured (reachable only from a malformed `git cat-file` header; the `complete=False` row is graded through the non-git path) |
| J14 | B4-O4 | root-level governance prose (`CLAUDE.md`, `README.md`, `AFCL.md`) votes in the all-prose fallback | enumeration gap | FIXED r4 · LOCAL (a root-level doc never votes — the rule replaces the list; `_NON_VOTING_DOCS` is gone; graders: the CLAUDE/README/INDEX/`changelog.md` pairs) |
| J15 | B4-O4 | `_until_heading` takes a heading inside an HTML comment as the stop | fail-open | FIXED r4 · LOCAL (`_blank_quoted` — the file's one quoting model; grader: the commented fixture) |
| J16 | B4-O4 | `_dedupe`'s docstring says summed while the body takes `max` | doc↔code | FIXED r4 · LOCAL (the docstring states the LINE's count and that phrase and claim walks never merge) |
| J17 | B4-O4 | docs/FEATURES.md:91 enumerates four of five non-voting docs | doc↔code | FIXED r4 · LOCAL (the sentence states the root-level rule) |
| J18 | B4-O4 | `changelog.md` (lower case) voted | consistency | FIXED r4 · LOCAL (the root-level rule is case-blind; grader: the `changelog.md` pair) |
| J19 | B4-O4 | the pair cases asserted only the label, not the doc floor | test-quality | FIXED r4 · LOCAL (every pair asserts `FLOOR_PACKS_TOOLING_DOC`) |
| J20 | B4-O3 | `_PATHISH`'s whitespace anchor missed every markdown-delimited path | removed-guard regression | FIXED r4 · LOCAL (a look-behind on `[\w.-]`; grader: backticked, bold, linked and quoted paths fire) |
| J21 | B4-O3 | dotfiles, directory paths and upper-cased extensions were not paths | fail-open | FIXED r4 · LOCAL (`.env(.x)`, two segments or a trailing slash, `re.I`; grader: `.env.example`, `scripts/enforcement/`, `docs/NOTES.MD`; `.envrc` stays quiet) |
| J22 | B4-O3 | `#`/`|` lines kept a bullet's continuation alive | false negative, contained | FIXED r4 · LOCAL (only `|`, comments and blank lines carry it; a `#` line cannot occur inside the section) |
| J23 | B4-O3 | the round-3 test's docstring claimed a table-row case it lacked | test-quality | FIXED r4 · LOCAL (a table row joined the fixture) |
| J24 | B4-S1 | the pin assertion pinned pytest's default basetemp name | brittle test | FIXED r4 · LOCAL (asserts the dir is under the session's basetemp; moved to tests/test_conftest_isolation.py beside the other pins) |
| J25 | B4-S1 | hex-named real paths (`cafebabe`) are not paths | stated cost | RECORDED — by design (D-243; stated in `_is_path_token`'s docstring — a bare hex name is an md5 to the receipt grammar) |
| J26 | B4-S2 | H30's cell cited a battery kill the file did not show at the time | receipt accuracy | FIXED r4 · LOCAL (the battery was re-run whole after R3-20 was re-pointed; the cell names the grader; `execB/battery.out` is regenerated after every round) |
| J27 | B4-S2 | 3d975da7's message says `63 mutants` | commit message | RECORDED — measured (immutable; the battery at that moment was 64 mutants, 63 killed, 1 survived — the survivor was R3-20 before its grader was re-pointed) |
| J28 | B4-S2 | the round-3 header counted 25 FIXED · 1 · 4 | receipt accuracy | FIXED r4 · LOCAL (20 · 1 · 9 by the rows' leading verdicts; the record's round 3 carries 25 — the receipt states the corrected count) |
| J29 | B4-S2 | `3 of 166` stems re-derives as 165 today | population moved | RECORDED — measured (the count is dated in the comment; the tree is live) |
| J30 | B4-S2 | three dual-verdict cells in the receipt | hygiene | FIXED r4 · LOCAL (F8, F14, H30 reworded to one leading verdict each; hygiene 0 hits over the receipt) |

## Disposition ledger — round 5 (delta over e50c22a9)

26 rows by their leading verdict: 17 FIXED · 0 REFUTED · 9 RECORDED (K11–K13 are one-hop rows fixed in-run and not counted). Six fresh seats (B5-O1 command_run.py · B5-O2 check_convergence.py + check_review_coverage.py · B5-O3 check_plan_tickets.py · B5-O4 check_review_hygiene.py + review_rubric.py · B5-S1 the round-4 tests and the conftest pin · B5-S2 the doc and comment mirrors, the commit message and this receipt) over e50c22a9's 14 files, pinned per commit. The confirmed series reads 43 → 28 → 20 → 24 → 17.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| K1 | B5-O3 | the two-segment directory alternative fires on `and/or/nor`, `TCP/IP/UDP` and URLs | false positive (fleet, ERROR severity) | FIXED r5 · LOCAL (both directory alternatives dropped — a directory and a slash-joined prose triple are one shape, stated as the cost; a URL is stripped before the test; graders: the three prose shapes and the URL through `check_plan_dir`) |
| K2 | B5-O3 | the trailing-slash alternative had no grader and fired on `w/` | fail-open + false positive | FIXED r5 · LOCAL (dropped with K1; `runs w/ the flag` asserted quiet) |
| K3 | B5-O3 | a `#` line keeping a bullet's continuation | unreachable | RECORDED — measured (`_section` ends at the next `##`; only an H1 can sit inside, an authoring error already) |
| K4 | B5-O3 | `./scripts` quiet while `src/app/x` was loud | stated cost | RECORDED — by design (D-243; directories are not paths here after K1) |
| K5 | B5-S1 | the pin assertion asserted the fixture, never the consumer | tautological test | FIXED r5 · LOCAL (`command_run._state_dir()` and `_session_id(None)` asserted against the pinned values; battery R5-13 kills a renamed env read) |
| K6 | B5-S2 | docs/FEATURES.md:91 read as a closed enumeration | doc precision | FIXED r5 · LOCAL (`and the like: any file with no `/``; and the machinery-docs exception) |
| K7 | B5-S2 | J13's cell embedded a quoted second verdict | receipt hygiene | FIXED r5 · LOCAL (one verdict; J13b holds the other) |
| K8 | B5-S2 | the brief's battery count (66) vs the file's summary (68 with one stale anchor) | machinery | RECORDED — measured (the file is regenerated whole after every round; the brief quoted the pruned list) |
| K9 | B5-O4 | an unterminated `<!--` swallows the stop heading through `_blank_quoted` | fail-open (noisy) | FIXED r5 · LOCAL (the opener with no closer below it is literal text; grader: the unterminated fixture) |
| K10 | B5-O4 | a `docs/reference/`-only hub diff gets the service floor | design | FIXED r5 · LOCAL (docs/reference and docs/workstation are the hub's machinery prose — tooling; plans, specs and reviews stay service; graders: the three docs/ cases) |
| K11 | B5-O4 | a heading with a trailing closed comment is not the stop | one hop out | RECORDED — measured (fixed in-run: the closed span is stripped from the heading text; graded) |
| K12 | B5-O4 | a heading inside an indented code block is the stop | one hop out | RECORDED — measured (fixed in-run: a 4-space or tab-indented line is skipped; graded) |
| K13 | B5-O4 | `--phrase x --phrase X` are two rows at one site | one hop out | RECORDED — measured (fixed in-run: the dedupe key lowers `what`; graded) |
| K14 | B5-O4 | `max` in `_dedupe` is unreachable-distinct; the note count and the heading line itself are ungraded | test-quality | FIXED r5 · LOCAL for the note count (`3 line(s) from` asserted); `max` stays — two hits with one key always carry one count, stated in the docstring |
| K15 | B5-O2 | the hex limb of `_is_path_token` was dead | dead guard | FIXED r5 · LOCAL (removed; the docstring says why an md5 is dropped) |
| K16 | B5-O2 | `len(heads)` proxied `records examined` in the advisory wording | message accuracy | FIXED r5 · LOCAL (one wording from the counts: blobs read of plans requested, absent-at-HEAD not counted either way) |
| K17 | B5-O2 | a dotted prose symbol (`os.getenv`) counts as a path | stated cost | RECORDED — by design (D-243; indistinguishable from a file name — stated in the docstring; 0 of 139 backticked tokens in the hub's receipts) |
| K18 | B5-O2 | `x.py:44`, `x.py::test`, `~/…` silently not paths | fail-open | FIXED r5 · LOCAL (a location suffix is stripped on both sides; `~` is path-shaped; grader: `x.py:44` and `x.py::test_a` satisfied by the `x.py` Hunt row) |
| K19 | B5-O2 | `_BARE_FILES` case-sensitive | fail-open | FIXED r5 · LOCAL (casefolded; grader: `makefile`) |
| K20 | B5-O2 | a bold non-bulleted or space-less quoted continuation ends the list | fail-open (contradicts the comment) | FIXED r5 · LOCAL (`\*{0,2}` before the plain backtick, `>+\s*`; grader: the bold-line fixture) |
| K21 | B5-O2 | the owner loop's `except` had no grader | test-quality | FIXED r5 · LOCAL (a `"started_epoch": "abc"` frame falls through; battery R5-14) |
| K22 | B5-O1 | the grammar's own filler after the marker passes the honest-shape exemption | fail-open (the class the hunk polices) | FIXED r5 · LOCAL (the tail after the marker must be a real surface: not empty, not a grammar noun; graders: the filler and the empty list) |
| K23 | B5-O1 | a lower-case `none` synonym head is now refused | tightening | RECORDED — by design (D-243; the grammar's honest value is the word `none`; asserted so the choice is pinned) |
| K24 | B5-O1 | `[a-z]*` truncates an alphanumeric plan suffix (`2v2`) | fail-closed, latent | FIXED r5 · LOCAL (`[^-\s]+` — the plan number is whatever sits between `plan-` and the next dash; grader: `plan-2v2`) |
| K25 | B5-O1 | the refusal's back-reference and the unstated 2 s grace | style | RECORDED — measured (H29 stands; the back-reference parses while the phase clause precedes it) |
| K26 | B5-O1 | the noun phrases had no graders | test-quality | FIXED r5 · LOCAL (every noun asserted as a placeholder) |

## Disposition ledger — round 6 (delta over 8a87fce1)

37 rows by their leading verdict: 20 FIXED · 6 REFUTED · 11 RECORDED (L13, L14 and L15 are one-hop rows fixed in-run and not counted). Six fresh seats (B6-O1 command_run.py · B6-O2 check_convergence.py + check_review_coverage.py · B6-O3 check_plan_tickets.py · B6-O4 check_review_hygiene.py + review_rubric.py · B6-S1 the round-5 tests and the conftest pin · B6-S2 the doc and comment mirrors, the commit message and this receipt) over the pin `execB/pinB6/` (8a87fce1, diff md5 83766fb7b3f48ad126f21e4a35d3bd11); every candidate executed by the orchestrator on a copy; two seats raised the same two defects (L8 and L24), merged into one row each.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| L1 | B6-O1 | `_is_placeholder`: a punctuation-only tail (`...`, `???`) after `surfaces exercised` is accepted while `--` is refused | fail-open (inconsistent inside one expression) | FIXED r6 · LOCAL (a tail with no word character names no surface; graders `...`, `???`, `--`; battery R6-1) |
| L2 | B6-O1 | the filler escapes the exact noun match on any decoration (`what your run touched.`, quoted, a doubled space) | fail-open | FIXED r6 · LOCAL (whitespace collapsed and edge punctuation stripped before the membership test; `…, mail.py` stays honest as round 4 decided; graders the three decorations; battery R6-2) |
| L3 | B6-O1 | the rewritten prefix match dropped `re.I` while the prefix it builds keeps it (`2026-09-12-PLAN-2-Foo` loses the escape) | case asymmetry | FIXED r6 · LOCAL (`re.I` restored; grader a mixed-case stem; battery R6-4; 0 of 167 fleet stems are mixed-case) |
| L4 | B6-O1 | the comment still says "the plan number may carry a letter (3 of 166)" while `[^-\s]+` widened the escape to word-slug plans (21 of 167 stems) with a first-token collision class | doc↔code | FIXED r6 · LOCAL (the comment names the slug rule, the 21 of 167 and the 0-collision-in-41-repos stated cost; grader a `plan-worker-node` stem's `plan-worker` receipt) |
| L5 | B6-O1 | a dotted plan id (`plan-2.1-x`) now keys `plan-2.1` where the old regex keyed `plan-2` | narrowing, one hop (`_plan_stem`) | RECORDED — measured (0 of 167 stems carry a dotted id; backlog row beside K30 at Finish) |
| L6 | B6-O1 | `<01M1ABCDEF — surfaces exercised: >` is refused without the refusal text saying "name a surface or drop the marker" | wording, one hop (the refusal) | RECORDED — by design (round 5 K-rows: an empty surface list is the placeholder; the refusal names the grammar — wording backlog at Finish) |
| L7 | B6-O1 | two mutants survive the round-5 grader: splitting the ORIGINAL-case string (`What Your Run Touched` accepted) and dropping the dashes from the lstrip set (`--` accepted) | test-quality | FIXED r6 · LOCAL (graders Title Case and `--`; battery R6-3; the lstrip mutant is subsumed by R6-1's no-word-character rule) |
| L8 | B6-O2 + B6-S2 | `_hunt_gaps` strips the location suffix on the Changed side only — a hand-written `Hunt: \`x.py:44\`` row never satisfies its own `\`x.py\`` entry (K18's "both sides" claim was false) | fail-closed false positive | FIXED r6 · LOCAL (both sides read through one key; grader the suffixed Hunt row; battery R6-10; K18's claim is now true) |
| L9 | B6-O2 | `makefile` is accepted case-blind but compared case-sensitively against `Hunt: \`Makefile\`` | created by the hunk | FIXED r6 · LOCAL (the bare-file class is case-folded on both sides, paths stay case-sensitive; battery R6-11) |
| L10 | B6-O2 | `_LOCATION_SUFFIX` strips the LAST segment only — `x.py::TestA::test_b` and `x.py:44:12` keep a colon, fail the shape and are silently DROPPED from the Changed list | fail-open | FIXED r6 · LOCAL (every `::` segment and a `line:col` suffix; graders the three shapes; battery R6-12) |
| L11 | B6-O2 | the advisory says "cut short after N of N — the plans it never reached were NOT examined" when every blob was read and only git's exit was non-zero | evidence truth | FIXED r6 · LOCAL (the tail is conditional on the count; grader a faked full stream with rc 1; battery R6-16) |
| L12 | B6-O2 | `_head_texts` slices past a truncated body and grades 3 bytes of a 600-byte plan as COMPLETE — no advisory at all | fail-open, contradicts the docstring | FIXED r6 · LOCAL (a body shorter than its header's size ends the batch as incomplete; grader the truncated stream; battery R6-14) |
| L13 | B6-O2 | a short header that is not `missing` (`<spec> ambiguous`) desyncs every later plan with `complete` still True | one hop (pre-existing loop) | RECORDED — one hop, fixed in-run beside L12 (a header git never writes ends the batch as incomplete; battery R6-15) — not counted |
| L14 | B6-O2 | `_CHANGED_LINE`'s continuation lookahead crosses a newline (`\s*`): a lone `>` line does not end the list | one hop (the `\s` crossing predates the hunk) | RECORDED — one hop, fixed in-run (`[ \t]*`; grader the lone `>` line; battery R6-13) — not counted |
| L15 | B6-O2 | `started_epoch: "nan"` passes both validity guards | robustness, no behaviour change | RECORDED — measured (the outcome is `[]` either way: `nan` falls through to the outer frame like `"abc"`, and a last-frame `nan` never matches an mtime); `if not since > 0` applied, no grader — behaviour-equivalent |
| L16 | B6-O2 | `battery.out` recorded R5-3 as SURVIVED while the same mutant KILLED on the seat's copy | evidence | REFUTED as a grader defect — the recorded `battery.out` was STALE (a run before the round-5 graders landed; a seat had also compiled `__pycache__` into the pin): on a fresh, pycache-free copy R5-3, R5-6, R5-14 and R4-10 all KILLED; the battery is rebuilt with `tar --exclude='__pycache__'` and re-run whole at every close from now on (MACHINERY, in the FEEDBACK) |
| L17 | B6-O3 | `_URL_RE` is case-sensitive while `_PATHISH` carries `re.I`: `HTTP://EXAMPLE.COM/a/b/notes.md` is not stripped and its path fires | false positive | FIXED r6 · LOCAL (`re.I`; grader through `check_plan_dir`; battery R6-5) |
| L18 | B6-O3 | the comment claims "a URL is stripped before the test" while a scheme-less link (`docs.python.org/3/library/re.html`) still reads as a path | doc↔code | FIXED r6 · LOCAL (the comment names the scheme-less link and `Node.js` as the stated cost — quote links with their scheme; grader pins both shapes as paths) |
| L19 | B6-O3 | only `http(s)` schemes are stripped: `file:///opt/x.py` and `ftp://…/readme.md` fire | false positive | FIXED r6 · LOCAL (any scheme; graders `file:///` and `ftp://`; battery R6-6) |
| L20 | B6-O3 | two mutants survive the whole file: `https?` → `https` and `sub(" ")` → `sub("")` | test-quality | FIXED r6 · LOCAL for the first (the `HTTP://` fixture kills it); the second RECORDED — measured (no failing input: `_PATHISH`'s lookbehind is only loosened by an inserted space) |
| L21 | B6-O3 | a dotted product name (`Node.js`, `Vue.js`) reads as a path | one hop (the extension alternative predates the hunk) | RECORDED — measured (0 of 638 `## Touches` lines in the hub's 317 plan files are blockquotes — the whole branch is prospective; stated in the `_URL_RE` comment) |
| L22 | B6-O3 | a quoted line whose URL precedes ` - text` escapes the bullet arm | theoretical | REFUTED (`> see https://x - do the thing` is prose with a dash; stripping before the anchored bullet test would flag it as a hidden bullet — the anchor is right) |
| L23 | B6-O3 | `--plan-dir docs/development/plans/archived/<set>` is refused as "not under docs/development/plans/" without `--allow-external` | one hop (containment) | RECORDED — measured (the containment accepts direct children only; 12 of the 17 dated sets are archived; backlog row at Finish; brief MACHINERY) |
| L24 | B6-O4 + B6-S2 | `_until_heading` neutralises at most ONE opener (the last `<!--`, then `break`): two unterminated openers still swallow the stop | fail-open, the round-5 class | FIXED r6 · LOCAL (every opener with no closer below is neutralised; grader two openers; battery R6-7) |
| L25 | B6-O4 | the neutraliser reads `-->` raw while `_blank_quoted` reads it through `_mask_code_spans`: a closer inside a code span counts for one and not the other | cross-model asymmetry | FIXED r6 · LOCAL (both read masked; grader a `\`-->\`` span; battery R6-8) |
| L26 | B6-O4 | `_floor_for`'s round-5 comment says "plans, specs, reviews … describe service work" while a plan beside a reference doc is silently outvoted to TOOLING (SERVICE before round 5) | doc↔code | FIXED r6 · doc (the comment now says plans ABSTAIN — alone they leave the service floor, beside a tooling doc they are outvoted; the code is by design: a hub plan is hub work and the partition is documentation; grader pins the mixed case) |
| L27 | B6-O4 | a 4-space-indented heading inside a list item is skipped as an indented code block | over-broad skip | REFUTED (outside a list a 4-space line IS an indented code block by CommonMark, and the receipt template's section heading sits at column 0 — 1–3 spaces stay headings, grader `three.md`) |
| L28 | B6-O4 | a closed comment INSIDE the heading text leaves a doubled space, so `## Pass <!-- x --> Ledger` is not the stop | whitespace | FIXED r6 · LOCAL (runs of whitespace compared as one space; grader `inner.md`; battery R6-9) |
| L29 | B6-O4 | a heading line that OPENS a multi-line comment is blanked whole by `_blank_quoted` | one hop | RECORDED — one hop (`_blank_quoted` blanks from the line, not the column; backlog row at Finish) |
| L30 | B6-O4 | `docs/reference/data.json` now votes TOOLING | widening | RECORDED — by design (a non-prose file under a tooling prefix is tooling; 0 such files in the hub) |
| L31 | B6-O4 | mutants the battery misses: 3-space indent, a greedy comment strip | test-quality | RECORDED — measured (the 3-space heading is now asserted; the greedy strip is behaviour-equivalent under the whitespace normalisation) |
| L32 | B6-S1 | the round-5 comment credits the tail check with refusing `<nothing filed — surfaces exercised: mail.py>`; the several-prose-tokens rule refuses it, with the tail check reverted too | test-quality | FIXED r6 · doc (the case stands as its own assertion with the honest comment; a dotted real tail `mail.py.` asserted honest) |
| L33 | B6-S1 | `x.py:44, x.py::test_a` → rc 0 passes identically with the round-5 hunk reverted | test-quality | REFUTED (a satisfied-side assertion is one half of a pair; its sibling `y.py:44` → rc 1 discriminates, as the seat's own mutant showed) |
| L34 | B6-S1 | the noun loop exercises the unchanged noun branch | test-quality | REFUTED (a regression assertion, not claimed as round 5's) |
| L35 | B6-S2 | K21 cites battery R5-14 as proof while `battery.out` recorded it SURVIVED | evidence | REFUTED (L16: the recorded run was stale; R5-14 KILLED on a fresh copy — the citation stands) |
| L36 | B6-S2 | docs/FEATURES.md:91 lists 6 of the 8 `TOOLING_PREFIXES` as if enumerating the constant | doc | FIXED r6 · LOCAL (the two round-5 prefixes added to the list) |
| L37 | B6-S2 | Coverage Checklist row for check_review_coverage.py still reads `FIXED r1 (F10–F16)` after rounds 2–5 touched the file | receipt hygiene | RECORDED — by design (the checklist rows are adjudicated to their FINAL state at the close, one leading verdict each; the close names K18's corrected claim) |

## Disposition ledger — round 7 (delta over f6541b4e)

22 rows by their leading verdict: 13 FIXED · 2 REFUTED · 7 RECORDED (M3 and M15 are one-hop rows fixed in-run and not counted). Six fresh seats (B7-O1 command_run.py · B7-O2 check_convergence.py + check_review_coverage.py · B7-O3 check_plan_tickets.py · B7-O4 check_review_hygiene.py + review_rubric.py · B7-S1 the round-6 tests · B7-S2 the doc and comment mirrors, the commit message and this receipt) over the pin `execB/pinB7/` (f6541b4e, diff md5 3c4358ce4773561c71c6362157746acf); every candidate executed by the orchestrator on a copy. The round's class: the OTHER SIDE of round 6's symmetry fixes — the argument where the line was fixed, the head where the tail was, the delimiter the anchor missed, the count copied from a seat instead of re-derived.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| M1 | B7-O1 | `_is_placeholder`: the hand-listed edge-strip set misses `(…)`, backticks, `[…]`, `!` — the filler passes as honest | fail-open (every occurrence) | FIXED r7 · LOCAL (any run of non-word characters at either edge is stripped; graders the four shapes; battery R7-1) |
| M2 | B7-O1 | a doubled space INSIDE the marker (`surfaces  exercised`) drops an honest value out of the honest branch — refused with no satisfying value | fail-closed (any whitespace) | FIXED r7 · LOCAL (`low` is whitespace-collapsed; `surfaces?\s+exercised` in the guard and the split; grader the doubled-space honest value; battery R7-2/R7-2b) |
| M3 | B7-O1 | a decorated HEAD (`'none'`, `(none)`) is refused while the tail tolerates decoration | one hop (the head test pre-dates the hunk) | RECORDED — one hop, fixed in-run (the head match skips leading quotes, brackets and backticks; graders the three heads; battery R7-3) — not counted |
| M4 | B7-O1 + B7-S1 | the `plan-worker-node` case passes on the pre-round-6 code — the comment presents a regression guard as round-6 coverage | test-quality (attribution) | FIXED r7 · doc (the comment says regression) |
| M5 | B7-O2 | the advisory tail keys on `len(heads) < len(plans)`, but a plan `missing` at HEAD is REACHED and not stored — with a non-zero exit it says "never reached" for plans git answered | evidence truth (the other side of the `missing` arm) | FIXED r7 · LOCAL (`_head_texts` returns how many plans the batch reached; the tail keys on that; grader the missing-plus-rc-1 stream; battery R7-4) |
| M6 | B7-O2 + B7-S1 | `x.py::test_a[docs/x.md]` — a param carrying `/` — is not stripped and silently DROPPED from the Changed list; the round-6 `y.py::t[a-b]` case duplicated single-segment coverage | fail-open (every occurrence) | FIXED r7 · LOCAL (`/` in the segment class; the duplicate case replaced by the `/` param; battery R7-5) |
| M7 | B7-O2 | `settled.md` rows truncated at a pipe inside a code span; § Residual rows with an empty locator | machinery (the digest generator) | RECORDED — machinery (the generator masks code spans and labels Residual rows; regenerated for round 8) |
| M8 | B7-O3 | `\b` before the scheme fails after `_` — an emphasised `__https://…__` is not stripped and its path fires; the comment claims "any scheme, any case" | false positive + doc↔code (the delimiter the anchor missed) | FIXED r7 · LOCAL (no anchor — the scheme is found wherever it sits; graders `__…__` and `_…_`; battery R7-6) |
| M9 | B7-O3 | `\S+` swallows a path glued to the URL by punctuation (`https://a/x.md,src/y.py`) | one hop (the tail pre-dates the hunk) | RECORDED — measured (stated in the `_URL_RE` comment as the cost; 0 of 374 non-blank Touches lines carry `://`; backlog row at Finish) |
| M10 | B7-O3 | the grader pins alphabetic schemes only — `[a-z]+://` survives | test-quality | FIXED r7 · LOCAL (`s3://` and `git+ssh://` asserted; battery R7-7) |
| M11 | B7-O3 | `_list_paths` takes a bullet URL as a declared Touches path | one hop (the bullet collector) | RECORDED — one hop (backlog row at Finish beside F32) |
| M12 | B7-O3 | dropping the `\b` anchor survived the suite | test-quality | REFUTED (moot — the anchor is gone by M8, whose own fixture kills its restoration; the earlier reason given here, that the two forms agree on every pinned input, was false — round 8, N11) |
| M13 | B7-O4 | the ARGUMENT-side whitespace normalisation (`want = " ".join(want.split())`) had no grader | test-quality (the other side) | FIXED r7 · LOCAL (grader `##  Pass\tLedger` as the argument; battery R7-8) |
| M14 | B7-O4 | the opener test's `_mask_code_spans` had no grader (only the closer lookahead's) | test-quality (every occurrence) | FIXED r7 · LOCAL (grader a heading whose text carries a code-spanned opener; battery R7-10) |
| M15 | B7-O4 | `want` never strips a trailing `#` while the line does — a closed ATX argument (`## Pass Ledger ##`) matches nothing, against the docstring | one hop (pre-dates the hunk) | RECORDED — one hop, fixed in-run (the argument is read like the line; grader the closed ATX argument; battery R7-9) — not counted |
| M16 | B7-O4 | comment markers inside an indented code block are not quoted by `_blank_quoted` | one hop | RECORDED — measured (0 of 296 receipts under docs/development/reviews carry an indented `<!--`; backlog row at Finish beside L29) |
| M17 | B7-S1 | the round-6 comment credits `What Your Run Touched`, `--` and `mail.py.` to round 6; all three passed before it | test-quality (attribution) | FIXED r7 · doc (the comment names them as regression guards) |
| M18 | B7-S1 | `y.py::t[a-b]` duplicates the single-segment case | test-quality | FIXED r7 · LOCAL (merged into M6: replaced by the `/` param case) |
| M19 | B7-S1 | `three.md`, `early.md` and the `missing` stream case exercise pre-round-6 behaviour | test-quality (attribution) | RECORDED — by design (regression guards for L27, the CommonMark comment rule and the `missing` arm; their comments now say so) |
| M20 | B7-S2 + B7-O3 | the `_URL_RE` comment's "0 of 638 `## Touches` lines" counted blank lines — 374 non-blank in 124 of 317 files | doc (a count copied from a seat) | FIXED r7 · doc (re-derived by the orchestrator with the method stated: 374 non-blank, 264 blank, 124 files) |
| M21 | B7-S2 | the prefix-escape comment's "21 of 167 stems, 41 repos" was a looser listing — 110 dated-plan stems in 18 repos by the stated shape | doc (a count copied from a seat) | FIXED r7 · doc (re-derived: 110 direct-child dated-plan stems, 21 word-slug, 18 repos, 0 collisions; the method named) |
| M22 | B7-S2 | L36 (FEATURES.md lists 6 of 8 prefixes) still open | receipt | REFUTED (the pin's sentence lists all eight in the constant's order) |

## Disposition ledger — round 8 (delta over f8d9e478)

20 rows by their leading verdict: 10 FIXED · 0 REFUTED · 10 RECORDED (N6 and N16 are one-hop rows fixed in-run and not counted). Six fresh seats (B8-O1 command_run.py · B8-O2 check_convergence.py + check_review_coverage.py · B8-O3 check_plan_tickets.py · B8-O4 check_review_hygiene.py · B8-S1 the round-7 tests · B8-S2 the comments, the commit message and this receipt) over the pin `execB/pinB8/` (f8d9e478, diff md5 5543c7663a006bd23265b891a59751ec); every candidate executed by the orchestrator on a copy. The round's class: ONE READING for both sides — a rule stated as a class on one side and as a hand-list on the other, a normalisation applied to the line and not the argument, a sentence whose head still says what its tail no longer says.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| N1 | B8-O4 | the `--stop-at-heading` ARGUMENT is not stripped of a closed comment while the line is — a heading copied verbatim with its inline comment matches nothing (the hunk's comment says "read like the line") | the other side (doc↔code) | FIXED r8 · LOCAL (one `_heading_key` for the argument and the line; grader the commented argument; battery R8-7) |
| N2 | B8-O4 | the argument's unconditional `rstrip("#")` makes `## C#` the stop for `## C` — a live section blanked, hygiene reports zero (fail-closed, silent) | any case (created by the hunk) | FIXED r8 · LOCAL (a closing `#` run only when whitespace precedes it, CommonMark 4.2, on both sides; graders `## C#` vs `## C` and the key's two values; battery R8-6) |
| N3 | B8-O4 | a whitespace-only argument prints "heading `` not found" | cosmetic, one hop (`_note_stop`) | RECORDED — one hop (backlog wording row at Finish) |
| N4 | B8-O4 | `_note_stop` echoes the raw argument, not what was matched | cosmetic, one hop | RECORDED — one hop (same backlog row as N3) |
| N5 | B8-O1 | the round-7 head class is a hand-listed ASCII set while the tail went general — `“none”`, `**none**`, `_none_` are refused with no satisfying alternative | the other side (fail-closed) | FIXED r8 · LOCAL (the head prefix is the tail's class `[\W_]*`, the id bounded by a non-word lookahead so `_none_` reads as `none`; graders the three heads; battery R8-1) |
| N6 | B8-O1 | a surface list with no ASCII alphanumeric (`日本語.md`) strips to empty and is refused | one hop (pre-existing, behaviour-equivalent before round 7) | RECORDED — one hop, fixed in-run (the edge class is `[\W_]`, Unicode-aware; grader the non-ASCII surface; battery R8-2) — not counted |
| N7 | B8-O1 | a `command_run.py` CLI probe in a scratch dir that is not a git repo refuses every `done` as rootless | machinery (brief) | RECORDED — machinery (the brief now says to `git init` the probe dir or probe `_parse_usage_feedback` in-process) |
| N8 | B8-S2 | the commit message says "six re-pointed anchors", the Pass 7 row said "seven"; nine were re-pointed (seven round-6, R3-16, R4-1 — the message was written before the last three) | a count copied instead of re-derived (receipt) | FIXED r8 · LOCAL (the Pass 7 row carries the re-derived nine and names the immutable message's number) |
| N9 | B8-O3 + B8-S1 | the `git+ssh://` case grades nothing about `+` (the match restarts at `ssh`), and the seven-entry loop's `_PATHISH` assertion passes for a reason unrelated to the URL rule (`x.md__` fails the extension boundary on its own) | test-quality | FIXED r8 · LOCAL (the loop asserts the STRIP — no `://` survives — and the `git+ssh` residue is asserted exactly; battery R8-8) |
| N10 | B8-O3 | a real path token glued to `://` (`src/x.py://weird`) vanishes with the URL | one hop (pre-existing under the anchored form too) | RECORDED — measured (the M9 glued-tail cost; the backlog row at Finish extends) |
| N11 | B8-O3 | the settled digest's M12 row gave a false reason ("the two forms agree on every pinned input" — restoring the anchor is killed by M8's fixture) | receipt | FIXED r8 · LOCAL (the M12 row's reason corrected; the digest regenerated) |
| N12 | B8-O3 + B8-O2 + B8-O1 | the brief's probe recipe: the root `conftest.py` is tracked but `tests/conftest.py` carries the fixtures; the committed-receipt tests fail outside a git repo; a relative path to `spec_from_file_location` under a `cd` resolves against the live tree | machinery (brief) | RECORDED — machinery (all three in the round-9 brief) |
| N13 | B8-O2 | a bracket param carrying `,`, `=`, `:`, a space or `<>` is still silently DROPPED — the `/` fix widened the alphabet by one character (0 of 114 backticked `::` tokens in 31 receipts carry a bracket today) | every case (fail-open) | FIXED r8 · LOCAL (the param is its own alternative `\[[^\]]*\]`; graders `[a,b]`, `[x=1]`, `[RTX 4090]`; battery R7-5 re-pointed) |
| N14 | B8-O2 | round 7 REPLACED the `t[a-b]` grader instead of adding to it — `-` in the class was ungraded | test-quality | FIXED r8 · LOCAL (`t[a-b]` restored beside the new cases) |
| N15 | B8-O2 | the advisory's HEAD still says "cut short after 0 blob(s)" beside the new tail "every plan was read" when the only plan is `missing` | the head where the tail was | FIXED r8 · LOCAL (a neutral head "returned N blob(s) of M"; grader `cut short` absent; battery R8-5) |
| N16 | B8-O2 | the boundary plan of a cut body or a malformed header is not counted as reached although git answered its header | one hop (docstring wording) | RECORDED — one hop, docstring fixed in-run ("answered WHOLE — a `missing` answer counts, a cut body does not") — not counted |
| N17 | B8-O2 | a real path containing `::` (`docs/a::b/c.md`) is now read as `docs/a` with a suffix | stated cost | RECORDED — measured (0 on this box; stated beside `_LOCATION_SUFFIX`) |
| N18 | B8-O2 | `test_quiet_pass_is_set_identical…` fails on a copy that is not a git repo | machinery (brief) | RECORDED — machinery (N12) |
| N19 | B8-S1 | the `(mail.py)` entry of the `real` tuple passes on the pre-round-7 code | test-quality (attribution) | RECORDED — by design (a guard that the widened tail strip never over-eats a decorated real surface; its comment says so) |
| N20 | B8-S1 | the hygiene loop's comment presents `arg-ws.md` and `span-head.md` as round-7 coverage; only `arg-atx.md` discriminates round 7 | test-quality (attribution) | FIXED r8 · doc (the comment names which case is which round's) |

## Disposition ledger — round 9 (delta over 4fe50105)

14 rows by their leading verdict: 7 FIXED · 0 REFUTED · 7 RECORDED (P2 and P4 are one-hop rows fixed in-run and not counted). Six fresh seats (B9-O1 command_run.py · B9-O2 check_convergence.py · B9-O3 check_review_coverage.py · B9-O4 check_review_hygiene.py · B9-S1 the round-8 tests · B9-S2 the comments, the commit message and this receipt) over the pin `execB/pinB9/` (4fe50105, diff md5 54a8e3dcb91c860e10a33b98ff154e7c); every candidate executed by the orchestrator on a copy. The round's class: the RE-CUT LOSES A SHAPE THE OLD CUT HAD — a class rewritten as an alternative drops nested brackets, a Unicode class admits a decoration the ASCII class refused, a neutralised line no longer equals its verbatim argument, a word swapped in one clause of a sentence and not the other.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| P1 | B9-O2 | the advisory tail says "every plan was read" when every plan was `missing` — answered, never read ("returned 0 blob(s) of 1 … every plan was read") | one reading for both clauses | FIXED r9 · LOCAL (the tail uses the docstring's word: "answered"; grader the all-missing rc-1 stream; battery R9-1) |
| P2 | B9-O2 | three readings of one integer — docstring "answered whole", comment "REACHED", tail "never reached" | one hop (the comment and the tail's word) | RECORDED — one hop, fixed in-run (the tail says "did not answer whole"; the comment quotes the docstring) — not counted |
| P3 | B9-O4 | an UNTERMINATED opener on the heading line: the line is neutralised (`<!- -`) before the compare, the argument is not — a verbatim-copied heading matches nothing and the history is graded live | one reading for both sides (created by the hunk's own claim) | FIXED r9 · LOCAL (the key drops an unterminated opener's tail; the compare reads the RAW line; grader `arg-unterm.md`; battery R9-2) |
| P4 | B9-O4 | `lstrip("#")` eats the heading text's own `#` (`## #hashtag` → `hashtag`); the key is not idempotent | one hop (the strip pre-dates the hunk) | RECORDED — one hop, fixed in-run (the leading run is an ATX run `#{1,6}` then whitespace; graders `hashtag.md` and the idempotency loop; battery R9-3) — not counted |
| P5 | B9-O4 | a setext heading is never a stop | by design | RECORDED — by design (the docstring says a `#`-led line; the receipt template's Pass Ledger is ATX) |
| P6 | B9-O3 | a param carrying its own brackets (`t[a[0]]`, `t[a][b]` — real pytest ids) is no longer stripped: the round-8 alternative `\[[^\]]*\]` cannot nest, the replaced class could — silently DROPPED again | the re-cut loses a shape the old cut had (fail-open) | FIXED r9 · LOCAL (one nesting level and repeated groups; the unbalanced `t[a` stated as the cost; graders the three shapes; battery R9-4) |
| P7 | B9-S1 | the `_what your run touched_` case is credited to round 8; round 7's ASCII class already stripped `_` | test-quality (attribution) | FIXED r9 · doc (the comment says regression) |
| P8 | B9-S2 | the Pass 8 row says "one duplicate dropped"; two were (R6-9, R5-8) | receipt count | FIXED r9 · LOCAL (the row names both and the 110-listed-plus-M21 total) |
| P9 | B9-S2 | the brief says "110-mutant battery" while battery.out says 111 (M21 is appended outside the list) | machinery (brief) | RECORDED — machinery (the round-10 brief states "111: 110 listed plus the appended M21") |
| P10 | B9-O1 | the round-8 `[\W_]` tail class admits a Unicode alnum decoration the ASCII class refused: `what your run touched¹` is accepted as honest (fail-open, a regression against round 7) | the re-cut loses a shape (any alphabet) | FIXED r9 · LOCAL (emptiness decided on the Unicode class, the noun equality on the ASCII-stripped form; graders `¹` and `①`; battery R9-5) |
| P11 | B9-O1 | the round-8 lookahead `(?![^\W_])` is ungraded — deleting it lets `nonentity` enter the honest branch | test-quality (surviving mutant) | FIXED r9 · LOCAL (grader `<nonentity — …>`; battery R9-6) |
| P12 | B9-O1 | `<>` and `< >` pass both the placeholder test and the emptiness test | one hop (pre-existing) | RECORDED — one hop (backlog row at Finish beside L6) |
| P13 | B9-O1 | a lower-case mail id at the head is refused | by design | RECORDED — by design (hub mail ids are upper-case ULIDs; K23's sibling) |
| P14 | B9-O1 + B9-O4 + B9-O2 | the probe recipe's copy list lacks `.claude/hooks` and `commands` (five command_run tests read them); the expected baseline is unstated; the brief named three `reached += 1` sites, there are two | machinery (brief) | RECORDED — machinery (the round-10 brief) |

## Disposition ledger — round 10 (delta over 4fe50105: the two round-9 commits 7dad5031 + 2d93ad84)

16 rows by their leading verdict: 9 FIXED · 1 REFUTED · 6 RECORDED (Q1, Q6 and Q10 are one-hop rows fixed in-run and not counted). Six fresh seats (B10-O1 command_run.py · B10-O2 check_convergence.py · B10-O3 check_review_coverage.py · B10-O4 check_review_hygiene.py · B10-S1 the round-9 tests · B10-S2 the comments, both commit messages and this receipt) over the pin `execB/pinB10/` (2d93ad84, diff md5 18cd01d84df9f051ba433dd7c56213ef); every candidate executed by the orchestrator on a copy. The round's class: PROSE THAT OVERCLAIMS ITS CODE — a comment's "never", an example the code does not produce, a cost list that omits a real shape, a grader that names a retired phrase — five of the nine are the orchestrator's own sentences; round 10's comments state shapes and costs only.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| Q1 | B10-O1 | the head's skip class `[\W_]*` cannot skip a `\w` non-ASCII decoration (`¹none`, `none¹`) that the tail now tolerates — an honest value refused | one hop (the head pre-dates the hunk) | RECORDED — one hop, fixed in-run (the head's skip class is the tail's ASCII complement; graders `¹none`, `none¹`; battery R10-1) — not counted |
| Q2 | B10-O1 | the round-9 comment says a `\w` decoration "never makes the grammar's own filler honest"; an INTERIOR homoglyph (`t①uched`) does | doc↔code (the comment's quantifier) | FIXED r10 · doc (the comment says "at either edge" and states the interior homoglyph as the cost) |
| Q3 | B10-O1 | a tail of non-ASCII word characters only (`①①①`) names no surface and passes | one hop (pre-existing) | RECORDED — one hop (backlog row at Finish beside P12) |
| Q4 | B10-O3 | the comment's own example `docs/a::b/c.md` is not "read as `docs/a`" — the `/` after `::` fails the segment class and the token is silently DROPPED | doc↔code (an example the code does not produce) | FIXED r10 · LOCAL (the node id is everything after the first `::` — it is not parsed; the example is now true; battery R6-12/R7-5/R9-4 re-pointed) |
| Q5 | B10-O3 | the cost list names only the unbalanced `t[a` while two ids pytest DOES emit (`t[a[b[0]]]`, `t[a]b]`) are still dropped — rounds 6–9 grew a bracket grammar one alphabet at a time and each cut lost a shape | the re-cut loses a shape (fail-open) | FIXED r10 · LOCAL (same change as Q4; graders the two ids) |
| Q6 | B10-O3 | a param followed by a `:line` suffix (`t[a]:44`) is dropped | one hop (pre-existing) | RECORDED — one hop, fixed in-run by Q4's change (grader `t[a]:44`) — not counted |
| Q7 | B10-O2 | the tail-B guard still asserts `"never reached" not in adv[0]` — a phrase no branch produces since round 9 (a vacuous conjunct) | test-quality (in-hunk test) | FIXED r10 · LOCAL (the guard names the live phrase) |
| Q8 | B10-O2 | the round-7 comment above the rewritten assertion still names the retired phrase | doc | FIXED r10 · doc |
| Q9 | B10-O2 | tail B explains an absent plan inside itself AND the sentence's trailing parenthetical explains it again | wording (in-hunk) | FIXED r10 · LOCAL (each branch explains an absent plan once; grader `count("a plan absent at HEAD") == 1` on both branches; battery R10-2) |
| Q10 | B10-O2 | the round-9 comment quotes the docstring minus "a header git never writes" | one hop (comment fidelity) | RECORDED — one hop, fixed in-run — not counted |
| Q11 | B10-O4 + B10-S1 | `_heading_key` strips closed comments on the RAW string before masking, so a `-->` inside a code span closes a real opener (and a span's `<!--` is eaten up to a later real closer) — the stop is missed and the history graded live | one reading for both markers (created by the round-9 order) | FIXED r10 · LOCAL (closed comments located on the masked copy, then the opener; graders `span-closer.md`, `span-opener.md`, two key values; battery R10-3; the round-9 span assertion, which exercised nothing, replaced) |
| Q12 | B10-O4 | the neutralisation look-ahead reads `-->` through the code-span mask but not through fences | one hop (pre-existing loop) | RECORDED — one hop (backlog row at Finish beside L29/M16) |
| Q13 | B10-S1 | `## #hashtag` and `## Pass Ledger # #` pass on the pre-round-9 key (the space already kept the text's `#`; the closing run was unchanged); the `` ## Pass `<!--` Ledger `` assertion exercised no mask (no closer present) | test-quality (attribution) | FIXED r10 · doc (the two regression comments; the third assertion replaced by Q11's discriminating pair) |
| Q14 | B10-S1 | `<nonentity — …>` is satisfied by the generic rule, never the lookahead | test-quality | REFUTED (battery R9-6 — the lookahead deleted — is KILLED by that assertion on the fresh run; the seat's mutant differed) |
| Q15 | B10-S2 | the Pass 9 row is silent on the hold and on round 9 landing as two commits | receipt | FIXED r10 · LOCAL (the row names 7dad5031 UNGATED, the hold, and 2d93ad84) |
| Q16 | B10-O1 + B10-O2 + B10-O3 + B10-O4 | the brief and the settled digest carry two battery counts; per-suite baselines are stated for two suites and wrong for one (60 collected, 56 passed 4 skipped) | machinery (brief) | RECORDED — machinery (the round-11 brief carries one count and per-suite baselines) |

## Disposition ledger — round 11 (delta over f1905d50)

14 rows by their leading verdict: 9 FIXED · 0 REFUTED · 5 RECORDED (R11 is a one-hop row fixed in-run and not counted). Six fresh seats (B11-O1 command_run.py · B11-O2 check_convergence.py · B11-O3 check_review_coverage.py · B11-O4 check_review_hygiene.py · B11-S1 the round-10 tests · B11-S2 the comments, the commit message and this receipt) over the pin `execB/pinB11/` (f1905d50, diff md5 fc4ba37c7304ae7af081bc1166334bfd); every candidate executed by the orchestrator on a copy. The round's class: A GRADER THAT PINS THE FIXTURE, NOT THE PROPERTY — the `$` anchor, the `reversed`, the tail-B phrase and the head's letter class each had exactly the fixture the seat named and nothing on the property's other side; round 11's graders cover both branches, two occurrences and the negative side of each rule, and the head's decoration rule is one predicate (`_is_decoration`) instead of a fourth regex spelling.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| R1 | B11-O2 | tail B's explanation is graded by COUNT only — swapping it for tail A's wording survives the suite | test-quality (the property, not the fixture) | FIXED r11 · LOCAL (both branches asserted by phrase; battery R11-3) |
| R2 | B11-O2 + B11-S1 | tail A's carried parenthetical "not counted either way" names two ways inside a sentence carrying three counts; the round-10 rc-0 case could not fail (the count was 1 before and after) | wording (in-hunk line) + a vacuous grader | FIXED r11 · LOCAL (one phrase in both branches: "was answered and has no blob"; the rc-0 case asserts the phrase and the retired one's absence — it now discriminates) |
| R3 | B11-O2 | the exception path prints "git cat-file returned 0 blob(s)" when git never ran | one hop (the head, pre-existing) | RECORDED — one hop (backlog row at Finish beside H29) |
| R4 | B11-O3 | the `$` anchor of `_LOCATION_SUFFIX` has no grader — dropping it invents `redis-main/0` from `` `redis-main:6379/0` `` and the suite stays green | test-quality (surviving mutant) | FIXED r11 · LOCAL (a mid-token `:port` token asserted prose; battery R11-4) |
| R5 | B11-O3 | `os.path::x` → `os.path` claimed (the K17 dotted-symbol cost through `::`); `a:1:2:3` → `a:1` then dropped | one hop (pre-existing) | RECORDED — by design (K17's docstring cost; harmless) |
| R6 | B11-O1 | the head's skip class `[^0-9A-Za-z]*` skips Unicode LETTERS — `日本語none` reads as the head `none`, `none日`/`01M1ABC日` pass the ASCII lookahead (fail-open; the round-9 grader's own rule "none inside a word is no head" broken for non-Latin words) | the re-cut loses a shape (any script) | FIXED r11 · LOCAL (`_is_decoration`: neither a letter nor a decimal digit in any script, applied before and after the head; graders the three shapes; battery R11-1/R11-2, R7-3/R8-1/R9-6 re-pointed) |
| R7 | B11-O1 | the comment says the head's skip class is "the same ASCII complement" — three spellings of two classes, equal only because the tail is lowercased | doc (overclaim) | FIXED r11 · doc (the comment names the predicate and states where the two agree) |
| R8 | B11-O1 | any 6+ upper-case word reads as a mail id (`NONEXISTENT`) | by design (pre-existing) | RECORDED — by design (P13/K23's sibling; backlog row) |
| R9 | B11-O4 | the round-10 heading fix holds only while no LATER line carries `-->`: the neutralisation loop then treats the heading as a comment opener, blanks it, and the key is never consulted (65 of 296 receipts carry a `-->` line) | the grader concealed the condition | FIXED r11 · LOCAL (a closer inside a span on the opener's own line ends the block for the renderer — that opener is literal text; grader `span-closer-later.md`; battery R11-5) |
| R10 | B11-O4 | `reversed` in the splice has no grader — both fixtures carry one comment | test-quality | FIXED r11 · LOCAL (a two-comment heading asserted; battery R11-6) |
| R11 | B11-O4 | CommonMark's empty comments `<!-->`/`<!--->` are unterminated openers to the key; a splice can join `<!-`+`- B` into a fresh opener | one hop (pre-existing regex) | RECORDED — one hop, the empty forms fixed in-run (`<!--(?:-?>|.*?-->)`; graders both forms; battery R11-7); the splice-join stays a backlog row — not counted |
| R12 | B11-S1 | the round-10 rc-0 case is vacuous relative to round 10 | test-quality | FIXED r11 · LOCAL (merged into R2 — the case now asserts the new phrase) |
| R13 | B11-S2 | the "adjudicated at the close" convention for the Coverage Checklist lives only in one L-row | receipt clarity | FIXED r11 · LOCAL (one line under the checklist heading) |
| R14 | B11-O1 + B11-O3 + B11-O4 | the copy list is heavier than most slices need; the deselect is named for a suite whose single node needs none | machinery (brief) | RECORDED — machinery (the round-12 brief tags the copy list per suite) |

## Disposition ledger — round 12 (delta over 17f7ec90)

12 rows by their leading verdict: 7 FIXED · 0 REFUTED · 5 RECORDED (S6 is a one-hop row fixed in-run and not counted). Six fresh seats (B12-O1 command_run.py · B12-O2 check_convergence.py · B12-O3 the coverage grader · B12-O4 check_review_hygiene.py · B12-S1 the round-11 tests · B12-S2 the comments, the commit message and this receipt) over the pin `execB/pinB12/` (17f7ec90, diff md5 8f16af2392b684049fdc1e327cc8e2dc); every candidate executed by the orchestrator on a copy. The round's class: A RULE ANCHORED TO THE WRONG POINT — a same-line closer test that did not know where the opener was, a parenthetical true on the other branch, an alternation whose first arm hid the second, a boundary predicate with one half graded.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| S1 | B12-O2 | tail A's new parenthetical "was answered and has no blob" is false on the truncated and exception paths (absent plans after the break were never answered) | a clause true on the other branch | FIXED r12 · LOCAL (tail A qualifies the absent plan as an ANSWERED one; grader the whole parenthetical per branch; battery R12-1) |
| S2 | B12-O2 + B12-S1 | the round-11 graders pin a shared fragment and the retired phrase's absence — swapping the branches' parentheticals survives; the tail-B assertion could not fail | test-quality (the property, not the fixture) | FIXED r12 · LOCAL (each branch's parenthetical asserted whole, the other branch's absent) |
| S3 | B12-O4 | the round-11 same-line test `"-->" in ln` is unanchored — a `-->` BEFORE the opener neutralises a genuine multi-line comment and a commented-out ledger heading is the stop again (round 4's defect, 4 live lines blanked) | a rule anchored to the wrong point (regression) | FIXED r12 · LOCAL (the test reads from the opener: `ln[ln.find("<!--"):]`; grader `span-closer-before.md`; battery R12-2) |
| S4 | B12-O4 | the heading-line prefilter kept the old closed-comment spelling — a heading behind a leading `<!-->` is never the stop (a third reading of the heading) | every occurrence | FIXED r12 · LOCAL (one module pattern `_CLOSED_COMMENT` for every closed-comment reader; grader the `<!--> ## Pass Ledger` line; battery R12-3, R10-3/R11-6/R11-7/R8-7/R4-9 re-pointed) |
| S5 | B12-O4 | `_blank_quoted` still opens a multi-line comment for a line whose only `-->` sits in a span, while `_until_heading` reads it as literal — the two raw call sites (`_ledger_rows`, table-parity) diverge, widened by round 11 | one hop (pre-existing model) | RECORDED — one hop (backlog row at Finish beside L29/M16/Q12) |
| S6 | B12-O3 | `_is_path_token`'s docstring never says the suffix strip is END-anchored (a mid-token `:port` token is dropped whole) | one hop (doc) | RECORDED — one hop, fixed in-run (the clause added) — not counted |
| S7 | B12-S2 | the brief template's "commit message of the delta" line still pointed at `msg6.txt` | machinery (brief) | RECORDED — machinery (the template names the round's message) |
| S8 | B12-O1 | the boundary predicate's decimal half is ungraded — dropping it lets `none3`/`none٣` pass as honest heads | test-quality (one half graded) | FIXED r12 · LOCAL (graders `none3`, `none٣`; battery R12-5) |
| S9 | B12-O1 | with the lookahead gone the alternation `(?:(?i:none)\|[0-9A-Z]{6,})` never tries the id arm on a `NONE`-prefixed word — `NONEXISTENT` (an id by R8) is now refused, an unstated reversal of a recorded behaviour | the first arm hides the second | FIXED r12 · LOCAL (the id arm first; grader `NONEXISTENT` honest; battery R12-4) |
| S10 | B12-O1 | the tail comment's "the two agree on every ASCII character" is false under its own antecedent (the noun-edge class differs on A–Z) | doc (ambiguous antecedent) | FIXED r12 · doc (the comment names the class it agrees with and why A–Z is moot) |
| S11 | B12-O1 | a zero-width character between `none` and a letter defeats the boundary while rendering as the refused shape | one hop | RECORDED — one hop (backlog row beside P12/Q3) |
| S12 | B12-O1 | `isdecimal()` catches Arabic-Indic digits but not Nl/No numerals (`Ⅳnone` honest, `IVnone` refused) | stated cost | RECORDED — by design (the docstring says "decimal digit"; backlog row beside Q3) |

## Disposition ledger — round 13 (delta over 15436a3f)

15 rows by their leading verdict: 10 FIXED · 1 REFUTED · 4 RECORDED (T11 and T12 are one-hop rows fixed in-run and not counted). Six fresh seats (B13-O1 command_run.py · B13-O2 check_convergence.py · B13-O3 the coverage docstring · B13-O4 check_review_hygiene.py · B13-S1 the round-12 tests · B13-S2 the comments, the commit message and this receipt) over the pin `execB/pinB13/` (15436a3f, diff md5 068c53d746c1e3b644cedbd6a47ee94c); every candidate executed by the orchestrator on a copy. The round's class, and the CLASS REWRITE it triggers (D10 rule 3 — the same two spots yielded in every round since round 5): the hygiene checker's HTML-comment reading is now ONE function (`_comment_cuts`, read through the code-span mask) used by `_blank_quoted`, `_heading_key` and `_until_heading`; the placeholder rule's honest head is graded by ONE table over every arm and boundary instead of fixtures added one per round.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| T1 | B13-O4 | the round-12 anchor `ln.find("<!--")` is computed on the RAW line while the entry test read the masked one — a code span holding a closed `<!-- x -->` moves the anchor to the span's opener, a real comment is neutralised, the stop fires inside it and a live stale phrase after the closer is dropped (fail-open) | the reader and the anchor disagree | FIXED r13 · LOCAL (`_comment_cuts` returns the opener the masked read found; grader `span-holds-closed.md` → [3, 6, 9]; battery R13-3) |
| T2 | B13-O4 | the round-12 comment says "the ONE pattern every closed-comment reader uses" — 2 of 7 readers used it; `_blank_quoted`'s and `_until_heading`'s substring entry tests read an empty comment before a real opener as a closed line (fail-open, 4 lines blanked) | prose overclaim + a live contradiction | FIXED r13 · LOCAL (every reader goes through `_comment_cuts`; graders the `<!--> note <!-- opens` line in both `_until_heading` and `_blank_quoted`; battery R13-4) |
| T3 | B13-O4 | the `_CLOSED_COMMENT` alternation order is ungraded — swapped, `<!--> B --> C` eats live heading text | test-quality | FIXED r13 · LOCAL (grader `## A <!--> B --> C` → `a b --> c`; battery R13-5) |
| T4 | B13-O4 | the deliberately RAW heading-line prefilter is unpinned — masking it would make a span-led line the stop | test-quality (a choice with no grader) | FIXED r13 · LOCAL (negative fixture; battery R13-6) |
| T5 | B13-O1 | the id-first reorder left the `(?i:)` arm with no fixture — `None`/`NONE`/`nOnE` heads flip to refused under a mutant | test-quality (an arm ungraded) | FIXED r13 · LOCAL (the head table; battery R13-1) |
| T6 | B13-O1 | the `{6,}` threshold now carries R8's whole boundary and is ungraded (`NONE3` vs `NONE12`) | test-quality | FIXED r13 · LOCAL (the head table; battery R13-2) |
| T7 | B13-O1 | the tail comment attaches one "moot" to two classes read on different strings (`c` vs `low`) | doc (antecedent) | FIXED r13 · doc |
| T8 | B13-O3 | the round-12 docstring clause states the anchor's safe side only — a TRAILING `:port` is a `:line` and IS stripped (`a/b:80` → the path `a/b`) | one side of the symmetry (doc) | FIXED r13 · doc + grader (`a/b:80` → rc 1 with `a/b`) |
| T9 | B13-O3 | the docstring's "a version" is only a BARE dotted numeral (`v1.2.3` is a path) and the version regex is ungraded | doc overclaim + test-quality | FIXED r13 · doc + graders (`1.2.3` prose, `v1.2.3` a path; battery R13-7) |
| T10 | B13-O3 | two disjoint cost lists for one function (the constant's comment and the docstring) | one hop | RECORDED — one hop (backlog row at Finish beside N17) |
| T11 | B13-O3 | the added docstring line is 131 characters | style, one hop | RECORDED — one hop, rewrapped in-run — not counted |
| T12 | B13-O2 | the comment above the tail does not say why the two parentheticals differ | one hop (wording) | RECORDED — one hop, four words added in-run — not counted |
| T13 | B13-S1 | the `none3`/`none٣` comment credits round 12 for a boundary that pre-dates it (round 12 added their graders) | test-quality (attribution) | FIXED r13 · doc (the table's comment names the rule, not a round) |
| T14 | B13-S1 | the `[3, 6]` expectation encodes the file's any-position-opener model, not CommonMark's block-start rule | by design (the L29 family) | RECORDED — by design (the file's one quoting model; L29/S5/Q12/M16's backlog row) |
| T15 | B13-S2 | "eight anchors re-pointed" — the seat found seven | receipt count | REFUTED (R4-9, R8-7, R10-2, R10-3, R11-5, R11-6, R11-7 and R6-8 — the eighth the seat missed) |

## Disposition ledger — round 14 (delta over fd87d358)

9 rows by their leading verdict: 5 FIXED · 2 REFUTED · 2 RECORDED. Six fresh seats (B14-O1 the head table · B14-O2 the coverage docstring and the advisory comment · B14-O3 `_comment_cuts` · B14-O4 `_until_heading` on the new reader · B14-S1 the round-13 tests · B14-S2 the docstrings, the commit message and this receipt) over the pin `execB/pinB14/` (fd87d358, diff md5 6883f6df1c7437c2804d04bdfeaf9c87); every candidate executed by the orchestrator on a copy. The class rewrite held: the 21-row head table kills every one of twelve mutants with no dead row; `_comment_cuts` survived a 200,000-line fuzz (offsets raw-true, no straddling span). The residue is the CLOSER side of the same model — the opener reading was unified in round 13, the three closer readings were not.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| U1 | B14-O2 | the `_is_path_token` docstring now says both "a bare dotted version (`1.2.3` — `v1.2.3` is a path)" and, in the retained list, "a version … is not" — two incompatible rules; `1.2.3-rc1` is a path | prose overclaim (in-hunk) | FIXED r14 · doc (the bare word removed; `1.2.3-rc1` named as the cost; grader `1.2.3-rc1` → rc 1) |
| U2 | B14-O2 | the trailing-`:port` clause exemplifies only `a/b:80` while the shape that bites on this box is a dotted host (`db.example.com:5432` → the path `db.example.com`) | a cost list missing the real shape | FIXED r14 · doc (the box shape named; graders `db.example.com:5432` → rc 1, `10.99.0.1` → rc 0; battery R14-4) |
| U3 | B14-O4 | the round-13 from-opener neutralisation (`ln[:opener] + ln[opener:].replace(...)`) is ungraded — the whole-line replace mangles a closed comment before the heading, the raw prefilter misses it, the stop is lost | test-quality (an ungraded hunk line) | FIXED r14 · LOCAL (grader `<!-- x --> ## Pass Ledger <!-- open` → 2; battery R14-3) |
| U4 | B14-O3 + B14-S2 | `_comment_cuts`'s docstring says "the ONE reading … share" — `_until_heading`'s prefilter strips closed comments RAW by design and `_blank_quoted`'s closer test read masked | prose overclaim (in-hunk) | FIXED r14 · doc (the docstring names the opener reading it IS, and where the two other readings live and why) |
| U5 | B14-O3 | the `c[1] <= opener` filter in `_heading_key` is dead defensive code (no closed span can straddle the opener — fuzzed) | one hop | RECORDED — measured (a comment says so) |
| U6 | B14-S1 | the `## A <!--> B --> C` and raw-prefilter assertions pass on the pre-round-13 file | test-quality | REFUTED (they pin a CHOICE, not a round-13 change: each kills its battery mutant — R13-5 the alternation order, R13-6 the raw prefilter — which is what they were written for) |
| U7 | B14-S2 | `_blank_quoted`'s closer test is MASKED while `_until_heading`'s round-11 same-line test read a span's `-->` RAW — two closer readings; and the closing line's tail after `-->` is blanked whole, so G29's "the tail after the close is re-tested" is false and a dual-verdict row after a closer goes unflagged | one reading for both sides (the closer side) + a settled row contradicted | FIXED r14 · LOCAL (the file's ONE model is the MASKED one — I4, its oldest grader: a receipt cell quoting `-->` is prose — so the round-11 same-line raw exception is removed and its ONE grader flips to the masked verdict (`span-closer-later.md`, [3] to [3, 5]) with two NEW graders beside it; the closing line's tail is live at its own offset; graders the span closer, the tail row, the look-ahead span; battery R14-1, R14-2 keyed on the I4 test, R6-8 re-pointed, R11-5/R12-2 dropped with the deleted line) |
| U8 | B14-S2 | T15's eight-anchor list includes R10-2, a convergence mutant | receipt | REFUTED (round 12 re-pointed eight anchors, R10-2 among them for tail A's text — T15 never said all eight were comment-model) |
| U9 | B14-O4 | a seat's earlier run left `__pycache__` in the pin's `tests/` | machinery | RECORDED — machinery (the round-15 brief says: never run anything inside the pin; copy first) |

## Disposition ledger — round 15 (delta over 993524de)

11 rows by their leading verdict: 8 FIXED · 3 RECORDED (0 refuted). Six fresh seats (B15-O1 `_blank_quoted`'s closer exit · B15-O2 the reader trio and their docstrings · B15-O3 `_is_path_token` and the location suffix · B15-O4 the CALLERS of the four changed readers · B15-S1 the round-14 graders · B15-S2 the prose, the commit message and this receipt) over the pin `execB/pinB15/` (993524de, diff md5 a430eaa6ce314d4ac78d6827651cd480); every candidate executed by the orchestrator on a copy. The round's class is THE FIX SERVED NO CONSUMER: round 14 made the closing line's tail live to satisfy G29, but every consumer of that copy is a line-SHAPE classifier and the prose classes G29 was about read the RAW text — so the change could only fail open, and it did, in two shapes. Re-derived, pre- vs post-round-14, on copies: with the tail blanked whole the prose class reports it (`stale-phrase` line 4) AND the table classes grade (`dual-verdict` line 7); with the tail live a `|` tail returns `hits=[] ungraded=3` and a `~~~` tail returns `hits=[] ungraded=0` — silent in both directions.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| V1 | B15-O4 | the round-14 live tail joins the pipe run of the table below it — no header pair forms, every row reads `ungraded`, `dual-verdict`/`raw-pipe`/`table-parity` grade nothing; a `~~~` tail opens a fence that blanks the rest of the file and `ungraded` reads 0 | the fix served no consumer (in-hunk) | FIXED r15 · LOCAL (the closing line belongs to the HTML block and is blanked WHOLE, with the comment STATE still read from the tail so a second opener there keeps the block open; the comment states the measurement and names the prose classes as G29's real answer; grader `test_a_comment_closing_line_never_joins_the_table_or_fence_run_below_it` over all three tail shapes plus the re-opener row, red-on-revert `[] == [('dual-verdict', 7)]`; battery R15-1, R15-2 replacing R14-1) |
| V2 | B15-O1 | `find` vs `rfind` on the closing line's closer is ungraded — the mutant survives the whole suite | a grader that pins the fixture, not the property | FIXED r15 · by the class (with the line blanked whole, `j` feeds only the state read and the two are behaviourally identical — the ungraded property no longer exists; re-derived on the fixed copy) |
| V3 | B15-O2 | `_comment_cuts`'s docstring says "the one deliberate raw read is `_until_heading`'s heading-line prefilter" — the neutralisation beside it (`ln[opener:].replace(...)`) is a second raw read | prose overclaim (in-hunk) | FIXED r15 · doc (the docstring names BOTH raw reads and why the second is broad — a second opener left standing would re-open the block) |
| V4 | B15-O3 | the round-14 docstring deleted `a version,` and left its clause verbless: the file no longer states that `1.2.3`/`10.99.0.1` are not paths, while the grader added in the same commit cites the docstring for exactly that verdict | the re-cut loses the predicate the old cut carried (in-hunk) | FIXED r15 · doc (the verdict restored) |
| V5 | B15-O3 | "…are paths **by the dotted-symbol cost**" names a mechanism that cannot decide them — `1.2.3` carries a dot too; the predicate is the `\d+(\.\d+)*` fullmatch, under which `1.` and `.5` are also paths | state the mechanism, not the shapes (in-hunk prose) | FIXED r15 · doc (the fullmatch stated as the whole rule, `1.`/`.5` named as its cost; the behaviour is pre-existing and unchanged) |
| V6 | B15-S2 | the Pass 14 row says "three anchors re-pointed to the raw look-ahead" — `msg14.txt` and the battery record ONE (R6-8), and round 14 made that look-ahead MASKED, not raw | receipt (count + direction) | FIXED r15 · doc (re-derived from `battery.py.keep` and the commit message) |
| V7 | B15-S2 | U7's "its three graders flip to the masked verdicts" — the diff removes exactly ONE existing assertion; the other two named graders are NEW | receipt (prose overclaim) | FIXED r15 · doc (one flips, two are new) |
| V8 | B15-S2 | the RESUME block's series carries `25` for round 3, which the Pass 3 row's own method cell says was superseded by the re-derived `20` | receipt (a stale count beside its own correction) | FIXED r15 · doc (the series now reads the ledger's counts) |
| V9 | B15-O2 | the neutralisation grows the line by one character per occurrence | one hop | RECORDED — measured (inert: no consumer of that copy reads a column — `_blank_quoted` promises same-length against its OWN input, the prefilters test a prefix, the key reads `lines[i]`; a comment says so. Backlog row at Finish) |
| V10 | B15-O3 | the round-14 COMMIT MESSAGE calls `db.example.com:5432` "the box's own shape" — it is an RFC-2606 documentation domain, and the box's real host:port forms decide the other way | commit message (immutable) | RECORDED — by design (J27: a landed message is not rewritten; the docstring itself makes no such claim) |
| V11 | B15-O4 | `briefB15.md`'s probe recipe does not name the `Hit` fields, and the slice brief's "run the fd87d358 file" reads as if a checkout were wanted | machinery | RECORDED — machinery (the round-16 brief names the `Hit` fields and says: redirect `git show` into your own dir, never a checkout) |

## Disposition ledger — round 16 (delta over 66a02836)

12 rows by their leading verdict: 6 FIXED · 2 REFUTED · 4 RECORDED. Four fresh seats (B16-O1 the class change and its callers · B16-O2 the two rewritten docstrings · B16-S1 the round-15 graders · B16-S2 the prose, the commit message and this receipt) over the pin `execB/pinB16/` (66a02836, diff md5 84bf8bbfb38c6dadf2f770e6a263fea9); the partition was sized to the fix (80 changed lines over 3 files → opus=2,sonnet=2, not the round-1 six). Every candidate executed by the orchestrator on a copy. The round's class is A CLAIM TRUE ON ONE PATH STATED AS TRUE: round 15's own comment said the prose classes answer G29 — they do, but only on the SURFACE path, and the gate's self-selection passes receipts.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| W1 | B16-O1 | the round-15 comment says the prose classes report the tail — on the RECEIPT path (what the gate self-selects) `_receipt_hits` runs the two table classes and nothing else, so they do not | a claim true on one path stated as true (in-hunk) | FIXED r16 · doc + LOCAL (the cost stated in the comment; grader `scan(receipts=[f]).hits == []` beside the surface assertion; executed both paths: receipt 0 hits, surface 2) |
| W2 | B16-O2 | round 15's `_comment_cuts` docstring says the two raw reads are "both on the line it is about to neutralise" — the heading prefilter reads `_blank_quoted`'s BLANKED copy, and the compare reads the raw line through the mask | prose that overclaims its code (in-hunk) | FIXED r16 · doc (the sentence names the two copies and the third, masked read; grader `_until_heading("intro\n<!-- open\n## Pass Ledger\n-->\nafter\n", "Pass Ledger") == (text, 0)`, red on re-pointing the prefilter at `quoted`; battery R16-1) |
| W3 | B16-O2 | "that fullmatch is the whole rule" — the `a..b` range rule two lines below refuses `1..2` first, so the fullmatch is not the whole rule | the re-cut overstates its own boundary (in-hunk) | FIXED r16 · doc (the sentence names the boundary: the fullmatch decides every dotted number the range rule has not already refused) |
| W4 | B16-O2 | the cost list names `1.` and `.5` — `1.2.`, `.1.2`, `1.2e3` and a bare `.` are the same class and unnamed | a cost list missing real shapes (in-hunk) | FIXED r16 · doc (all six named; executed: every one is a path, `1..2`/`1..`/`a..b` are not) |
| W5 | B16-O1 + B16-S1 | the round-15 grader's third tail (`ok`) passes on the round-14 code too — a guard, not a discriminator, and its docstring did not say so | a grader whose kind is unstated (in-hunk) | FIXED r16 · doc (the docstring says which of the three is the guard — N19's convention) |
| W6 | B16-S1 | a fence marker on the closing line is no longer tracked, so a later stray `<!--` blanks the rest of the file — claimed a regression of round 15 | the fix served no consumer (claimed) | REFUTED (executed: the SAME input with no enclosing comment at all — `# R\n\nexample with <!-- stray…\n` + the table — blanks identically, `hits=[] ungraded=0`. The mechanism is the pre-existing "an unclosed opener blanks to EOF"; round 14 masked it in this one shape by letting its padded tail open a fence, which is V1. Not created by the hunk — RECORDED-SHAPE below) |
| W7 | B16-O1 | `_fence_step` opens a fence at ANY indent (`    ~~~`, `\t~~~` → True); CommonMark allows at most 3 spaces, and `_until_heading:777` already applies the 4-space rule on the other side | one reading for two sides (one hop) | RECORDED — measured (executed, all four indents open; it is also why round 14's 10-space `~~~` tail opened a fence. Backlog row at Finish beside L29/M16/Q12, with the copied-with-attribution sibling `check_command_corpus.py::_fence_step` to keep in step) |
| W8 | B16-S1 | `_comment_cuts(ln[j:])` instead of `ln[j + 3 :]` survives every grader | a possible coverage gap | REFUTED (equivalent mutant: only `[1]` is consumed and the literal `-->` can neither start nor extend a `<!--` match, so the boolean cannot differ — the seat named it inert itself) |
| W9 | B16-S2 | the round-16 brief still pointed at `msg14.txt` for a delta whose message is `msg15.txt` | machinery (S7's recurring shape) | RECORDED — machinery (FIXED in `briefB15.template.md`: the pointer is now a `__MSG__` placeholder, so it cannot be carried stale) |
| W10 | B16-S2 | the brief said "the 141-mutant battery (140 listed plus M21) … 140 of 141 killed" while the authority line says `140 mutants, killed 140` — a second number beside the one the brief calls authoritative | denominator (machinery) | RECORDED — machinery (FIXED in the template: ONE `__NMUT__` placeholder, the parenthetical now forbids a second number outright) |
| W11 | B16-S2 | the RESUME block's "every RECORDED row with its backlog destination: F7, F32, …" was a hand-list that had gone stale (V9–V11 absent) | a hand-kept list beside its generated source | FIXED r16 · doc (§ Residual is now generated from the ledgers — 108 rows — and the RESUME line says so instead of restating it) |
| W12 | orchestrator | a review record the operator PAUSES ages past the 12h stale bound, and `_review_window` then returns None for a `running` record — every edit made under that live run reads as UNREVIEWED SPONTANEOUS WORK | machinery (the hooks surface) | RECORDED — measured (this record: 24.1h old, `state: running`, round-15's committed review-fix work flagged. The bound is meant to fail OPEN; here staleness REMOVES a window and thereby ARMS the sixth cause. Written up at `execB/machinery-stale-window.md`; destination: fabrik-mail to the infra beat at the close) |

## Disposition ledger — round 17 (delta over 07bc448b)

9 rows by their leading verdict: 5 FIXED · 0 REFUTED · 4 RECORDED. Three fresh seats (B17-O1 the two hygiene prose hunks · B17-O2 the coverage docstring · B17-S1 the round-16 graders) over the pin `execB/pinB17/` (07bc448b, diff md5 8c335f43271a1766da3fae1d51d3028c); the partition was sized to the fix again (40 changed lines, opus=2,sonnet=1). The round's class is THE PROSE PROMOTED A BEHAVIOUR TO CONTRACT AND NOTHING GRADED IT: round 16's docstring named six token shapes as stated costs; two mutants that flip all of them survived the whole suite. The round's fixes are the graders that prose owed.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| X1 | B17-O2 | the round-16 sentence names six stated costs and the range rule's rationale; NOTHING grades any of them — widening the fullmatch to `\d*(?:\.\d*)*` flips all six and deleting the range arm flips all three, and the suite passed under both | the prose promoted a behaviour to contract and nothing graded it (in-hunk) | FIXED r17 · LOCAL (graders for all six costs and the three range-refused shapes plus the bare count, in the file's own `_run_on` shape; both mutants re-run on a copy and now RED by name; battery R17-1, R17-2) |
| X2 | B17-O2 | "every dotted number the fullmatch does not match" is a universal its own parenthetical refutes — `1..2`, `.1..2`, `1..2.3` do not match it and are NOT paths, because the range rule refuses them first | a claim true on one subset stated as true (in-hunk) | FIXED r17 · doc (the carve-out is carried into the clause it qualifies; executed: all four `..` shapes decided by the range rule) |
| X3 | B17-O1 | the round-16 STATED COST says a `--receipt`-only file "runs the two TABLE classes and nothing else" — `dead-symbol` grades it too, over the same text | a claim true on one path stated as true (in-hunk, the round-16 class recurring one level up) | FIXED r17 · doc + LOCAL (the cost now names the CLASSES, not the path; graders both ways — a missing symbol fires `dead-symbol` on a receipt, a present one does not) |
| X4 | B17-O1 | the docstring says the prefilter reads "`_blank_quoted`'s BLANKED copy" — it reads the blanked copy of the NEUTRALISED lines, and the distinction is load-bearing (blanking the raw lines would swallow the file from any unterminated opener to EOF) | a copy named one level short (in-hunk) | FIXED r17 · doc (the copy named exactly, with the reason the neutralisation runs first) |
| X5 | B17-O2 | "a bare count" sits in the fullmatch's parenthetical; `44` is dropped two rules earlier by no-dot-no-slash | a rule credited to the wrong arm (in-hunk) | FIXED r17 · doc (re-attributed; executed rule-replay per token) |
| X6 | B17-O1 | `--phrase` with only `--receipt` is silently inert — no hit, no note, a clean green — while the sibling flag `--claim` without `--surface` is REFUSED aloud | fail-open asymmetry between two flags of one shape (one hop, pre-existing) | RECORDED — measured (executed both: `--receipt --phrase` rc 0 and silent, `--receipt --claim` prints `REFUSED — --claim needs --surface`. Backlog row at Finish beside L6/P12: refuse or note `--phrase` with no `--surface`) |
| X7 | B17-S1 + B17-O2 | the brief's per-suite baselines were stale in two of four rows — one of them stale because the PREVIOUS round added a test | machinery (a hard-coded count in a reusable template) | RECORDED — machinery (FIXED at the source: the template no longer carries counts; it tells the seat to establish and report its own baseline, the only form that cannot drift) |
| X8 | B17-O1 | the brief's probe recipe does not carry the hub's own chain rule, and a seat lost a mutant run to a `&&` chain short-circuiting on a legitimate `grep -c 0` | machinery | RECORDED — machinery (the round-18 brief carries it: chain probes with `;` and an explicit rc read, never `&&`) |
| X9 | B17-O1 | the probe recipe's `cp -r` carries no `--exclude='__pycache__'` where the battery's tarball mandates one | machinery | RECORDED — machinery (the pin is bytecode-free by construction — round 14's U9 — so the copy is clean; the round-18 brief adds the exclude anyway) |

## Disposition ledger — round 18 (delta over 07bc448b..1bb9a84f — the D10 rule-3 class rewrite)

9 rows by their leading verdict: 7 FIXED · 0 REFUTED · 2 RECORDED. Two fresh Opus seats (B18-O1 the rule ladder vs the branches it claims to describe · B18-O2 the compressed closer comment and the round-17 graders) over the pin `execB/pinB18/` (1bb9a84f, diff md5 52b16840cfc9ba5a42fb0dec62510e25). The brief asked a class rewrite's one question — WHAT DID IT LOSE — and both seats answered it with a denominator: of the 31 distinct example tokens the old `_is_path_token` docstring carried, 9 are kept, 17 are covered by the executed table, 4 by a ladder rule, and ONE clause was genuinely lost (rule 5's `"/" not in tok` guard), which had also been ungraded the whole time. Every candidate executed by the orchestrator on a copy.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| Y1 | B18-O1 | rule 6's gloss says rules 4 and 5 "have already taken the undotted and the `..` shapes" — both are guarded by `"/" not in tok`, so `../x.py`, `a..b/c`, `1..2/x` fall past them and ARE paths; the old docstring's "with no slash" was dropped in the rewrite | the re-cut loses a shape the old cut had (in-hunk) | FIXED r18 · doc + LOCAL (the qualifier restored as its own paragraph; graders for the three slash-carrying shapes; executed — all three decided by rule 7) |
| Y2 | B18-O1 | deleting rule 5's no-slash guard SURVIVED the whole suite — the range rule was graded only on slashless tokens | the prose promoted a behaviour to contract and nothing graded it | FIXED r18 · LOCAL (both no-slash guards now graded: each mutant re-run on a copy and RED by name) |
| Y3 | B18-O1 | the pointer promised "EVERY other cost … is enumerated" in the test — an absolute the table does not carry | prose that overclaims its code (in-hunk) | FIXED r18 · doc (the pointer now states what the table IS — the place a newly discovered cost is recorded — instead of a universal) |
| Y4 | B18-O2 | the round-17 comment says "the three the RANGE rule refuses first" and "flips all three"; the loop three lines below iterates FOUR | a count stated without its denominator (in-hunk) | FIXED r18 · doc (four, re-derived from the loop itself) |
| Y5 | B18-O2 | "widening the fullmatch … (flips all six)" — it flips FIVE; `1.2e3`'s `e` defeats the widened pattern too, so that row is ungraded by the only mutant named for it | a grader whose named mutant does not reach it (in-hunk) | FIXED r18 · doc (five of six, with the exception named; executed per token) |
| Y6 | B18-O2 | the comment claimed "Both sides graded in `test_a_comment_closing_line_never_joins_…`" — the table/fence loop does NOT discriminate the whole-line blank (all three tails stay green under `out.append(ln)`); the test reds at its `_until_heading` assertion instead | a pointer that names the wrong grader (in-hunk) | FIXED r18 · doc + LOCAL (the comment names which assertion pins what. The row added to close it was itself measured NON-discriminating — an un-blanked closing line still begins with the text before `-->`, so it forms no header and opens no fence — and is kept and LABELLED a guard, per N19, rather than shipped as a discriminator) |
| Y7 | B18-O2 | the compression dropped "which is what the gate's own self-selection passes" — the clause that makes the receipt-path cost matter, since it is the DEFAULT gate path and not a hand-typed invocation; stated nowhere afterwards (`command grep` → 0 in the whole source tree) | WHAT THE REWRITE LOST (in-hunk) | FIXED r18 · doc (restored, with the `self_selected` branch named as its anchor) |
| Y8 | B18-O2 | `briefB18.md`'s probe recipe mandates `cp -r --exclude='__pycache__'`; GNU `cp` has no `--exclude` and the copy silently does not happen | machinery | RECORDED — machinery (FIXED at the source: the template now carries the `tar` form and says why `cp -r --exclude` is a silent no-op) |
| Y9 | B18-O2 | `--symbol` counts with `str.count`, i.e. case-SENSITIVE, while `_term_sites` is case-insensitive | one hop, pre-existing | RECORDED — measured (`check_review_hygiene.py` symbol count; backlog row at Finish) |

Beside the round's own findings, the completion gate's read-only dry run came back RED on the repo-wide lint ratchet — an unused `import subprocess as sp` left by round 7 in `tests/test_check_convergence.py`. Removed (113 tests still pass, `ruff check .` clean repo-wide); the gate then read `status: success`, 62 passed, 0 failed.

## Disposition ledger — round 19 (delta over 1bb9a84f..a6ace29f — the claim-surface trim)

6 rows by their leading verdict: 4 FIXED · 0 REFUTED · 2 RECORDED. Two fresh Opus seats (B19-O1 the coverage ladder, its graders and what the trim left · B19-O2 the hygiene closer branch, its comment and its graders) over the pin `execB/pinB19/` (a6ace29f, diff md5 03760ae6d4d46aad80240d5570cd1f89). The brief asked the seats to test a CONVERGENCE CLAIM, and B19-O2 answered it directly: "the round-18 trim worked — every surviving sentence of the branch comment matched the code under execution, nothing true and load-bearing was lost in the rewrite." Every candidate this round is prose precision inside the comments the previous rounds ADDED to explain their own findings, and none is falsified by any run of the checker. The disposition is therefore not another correction but a DELETION: the explanatory claims are removed, the predicates kept.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| Z1 | B19-O1 | the round-18 comment says "Deleting either guard flips these three" — rule 4's guard deletion flips NONE of the loop's tokens (all carry a dot) and is killed 28 lines up by the round-13 `a/b:80` assertion; "invisible to every grader before this loop existed" is false for it | a claim credited to the wrong grader (in-hunk) | FIXED r19 · doc (executed both guard mutants: rule 4's reds at `:278`, rule 5's at `:311`. The credit claim is DELETED rather than corrected — the loop and the older assertion each speak for themselves) |
| Z2 | B19-O1 | the ladder names `1..2/x` as an example, but the executed table it points at does not carry it — the hunk's own contract says the table is where a cost is recorded | prose-only example beside a table contract (in-hunk) | FIXED r19 · LOCAL (`1..2/x` added to the loop; it is also the only one of the three that exercises the rule-6 arm on a slashed numeric token) |
| Z3 | B19-O2 | the round-18 label "a GUARD, not a discriminator … it cannot discriminate the whole-line blank" is true for `out.append(ln)` and FALSE for the sibling mutant that blanks the line but keeps the tail, under which the row flips to `[('dual-verdict', 6)]` | a claim true for one mutant stated as a universal (in-hunk) | FIXED r19 · doc (re-executed both mutants and confirmed the seat; the label is DELETED — the row now states only what it asserts, and no comment in this test claims what any mutant does) |
| Z4 | B19-O2 | the appositive equates "receipt-only" with the gate's self-selection, but `self_selected` is false whenever `--symbol` is given, so the `--symbol` parenthetical is unreachable on the path the appositive names | a claim true on one path stated as true (in-hunk) | FIXED r19 · doc (the unreachable parenthetical and the over-equation removed; the sentence now states the default path as a fact about `main`, with no claim about flags it cannot carry) |
| Z5 | B19-O1 | battery anchor R18-1 targets the whole test node, so it is satisfied by an assertion 28 lines from the loop it is named for — a node-level battery cannot detect a mis-credited grader | machinery (one hop) | RECORDED — measured (the anchor still kills its mutant; only its NAME implied a location. Backlog row at Finish: battery anchor titles should not imply which assertion kills them) |
| Z6 | B19-O2 | the brief's 10-minute box and its "build the mutant for every row" instruction are in tension at this surface size | machinery | RECORDED — machinery (the seat ran three cycles and named the remainder under CLEAN rather than guessing, which is the contract working; the round-20 brief asks for the mutants that decide the round's own claims, not every row) |

## Disposition ledger — round 20 (delta over 76673ee9 — 20 lines, all deletions)

4 rows by their leading verdict: 3 FIXED · 0 REFUTED · 1 RECORDED. ONE fresh Opus seat plus the hygiene script — the fragment's delta budget at 20 changed lines (D-229); the script returned 0 hits over both changed sources. The round's class is THE DELETION OVER-DELETED: round 19 removed the explanatory claims, and two of them were TRUE and load-bearing. The seat found both by execution, and one of them was a live CROSS-FILE contradiction — the grader's own comment, written in round 17 and never touched since, still asserted the qualifier round 19 had just deleted.

| # | Seat | Where | Class | Disposition |
|---|---|---|---|---|
| AA1 | B20-O1 | round 19 deleted "(`--symbol` still counts over the text)" and rewrote the cost as "no prose class at all, so the tail's prose is unreported" — but `scan` stores the RAW text for receipts and the symbol loop counts over it, so a symbol whose only mention is on the tail is NOT dead; the grader's comment at `test_check_review_hygiene.py:1352-1353` still says exactly that | the deletion over-deleted (in-hunk) · a live cross-file contradiction | FIXED r20 · doc (executed both ways: `--receipt … --symbol TAILWORD` → 0 hits, `--symbol ABSENTWORD` → `dead-symbol`. The cost now names the PROSE classes it is about and states that `--symbol` reads the raw text, which is what the existing grader pins) |
| AA2 | B20-O1 | the replacement sentence says `main` self-selects "when no surface, phrase or symbol is given" — the predicate is a FOUR-way disjunction, `not (surfaces or receipts or args.phrase or args.symbol)`, so `--receipt` suppresses it too; executed counterexample: a named receipt scans 1 file where self-selection scans 0 | a sufficient condition the code refutes (in-hunk) | FIXED r20 · doc + LOCAL (all four selectors named; and the missing arm is now graded — `test_no_argument_mode_self_selects_a_changed_receipt` asserts a named receipt is scanned and the self-selected one is not, proven red by dropping `receipts` from the disjunction) |
| AA3 | B20-O1 | the round-20 brief states the battery count twice in one parenthetical — the `__NMUT__` placeholder was substituted into both halves of the anti-second-number clause | machinery (the template fix applied twice) | RECORDED — machinery (the template's clause is collapsed for the next round; the count itself was correct in all three sources) |
| AA4 | B20-O1 | `-k 'not committed_receipts'` leaves 7 nodes, so the deselected half is the majority and is never exercised on a copy — a seat reading "7 passed" would overstate the suite's coverage | machinery (a bounded denominator) | RECORDED — measured (the deselect is required because those nodes read git; the round-21 brief states the bound beside the flag) |

## Pass Ledger

ONE table, one row per pass, counts punctuated (`found: F, new: N, confirmed: C, fixed: X,
unexecuted: U`) — the gate reads the LAST row as the exit round and refuses a second ledger group.
`found:` counts raw candidates, `new:` is prose the graders do not parse, `confirmed:` counts the
candidates EXECUTED and reproduced (the exit counter — a round is quiet at `confirmed: 0` and
`fixed: 0` with `unexecuted:` 0 or absent), `unexecuted:` counts code candidates RECORDED
unexecuted. Minimum two passes; the fixing pass is never the last; the closing pass re-derives
every count and anchor and says so in its Method cell, and its Finders cell names the seats that
read it by model token (`opus×1`, `sonnet×2`) — a round the orchestrator alone read cannot close.

| Pass | Finders | Counters | Method |
|---|---|---|---|
| Pass 1 | native opus×4 + sonnet×3 (dispatched 7, returned 7 — B-O1 command_run.py · B-O2 check_convergence.py + check_review_coverage.py · B-O3 check_plan_tickets.py + check_ticket_breadth.py + check_hooks_index.py + check_doc_index.py · B-O4 check_review_hygiene.py + final_gate.py + review_rubric.py · B-S1 the four command_run/convergence/coverage test files · B-S2 the nine enforcement/rubric/gate test files · B-S3 term-edit.md + FINAL_GATE_WORKFLOW.md + plan↔code parity) | found: 55, new: 55, confirmed: 43, fixed: 43, unexecuted: 0 | method: citation — every candidate executed by the orchestrator on a copy of the pin (702b4c2f, diff md5 1f4d05aa28cf5cb538fbcd8f5eecd074) or a live regex/suite run; fixes committed bb995e60; hygiene at start and close: 4 hits, all F55; round-zero probe — 32 mutants of the fixes killed by the named test on a copy (`execB/battery.out`, three first-run survivors were test-side and fixed) |
| Pass 2 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring — B2-O1 command_run.py · B2-O2 check_convergence.py + check_review_coverage.py · B2-O3 check_plan_tickets.py + check_hooks_index.py + check_doc_index.py + the ticket_breadth readers · B2-O4 check_review_hygiene.py + review_rubric.py · B2-S1 the round-1 tests · B2-S2 the doc and comment mirrors) | found: 38, new: 38, confirmed: 28, fixed: 28, unexecuted: 0 | method: citation — DELTA over bb995e60's 24-file fix diff plus one hop (972 changed lines → the round-1 partition, D-229); every candidate executed by the orchestrator on a copy of the pin (bb995e60, diff md5 8d89f8220908eeeb2ca2fd2451e7ffe5); round 1's REFUTED and RECORDED rows carried verbatim, none re-raised; hygiene at start and close: the F55 pair only; round-zero probe — the battery re-run whole with round 2's 27 mutants added (`execB/battery.out`) |
| Pass 3 | native opus×4 + sonnet×2 (dispatched 6, returned 6 — two after a re-dispatch on API timeouts; all fresh non-authoring: B3-O1 command_run.py · B3-O2 check_convergence.py + check_review_coverage.py · B3-O3 check_plan_tickets.py + check_hooks_index.py + check_doc_index.py · B3-O4 check_review_hygiene.py + review_rubric.py · B3-S1 the round-2 tests · B3-S2 the doc and comment mirrors) | found: 30, new: 30, confirmed: 20, fixed: 20, unexecuted: 0 | method: citation — DELTA over 7cb15def + 346e8a40 (the record's round 3 carries `--confirmed 25`, written before the rows were re-derived — 20 is the ledger's count) (1083 changed lines → the round-1 partition, D-229); every candidate executed by the orchestrator on a copy of the pin (346e8a40, diff md5 2aba4a436b97fe32855063f9eafb0551); one candidate REFUTED by two pre-existing graders that pin the plain refusal as the whole stdout; hygiene at start and close: the F55 pair only; round-zero probe — the battery extended with round 3's 20 mutants and re-run whole (`execB/battery.out`) |
| Pass 4 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B4-O1 command_run.py · B4-O2 check_convergence.py + check_review_coverage.py · B4-O3 check_plan_tickets.py + check_hooks_index.py + check_doc_index.py · B4-O4 check_review_hygiene.py + review_rubric.py · B4-S1 the round-3 tests and the conftest pin · B4-S2 the mirrors, the commit message and this receipt) | found: 31, new: 31, confirmed: 24, fixed: 24, unexecuted: 0 | method: citation — DELTA over 3d975da7 (470 changed lines → the round-1 partition, D-229), pinned PER COMMIT (H24); every candidate executed by the orchestrator on a copy of the pin (3d975da7, diff md5 f4d01bad5ee1af145691ee69c5a25fea); hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 4's 9 mutants and re-run whole (`execB/battery.out`) |
| Pass 5 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B5-O1 command_run.py · B5-O2 check_convergence.py + check_review_coverage.py · B5-O3 check_plan_tickets.py · B5-O4 check_review_hygiene.py + review_rubric.py · B5-S1 the round-4 tests and the conftest pin · B5-S2 the mirrors, the commit message and this receipt) | found: 26, new: 26, confirmed: 17, fixed: 17, unexecuted: 0 | method: citation — DELTA over e50c22a9 (297 changed lines → the round-1 partition, D-229), pinned per commit; every candidate executed by the orchestrator on a copy of the pin (e50c22a9, diff md5 c1d9562c11c2a48a70b3baa95eb3bdce); hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 5's 14 mutants and re-run whole (`execB/battery.out`) |
| Pass 6 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B6-O1 command_run.py · B6-O2 check_convergence.py + check_review_coverage.py · B6-O3 check_plan_tickets.py · B6-O4 check_review_hygiene.py + review_rubric.py · B6-S1 the round-5 tests and the conftest pin · B6-S2 the mirrors, the commit message and this receipt) | found: 37, new: 37, confirmed: 20, fixed: 20, unexecuted: 0 | method: citation — DELTA over 8a87fce1 (197 changed lines → the round-1 partition, D-229), pinned per commit; every candidate executed by the orchestrator on a copy of the pin (8a87fce1, diff md5 83766fb7b3f48ad126f21e4a35d3bd11); hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 6's 16 mutants, seven superseded anchors re-pointed, and re-run whole on a fresh pycache-free copy: 97 of 97 killed (`execB/battery.out`; the previous file was a stale run — L16) |
| Pass 7 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B7-O1 command_run.py · B7-O2 check_convergence.py + check_review_coverage.py · B7-O3 check_plan_tickets.py · B7-O4 check_review_hygiene.py + review_rubric.py · B7-S1 the round-6 tests · B7-S2 the mirrors, the commit message and this receipt) | found: 22, new: 22, confirmed: 13, fixed: 13, unexecuted: 0 | method: citation — DELTA over f6541b4e (235 changed lines → the round-1 partition, D-229), pinned per commit; every candidate executed by the orchestrator on a copy of the pin (f6541b4e, diff md5 3c4358ce4773561c71c6362157746acf); the two quoted counts re-derived by the orchestrator before they were written back (M20, M21); hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 7's 10 mutants (one equivalent mutant dropped), nine earlier anchors re-pointed — seven round-6 ones plus R3-16 and R4-1; the commit message's "six" was written before the last three (round 8, N8), re-run whole on a fresh pycache-free copy: 107 of 107 killed (`execB/battery.out`) |
| Pass 8 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B8-O1 command_run.py · B8-O2 check_convergence.py + check_review_coverage.py · B8-O3 check_plan_tickets.py · B8-O4 check_review_hygiene.py · B8-S1 the round-7 tests · B8-S2 the comments, the commit message and this receipt) | found: 20, new: 20, confirmed: 10, fixed: 10, unexecuted: 0 | method: citation — DELTA over f8d9e478 (173 changed lines → the round-1 partition, D-229), pinned per commit; every candidate executed by the orchestrator on a copy of the pin (f8d9e478, diff md5 5543c7663a006bd23265b891a59751ec); hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 8's 6 mutants, nine anchors re-pointed, two superseded anchors dropped (R6-9, R5-8 — both folded into the `_heading_key` graders; round 9, P8), re-run whole on a fresh pycache-free copy: 111 of 111 killed — 110 listed plus the appended M21 (`execB/battery.out`) |
| Pass 9 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B9-O1 command_run.py · B9-O2 check_convergence.py · B9-O3 check_review_coverage.py · B9-O4 check_review_hygiene.py · B9-S1 the round-8 tests · B9-S2 the comments, the commit message and this receipt) | found: 14, new: 14, confirmed: 7, fixed: 7, unexecuted: 0 | method: citation — DELTA over 4fe50105 (102 changed lines → the round-1 partition, D-229), pinned per commit; every candidate executed by the orchestrator on a copy of the pin (4fe50105, diff md5 54a8e3dcb91c860e10a33b98ff154e7c); hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 9's 6 mutants and re-run whole on a fresh pycache-free copy: 116 of 116 (`execB/battery.out`). Round 9 landed as TWO commits: 7dad5031, committed by pathspec UNGATED when the fleet-quota HOLD refused every probe mid-close (its body says so), and 2d93ad84 after the relief wake — the two remaining hygiene graders, the one-closing-run docstring, the gates run green (round 10, Q15) |
| Pass 10 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B10-O1 command_run.py · B10-O2 check_convergence.py · B10-O3 check_review_coverage.py · B10-O4 check_review_hygiene.py · B10-S1 the round-9 tests · B10-S2 the comments, both commit messages and this receipt) | found: 16, new: 16, confirmed: 9, fixed: 9, unexecuted: 0 | method: citation — DELTA over 4fe50105 (the two round-9 commits together, 91 changed lines over 8 files, sibling commits in the range excluded by pathspec → the round-1 partition, D-229), pinned at 2d93ad84 (diff md5 18cd01d84df9f051ba433dd7c56213ef); every candidate executed by the orchestrator on a copy; hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 10's 3 mutants, four anchors re-pointed, re-run whole on a fresh pycache-free copy (`execB/battery.out`) |
| Pass 11 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B11-O1 command_run.py · B11-O2 check_convergence.py · B11-O3 check_review_coverage.py · B11-O4 check_review_hygiene.py · B11-S1 the round-10 tests · B11-S2 the comments, the commit message and this receipt) | found: 14, new: 14, confirmed: 9, fixed: 9, unexecuted: 0 | method: citation — DELTA over f1905d50 (88 changed lines over 8 files → the round-1 partition, D-229), pinned per commit (diff md5 fc4ba37c7304ae7af081bc1166334bfd); every candidate executed by the orchestrator on a copy; hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 11's 7 mutants, six anchors re-pointed, one superseded entry dropped, re-run whole on a fresh pycache-free copy (`execB/battery.out`) |
| Pass 12 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B12-O1 command_run.py · B12-O2 check_convergence.py · B12-O3 the coverage grader · B12-O4 check_review_hygiene.py · B12-S1 the round-11 tests · B12-S2 the comments, the commit message and this receipt) | found: 12, new: 12, confirmed: 7, fixed: 7, unexecuted: 0 | method: citation — DELTA over 17f7ec90 (55 changed lines over 7 files → the round-1 partition, D-229), pinned per commit (diff md5 8f16af2392b684049fdc1e327cc8e2dc); every candidate executed by the orchestrator on a copy; hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 12's 5 mutants, eight anchors re-pointed, re-run whole on a fresh pycache-free copy (`execB/battery.out`) |
| Pass 13 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B13-O1 command_run.py · B13-O2 check_convergence.py · B13-O3 the coverage docstring · B13-O4 check_review_hygiene.py · B13-S1 the round-12 tests · B13-S2 the comments, the commit message and this receipt) | found: 15, new: 15, confirmed: 10, fixed: 10, unexecuted: 0 | method: citation — DELTA over 15436a3f (69 changed lines over 7 files → the round-1 partition, D-229), pinned per commit (diff md5 068c53d746c1e3b644cedbd6a47ee94c); every candidate executed by the orchestrator on a copy; hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 13's 7 mutants, the comment-model anchors re-pointed to `_comment_cuts`, re-run whole on a fresh pycache-free copy (`execB/battery.out`); D10 rule 3 applied: the two residue spots rewritten as classes |
| Pass 14 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B14-O1 the head table · B14-O2 the coverage docstring and the advisory comment · B14-O3 `_comment_cuts` · B14-O4 `_until_heading` · B14-S1 the round-13 tests · B14-S2 the docstrings, the commit message and this receipt) | found: 9, new: 9, confirmed: 5, fixed: 5, unexecuted: 0 | method: citation — DELTA over fd87d358 (150 changed lines over 7 files → the round-1 partition, D-229), pinned per commit (diff md5 6883f6df1c7437c2804d04bdfeaf9c87); every candidate executed by the orchestrator on a copy; hygiene over the receipt at close: 0 hits; round-zero probe — the battery extended with round 14's 4 mutants, ONE pre-existing anchor re-pointed (R6-8, to the MASKED look-ahead), R11-5/R12-2 dropped with the deleted line, re-run whole on a fresh pycache-free copy (`execB/battery.out`) |
| Pass 15 | native opus×4 + sonnet×2 (dispatched 6, returned 6, all fresh non-authoring: B15-O1 `_blank_quoted`'s closer exit · B15-O2 the reader trio and their docstrings · B15-O3 `_is_path_token` · B15-O4 the CALLERS of the four changed readers · B15-S1 the round-14 graders · B15-S2 the prose, the commit message and this receipt) | found: 11, new: 11, confirmed: 8, fixed: 8, unexecuted: 0 | method: citation — DELTA over 993524de (98 changed lines over 4 files → the round-1 partition, D-229), pinned per commit (diff md5 a430eaa6ce314d4ac78d6827651cd480, pin md5s in `execB/pinB15.md5`, verified byte-identical after the round); every candidate executed by the orchestrator on a copy, pre- AND post-round-14, so the round's class (the fix served no consumer) is a measured comparison and not a reading; the new grader proven red-on-revert against 993524de (`[] == [('dual-verdict', 7)]`); hygiene over the receipt at close: 0 hits; round-zero probe — the battery re-run whole on a fresh pycache-free copy of 993524de before the round (`execB/battery15.out`, 139 of 139 killed) and again after the fix with R14-1 replaced by R15-1/R15-2 (`execB/battery16.out`, 140 of 140 killed — R15-2 survived its first run keyed to the wrong node and was re-pointed to the test that owns the assertion, then killed); the gate selection is the battery's own test files plus `tests/enforcement/` — 1632 tests, 4 failed, 1628 passed, the red set exactly the four RECORDED reds (R1, R2) |
| Pass 16 | native opus×2 + sonnet×2 (dispatched 4, returned 4, all fresh non-authoring: B16-O1 the class change and its callers · B16-O2 the two rewritten docstrings · B16-S1 the round-15 graders · B16-S2 the prose, the commit message and this receipt) | found: 12, new: 12, confirmed: 6, fixed: 6, unexecuted: 0 | method: citation — DELTA over 66a02836 (80 changed lines over 3 files), the partition SIZED TO THE FIX (opus=2,sonnet=2 — not the round-1 six; D-229), pinned per commit (diff md5 84bf8bbfb38c6dadf2f770e6a263fea9, pin md5s in `execB/pinB16.md5`); every candidate executed by the orchestrator on a copy, including both REFUTATIONS (the claimed fence regression reproduced identically with no comment in the input at all, so the mechanism is pre-existing, not the hunk's); the new prefilter grader proven red by re-pointing it at the un-blanked copy; hygiene over the receipt at close: 0 hits; round-zero probe — the battery re-run whole on a fresh pycache-free copy with R16-1 added (`execB/battery17.out`, 141 of 141 killed — the file's own `battery:` line, the only count); the gate selection green with the red set exactly the four RECORDED reds (1628 passed, 4 failed) |
| Pass 17 | native opus×2 + sonnet×1 (dispatched 3, returned 3, all fresh non-authoring: B17-O1 the two hygiene prose hunks · B17-O2 the coverage docstring · B17-S1 the round-16 graders) | found: 9, new: 9, confirmed: 5, fixed: 5, unexecuted: 0 | method: citation — DELTA over 07bc448b (40 changed lines over 3 files), partition sized to the fix (opus=2,sonnet=1; D-229), pinned per commit (diff md5 8c335f43271a1766da3fae1d51d3028c, pin md5s in `execB/pinB17.md5`); every candidate executed by the orchestrator on a copy — the two SURVIVING mutants a seat reported were re-run here, and the graders added this round make both RED by name; hygiene over the receipt at close: 0 hits; round-zero probe — the battery re-run whole on a fresh pycache-free copy with R17-1/R17-2 added (`execB/battery18.out`, 143 of 143 killed — the file's own `battery:` line, the only count); the gate selection green with the red set exactly the four RECORDED reds (1628 passed, 4 failed) . CLASS ACTION (D10 rule 3 — rounds 15, 16 and 17 each found the PREVIOUS round's prose wrong, not its code): the drifting prose was REWRITTEN rather than patched a fourth time — `_is_path_token`'s 36-example docstring became the seven-rule ladder the code is, pointing at the executed token table for every other cost, and `_blank_quoted`'s closer comment became predicates with its narrative and round numbers deleted (commit 1bb9a84f; no behaviour change — 143 of 143 mutants still killed on a fresh copy, every anchor still hitting, gate selection unchanged) |
| Pass 18 | native opus×2 (dispatched 2, returned 2, both fresh non-authoring: B18-O1 the rule ladder vs its branches · B18-O2 the compressed closer comment and the round-17 graders) | found: 9, new: 9, confirmed: 7, fixed: 7, unexecuted: 0 | method: citation — DELTA over 07bc448b..1bb9a84f (the class rewrite, 84 changed lines over 2 files), partition sized to the fix (opus=2; D-229), pinned per commit (diff md5 52b16840cfc9ba5a42fb0dec62510e25, pin md5s in `execB/pinB18.md5`); the brief asked the one question a re-cut owes — WHAT DID IT LOSE — and the answer carries its denominator (31 example tokens in the old docstring: 9 kept, 17 in the executed table, 4 by a ladder rule, 1 clause LOST and also ungraded); every candidate executed by the orchestrator on a copy, including the grader I added to close Y6, which MEASURED non-discriminating and is labelled a guard rather than shipped as one; hygiene over the receipt at close: 0 hits; round-zero probe — the battery re-run whole on a fresh pycache-free copy (`execB/battery20.out`, 143 of 143 killed) and extended with R18-1/R18-2 for the two guards this round found unguarded; the gate selection green with the red set exactly the four RECORDED reds (1628 passed, 4 failed); `final_gate.py --check --json` → `status: success`, 62 passed, 0 failed, after clearing a repo-wide lint red left by round 7 . FOLLOW-ON (same round, orchestrator): rounds 15–18 confirmed 8 · 6 · 5 · 7 and EVERY finding was in the previous round's own fix prose on the same two files, never in the Phase B code — so the two shapes that generate those findings were removed from my hunks rather than corrected again: a COUNT written beside a loop (the loop is already its own denominator, and round 18 found two such counts wrong) and a POINTER naming which assertion inside a test grades what. The predicate and the stated cost stay; the file's own round-citing convention is left alone, predating this phase (commit a6ace29f) |
| Pass 19 | native opus×2 (dispatched 2, returned 2, both fresh non-authoring: B19-O1 the coverage ladder and its graders · B19-O2 the hygiene closer branch and its graders) | found: 6, new: 6, confirmed: 4, fixed: 4, unexecuted: 0 | method: citation — DELTA over 1bb9a84f..a6ace29f (the claim-surface trim, 51 changed lines), partition sized to the fix (opus=2), pinned per commit (diff md5 03760ae6d4d46aad80240d5570cd1f89, pin md5s in `execB/pinB19.md5`); the brief inverted the usual job and asked the seats to TEST A CONVERGENCE CLAIM — B19-O2's verdict was that the trim worked and nothing load-bearing was lost, and B19-O1's lost-clause diff found none either; every candidate executed by the orchestrator on a copy (both guard mutants re-run, red at `:278` and `:311`; the tail-keeping mutant re-run and the row confirmed to flip); all four fixes are DELETIONS of explanatory claims, not new prose; hygiene over the receipt at close: 0 hits; round-zero probe — the battery re-run whole on a fresh pycache-free copy (`execB/battery21.out`, 145 of 145 killed, no anchor miss); the gate selection green with the red set exactly the four RECORDED reds (1628 passed, 4 failed) |
| Pass 20 | native opus×1 + the hygiene script (the delta budget at 20 changed lines, D-229; dispatched 1, returned 1, fresh non-authoring: B20-O1 the whole delta) | found: 4, new: 4, confirmed: 2, fixed: 2, unexecuted: 0 | method: citation — DELTA over 76673ee9 (20 changed lines over 3 files, all deletions of prose), pinned per commit (diff md5 6a22c87d9c31358969f38eb1e4958c7f, pin md5s in `execB/pinB20.md5`); both candidates executed by the orchestrator (`--receipt … --symbol` both ways; the four-way `self_selected` disjunction read at `:1003` and its counterexample run), and the new self-selection arm proven red by dropping `receipts` from the disjunction; `check_review_hygiene.py --surface` over both changed sources: 0 hits; hygiene over the receipt at close: 0 hits; round-zero probe — the battery re-run whole on a fresh pycache-free copy (`execB/battery21.out`, 145 of 145 killed) with all 145 anchors re-verified against the live tree after the fixes; the gate selection green with the red set exactly the four RECORDED reds (1628 passed, 4 failed) |

Row shapes (quoted here, so the gate does not read them as passes):

```text
| Pass 1 | native opus×1 + sonnet×2 | found: N, new: N, confirmed: C, fixed: X, unexecuted: U | citation |
| Pass 2 | native opus×1 + sonnet×2 | found: 0, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | method: re-derivation |
```

## Residual

Every candidate that did not enter `confirmed:` is recorded here, one row each, in the verdict
grammar of `/fabrik-review` § Phase 2 (the fenced block under the next heading is an EXAMPLE, never rows).

### Verdict grammar — an EXAMPLE, never rows

(quoted so no reader — human or scan — takes this template's own sample rows for the receipt's residuals):

```text
| F12 | RECORDED — unexecuted (3 probe attempts timed out) |
| F19 | RECORDED — by design (F4, round 3; D-203) |
| F31 | RECORDED — measured (a prevalence figure; makes no code or doc claim) |
```

`RECORDED — by design` names the OWNING row's first-cell id and the EARLIER round that adjudicated
it (or a `D-nnn` with no round); the gate refuses an absent owner and a round that is not below the
closing `Pass N`. `RECORDED — measured` and `RECORDED — unexecuted` never enter `confirmed:`;
`unexecuted:` on the closing row is what keeps an unexecuted CODE candidate from closing the loop.

| F6 | RECORDED — by design (D-229; the plan's T3.2 row — `new:` is the classes ledger's count unless `--new` is given) |
| F7 | RECORDED — measured (a `<date>-plan-<n>` prefix rule would refuse 67 of 132 phase receipts on this box, so the prefix is an additive escape; a record with no parseable plan surface keeps the time bound — backlog row at Finish) |
| F18 | RECORDED — measured (0.61 s over 317 plan files on the hub before batching; one git process after) |
| F23 | RECORDED — measured (F18's number; a timing assertion in a unit test is a flake, not a guard) |
| F32 | RECORDED — measured (0 of 5 dated hub plan sets carry a table under `## Touches`; the bullet collector never read tables — backlog row at Finish) |
| F45 | RECORDED — by design (D-238; the file's own raw-argv convention for `--json` at check_doc_index.py:219) |
| F53 | RECORDED — measured (the pre-existing `--classes-new a,b` test reads the `len(new)` fallback — `new: 2`) |
| F54 | RECORDED — measured (the grammar round-trip coupling is the test's point) |
| F55 | RECORDED — hygiene false positive (docs/workflows/FINAL_GATE_WORKFLOW.md:164 and :267 describe the residue class by its own two shapes) |
| R1 | RECORDED — measured (tests/enforcement/test_pack_reachability.py: three tests fail only when tests/enforcement/test_flip_gate_matrix.py runs first in the same pytest session — reproduced on a 5565a608 archive, before Phase B; backlog row at Finish) |
| R2 | RECORDED — measured (tests/enforcement/test_plan_tickets_epic_scope.py::test_frontmatter_parser_matches_epic_order_verbatim — the `_find_fences` parity gap between scripts/epic_order.py and check_plan_tickets.py, pre-existing at 5565a608, owned by the multi-agent-adoption plan's T03 in flight) |
| R3 | RECORDED — by design (D-243; Phase A's residual F1–F5 stand as recorded in docs/development/reviews/2026-09-12-plan-2-mail-triage-phase-A-review.md § Residual and are not re-raised here) |
| G8 | RECORDED — measured (fixed in-run with G2's chain and the stack union, graded by the running-record test's `CLAUDE_CODE_SESSION_ID` assertion; not counted under the delta bar — it is this phase's own code, so it did not wait) |
| G9 | RECORDED — measured (git never emits a rename on an untracked line; harmless) |
| G16 | RECORDED — measured (pre-dates the hunk; backlog row at Finish: `tests/enforcement/` + `tests/sysadmin/` rather than bare `tests/`) |
| G17 | RECORDED — measured (`.pre-commit-config.yaml` sits under no tooling prefix; unchanged by the hunk) |
| G21 | RECORDED — measured (the reduced re-run's output was not captured into the file; the full battery is re-run after round 2 and its output replaces the file) |
| G23 | RECORDED — by design (D-243; DISPATCHER mode's contract never binds a ticket receipt to a phase — /fabrik-execute-plan § D4; the cost is now STATED in the function's comment and in docs/reference/command-run-protocol.md) |
| G25 | RECORDED — measured (fixed in-run: both name the two shapes, the stem bind, the time bound and the prefix escape; rendered; not counted under the delta bar — Phase B's T3.1 made them stale, not bb995e60) |
| G36 | RECORDED — measured (0 of 60 hub tickets; benign under pathlib; cited for the record) |
| G38 | RECORDED — measured (fixed in-run: `[]` under `--json`; graded by `test_a_discovery_note_rides_stderr_under_json`; not counted under the delta bar) |
| H6 | RECORDED — measured (0 of 123 hub tickets; backlog rows at Finish) |
| H11 | RECORDED — measured (fixed in-run: `max`; the round-2 grader reads `(×2)`; not counted under the delta bar) |
| H17 | RECORDED — measured (`command_run.py` pops the parent on every close path — `stack.pop()` at the close — so a frame outlives its run only when the record itself stays `running`, which is the state the gate must honour) |
| H18 | RECORDED — measured (fixed in-run: `[*_`]*` on `_PASS_ROW`; asserted in the prose test; 0 of 1197 docs carried a row the two disagreed on) |
| H19 | RECORDED — measured (fixed in-run: a `- `-led backticked line continues the list; graded; 0 of 30 receipts regressed) |
| H20 | RECORDED — measured (fixed in-run: the rule is inverted — a backticked token is a path unless it is an md5/sha, a count, a range or carries whitespace; graded) |
| H21 | RECORDED — by design (D-243; the round-1 fallback for a scoped review over commits — bounded now by H14's IN-PROGRESS carve-out) |
| H24 | RECORDED — measured (every seat was pointed at the two commits by sha; the next round's pin diffs each commit against its own parent) |
| H29 | RECORDED — measured (`_finite_ts` predates the phase; the under-statement is in the safe direction; backlog row at Finish) |
| J6 | RECORDED — measured (the noun is deleted in-run — subsumed by the ellipsis rule; the colon predates the phase) |
| J8 | RECORDED — by design (D-243; the backtick is dropped in-run and the shape is out of grammar; the masking rule is the file's own — backlog row at Finish) |
| J12 | RECORDED — measured (0 of 1,230 ledger lines under the pin's reviews carry it) |
| J13b | RECORDED — measured (reachable only from a malformed `git cat-file` header; the `complete=False` row is graded through the non-git path) |
| J25 | RECORDED — by design (D-243; stated in `_is_path_token`'s docstring — a bare hex name is an md5 to the receipt grammar) |
| J27 | RECORDED — measured (immutable; the battery at that moment was 64 mutants, 63 killed, 1 survived — the survivor was R3-20 before its grader was re-pointed) |
| J29 | RECORDED — measured (the count is dated in the comment; the tree is live) |
| K3 | RECORDED — measured (`_section` ends at the next `##`; only an H1 can sit inside, an authoring error already) |
| K4 | RECORDED — by design (D-243; directories are not paths here after K1) |
| K8 | RECORDED — measured (the file is regenerated whole after every round; the brief quoted the pruned list) |
| K11 | RECORDED — measured (fixed in-run: the closed span is stripped from the heading text; graded) |
| K12 | RECORDED — measured (fixed in-run: a 4-space or tab-indented line is skipped; graded) |
| K13 | RECORDED — measured (fixed in-run: the dedupe key lowers `what`; graded) |
| K17 | RECORDED — by design (D-243; indistinguishable from a file name — stated in the docstring; 0 of 139 backticked tokens in the hub's receipts) |
| K23 | RECORDED — by design (D-243; the grammar's honest value is the word `none`; asserted so the choice is pinned) |
| K25 | RECORDED — measured (H29 stands; the back-reference parses while the phase clause precedes it) |
| L5 | RECORDED — measured (0 of 167 stems carry a dotted id; backlog row beside K30 at Finish) |
| L6 | RECORDED — by design (round 5 K-rows: an empty surface list is the placeholder; the refusal names the grammar — wording backlog at Finish) |
| L13 | RECORDED — one hop, fixed in-run beside L12 (a header git never writes ends the batch as incomplete; battery R6-15) — not counted |
| L14 | RECORDED — one hop, fixed in-run (`[ \t]*`; grader the lone `>` line; battery R6-13) — not counted |
| L15 | RECORDED — measured (the outcome is `[]` either way: `nan` falls through to the outer frame like `"abc"`, and a last-frame `nan` never matches an mtime); `if not since > 0` applied, no grader — behaviour-equivalent |
| L21 | RECORDED — measured (0 of 638 `## Touches` lines in the hub's 317 plan files are blockquotes — the whole branch is prospective; stated in the `_URL_RE` comment) |
| L23 | RECORDED — measured (the containment accepts direct children only; 12 of the 17 dated sets are archived; backlog row at Finish; brief MACHINERY) |
| L29 | RECORDED — one hop (`_blank_quoted` blanks from the line, not the column; backlog row at Finish) |
| L30 | RECORDED — by design (a non-prose file under a tooling prefix is tooling; 0 such files in the hub) |
| L31 | RECORDED — measured (the 3-space heading is now asserted; the greedy strip is behaviour-equivalent under the whitespace normalisation) |
| L37 | RECORDED — by design (the checklist rows are adjudicated to their FINAL state at the close, one leading verdict each; the close names K18's corrected claim) |
| M3 | RECORDED — one hop, fixed in-run (the head match skips leading quotes, brackets and backticks; graders the three heads; battery R7-3) — not counted |
| M7 | RECORDED — machinery (the generator masks code spans and labels Residual rows; regenerated for round 8) |
| M9 | RECORDED — measured (stated in the `_URL_RE` comment as the cost; 0 of 374 non-blank Touches lines carry `://`; backlog row at Finish) |
| M11 | RECORDED — one hop (backlog row at Finish beside F32) |
| M15 | RECORDED — one hop, fixed in-run (the argument is read like the line; grader the closed ATX argument; battery R7-9) — not counted |
| M16 | RECORDED — measured (0 of 296 receipts under docs/development/reviews carry an indented `<!--`; backlog row at Finish beside L29) |
| M19 | RECORDED — by design (regression guards for L27, the CommonMark comment rule and the `missing` arm; their comments now say so) |
| N3 | RECORDED — one hop (backlog wording row at Finish) |
| N4 | RECORDED — one hop (same backlog row as N3) |
| N6 | RECORDED — one hop, fixed in-run (the edge class is `[\W_]`, Unicode-aware; grader the non-ASCII surface; battery R8-2) — not counted |
| N7 | RECORDED — machinery (the brief now says to `git init` the probe dir or probe `_parse_usage_feedback` in-process) |
| N10 | RECORDED — measured (the M9 glued-tail cost; the backlog row at Finish extends) |
| N12 | RECORDED — machinery (all three in the round-9 brief) |
| N16 | RECORDED — one hop, docstring fixed in-run ("answered WHOLE — a `missing` answer counts, a cut body does not") — not counted |
| N17 | RECORDED — measured (0 on this box; stated beside `_LOCATION_SUFFIX`) |
| N18 | RECORDED — machinery (N12) |
| N19 | RECORDED — by design (a guard that the widened tail strip never over-eats a decorated real surface; its comment says so) |
| P2 | RECORDED — one hop, fixed in-run (the tail says "did not answer whole"; the comment quotes the docstring) — not counted |
| P4 | RECORDED — one hop, fixed in-run (the leading run is an ATX run `#{1,6}` then whitespace; graders `hashtag.md` and the idempotency loop; battery R9-3) — not counted |
| P5 | RECORDED — by design (the docstring says a `#`-led line; the receipt template's Pass Ledger is ATX) |
| P9 | RECORDED — machinery (the round-10 brief states "111: 110 listed plus the appended M21") |
| P12 | RECORDED — one hop (backlog row at Finish beside L6) |
| P13 | RECORDED — by design (hub mail ids are upper-case ULIDs; K23's sibling) |
| P14 | RECORDED — machinery (the round-10 brief) |
| Q1 | RECORDED — one hop, fixed in-run (the head's skip class is the tail's ASCII complement; graders `¹none`, `none¹`; battery R10-1) — not counted |
| Q3 | RECORDED — one hop (backlog row at Finish beside P12) |
| Q6 | RECORDED — one hop, fixed in-run by Q4's change (grader `t[a]:44`) — not counted |
| Q10 | RECORDED — one hop, fixed in-run — not counted |
| Q12 | RECORDED — one hop (backlog row at Finish beside L29/M16) |
| Q16 | RECORDED — machinery (the round-11 brief carries one count and per-suite baselines) |
| R5 | RECORDED — by design (K17's docstring cost; harmless) |
| R8 | RECORDED — by design (P13/K23's sibling; backlog row) |
| R11 | RECORDED — one hop, the empty forms fixed in-run (`<!--(?:-?>|.*?-->)`; graders both forms; battery R11-7); the splice-join stays a backlog row — not counted |
| R14 | RECORDED — machinery (the round-12 brief tags the copy list per suite) |
| S5 | RECORDED — one hop (backlog row at Finish beside L29/M16/Q12) |
| S6 | RECORDED — one hop, fixed in-run (the clause added) — not counted |
| S7 | RECORDED — machinery (the template names the round's message) |
| S11 | RECORDED — one hop (backlog row beside P12/Q3) |
| S12 | RECORDED — by design (the docstring says "decimal digit"; backlog row beside Q3) |
| T10 | RECORDED — one hop (backlog row at Finish beside N17) |
| T11 | RECORDED — one hop, rewrapped in-run — not counted |
| T12 | RECORDED — one hop, four words added in-run — not counted |
| T14 | RECORDED — by design (the file's one quoting model; L29/S5/Q12/M16's backlog row) |
| U5 | RECORDED — measured (a comment says so) |
| U9 | RECORDED — machinery (the round-15 brief says: never run anything inside the pin; copy first) |
| V9 | RECORDED — measured (inert: no consumer of that copy reads a column — `_blank_quoted` promises same-length against its OWN input, the prefilters test a prefix, the key reads `lines[i]`; a comment says so. Backlog row at Finish) |
| V10 | RECORDED — by design (J27: a landed message is not rewritten; the docstring itself makes no such claim) |
| V11 | RECORDED — machinery (the round-16 brief names the `Hit` fields and says: redirect `git show` into your own dir, never a checkout) |
| W7 | RECORDED — measured (executed, all four indents open; it is also why round 14's 10-space `~~~` tail opened a fence. Backlog row at Finish beside L29/M16/Q12, with the copied-with-attribution sibling `check_command_corpus.py::_fence_step` to keep in step) |
| W9 | RECORDED — machinery (FIXED in `briefB15.template.md`: the pointer is now a `__MSG__` placeholder, so it cannot be carried stale) |
| W10 | RECORDED — machinery (FIXED in the template: ONE `__NMUT__` placeholder, the parenthetical now forbids a second number outright) |
| W12 | RECORDED — measured (this record: 24.1h old, `state: running`, round-15's committed review-fix work flagged. The bound is meant to fail OPEN; here staleness REMOVES a window and thereby ARMS the sixth cause. Written up at `execB/machinery-stale-window.md`; destination: fabrik-mail to the infra beat at the close) |
| X6 | RECORDED — measured (executed both: `--receipt --phrase` rc 0 and silent, `--receipt --claim` prints `REFUSED — --claim needs --surface`. Backlog row at Finish beside L6/P12: refuse or note `--phrase` with no `--surface`) |
| X7 | RECORDED — machinery (FIXED at the source: the template no longer carries counts; it tells the seat to establish and report its own baseline, the only form that cannot drift) |
| X8 | RECORDED — machinery (the round-18 brief carries it: chain probes with `;` and an explicit rc read, never `&&`) |
| X9 | RECORDED — machinery (the pin is bytecode-free by construction — round 14's U9 — so the copy is clean; the round-18 brief adds the exclude anyway) |
| Y8 | RECORDED — machinery (FIXED at the source: the template now carries the `tar` form and says why `cp -r --exclude` is a silent no-op) |
| Y9 | RECORDED — measured (`check_review_hygiene.py` symbol count; backlog row at Finish) |
| Z5 | RECORDED — measured (the anchor still kills its mutant; only its NAME implied a location. Backlog row at Finish: battery anchor titles should not imply which assertion kills them) |
| Z6 | RECORDED — machinery (the seat ran three cycles and named the remainder under CLEAN rather than guessing, which is the contract working; the round-20 brief asks for the mutants that decide the round's own claims, not every row) |

## Per-phase verdicts

### Phase B — `command_run.py` (T3) and the graders (T4): CLOSED ON THE SCOPE-GROWTH STOP (D-252)

Twenty rounds (confirmed 43 · 28 · 20 · 24 · 17 · 20 · 13 · 10 · 7 · 9 · 9 · 7 · 10 · 5 · 8 · 6 · 5 · 7 · 4 · 2). Rounds 1–13 closed the Phase B code itself: `command_run.py`'s phase-review gate, `new:` derivation and placeholder refusal; the convergence graders' untracked claims, ledger-row quiet pattern, advisory for an EXECUTED plan with no review, and Hunt↔Changed cross-check; the hygiene checker's dedupe, heading stop and comment model; the coverage gate's path-token rule; the rubric's surface-aware floor. Rounds 9–13 oscillated on two spots until D10 rule 3 forced the class rewrite in round 13 — one masked comment reader for every caller, one table grading every arm of the placeholder head.

**Rounds 14–20 never touched the Phase B code, which has been quiet since round 13.** They touched only `check_review_hygiene.py` and `check_review_coverage.py`, and every confirmed finding was inside the PREVIOUS round's own fix prose: a change that served no consumer and failed open (15), claims true on one path stated as true (16), behaviours promoted to contract with nothing grading them (17), a qualifier lost in a re-cut and two guards found ungraded behind it (18), explanatory claims added by earlier fixes (19), and two of those deletions found to have over-deleted (20). Two dispositions were structural rather than editorial — a 36-example docstring became the seven-rule ladder the code is, pointing at an executed token table (18); the remaining falsifiable claims were deleted rather than corrected again (19) — and the loop still did not stop, because the counts were FALLING (7 · 4 · 2) and the stall circuit-breaker only fires on counts that stop falling.

**There is no round 21.** The counted scope-growth stop shipped as D-252 (`scripts/command_run.py::scope_growth_warning`, receipt `docs/development/reviews/2026-09-14-scope-growth-stop-review.md`) was run against this review's own last two adjudicated rounds and FIRED: round 19 confirmed 4, all four inside round 18's fixes; round 20 confirmed 2, both inside round 19's deletions — `own_fix == confirmed > 0` for two consecutive rounds. Its exit is: stop the loop, route the remaining own-fix work to a backlog row with a named destination, and close on the ORIGINAL delta's state. That is this close. The original delta's state at `a86950e2` is fully fixed and committed — every finding of rounds 1–20, including round 20's two, landed with its grader — so the stop costs this phase nothing but the twenty-first round it would otherwise have spent on its own prose.

Evidence at the close: the 145-mutant battery re-run whole IN THIS TURN on a fresh `__pycache__`-free copy of the LIVE tree — which now also carries D-252's change to `scripts/command_run.py`, so the Phase B anchors are re-verified against the file as it stands, not as it stood at `a86950e2` — `battery: 145 mutants, killed 145, survived 0, other 0`, no anchor miss and no failed baseline (`execB/batteryCLOSE/battery.out`); `final_gate.py --check --json` `"status": "success"`, 64 passed, 0 failed, `skipped_checks: ["pytest"]` (the hub's pytest leg is OFF by design, so that green asserts nothing about the suite — the suite evidence is the battery and the gate selection); `check_review_hygiene.py --surface` over both changed sources: 0 hits. Over THIS receipt it is 7 ADVISORY `dual-verdict` hits, not 0 — see § CLOSE, which states the measurement and corrects the two Pass rows that claimed otherwise.

**Why the Status stays IN-PROGRESS:** the exit round was not quiet (`confirmed: 2`), and `check_review_coverage.py` refuses a CONVERGED flip on a non-quiet exit round unless the stuck finding is BLOCKED-escalated. Nothing here is stuck — the loop is stopped by rule, not blocked by a defect — so the honest record is IN-PROGRESS plus a `blocked` close naming the stop, exactly as D-252's own review closed.

## Gate

`final_gate.py --check --json`, pasted verbatim at the flip (check_convergence reads the fenced
`"status": "success"`):

```json
{
  "status": "success",
  "tier": 2,
  "passed": 64,
  "failed": 0,
  "skipped": 1,
  "skipped_checks": [
    "pytest"
  ]
}

(full output: `execB/gate-close.json`, 20,258 bytes — 'advisory' and 'warnings' elided here; 'failures' is [] and 'blocking' is 0)
```

## CLOSE — stopped by D-252, not converged

The review is CLOSED at round 20, on the counted scope-growth stop. It is not CONVERGED and no round 21 was dispatched: round 21's brief (``execB/briefB21.md``, one fresh Opus seat over `a86950e2`, 23 changed lines) is retired unused.

- **The stop, executed:** `scope_growth_warning([{"confirmed":4,"own_fix":4},{"confirmed":2,"own_fix":2}], command="fabrik-review")` → FIRES. The control — the same counts with round 20's findings placed outside the previous fix (`own_fix: 1`) — returns `''`, so the rule is discriminating, not a blanket round cap. The live record carries both rounds with `--own-fix` and printed the warning at round 20.
- **Why rounds 19 and 20 are all-own-fix, per this receipt's own ledgers:** round 19's Z1–Z4 are each marked `(in-hunk)` against round 18's comment, ladder, label and appositive; round 20's AA1 and AA2 are both "the deletion over-deleted", i.e. inside round 19's own deletions. The two RECORDED rows in each round are machinery, which the stop does not count.
- **Why the stall circuit-breaker never fired:** it needs three consecutive rounds with a non-decreasing, nonzero `confirmed`. This failure mode produced 7 · 4 · 2 — a textbook convergence curve made entirely of the review's own residue. The breaker is blind to it by construction; that is the gap D-252 closes.
- **What is owed at the plan's Finish (not here):** § Residual's RECORDED rows that name a backlog destination still owe their `docs/STRATEGIC_BACKLOG.md` rows, as every round's disposition says ("backlog row at Finish"). They are listed in § Residual with their destinations and are unaffected by this close.
- **Already verified and NOT owed again:** the Coverage Checklist re-adjudicated against the commit chain (24 of 25 rows already final; `check_ticket_breadth.py` corrected FIXED→CLEAN); § Residual complete at 115 of 115 RECORDED rows; the Pass Ledger's `confirmed:` column ≡ the series; the receipt passes `check_review_coverage --root .` (0 unproven claims) and `check_convergence` under a private-index stage.
- **Hygiene at this close, measured (and a correction):** `check_review_hygiene.py --receipt <this file>` → **7 ADVISORY `dual-verdict` hits** (`:478 :704 :707 :708 :724 :741 :757`), rc 0, 168 rows ungraded. Every one is a false positive of that advisory's word-count rule: each cell's LEADING verdict is correct and singular, and the second verdict word sits inside explanatory prose about a different row or a different artifact (`:707` — "RECORDED — machinery (FIXED in `briefB15.template.md` …)" — is the shape). **This falsifies the `hygiene over the receipt at close: 0 hits` clause in the Pass 19 and Pass 20 method cells**: the `dual-verdict` rule last changed in round 18 (`1bb9a84f`) and row Z6 at `:757` was added by round 19 itself, so at least the Pass 19 claim could not have been true as written. The rows are left as they are — correcting them is precisely the own-fix prose work the stop forbids — and the class is RECORDED here with its destination: `docs/STRATEGIC_BACKLOG.md` at the plan's Finish, beside the existing hygiene rows, as "the `dual-verdict` advisory counts verdict words anywhere in a cell, so a cell that EXPLAINS another row's verdict reads as dual".
- **Then:** Phase B's own commit (plan Execution notes, CHANGELOG hunk as HEAD blob + own hunk, INDEX row for this receipt), then Phases C–H and Finish per the plan — each under D-252, which now stops this class of loop by count rather than by judgement.
