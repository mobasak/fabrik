# T08 — /fabrik-deploy-plan: 63b manifesto conformance

Status: CONVERGED — 2 source fixes + 2 cells corrected + T09 seeding formally wired; closing round new: 0

Surface: commands/_sources/fabrik-deploy-plan.md (322 lines, read in full; anchors from THIS run's grep output) + rendered composition (run-record + close-feedback, T01-swept).
Outcome: 2 FIXES — eighth consecutive falsified stamp: the target-VPS pick and answered operator batches never reached the ledger (both now mint rows in the authoring commit); my (c) conflated Phase-8's plan-level first-days rule with Phase-4's per-step rollbacks; my (e) cited the deployed-service's monitoring instead of this command's OWN Self-audit mechanism. The informal T07→T09 seeding is now wired into T09's Context Files (orchestrator queue maintenance).

## 63b Verdict Table

| intersection | verdict (grep-derived SOURCE anchors) |
|---|---|
| (a) checkable gates | CONFORMS — the terminal artifact is a DRAFT plan with every mandated section; CONVERGED is explicitly NOT this command's to claim ("the review command's md5-verified no-op is what earns CONVERGED", :39); "release: PASS with no fenced evidence is the exact fabrication the review command is instructed to reject" (:57-58) |
| (b) ledger routing + one-way field block | FIXED — the plan-as-record reasoning failed the abandoned-DRAFT scenario (verifier: a ruling in a never-converged DRAFT is ungreppable); Phase 1's target pick and every answered batch now mint their docs/DECISIONS.md rows IN the authoring commit (:120-123, :287-290 post-fix); the CONVERGED-flip row stays T09's, now formally seeded via its Context Files |
| (c) rigor scales with irreversibility | CONFORMS (cell corrected — two distinct mechanisms, no longer conflated): Phase 4 mandates the PER-STEP half — every step's exact executable rollback with its own verification, never prose (:158-160); Phase 8 mandates the PLAN-LEVEL half — the first-days rollback decision rule, what observation triggers it and who decides (:271-275); the WAIT BOUND is declared at authoring (:237) |
| (d) labeled verified/assumption evidence | CONFORMS — every `shape:` flag re-verified at path:line (:128); hub-repo evidence pasted for a service claim named as the same fabrication class (:58); fenced evidence or the verbatim waiver in the header (:290-291) |
| (e) captured disorder | CONFORMS (cell corrected — the right mechanism is this command's OWN): `## Self-audit` records what was verified vs assumed + the named residuals the review must attack (:311), with inline finding-language ("record the divergence in the plan's Self-audit" :86; "An untraceable var is a finding, not a hope" :132). The :264 monitoring-truth section remains the manifesto's armed-tripwire clause FOR THE SHIPPED SERVICE — real, but a different intersection's evidence |
| (f) most-reversible default under ambiguity | CONFORMS — questions batch ONCE per DECISION POINT (:105, :283 — "a question that only becomes KNOWABLE after a prior ask's" answer defines the batching boundary); "Never refuse" where evidence + the ask can settle it (:105-106) |

6/6 adjudicated after verification: 5 CONFORMS · 1 FIXED.

## Scoped verification review

| pass | finders | found | new | fixed | verdict |
|---|---|---|---|---|---|
| Pass 1 | 1 native author-blind verifier: 10 anchors re-derived (all landed — the grep-derivation discipline held this time), abandoned-DRAFT scenario, per-step-vs-plan-level distinction, sibling-pattern comparison for (e) | 3 | 3 | 3 | B1 ledger gap FIXED (2 source clauses + T09 wiring) · B2 (c) conflation FIXED (cell) · B3 (e) wrong-mechanism FIXED (cell) |
| Pass 2 (closing, method: gate) | mechanical: both source clauses grep clean; T09 Context Files carries the seed; emit gate re-run exit 0 | 0 | 0 | 0 | → EXIT |

## Per-finding disposition ledger

| # | finding | state |
|---|---|---|
| B1 | target-VPS pick + answered operator batches never reached the ledger; the T09 deferral was informally seeded (T09's Context Files carried nothing) | FIXED — both mint rows in the authoring commit; T09's Context Files now carries T07's artifact + the adjudication instruction |
| B2 | (c) misattributed Phase-8's single plan-level rule as per-step | FIXED — cell distinguishes Phase-4 per-step rollbacks from Phase-8's first-days decision rule |
| B3 | (e) cited the shipped service's monitoring instead of the command's own Self-audit capture | FIXED — cell re-grounded on :311/:86/:132; :264 re-classified as the shipped-service tripwire clause |
