# Freshness-guard surface — review round 2 (2026-09-02)

Surface: `4ac17e0fe973eab5d6a8fcb9232717efe3fbf8b2` + surface-md5 `371069221cb60f2e12e68d728eeefc31`
(`git show cfa906bc -- <the 4 scope paths> | md5sum`)

**Anchor did NOT match, and this is why it matters.** The newest prior report for this scope
(`2026-09-02-selection-freshness-guard-review.md`) pins `Surface: 3bf64937…`. Two commits have landed
on the same files since — `5c925dd0` (that review's own 31 fixes) and **`cfa906bc`**, the coverage
denominator correction plus a new grader test, which has had **no full review at all**. A non-matching
anchor forbids inheriting the prior checklist's `CLEAN` rows, so this is a **full WIDE pass 1**, not a
verification-and-delta run.

**Scope (4 files, 790 lines at pass 1; 860 after this round's fixes):** `guard_selection_freshness.py` (153) ·
`tests/test_guard_selection_freshness.py` (201) · `tests/test_autocommit_freshness_wiring.py` (155) ·
`autocommit_pipeline_outputs.sh` (281).

## Rubric (verbatim header; MATCHED sections listed)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.
```

MATCHED/FLOOR sections: core/35-security-auth.md, core/25-data-postgres.md, core/30-ops.md, 12-FACTOR (all twelve axes), core/10-python.md, core/45-testing-strategy.md

## Coverage Checklist

| # | class | source | verdict |
|--:|---|---|---|
| 1 | fail-open vs fail-closed on every gate/guard | standing | **CLEAN** — every branch of `is_regression`/`head_text`/`refresh_date` walked: OSError→KEEP, no HEAD copy→KEEP, either date unparseable→KEEP, git error or non-zero rc→KEEP; the single DROP is `(h-w).days > 0`. An unexpected exception in `main` exits non-zero, and the shell's `if` then takes the `GUARD_FAILED_KEEPING_ALL` branch — fail-open at both layers |
| 2 | cost/quota/limit accounting edges | standing | **CLEAN** — no cost, quota or limit arithmetic on this surface; the only counter is `_GUARD_DROPPED = _GUARD_N - ${#_GUARD_KEPT[@]}`, exercised by `test_a_dropped_path_is_reported_not_silent` and `test_all_dropped_does_not_masquerade_as_an_empty_stage_list` |
| 3 | boundary/sentinel/prefix collisions | standing | **CLEAN** — executed, not reasoned: CRLF→parses, BOM+CRLF→parses, ISO-8601 with time→date extracted, header at line 25→parses, line 26→None (the documented bound). Pool candidates on CRLF/BOM fragility REFUTED by that run |
| 4 | behavior without a test | standing | **FIXED(2)** — the crashing-helper path had no test (C2) and the ai-render half of the coverage claim had no grader (C7). Both added, both proven red-on-revert |
| 5 | Python discipline — typing, env, exception scope | `core/10-python.md` | **CLEAN** — `except Exception` in `head_text` is deliberate and tested (`test_fail_open_when_subprocess_itself_raises` drives the raise directly); `GUARD_REPO` read via `os.environ.get(... ) or os.getcwd()` with the CWD fallback covered by `test_guard_reads_the_repo_it_is_told_to_not_a_hardcoded_root`; `ruff check scripts/kilo-benchmarks/` → All checks passed |
| 6 | testing strategy — per-behaviour, watched-fail-first, real not mocked | `core/45-testing-strategy.md` | **CLEAN** — 25 tests, real throwaway git repos and a real bash interpreter, no mocked `git show`; both tests added this round proven red before green |
| 7 | 12-Factor XI — stdout only, no logfile | 12-FACTOR | **CLEAN** — the guard prints to stdout/stderr and opens no file; the new failure message goes to `>&2`, which the pipeline log captures |
| 8 | 12-Factor III — config via env, no hardcoded constants | 12-FACTOR / `core/30-ops.md` | **CLEAN** — `GUARD_REPO` is the only config; `_SKEW_DAYS`/`_HEAD_LINES` are documented invariants, not deployment config, and both carry the reasoning for their value |
| 9 | secrets / zero hardcoding | `core/35-security-auth.md` | **CLEAN (evidence corrected in pass 4)** — the guard: 0 hits. The shell: **1** hit, not 0 as an earlier draft of this row asserted — `autocommit_pipeline_outputs.sh:266`, the string `forbid-secrets` naming a pre-commit HOOK inside a comment. Refuted as a match on a hook name, not a credential. The row is corrected rather than left saying 0, because a checklist whose evidence is wrong is the thing this gate exists to prevent |
| 10 | shell-wiring correctness | scope-specific | **FIXED(2)** — the ai-render feeder still used `< <(cmd \|\| true)`, the exact pattern the comment four lines below it BANS (C2); and the guard tempfile had no EXIT trap (C1). Fixed as ONE trap covering both files after pass 2 caught the fix reintroducing the leak |
| 11 | the docstring denominator vs the live stage list | scope-specific (`cfa906bc`) | **CLEAN** — `7 of 12 static paths` / `5 undated` re-derived from the live `PATHS=( … )` array and the graded test passes |
| 12 | grader test actually grades — red-on-revert, not vacuous | scope-specific (`cfa906bc`) | **FIXED(1)** — vacuity probed (a missed PATHS regex ASSERTS rather than passing; zero-path and reworded-docstring cases fail loudly). The real gap was COVERAGE: it graded only the static half of a two-claim sentence, leaving the fleet-synced ai-render half asserted. Now graded |

## Disposition ledger

| # | candidate | source | disposition |
|--:|---|---|---|
| C1 | `mktemp` with no EXIT trap → leak on abnormal exit | pool u2 (HIGH) + my own hunt | **FIXED** — one trap, both files; verified cleaned on normal AND early (`exit 7`) exit |
| C2 | ai-render feeder uses `< <(cmd \|\| true)`, hiding the helper's exit code | pool u2 (HIGH) | **FIXED** — the file's own comment at `:136` bans this pattern (re-derived in the closing pass — my own fix shifted it from `:132`); the block four lines above used it. A crash now prints and the status is observable. Test added |
| C3 | filename containing a newline corrupts `PATHS` | pool u1 (HIGH) | **REFUTED** — unreachable on this surface: the feeder emits `.windsurf/rules/ai/*.md` pack paths and the static list is literal. No path on the stage list can contain a newline; adding `-d ''` plumbing for an unreachable input is speculative hardening |
| C4 | `git show HEAD:<rel>` mis-parses a path with a colon or leading dash | pool u1 (HIGH) | **REFUTED** — same reachability argument, and the failure direction is safe: any git error returns None → KEEP → fail-open. The guard cannot mis-DROP through this path |
| C5 | BOM/CRLF handling fragile; `"\n".join` distorts the head | pool u1 (HIGH) | **REFUTED by execution** — CRLF, BOM+CRLF and ISO-with-time all parse to the correct date (run above) |
| C6 | header at exactly the 25-line bound may be cut off | pool u3 | **REFUTED by execution** — line 25 parses, line 26 returns None; that is the documented bound, and `test_the_head_bound_still_reaches_the_ai_pack_position` guards the ai-pack position at 14-16 |
| C7 | the coverage docstring's ai-render half is ungraded | pool u3 + my own hunt (independent) | **FIXED** — `test_the_ai_render_denominator_is_graded_too`, red-on-revert proven |
| C8 | no test for `GUARD_REPO` unset (CWD fallback) | pool u3 | **REFUTED** — covered by `test_guard_reads_the_repo_it_is_told_to_not_a_hardcoded_root`, which runs with `env={"PATH": …}` (no `GUARD_REPO`) and `cwd=r` |
| C9 | `head_text`'s `p.returncode != 0` branch is dead code | pool u0 | **REFUTED** — the unit refuted itself mid-answer: `subprocess.run` defaults to `check=False`, so a non-zero rc returns normally and the branch is the live path for an untracked file. `test_fail_open_when_git_itself_fails` exercises it |
| C10 | **my own fix reintroduced the leak it closed** — `_AI_OUT` had a linear `rm` only, and a second `trap … EXIT` would have REPLACED the first | pass 2, self | **FIXED** — proven: `trap "echo FIRST" EXIT; trap "echo SECOND" EXIT` prints only SECOND |
| C11 | `N802` — uppercase in a new test name | pass 2, `ruff` | **FIXED** — renamed; `ruff check scripts/kilo-benchmarks/` clean |

## Pass Ledger

| Pass | method | found | new | fixed | finders |
|---|---|---|---|---|---|
| Pass 1 | method: citation | found: 9 | new: 9 | fixed: 0 | pool dispatched 4 / returned 4 (deepseek-v4-flash · gemini-3-flash · qwen3-max · deepseek-v3.2-exp) + 1 native Opus dispatched, NOT returned at write time — its partition re-covered by the orchestrator (classes 1–7 all hunted directly, evidence in the checklist) + my own hunt (C7, C1) |
| Pass 2 | method: gate | found: 2 | new: 2 | fixed: 5 | orchestrator, scoped to the fix diff + its callers; `ruff` + `pytest` as gates. Caught C10 (the fix reintroducing its own leak; a 2nd `trap … EXIT` REPLACES the 1st) and C11 (`N802`) |
| Pass 3 | method: re-derivation | found: 2 | new: 2 | fixed: 2 | pool dispatched 3 / returned 2 at write time (gemini-3-flash · qwen3-max); orchestrator re-derived EVERY count and anchor from primary source — static 12/dated 7, ai packs 11/dated 7/undated 4, 25 tests, the ban-comment anchor (found `:136`, report said `:132`), and the scope line count (860 post-fix, 790 at pass 1) |
| Pass 4 | method: re-derivation | found: 1 | new: 1 | fixed: 1 | orchestrator — full fresh sweep over the post-fix surface, every checklist row re-read against the code and every probe re-run. Raised ONE: the secrets grep returned 1 hit where row 9 asserted 0 (`:266`, the hook name `forbid-secrets` in a comment). Refuted, and the row's EVIDENCE corrected |
| Pass 5 | method: re-derivation | found: 0 | new: 0 | fixed: 0 | orchestrator — confirming sweep after pass 4's evidence correction; all 12 rows re-verified, every probe re-run verbatim, zero candidates raised |

**Finder accounting:** pass 1 dispatched 5 (4 pool + 1 native Opus), 4 returned. The native Opus
finder had not returned when the loop closed; **its partition was re-covered by the orchestrator** —
classes 1–7 were each hunted directly with the evidence recorded in the Coverage Checklist, and two of
the three fixes (C1, C7) were found by that direct hunt independently of any finder. Recorded rather
than counted as swept-by-proxy.

## Disposition ledger

| # | candidate | source | disposition |
|--:|---|---|---|
| C1 | `mktemp` with no EXIT trap → leak on abnormal exit | pool u2 (HIGH) + my own hunt | **FIXED** — one trap, both files; verified cleaned on normal AND early (`exit 7`) exit |
| C2 | ai-render feeder uses `< <(cmd \|\| true)`, hiding the helper's exit code | pool u2 (HIGH) | **FIXED** — the file's own comment at `:136` bans this pattern (re-derived in the closing pass — my own fix shifted it from `:132`); the block four lines above used it. A crash now prints and the status is observable. Test added |
| C3 | filename containing a newline corrupts `PATHS` | pool u1 (HIGH) | **REFUTED** — unreachable on this surface: the feeder emits `.windsurf/rules/ai/*.md` pack paths and the static list is literal. No path on the stage list can contain a newline; adding `-d ''` plumbing for an unreachable input is speculative hardening |
| C4 | `git show HEAD:<rel>` mis-parses a path with a colon or leading dash | pool u1 (HIGH) | **REFUTED** — same reachability argument, and the failure direction is safe: any git error returns None → KEEP → fail-open. The guard cannot mis-DROP through this path |
| C5 | BOM/CRLF handling fragile; `"\n".join` distorts the head | pool u1 (HIGH) | **REFUTED by execution** — CRLF, BOM+CRLF and ISO-with-time all parse to the correct date (run above) |
| C6 | header at exactly the 25-line bound may be cut off | pool u3 | **REFUTED by execution** — line 25 parses, line 26 returns None; that is the documented bound, and `test_the_head_bound_still_reaches_the_ai_pack_position` guards the ai-pack position at 14-16 |
| C7 | the coverage docstring's ai-render half is ungraded | pool u3 + my own hunt (independent) | **FIXED** — `test_the_ai_render_denominator_is_graded_too`, red-on-revert proven |
| C8 | no test for `GUARD_REPO` unset (CWD fallback) | pool u3 | **REFUTED** — covered by `test_guard_reads_the_repo_it_is_told_to_not_a_hardcoded_root`, which runs with `env={"PATH": …}` (no `GUARD_REPO`) and `cwd=r` |
| C9 | `head_text`'s `p.returncode != 0` branch is dead code | pool u0 | **REFUTED** — the unit refuted itself mid-answer: `subprocess.run` defaults to `check=False`, so a non-zero rc returns normally and the branch is the live path for an untracked file. `test_fail_open_when_git_itself_fails` exercises it |
| C10 | **my own fix reintroduced the leak it closed** — `_AI_OUT` had a linear `rm` only, and a second `trap … EXIT` would have REPLACED the first | pass 2, self | **FIXED** — proven: `trap "echo FIRST" EXIT; trap "echo SECOND" EXIT` prints only SECOND |
| C11 | `N802` — uppercase in a new test name | pass 2, `ruff` | **FIXED** — renamed; `ruff check scripts/kilo-benchmarks/` clean |

## Pass Ledger

| Pass | method | found | new | fixed | finders |
|---|---|---|---|---|---|

## Disposition ledger

(one row per candidate raised)
