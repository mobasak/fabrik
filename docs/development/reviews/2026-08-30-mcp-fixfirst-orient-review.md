# Review — postgres-pro fix-first chase (D-031) + ORIENT MCP awareness (D-032)

Surface: scripts/sysadmin/emit_mcp_project_config.py (_uri_connects probe + absent-until-configured
+ container-host rewrite) · tests (+3, 16 total) · .claude/hooks/session_orient.py (_mcp_line, synced)
· docs/DECISIONS.md (D-031/D-032) · docs/workstation/mcp-roster.md · CHANGELOG.md · user-level roster
(postgres-pro removed via rotator, invariant md5=1, DR-backed). Mode: NO-POOL — solo native.

## Rubric (verbatim output — review_rubric.py --changed scripts/sysadmin/emit_mcp_project_config.py .claude/hooks/session_orient.py tests/test_emit_mcp_project_config.py)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)

## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: scripts/sysadmin/emit_mcp_project_config.py, .claude/hooks/session_orient.py)
### core/45-testing-strategy.md  (hit: tests/test_emit_mcp_project_config.py)
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | root-cause-before-fix | CLEAN | THREE measured reproductions before the final design: postgres-main DNS block 31.5s · localhost bad-auth 32.4s · port-9 refusal 30.0s · env-less = refuses to start ("No database URL provided") — every wrong shape reads dead in Claude's 30s timeout; the fix (absent-until-configured + probe) targets the whole class, not one symptom |
| 2 | behavior-without-a-test | CLEAN | 3 new red-first tests (probe-gated emission, refused-URI absence via a REAL closed-port probe, localhost-rewrite fed to the probe); suite 16/16 |
| 3 | fail-open/fail-closed | CLEAN | probe unavailable (None) → trust the URI (fail-open, never strips fleet-wide on a missing psycopg); probe False → absent (the safe shape); probe path swallows only subprocess/OS errors |
| 4 | security/secrets (35) | CLEAN | probe passes the URI as argv to a local interpreter, never logs it; hook reads server NAMES only |
| 5 | hook-safety | CLEAN | _mcp_line is fail-safe (bare except → universal-set line, proven by the /tmp render); ast.parse green; ORIENT text block only — no control flow, exit codes, or cause logic touched |
| 6 | sync-consciousness | CLEAN | the hook is a governance-sync trigger — the line is correct for all repos (runtime read of THAT repo's .mcp.json; catalog by absolute box-local path, same pattern as hooks-index) |
| 7 | rotation safety | CLEAN | user-level change via rotator only; md5 invariant re-proven = 1 across 6 files; headless profile matched; DR-backed |
| 8 | live end-to-end | CLEAN | hub `claude mcp list`: 15/15 Connected NO FAILURES; llm_batch: postgres-pro ✔ Connected from project scope — the real consumer, both scopes |
| 9 | decision-ledger | CLEAN | D-031 (narrow supersede of D-020 emission semantics) + D-032 (operator directive) — --check OK |
| 10 | cost/quota accounting | CLEAN | zero paid calls; probes are local subprocesses |
| 11 | boundary/sentinel | CLEAN | host rewrite is the exact `@postgres-main:` token; ModuleNotFoundError sentinel routes to the next interpreter, not a False verdict |
| 12 | doc-truth (40) | CLEAN | roster postgres-pro row + topology USER row state the D-031 reality incl. the 4 qualifying repos (denominator) |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — all 12 classes over the diff | citation + execution | solo native (NO-POOL; dispatched: 0) | 3 | 3 | 3 | user-scope env-less leak (the second claude-mcp-list failure) · F401 types import · the placeholder-URI idea killed by measurement (port-9 block) |
| Pass 2 | CLOSING — re-derivation: suite 16/16 fresh · gate 55/0 · both claude-mcp-list probes re-run (15/15 + llm_batch ✔) · invariant md5=1 · decisions --check | re-derivation | 0 | 0 | 0 | TERMINAL |

Quiet final pass: found: 0 · fixed: 0 — TERMINAL.

## Step verdict

Step verdict: PASS — the fix-first duty executed on its own trigger server; the awareness line ships
in the one surface every session reads.

```json
{"status": "success", "tier": 2, "passed": 55, "failed": 0}
```
