# T03 acceptance review — the `--check` ownership advisory + `advisory=True` at the gate (plan 2026-09-06-plan-2-multi-agent-adoption)

**Status:** CONVERGED
**Surface:** `git diff ac72db17..worktree-agent-a16957e0307ad8194 -- scripts/docs_updater.py tests/test_docs_updater_adopt.py scripts/final_gate.py` (r1 a72b44af: +116/−4 script, +164 tests, 1/1 final_gate; native Sonnet coder — never-route, `scripts/final_gate.py` in Touches).

## Coverage Checklist

Rubric invocation (verbatim head of the output):

```text
$ python scripts/review_rubric.py --changed scripts/docs_updater.py tests/test_docs_updater_adopt.py scripts/final_gate.py
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
```

| Class | Status |
|---|---|
| core/10-python.md — stdlib / no logfile / ruff sets | CLEAN (stdlib; ruff check clean on all three files; the Drift call re-wrapped for `ruff format` — native D4) |
| core/40-documentation.md — trailers, one CHANGELOG entry (orchestrator-applied) | CLEAN (trailers parse; the Deltas block carries the entry — the native (7) 'no CHANGELOG in the commit' row is the Deltas mechanism working as designed) |
| FLOOR 35/25/30 · 12-FACTOR | CLEAN (no surface; stdout only) |
| Recurrence: Behavior Contract row without a killing test (mutation probe) | FIXED r1 → r2 (native: 4 of 10 mutants survived — the terminal-status skip, the two counting limbs, the guard order, the print order) |
| Recurrence: Touches discipline / governance untouched | CLEAN (`--name-only` = the three Touches; `final_gate.py` numstat 1/1 at r1, the wrapped call only at r2) |
| Recurrence: fleet blast radius of a synced surface | CLEAN (native (3): one-session `--check` byte-identical to master's on the same fixture; hub + 3 sessions → nothing; a bare repo at 2 sessions fires with only the applicable part) |
| Recurrence: proxy-as-evidence — the ticket gate RUN in the worktree by the orchestrator | CLEAN (118 passed at r1; the final_gate hunk read; `run_optional_check(advisory=True)` probed on a stub script by the orchestrator at r2) |
| Recurrence: denominator on every count | CLEAN — mutants killed of probed (6 of 10 at r1), tests passed of collected, sessions of pids, files of Touches |
| Recurrence: fail-open/fail-closed | FIXED r1 (contract): the plan's row 4 assumed a passing advisory row surfaces in `--check --json`; executed: the Drift call is tier-3 and a green row's stdout is unreachable in JSON — row re-cut to the behavioural `run_optional_check` test; the JSON surfacing filed as a backlog row at T06 |
| Recurrence: cost/quota accounting | CLEAN (pool r1 3/3/2 scored; native opus×1 r1 + r2) |
| Recurrence: boundary/sentinel/prefix | FIXED r1 → r2 (the `>= 2` boundary killed at r1; the guard ORDER — files read before the count — was the survivor, D5) |
| Recurrence: behavior-without-a-test | FIXED r1 → r2 (D1, D2, D3, D6) |

## Round 1 — over `ac72db17..a72b44af`
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.

### Adjudication (pool layer)
- deepseek-v4-flash (3/5): the docstring-only gate-wiring test is syntactic (ACCEPTED → D3, a behavioural `run_optional_check` test); the all-three-limbs case untested (ACCEPTED, folded into D2); "the exit-code test proves content-independence only" — NOTED, the native (2) executed the invariant on both docs states.
- gemini-3-flash (3/5): CLEAN with the single-session early exit traced at `:1846`. Accepted.
- qwen3-max (2/5): the gate-wiring gap — ACCEPTED (same as above); "a stray manifest file misidentifies the hub" — REFUTED (hub-only file, never synced; the hook uses the same test); "a bullet `- [alpha]` is not a tagged row" — REFUTED (bullets are one of the three shapes, T02b).

### Native finder (opus) — 10 mutants on a copy (baseline 95 passed), 8 hunt items executed; 5 raised
- Killed: `>= 3` (2 tests), hub suppression dropped (1), advisory appended to `issues` (1), dropped from the valid path (2).
- **D1 H** — `:1753` the EXECUTED/COMPLETE skip in `_count_unowned_plans` survives deletion (archived unowned plans would fire forever). **D2 H** — `:1797` forcing the untagged count to 0 survives; no assertion names "unowned plans" / "untagged backlog rows" — BC row 3's fixture covaries the three limbs. **D3 H (plan-text)** — the `Documentation Drift` call is under `if tier == 3` (`final_gate.py:1783`) and a GREEN advisory row's stdout is unreachable in `--check --json` (`WARN_ONLY_CHECKS`/`warnings` only): BC row 4 as written was unachievable under the ticket's DO-NOT → the orchestrator re-cut the row (ticket + spine) to the behavioural test and filed the JSON surfacing for T06's backlog Deltas. **D4 H** — `final_gate.py:1897` is a 106-char line `ruff format` would rewrite (the hub gate formats changed `scripts/*.py`) → wrap it. (7) "no CHANGELOG in the commit" — REFUTED: governance files are orchestrator-applied Deltas by the plan grammar. Findings L/M: print order unpinned (→ D6); the `n < 2` guard order unpinned (→ D5).
- CLEAN by execution: the exit-code invariant on valid AND invalid docs states (2); blast radius — one-session output byte-identical to master, hub silent at 3, bare repo fires with only the applicable part (3); the string format (5); no file read below 2 sessions (6); Touches/ruff/stdlib/`test_docs_updater.py` unmodified (7).

Verdict r1: 6 items (D1–D6) → fixup dispatched to the same coder; the ticket's row 4 amended on the spine. Board T03 stays 🔵.

## Round 2 — over `ac72db17..34ed7a86` (the round-1 fixup 34ed7a86: tests +131, `final_gate.py` 3/1 — the Drift call wrapped; `scripts/docs_updater.py` byte-identical between the two commits: D1/D2/D5/D6 were test-coverage gaps, not implementation bugs)
Finders: pool deepseek/deepseek-v4-flash ×2 + deepseek/deepseek-v3.2-exp (a single-family draw — noted, the pool's diversity reorder did not hold this batch) + native opus×1 — round 2.

### Adjudication (pool layer)
- deepseek-v4-flash (3/5), deepseek-v3.2-exp (4/5), deepseek-v4-flash (4/5): all CLEAN, every row mapped to its test by name. Accepted.

### Orchestrator execution
- In the coder's worktree: `pytest tests/test_docs_updater_adopt.py tests/test_docs_updater.py -q` → **124 passed**; `ruff check` clean on all three files; `ruff format --check` clean on all three (no pre-existing drift in `final_gate.py`).
- The `final_gate.py` hunk: exactly the Drift call, wrapped over three lines with `advisory=True`.
- `run_optional_check` probed directly on a stub script printing `ADVISORY: probe line` (PROJECT_ROOT pointed at a tmp dir): `advisory=True → passed, carries the line`; `advisory=False → passed, line dropped` — the mechanism the re-cut row 4 asserts.

### Native finder (opus) — the six round-1 mutants re-applied on a copy: ALL KILLED by their named tests (D1 the terminal skip; D2 both counting limbs, the combined line `merge owner undeclared; 1 unowned plans; 1 untagged backlog rows`; D3 `run_optional_check` returning `""` killed by the stub-script test in both directions; D4 numstat 3/1 confined to the Drift call; D5 the TRUE mutant — guard moved below the reads while still returning `[]` — killed only by the never-read test; D6 print order). CLEAN by execution: `ruff format --check` clean on all three files (the wrap is required: `advisory=True` pushes the call past `line-length = 100`, pyproject.toml:62); `tests/test_docs_updater.py` untouched and green; one-session `--check` byte-identical to master's on both the pass and the forced-failure branch; Touches exact; 61 test defs, 0 duplicates; combined with the two final_gate advisory suites 142 passed. VERDICT: CLEAN.

Verdict r2: found: 0, fixed: 0 — **CONVERGED**. Merge target: `worktree-agent-a16957e0307ad8194` @ 34ed7a86. Fleet-synced RUN_SCRIPT → `sync_enforcement_to_projects.py --force` after the merge; the `final_gate.py` JSON surfacing of passing advisory rows is a STRATEGIC_BACKLOG row at T06.

## Pass Ledger
| Pass | Layer | Method | Findings → fixed |
|---|---|---|---|
| Pass 1 | pool ×3 + native opus×1 | method: re-derivation (10 mutants; the exit-code invariant and the blast radius executed on fixtures; the gate's tier/JSON paths read at final_gate.py:1783/:2570-2597) | 5 native + 1 pool → 6 (row 4 re-cut on the spine) |
| Pass 2 | pool ×3 (single-family draw) + native opus×1 + orchestrator execution | method: re-derivation (six mutants re-applied and killed; `run_optional_check` probed on a stub; 124 tests re-run) | 0 → 0 |
