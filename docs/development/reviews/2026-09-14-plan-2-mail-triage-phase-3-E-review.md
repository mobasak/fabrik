# Review — 2026-09-14-plan-2-mail-triage-phase-3-E

**Status:** CONVERGED (2026-09-15) on the D-252 scope-growth stop, not on a quiet exit round — rounds 3 and 4 each confirmed ONLY defects lying inside this review's own previous fixes (12/12 then 8/8), which is the stop by its own definition. The GRADED surface (`84f88595..e883d1ff`) was last swept whole in round 2 and confirmed zero. Every CONFIRMED defect across all four rounds is fixed with a grader proven red on the mutation that removes it; the classes round 4 opened are closed by those fixes and routed below rather than re-swept, per the stop's own instruction.
**Surface:** `git rev-parse HEAD` = ef87f2463491b24006b81df1a5110a59424a8b71; range tip e883d1ff9a5c7d781eb46e827ea439eb042dcd66; `git diff 84f88595..e883d1ff -- CHANGELOG.md CLAUDE.md INDEX.md docs/DECISIONS.md docs/STRATEGIC_BACKLOG.md docs/development/plans/archived/2026-09-12-plan-2-mail-triage-command-machinery.md docs/workflows/FINAL_GATE_WORKFLOW.md scripts/doc_reconcile.py scripts/docs_updater.py scripts/enforcement/_doc_registry.py scripts/enforcement/check_decisions_unique.py scripts/enforcement/check_doc_index.py scripts/enforcement/check_script_headers.py scripts/enforcement/check_structure.py scripts/final_gate.py scripts/rivals_run.py scripts/scratch_sweep.py scripts/sync_enforcement_to_projects.py templates/governance/CLAUDE.md tests/enforcement/test_check_decisions_unique.py tests/enforcement/test_check_doc_index.py tests/enforcement/test_check_script_headers.py tests/test_check_structure_docs_allowlist.py tests/test_docs_updater_dry_run.py tests/test_final_gate_advisory_display.py tests/test_final_gate_tier_counts.py tests/test_rivals_run.py tests/test_scratch_sweep_sync_materialised.py tests/test_sync_head_source.py` md5 0e06fef188d532918dd7bf8d9c686cdd (174089 bytes)
**Command:** /fabrik-review · **Changed:** `CHANGELOG.md`, `CLAUDE.md`, `INDEX.md`, `docs/DECISIONS.md`, `docs/STRATEGIC_BACKLOG.md`, `docs/development/plans/archived/2026-09-12-plan-2-mail-triage-command-machinery.md`, `docs/workflows/FINAL_GATE_WORKFLOW.md`, `scripts/doc_reconcile.py`, `scripts/docs_updater.py`, `scripts/enforcement/_doc_registry.py`, `scripts/enforcement/check_decisions_unique.py`, `scripts/enforcement/check_doc_index.py`, `scripts/enforcement/check_script_headers.py`, `scripts/enforcement/check_structure.py`, `scripts/final_gate.py`, `scripts/rivals_run.py`, `scripts/scratch_sweep.py`, `scripts/sync_enforcement_to_projects.py`, `templates/governance/CLAUDE.md`, `tests/enforcement/test_check_decisions_unique.py`, `tests/enforcement/test_check_doc_index.py`, `tests/enforcement/test_check_script_headers.py`, `tests/test_check_structure_docs_allowlist.py`, `tests/test_docs_updater_dry_run.py`, `tests/test_final_gate_advisory_display.py`, `tests/test_final_gate_tier_counts.py`, `tests/test_rivals_run.py`, `tests/test_scratch_sweep_sync_materialised.py`, `tests/test_sync_head_source.py`
**Plan:** `docs/development/plans/archived/2026-09-12-plan-2-mail-triage-command-machinery.md`

## Coverage Checklist

Rubric invocation (verbatim output — the gate reads the generated header, never a prose mention):

```text
$ python scripts/review_rubric.py --changed CHANGELOG.md CLAUDE.md INDEX.md docs/DECISIONS.md docs/STRATEGIC_BACKLOG.md docs/development/plans/archived/2026-09-12-plan-2-mail-triage-command-machinery.md docs/workflows/FINAL_GATE_WORKFLOW.md scripts/doc_reconcile.py scripts/docs_updater.py scripts/enforcement/_doc_registry.py scripts/enforcement/check_decisions_unique.py scripts/enforcement/check_doc_index.py scripts/enforcement/check_script_headers.py scripts/enforcement/check_structure.py scripts/final_gate.py scripts/rivals_run.py scripts/scratch_sweep.py scripts/sync_enforcement_to_projects.py templates/governance/CLAUDE.md tests/enforcement/test_check_decisions_unique.py tests/enforcement/test_check_doc_index.py tests/enforcement/test_check_script_headers.py tests/test_check_structure_docs_allowlist.py tests/test_docs_updater_dry_run.py tests/test_final_gate_advisory_display.py tests/test_final_gate_tier_counts.py tests/test_rivals_run.py tests/test_scratch_sweep_sync_materialised.py tests/test_sync_head_source.py
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3; TOOLING surface)

### core/10-python.md
**`uv`** is the mandated Python package manager. Never use raw `pip`, `pip install`, `poetry`, or `pipenv`.
- Dependencies live in `pyproject.toml` + `uv.lock`. Do not modify these files unless the ticket authorises it.
- its own reviewed commit, never as a side effect of unrelated work.
- The one RULE: use SQLAlchemy async consistently — never mix `async def` with sync `.query().all()` (the Banned table row; the full session pattern is `25-data-postgres.md`'s).
- The canonical `engine`, `async_session`, and `get_db` are defined in `src/database.py` — owned by `25-data-postgres.md`. Import from there, never redefine:
**Config convention:** apps read a complete `DATABASE_URL` (`postgresql+asyncpg://user:pass@host:port/db`) and `REDIS_URL` from env. Discrete `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD` for the app to assemble are **banned**. The env supplies the complete URL — `localhost` in WSL, `postgres-main` on VPS — so the host concern is an env-layer responsibility, never code logic. See `30-ops.md` compose template for how discrete vars are interpolated into `DATABASE_URL` at the compose level.
- volume** (`30-ops.md` § Volumes), never in `.tmp` and never in `/tmp`.
**GlitchTip discipline:** unhandled exceptions (FastAPI 500s) are auto-captured by GlitchTip with full stacktraces. In the `except Exception` branch, log a **short event name + correlation_id** — never `logger.exception()` (that duplicates the traceback in Loki AND GlitchTip). See `55-observability.md` § Error Reporting for the full rule.
**Note:** Use the scaffolded logger: `from {package}.logger import get_logger` (see `55-observability.md` § Pre-Scaffolded Logging). Do not use `structlog.get_logger()` directly or `logging.getLogger(__name__)`.
- **Never a bare `asyncio.create_task()`** — an unreferenced task is silently garbage-collected and its exceptions vanish. Hold the reference and await it, or use `asyncio.TaskGroup`.
- **`datetime.now(UTC)`, never `datetime.utcnow()`** — deprecated and naive; naive datetimes are a real cross-service defect class.
- Ruff's selected rule-sets MUST include `ASYNC` (blocking IO in async code — machine-enforces this pack's hardest-to-review rule), `B` (bugbear) and `S` (bandit) alongside the defaults; configured in `pyproject.toml`, emitted by the scaffolder.
- Production services run via `uvicorn` CLI in the Dockerfile, not `uvicorn.run()` in code. Base image is always the pinned Debian `-slim` variant on `linux/amd64` (the variant is pinned fleet-wide in `30-ops.md` § Container Base Images — change it THERE, never per-repo). Never use Alpine — musllinux wheels exist now (PEP 656) but coverage is still partial, source builds are dramatically slower, and musl's allocator/stack defaults degrade CPython; the trade never pays on this fleet.
- `uvicorn.run()` is for local development only. Never ship it in production code.
- a fleet scaling decision (more containers), never a per-app flag.
**BANNED: grouped/named env config sets.** 12F is explicit — *"env vars are granular controls, each fully orthogonal to other env vars"* — so a `config/production.yml`, a `settings.production` group, or a `config/{dev,staging,prod}.yaml` tree is a violation. Env vars are granular and set **per deploy**, never batched into a named "environment".
**BANNED:** `logging.FileHandler`, `logging.handlers.RotatingFileHandler`, `TimedRotatingFileHandler`, `loguru` file sinks, any `*.log` file write, any in-app log rotation/retention/cleanup. The app never decides where logs are stored or routed — Docker → Promtail → Loki does. Full rule: `55-observability.md` § Logs.
**Factor XII — Admin processes. NEVER migrate from app startup.**
**BANNED: `alembic upgrade head` in FastAPI's `lifespan`, in an `@app.on_event("startup")`, or as an import side-effect.** With more than one replica (or a restart storm) two containers run `upgrade head` **concurrently** → they race the Alembic version table → duplicate DDL → **wedged deploy**. Migrations are a **one-off admin process against the deployed release**: `docker compose run --rm <svc> alembic upgrade head` (see `30-ops.md` § Release & Admin Processes).

### 12-FACTOR (all twelve axes)
- I codebase: shared code → fabrik-lib, never two apps in one repo
- II deps: every shelled-out binary installed + pinned in the Dockerfile
- III config: granular env vars; no secrets in code; no grouped env sets
- IV backing services: swappable by DSN/config change only
- V build/release/run: releases immutable; never hot-patch a container
- VI processes: stateless; session state → redis-main; no sticky sessions
- VII port binding: bind in-container; Traefik routes; no host ports:
- VIII concurrency: scale out; never daemonize or write PID files
- IX disposability: SIGTERM returns in-flight jobs to the queue; jobs idempotent
- X dev/prod parity: same backing services everywhere; no SQLite-for-Postgres
- XI logs: unbuffered stdout only; the app never writes/rotates a logfile
- XII admin: migrations/one-offs run against the deployed release, never startup

## MATCHED — packs whose globs hit the changed paths

### core/40-documentation.md  (hit: CHANGELOG.md, CLAUDE.md, INDEX.md)
- > **⚠️ `docs/OPERATIONS.md` + `docs/DEPLOYMENT.md` are FLEET-AI INTERFACES, not just docs (D-065).**
- **Tier-1 (author → verify → converge; the author leg is NATIVE while the pool is OFF, D-181 — `scripts/doc_reconcile.py`'s pool author cannot dispatch):** for each **mechanically-detectable** doc whose Doc-Sync trigger fired (`docs/QUICKSTART.md` · `docs/CONFIGURATION.md` · `docs/data-contract.md` · `docs/SERVICES.md` · `docs/OPERATIONS.md` — the reliable-signal subset), `scripts/doc_reconcile.py` dispatches a cheap OpenRouter-pool author (`libs.subagents`, `pick_models("docs")`) to emit a **minimal structured patch**, **verifies it before applying** (a symbol cross-check catches invented endpoints; the orchestrator injects a higher-assurance native-Claude verify), and loops to a zero-edit round. Runs per phase in `/fabrik-execute-plan`; never blocks (fail-safe). The other docs (CHANGELOG, INDEX, FEATURES, RESILIENCE, PORTS, the READMEs, `db/schema.sql`, …) have no reliable mechanical content-signal → they rely on the touch-on-change backstop below + your own edit (force-update, not force-correct).
- The SSOT is the type-aware registry (`scripts/enforcement/_doc_registry.py::PROJECT_DOCS`) — this table is its project-facing rendering, kept in step, never a second truth. `/fabrik-plan-after-chat` (the plan set's spine + tickets — the ticket-format authority) injects these rows per ticket as its `Docs:` line.
- Standalone work (not plan execution) → `Agent-Role: primary`. Trailers go below a blank line, above `Co-Authored-By`. ⚠️ The trailer block must be its OWN paragraph with NO blank line inside it: git parses only the LAST paragraph, and only if it is all-trailers. A blank line before `Co-Authored-By:` demotes everything above it to prose; so does a prose line glued to the top of the block. Measured 2026-08-15: 200 of the last 200 hub commits carried `Agent-Role:` and only 10 parsed, because the old example here shipped the blank line.
- **⚠️ Link it or it is decoration.** *Measured:* requests for files that do NOT exist came ~zero from AI bots — agents never go looking. It follows (inference, not measurement) that a file only gets read when something points at it: reference it from the docs index or README.
- ⚠️ **In THIS repo `llms.txt` is GENERATED** (`scripts/generate_capability_index.py`, refreshed daily) — never hand-edit it; change the generator. A project writing one by hand owns it.
- either way. Cheap and reversible — never at the expense of `OPERATIONS.md`/`DEPLOYMENT.md`, which are the load-bearing agent interfaces (D-065).
- **No skipped heading levels** — `##` to `###`, never `##` to `####`
- **Fenced code blocks only** — never indented code (AI treats it inconsistently)

### core/45-testing-strategy.md  (hit: tests/enforcement/test_check_decisions_unique.py, tests/enforcement/test_check_doc_index.py, tests/enforcement/test_check_script_headers.py)
- **Behavior Contract**: every ticket enumerates its distinct **user-observable behaviors / acceptance criteria** and tests **each one** — one high-value integration/E2E test per behavior, risk-ordered, TDD for the risky ones. Skip trivia (getters / framework glue / config): **lean-but-complete, NOT 100%-line-coverage dogma**. Do not chase line coverage — ensure every behavior has a test that would fail if that behavior regressed. (Cheap pool subagents can author the per-behavior tests — the suggest→curate→author→fix workflow in `62-using-subagents.md` § Dispatch policy + `~/.claude/commands/fabrik-review.md`.)
- **No cosmetic assertions**: never assert against CSS classes, Tailwind utility strings, pixel measurements, or snapshot hashes. Assert application state and user-visible outcomes only.
- **Watched-fail-first** (for tests this change adds or modifies; trivia stays skipped per the Behavior Contract): a non-trivial behavior's test proves something only if it has been SEEN RED — either write it first and watch it fail, or (after the fact) neuter the fix/feature, prove the test goes red, then RESTORE and re-run to green. The neutered state is never staged, committed, or left in the tree. A green test never seen red is unverified — a suite can pass with its guard deleted.
- **Run tests**: `uv run pytest tests/` (never bare `pytest` — Fabrik uses `uv`) — **when the project has a `pyproject.toml`/`uv.lock`**. A `requirements.txt`-only project (no manifest) runs `.venv/bin/python -m pytest tests/` — the manifest clause chooses the RUNNER, it never disarms the mandate to run the suite (web-ecommerce-factory 01M1QEY5, 2026-09-05: the clause read as "does not apply here"). ⚠️ **Gate this on the manifest, because this line is FLOOR-injected into finder prompts and a vendored fabrik-lib MODULE has neither by design**: the module recipe ships `requirements.txt` (`fabrik-lib/README.md` § Creating a Reference Implementation), so `uv run` cannot resolve it and `python3 -m pytest` is the only thing that works. Telling a finder the sole working … (wrapped further — read the pack)
- **Zero-mock database policy**: never mock SQLAlchemy, SQLModel, or database sessions. All backend tests execute against a real PostgreSQL instance.
- **`ASGITransport` never runs lifespan** — anything the app initializes at startup (scaffolded apps are lifespan-based) silently does not exist in tests; wrap with `asgi-lifespan`'s `LifespanManager` when a test needs startup state.
- Use `structlog` in test helpers if logging is needed — never `print()`. See `55-observability.md`.
- **Never stub a server action from Playwright** — the server is the E2E boundary; stubbing belongs in the unit lane where the action is a plain function.
- Run Playwright against the PRODUCTION build (`next build && next start`), never the dev server.
- All locators must be **semantic**: `page.getByRole('button', { name: /submit/i })`. Never use CSS selectors or XPath.
- Launch Playwright's **bundled Chromium** (`channel: 'chromium'`) — stable Chrome/Edge removed the `--load-extension` / `--disable-extensions-except` side-load flags (Chrome 137/139), so those args only work under bundled Chromium, never installed stable Chrome.
- Run `@axe-core/playwright` with **`bypassCSP: true`** (the non-relaxable extension CSP otherwise makes axe throw on `chrome-extension://` pages); keep `@axe-core/playwright` a **dev-dependency only** (MPL-2.0 — never bundled into the shipped artifact). Gate bundle size with `size-limit` **per surface** (popup / side-panel / content-script). Full loop: `chrome-ext/70-chrome-ext.md` § Testing & UI Verification.
- Keep the generated types committed and re-generate on schema changes (`uv run python -c "import json; from <package>.main import app; print(json.dumps(app.openapi()))" > openapi.json` — the scaffold emits `src/<package>/main.py`, never a flat `src/main.py`, so `src.main` imports nothing).
**BANNED in tests:**
| A GUARD proven only by the ONE spelling of the defect you already fixed | Write the guard's subject five LEGITIMATE ways — five a DIFFERENT author would plausibly write, not five typos of yours — and count how many it still catches; one of five means it is keyed on your fix, not on the class — and one of five is the FLOOR of the failure, never its definition: four of five is a partial class and is reported as four of five. This is IN ADDITION to red-on-revert below, not a rival bar: that one proves the guard fires at all, this one proves it fires on the class. ⚠️ Cheapest ways to satisfy it WITHOUT the outcome (`CLAUDE.md` § UNIVERSAL governance markers, the entry whose anchor is **you get the behavior you measure** — search the ANCHOR, not the rule name: the project-facing contract lists that section by anchor alone and carries the name `cobra-effect` nowhere): (i) write five near-identical spellings and count 5/5; (ii) ship at 2/5 and REPORT it, needing no fabrication at all, in the hope that a reported count reads as a passed one — it does not: under 5/5 is a finding; (iii) claim the exercise and record nothing, since the five are never committed. So the bar is TWO things and needs both: **the five go IN the test file as executable CASES**, never a comment — a comment cannot go RED, so nothing can falsify it, and that is the objection, not that it records nothing — **and anything under 5/5 is a finding, not a pass**. ⚠️ Two paths this row does NOT close, stated rather than pretended away: you can shrink the SUBJECT until five legitimate spellings all land inside what the guard already catches (nothing is fabricated; the claim narrowed, not the guard), and an honest 4/5 — real information, 80% of the class — costs the author something to report, so the cheapest response to it is silence. Report the count you got either way — a 4/5 with the miss NAMED is a finding someone can act on, and a 5/5 nobody can execute is not a pass at all. Measured 4× in one day across 2 repos (01M1S4D78KRM0ZSYDNGTHS9HYQ), and once more the day this row landed: a contract-parity grader that read the LIVE file instead of the tree under test stayed green under the exact drift it existed to catch |
| A test THIS change adds/modifies that was never seen red (no fail-first, no red-on-revert proof) | Watch it fail first, or neuter the change → prove red → restore → re-run green |
- [ ] Destructive DB tests call `require_throwaway(TEST_DATABASE_URL)` before connecting — never point them at a dev/shared DB.

# promote-to-check_*: 34 injected mandate(s) look deterministically greppable — their backtick literals, one line each (the full mandates are ABOVE, not repeated: re-emitting ~20 FLOOR lines verbatim doubled the rubric and got it skimmed — web-ecommerce-factory 01M1QEY5, 2026-09-05)
- `uv` `pip` `pip install` `poetry` `pipenv`
- `pyproject.toml` `uv.lock`
- `async def` `.query().all()` `25-data-postgres.md`
- `engine` `async_session` `get_db` `src/database.py` `25-data-postgres.md`
- `DATABASE_URL` `postgresql+asyncpg://user:pass@host:port/db` `REDIS_URL` `DB_HOST` `DB_PORT` `DB_NAME` `DB_USER` `DB_PASSWORD` `localhost` `postgres-main`
- `30-ops.md` `.tmp` `/tmp`
- `except Exception` `logger.exception()` `55-observability.md`
- `from {package}.logger import get_logger` `55-observability.md` `structlog.get_logger()` `logging.getLogger(__name__)`
- `asyncio.create_task()` `asyncio.TaskGroup`
- `datetime.now(UTC)` `datetime.utcnow()`
- `ASYNC` `B` `S` `pyproject.toml`
- `uvicorn` `uvicorn.run()` `-slim` `linux/amd64` `30-ops.md`
- `uvicorn.run()`
- `config/production.yml` `settings.production` `config/{dev,staging,prod}.yaml`
- `logging.FileHandler` `logging.handlers.RotatingFileHandler` `TimedRotatingFileHandler` `loguru` `*.log` `55-observability.md`
- `alembic upgrade head` `lifespan` `@app.on_event("startup")` `upgrade head` `docker compose run --rm <svc> alembic upgrade head` `30-ops.md`
- `docs/OPERATIONS.md` `docs/DEPLOYMENT.md`
- `scripts/doc_reconcile.py` `docs/QUICKSTART.md` `docs/CONFIGURATION.md` `docs/data-contract.md` `docs/SERVICES.md` `docs/OPERATIONS.md`
- `scripts/enforcement/_doc_registry.py::PROJECT_DOCS` `/fabrik-plan-after-chat` `Docs:`
- `Agent-Role: primary` `Co-Authored-By` `Co-Authored-By:` `Agent-Role:`
```

| Class | Status |
|---|---|
| Hunt: `CHANGELOG.md` — every changed hunk, its enclosing function, its callers | CLEAN (`CHANGELOG.md` — the Phase E entries under `[Unreleased]`; each claim re-read against the commit it names, and the two that overstated a fix were rewritten in the entry itself, not appended to) |
| Hunt: `CLAUDE.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (`CLAUDE.md` § EXIT worktree-verdict list gained `wt-sync-only`, and the Tier-3 clause that claimed `--systemic` checks ports and deps was corrected — both UNWIRED; byte-identical to `templates/governance/CLAUDE.md`) |
| Hunt: `INDEX.md` — every changed hunk, its enclosing function, its callers | CLEAN (`INDEX.md` — the rows this phase added for the four new test files and the review receipt; each path `ls`-verified to exist at HEAD and each row's description re-read against the file's own docstring) |
| Hunt: `docs/DECISIONS.md` — every changed hunk, its enclosing function, its callers | CLEAN (`docs/DECISIONS.md` — the rows minted this phase re-read whole; ids re-derived with `decisions.py --next-id .` at commit time, no duplicate id, every row at the separator's width) |
| Hunt: `docs/STRATEGIC_BACKLOG.md` — every changed hunk, its enclosing function, its callers | FIXED r3 (`docs/STRATEGIC_BACKLOG.md:192` — the INDEX row's HEADING still carried `584 of 863` while its body said 865, and a heading is what a TOC and a grep show; re-derived `~585 of ~866`, dated, tilde'd because it moves. The bandit row named THREE exclusions where the gate uses FIVE, so a reader reproducing it got 37 and `B608 × 8` against the row's 36 and `× 7`) |
| Hunt: `docs/development/plans/archived/2026-09-12-plan-2-mail-triage-command-machinery.md` — every changed hunk, its enclosing function, its callers | CLEAN (`docs/development/plans/archived/2026-09-12-plan-2-mail-triage-command-machinery.md` — the Phase E step markers and their Evidence blocks; every cited `path:line` re-opened at HEAD and every fenced command output re-executed) |
| Hunt: `docs/workflows/FINAL_GATE_WORKFLOW.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (`docs/workflows/FINAL_GATE_WORKFLOW.md:220` contradicted itself inside one sentence — 36/55 beside 54/34; every tier number re-derived and pinned by `<!-- GATE-COUNTS: -->` with `tests/test_final_gate_tier_counts.py` as its grader) |
| Hunt: `scripts/doc_reconcile.py` — every changed hunk, its enclosing function, its callers | CLEAN (`scripts/doc_reconcile.py:43-48` — the changed hunk, its enclosing function and both callers read whole; the registry lookup it added is total over `PROJECT_DOCS` and raises on an unknown key rather than returning a default) |
| Hunt: `scripts/docs_updater.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (`scripts/docs_updater.py::run_adopt` — `--adopt --dry-run` could never print `(nothing to adopt)` in any repo with an epics dir; `dry_run` now guards every write and `_epic_assign_would_change()` runs the assignment against a COPY so the dry-run report matches the real one) |
| Hunt: `scripts/enforcement/_doc_registry.py` — every changed hunk, its enclosing function, its callers | CLEAN (`scripts/enforcement/_doc_registry.py::PROJECT_DOCS` — every added key read against the doc it names; the keys are consumed by `doc_reconcile.py` and `check_doc_index.py`, both re-run over this tree at HEAD) |
| Hunt: `scripts/enforcement/check_decisions_unique.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (`scripts/enforcement/check_decisions_unique.py::_DELIMITER` took the first dash-ish line anywhere, so a two-character `| |` silenced a now-blocking check; anchored to a real `_HEADER_ROW`, cobra path written into the docstring, blast radius measured 0 of 49 fleet ledgers) |
| Hunt: `scripts/enforcement/check_doc_index.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`scripts/enforcement/check_doc_index.py` gained `--quiet` — `advisory=True` preserves ALL stdout, so the clean-path OK banner became a green line on every human gate run while lacking the ⚠ the JSON filter keys on: chatter in one mode, silence in the other; findings are never suppressed by the flag) |
| Hunt: `scripts/enforcement/check_script_headers.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (`scripts/enforcement/check_script_headers.py` read the INDEX blob while claiming the working tree, so an added coupling passed green and a removed one warned about a line that no longer exists; `_working_head()` added and the scope named in every output line) |
| Hunt: `scripts/enforcement/check_structure.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`scripts/enforcement/check_structure.py` — `if not declared:` could not tell `None` (no SCAFFOLD_TYPES assignment at all) from `set()` (found it, no string literals), so a renamed constant sent the reader hunting a comprehension that does not exist; the two states now report differently) |
| Hunt: `scripts/final_gate.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`scripts/final_gate.py` — narrowing the two ruff legs to `get_writable_files()` made them VANISH for the author's own unstaged work: measured end to end, two real ruff errors reported as `status: success, skipped: 0, ruff rows: []`; a narrowed-to-nothing leg is now a `(SCOPE-NARROWED …)` row carrying a `_SKIP_MARKERS` token, and the advisory row no longer prints twice nor renders `[PASS]`) |
| Hunt: `scripts/rivals_run.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (`scripts/rivals_run.py` — the fail-open note named only the exception class, so an operator could not tell WHICH credential was absent; it now names the absent keys) |
| Hunt: `scripts/scratch_sweep.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`scripts/scratch_sweep.py::_sync_materialised_paths` materialised a whole `rglob` before `all()` could short-circuit — `.venv/` measured 1.27 s against 0.006 s for the identical verdict; the expansion is lazy now, 227×, with the empty-directory case preserved) |
| Hunt: `scripts/sync_enforcement_to_projects.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`scripts/sync_enforcement_to_projects.py::sync_single_file` still hashed the WORKING TREE while the writer shipped HEAD bytes — a drifted file ran COPY·COPY·COPY over three runs instead of COPY·SKIP·SKIP, and a project copy already holding the hub's uncommitted bytes was SKIPped as identical; `_head_source` also memoised and the drift report's verb bound to the branch) |
| Hunt: `templates/governance/CLAUDE.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (`templates/governance/CLAUDE.md` — the same two clauses as `CLAUDE.md`, diffed byte-for-byte after the edit so the shared clauses stay identical across the ~46 projects this file is distributed to) |
| Hunt: `tests/enforcement/test_check_decisions_unique.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`tests/enforcement/test_check_decisions_unique.py` — the anchored-delimiter fixture `| |` is rejected by the repaired `_DELIMITER` outright, so the anchor was never reached: executed both arms, deleting the anchor left the result byte-identical. The fixture is now `| - |`, a real delimiter, with the dash requirement graded separately) |
| Hunt: `tests/enforcement/test_check_doc_index.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`tests/enforcement/test_check_doc_index.py` gained the `--quiet` grader: the banner is silenced, every ⚠ finding and every `ERROR:` line the loud run printed is asserted still printed by the quiet one, so the flag cannot grow into a finding-suppressor) |
| Hunt: `tests/enforcement/test_check_script_headers.py` — every changed hunk, its enclosing function, its callers | CLEAN (`tests/enforcement/test_check_script_headers.py` — the pre-stage fallback graders hand the function distinct inputs and assert distinct outputs, so a stub returning one value for every scope cannot pass them) |
| Hunt: `tests/test_check_structure_docs_allowlist.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`tests/test_check_structure_docs_allowlist.py` gained the grader that builds BOTH scaffold shapes — a module with no `SCAFFOLD_TYPES` and one whose assignment carries no literals — and asserts the two messages differ; a single message for two facts is what it now kills) |
| Hunt: `tests/test_docs_updater_dry_run.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (`tests/test_docs_updater_dry_run.py` — added with the `run_adopt` dry-run fix and proven red against the unfixed function; the copy-based `_epic_assign_would_change` assertion distinguishes a real dry run from a no-op that prints the same line) |
| Hunt: `tests/test_final_gate_advisory_display.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`tests/test_final_gate_advisory_display.py` — two graders asserted literal SOURCE SUBSTRINGS of the fix (`"changed if fix_all else changed & get_writable_files()"`), so they could not fail for any semantics, sat beside the silent-narrowing defect, and were invalidated by this round's own reformat; all rewritten behaviourally with scope-discriminating stubs) |
| Hunt: `tests/test_final_gate_tier_counts.py` — every changed hunk, its enclosing function, its callers | CLEAN (`tests/test_final_gate_tier_counts.py` — the grader that pins `FINAL_GATE_WORKFLOW.md`'s `<!-- GATE-COUNTS: -->` comment to the gate's live registry; re-run at HEAD and watched red by editing one tier number in the comment) |
| Hunt: `tests/test_rivals_run.py` — every changed hunk, its enclosing function, its callers | CLEAN (`tests/test_rivals_run.py` — the fail-open-note grader asserts the absent KEY NAMES appear in the message, not merely that a message exists, so the pre-fix exception-class-only note fails it) |
| Hunt: `tests/test_scratch_sweep_sync_materialised.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (`tests/test_scratch_sweep_sync_materialised.py` — added with the `_is_sync_materialised` fix; two graders SKIP when the sampled synced file sits at the repo root, so the sampled path is asserted non-root rather than left to chance) |
| Hunt: `tests/test_sync_head_source.py` — every changed hunk, its enclosing function, its callers | FIXED r3 (`tests/test_sync_head_source.py` — the mtime grader compared two files both born inside the test, so `abs(…) < 1` was satisfied by test SPEED: executed with `os.utime` neutered in BOTH writers it still passed. It now pins an old mtime and asserts equality, and four graders were added for the convergence, the uncommitted-bytes case, the report verb and the cache) |
| Recurrence: fail-open/fail-closed — a swallowed error or an absent check that reads as success | FIXED r1 (three in this surface: `_scaffold_registry_drift` returned `[]` when it found no string literals — an absent check reading as success, now an explicit "parity was NOT checked" finding; `check_doc_index.py`'s advisory stdout discarded on exit 0; `_DELIMITER` silenced by a bare `| |`) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted, a limit at its edges | RECORDED — by design (D-181). The OpenRouter pool is OFF by ruling, so no pool unit is scored anywhere in this surface; the native seats of this review are counted by `dispatch_headroom.py` and stamped with `command_run.py dispatch --seats`, both executed before each round |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | FIXED r1 (`skip_advisory` took the FIRST regex match where the meaningful one is the LAST, and missed `deselected` entirely; `SKIP_PATTERNS` is a substring test and its two new lock-dir entries were checked for over-match; `_DELIMITER` is the prefix-vs-exact case, now anchored) |
| Recurrence: behavior-without-a-test — a contract row no test kills (mutation asserted) | FIXED r1 (four behaviours shipped this phase carried no grader — the `run_adopt` dry run, `_shipped_hash`/mtime, `_is_sync_materialised`, and the `--json` skip notice; each gained one, and each was watched RED by revert-in-place, never by copy-tree — test files here hardcode `REPO = Path("/opt/fabrik")` and a copy silently passes) |
| Recurrence: denominator on every count — bounded searches state their bound | FIXED r1 (`584 of 863` was stale by the time it landed — it is 584 of 865, and the two extra files are ones this phase itself added; the bandit row named two excluded subtrees where the gate excludes three; every count in this surface re-derived with `command grep` named beside it) |
| Hunt: `scripts/enforcement/check_review_coverage.py` — in-run fix, outside the graded surface | FIXED r3 (`scripts/enforcement/check_review_coverage.py` knew three exits — quiet, `## BLOCKED:`, `Status: IN-PROGRESS` — and refused D-252's scope-growth stop, so a review that ended correctly on it graded as unconverged and reddened the SHARED gate. `_scope_growth_exit` demands the header-zone declaration AND two consecutive confirming ledger rows, with the cobra path in its own docstring; fire rate measured 1 of 826 fleet review artifacts before shipping) |
| Hunt: `tests/test_check_review_coverage_scope_growth.py` — the seven graders for that exit | FIXED r3 (`tests/test_check_review_coverage_scope_growth.py` — each proven red on the mutation that removes its half: un-wiring the exit reds the acceptance test, removing the ledger-evidence guard reds both the cobra test and the unexecuted test, and the lockstep test reads `SCOPE_GROWTH_ROUNDS` back out of `command_run.py` so the hand-copied twin cannot drift) |
| Recurrence: grader-cannot-fail — a test that passes with its own fix removed | FIXED r3 (four in this surface, all executed: the mtime grader passed with `os.utime` neutered in both writers; the anchored-delimiter fixture never reached the anchor; two gate graders asserted literal source substrings. Every grader added or repaired since is proven RED on the mutation that removes its fix, by revert-in-place with the backup md5-verified) |
| Recurrence: proxy-as-evidence — the real check EXECUTED, not read | CLEAN (every verdict in this receipt was EXECUTED: each grader run, each enforcement script run over this tree at HEAD, each fleet claim measured across the 49 repos carrying a `scripts/` dir with `.py` — never read, grepped or inferred) |

Verdict grammar (the gate refuses anything else): `CLEAN (<the paths/lines hunted>)` — a CLEAN row
must name a path and run past 70 characters · `FIXED r<n> (<what changed>)` · `REFUTED (<the
disproving line>)` · `RECORDED — unexecuted (<why>)` · `RECORDED — by design`, parenthesising the
owning row's first-cell id and the EARLIER round that adjudicated it, or a `D-nnn` with no round
(the § Residual block below shows the shape) · `RECORDED — measured (<why>)` ·
`RECORDED — hygiene false positive (<why>)` — every RECORDED reason is PARENTHESISED, never
colon-delimited (a colon would spell the `unexecuted: N` counter the ledger refuses). `UNCHECKED`
may survive only under a `## BLOCKED` escalation (a finding + 3 failed attempts).

## Pass Ledger

ONE table, one row per pass, counts punctuated (`found: F, new: N, confirmed: C, fixed: X,
unexecuted: U`) — the gate reads the LAST row as the exit round and refuses a second ledger group.
`found:` counts raw candidates, `new:` is prose the graders do not parse, `confirmed:` counts the
candidates EXECUTED and reproduced (the exit counter — a round is quiet at `confirmed: 0` and
`fixed: 0` with `unexecuted:` 0 or absent), `unexecuted:` counts code candidates RECORDED
unexecuted. Minimum two passes; the fixing pass is never the last; the closing pass re-derives
every count and anchor and says so in its Method cell, and its Finders cell names the seats that
read it by model token (`opus×1`, `sonnet×2`) — a round the orchestrator alone read cannot close.

| Pass | Finders | Counters | Method |
|---|---|---|---|
| Pass 1 | native opus×1 + sonnet×2 | found: 28, new: 28, confirmed: 21, fixed: 21, unexecuted: 0 | partitioned by file over `git diff 84f88595..e883d1ff`; every candidate re-executed by the orchestrator (a grader run, a script run on a copy, a quoted line) before it was counted |
| Pass 2 | orchestrator only (seats 0 — cannot close) | found: 21, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | re-sweep of the fixed class ledger over the round-1 fix diff; all ten classes swept clean, no fresh non-authoring seat read it, so the tool's TERMINAL verdict was DECLINED |
| Pass 3 | native opus×1 + sonnet×2 | found: 18, new: 6, confirmed: 12, fixed: 12, unexecuted: 0 | delta over the round-1 fix diff `e883d1ff..HEAD` (2,346 lines, 29 files), partitioned writers-and-enforcement / graders-and-claims; method: re-derivation — every candidate re-executed by the orchestrator before it was counted: the convergence probe, the end-to-end gate probe in a throwaway repo, the `os.utime`-neutered mutation, the two-arm delimiter probe and both bandit scopes. All 12 lay INSIDE round 1's own fixes; committed `ea1cd6aa` |
| Pass 4 | native opus×1 + sonnet×1, both fresh and non-authoring | found: 12, new: 4, confirmed: 8, fixed: 8, unexecuted: 0 | delta over the round-3 fix diff `980f2ca2..HEAD` (824 lines, 14 files); method: re-derivation — every count, anchor and claim re-derived from its primary source and every candidate re-executed before it was counted: the three evasion shapes against the exit regex, the real gate in a throwaway repo for the `[PASS]` row, a sibling commit landed mid-run for the memo, the prefix-sibling constant, and both bandit exclusion scopes. ⚠️ ALL 8 lay inside round 3's own fixes, which with round 3's 12/12 is the D-252 scope-growth stop; committed `bebe797e` |

Row shapes (quoted here, so the gate does not read them as passes):

```text
| Pass 1 | native opus×1 + sonnet×2 | found: N, new: N, confirmed: C, fixed: X, unexecuted: U | citation |
| Pass 2 | native opus×1 + sonnet×2 | found: 0, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | method: re-derivation |
```

## Residual

⚠️ **THE STOP'S ROUTING.** D-252 says a loop that has begun reviewing its own fixes must stop and
route the remainder to a backlog row with a named destination, because correcting prose regenerates
the surface being corrected. Four classes were OPENED by round 4 and CLOSED by its own fixes
(`exemption-reach-forward`, `skip-row-renders-pass`, `memo-blind-to-head-move`,
`lockstep-parse-fail-open`), plus two carried from round 3 (`grader-cannot-fail`,
`uncached-subprocess-per-pair`). They are not re-swept here. Their destination is
`docs/STRATEGIC_BACKLOG.md` — a row for the next review of this machinery, which will read the
round-4 fix diff (`750ef704..bebe797e`) as its ORIGINAL surface rather than as a delta, and so will
not inherit this loop's own-fix momentum.

Recorded, each with its destination, from the round-3 and round-4 seats:

- **`check_structure.py:434` — `_doc_registry is None` returns `[]`**, the same fail-silent-green
  shape round 3 fixed for `declared`, two lines above the fix. One hop out of the graded surface.
  → backlog row, same file.
- **`_atomic_copy`'s docstring claims a plain-copy fallback the code does not have** (the `except`
  unlinks the temp and re-raises). Pre-existing, re-exposed by editing its neighbourhood. → doc-only
  fix, same file.
- **`skipped` counts LEGS, not narrowings** — one dropped file yields `skipped: 2`. An agent asking
  `skipped == 0` still gets the right answer, but the count doubles on `--check`. → the
  `_summarize_skipped` docstring.
- **The sync's per-file COPY lines print only under `--verbose`** while SKIP and WARN always print,
  so a `--dry-run` names everything it will NOT touch and nothing it will. Defensible for a real
  run; it inverts the point of a dry run. Measured: `180 copied` reported, 0 named; with
  `--verbose`, 45 × 4. → backlog row, `sync_enforcement_to_projects.py`.
- **`raw.strip().strip('"')` strips git-porcelain quoting without unescaping C-style escapes**, so a
  path git quoted for a tab or newline resolves wrong. Fail-safe (the path stays "authored"). →
  backlog row, `scratch_sweep.py`.
- **The lockstep grader is not wired to any automatic runner** — the hub's pytest gate leg is OFF by
  design and there is no CI workflow, so the drift guard binds only a session that runs it by hand.
  → infra, beside the existing hub pytest-leg ledger row.
- **Four `tests/enforcement` failures**, pre-existing and already filed: three pass in isolation
  (suite-ordering pollution); `test_frontmatter_parser_matches_epic_order_verbatim` fails in
  isolation too and is a genuine `_find_fences` drift. → already in the backlog.

Every candidate that did not enter `confirmed:` is recorded here, one row each, in the verdict
grammar of `/fabrik-review` § Phase 2 (the fenced block under the next heading is an EXAMPLE, never rows).

### Verdict grammar — an EXAMPLE, never rows

(quoted so no reader — human or scan — takes this template's own sample rows for the receipt's residuals):

```text
| F12 | RECORDED — unexecuted (3 probe attempts timed out) |
| F19 | RECORDED — by design (F4, round 3; D-203) |
| F31 | RECORDED — measured (a prevalence figure; makes no code or doc claim) |
```

`RECORDED — by design` names the OWNING row's first-cell id and the EARLIER round that adjudicated
it (or a `D-nnn` with no round); the gate refuses an absent owner and a round that is not below the
closing `Pass N`. `RECORDED — measured` and `RECORDED — unexecuted` never enter `confirmed:`;
`unexecuted:` on the closing row is what keeps an unexecuted CODE candidate from closing the loop.

## Per-phase verdicts

### Phase E — T12: the gate, enforcement and sync findings (23 rows): CONVERGED on the D-252 stop

All 23 T12 rows dispositioned — 12 code fixes each with a red-first grader, 5 refuted or partly
refuted at HEAD by execution, 1 mechanism measured and rejected under FIX DIRECTIVE 5, 22 mails
replied to and acked (inbox 143 → 130). Four review rounds: 21 confirmed and fixed on the graded
surface, then a quiet full re-sweep of every class, then 12 and 8 confirmed entirely inside this
review's own fixes — the scope-growth stop.

The graded surface (`84f88595..e883d1ff`) is quiet: round 2 swept all ten of its classes and
confirmed zero, and no round since has raised a defect in it. What rounds 3 and 4 found was in the
FIXES — including one fail-open I shipped in round 3's own commit, a `Status:` reach-forward that
let any mention of the stop exempt a non-quiet review, caught by a fresh seat within the hour.

Commits: `e883d1ff` (the T12 fixes) · `ea1cd6aa` (round 3, 12 defects) · `130c6ef0` (the D-252 exit
`check_review_coverage` did not recognise) · `bebe797e` (round 4, 8 defects).

## Gate

`final_gate.py --check --json`, pasted verbatim at the flip (check_convergence reads the fenced
`"status": "success"`):

⚠️ THE SELF-REFERENTIAL SEAM, stated rather than worked around. Before this fence existed the
gate's only failure was `Convergence Evidence (plans + reviews)` — which is this check reading this
file for the proof the fence carries. So no run can be green until the paste is in, and the paste
must come from a run that was not. The block below is the run made immediately AFTER pasting, on
this tree, and it is green: that is the proof, and the sequence is the honest way to reach it.

```json
{
  "status": "success",
  "passed": 63,
  "failed": 0,
  "skipped": 4,
  "skipped_checks": [
    "bandit",
    "bandit scripts/",
    "semgrep",
    "pytest"
  ]
}
```

`skipped_checks` read beside `status`, per the contract: `bandit`, `bandit scripts/` and `semgrep`
are diff-sensed skips (this commit's own files are already committed, so the working diff carries
no `src/` or `scripts/` change) and the hub's `pytest` leg is OFF by design — 5,913 tests under
`-x` would brick every completion gate three sessions run. The suites this surface touches
were run directly instead: **179 passed** across the eleven touched files, and `tests/enforcement/`
**1382 passed** with only the four pre-existing failures already filed.
