# Acceptance review — T14b (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** CONVERGED

**Surface:** the coder's worktree branch diff against the dispatch base 3a4b5e77 (master after the T09 merge) — see the round sections below (one file per ticket, rounds APPENDED).

## Round 1 — over `3a4b5e77..cb65dafa` (five files, hit counts 9/1/1/9/4 → 0: agents-fabrik.md 37/31 — § Workflow's single-epic tier is the epic-file intake (`/fabrik-spec docs/development/epics/<n>-<slug>.md`, inheriting the Vision's fabrik-lib verdict, `owned_paths` → spine File Scope, `check_plan_tickets` enforcing ticket ⊆ spine ⊆ epic) and the multi-epic tier `/fabrik-vision` → `/fabrik-epics` → `/fabrik-epics-review` (`epic_order.py --check` → `--assign <names>` → `--check --owners`), then agent-1 in the main checkout and agents 2..N `CLAUDE_AGENT=<name> claude --worktree <name> -n <name>-<repo>` each running `/fabrik-spec <its epic>`, agent-1 merging in `epic_order` phase order and running the tail from `5-certify`; Platform rows, Phase steps, File Ownership, Key docs rewritten; agents-fabrik-core.md 5/3 — § Front door's three tiers (every claim grounded in the sources: fabrik-vision.md:11-12/:427/:874, fabrik-epics.md:120, fabrik-epics-review.md:444-470, fabrik-spec.md:71/:105); commands/_fragments/grounding-rules.md 1/1 — the `:1` HTML comment names it a RENDER INPUT inlined by FIVE sources (the ticket said two; T09's three sources also carry the directive), no literal `{{…}}`; the north-star 48/44 — live sections present-tense on the corpus, D1/D6, D2 and the command-chain build plan marked HISTORY in-sentence and reworded off the retired paths, two hits the ticket's count missed (:32, :34) fixed, § Still open gains "Chain consolidation — EXECUTED (D-102)"; docs/reference/command-corpus-check.md 25/44 — the orchestrator-corpus / wrapper-audit / `templates/**`-fallback section replaced by § What it reads — the denominator (63 = 36 sources + 22 fragments + the assembler + 4 agent definitions, backed by `_corpus_files` + `AUDITED.add` at `check_command_corpus.py:818/:1013`), the three new sources audited by the same eight predicates; gates: the scoped grep 0; `--check` 0 render errors with five HAND-EDITED consumers; the corpus check green over 63; doc links 0 into docs/orchestrator/; residuals declared: north-star :218 `03-tech-plan` inside a HISTORY section; agents-fabrik.md:116,399 cite the kept `docs/traycer/fabrik-workflow.md`)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 (the canonical planning map + the @imported core — false claims here reach every session) — round 1.
### Orchestrator execution (in the worktree)
- the scoped grep gate → 0 files; `assemble_commands.py --check` → 0 render errors, 5 HAND-EDITED rows; `check_command_corpus.py` → all sound; doc-link errors naming docs/orchestrator → 0; `grep -l include:grounding-rules commands/_sources/*.md` → 5 (the fragment's claim holds).
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0223)
- v3.2-exp CLEAN. v4-flash (1): the epic-file naming — the north-star's `YYYY-MM-DD-epic-<n>-<slug>.md` vs the front door's `<n>-<slug>.md` — UPHELD (= native 3: the truncated stem, at four sites). v4-flash (2): "eight predicates" vs the doc's seven — REFUTED (the script's own selftest prints "of the eight predicates" and the success line enumerates eight); `5-certify` "a non-existent command" — REFUTED (it is the pipeline STAGE name in CLAUDE.md's routing table; the corpus and the operating-model doc use it the same way).
### Native layer (opus×1, by execution against the code and sources every rewritten sentence names)
- **[H] 1 — the ticket's own scoped gate can never exit 0:** `set -e` + `got=$(git grep -l …)` — git grep exits 1 on an EMPTY result, so the gate aborts before `test -z` runs, red exactly when the surface is clean (proven with a no-such-token control). The surface IS clean (0 of 5). The same shape sits in T16's gate. ORCHESTRATOR: both plan-set gate lines gain `|| true` (this commit).
- **[H] 2 — `docs/reference/command-corpus-check.md:186` "every file the audit reads is a file predicate 7 grades" is FALSE:** predicate 7 iterates `_corpus_files()` = 59 (sources + fragments + assembler); the denominator is 63 — the 4 agent definitions are read into AUDITED but graded by predicate 6 only. FIXUP.
- [M] 3 — the epic path stem truncated to `<n>-<slug>.md` at `agents-fabrik-core.md:18` (box-wide @import), `agents-fabrik.md:53`, the north-star `:49`/`:84`; the mandated shape is `YYYY-MM-DD-epic-<n>-<slug>.md` (`check_doc_sprawl.py:123`, `fabrik-spec.md:3`, the north-star's own `:207`). FIXUP. [M] 4 — `agents-fabrik.md:57` "ticket Scope ⊆ …" — the check reads `## Touches` (`check_plan_tickets.py:797/:1319/:38`). FIXUP. [M] 5 — `:55` "(the ladder is never re-run)" overstates `fabrik-spec.md:102-105` (re-run for capabilities the Vision never adjudicated). FIXUP. [M] 6 — "Step 0" (`:18`, `:68`, `:496`) does not exist; `fabrik-vision.md:27` is `## Phase 0`. FIXUP. [M] 7 — the `--worktree` form overgeneralized to every window at `agents-fabrik.md:19` and the north-star `:94` while both docs state agent-1's main-checkout form correctly elsewhere — each contradicts itself. FIXUP. [M] 8 — the declared residual `agents-fabrik.md:116`/`:399` is NOT acceptable: live-tense "Step 2.5 in `docs/traycer/fabrik-workflow.md` is the planning-side companion" and ✅ edit rights on the retired layer's 1598-line prompt. FIXUP (HISTORY markings, reference-only). [M] 9 — T16's allowlist gate: the three absence-grader test files can never clear (they assert the tokens' absence) — ORCHESTRATOR: the allowlist grows by them (this commit), reason stated. [L] 10 — the north-star `:198` D-Zed unmarked live-tense on retired folders. FIXUP. [L] 11 — `scripts/epic_order.py:4,6` docstring names the retired mega docs, owned by no ticket — orchestrator delta at T14b's merge. [L] 12 — `cb65dafa`'s `Agent-Name` blank — the subagent shape; accepted.
- Verified clean by execution: the scoped surface 0 of 5; `check_command_corpus.py` green and 36 + 22 + 1 + 4 = 63 exact; "eight predicates" the script's own count; the `scripts/` resolver hub-root-only after T08a (`:1126-1131`); the project early-return and the unreadable-file listing true; the fragment's five consumers exactly the five named; `--check` 0 render errors with the five one-line drift rows (a manual installed-vs-render diff); doc links 0 into docs/orchestrator/; `/fabrik-spec`'s epic-file intake real (`fabrik-spec.md:3, :13-35`); the up-route at `:71`; `epic_order.py --check/--assign/--check --owners` as described; the two-link containment real (`:1319`); the launch form byte-exact against three sources; "runs the tail from 5-certify" written in the corpus (`fabrik-epics-review.md:466-467`, the operating-model doc `:75/:110`); the three sources render with every fragment; Tier 3 live in `/fabrik-epics-review:226`; every referenced path exists; D-102 real; the HISTORY markings adjudicated at all 11 hits (the declared CC3 residual acceptable under the `:212` heading); five files exactly; DO-NOT honoured.
### Verdict
**10 findings (2 H + 7 M + 1 L) → 8 to the coder as FIXUP round 1; two plan-set gate lines fixed by the orchestrator in this commit; the epic_order docstring queued for the merge.** Ledger: gate satisfiability (structural) · doc truth vs code (coverage claim, path shape, heading name, ladder rule, phase name, launch-form scope, retired-layer live tense) · history markings · render/link cleanliness.

## Round 2 — over `3a4b5e77..af2998ef` (the round-1 fixup cb65dafa..af2998ef: agents-fabrik-core.md 1/1, agents-fabrik.md 9/9, the north-star 4/4, command-corpus-check.md 1/1 — the eight items each grounded to its line: the coverage sentence (predicate 7 over the 59-file corpus set, agents by predicate 6 — `check_command_corpus.py:1013/:1149-1150/:1274`); the epic stem `YYYY-MM-DD-epic-<n>-<slug>.md` at all four sites (`check_doc_sprawl.py:123`, `fabrik-spec.md:3`); "ticket Touches ⊆ …" (`check_plan_tickets.py:38/:797/:1319`); the ladder re-run rule (`fabrik-spec.md:104-105`); "Phase 0" ×3 (`fabrik-vision.md:27`); agent-1 in the main checkout at both sites (the operating-model doc `:20-23`); `docs/traycer/fabrik-workflow.md` HISTORY (D-102) with planner edit rights ❌ and `:399` past tense — the pre-plan read attributed to `/fabrik-vision`'s research discovery over `docs/preplans/` (a grounded deviation from the orchestrator's brief: preplan mentions fabrik-vision.md 9, fabrik-spec.md 0); the D-Zed paragraph marked history; gates: the scoped grep 0, `--check` 0 render errors with the five drift rows, the corpus check green, doc links 0 into docs/orchestrator/)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own execution (eight prescribed replacements, each grounded by the coder; stated) — round 2.
### Orchestrator execution (in the worktree)
- the scoped grep → 0 files; `--check` → 0 render errors; the corpus check green; `epics/<n>-<slug>` → 0 in all three docs; `Step 0` → 0; the predicate-7 sentence reads the 59/4 split; the `:116` row reads HISTORY / ❌ No.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0114)
- Two walk all eight items to their lines (present, grounded, grammar and cell counts intact). The third raised two claims — D-Zed "not marked HISTORY" and "Phase 0 only twice" — both REFUTED by execution in the worktree: the north-star carries "Built after the corpus chain is finalized … (the analog of the retired Traycer front-end)"; `grep -c 'Phase 0'` → 3 in agents-fabrik.md, 1 in the north-star, and `Step 0` → 0 across the three docs.
### Verdict
**0 findings — no-op round.** **Status: CONVERGED** at `af2998ef`; merge with the main-checkout render (five consumers of the fragment) and the fleet sync (`agents-fabrik*.md` are triggers); the merge owner's delta: `scripts/epic_order.py`'s docstring lines naming the retired mega docs (round-1 native item 11).
