# T05 acceptance review — `/fabrik-vision` + `/fabrik-epics-review` text (plan 2026-09-06-plan-2-multi-agent-adoption)

**Status:** CONVERGED
**Surface:** `git diff ca4ec9ed..worktree-agent-ac49238fb7bb4c376 -- commands/_sources/fabrik-vision.md commands/_sources/fabrik-epics-review.md tests/test_vision_reads_work_stores.py` (r1: +185/−3 over 3 files, native Sonnet coder af9413da). Box-wide render surface: a wrong sentence misroutes every future `/fabrik-vision` run.

## Coverage Checklist

Rubric invocation (verbatim head of the output):

```text
$ python scripts/review_rubric.py --changed commands/_sources/fabrik-vision.md commands/_sources/fabrik-epics-review.md tests/test_vision_reads_work_stores.py
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
```

| Class | Status |
|---|---|
| core/40-documentation.md — trailers; one CHANGELOG entry; commands are rules, not changelogs | CLEAN (present-tense rules; D-154 cited, no literature — native (3)) |
| core/10-python.md — the new test file (stdlib subprocess, ruff clean) | CLEAN |
| FLOOR 35/25/30 · 12-FACTOR | CLEAN (no surface) |
| Recurrence: Behavior Contract row without a killing test | FIXED r1 → r2 (row 3's `check_command_corpus.py` half had no test — pool deepseek, accepted) |
| Recurrence: Touches discipline / frontmatter untouched | CLEAN (`--name-only` = the three Touches; `grep -c '^[-+]description'` = 0 — native (5)) |
| Recurrence: READ-BEFORE-YOU-EDIT — a restatement creating two sources of truth | FIXED r1 → r2 (native (2): three parallel EXISTING read enumerations, one edited — the load-bearing `:646` + `:349` bullets now carry the clause) |
| Recurrence: live-tense text about an unmerged script | CLEAN (Merge Order T01, T02a precede T05 — native (4); T05 is merged AFTER T02a for exactly this reason) |
| Recurrence: proxy-as-evidence — gates RUN in the worktree | CLEAN (5 passed; `--check` drift on exactly the two sources, zero render errors; corpus check green — orchestrator + native (6)) |
| Recurrence: denominator on every count | CLEAN — every count carries its population: diff bytes/lines (`git diff --numstat`), tests passed of collected, finders usable of dispatched, mutants killed of probed |
| Recurrence: fail-open/fail-closed | CLEAN (the test skips loudly with a reason when `~/.claude/commands` is absent, never silently) |
| Recurrence: cost/quota accounting | CLEAN (pool r1 4/1/1 scored; native opus×1 r1) |
| Recurrence: boundary/sentinel/prefix | CLEAN (the greps are window-bounded — native (7)) |
| Recurrence: behavior-without-a-test | FIXED r1 → r2 (acceptance criteria of the epic review did not require the row — native (3) sub-defect) |

## Round 1 — over `ca4ec9ed..af9413da`
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.

### Adjudication (pool layer)
- deepseek-v4-flash (4/5): row 3's `check_command_corpus.py` half never invoked by a test — ACCEPTED (fix: a subprocess test).
- gemini-3-flash (1/5): "the Step 1.5 text sits in a different section and the test will fail" — REFUTED (5 passed; the native read places `:204-207` inside Step 1.5's 2b); two "fleet" rows treat a hub-only test as a synced surface — REFUTED; "passes on old code" — REFUTED by the coder's red run.
- qwen3-max (1/5): five vague high-confidence rows without `path:line` — no actionable claim.

### Native finder (opus) — 9 hunt items executed; 2 raised
- (1) placement: `:810` is inside EXISTING E-analysis "Scope the continuation" (`:635`/`:802`); the Scale Assessment at `:455` is NEW-mode-only with no down-route reading these stores — placement RIGHT, the ticket's phase name was loose. **(2) DEFECT M** — three parallel EXISTING read enumerations (`:63-66` edited; `:345-350` and `:646` not) → mirror the clause. (3) Step 1.5 sentence CLEAN (`:204-207`, cites D-154, no contradiction with `:58`/`:463`/`:468-470`); **sub-DEFECT L-M** — § Acceptance criteria (`:512-519`) lists only `--check --owners`. (4) live-tense safe by Merge Order. (5) frontmatter untouched; composed descriptions 786 / 967 ≤ 1024. (6) `--check` drift 13 + 31 lines on exactly the two sources, zero errors; corpus check green. (7) all three greps red on the base copies; windows bounded. (8) Touches exact. (9) no restatement; one advisory (the terminal set spelled twice).

Verdict r1: 3 defects (2 native + 1 pool) → fixup dispatched to the same coder. Board T05 🟡 → 🔵.

## Round 2 — over `ca4ec9ed..4e6c3435` (the round-1 fixup: fabrik-vision.md +23/−, fabrik-epics-review.md +7/−, tests +230 — 6 tests)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own EXECUTED verification (the three fixes were prescribed verbatim; the closing layer re-ran the gates and grep-counted the clauses) — round 2.

### Adjudication (pool layer)
- deepseek-v4-flash (3/5): (1) the `--check` test accepts exit 1 on expected drift — REFUTED as designed: the test is worktree-aware (exit 0 after the main-checkout render, exit 1 with drift on exactly the two sources before it); the drift guards are the `command-corpus-check` pre-commit hook and the gate, not this test. (2) the two mirrored read clauses name the stores unconditionally while the Phase-0 bullet says "report absence" — ACCEPTED as a one-clause text fix: the orchestrator's mechanical fixup at merge adds "when present" to both mirrored clauses (`fixup_T05.py`, ≤2 lines, no new logic; a pool finder pass covers it at D7).
- gemini-3-flash (1/5): the same "fleet blast radius of a hub-only test" misread as round 1 — REFUTED.
- qwen3-max (3/5): CLEAN. Accepted.

### Orchestrator execution (the closing layer)
- In the coder's worktree: `pytest tests/test_vision_reads_work_stores.py -q` → **6 passed**; ruff clean; `assemble_commands.py --check` → drift on exactly the two sources (23 + 53 lines), zero render errors; `check_command_corpus.py` → green over 63 files.
- r1 (2): `grep -c STRATEGIC_BACKLOG fabrik-vision.md` → 4 (the read list, the two mirrored bullets, the E-analysis paragraph); the `:349`/`:646` clauses present.
- r1 (3): `fabrik-epics-review.md:519-520` — "…ledger row present or minted (`decisions.py --merge-owner .` ≠ `UNDECLARED`)".
- pool r1: `test_check_command_corpus_exits_clean` present and green; the extended read-list test's red quoted by the coder (`PLANS.md missing from the Phase-2 required-inputs bullet`).

Verdict r2: found: 0 blocking, 1 text nit absorbed as the merge-time mechanical fixup — **CONVERGED**. Merge target: `worktree-agent-ac49238fb7bb4c376` @ 4e6c3435, merged AFTER T02a (its text names `--adopt`) with the corpus render from the main checkout.

## Pass Ledger
| Pass | Layer | Method | Findings → fixed |
|---|---|---|---|
| Pass 1 | pool ×3 + native opus×1 | method: re-derivation (placement re-read at :635/:802/:810; greps replayed on base copies; descriptions composed 786/967) | 3 → 3 |
| Pass 2 | pool ×3 + orchestrator execution | method: re-derivation (gates re-run, clauses grep-counted, acceptance line re-read) | 1 nit → fixup at merge |
