# /fabrik-review — the provider-death EXECUTION (independent second round)

Status: CLOSED — coverage-adjudicated exit; closing round `found: 0, fixed: 0`, `new: 0`.

**Surface:** `HEAD 15a0202b5edfaba15c0bf32d33cf4b6749fc8cd0` · worktree-diff md5 `25680523783e488eaa69beefbbb8f4b9`
**Under review:** the executed diff `61d03d16` (A) · `e056a415` (B) · `31426294` (C) · `15a0202b` (Finish).

**Anchor.** The prior report (`2026-08-28-plan-1-provider-death-enforcement-review.md`) records its
`Surface:` as a commit RANGE, not a hash pair, and the surface has since grown by the Finish commit. So
this is a **new review, not a re-adjudication** — and it is the first one run on this work in a genuinely
adversarial posture rather than as the executor's own closing step. That distinction earned its keep: it
found four defects the executor's own review passed clean.

**Finders.** Native orchestrator only. `NO-POOL: operator standing directive — no subagent or pool dispatch
for hub work.` Every finding reproduced by executing the real artifact.

## Rubric

Classes below derive from a `scripts/review_rubric.py --changed` invocation run this session over the six changed paths (`76-gpu-workers.md`, `58-resilience.md`, `self-healing.md`, `fabrik-spec-review.md`, `fabrik-plan-review.md`, `test_rule_pack_scaffold_coverage.py`), verbatim output below.

```
$ python scripts/review_rubric.py --changed <the 6 changed paths>
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.
## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: tests/test_rule_pack_scaffold_coverage.py)
### core/40-documentation.md  (hit: 58-resilience.md, 76-gpu-workers.md, self-healing.md)
### core/45-testing-strategy.md  (hit: tests/test_rule_pack_scaffold_coverage.py)
# promote-to-check_*: 54 injected mandate(s) look deterministically greppable
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | **Cross-pack contradiction** (new class, found this run) | **FIXED(1)** | **F1.** `76-gpu-workers`'s corrected example SWAPPED on 429; `self-healing.md:33` row 3 prescribes `pause-state.set_global_pause(resource, ttl=Retry-After)` for the same signal — and the two packs now cross-reference each other, so a reader got contradictory guidance for one status code. Reconciled: swap only when a live sibling exists in the rebuilt chain, else take row 3's path with the vendor's own TTL. |
| 2 | **Capability overclaim** (new class, found this run) | **FIXED(2)** | **F2.** Both packs instructed projects to *"Vendor `fabrik-lib/health-probe/` — do not re-derive"* for the chain-rebuild helper. That helper is **REQUESTED, not shipped** (`01M14E3MWN`, spec U1 explicitly unresolved). This is the exact "names something that does not exist" defect the whole plan was organised to remove — committed inside the fix for it. Both instances corrected to name what health-probe/ *does* do and mark the helper as not-yet-shipped. |
| 3 | **Over-broad rule row** (new class, found this run) | **FIXED(1)** | **F3.** The new `python-api-gpu` matrix row asserted pause-state pipeline and queue-bloat as unconditional `Yes`, but `76-gpu-workers.md:31` states real-time streaming requests **bypass the queue** — so a real-time-only orchestrator was handed an obligation it cannot meet. Every sibling row in that matrix is conditional; mine now is too. |
| 4 | **fail-open vs fail-closed** (standing) | CLEAN | **Probe executed, not asserted:** pointing the matrix helper at a heading that does not exist returns `FAILS CLOSED — heading missing`, never a silent green over an empty section. Same guard on both `76-gpu-workers` assertions. |
| 5 | **cost / quota / limit accounting** (standing) | REFUTED | No metered call added anywhere in the diff; `NO-POOL` held for the entire run, so zero pool spend. The rule text discusses provider cost; the change consumes nothing. |
| 6 | **boundary / sentinel / prefix** (standing) | CLEAN | The matrix assertion is exact-set equality (reports `EXTRA` as well as `MISSING`, so a stray backticked token cannot inflate it) and is scoped to the section, not the file. Re-verified live: `matrix==registry: True`. The `except` regex accepts both a parenthesized tuple and a bare exception with an optional `as` binding. |
| 7 | **behavior-without-a-test** (standing) | CLEAN | Everything mechanically assertable is asserted (registry coverage · the example's `except` · breaker granularity · corpus render parity). The ladder row and the standard's prose have no test and cannot — they are graded by an LLM reading a design, and both artifacts say so in themselves rather than implying a gate. |
| 8 | Render / corpus parity | CLEAN | This round touched only rule packs, no `commands/_sources/` — `git status --porcelain -- commands/_sources/` empty, so no render is owed. The Phase-C render was separately verified: both rubric rows are present in the INSTALLED `~/.claude/commands/*` files (grep = 1 each), not just in the sources. |
| 9 | Table structural integrity | CLEAN | Banned Patterns 22 rows × 4 fields; Per-Scaffold matrix 14 × 8; the new per-route table 5 × 6 — all internally consistent, so no row silently drops a column. |
| 10 | Cross-reference resolution | CLEAN | `58-resilience § Provider-death resilience` (`:340`) is a SIBLING of `## Advanced: … (workers only)` (`:268`), not nested inside a workers-only scope; `§7a` knob reference resolves (`:229`); `alerting/`'s "title-based dedup" claim matches the module README verbatim. |
| 11 | **Shared-tree hygiene** (standing) | CLEAN | Only 3 files of mine changed under `.windsurf/rules/core/`. The 21 dirty sibling paths (`rules/ai/*`, `65-rag-search.md`, `libs/subagents/*`, the zitadel plan) were never staged, stashed, reverted or edited. |
| 12 | CHANGELOG integrity after a scripted edit | CLEAN | Exactly one `## [Unreleased]` heading; section order intact (`:5` Unreleased → `:24510` `[0.0.0]`). |
| 13 | FLOOR: secrets / Postgres / Docker / 12-Factor | REFUTED | No auth, DB, container, port, secret or service surface — the diff is three markdown rule packs, two command sources, one test, a CHANGELOG entry and plan/review docs. |

## Pass Ledger

| Pass | finders | found | new: | fixed |
|---|---|---:|---:|---:|
| Pass 1 (WIDE) | native orchestrator, all 13 classes over `7fbc2b6d..15a0202b` | 7 | 7 | 3 |
| Pass 2 (SCOPED) | native, the fix diff + the two classes F1/F2 belong to | 1 | 1 | 1 |
| **Pass 3 (QUIET, closing FULL sweep)** | native, all 13 classes vs the FIXED tree | **0** | **0** | **0** |

**Closing round verdict — `found: 0, fixed: 0`.** Pass 3 re-executed every mechanical check against the
final state: fail-closed probe run · `matrix==registry: True` · zero remaining "go vendor it" claims across
all three packs · the 429 reconciliation present · the conditional matrix row present · zero sibling files
in my change set · `pytest` 3 passed.

## Findings

- **F1 — the corrected example contradicted `self-healing` row 3 on 429.** CONFIRMED by reading both packs.
  I widened the `except` to swap on `402/403/429` without checking what the ladder already prescribed for a
  rate-limit, and the two packs now cite each other. FIXED by scoping the swap to "a live sibling exists"
  and routing the single-provider case back to row 3's pause-with-`Retry-After`.
- **F2 — both packs told projects to vendor a helper that does not exist.** CONFIRMED against
  `01M14E3MWN` and the spec's own U1, which states in writing that health-probe/'s interface is unverified.
  This is the defect the entire plan exists to prevent, committed inside the fix for it — the most
  instructive finding of the run. FIXED in both places.
- **F3 — the new `python-api-gpu` row was unconditional where its own pack is conditional.** CONFIRMED at
  `76-gpu-workers.md:31` ("Real-time streaming requests bypass the queue"). FIXED to match sibling rows.
- **F4 (Pass 2) — the F2 fix was applied to only ONE of the two packs.** CONFIRMED by re-sweeping the class
  rather than trusting the first fix: `58-resilience.md:364` still carried "Vendor … do not re-derive"
  after `76-gpu-workers` was corrected. This is why the scoped middle pass re-checks the fix's own class.

## Not mine — reported, not touched

`docs/development/plans/2026-08-28-plan-2-zitadel-umbrella-idp.md` is an **untracked sibling WIP**. It
fails two gate checks (an empty Behavior Contract, and a stage-skip: it claims CONVERGED while citing a
spec that is not). It is not in my diff and I did not edit it — a gate flagging a sibling's file is a
shared-tree false positive for me, and editing another session's work-in-progress is the critical failure
this repo's contract names. Whoever owns that plan owns those two findings.

## Closing evidence

```
$ python -m pytest tests/test_rule_pack_scaffold_coverage.py -q
3 passed

$ python -c "…point the matrix helper at a heading that does not exist…"
FAILS CLOSED — heading missing

$ grep -c "do not re-derive" 58-resilience.md 76-gpu-workers.md
0 0

$ python scripts/final_gate.py --json
{"status": "failure", "failures": [Stage-Skip Artifact Gate :: 2026-08-28-plan-2-zitadel-umbrella-idp.md …]}
        ^ the ONLY red, and it is the sibling's untracked plan — not attributable to this diff
```

## Per-phase verdict

Each executed phase ran the full Tier-2 gate at its boundary; these are the real runs, captured at the time.

### Phase A (`61d03d16`) — VERDICT: PASS

 Test written first, all three assertions watched RED, then fixed
  and proven red-on-revert with each mutation asserted on disk.

```
$ python scripts/final_gate.py --json
{"status": "success", "blocking": 38, "failures": []}
```

### Phase B (`e056a415`) — VERDICT: PASS

 Ladder row 10 + the deadman distinction + § Provider-death
  resilience; `select_rules.py` confirmed all three packs still ACTIVE (a malformed table silently drops one).

```
$ python scripts/final_gate.py --json
{"status": "success", "blocking": 38, "failures": []}
```

### Phase C (`31426294`) — VERDICT: PASS

 Both rubric rows + a render from the MAIN checkout, `--check`
  clean before / correctly drifted after edits / clean after render; corpus predicates green across 47 files.

```
$ python scripts/final_gate.py --json
{"status": "success", "blocking": 38, "failures": []}
```

### Phase R — this review round's fixes (F1–F4) — VERDICT: PASS

PASS on every check attributable to this diff. Honest
  caveat, because it matters more than a green box: at the moment of writing, a `final_gate` run also
  surfaced a red on `docs/development/plans/2026-08-28-plan-2-zitadel-umbrella-idp.md`, an **untracked
  sibling WIP** that is not in my diff and that I will not edit. That red is not attributable to this work
  and its disappearance or persistence is the other session's to resolve.

```
$ python -m pytest tests/test_rule_pack_scaffold_coverage.py -q
3 passed

$ python scripts/enforcement/check_review_coverage.py
  coverage-checklist: PASS
```

## Per-finding disposition — 4 findings → 4 FIXED

F1 FIXED · F2 FIXED(2 instances) · F3 FIXED · F4 FIXED. The executor's own review of this same work
returned clean; an independent posture found four real defects, one of them the precise defect the work
was commissioned to eliminate.
