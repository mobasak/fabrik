# Review — enforcement false-positive fixes (5daa144a + tightening round)

Status: CLOSED
Surface: commit 5daa144a (check_convergence citation matching · check_ticket_breadth doc-sync
companions · check_plan_tickets --allow-external, off mail findings 01M1DJYH + 01M1DMBS) and the
round-2 tightenings (82f43e71). Compact: 1 native finder + authority pass (pool skipped —
libs/subagents still sibling WIP, standing disposition). Review-before-reply: both finding
senders' replies held until this quiet round.

## Rubric (verbatim head of `review_rubric.py --changed` over the surface — armed the finder)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
```

## Coverage Checklist

| Class | Verdict | Evidence |
|---|---|---|
| cite false-accepts (fuzzy path) | FIXED | one-token slugs supersetted into unrelated reviews (`plan-2-mail` → any later `*mail*` review — finder verified live); undated names voided the date guard entirely → fuzzy path now requires ≥2 slug tokens AND both names dated; pinned in `test_01m1djyh_*` |
| same-slug twin-plan acceptance | REFUTED (bounded) | a superseded twin plan fuzzy-matches the newer review — accepted and documented in the function: this gate's ceiling is evidence PRESENCE, never truth (its own module docstring); exact-stem keeps date-uniqueness where names allow |
| docs-only mislabel | FIXED | a docs-only ticket read `<tests-only>` (finder ran it live); fallback now labels `<docs-only>`; my own round-1 test asserted only `score >= 1` — strengthened to assert the label |
| governance-mix loss | FIXED | excluding docs from `prod` dropped `mix` on a .windsurf+docs ticket (finder measured True→False side-by-side); `has_code` now counts doc-sync companions as local work; pinned `test_governance_plus_docs_ticket_keeps_the_mix_signal` |
| retro-safety denominator | FIXED | my "retro-safe" claim was hub-verified only (249 plans, 0 flips — executably measured old-vs-new); the finder's fleet sweep found ONE flip at seo — fail→pass, an archived EXECUTED plan the OLD rule wrongly failed; CHANGELOG wording now carries both numbers |
| --allow-external coverage | FIXED | shipped untested (the flag whose purpose is enabling red-on-mutation proofs had none); subprocess pin covers refused-without / admitted-with / dated-rule-still-binds |
| --allow-external injection | CLEAN | the gate's discovery path never passes it; the flag is consulted only inside the `--plan-dir` branch (finder traced final_gate.py:1516) |
| date-guard comparison | CLEAN | zero-padded lexicographic == chronological across every real corpus filename (finder swept all /opt plans+reviews trees) |
| empty-token edge | CLEAN | empty slug-token set refuses (finder verified) |
| readiness/verify strip symmetry | CLEAN | 0 real (plan, review) pairs depend on those specific strips (finder measured fleet-wide) |
| fail-open/fail-closed | FIXED | both false-accept holes closed fail-closed (refuse → exact-stem-only fallback); breadth check stays fail-soft by contract |
| boundary/sentinel/prefix | FIXED | the token-count floor and date-prefix presence are this surface's boundaries — both now explicit preconditions with pins |
| behavior-without-a-test | FIXED | every adjudicated fix carries its pin; 177 tests green across the three suites |
| cost/quota accounting | CLEAN | no cost surface in range |

## Pass Ledger

| Pass 1 | method: gate | found: 7 | new: 7 | fixed: 5 | finders: enforcement-fp-finder(native, returned 7 candidates + 5 clean — verified empirically against 249 hub + 39 project-repo plan trees) · authority(self: retro-safety old-vs-new sweep, 249/249 zero flips) |
| Pass 2 | method: re-derivation | found: 0 | new: 0 | fixed: 0 | finders: authority closing sweep — 177 tests green, retro re-sweep post-tightening 249 plans 0 flips, the 01M1DJYH live case still passes |

## Gate

Commit hooks green on 5daa144a + 82f43e71 (both pushed, governance-synced); `final_gate --lean
--check --json` → `success` earlier this session; the three affected suites re-run green this turn.

## Terminal

The exit round is the ledger's last row (Pass 2 — the quiet round). Totals: 7 raised, 6 fixed
(5 code + 1 wording), 1 refuted-bounded with its disposition in the function docstring.
