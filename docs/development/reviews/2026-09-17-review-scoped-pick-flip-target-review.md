# Heavy review — routed up from `/fabrik-review-scoped` over the `_pick_flip_target` fix (2026-09-17)

Status: IN-PROGRESS (round 1 seats dispatched 2026-09-17; the closing delta round is owed)

**Surface:** `639caf855bfc5d71264c032cd237a388a7d9f984` + `13a759960b14a980389b7ec57ae9e314` (HEAD at
round 1's dispatch + `git diff HEAD | md5sum`). The cumulative diff `67421508d..639caf855` over
`scripts/sysadmin/claude_rotate.py` (+114/-88; the `scripts/aro-wake/` twin byte-identical),
`tests/test_claude_fleet.py` (+195/-0) and the two `CHANGELOG.md` entries at `0b4df223e` and
`dd1309b6a`. Pins under `<scratch>/rs/hv/`: `pin_code.diff` md5 `091c29477a9031ba2124407a36adeb55`,
`pin_tests.diff` md5 `6b3763dcf715fccba4bceda2bd6db48f`, `pin_changelog.diff` md5
`e4daef91600200f498293ef5b5700c9c`.

**Anchor:** the newest prior report for this scope is the resume artifact
`2026-09-17-review-scoped-pick-flip-target-resume.md`, whose `Surface:` is a prose description, not
a hash — it cannot match by construction, so this is a full WIDE pass 1 (its `Open rows` table is
updated in the same change).

**Why heavy:** the scoped run's two rounds each confirmed defects (5, then 7 of which 5 lay inside its
own round-1 fix), which is the scoped command's route-up condition (step 5, after two consecutive
confirming rounds). Round zero of this review — the orchestrator's own class sweep, D-231 — then closed
thirteen more mirror sites of the same shape (`639caf855`) before any finder saw the surface.

**Partition:** by FILE (D-207), `dispatch_headroom.py --slices opus=1,sonnet=2` → `SEATS: 3`, stamped
before dispatch. No file's logic read by two seats.

| Slice | Model | Subject |
|---|---|---|
| 1 | Opus | `scripts/sysadmin/claude_rotate.py` — the cumulative code diff, the 16 classes |
| 2 | Sonnet | `tests/test_claude_fleet.py` — the seven new and two extended graders: test quality, mutants, red-on-revert re-derivation |
| 3 | Sonnet | the two CHANGELOG entries, every added comment/docstring line, the four commit messages, the resume artifact — prose truth |

## Rubric

```
$ .venv/bin/python scripts/review_rubric.py --changed scripts/sysadmin/claude_rotate.py scripts/aro-wake/claude_rotate.py tests/test_claude_fleet.py CHANGELOG.md
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
## FLOOR — always injected, regardless of glob (spec L3; TOOLING surface)
### core/10-python.md   (uv · SQLAlchemy async · DATABASE_URL · GlitchTip · logger · create_task · datetime.now(UTC) · ruff ASYNC/B/S · uvicorn · 12-factor config · no file logs · no startup migrations)
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/40-documentation.md  (hit: CHANGELOG.md)
### core/45-testing-strategy.md  (hit: tests/test_claude_fleet.py)
# promote-to-check_*: 34 injected mandate(s) look deterministically greppable
```
(93 lines; the full output is at `<scratch>/rs/hv/rubric.txt` — the class rows below derive from it.)

## Hygiene (round 1 open)

`check_review_hygiene.py --surface scripts/sysadmin/claude_rotate.py --surface tests/test_claude_fleet.py --surface CHANGELOG.md --receipt <the resume artifact>` → 12 hits over 4 files, 1 row ungraded. All 12 are `template-residue` in `CHANGELOG.md` at lines 2281–20987 — historical entries quoting `{{include:…}}`/`{{VAR}}` syntax, none on this surface's two hunks (lines 5–10): `RECORDED — hygiene false positive (historical entries quoting template syntax; the surface hunks are the top two entries)`. The ungraded row is the resume artifact's `Open rows` table row, which carries no counters by design.

## Coverage Checklist

| Class | Status | Evidence |
|---|---|---|
| `scripts/sysadmin/claude_rotate.py` (+ twin) — the changed file | FIXED(5) | slice 1: F1 `_tick_burn`'s previous reading bare (executed: OverflowError at the `float(pr)` compare, file left poisoned) · F2 `0.0` wins `_soonest_reset` (executed) · F3 an undateable relief promised to the stamp/ledger (executed via `_next_session_relief` returning `(1e300, …)`) · F4 `_walled` + sort elements bare (executed: TypeError) · F5 the validator's docstring named two readers (doc half FIXED; the RANGE half `RECORDED — extension (a finite out-of-range utilization renders 321 chars; the band still reads RED — fail-closed; a range validator is a new mechanism, backlog)`) — all at `12066600b` |
| `tests/test_claude_fleet.py` — the changed file | FIXED(5) | slice 2: F1 the except-tuple mutant survived (executed: 10 passed under the narrowed tuple) · F2 the weekly arm untested (mutant survived) · F3 `split("5h")[-1]` sliced the `burn 5h` field (executed) · F4 `"tick:" in out` was decoration · F5 a NEGATIVE reset wins `_pick_successor` through `or far` (executed: picked `negative`) — graders strengthened + `_future_or_far` at `12066600b` |
| `CHANGELOG.md` — the two entries | FIXED(3) | slice 3: F4 `639caf855` shipped with no entry · F5 the round-2 paragraph glued to the next heading · F1 the round-zero commit says thirteen sites, the diff has fifteen (`grep -c '^+.*_usable_ts('` = 15) — entry written with the re-derived count at `ec65c5c29`; F2 the `e96386725` title says seven graders, six is right: `RECORDED — immutable commit message; corrected in the CHANGELOG entry and here` |
| core/10-python floor (datetime.now(UTC), no bare create_task, logging) | CLEAN | slice 1 read all 400 pin lines; the only `datetime` uses are `fromtimestamp(…, tz=UTC)` and the guarded local-time renderers; no tasks, no file logging on this surface |
| core/40-documentation (CHANGELOG grammar, no skipped headings, fenced code only) | FIXED(1) | slice 3 F5 (the missing blank line); both entries keep the `### Fixed — Title (date)` grammar under `[Unreleased]` |
| core/45-testing-strategy (watched-fail-first, guard keyed on the fix, no cosmetic assertions) | FIXED(3) | slice 2 re-derived red-on-revert for two graders at `67421508d~1` (OverflowError both); 6 of 7 mutants killed, the surviving one (narrowed except tuple) now killed; the decoration assertion replaced by the branch's own text |
| fail-open vs fail-closed on every gate/guard | FIXED(2) | slice 1 F3 (the promise the re-arm could never reach — a week of silence) and slice 3 F7 (a NaN read as headroom on the comparison-only sites; `json.loads` admits NaN — executed); the tick's `INTERNAL ERROR … return 0` shape stays as designed |
| cost/quota/limit accounting edges (unknown≠0, None vs 0 readings) | CLEAN | slice 1: every `_usable_ts` None routes to the no-reading arm (`_fleet_band` RED on a required window, `_account_status` valid=False, `_drain_trigger_reason` "no readable figure"), never to 0 |
| boundary/sentinel/prefix collisions (0, -0.0, True, `far`, the era floor, `<= 0`) | FIXED(2) | slice 1 F2 (`0.0`/`-0.0` won the min) and slice 2 F5 (a negative epoch beat `or far`); the `ts <= 0` arm of `_fmt_reset_clock` and the picker's `> _now()` now share one shape (`_future_or_far`, `_dateable_ts`) |
| behavior-without-a-test | FIXED(2) | slice 2 F2 (the weekly arm of the flip-leg guard) and slice 1 F1 (the memory file's rewrite) — graders added at `12066600b` |
| removed-guard / changed-semantics regressions (isinstance → `_usable_ts`) | CLEAN | slice 1 enumerated every site: a bool previously read as 1.0 now reads None (intended, `# \`true\` is not 1%`); `_fleet_band._u`'s old `math.isfinite` RAISED on the giant, the new None routes to the pre-existing required-window RED arm; `_urgent_drain_message`'s recursion is single-level (relief=None returns before the try) |
| cross-file contract breaks (posture readers: dispatch_headroom / quota_posture_hook / quota_dashboard) | CLEAN | no return shape changed (`_row_utils` still `dict[str, float \| None]`, `_fleet_band` still `str \| None`); `tests/test_dispatch_headroom_posture.py` + `tests/test_quota_dashboard_posture.py` + `tests/test_quota_posture.py` green at `12066600b` (500 passed across five suites) |
| prose truth (comments, docstrings, commit messages, the resume artifact) | FIXED(4) | slice 3: F3 the resume artifact stale (updated in this commit) · F6 the picker comment's history list incomplete (cut to the grep pointer) · F7 "fails CLOSED" false for NaN (sites fixed, claim withdrawn) · slice 1 F5 doc half (the `_usable_ts` docstring); F1/F2 the two immutable commit-message counts are corrected in `ec65c5c29`'s entry |

## Pass Ledger

| Pass | method | counters | finders |
|---|---|---|---|
| Pass 0 | method: re-derivation; round-zero probe — the class swept at 13 mirror sites, one grader red on HEAD (OverflowError in `_usage_windows`), 495 passed across five suites, committed at `639caf855` | found: 13, new: 13, confirmed: 13, fixed: 13, unexecuted: 0 | the orchestrator alone (D-231 pre-pin probe; not a finder round) |
| Pass 1 | method: citation — every seat cited `path:line` at `639caf855`, the orchestrator EXECUTED all 17 (probe scripts under `<scratch>/rs/hv/adj/`, six graders red on HEAD in a throwaway worktree) | found: 17, new: 17, confirmed: 17, fixed: 15, unexecuted: 0 | native opus×1 (slice 1: claude_rotate.py) + sonnet×2 (slice 2: the graders; slice 3: CHANGELOG + prose); 3 dispatched, 3 returned; 2 of the 17 are RECORDED (immutable commit-message counts, corrected in `ec65c5c29`), 1 half-RECORDED (the range extension); own-fix 4 of 17 (the round-zero commit's message, its missing entry, its NaN claim, its grader's assertion) |

## Gate

pending — re-measured in the closing pass.

## Verdict

pending.
