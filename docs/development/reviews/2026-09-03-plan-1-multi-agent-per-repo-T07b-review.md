# Acceptance review — T07b (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** CONVERGED

**Surface:** the coder's worktree branch diff against the dispatch base a6518d1e (master after the joint T06/T07a merge) — see the round sections below (one file per ticket, rounds APPENDED).

## Round 1 — over `a6518d1e..8b8d8661` (.claude/hooks/skill_router.py 61/2 — `vision`/`epics`/`epics-review` in both `STEM_SKILLS` and `KEYWORD_STEMS` at indices 4–6 above `spec` 9 / `plan` 12, the review twin first, TR forms; tests/test_skill_router_hook.py 217/27 — 75 new tests incl. a 55-row routing snapshot re-derived against the base router and neighbour-theft tests; the rest of both files `ruff format` of pre-existing dirt; 244 passed (169 at base); the false-positive surface measured over 9,849 real operator prompts — a bare-noun cut changed 96 and was rejected, the shipped cut changes 2 (both the new stems on their advertised phrases); the real hook binary end-to-end on six prompts incl. two Turkish)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0088)
- All three CLEAN: every regex row probed against the near-miss list, ordering vs `spec`/`plan`, `resolve_target` on each stem, the absent-skill case silent (`test_empty_roster_stays_silent`), format churn byte-identical to `ruff format` of the base, 244 passed, the grep 1.
### Native layer (opus×1, by execution — the coder's corpus harness re-run on a re-extracted superset: 10,590 prompts from 120 transcripts, 741 new since the coder's cut; the same 2 verdict changes, 0 on the new-only slice)
- **[M] 1 — the block sits above `conformance` (index 8), not just above `spec`/`plan`; the coder's own corpus run shows the cost:** `conformance → vision` on "…for a multi-epic project run /fabrik-conformance-review before step 2…" — a prompt ABOUT conformance, a measured Behavior-Contract row-4 violation the 55-row hand-picked snapshot cannot see. Alternative placement executed in memory (the three immediately above `spec`, below `spec-review`+`conformance`): 17/17 positives, 55/55 snapshot rows, corpus changes 2 → 1; the ordering test `tests/test_skill_router_hook.py:1354-1356` still passes. FIXUP.
- **[M] 2 — `\bvizyon\w*\b[^.]{0,20}\bvar\b` (`.claude/hooks/skill_router.py:357`) matches the Turkish existential ("vizyon dokümanı var" → vision; the English twin stays None)** and carries no declared trigger — "büyük bir vizyonum var" is already won by `:356`. TR is 35 of 10,590 corpus prompts (0.3%), so the row is unmeasured by the corpus. Deleting it: 17/17 positives, 11/11 declared TR/EN phrases, 55/55 snapshot, the three TR false positives silent. FIXUP.
- **[M] 3 — the intra-three ordering rationale is false in the fleet-synced comment (`:315-317`) and the test messages (`:1357-1359`):** the three patterns are disjoint on all 17 positives and every probed input (`vision` vs "vizyonu epiklere ayır" False; `epics` vs "assign the epics" False); all 6 intra-three orderings give identical results (positives 0 fail, snapshot 0 fail, corpus 2). The guard can only red a legitimate reorder. FIXUP: state the measured truth (order among the three not load-bearing; only the position above `spec`/`plan` is) or drop the two intra-three assertions.
- [L] 4 — `\bepics?\s+review\b` (`:330`) is the bare-noun collocation the comment above it disclaims ("the epic review process" → epics-review, base workflow-review; 0 corpus firings; no advertised trigger depends on it — "review the epics" at `:329` covers the intent). [L] 5 — `there'?s\s+a\s+(big|huge)\s+(idea|initiative|vision)` (`:353`) reaches past the declared "I have a big idea" ("there's a big idea in this paper" → vision; 0 corpus firings). Both routed to the coder's judgment: apply unless a declared trigger breaks, state which.
- Verified clean by execution: the 55-row snapshot re-derived against a byte-identical base (`router_head.py` md5 = `git show a6518d1e:…`); 66-prompt near-miss battery all None on both routers except the two intended; the real hook binary on stdin JSON for all three + near-misses (empty stdout, exit 0); absent-skill rosters silent via `resolve_target`'s roster check (`:778-780`), roster read from `~/.claude/skills` (`main():970`); `_TIER2_ROSTER` carries the three; all 13 declared trigger phrases 13/13, `check_trigger_routing.py` 0 mis-routed; format churn 0 deleted lines in either file; base 169 → 244 collected = exactly 75 new; trailers parse; DO-NOT honoured.
### Verdict
**5 findings (3 M + 2 L) → FIXUP round 1 routed to the coder.** Ledger: placement/ordering · TR rows · rationale truthfulness · bare-noun collocations · fleet safety (clean) · format churn (clean) · gates (clean).

## Round 2 — over `a6518d1e..5bda5c6f` (the round-1 fixup 8b8d8661..5bda5c6f: router 52/49, tests 42/8 — the three-tuple block moved to sit immediately above `spec` and below `conformance`/`spec-review` (spec-review 4, conformance 5, epics-review 6, epics 7, vision 8, spec 9, plan 12); the TR existential alternative deleted; the ordering comment and test messages rewritten to the measured truth with the intra-three assertion kept as an explicit snapshot and a new pairwise-disjointness test on 8 positives; `\bepics?\s+review\b` and the `there'?s` alternative dropped; corpus verdict changes vs base 2 → 1 (the survivor `None → vision` on "create vision summary"); 245 passed; 11/11 declared phrases)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own execution (the fixups were prescribed verbatim from round 1's executed findings; stated) — round 2.
### Native layer (orchestrator execution in the worktree)
- `pytest tests/test_skill_router_hook.py -q` → 245 passed; `grep -c fabrik-epics-review` → 1; `ruff check` clean, `ruff format --check` 2 files already formatted; `check_trigger_routing.py` → 149 advertised, 108 reach their own command, 41 route nowhere, 0 mis-routed; the order read from the loaded module: `{'spec-review': 4, 'conformance': 5, 'epics-review': 6, 'epics': 7, 'vision': 8, 'spec': 9, 'plan': 12}`; probes through `first_regex_match`: "for a multi-epic project run /fabrik-conformance-review before step 2" → conformance; "vizyon dokümanı var" / "vizyon sorunu var" → None; "büyük bir vizyonum var" → vision; "the epic review process" → workflow-review; "review the epics" → epics-review; "there's a big idea in this paper" → None; "I have a big idea" → vision; the three contract prompts → epics / epics-review / vision; "the epics dir has four files" / "an epic failure" → None.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0060)
- All three CLEAN: the placement below `conformance`/`spec-review` and above `spec`/`plan` read from the indices; the TR existential row absent; the disjointness test's 8 positives include a Turkish one; the snapshot assertion's message honest ("snapshot, not a rationale"); every rewritten comment sentence true against the code; "review the epics" → epics-review, "the epic review process" → workflow-review; the 55-row snapshot 55/55; edits within the two Touches; no format churn beyond ruff.
- The orchestrator's corpus-harness run (the coder's `probe.py` against 5bda5c6f): item 1 → conformance; the three TR false positives → None; "the epic review process" / "mega epic review checklist" → workflow-review, "epics review meeting notes" → None, "review the epics" → epics-review; "there's a big idea in this paper" → None, "I have a big idea" / "I have a big multi-epic idea" → vision; 11/11 declared EN+TR phrases OK.
### Verdict
**0 findings — no-op round.** Ledger: placement/ordering · TR rows · rationale truthfulness · bare-noun collocations · fleet safety · format churn · gates — all swept, all clean. **Status: CONVERGED** at `5bda5c6f`; merge owner applies the CHANGELOG delta and the fleet sync (`.claude/hooks/` is a governance-sync trigger surface).
