# Review — decision-ledger shipped work, POST-SHIP fresh-eyes pass (operator-invoked)

Surface: 4d002df6 + working-tree md5 8e0a22a2eff4834915edb09c1556a53d at review start (the whole
shipped plan: 0d2ca091 · 18e38d9c · b321ea39 · 4d002df6) + the in-run fix 894dffdc.
Mode: NO-POOL — solo native; closing pass re-derivation.
Prior-report anchor: `2026-08-30-decision-ledger-plan-review.md` — its `Surface:` is a commit RANGE
(dd6e6172..HEAD), which cannot hash-match by construction → full WIDE pass 1 run, per the contract.
That mismatch earned its keep: the wide pass found what four converged loops (3 phase + 1 whole-plan)
had all missed.

## Rubric (review_rubric.py --changed <the cumulative primary paths> — verbatim section heads)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: scripts/decisions.py, scripts/enforcement/_doc_registry.py, scripts/fabrik_synced_manifest.py)
### core/40-documentation.md  (hit: CLAUDE.md, commands/_sources/fabrik-spec.md, docs/reference/decision-ledger.md)
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | security-auth floor (35) | CLEAN | re-adjudicated: no auth surface across the 4 commits (phase reviews' greps re-trusted only after the wide pass found their blind spot was elsewhere — the manifest side-effects, not the code they swept) |
| 2 | data-postgres floor (25) | CLEAN | no DB anywhere in the shipped surface (grep re-run over the cumulative diff: 0 DB-API uses) |
| 3 | ops floor (30) | CLEAN | no deploy/compose surface |
| 4 | 12-Factor | CLEAN | helper + sync remain stdout CLIs; no service |
| 5 | side-effect completeness (the class the miss belongs to) | FIXED(1) | **the manifest is consumed by THREE things** — the sync copier (reviewed), the unmodified-gate set (reviewed via SEEDED_NOT_ENFORCED), and `gitignore_block_text()` (NEVER reviewed by any prior loop): adding the GOVERNANCE_TEMPLATES pair silently gitignored `docs/DECISIONS.md` in all 48 repos, so no project could COMMIT a ledger row — the git who/when layer, a load-bearing spec claim, was dead on arrival. Hunt trail: H1's `git status` silence → `git check-ignore -v` → transdoc `.gitignore:66` → `gitignore_dest_paths()`. Fixed at the generator (SEED_IF_MISSING dests excluded, red-first, 12/12), commit 894dffdc; its own post-commit sync rewrote every project block; live verify: check-ignore exits 1 on 3 samples |
| 6 | parser robustness | CLEAN | H2: bold/backtick row ids are invisible to ROW_RE — adjudicated ACCEPTED: the seeded header shows the plain shape, the helper's no-hit line names its own denominator, and a formatting-drift row is a visible authoring deviation, not silent data loss; noted in the reference doc's remit (reading quality is per-repo agents') |
| 7 | portability | CLEAN | H3: helper runs from a project cwd (48 rows via absolute /opt/fabrik path — the exact invocation both CLAUDE.md texts teach) |
| 8 | output correctness | CLEAN | H4: a where-cell containing `·` prints intact (cells indexed, not split on the separator) |
| 9 | rollout integrity re-derived | CLEAN | H1 completed post-fix: the seeded file was never tracked ANYWHERE pre-rollout (git ls-files → 0), so nothing was overwritten; 48 seeds re-counted; hub ledger 12 rows |
| 10 | fail-open/fail-closed | CLEAN | the fix direction: excluding from gitignore FAILS toward visibility (an untracked file appears in status and gets committed) — the correct direction for data; seed-skip semantics unchanged (12/12 incl. both force paths) |
| 11 | boundary/sentinel/prefix | CLEAN | the exclusion is exact-dest membership (`dest not in SEED_IF_MISSING`) — CLAUDE.md + PORTS.md ignore lines pinned still-present in the same red-first test |
| 12 | cost/quota accounting | CLEAN | no spend surface |
| 13 | behavior-without-a-test | CLEAN | the new invariant has its red-first guard (`test_seed_if_missing_dests_are_never_gitignored`, watched fail); CHANGELOG Fixed entry rides this batch |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — full re-adjudication with four fresh hunts (H1 overwrite-audit · H2 parser shapes · H3 project-cwd · H4 separator-in-cell) | citation | solo native (NO-POOL; dispatched: 0) | 2 | 2 | 1 | F1 the gitignore side-effect (FIXED 894dffdc) · F2 bold-id invisibility (REFUTED as accepted-by-design, row 6) |
| Pass 2 | SCOPED — the fix + its consumers (block generator, scaffold emission path, sync block-replace) + live fleet verify | citation | solo native | 0 | 0 | 0 | 3-sample check-ignore exit 1; 12/12 suites |
| Pass 3 | CLOSING — re-derivation: gitignore_block_text() re-probed (no DECISIONS, CLAUDE/PORTS present); 48 seeds re-counted; --check exit 0; gate 54/0 fresh; H3 re-run 48 rows | method: re-derivation | 0 | 0 | 0 | TERMINAL |

Pass 3 = the no-op: `new: 0`, every candidate adjudicated.

## Step verdict (whole shipped surface)

Step verdict: PASS after FIXED(1) — the ledger now works end-to-end INCLUDING its git layer: a
project can commit its rows, which is what makes who/when tamper-evident. The finding class
(a manifest edit's third consumer never swept) is exactly what a genuinely fresh wide pass exists
to catch; the four prior loops were each scoped to the surfaces they named.

```json
{"status": "success", "tier": 2, "passed": 54, "failed": 0}
```
