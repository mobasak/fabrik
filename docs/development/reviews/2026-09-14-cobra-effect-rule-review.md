# Review — the Cobra Effect becomes a manifesto rule and the 14th universal anchor

**Status:** CONVERGED
**Surface:** `git rev-parse HEAD` = dffcca1a034e96833b7919860112fea5e545fc5b; `git diff HEAD -- docs/reference/operating-manifesto.md CLAUDE.md templates/governance/CLAUDE.md commands/_fragments/close-feedback.md docs/DECISIONS.md CHANGELOG.md` md5 9a96be5c7931af0edcc8c6e2247563e7 (98621 bytes) — re-pinned at the closing round; the start-of-review base was `7a4b161a`, four sibling commits back.
**Command:** /fabrik-review · **Changed:** `docs/reference/operating-manifesto.md`, `CLAUDE.md`, `templates/governance/CLAUDE.md`, `commands/_fragments/close-feedback.md`, `docs/DECISIONS.md`, `CHANGELOG.md`

## Coverage Checklist

Rubric invocation (verbatim output — the gate reads the generated header, never a prose mention):

```text
$ python scripts/review_rubric.py --changed docs/reference/operating-manifesto.md CLAUDE.md templates/governance/CLAUDE.md commands/_fragments/close-feedback.md docs/DECISIONS.md CHANGELOG.md
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3; TOOLING surface)

### core/40-documentation.md
- > **⚠️ `docs/OPERATIONS.md` + `docs/DEPLOYMENT.md` are FLEET-AI INTERFACES, not just docs (D-065).**
- **Tier-1 (author → verify → converge; the author leg is NATIVE while the pool is OFF, D-181 — `scripts/doc_reconcile.py`'s pool author cannot dispatch):** for each **mechanically-detectable** doc whose Doc-Sync trigger fired (`docs/QUICKSTART.md` · `docs/CONFIGURATION.md` · `docs/data-contract.md` · `docs/SERVICES.md` · `docs/OPERATIONS.md` — the reliable-signal subset), `scripts/doc_reconcile.py` dispatches a cheap OpenRouter-pool author (`libs.subagents`, `pick_models("docs")`) to emit a **minimal structured patch**, **verifies it before applying** (a symbol cross-check catches invented endpoints; the orchestrator injects a higher-assurance native-Claude verify), and loops to a zero-edit round. Runs per phase in `/fabrik-execute-plan`; never blocks (fail-safe). The other docs (CHANGELOG, INDEX, FEATURES, RESILIENCE, PORTS, the READMEs, `db/schema.sql`, …) have no reliable mechanical content-signal → they rely on the touch-on-change backstop below + your own edit (force-update, not force-correct).
- The SSOT is the type-aware registry (`scripts/enforcement/_doc_registry.py::PROJECT_DOCS`) — this table is its project-facing rendering, kept in step, never a second truth. `/fabrik-plan-after-chat` (the plan set's spine + tickets — the ticket-format authority) injects these rows per ticket as its `Docs:` line.
- Standalone work (not plan execution) → `Agent-Role: primary`. Trailers go below a blank line, above `Co-Authored-By`. ⚠️ The trailer block must be its OWN paragraph with NO blank line inside it: git parses only the LAST paragraph, and only if it is all-trailers. A blank line before `Co-Authored-By:` demotes everything above it to prose; so does a prose line glued to the top of the block. Measured 2026-08-15: 200 of the last 200 hub commits carried `Agent-Role:` and only 10 parsed, because the old example here shipped the blank line.
- **⚠️ Link it or it is decoration.** *Measured:* requests for files that do NOT exist came ~zero from AI bots — agents never go looking. It follows (inference, not measurement) that a file only gets read when something points at it: reference it from the docs index or README.
- ⚠️ **In THIS repo `llms.txt` is GENERATED** (`scripts/generate_capability_index.py`, refreshed daily) — never hand-edit it; change the generator. A project writing one by hand owns it.
- either way. Cheap and reversible — never at the expense of `OPERATIONS.md`/`DEPLOYMENT.md`, which are the load-bearing agent interfaces (D-065).
- **No skipped heading levels** — `##` to `###`, never `##` to `####`
- **Fenced code blocks only** — never indented code (AI treats it inconsistently)

### 12-FACTOR (all twelve axes)
- I codebase: shared code → fabrik-lib, never two apps in one repo
- II deps: every shelled-out binary installed + pinned in the Dockerfile
- III config: granular env vars; no secrets in code; no grouped env sets
- IV backing services: swappable by DSN/config change only
- V build/release/run: releases immutable; never hot-patch a container
- VI processes: stateless; session state → redis-main; no sticky sessions
- VII port binding: bind in-container; Traefik routes; no host ports:
- VIII concurrency: scale out; never daemonize or write PID files
- IX disposability: SIGTERM returns in-flight jobs to the queue; jobs idempotent
- X dev/prod parity: same backing services everywhere; no SQLite-for-Postgres
- XI logs: unbuffered stdout only; the app never writes/rotates a logfile
- XII admin: migrations/one-offs run against the deployed release, never startup

## MATCHED — none (no pack glob hits the changed paths; the FLOOR still arms you)

# promote-to-check_*: 7 injected mandate(s) look deterministically greppable — their backtick literals, one line each (the full mandates are ABOVE, not repeated: re-emitting ~20 FLOOR lines verbatim doubled the rubric and got it skimmed — web-ecommerce-factory 01M1QEY5, 2026-09-05)
- `docs/OPERATIONS.md` `docs/DEPLOYMENT.md`
- `scripts/doc_reconcile.py` `docs/QUICKSTART.md` `docs/CONFIGURATION.md` `docs/data-contract.md` `docs/SERVICES.md` `docs/OPERATIONS.md`
- `scripts/enforcement/_doc_registry.py::PROJECT_DOCS` `/fabrik-plan-after-chat` `Docs:`
- `Agent-Role: primary` `Co-Authored-By` `Co-Authored-By:` `Agent-Role:`
- `llms.txt` `scripts/generate_capability_index.py`
- `OPERATIONS.md` `DEPLOYMENT.md`
- `##` `###` `##` `####`
```

| Class | Status |
|---|---|
| Hunt: `docs/reference/operating-manifesto.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (the Phase 3 bullet cut 1047→~360 chars against a 148-char file median, its repo-local ids and its series removed; the Phase 3 gate restored to one condition; the operative half moved into § Binding's rollout law; the closing paragraph amended so the addition obeys it rather than contradicting it) |
| Hunt: `CLAUDE.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (item 5's "fell for eight rounds" replaced by the canonical series 8 · 6 · 5 · 7 · 4 · 2 with its window; the ambiguous "the outcome written down" reordered and given its artifact "IN THE SAME CHANGE, beside the mechanism"; the exemplar's own cheapest path — omit `--own-fix` — written in; marker bullet 14 parses under the real consumer's regex) |
| Hunt: `templates/governance/CLAUDE.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (same item-5 corrections; the marker sentence machine-derived from nine to fourteen; the Script-coupling rule rewritten to carry the anchor it now advertises — `check_governance_drift.declared_markers` + `_strip_anchor_index` report 0 of 14 anchors missing from this file's rule text, against 1 of 14 before the fix) |
| Hunt: `commands/_fragments/close-feedback.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (the `waste:` misdirection removed — that field means tokens SPENT without changing the outcome, the opposite polarity to gaming by under-spend; the clause now points at the terminal condition and `done --evidence`; a duplicated REFUSES sentence I introduced while fixing was caught and removed; corpus re-rendered, 36 of 36 installed commands carry the clause) |
| Hunt: `docs/DECISIONS.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (D-253 moved from the file's bottom to the top, the header's own "Append-at-top" convention; its series framing corrected — "fell" is false of a run that rises 5 → 7 mid-window — and the un-re-derivable round total dropped; numstat stays a pure 1/0 insertion, no existing row touched) |
| Hunt: `CHANGELOG.md` — every changed hunk, its enclosing function, its callers | CLEAN (one entry atop `## [Unreleased]`, shape `### Changed — <title> (2026-09-14)`; its "said The nine and listed 9 of 13" claim re-derived from `git show HEAD:templates/governance/CLAUDE.md` and `git show HEAD:CLAUDE.md`; the sibling hunks in the same file are untouched and will be excluded by a private-index commit) |
| Recurrence: fail-open/fail-closed — a swallowed error or an absent check that reads as success | FIXED r1 (the change ships no code, but its cited exemplar fails OPEN: `scope_growth_warning` returns "" when a round omits `--own-fix`, so the stop is advisory by construction — that cheapest satisfying path is now stated beside the exemplar in both contracts rather than left implicit) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted, a limit at its edges | CLEAN (no pool dispatch — D-181/D-182 stand; native seats only, 3 stamped for round 1 and 1 for each of the three delta rounds via `command_run.py dispatch`, sized first by `dispatch_headroom.py --slices opus=1,sonnet=2` which printed SEATS: 3 against a box cap of 17) |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | FIXED r1 (two off-by-window defects, both in the same claim: "fell for eight rounds" read the series' leading VALUE 8 as a round COUNT, and the cited window 8 · 6 · 5 · 7 · 4 · 2 silently omits round 14's confirmed count of 5 — the text now states the six values and the round-13 quiet boundary and asserts no total) |
| Recurrence: behavior-without-a-test — a contract row no test kills (mutation asserted) | RECORDED — by design (D-253) — the change adds no code, so there is no behaviour for a grader to kill; gameability is not expressible as a detector and the manifesto's own § Binding rollout law wants a measured fire rate before anything blocks. The executable guards it DOES have: `check_governance_drift.py`, which reported `missing: cobra-effect` against fabrik-lib until that repo landed the anchor at `26fb8c52` and is silent there now — a positive control on a copy reproduces the warning, so the silence is a satisfied check, not an inert one — and `assemble_commands.py --check` |
| Recurrence: denominator on every count — bounded searches state their bound | FIXED r1 (every number in the diff re-derived from primary source: 14 of 14 markers by the consumer's own parser; 36 of 36 rendered commands; 9 of 13 at HEAD for the CHANGELOG's claim; the 20-value confirmed series from the receipt's Pass Ledger; 375 and 0.15.12 → 0.14.10 from the mail body — the only stated total that could not be re-derived, the review's round count, was removed and routed to infra as `01M2G2X9R5FXSX88RGR86KDHHD`) |
| Recurrence: proxy-as-evidence — the real check EXECUTED, not read | CLEAN (the consumer's parser was IMPORTED and run over both contracts rather than reasoned about; `check_governance_drift.py` executed from /opt/fabrik-lib; `assemble_commands.py` rendered then `--check`ed; `check_review_hygiene.py` run over the claim, 6 mirror sites across 4 files, all read; `final_gate.py --json --check` run this turn) |

Verdict grammar (the gate refuses anything else): `CLEAN (<the paths/lines hunted>)` — a CLEAN row
must name a path and run past 70 characters · `FIXED r<n> (<what changed>)` · `REFUTED (<the
disproving line>)` · `RECORDED — unexecuted (<why>)` · `RECORDED — by design`, parenthesising the
owning row's first-cell id and the EARLIER round that adjudicated it, or a `D-nnn` with no round
(the § Residual block below shows the shape) · `RECORDED — measured (<why>)` ·
`RECORDED — hygiene false positive (<why>)` — every RECORDED reason is PARENTHESISED, never
colon-delimited (a colon would spell the `unexecuted: N` counter the ledger refuses). `UNCHECKED`
may survive only under a `## BLOCKED` escalation (a finding + 3 failed attempts).

## Pass Ledger

ONE table, one row per pass, counts punctuated (`found: F, new: N, confirmed: C, fixed: X,
unexecuted: U`) — the gate reads the LAST row as the exit round and refuses a second ledger group.
`found:` counts raw candidates, `new:` is prose the graders do not parse, `confirmed:` counts the
candidates EXECUTED and reproduced (the exit counter — a round is quiet at `confirmed: 0` and
`fixed: 0` with `unexecuted:` 0 or absent), `unexecuted:` counts code candidates RECORDED
unexecuted. Minimum two passes; the fixing pass is never the last; the closing pass re-derives
every count and anchor and says so in its Method cell, and its Finders cell names the seats that
read it by model token (`opus×1`, `sonnet×2`) — a round the orchestrator alone read cannot close.

| Pass | Finders | Counters | Method |
|---|---|---|---|
| Pass 1 | native opus×1 + sonnet×2 (the round-1 slices: contracts+anchors · manifesto · fragment/ledger/changelog) | found: 24, new: 24, confirmed: 18, fixed: 18, unexecuted: 0 | method: citation — full pass over all six paths, plus the orchestrator's own pre-pin probes (`probes/anchors.sh`, `probes/d252-series.sh`, `probes/lint-ratchet.sh`, `probes/uniqueness.sh`); 2 of the 24 were the orchestrator's own, 4 RECORDED (see § Residual) |
| Pass 2 | native opus×1 (fresh, non-authoring) · delta over the round-1 fix diff + one hop | found: 12, new: 3, confirmed: 10, fixed: 10, unexecuted: 0 | method: re-derivation — every fix re-executed against its consuming code (`check_governance_drift.declared_markers` + `_strip_anchor_index` over both contracts, `assemble_commands.py --check`, the receipt's Pass Ledger, `command_run.py` probed on a COPY); own-fix 10 of 10 — the first of D-252's two consecutive scope-growth rounds; 1 candidate RECORDED as another session's uncommitted work and routed by mail, not fixed |
| Pass 3 | native opus×1 (fresh, non-authoring) · delta over the round-2 fix diff + one hop | found: 6, new: 1, confirmed: 3, fixed: 3, unexecuted: 0 | method: re-derivation — all eight round-2 fixes verified CLOSED against their consuming code, none regressed; own-fix 2 of 3, because the third was a defect on the change's ORIGINAL surface (D-253 cited a "Phase-5 rollout law"; `rollout law` occurs only at manifesto:114 under § Binding, and Phase 5, lines 68–81, contains zero rollout/fire-rate/advisory language) — that breaks D-252's two-consecutive-all-own-fix streak, so this was not a scope-growth round and round 4 was owed |
| Pass 4 | native opus×1 (fresh, non-authoring) · delta over the round-3 fix diff + one hop | found: 7, new: 1, confirmed: 7, fixed: 7, unexecuted: 0 | method: re-derivation — R3-1/R3-2 CLOSED, R3-3 NOT CLOSED because fabrik-lib landed the anchor at `26fb8c52` 99 seconds after the row was written, making a present-tense claim stale; seven bookkeeping-truth corrections applied across the ledger row, the receipt and the manifesto's closing sentence. Residue: five rejected phrasings at 0 live hits over 2,952 tracked files (`/usr/bin/grep`, not `command grep` through `xargs` — a builtin cannot be exec'd there and returns a false clean zero). Gate re-run in-turn: success |
| Pass 5 | native opus×1 (fresh, non-authoring) · delta over the round-4 fix diff + one hop | found: 5, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | method: re-derivation — all seven round-4 corrections CLOSED; **no candidate meeting the confirm bar in 98 claims examined** across 10 files plus 2 executions. The seat re-derived every one with the consuming code: `_strip_anchor_index` over three blobs of fabrik-lib's `CLAUDE.md` (`26fb8c52^` False → `26fb8c52` True → live True), its own positive control on a copy (anchor reworded ⇒ the warning returns), the corpus-weight deltas against per-file `git show HEAD:<f> | wc -c`, and the Surface pin re-run byte-for-byte. Five items RECORDED below the bar, none reopening the loop |

Row shapes (quoted here, so the gate does not read them as passes):

```text
| Pass 1 | native opus×1 + sonnet×2 | found: N, new: N, confirmed: C, fixed: X, unexecuted: U | citation |
| Pass 2 | native opus×1 + sonnet×2 | found: 0, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | method: re-derivation |
```

## Residual

Every candidate that did not enter `confirmed:` is recorded here, one row each, in the verdict
grammar of `/fabrik-review` § Phase 2 (the fenced block under the next heading is an EXAMPLE, never rows).

### Verdict grammar — an EXAMPLE, never rows

(quoted so no reader — human or scan — takes this template's own sample rows for the receipt's residuals):

```text
| F12 | RECORDED — unexecuted (3 probe attempts timed out) |
| F19 | RECORDED — by design (F4, round 3; D-203) |
| F31 | RECORDED — measured (a prevalence figure; makes no code or doc claim) |
```

`RECORDED — by design` names the OWNING row's first-cell id and the EARLIER round that adjudicated
it (or a `D-nnn` with no round); the gate refuses an absent owner and a round that is not below the
closing `Pass N`. `RECORDED — measured` and `RECORDED — unexecuted` never enter `confirmed:`;
`unexecuted:` on the closing row is what keeps an unexecuted CODE candidate from closing the loop.

### Residual rows

| R1-a | RECORDED — by design (D-253) — "no enforcement check for gameability". A detector cannot decide whether a metric's cheapest satisfying path is cheaper than the work; the manifesto's own rollout law wants a measured fire rate before anything blocks. The executable guards this change DOES have: `check_governance_drift.py`, which reported `missing: cobra-effect` against fabrik-lib until `26fb8c52` landed the anchor there, and `assemble_commands.py --check`. |
| R1-b | RECORDED — measured (hub D-ids cited in the fleet-synced template are unresolvable from a project repo, whose ledger has an independent D-sequence). Pre-existing convention, not introduced here: 19 hub D-ids are already cited in that file. The highest non-hub id measured across the 49 `/opt` repos carrying a `docs/DECISIONS.md` is 230 in fabrik-lib, which is sync-excluded and never receives the template, and 162 in web-ecommerce-factory — so no synced repo can mis-resolve D-252 or D-253 today. Worth a fleet-wide convention decision when any synced repo passes 253; not this change's defect. |
| R1-c | RECORDED — measured (`assemble_commands.py --check` proves SYNC, never prose correctness — it renders sources to a tempdir and diffs byte-for-byte against the installed corpus). Stated so no later reader treats its green as a content verdict; the in-place reading of two rendered commands is what covers content. |
| R2-a | RECORDED — another session's uncommitted work, routed not fixed (mail `01M2G3MFRMVTKCWY34T98Z4QF2`, broadcast). A committed `[Unreleased]` entry — the 2026-09-13 account-rotation entry — is in HEAD and absent from the working tree; `CHANGELOG.md` was already `M` at session start, before this change's first edit, so the deletion is not this change's. Restoring it would overwrite a sibling's live WIP, the one hands-off case in the shared-tree contract. This change's own CHANGELOG entry is committed hunk-scoped through a private index whose blob is `git show HEAD:CHANGELOG.md` plus four lines, verified to preserve the rotation entry. |
| R2-b | RECORDED — measured (`check_review_hygiene.py --claim` keys on the anchor SUBSTRING, so a mirror restating the claim in other words is invisible to it: the prescribed invocation reported 5 hits over its 3 `--surface` files while a repo-wide `command grep` found the anchor in 6 files, and `commands/_fragments/term-edit.md` + `term-coverage.md` state the same claim with no anchor string at all). A machinery limit of the sweep, surfaced for infra; both fragments were read by hand in round 3's brief. |
| R2-c | RECORDED — measured (10 harness worktrees under `.claude/worktrees/agent-*/` still carry the pre-fix template text; they are `wt-foreign` checkouts, not synced surfaces, and the scratch-sweep contract forbids touching a worktree this run did not create) |
| R3-a | RECORDED — measured (`check_corpus_weight.py`, committed by a sibling AFTER this change was drafted, emits three advisories on it — `CLAUDE.md` +1678 B, `templates/governance/CLAUDE.md` +1763 B, `commands/_fragments` +537 B, each matching this change's own `git show HEAD:<f> | wc -c` delta exactly once the sibling hunks in those files were committed; read mid-round, before that commit, the first two were +4193 B and +4278 B because the check measures `origin/master → tree` and a shared tree carries every session's uncommitted bytes — its per-change attribution is only as clean as the tree is. Each asks the accepting review to cite the D-row naming what the growth RETIRES. D-253 names no retirement, and the honest answer is that this growth retires nothing — it adds a rule the operator directed. Stated rather than dressed up, because that ratchet is itself a metric and its own cheapest satisfying move is a review that says whatever the warning wants to hear. The check also reports `no baseline — run --seed`, so it asserts nothing yet; both the baseline and the warning's wording are the sibling plan's to settle.) |
| R3-b | RECORDED — measured (`commands/_fragments/close-feedback.md:34`, unchanged context: "Wall-clock and the round count are captured for you" omits the token totals, which `command_run.py` does capture and this change's new sentence eight lines above correctly says are captured. The understatement predates this change and sits in a paragraph it did not author; fixing it here would widen the diff on a fleet-appended fragment for no reader benefit.) |
| R3-c | RECORDED — measured (`command grep` is safe for a negative only as a DIRECT invocation: `command` is a bash builtin, so `xargs command grep` cannot exec it and returns a silent empty result with rc 0 — indistinguishable from a clean zero. A round-3 seat lost a real hit to exactly this and caught itself with `/usr/bin/grep`. The denominator-honesty row in `CLAUDE.md` § HARD STOPS names `command grep` as one of the two tools safe for a negative without naming that limit; routed to infra with the other machinery notes.) |
| R5-a | RECORDED — measured (at 17:37 the repo-wide Lint Ratchet read `ruff errors ROSE 0 → 3` — three B023 hits existing ONLY in a sibling's uncommitted edit to `tests/test_stop_hook_spontaneous_review.py`, whose HEAD blob lints clean; outside the six-path surface and hands-off by contract. That session committed, and the gate re-run at the flip is `status: success`, 64 passed, 0 failed.) |
| R5-b | RECORDED — measured (this receipt was UNTRACKED while the loop ran, so the gate's `untracked sources` advisory named it and no gate check had scanned it; it is committed with the change, which is what puts it in scope for the next run's gate.) |
| R5-c | RECORDED — measured (Pass 4's method cell omits the own-fix counter — 6 of 7 per the run record — that Passes 2 and 3 state. D-252's stop is computed from the record, not the receipt, so no reader acts on it.) |
| R5-d | RECORDED — measured ("99 seconds after the row was written" is unfalsifiable from git because the row lived in an uncommitted file; it is corroborated at the bounding end — mail `01M2G4DV3SVDKN2Q38ZSN3J2K9` at 17:16:37 +03, fabrik-lib's commit `26fb8c52` at 17:18:26 +03, 109 s.) |
| R5-e | RECORDED — measured (the Surface line calls the four intervening commits "sibling"; git attributes all four to author `t` with no `Agent-Name` trailer, so session attribution is not derivable from git — their subjects name three distinct work streams, which is the available evidence.) |

## Per-phase verdicts

### Phase 1 — the rule's placement and wording: PASS

The Cobra Effect is stated in `docs/reference/operating-manifesto.md` § Phase 3 (nested under
Second-Order Effects, whose second-order effect a measure's induced behaviour is) and BOUND in that
file's § Binding rollout law, which demands it be answered before any advisory check is promoted to blocking.
Round 1 rejected the alternatives on evidence, not taste: Phase 1 owns the moment a metric is CHOSEN
but has no enforcement surface; § Invariants is deliberately five items and this is not a discipline
that decays under time pressure so much as one nobody had named; Phase 5 closes an individual
decision's loop rather than authoring shared machinery. The file's own closing paragraph — "every
gain left is in binding, budgeting, and instrumentation" — is what settled it: the operative half
belongs in § Binding, and Phase 3 carries the statement it points at.

### Phase 2 — propagation to every command and rule: PASS

THE FIX DIRECTIVE item 5 of BOTH contracts carries the rule as the mirror of its fire-rate half
(fire rate asks whether a check fires WRONGLY; this asks what it REWARDS when it fires correctly).
`cobra-effect` is the 14th § UNIVERSAL governance marker, anchor **you get the behavior you measure**
— parsed as marker 14 of 14 by fabrik-lib's own `check_governance_drift.declared_markers()`, with
all 14 anchors present in each contract's rule text under `_strip_anchor_index()`, so no detector is
silenced. `commands/_fragments/close-feedback.md` is auto-appended by the assembler to every rendered
command: 36 of 36 installed commands carry the clause. The template distributes byte-identical to the
synced fleet on the next governance sync. fabrik-lib is sync-excluded and hand-maintained, so its copy
goes by mail; its drift check reported `missing: cobra-effect` within minutes and that repo landed the anchor in its own rule text at `26fb8c52` the same hour — declare, detect, reconcile, end to end.

### Phase 3 — the two measured instances: PASS

Both re-derived from primary source, twice, by seats that did not author them. D-252: the confirmed
series 8 · 6 · 5 · 7 · 4 · 2 is passes 15–20 of the receipt's Pass Ledger, the findings from round 14
on lay inside the review's own fix prose, and the series is NOT monotone (it rises 5 → 7 at passes
17 → 18) — round 1's "fell for eight rounds" was false on every reading and is gone. The lint ratchet:
`ruff_errors` unchanged at 375 while `ruff_version` moved 0.15.12 → 0.14.10, the OLDER value, so the
reference floats; `check_lint_ratchet.py` re-seeds at the current count and `git add`s the baseline,
which makes the prose an understatement rather than an overstatement. A round total that could not be
re-derived across a mid-review handoff was removed from D-253 and routed to infra as
`01M2G2X9R5FXSX88RGR86KDHHD`.

## Gate

`final_gate.py --check --json`, pasted verbatim at the flip (check_convergence reads the fenced
`"status": "success"`):

```json
$ python3 scripts/final_gate.py --json --check
{
  "status": "success",
  "passed": 64,
  "failed": 0,
  "skipped_checks": [
    "pytest"
  ]
}

(the hub's pytest leg is OFF by design — `skipped_checks` says so, so this green asserts the static and governance tiers and nothing about the suite; the touched surface is six markdown files and no Python.)
```
