# Acceptance review — T03b (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** IN-PROGRESS

**Surface:** the coder's worktree branch diff against the dispatch base (master after T03a's merge, 28de4900 lineage) — see the round sections below (one file per ticket, rounds APPENDED).

## Round 1 — over the merge-base..e9b84dc7 diff (scripts/epic_order.py +191/−15, tests/test_epic_order_disjointness.py +343; 47 tests, every contract row red on base; three on-disk mutations each red; hub epics before/after byte-identical; the two zitadel epics in phases 1 and 2)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.
### Adjudication (pool layer)
- gemini — CLEAN (the `*`-first two-pointer; the reach frontier with trailing `**` ≥1 and a target `**` consumable only by a pattern `**`; subsumption `src/app/**` ⊇ `src/app/models/**` and `src/a/*` vs `src/a/b/**` disjoint; `_tracked_files` repo-root-relative via `:/`, empty on OSError/non-zero; the cycle caught; the parallel contradiction; migrations via subsumption; empty owned_paths filtered; every `check_integrity` caller keyword-only).
- deepseek — 2 raised, both put to the native finder by execution: `_seg_matches("*", "**")` should be True (a literal `**` directory name) and its hand trace ends False — the exact sibling of the ordering trap T05a's finder found in its matcher (the coder claims `*`-first ordering; execution decides); the trailing-`**` ≥1 rule inside `_glob_subsumes` (a glob's own trailing `**` as the TARGET of another glob's `**`).
- qwen — 1 raised, UPHELD as a question: with a single `dangling` boolean, `phased_order(good)` may still run when only SOME epics have dangling deps, and an epic whose dependency is unknown is never schedulable, so the phasing raises "cycle" for what is a dangling target — a mis-named finding (E1→E2→E3→[999]); its `_tracked_files` item self-cleared.
### Native finder (opus) — executed: 14 contract fixtures (both overlap shapes, the `libs/**` twins silent over a real repo, different phases silent, migrations by equality and subsumption both directions, the cycle named in all four modes at rc 1, the dangling target named without "cycle"); a 75-pair matcher table (74 agree; the one divergence `_glob_matches("**","")` on the empty string — dead input, filtered); a 60,000-pair differential fuzz, 0 divergences; T05a's ordering trap ABSENT (`('*','**')` True and 10 more); subsumption SOUND — 0 false-fires over 4,000 random pairs against a 3,021-path corpus; 12-star chain 0.006 ms, 12 chained `**` 0.009 ms; `_tracked_files` repo-relative from the epics dir (2,798 paths, 14.5 ms), `[]` in a non-repo, full tree through a symlink, gitignored excluded, a submodule as one gitlink, deleted-but-tracked listed, `-z` survives a newline filename; `_realise` on 12,000 files ≤ 61 ms and fetched lazily only when a phase holds ≥2 epics; `--assign` byte-untouched per function md5 (its behaviour now refuses an overlapping set — the documented coupling); `--json` byte-identical on the hub's 3 epics in all four modes; the T05b simulation silent (`phases: [[1, 3], [2]]`); empty/absent `owned_paths` → `[]`; the three mutations red on a copy (7 / 2 / 3 failures), restore asserted; the property guard holds; 64 + 47 = 111 passed; ruff clean; stdlib; two files; no positional `check_integrity` caller anywhere (all CLI). 3 raised:
- [M] a WILDCARD-FREE entry (`src/app`, `src/app/`, `alembic/versions`, `db`) is invisible to BOTH predicates and to `_owns_migrations` — silent rc 0 on the ticket's own case (`src/app` vs `src/app/**` over a real tree → `[]`); T05a's sibling declares "a directory entry owns its subtree"; the finder validated the one-line subtree form in `_glob_subsumes` (all three gaps close, every negative control stays silent) and measured the naive expand-everything variant as unsound (200 false-fires of 495) → FIXUP (1).
- [L] the docstring's "exotic shapes" understates overlap-without-subsumption (5.7% of random pairs; `src/*/x.py` vs `src/a/*` ordinary) → FIXUP (2). [L] `parallel_with` naming the epic's own number silently accepted → FIXUP (3).
Merge-owed: CHANGELOG + INDEX rows for T03b (orchestrator Deltas). Round 1 verdict: 3 raised → 3 routed (1 M, 2 L); pool: gemini CLEAN, deepseek's `('*','**')` claim REFUTED by execution, qwen's dangling-vs-cycle question REFUTED (the dangling target is named without "cycle"). Not the no-op round.

## Round 2 — over `6a5c5990..291f79fc` (the round-1 fixup 291f79fc, 48/12 + 63/0: `_forms(pattern)` — a wildcard-free entry stands for itself AND its subtree, ORed into both predicates so `_realise`/`_owns_migrations` inherit it; 14 red on e9b84dc7 → green; the naive expand-everything variant proven load-bearing by an on-disk mutation (4 red); the docstring names overlap-without-subsumption with the soundness figure; `parallel_with` naming itself is a finding; 66 + 64 green)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 2.
### Adjudication (pool layer) — the draw returned three deepseek-family units (v4-flash ×2 + v3.2-exp); noted for the flywheel
- deepseek v4-flash (1) — "no defects"; its one edge: a wildcard-free FILE entry (`src/app.py`) also stands for `src/app.py/**`, so `_glob_matches("src/app.py", "src/app.py/models/m.py")` is True — harmless in practice (no real path has a file as a directory component) — put to the native finder for the false-finding question against a sibling; `.` → owns everything (correct for "the whole repo").
- deepseek v3.2-exp — CLEAN (5 rows; `_forms`; the docstring; the self-reference finding; 64 + 66 green).
- deepseek v4-flash (2) — CLEAN (rows; empty/absent owned_paths; the three `parallel_with` contradictions; separator-awareness; the subtree expansion; the perf test; 25 test functions).
### Native finder (opus) — executed: every `_forms` positive fires and every negative control stays silent (incl. the prefix trap `src/app` vs `src/apple/**`); the BC rows; the self-reference finding and its symmetric `depends_on: [1]` already a cycle; the edge inputs benign (a FILE entry's nonsense subtree form realises nothing); `_owns_migrations(['alembic'])` True by design (the dir containing `versions/`); the naive-variant mutation 4 red on a copy; performance unaffected (three bare dirs 104 ms on 12k files); `--assign` byte-untouched and every mode byte-identical to e9b84dc7 on the hub's epics incl. the written files; `_tracked_files` honest; the property guard; 66 + 64; 60k fuzz — the only divergences the intended subtree semantics. 3 raised:
- [H] `_glob_subsumes` (:441) ORs over BOTH sides' forms, so an outer covering only the BARE form of a wildcard-free inner is accepted — `docs/*` "subsumes" `docs/reference`, `src/*` "subsumes" `src/app` — and the check FALSE-FIRES on epics whose realised sets are non-empty and permanently disjoint (the message says "no realised file in common yet"); corpus 55 false-fires of 321 over 4,000 pairs — the failure mode the ticket forbids; no grader (the three new negative controls all have a glob inner) → FIXUP (1): quantify `all(any(…))` (measured UNSOUND 0/265 with every intended behaviour kept) with two mirrors — the wrong test row `("src/app/**","src/app",True)` flipped, and `_owns_migrations` kept loose locally (it regresses `['db/*']` under the strict predicate).
- [M] the docstring's two figures (0 false-fires; 5.7%/229) were measured on the pre-`_forms` predicate — at HEAD 55 and 360/4,000; the mutual-subsumption clause true in one direction only → FIXUP (2). [L] an entry with no segments (whitespace-only, `/`, `./`) now owns the whole repo (`[[], ['**']]`; `if p` drops only the byte-empty string) → FIXUP (3): strip at the parse filter.
Round 2 verdict: 3 raised → 3 routed (1 H, 1 M, 1 L); pool three-deepseek clean (its file-entry edge confirmed benign). Not the no-op round.

## Round 3 — over `6a5c5990..8374af73` (the round-2 fixup 8374af73, 27/12 + 43/2: the quantifier `all(any(…))` with both mirrors — the wrong test row flipped, `_owns_migrations` kept loose (its regression seen red under the strict predicate alone, then green); the docstring re-cites 0 false-fires / 265 fires / 4,000 pairs, 515/4,000 = 12.9% overlap-without-subsumption, 6 corpus-bounded misses, the one direction; whitespace-only entries own nothing; 74 + 64 green)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 3.
### Adjudication (pool layer) — again a three-deepseek draw (v4-flash ×2 + v3.2-exp)
- deepseek v4-flash (1) — CLEAN (5 rows; the edge cases; the quantifier revision; the loose migration reading; `--assign`/docs immutable; two files).
- deepseek v3.2-exp — CLEAN (rows 1–5 each with its test; the additional requirements).
- deepseek v4-flash (2) — 1 raised: a bare `owned_paths:` (no value) parses to `None`, survives the `str(p).strip()` filter as `'None'` or crashes `_pattern_segs` — rounds 1 and 2 EXECUTED that shape (the hand-rolled `_parse_frontmatter` yields `''`, filtered to no paths) so this is likely refuted; handed to the native finder to execute the bare / `~` / `null` / `""` / absent / empty-block shapes explicitly.
### Native finder (opus) — executed: the soundness script reproduces the docstring exactly (0 / 265 / 4,000 / 3,021; 515 = 12.9%; 6 corpus-bounded misses); the three false-fire fixtures silent; positives and the one-direction claim pinned; the loose migration reading — 9 True (`db/*`, `db`, `alembic/versions/…`, `alembic/**`, `db/schema.sql`) / 8 False (`db/README.md`, `db/seed.sql`, `alembic/env.py`, `docs/db`, `dbx`, `db.py`, …) — no false positive; double-reporting on `db/**` vs `db/schema.sql` adjudicated ACCEPTABLE (two orthogonal violations; each fires alone on its own fixture); the pool's bare/null claim REFUTED on ten shapes with a type-spy (only `str` ever reaches `_pattern_segs`; `~`/`null` survive as literal strings owning nothing); whitespace entries own nothing; cycle/dangling/self-cycle in six modes at rc 1 — the base crashed on default/`--json` and PASSED a cycle under `--check`; `parallel_with` four cases; `--assign` per-function md5 unchanged, the written files byte-identical branch vs base on the real hub epics; the hub with epic 3 identical in four modes; the five BC rows by CLI + the fnmatch-trap control; six mutations killed incl. the OR-quantifier revert; a `_forms`-aware 60k fuzz 0 divergences; reflexivity 400/400, transitivity 0 violations, C(4,2) findings, determinism; perf; ruff; stdlib; two files; 74 + 64. 1 raised:
- [L] `_pattern_segs` normalises `./` but not a bare `.`, so `owned_paths: ["."]` survives as a literal segment and the epic owns NOTHING — a missed overlap (`.` vs `src/app/**` → `[]` while `./`, `/`, `//`, `**` all fire) — outside the five BC rows, 0 instances in the hub's epics, the narrow normalisation the only defensible repair (a "matches nothing" warning would fire on the design's primary case) → FIXUP (1).
Round 3 verdict: 1 raised → 1 routed (L); pool three-deepseek clean with its bare-key claim refuted by execution. Not the no-op round.

## Round 4 — over `6a5c5990..29bd52ca` (the round-3 fixup 29bd52ca, 2/0 + 16/0, prescribed verbatim) — the CLOSING round
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own re-read of the verbatim-prescribed fixup — round 4.
### Native layer (orchestrator re-read of 8374af73..29bd52ca, executed)
- The diff is exactly the two lines (`if p == ".": p = ""` before the `./` branch) plus four graders (the `.`/`./`/`/` root entries each overlapping `src/app/**` in one phase — `.` red on HEAD; an interior `a/./b` still literal).
- Executed in the worktree: `_forms('.') == _forms('./') == [[], ['**']]`; `a/./b` matches itself and not `a/b`; 78 + 64 = 142 passed; `ruff check` clean; `re.compile(` once.
Pool: PENDING — appended when it returns.

