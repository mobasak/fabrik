# Review — the mail-queue session's nine commits (hooks · enforcement · synced rules · fragments)

Status: IN-PROGRESS — passes 1–13 recorded, pass 14 in flight; every pass since 11 has found one more pre-existing hole in the quota hold by execution, each fixed with a grader.

Surface: `8f0b9910957dd2ab26ccf4ff83aa9905194ea74b` + combined-diff md5 `8012bcaba5a597cb7b8eb0745d016ee5` (post pass-2; the pass-1 surface was `f4e67cfe…` / `e387e471…`)
(the surface is a COMMIT SET, not the working tree — `git diff HEAD` is empty at a clean tree, so its
md5 `d41d8cd9…` anchors nothing. The hash above is `git show --format=` over the nine commits, in the
order listed below.)

**Anchor: NO MATCH — there is no prior review artifact for this scope.** The newest reports on the box
cover other surfaces (multi-agent-per-repo plan, external-services chain, rotation drain, watchdog
default, mailbox drain). So this run is a FULL WIDE pass 1, inheriting no checklist rows.

## Scope

Nine commits authored by infra in this session, all pushed:

| sha | subject |
|---|---|
| `7af1fec1` | quota_stop: mask shell-quoted spans before the operator vetoes |
| `4567e606` | three plan-set gates whose OUTPUT could not be read honestly |
| `0dd638f6` | payments provider set is THREE, four hub surfaces said two (D-120) |
| `214d9e49` | three rule packs pointing at things that do not exist |
| `e5df1eed` | grounding fragments taught four ways to be confidently wrong |
| `5abab982` | check_convergence: out-of-surface gate attribution |
| `f35c6b64` | rotation: a retry bound a second probe host silently doubled |
| `c7a825ee` | skill_router: /fabrik-deploy-checklist had no prose stem |
| `f4e67cfe` | lessons 154 + 155 |

**Explicitly OUT of scope — siblings' interleaved commits on the same master:** `3ea3da24`,
`0276d2f8`, `51bff3e6`, `5850449f`, `c3ffa54a`, `83ef9d1a` (fleet's and intel's). 35 files in scope.

⚠️ **Blast radius: five of the nine touched governance-sync TRIGGER surfaces** (`.claude/hooks/`,
`scripts/enforcement/`, `.windsurf/rules/`) and have already distributed to ~46 repos via the
post-commit sync. A defect here is a defect everywhere, which is why (f) is a first-class class below.

## Rubric (verbatim, `review_rubric.py --changed <35 paths>`)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)

## MATCHED — packs whose globs hit the changed paths
### ai/00-ai-model-selection.md  (hit: .windsurf/rules/ai/20-vision.md)
### core/10-python.md  (hit: .claude/hooks/quota_stop.py, .claude/hooks/skill_router.py, scripts/aro-wake/claude_rotate.py)
### core/40-documentation.md  (hit: .windsurf/rules/ai/20-vision.md, .windsurf/rules/core/57-external-data-sourcing.md, .windsurf/rules/core/75-workers-jobs.md)
### core/45-testing-strategy.md  (hit: tests/enforcement/test_check_plan_tickets.py, tests/test_check_convergence.py, tests/test_check_ticket_breadth.py)
### core/62-using-subagents.md  (hit: commands/_fragments/subagents-core.md)
```

(210 lines total; the section headings above are the class source. Full output at
`scratchpad/rubric.txt` for this run.)

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| C1 | fail-open vs fail-closed on every gate/guard (standing) | CLEAN | N2 walked every guard the diff touched (`check_plan_dir` layout guard, `_staleness` root, PASS-row context gate, `GATE_OK`/`GATE_ANY`/`GATE_OUT_OF_SURFACE`): each fails closed on the missing-evidence side; N1 confirmed an unbalanced quote returns the RAW line so no operator is ever hidden (probe `git status 'abc` → allow only because no operator exists). Pass 1, 2 native finders |
| C2 | cost/quota/limit accounting edges (standing) | FIXED(6) | Passes 1–9 as recorded. **Pass 10 (P10-A) and pass 11 (P11-A):** two further fresh readers re-derived every figure from code (per-host 16.3s; 32.6/130.4; 32.6/65.2/260.8; N=4 by `_fleet_dirs()` and `assignments.json`) and found ZERO mismatches against the single-copy backlog row; the docstring's 17 numeric tokens all classify as constants, status codes, dates or history — no derived total. CLEAN twice |
| C3 | boundary/sentinel/prefix collisions (standing) | CLEAN | mask char `x` never creates a veto match (N1, 20 probes); `<scratchpad>` is an established corpus token (3 files: fabrik-review.md:119, fabrik-execute-plan.md:771,774); `GATE-SCOPE:` in check_convergence's own fix-hint cannot self-satisfy (regex requires digits, hint carries `<N>`); `deploy-checklist` vs `deploy` ordering measured over 44 prompts. Pass 1 |
| C4 | behavior-without-a-test (standing) | FIXED(4) | P4 named three `_mask_quoted` branches with no test (single-quote imbalance, `$VAR` in double quotes, non-special escape) — added to `test_the_masker_never_invents_or_hides_an_operator`; N2 named `_staleness` under external_root with no test — added `test_staleness_resolves_a_scratch_copy_against_the_given_root_too`. Pass 2 |
| C5 | `_mask_quoted` bypass surface | FIXED(21) | Passes 1–27 as recorded. **Pass 27→28 (own probe + pool 27 p1 + P27-A, all by execution):** the pass-27 cluster rule stripped DIGITS before asking whether the first `m`/`F` was last, so `-m5` read as a cluster ending at `m` and the next token was exempted — git takes `5` as the message and `.` as a pathspec: a sibling's staged file was committed (own probe; P27-A: four prefixes `-m5`/`-vm5`/`-qvm5`/`-om5`, same effect). The rule now reads the raw cluster: the next token is the value only when NOTHING follows the first m/F. Graded both ways (`-m5 .` / `-qm5 .` held; `-qm .` allowed — pool 27 p3's missing allow-side assertion), red on revert; ledger 26 of 26 denied. Named, not held: `-m"msg"` attached-and-quoted (shlex joins it to `-mmsg`; refused by the letter check — agents write `-m "msg"` with a space; pool 27 p5), `-F . -- f` exempts `.` as the file value (git refuses a directory as a log file; a readable file would only disclose its content into a message — not a sweep; P27-A #11), a `--` token is skipped unconditionally rather than modelled as a possible pending value (P27-A #9: every stressed form stayed safe; a residual soft spot, not an exploit). **Pass 26→27 (pool 26 CLEAN 5 of 5 — the run's first clean pool layer; P26-A on the pinned copy: ledger 25 of 25 denied, 15 new probes):** one real — `git commit -mF . -- f`: git attaches whatever follows the first value-taking letter as its value (`-mF` = message "F"), so the next token `.` is a pathspec; my pass-24 cluster rule keyed on the LAST letter and exempted it as the message — a sibling's staged file was committed (executed). Only a cluster that ENDS at its first m/F now takes the next token as the value; three forms graded, red on revert. Named, not fixed (measured): four HANG forms on the read tools — `wc -c /dev/zero`, `cat /dev/urandom`, `tail -f`, a bare `cat` on an open stdin — an availability class bounded by the Bash tool's own timeout; no legitimate held-session use was found in the exit template or the ladder, the cost is minutes not data, and a positive flag set for six read tools would be the next list to be wrong about. Refuted by execution: CR/FF/VT as separators (bash does not split on them), unicode dashes (git rejects), `-m -- -- f` (only the named file moved). 30 hook tests. **Pass 25→26 (pool 25 + P25-A, SHA-pinned):** two holes, both mirrors of my own earlier fixes. (a) An escaped NEWLINE inside double quotes: the masker's escape branch masked both characters, so the multi-line veto never saw it and a `$` after the pair was no longer span-opening — bash removes the continuation and hands git `"$F"` (pool 25 p1; executed: decide=allow) — the newline now stays visible. (b) `git add "./$FILES"` with `FILES='.'`: the `$` is not span-opening so the masker hides it, my pass-22 `normpath` collapses `./$FILES` to `$FILES`, every literal check passes, bash expands to `./.` — add, commit and reset each swept a sibling's work (P25-A, executed; re-proven here: a sibling's untracked file staged). A positional token (pathspec/refspec on push/fetch/add/commit/reset) containing `$` or a backtick anywhere is now held outright — the checker cannot evaluate it and bash will; the message value keeps its `$`-inside-text allowance. P24-A's 4: 4 of 4 denied; ledger 12 of 12. Also recorded: the pass-25 chain COMMITTED PAST a red test because `pytest | tail -1` hid the exit code from `set -e` — fixed in 25b and the chain now carries the code (a proxy read as evidence, on my own gate — the third time this run). Graders red on revert; 30 hook tests. **Pass 24→25 (own probe + pool 24 + P24-A, SHA-pinned):** my pass-24 design claim — "a quoted expansion is one word and cannot become a flag" — was FALSE for the whole-word form, and I falsified it myself before the finders returned: `git push bare.git "$F" HEAD:master` with `F='--force'` → decide=allow → `(forced update)` on the remote. P24-A confirmed 4 of 32 independently (`"$F"` twice, `git add "$X"` staged a sibling's untracked file, `git commit -m x -- "$FILES"` swept a sibling's staged work) and pool 24 p5 found the unquoted `$@`/`$*` gap in `\\$[\\w{]`. One rule closes the class: a `$` that OPENS a word is a veto — bare `\\$` on the masked line — and the masker keeps exactly that `$` visible (unquoted anywhere; first inside a double-quoted span) while masking a `$` inside text (`"fix $X"` starts with text and can never be flag-shaped). `cat "$F"` (a pass-24 allow) flips to deny, fail-closed. P23-A's 2: 2 of 2 denied; ledger spot-check 10 of 10. Refuted by reading: `-m ':tada: fix'` (not flag-shaped — allowed by the passing test), `--message=--amend` (a message), `-m -- -- f` (is_message_value is cleared at loop top). Two graders + three masker assertions (incl. the backtick-in-quotes branch pool 24 p3 found unasserted), red on revert; 30 hook tests. **Pass 23→24 (pool 23 + P23-A, SHA-pinned):** two closures. (a) The positional rule ran on the MESSAGE value — `git commit -m 'fix *.py?' -- f` was refused as a glob sweep (pool 23 p1, 7 forms): the token after `-m`/`-F`/`--message`/`--file` is exempt from the pathspec check (a flag-shaped message such as `-m --amend` stays refused — fail-closed, named). (b) `$VAR` word-splitting, named twice as an environment precondition, now proven severe: `git push $PV` with `PV='origin --force'` made a FORCED UPDATE on a remote and `git commit -m $MSG` with `--amend` inside amended a pushed commit (P23-A, executed on the pinned copy) — the checker's argv has one opaque token where bash has two. An UNQUOTED `$NAME`/`${NAME}` is now a veto on the masked line; the masker masks `$` inside double quotes unless it opens `$(` (a quoted expansion is one word and cannot become a flag), so `git commit -m "fix $X" -- f` stays allowed. P22-A's 4 forms: 4 of 4 denied. Named, not held: `-m -x` (flag-shaped message), a real file named `-notes.md`, the directory-pathspec and same-file residuals (re-proven). Two graders, red on revert; 29 hook tests. Also recorded: the pass-23 commit's `final_gate_stop.py` 72/32 is `ruff format`'s reflow of a never-formatted hook (100 lines on the parent alone; the pre-edit tree was byte-identical to the parent — no sibling WIP swept; behaviour unchanged, 106 tests). **Pass 22→23 (pool 22 + P22-A, SHA-pinned per the new brief):** `git commit -m -- --amend` — `-m` CONSUMES `--` as its value, so my shape check and the `--` end-of-options both saw a separator git never saw, and `--amend` walked through as a "path": an amend FIRED on a pushed commit (P22-A, executed; the same shape reaches `-F`, `git log -S -- --output=x`, `git push origin -- --force`). Root: a `--` end-of-options in the CHECKER is only right if the checker knows which flags take values — one more list to be wrong about — so every flag-shaped token is now checked wherever it sits (a real file named `--hard` is refused, fail-closed). Also: the message scan looked past `--` (`git commit -- -m` passed — pool 22 p1); `git commit -m x --` unasserted (p3); the prose omitted `../x` (p4); and the fleet's OWN commit templates lacked `-m` — 9 lines in CLAUDE.md § EXIT (×3), templates/governance/CLAUDE.md (×3), fabrik-upstream + fabrik-execute-plan sources, and P22-A's cross-hook find: `final_gate_stop.py` (×2) and `AGENTS-compact.md` print the form the hold refuses — all now `-m <msg> -- <paths>`; corpus rendered from master, `--check` OK. Named, not held: a directory pathspec (proven to bundle two of a sibling's unstaged edits under `docs/`), the same-file residual, `-mx` attached-value shorthand (fail-closed). Grader red on revert; 28 hook tests + 106 Stop-hook tests. **Pass 21→22 (pool 21 + P21-A):** `./../`, `././`, `docs/..`, `.//` passed the literal pathspec list (pool 21 p1, P21-A #3 — swept the parent/tree on disk) — the pathspec is now `posixpath.normpath`'d; a bare `git commit -m x` committed a sibling's STAGED hunks (the whole index — P21-A #2, executed) and `git commit -- f` reached git with no message (P21-A #5, saved only by the headless-editor refusal) — a held commit must now carry the template shape exactly: a message flag AND `-- <paths>`; the doc and docstring said push holds any `:` while the code holds a LEADING `:` (pool 21 p4) — prose fixed. Named, not held: `[` in a real filename (git reads it as a glob — refusing is right), a directory pathspec, and a NAMED FILE a sibling also edited unstaged (`git commit -- f` commits the working tree — git semantics, the hook cannot see who edited; the shared-tree snapshot discipline owns it). MACHINERY (own beat, fixed here): finders must read a SHA-pinned brief via `git show <sha>:<path>` — P21-A got two verdicts for one probe because my in-flight edit moved the live file; the reviewer brief now says so. Grader red on revert; 28 hook tests. **Pass 20→21 (P20-A sweep: P19-A's 23 now 23 of 23 denied; 24 candidates, 5 CONFIRMED on disk; pool 20 partitions 1, 2, 5 added five):** the positive set scoped FLAGS and never the PATHSPEC — `git add .`, `git commit -m x -- .` and `git reset -q HEAD -- .` each swept a sibling's untracked/unstaged/staged work (the HARD STOP row names `.` beside `-A`); a fetch/push refspec starting `+` or containing `:` force-moved a local branch (`git fetch origin +x:x`, proven) and `--` ended only OPTIONS so `git push origin -- +master` was a force push; `fetch --prune` deletes refs a checkpoint never needs; `command_run.py.bak` passed `\\.py\\b`; `/opt/../scripts/x.py` passed `[\\w.-]+`; `rev-parse -q` and `python3.12` were false denies; the deny message's own template `git commit -- <paths>` fails headless without `-m` (executed). All closed in one edit, grader 18 deny + 9 allow + the template assertion, red on revert. Named, not held: a DIRECTORY pathspec (`docs/`) still sweeps that directory; a `$VAR` pre-dating the hold; `-5p` and git's abbreviations (fail-closed). **Pass 19→20 (P19-A convergence sweep, 93 candidates, 23 CONFIRMED on disk; pool 19 partitions 1, 2, 5 agreed on parts):** the ROOT — the allow-list admits git VERBS with no flag scope, and every list of dangerous flags was wrong the next pass (14: `--output`; 17: `--upload-pack`; 18: quoted/abbreviated; 19: `--ext-diff`/`--chmod`; now `push --force`/`-f`/`+ref`/`--delete`/`:ref` (a shared branch DELETED and a sibling's pushed commit REPLACED on a bare remote), `commit --amend` (a sibling's commit absorbed — the fabrik-lib cc362a0 shape), `add -A` + `commit -a` (a sibling's WIP bundled), `reset -q HEAD --hard` / `HEAD^` / `HEAD~1` (an edit destroyed, a commit rewound — `HEAD\\b` has no trailing anchor), `--textconv` (a driver ran), `/x/scripts/command_run.py` (absolute look-alike ran), `date -s` (root-gated here)). Fix at the root: a POSITIVE per-verb flag set (`_GIT_VERB_FLAGS`: what a checkpoint or read needs), everything else held incl. git's own abbreviations; short clusters letter-checked; push refspecs may not start with `+`/`:`; `reset` only as `[-q] HEAD -- <paths>`; scripts anchor `/opt/<repo>/scripts/` or bare; `date` off. Pool-19 claims executed: `$VAR` holding a flag (CONFIRMED — an ENVIRONMENT precondition: the variable must pre-date the hold; not closed — a `$` veto would refuse every quoted message containing one; named), glob `--outp*` beside a planted `--output` file (CONFIRMED on disk — closed by the positive set: `--outp*` is not a listed name), `--chmo` (REFUTED: the prefix rule already held it; moot now). Grader: 27 deny + 27 allow forms, red on revert. The cost is named in the code and the doc: an unlisted READ flag is refused under a hold. **Pass 18→19 (pool 18 partition 1 + P18-A, both by execution):** `$'--output=m'`, `$'\\x2d\\x2doutput=m'`, `$"--output=m"` and `$'--upl=./p'` wrote/ran — bash decodes ANSI-C/locale quoting to a plain word while shlex keeps the `$` glued, so the argv veto saw no `--`; `$` before a quote is now a veto on the masked line (the quote char survives masking; inside double quotes it does not, so a message containing `$'` stays data). A bare `--` ends options (`git log -- --output=m` is a pathspec — proven: no file — was a false deny). P18-A's three: `../scripts/command_run.py` ran through the anchor (prefix now absolute or absent), `--ext-diff` re-ran a pre-set `diff.external`, `git add --chmod=+x` flipped a staged mode — the last two join the vetoed prefixes. Refuted by execution: `#` mid-word (shlex's default is `comments=False`), short prefixes `--o`/`--ou` (len 3 > 2 holds), quote-splitting/backslash/tab argv divergence, backslash-newline (the newline veto). BY-DESIGN, named: `git commit` runs `.git/hooks/*`; `--push-option`/`--server-option` carry data to the remote. Three graders, red on revert; 26 hook tests. **Pass 17→18 (orchestrator's own MIRROR of the pass-17 design, proven before dispatch):** the new flag veto read the MASKED line, but a flag inside quotes is still a flag to git — `git log "--output=m"`, `git log '--output' m` and `git fetch "--upload-pack=./p" .` wrote / ran under `allow`; and git accepts any unambiguous PREFIX of a long option — `--upl=`, `--receive=` ran the program (`--outpu=`/`--output-dir=` did not; refuted). Fix: the veto reads ARGV (`shlex.split`) on the RAW line and holds any token that is a prefix of a vetoed option (`--output`, `--output-directory`, `--upload-pack`, `--receive-pack`, `--exec` — push's documented synonym); unparseable ⇒ held; a flag inside a commit MESSAGE is data (token does not start with `--`). Grader with 9 deny + 5 allow forms, red on revert. This is the third time the raw-vs-masked split bit this hook: the allow-list (raw), the operators (masked) and now the tool flags (argv) each need their own reading, and the pass-17 fix put the third on the wrong line. **Pass 16→17 (P16-A, 17 candidates, 9 CONFIRMED on disk; pool 16 partition 1 agreed on the class; a pool-16 lookbehind claim proven):** the same class over what remained — `rg --pre <program>` and `git fetch --upload-pack=` / `git push --receive-pack=` RAN a program; `git branch -D` deleted a ref; `\S*command_run\.py` matched a planted `x/command_run.py`; and the `&` lookbehind excused a DIGIT (`git status 1& touch m`, `git log 2>&1& touch m` — both ran the second command). Plus one FALSE POSITIVE of my own pass-15 veto: whole-line `\s-o\s` refused the allow-listed `grep -o` while no allowed git verb accepts `-o`. Fix: `rg` + `git branch` off the list, scripts anchored to `scripts/`, `(?<!>)&`, and a git-scoped `_GIT_OWN_FLAGS` (`--output*`, `--upload-pack`, `--receive-pack`; no `-o`). Refuted by execution: `git fetch --exec=` (no such option), `git log -o` (never writes), `2>&-x` (already held), `git -c … <verb>` (allow regex wants the verb first); GIT_EDITOR/GIT_EXTERNAL_DIFF pickup is an environment precondition, not a command-text verdict — named, not fixed. Two graders, red on revert. **Pass 15→16 (pool-12 candidate widened by execution):** `sed -n` was allowed "never with `-i`" — a RAW-line lookahead wanting whitespace before `-i`; six forms passed and were proven on disk: `'-i'`, `-Ei`, `--in-place`, GNU prefixes `--i`/`--in` (each TRUNCATED the file to 0 bytes — `-n` suppresses the in-place output), and with no flag at all the script's own `w file` wrote and `1e cmd` executed. `sed` is off the list like `find`; `cat -n`/`grep -n` cover a held read. Grader lists all six + the former allow, red on revert. The first probe read `bash-wrote=False` for the four `-i` forms because it asserted the substituted text instead of ANY change — a wrong-assertion denominator, self-caught, re-probed (0 bytes). **Pass 14→15 (P14-A, 22 candidates, 5 with bash side effects):** `find . -name x -delete` destroyed a file through the allow-list, and `git log --output=<path>` wrote an arbitrary file — a tool's own flags, invisible to shell-syntax vetoes; my pass-14 "in-design" call on `--output` is OVERTURNED by the proof. `find` dropped from the allow-list; git `--output`/`--output-directory`/bare `-o` vetoed by a third pattern `_GIT_FILE_OUTPUT`. Six pre-existing holes in one hold this session, all found by executing a candidate with a side effect. The class the hold cannot close by construction is named in the artifact's residuals: any allowed tool's own write flags |
| C6 | `check_plan_tickets` external_root | FIXED(3) | Pass 1→2: `_staleness` threaded (N2), cwd NOTE added. **Pass 3, R-N2 (executed):** the NOTE compared the RAW `--project-root` to `Path.cwd()`, so `--project-root .` and a symlinked `$(pwd)` silenced it while resolving to the same tree — now compares resolved roots; grader `test_an_external_run_names_a_defaulted_root_even_when_spelled_as_dot` drives the real CLI three ways, red on revert. R-N2 also proved in-place vs external-with-lock runs byte-identical incl. the staleness WARN, and `--json` carries exactly one PASS row on the real 33-ticket set |
| C7 | `GATE_OUT_OF_SURFACE` semantics | FIXED(7) | Passes 1–8 as recorded. **Pass 9 (P9-A, fixture):** the fix-hint prints as one sentence naming N ≥ 1, the three value forms, the block-fence and hard-wrap exclusions; `/fabrik-review` and `/fabrik-execute-plan` say the same four things (Unicode ≥ vs ASCII >= is the only difference, both describing the same regex). CLEAN this pass |
| C8 | rotation twin + `_OAUTH_HOSTS` | CLEAN | `diff` of both files at HEAD: IDENTICAL; 1 definition, 0 stray host tuples; test accesses `cr._OAUTH_HOSTS` and passes (P1's AttributeError candidate REFUTED by execution). Pass 1, orchestrator re-derivation |
| C9 | `skill_router` ordering | FIXED(1) | Orchestrator corpus walk (44 prompts, old vs new matcher): 4 intended changes + **1 theft** — `"verify the deploy checklist"` old `deploy-verify` → new `deploy-checklist` (P1 independently raised the same class). Reordered AFTER deploy-verify; re-walked: theft gone, 4 intended routes hold; regression added. P1's "create the deploy checklist → deploy-checklist" REFUTED (that is the correct route). Pass 1 → 2 |
| C10 | BLAST RADIUS (~46 repos) | FIXED(5) | Passes 1→3 as recorded. **Pass 4 (P4-B):** the 20-vision line still carried a COPIED count ("eleven reachable routes") from the reach map — the drift its own sentence disowns — removed; the dated dead-vendor note stays. P4-B verified 88 § Payment Routing internally consistent (:32 predicate, :37 blockquote, :188 item) and the researcher mail id present on disk with matching content. Pool re-raises ("three providers implies all active", debian placeholder vs the rendered code block, Recraft still named, 57 ambiguity, DECISIONS-vs-85 canon) CITED to their pass-1/3 rows |
| C11 | D-120 row shape | CLEAN | 6 cells vs 6 separator cells (code spans stripped); no bare pipe (P3#7 REFUTED by count); no `supersedes` pointer to resolve; cross-repo `fabrik-lib D-080` is prose, and the BLOCKING uniqueness check passed at commit `0dd638f6`. Pass 1 |
| C12 | DENOMINATOR audit | CLEAN | Re-derived independently: router 28/33 static-mapped, of which user/service-test are the dynamic `test` stem → 4 of 33 unmapped pre-fix holds (method note recorded); capabilities.json 32/33 (absentee `design-review`, not fabrik-*); slim-bookworm pre-commit 7 incl. the Dockerfile line = 6 prose, 0 now; D-062 warnings 3; `graded 33 / 115 / 62`; `20 of 33`; 4 files in `0dd638f6`. P3's 7 candidates all REFUTED (two different tools' counts, mails ≠ surfaces, D-121 is fleet's). Pass 1 |
| C13 | core/10-python.md on hooks/scripts | CLEAN | N1: `now: float | None = None` on `decide()` is a dead parameter — PRE-EXISTING, outside this diff, no caller passes it; noted, not fixed here. The double `command is not None` guard is load-bearing (short-circuit before `re.match(None)`). ruff clean repo-wide. Pass 1 |
| C14 | new tests' quality | FIXED(1) | P4: red-on-revert guaranteed for each new test by construction (each asserts the fixed behaviour, not an import); no isolation leaks (tmp_path only, no monkeypatch); one vacuous assertion I wrote was already removed before commit; one wrong mask-length assertion of mine caught by the suite (5 → 6 chars). Pass 1 → 2 |
| C15 | doc-sync completeness | FIXED(6) | Passes 1–5 as recorded. **Pass 6:** the fourth GATE-SCOPE source (`fabrik-execute-plan.md`) aligned with the other three (P6-B). The rotation reference doc's ambiguous sentence rewritten (P6-A #2). Grep for live copies of every retired phrase: 0 outside `.claude/worktrees/*` (stale agent copies) and CHANGELOG history |
| C16 | subagents-core fragment | CLEAN | P5: `<scratchpad>` established (see C3); an md5 cell on a Pass row parses — `_pass_counters` returned `(0, 0)` on a 7-cell row; the contract text says extra columns ride the same row. Pass 1 |

## Pass Ledger

| Pass 1 | method: citation | found: 13 | new: 13 | fixed: 0 | finders: pool P1–P5 (deepseek-v4-flash ×2, gemini-3-flash, qwen3-max, deepseek-v3.2-exp — dispatched 5, returned 5, $0.026, all scored) + native N1 (quota_stop, 20 probes + 5 bash cross-checks) + N2 (plan_tickets + convergence, 53 tool uses) + orchestrator (C8, C9 corpus walk, C11, C12, C15 AFTER-EDIT walk) — 46 candidates raised (33 pool + 6 N1 + 4 N2 + 3 orchestrator), 13 adjudicated real, 33 REFUTED by execution or surrounding text |
| Pass 2 | method: citation | found: 0 | new: 0 | fixed: 13 | finders: orchestrator scoped re-check of every fix + its callers (398 tests across the four suites; each script fix proven red on revert against HEAD with the backup asserted before and after) — the SCOPED middle pass, never the exit |

| Pass 3 | method: re-derivation | found: 6 | new: 6 | fixed: 6 | finders: pool R1–R5 (fresh briefs, re-derivation method — dispatched 5, returned 5, $0.034, all scored) + native R-N2 (enforcement, 39 tool uses, fixtures executed) + native R-N1b (hooks, 20 tool uses; R-N1 stopped after 11 min with no return and re-dispatched — dispatched 8 total, returned 7, 1 stopped) + orchestrator re-derivation (surface hash, twins `cmp`, D-062 3, bookworm 0, breadth 20 of 33 counted from blocks, 33 tickets on disk, router corpus 45 pairs / 0 real mismatches, 433 tests) — 6 NEW real (2 regressions in pass-2 fixes: same-line `measured by:`, resolved-root NOTE; 3 minor pack/doc; 1 false safety claim in a docstring I wrote), ~20 re-raises cited to their pass-1 rows, the rest refuted by execution |

| Pass 4 | method: re-derivation | found: 4 | new: 4 | fixed: 4 | finders: pool (5 fresh briefs over the pass-3 diff — dispatched 5, returned 5, $0.039, all scored; every candidate a re-raise or refuted: 0 new) + native P4-A (enforcement regressions' fixes: 19 tool uses, 10+ regex probes, 9 CLI runs — 1 new) + native P4-B (rotation docstring + pack/doc edits: 26 tool uses, call graph re-derived — 3 new, one of them correcting MY pass-3 attribution) + orchestrator (post-297cf150 surface `59ebaa72…`, twins `cmp`, router 46/0, masker probes 7/7, corpus check OK) |

| Pass 5 | method: re-derivation | found: 3 | new: 3 | fixed: 3 | finders: pool (5 fresh briefs over the pass-4 diff — dispatched 5, returned 4 + 1 partial, 1 DIED (`status=error`, 0 chars; its router/rotation partition re-covered by the orchestrator's executed 46-prompt corpus walk and by P5-B), $0.035, all 5 scored) + native P5-A (fence-token semantics, 13 tool uses, 3 fixture reproductions — 1 new, 1 in-design) + native P5-B (rotation trio, 15 tool uses, every claim mapped to a line — 2 new, both wording of MY pass-4 paragraph) + orchestrator (surface `82e413ad…` at 37348dda, twins `cmp`, the 13-file grep resolved to 1 artifact + 12 stale worktree copies, `eleven` resolved to the historical note) — pool candidates: 2 grep-checked and REFUTED (no remnant two-provider sentence; :16 no longer names PayTR as the exclusion reason), the rest re-raises cited to their rows |

| Pass 6 | method: re-derivation | found: 4 | new: 4 | fixed: 4 | finders: pool (5 fresh briefs over the pass-5 diff — dispatched 5, returned 2, DIED 3 (all deepseek, `status=error` 0 chars; the death rate across six dispatches 0·0·0·0·1·3 of 5 filed to intel as `01M1QA45VGBPRV3K4J81GPFV8W`); the dead partitions (router/rotation, tests, fragments) re-covered by P6-A, the orchestrator's executed twins/corpus/fleet-count checks, and the fact that pass 5 changed no test or fragment) + native P6-A (rotation paragraph, 27 tool uses — 3 new, 1 HIGH) + native P6-B (GATE-SCOPE contract, 15 tool uses, 5 fixtures executed — 1 new doc-drift) + orchestrator (surface `e442639a…` at 2a791065, twins `cmp`, corpus check OK, 4 fleet accounts counted). ⚠️ The run-record printed its oscillation advisory at this round (findings 13·0·6·4·3·4): passes 3–6's residue is almost entirely ONE item — the rotation budget paragraph, rewritten five times by restating a number — plus fix-of-fix regressions; pass 6 closes that item by stating the invariant instead of a sixth number, which is the re-sweep-not-re-scope the advisory asks for |

| Pass 7 | method: re-derivation | found: 3 | new: 3 | fixed: 3 | finders: pool (5 fresh briefs over the pass-6 diff — dispatched 5, returned 5, $0.038, all scored; every candidate a re-raise cited to its row, incl. the shape-1-vs-3 "contradiction" REFUTED once more: exa cannot read JSON at all, so WebFetch-first for JSON does not contradict exa-for-exact-quotes) + native P7-A (invariant paragraph, 16 tool uses — 1 CONFIRMED arithmetic, 1 informational) + native P7-B (GATE-SCOPE four-source agreement, 9 tool uses — 2 completeness gaps) + orchestrator (surface `6bd45231…` at ab4e0ec1, twins `cmp`, corpus check OK, 3 GATE-SCOPE sources, retired phrases 0 outside the docstring's own history quote) |

| Pass 8 | method: re-derivation | found: 3 | new: 3 | fixed: 3 | finders: pool (5 fresh briefs over the pass-7 diff — dispatched 5, returned 5, $0.040, all scored; one executable candidate, "review the parity contract" stealing from review, REFUTED by execution — `review` precedes `deploy-checklist`; the rest re-raises) + native P8-A (single-copy figures, 14 tool uses — 1 leftover derived figure) + native P8-B (grammar vs 7 fixtures, 24 tool uses — 1 hint-parity gap, and INDEPENDENTLY the shared-index hazard) + orchestrator (surface `4b2d6713…` at 16420f99, twins `cmp`, corpus OK, governance sync run for the private-index commit that bypassed the post-commit hook: 45 projects). ⚠️ PROCESS FINDING, mine: the two private-index commits (passes 6–7) never realigned the SHARED index, so it held pre-pass-6/7 blobs for eight of my paths — a bare commit by any session would have reverted them silently (CLAUDE.md § EXIT names exactly this); measured blob-by-blob, realigned with `git reset -q HEAD -- <8 paths>`. And fleet's 31af158b had swept my uncommitted pass-7 CHANGELOG hunk (nothing lost), so my private-index commit re-added it — HEAD carried the entry twice; deduplicated in this pass's commit |

| Pass 9 | method: re-derivation | found: 2 | new: 2 | fixed: 2 | finders: pool (5 fresh briefs over the pass-8 diff — dispatched 5, returned 5, $0.040, all scored; every candidate a re-raise; the iyzico-V3 signature formula claim is OUTSIDE this diff and ungrounded — recorded as a residual to ground live when `core/85` is next touched, not asserted) + native P9-A (docstring + hint, 19 tool uses — 0 new) + native P9-B (commit hygiene, 16 tool uses, git plumbing only — 2 CONFIRMED process findings + 1 REFUTED) + orchestrator (surface `ad289975…` at bde9b72b, twins `cmp`, 0 stale index entries among my 9 paths at dispatch, corpus OK, 79 tests). The two process findings, both the shared-tree class: (a) my pass-4 commit 37348dda swept fleet's in-progress vps1 memory-limits CHANGELOG paragraph under my trailer — nothing lost, their 31af158b finalized it; fleet told (`01M1QBW5H9MYE4NRV8M0QY3ZXP`); (b) the shared index held a stale `docs/DECISIONS.md` blob (= my pass-3 commit's) one commit behind intel's bf3c7e02 — measured, `git reset -q HEAD -- docs/DECISIONS.md` (index-only), intel told (`01M1QBW5KJ4E7KT7WJ26A6AK3V`). REFUTED: "the governance sync never ran after the private-index commits" — it was run by hand after both (`sync_enforcement_to_projects.py --force` → 45 projects, 0 failed, twice), which no commit message records. Two further staged entries (LESSONS_LEARNT, the plan spine) are not mine and were left alone. Neither finding changed a tracked file, so the surface at bde9b72b is unchanged into pass 10 |

| Pass 10 | method: re-derivation | found: 2 | new: 2 | fixed: 2 | finders: pool (5 fresh briefs — dispatched 5, returned 1, DIED 4 across THREE families (gemini, qwen, deepseek ×2) — provider-wide, filed to intel as `01M1QC3FKAV7HFDM8KN94GQQEA`; the one survivor a re-raise refuted by execution in pass 8) + native P10-A (C2+C7 closing re-derivation, 24 tool uses, 11 fixtures executed — C2: ZERO mismatches across every figure and trigger word, the first fully clean certification of that paragraph; C7: 2 re-raises CITED to standing rows — the empty inline fence is the module's declared evidence-presence ceiling, adjudicated pass 5; blockquote/table placement executed clean in pass 3) + native P10-B (hygiene over all ~38 paths of the 16 commits, blast-radius sample of 3 files × 3 projects all hash-identical to HEAD, 16 trailers parse — 2 CONFIRMED stale index entries, `docs/LESSONS_LEARNT.md` and the multi-agent plan spine, both worktree==HEAD, index one sibling-commit behind) + orchestrator (0 stale among my 14 paths at dispatch, twins `cmp`, corpus OK, 589 tests, closing gate `status: success`). The 2 findings are the pass-9 class (a sibling committing outside the shared index leaves it stale) on two further paths — measured, reset index-only. SCOPE RULING for the remaining passes: stale-index entries caused by SIBLINGS' commits are a standing environmental row (filed to intel), cited not counted on re-raise; only staleness of MY paths counts against this review's surface |

| Pass 11 | method: re-derivation | found: 1 | new: 1 | fixed: 1 | finders: pool (5 fresh briefs — dispatched 5, returned 5, $0.040, all scored; the pool RECOVERED after the 4-of-5 death in pass 10; every candidate a re-raise or list-order-by-design) + native P11-A (whole-surface re-derivation, 29 tool uses: 15 masker probes, the full router corpus via pytest, the plan-set scratch/spine cycle, breadth head/tail counted from blocks and disk, two convergence fixtures, twins `cmp`, 17 docstring tokens classified, 435 tests — 1 CONFIRMED adjacent finding: `_FILE_REDIRECT` refused `>> /dev/null` and `2>>/dev/null`) + native P11-B (hygiene: 40 of 40 paths index-aligned, 16 of 16 headings once, 20 of 20 synced copies identical across five projects, 16 of 16 trailers — 0 findings, 0 environment) + orchestrator (0 stale among 14 paths, 0 staged entries anywhere, twins `cmp`, corpus OK). The one finding is OUTSIDE the diff (the regex predates this review) but on the same hook and this beat; fixed at minimum size with a grader, red on revert |

| Pass 12 | method: re-derivation | found: 2 | new: 2 | fixed: 2 | finders: pool (5 fresh briefs over the pass-11 diff — dispatched 5, returned 5, all scored; 0 new) + native P12-A (redirect regex, 26 probes with real `bash -c` effects — 1 CONFIRMED: `>&word` writes a file and was allowed) + orchestrator (10-probe table on the new regex — 1 CONFIRMED: the `/dev/null` exemption was a prefix match, `>/dev/nullx` / `>/dev/null/../x` / `>>/dev/null.txt` allowed). Both fixed with graders, red on revert; 0 stale index entries; synced copies match in 3 projects |

| Pass 13 | method: re-derivation | found: 1 | new: 1 | fixed: 1 | finders: pool (5 fresh briefs over the pass-12 diff — dispatched 5, returned 5, all scored; 0 new — every candidate a re-raise or list-order-by-design) + orchestrator (7 edge probes on the refined exemption; the `&` probe led to a marker-proven bypass: a lone `&` is bash's background separator and was never vetoed — fixed) + the late-arriving P12-A verdict (26 probes with bash effects — `>&word` writes a file; fixed in the same commit, with the `>&1x` whole-token slip caught by its own assertion). P13-A returned after the commit (26 probes + bash effects): its two CONFIRMED items were the two already fixed in 2e03f0f8; its third — a QUOTED `/dev/null` target is refused because the masker blanks it — REFUTED as a defect by denominator (0 of the hub's 606 `> /dev/null` idioms quote the target) and pinned as a known fail-closed limit; its MACHINERY note is taken: a brief that pins a SHA while the worktree carries WIP should say "extract that blob", not "load the hook" |

| Pass 14 | method: re-derivation | found: 3 | new: 3 | fixed: 3 | finders: pool (dispatched 5 over the pass-13 diff — result pending at this row's writing, recorded in pass 15's row) + native P14-A (final veto pair, 22 candidates, 5 bash-proven — 2 CONFIRMED: `find -delete`, `git log --output=`; process substitution seen fixed mid-flight) + orchestrator (marker-proven `<(touch m)` — fixed in 392c0959; `--output` first mis-adjudicated as in-design, overturned by P14-A's proof). All three fixed with graders, red on revert |
| Pass 15 | method: re-derivation | found: 1 | new: 1 | fixed: 1 | finders: pool 12 (returned 5, all scored; 24 candidates: 23 re-raises or refuted by execution — `deploy` stem exists at router:151, `<scratchpad>` is the corpus convention (4 of 4 uses), D-120's `fabrik-lib D-080` is prose and the dup-check keys on the ID cell, RevenueCat is scoped out at 85:18, "parity contract" is the checklist's own vocabulary — and ONE executable candidate, deepseek-v4-flash's quoted `sed -i`, CONFIRMED and WIDENED to sed's whole write surface: six forms proven on disk, fixed in pass 16's commit) + pool 14 (dispatched 5, returned 3, DIED 2 — gemini + qwen, `status=error` 0 chars — all scored; 2 test-quality candidates refuted by execution: the subprocess test passes from `/tmp` (1 passed), the "vacuous" first assertion is the pinned no-root baseline) + orchestrator (pass-15 commit 49aa18e6 via the private index, 22 hook tests, red on revert; rounds 12–14 recorded to the run record late — the record had stood at round 11 while the artifact carried 14 rows) |
| Pass 16 | method: re-derivation | found: 10 | new: 10 | fixed: 10 | finders: pool 16 (dispatched 5 over the pass-15/16 diff, returned 5, $0.036, all scored — partition 1 named the exec-flag class (`rg --pre`, `--upload-pack`/`--receive-pack`) and the `grep -o` false positive; partition 2 raised five lookbehind claims of which TWO proved on disk (digit-`&`) and three were refuted by execution or already held; partitions 3–5 CLEAN or denominator notes on controls) + native P16-A (allow-list tools' own write/exec paths, 21 tool uses, 17 candidates: 9 CONFIRMED on disk, 1 false positive, 7 refuted) + orchestrator (re-proved every claim before the fix; `git fetch --exec` refuted as non-existent). One class fix (pass 17's commit), two graders, red on revert |
| Pass 17 | method: re-derivation | found: 1 | new: 1 | fixed: 1 | finders: orchestrator only — the mirror the pass-17 design owed (every contract change names the shape it breaks): quoted and abbreviated git flags, 5 probes, 3 CONFIRMED on disk (m5, m6, m4), 2 refuted; then git's own abbreviation semantics probed (5 more: `--upload=`, `--upl=`, `--receive=` ran; two log prefixes did not) and `git push --exec=` confirmed as the documented `--receive-pack` synonym. No finder was dispatched over the pass-17 diff before this fix because the surface was known-stale; pass 18's finders take the combined diff. Also: the working-tree CHANGELOG was HEAD minus 4 of a sibling's committed lines (0 added) — restored from HEAD, nothing lost |
| Pass 18 | method: re-derivation | found: 6 | new: 6 | fixed: 6 | finders: pool 18 (dispatched 5 over the pass-17/18 diff, returned 2, DIED 3 — gemini, deepseek-v3.2, deepseek-v4-flash, `status=error` 0 chars, the pass-10 class again — both returned scored; partition 1 raised the ANSI-C bypass (CONFIRMED, 4 forms on disk) and the `--` false deny (CONFIRMED: no file); its `#` claim refuted by execution; partition 3 CLEAN with denominator 9 of 9) + native P18-A (argv veto + remaining verbs, 9 tool uses, 11 candidates: 6 CONFIRMED incl. the same ANSI-C hole independently, the traversal anchor, `--ext-diff`, `--chmod`; 2 by-design; 3 refuted) + orchestrator (doc audit: 0 stale allow-list descriptions in docs/ templates/ commands/ .windsurf/ .claude/ scripts/ CLAUDE.md — the population searched). One class fix — SPLIT across two commits by my own defect: the pass-19 edit script died on a SyntaxError before any edit applied, the chain ran on, and 96868261 shipped only the ANSI-C + `--` code under a message, CHANGELOG entry and artifact row claiming all five fixes; I read the green test line as proof of application (the proxy-never-evidence class, on my own work — the only proof of an edit is the edit's own print or a grep of the marker). The anchor, `--ext-diff` and `--chmod` code landed in the follow-up commit with the false claim named there |
| Pass 19 | method: re-derivation | found: 12 | new: 12 | fixed: 12 | finders: pool 19 (dispatched 5 over the pass-18/19 diff, returned 5, $0.038, all scored; partition 1: `$VAR` expansion (env precondition, named) + glob-expanded flag (CONFIRMED, closed); partition 2: six claims — EDITOR/diff.external/textconv/alias/`add -p`/`grep -f` — three env preconditions named, alias refuted (git refuses builtin-shadowing aliases), `add -p` interactive (no write; refused by the positive set anyway), `grep -f` is a read; partitions 3–4 CLEAN with counts; partition 5: `--chmo` refuted, subdirectory `scripts/` named as a known limit, `git reset HEAD^` CONFIRMED) + native P19-A (whole-ledger convergence sweep, 29 tool uses, 93 candidates: 23 CONFIRMED — the verb-scope root — 1 plausible DoS (`add -p` on an open stdin), 1 usability false positive (`/bin/cat`), 68 refuted/by-design) + orchestrator (every claim re-executed: `reset HEAD^` rewound a commit, `--outp*` wrote, `$OUT` wrote). One ROOT fix (pass 20's commit) — the flag lists of passes 14–19 are superseded by the positive set |
| Pass 20 | method: re-derivation | found: 10 | new: 10 | fixed: 10 | finders: pool 20 (dispatched 5 over the pass-19/20 diff, returned 5, $0.052, all scored; p1: `fetch --prune` (accepted); p2: seven claims — forced fetch refspec CONFIRMED by execution (`+HEAD:other` moved a local branch), `-5p`/`python3.12`/`+`-in-repo-name named as fail-closed (0 of 59 /opt names outside `[\\w-][\\w.-]*`), `--max-count5` and `--=` refuted; p3 CLEAN: 9 of 27 deny forms pre-denied, all branches reached; p4 CLEAN: 3 prose sources consistent; p5: `.py.bak`, `/opt/..`, `rev-parse -q`, `push -- +master` CONFIRMED, `-qHEAD` refuted (not git syntax), the EXIT ladder's `pull`/`rebase` named BY-DESIGN — a held session's rejected push DEFERS) + native P20-A (positive-set sweep, 21 tool uses: 23 of 23 P19-A forms denied; 24 new candidates, 5 CONFIRMED — `add .`, `commit -- .`, the chain, `reset -- .`, the `-m`-less template; 16 by-design/refuted; `python3.12` + `a+b` plausible) + orchestrator (re-executed the forced fetch). One edit (pass 21's commit) |
| Pass 21 | method: re-derivation | found: 5 | new: 5 | fixed: 5 | finders: pool 21 (dispatched 5 over the pass-20/21 diff, returned 5, $0.051, all scored; p1: `./../` CONFIRMED; p2 CLEAN over 11 flags; p3: coverage — 18 of 18 deny forms newly denied, one fallback branch unasserted (assertion added); p4: push-`:` prose mismatch CONFIRMED; p5: `[` in a filename — by design, named) + native P21-A (pathspec/refspec sweep, 37 tool uses, 14 candidates: P20-A's 5 → 4 closed at the hook + the headless form; 2 CONFIRMED new — bare `commit -m x`, `commit -- f` without `-m`; 1 corroborated (`./../`, already in my edit); 9 refuted/by-design; residual named: a named file a sibling edited unstaged; MACHINERY: SHA-pin the surface) + orchestrator (normpath + commit shape + prose + the reviewer-brief note; `docs/.` reclassified as the directory form). One edit (pass 22's commit) |
| Pass 22 | method: re-derivation | found: 6 | new: 6 | fixed: 6 | finders: pool 22 (dispatched 5 over the pass-21/22 diff, returned 5, $0.046, all scored; p1: message scan past `--` CONFIRMED; p2 CLEAN over 9 pairs; p3: `git commit -m x --` unasserted CONFIRMED; p4: `../x` prose gap CONFIRMED; p5: CLAUDE.md § EXIT template CONFIRMED — population 9 lines across 4 files measured by grep) + native P22-A (commit-shape sweep on the PINNED copy, 37 tool uses, 31 candidates: 1 CRITICAL self-discovered — `-m -- --amend`; 1 cross-hook doc mismatch — `final_gate_stop.py`/`AGENTS-compact.md`; 2 by-design-risky proven; 2 false positives saved by git's own boundary check (`//x`, `~/x`); P21-A's 5: 5 of 5 denied) + orchestrator (the class fix: flags checked everywhere, no end-of-options in the checker). One edit (pass 23's commit) |
| Pass 23 | method: re-derivation | found: 2 | new: 2 | fixed: 2 | finders: pool 23 (dispatched 5 over the pass-22/23 diff, returned 4, DIED 1 — deepseek-v3.2, $0.049, all scored; p1: message-value false deny CONFIRMED (7 forms); p2: three claims — `--patch` as a filename by design, `rev-parse +force` refuted (a read), `-qm` refuted by the passing test; p3: two unasserted branches (bare `--` asserted now), one docstring nit; p5 CLEAN over 10 verbs) + native P23-A (flags-everywhere sweep on the pinned copy, 22 tool uses, 23 candidates: P22-A's 4 → 4 denied; 2 false denies (one closed here, one named); 2 residuals re-proven; 2 CONFIRMED severe — `$PV` forced push, `$MSG` amend; exit end-to-end allowed; the two Stop-hook strings match `_reason` verbatim) + orchestrator (masker + veto + message-value exemption). One edit (pass 24's commit) |
| Pass 24 | method: re-derivation | found: 3 | new: 3 | fixed: 3 | finders: pool 24 (dispatched 5 over the pass-23/24 diff, returned 5, $0.049, all scored; p1 CLEAN over 10 `$` forms — WRONG on `$@` (p5 caught it); p2: four claims, all refuted by reading; p3: denominator note + the backtick branch unasserted (CONFIRMED, assertion added); p4 CLEAN; p5: unquoted `$@`/`$*` and quoted `"$@"` CONFIRMED) + native P24-A (whole-ledger sweep on the pinned copy, 20 tool uses, 32 candidates: 4 CONFIRMED — the quoted whole-word class; 7 residual `$`-forms named; P23-A's 2 denied; ledger 10 of 10 denied) + orchestrator (falsified my own pass-24 claim on disk before the finders returned: `"$F"` forced the remote). One class fix (pass 25's commit) |
| Pass 25 | method: re-derivation | found: 2 | new: 2 | fixed: 2 | finders: pool 25 (dispatched 5 over the pass-24/25 diff, returned 5, $0.051, all scored; p1: escaped-newline hole CONFIRMED by execution; p2 CLEAN with counts (9 verbs, 78 long flags, 12 operators, 22 patterns); p3: a wrong denominator claim (`cat "$F"` was NOT pre-denied — `\\b` sits between `t` and the space) and a vacuous-control note; p4 CLEAN; p5: the deny message does not say variables are refused — named, not fixed: the message names the exact shape and a variable is not in it) + native P25-A (`$`-rule sweep on the pinned copy, 23 tool uses, ~30 probes: 1 class CONFIRMED on disk — `./$FILES` on add/commit/reset; `"x$F"` in flag/refspec positions named safe-by-coincidence (git's exact matching); 13 `$`-shapes refuted; P24-A's 4 denied; ledger 12 of 12) + orchestrator (the pass-25 chain's red-test slip named and fixed in 25b) |
| Pass 26 | method: re-derivation | found: 1 | new: 1 | fixed: 1 | finders: pool 26 (dispatched 5 over the pass-25/26 diff, returned 5, $0.050, all scored: CLEAN 5 of 5 — p2 with counts 47/33/15/112, p3 confirmed all 5 new deny forms as NEW denials, p4 consistent) + native P26-A (full ledger on the pinned copy, 17 tool uses: 25 of 25 ledger forms denied; 15 new probes — 1 CONFIRMED sweep via `-mF`, 4 availability hangs (named, measured, not fixed), 1 plausible tty hang, 3 refuted by execution, 6 by-design) + orchestrator (cluster value semantics; the LESSONS_LEARNT entry for the run's three proxy slips written). One edit (pass 27's commit) |
| Pass 27 | method: re-derivation | found: 1 | new: 1 | fixed: 1 | finders: pool 27 (dispatched 5 over the pass-26/27 diff, returned 5, $0.058, all scored; p1: `-m5` digits CONFIRMED; p2 CLEAN with counts (11/2/14 allow-list, 8 verbs, 76 long flags, 46 letters); p3: missing allow-side assertion CONFIRMED (added); p4 CLEAN; p5: `-m"msg"` false deny named) + native P27-A (cluster rule on the pinned copy, 9 tool uses, 23 candidates: 4 CONFIRMED — one class, digits after m; 9 by-design; residuals #9/#11 named; ledger 26 of 26 denied — reconstructed from the code's own fix history, the finder flagged, not a byte-identical replay) + orchestrator (probed `-m5` on disk before either finder returned; the digit strip moved after the value rule). One edit (pass 28's commit) |

(Pass 14 raised 3, fixed; Pass 15 — a fresh sweep of the pass-14 fixes — follows.)

## Gate

(re-measured in the closing pass, never inherited)
