# Acceptance review — T01b (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** IN-PROGRESS

**Surface:** the coder's worktree branch (worktree-agent-ae36d1bbd7a83c091, head b79c918b) against its merge base 7b71c336 — 3 files (`scripts/sync_enforcement_to_projects.py` +152, `tests/test_sync_worktree_adoption.py` +169 new, `.claude/settings.json` +4). Coder: native Sonnet worktree; 6 passed; both R1 probes recorded (plain `.worktreeinclude` and the 5-`#`-header variant both copy the gitignored file — comment lines are inert, the T01a-routed finding is CLOSED); fire rate measured by one `--dry-run`: 3 of 45 projects carry linked worktrees (seo 28, trade-intelligence 23, web-ecommerce-factory 31 = 82), so the R3 loop ships (FIX DIRECTIVE 5).

## Round 1
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.

### Adjudication (pool layer)
- gemini — CLEAN over the 3 Touches (seed: local scope, idempotent, key order matches scaffold, silent on non-git; resync: porcelain parse, `is_dir` on each entry, dry-run early return; settings block exact; tests assert real git state).
- qwen — 3 raised: (1) `git config --local --get` non-zero conflates "unset" (rc 1) with "error" (rc ≥2) so a corrupt config skips seeding — carried to the native finder as an evidence item (best-effort semantics are documented in the docstring: "unwritable: silently skip"); (2) the "Re-syncing worktree artifacts into N linked worktree(s)" line prints under `--dry-run` — NOT a defect: the ticket says "print the count" and dry-run's job is to report; (3) copytree-from-a-stale-main speculation without a failing input — no finding.
- deepseek — 7 raised, 5 self-refuted in its own text (dry-run branch, pruned entries, count-once, dry-run writes, capsys order — all "actually correct"); 2 stand: `tests/test_sync_worktree_adoption.py:161` pins the baseline as the hardcoded sha `7b71c336` via `git show` (fails on any checkout without that object; a hub-only test, so bounded, but the sha is a magic constant), and `:168-169` compares `hooks`/`permissions` as PARSED JSON while the ticket says byte-identical — a reflow or key reorder passes. Both CONFIRMED by the orchestrator's read of the test (quoted lines 150–169).

### Native finder (opus) — 12 raised (every check executed against the branch worktree)
- [H] `resync_worktree_artifacts` (~:315-331) copies `.mcp.json` — plaintext API keys in every project — into linked worktrees that do NOT ignore it: the sync patches only the MAIN checkout's tracked `.gitignore`, a worktree's copy comes from its older branch. Measured: 82 of 82 live worktrees unignored (`git check-ignore -v .mcp.json` → no rule; main checkout → `.gitignore:173`); `.env` 0 of 82 by branch age only. → FIXUP (1): seed the essentials into the shared `info/exclude` or refuse the copy; real-invariant test.
- [M] docstring (~:301) records a fire rate of ZERO as the keep-justification — false: 3 of 45, 82 worktrees, seo alone 267 files × 28 per sync → FIXUP (2).
- [M] the R3 premise is wrong: `.fabrik/synced.lock` is not among the 56 `worktreeinclude_text()` patterns, so a worktree has no lock and `check_synced_unmodified.py:68-71` SKIPS (never "green against a stale copy"); the resync call (:809) precedes the lock write (:897) → FIXUP (3): move the call after the lock, copy the lock, test byte-equality; rewrite both docstrings.
- [M] `tests/…:161,168` pins `git show 7b71c336:` — a simulated SessionEnd hook addition reds it; 8 hooks/permissions commits since 2026-08-01 → FIXUP (4): durable invariant, no SHA.
- [M] "R1 probe has no receipt" — REFUTED as delivered: the receipt is THIS artifact (D4); both probe outputs are recorded verbatim below.
- [L] key order reversed vs `scaffold.py:5991` → FIXUP (5). [L] orphans never pruned → FIXUP (6, mirror the main sync or state it does not prune). [L] `--backup`/`--force` not threaded → FIXUP (7). [L] count line printed before any copy and swallowed by `tail -3` in production → FIXUP (8). [L] `ruff format --check` would reformat the test → FIXUP (9). [L] `--dry-run` "Would set" ×82 is one-off → no change.
- [L] `.claude/settings.json:137` `baseRef: "head"` is NOT inert on the hub (CLI 2.1.258 schema: applies to `--worktree`, EnterWorktree and agent isolation; the hub has 9 live worktrees; no delta today only because `origin/master == master`) — the plan's Global Constraint "the settings block is inert on the hub" is FALSE → recorded for the whole-plan review (T16), no code change.
- [L] the branch conflicts with current master (`git merge-tree`: CONFLICT in the script at the insertion point, c22bd91c) → FIXUP (first step: merge master).
Coder claims verified: (a) seed semantics ✓ except key order; (b) resync edge cases ✓ (non-git, prunable, outside-anchor, missing source, symlinked path); (c) settings ✓ — `hooks`/`permissions` md5-identical, old text a byte-exact prefix of the new, schema-valid; (d) 6 red-first by revert ✓ (the weak "attribute absent" red); (e) fire rate ✓ exactly (3 of 45; 82 = 28+23+31; 12 of 45 repos have worktrees somewhere, only 3 under the anchor). Both changed files are governance-sync triggers (`.pre-commit-config.yaml:143`); the worktree commit did not fire it (`governance_sync_postcommit.sh` pwd guard), the main-checkout merge will — and that run is the one that would write `.mcp.json` into 82 worktrees, which is why (1) merges before it.

### R1 probe receipts (verbatim from the coder's run; the ticket's "receipt")
- plain `.worktreeinclude` naming `carried.txt` (gitignored, untracked): `claude -p --worktree probe-agent … "git branch --show-current; cat carried.txt"` → `"result":"worktree-probe-agent\nSECRET_CARRIED_VALUE_12345"` — the gitignored file was copied in.
- the same with the tracked template's 5 `#` header lines above the entry → `"result":"worktree-probe-agent2\nSECRET_CARRIED_VALUE_67890"` — comment lines are inert (T01a-routed question CLOSED; no manifest/template change).

Round 1 verdict: 12 raised → 10 fix classes routed to the coder (1 H, 3 M, 6 L), 1 refuted-as-delivered, 1 recorded for T16. Not the no-op round.

## Round 2 — over `5fd58526..16b59ddd` (36,454 B; the round-1 fixup: master merged at 90d568a3, ten classes, 12 passed + 40 neighbours)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 2.
### Adjudication (pool layer)
- gemini — CLEAN over the 8 behaviours (config seeding, worktree discovery, secrets-exclude seeding via the git-common-dir, check-ignore verification, dir mirroring with pruning, `--backup/--force`, lock syncing after the lock write, settings durability).
- qwen — CLEAN; the info/exclude seeding judged safe for 45 repos because `.env`/`.mcp.json` are already in the synced `.gitignore` block (a repo-wide safety net, not a behaviour change).
- deepseek — 1 raised: `_project_worktree_dirs` returns ALL linked worktrees, not only those under `.claude/worktrees/`, contradicting its docstring — the round-1 native finder proved the opposite by probe on b79c918b ("worktree outside `.claude/worktrees/` → skipped via `relative_to`"); whether the fixup changed that is carried to the native finder (a docstring-vs-code check).
### Native finder (opus) — the nine round-1 items HOLD under mutation (52 passed); 8 raised:
- [H] `_sync_dir_into_worktree`'s orphan prune deleted UNTRACKED worktree files with no output and no `--dry-run` preview (the dry-run path returned before the loop) — a worktree with one committed + one uncommitted file under `scripts/enforcement/` lost both, reported "Re-synced 0 file(s)" → FIXUP (1): a real prune contract (absent from source + previously synced + untracked), every deletion printed, dry-run enumerates.
- [M] the secrets warning was a permanent false positive for a TRACKED `.mcp.json` (23 of 23 trade-intelligence worktrees, every sync) → FIXUP (2): one NOTE per project. [M] the docstring's `.env` claim was false (0 of 82 unignored) and its reason wrong → FIXUP (3). [M] the `--backup`/`--force` threading survived four mutants → FIXUP (4). [M] the production wrapper's `tail -3` swallowed the count line and every WARN → FIXUP (5): fold the numbers into `Results:`.
- [L] `__pycache__/*.pyc` copied (36 of 269 per seo worktree) → FIXUP (6). [L] the secrets floor seeded only after the no-worktree early return → FIXUP (7). [L] the settings test pinned "worktree is the last key" and byte-equality → FIXUP (8).
Round 2 verdict: 8 raised → 8 fix classes routed; pool 2 CLEAN + deepseek's docstring-vs-code claim carried. Not the no-op round. (Fixed at 6d3c7698, 23 passed — round 3 re-verifies.)

## Round 3 — over `5fd58526..6d3c7698` (65,943 B; the round-2 fixup: eight classes, 23 passed — a real prune contract, one NOTE per tracked secret, measured docstrings, four mutant-killing tests, the tally in the `Results:` line, `__pycache__` skipped, the exclude seeded before the early return, durable settings pins)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 3.
### Adjudication (pool layer)
- gemini — CLEAN over 17 checks; 2 L: the per-project summary line excludes worktree files while the final `Results:` includes them → carried into the fixup (3); a tuple/iteration micro-cost → no finding.
- deepseek — 2 raised, both CONFIRMED by reading the predicate: (1) [H] condition (b) reads the worktree's lock, which is the MAIN checkout's lock copied in — it names paths the worktree never received (secret-skipped, force-skipped, failed copies), so an agent's untracked edit to a later-retired path is deleted; the round-2 guarantee is defeated → FIXUP (1): a per-worktree ledger of what the resync itself wrote; (2) [M] the worktree copy path drops every non-COPY/BACKUP result — a no-force WARN on a locally modified file vanishes → FIXUP (2).
- qwen — 2 raised: a file untracked-but-ignored via a local rule still warned — the floor runs `git check-ignore` first (an ignored file never reaches the tracked/untracked classification) → carried to the native finder to confirm by probe; the tally under `--dry-run` (truncated) — `main()` clears it and only real runs increment — carried.
### Native finder (opus) — all 8 round-2 fixes HOLD by execution (the prune's 3-part predicate + ORDER proven; one NOTE per tracked secret; the docstring numbers re-surveyed 82/82; 4/4 mutants killed; the tally in `Results:`; `__pycache__`; the exclude seeded on a zero-worktree project; the settings test durable and still grading); suite 63 passed; ruff/format clean; mypy = master's 3. The "225 warnings" DETERMINED: it was the COPIED count (5 changed synced files × 45 projects — sibling/master advancement since round 1); the live warning population is 60 (59 unprotected `.mcp.json` + 1 vendored-copy line). 9 raised:
- [H] the re-copy plants `libs/subagents/` + `libs/health_probe/` into every worktree and the sync's own `_unreachable_vendored_copies` detector reports each as a stray (0 → 8 on a 4-worktree fixture; 82 × 2 = 164 on the first real run, inside the wrapper's `tail -3`) → FIXUP (1): the detector skips `.claude/worktrees/`.
- [M] the production wrapper's `--force` would rewrite the whole set in all 82 live worktrees per governance commit (19,024 files, 340 MB, mtime churn), clobbering in-flight agent edits → ORCHESTRATOR DECISION (reversible; ledger row at merge): in a worktree, force never overwrites a locally modified file → FIXUP (2). (Mirror of round 1's "thread --force": a widened flag reached the live trees.)
- [M] `--dry-run` omits the worktree segment from `Results:` → FIXUP (3). [M] `--dry-run` skips the exclude seeding so it warns 59 times a real run does not and hides 59 copies a real run makes → FIXUP (4).
- [L] the dir prune `rmdir`s any empty dir unreported → FIXUP (5). [L] the prune's `unlink` ignores `--backup` → FIXUP (6). [L] "orphan(s) removed" past tense under dry-run → FIXUP (7). [L] the copied main lock as "proof of authorship" (also deepseek's pool item) and [M] the discarded WARN → the coder's in-flight ledger + WARN surfacing (corroborated).
Round 3 verdict: 9 raised → 7 new fix classes + 2 corroborating the in-flight fixup; pool 2 confirmed, 1 refuted, 1 carried. Not the no-op round.

## Round 4 — over `5fd58526..08cf1ae2` (98,232 B; the round-3 fixup: a per-worktree ledger as the only prune authority, the WARN surfaced, `.claude` pruned from the vendored-copy walk, `--force` gated per file by the ledger hash, dry-run tally + post-seed preview, gated dir prune, backup on prune, wording; 33 passed + 40 neighbours)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 4.
### Adjudication (pool layer)
- deepseek — CLEAN (4 rows; DO-NOTs; the ledger's first run; the agent-deleted path re-copied; the force gate; the excludesFile overlay cleaned; `--dry-run` writes neither ledger nor seed; no fail-open).
- gemini — CLEAN (ledger first run :276; ledger-only pruning :448; the stale-hash force gate :348; dry-run ledger isolation :246).
- qwen — 4 raised, all "missing test" claims (seeding on a zero-worktree full run; the first-run ledger; the stale-ledger overwrite; dry-run writing the ledger) — carried to the native finder, whose brief probes each by execution rather than by test presence.
### Native finder (opus) — all ten round-3 fixes HOLD by execution (73 passed; ruff/format clean; mypy = master's 3; the live dry-run's per-project lines sum to the `Results:` figure exactly; only the 2 known strays; 0 false secret warnings; dry-run provably write-free; the ledger probes all correct incl. corrupt/legacy shapes; 83 vs 82 = two new web-ecommerce-factory worktrees created at 14:40/14:42). 8 raised:
- [H] the force gate's fallback is `sync_single_file(force=False)`, whose only protection is the MTIME tiebreak — in production the agent's edit is OLDER than the freshly synced hub file (copy2 propagates mtimes; the wrapper runs --force right after), so it is overwritten silently, no WARN, no backup, and the path enters the ledger as sync-authored; live: 106 of 19,256 pairs `exists_differ_older`, 0 protected → FIXUP (1): a hash gate against the ledger on BOTH paths.
- [M] the "never clobbers" tests stamp +3600 s — the region the old tiebreak protects — so they prove a proxy (sign flipped → fail) → FIXUP (2). [M] `docs/workflows/SYNC_ENFORCEMENT_WORKFLOW.md` now wrong (`--force → COPY (forced overwrite)`; none of the new mechanisms) and the script's header mandates it; no `# AFTER-EDIT:` header → FIXUP (3, a fourth file). [M] dry-run silent on the `info/exclude` seed (a write into 45 projects) → FIXUP (4). [M] dry-run under-reports empty-dir prunes (1 vs 2) → FIXUP (5).
- [L] `--backup` prune litter inside the worktree → FIXUP (6). [L] a deliberately deleted file re-created every run — document as the prune's mirror → FIXUP (7). [L] `--force` re-copies byte-identical files (~19k writes per governance commit) → FIXUP (8): skip on equal hash.
Round 4 verdict: 8 raised → 8 routed (1 H, 4 M, 3 L); pool 2 CLEAN + qwen's coverage claims subsumed. Not the no-op round.

## Round 5 — over `5fd58526..5919aa33` (121,953 B over the three non-settings paths; the full diff is 4 files incl. `.claude/settings.json` +6; the round-4 fixup 5919aa33: ONE function `_copy_into_worktree_safely` with the four-way HASH decision — dest missing → copy; hash(dest)==hash(source) → skip, force or not; hash(dest)==ledger record → copy; else WARN "edit in the worktree preserved" — mtime never consulted; every never-clobbers test parametrized ±3600; the dry-run discloses the exclude seed and counts would-be-empty dirs from the would-delete set; prune backups under `<project>/.fabrik/backups/worktrees/<name>/`; the doc's worktree section + corrected decision flow; the `# AFTER-EDIT:` header; 41 passed; 8 of 41 red on the round-3 script)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 5.
### Adjudication (pool layer)
- gemini — CLEAN (the four contract rows by line; the hash matrix bypassing mtime; the worktree-own ledger; dry-run parity; the secrets floor + status gate; backups outside the tree; `.claude` in `_PRUNE_DIRS`).
- qwen — CLEAN (each row and each round-1–4 invariant named to its test).
- deepseek — 4 raised: "settings.json declared as a Touch but not in the diff" — REFUTED (the pool diff was cut on three paths; `git diff --stat 5fd58526..5919aa33` shows 4 files, `.claude/settings.json | 6 +` — an orchestrator diff-cut error, not a coder defect); the module-level `_WORKTREE_TALLY` residue across tests [M→L] → put to the native finder by execution; `_seed_worktree_secrets_exclude` failing silently except `OSError`, so the per-worktree WARN cannot distinguish "unprotected" from "the floor could not be written" [L] → native finder; `force` still accepted and forwarded though inert [L, dead parameter] → carried to the next fixup.
Native finder (opus): INTERRUPTED by the fleet-quota hold at 4 of 7 classes — executed so far: 41 passed; the −3600 parametrization is load-bearing (all four `[older]` variants red on the round-3 script while every `[newer]` twin passed; 8 of 41 genuinely red-on-revert, 2 environmental); all four hash branches executed on a real fixture asserting file bytes AND the ledger row (`--force` changed nothing on the identical and agent-edit branches; mtime −3600 preserved with WARN); `--force` never re-copies an identical file; the deliberate-delete mirror documented in both places; the doc's decision flow matches the code's branch order (:653/:660/:663/:675); the header present. 2 raised, both doc-only [L]: the § Worktree Re-sync preamble anchors all three steps "after the lock is (re)written" while git-config seeding runs ~100 lines earlier (:1516 vs :1614); "every REAL sync run" is wrong for item 1 (seeding runs under `--dry-run` too) → carried to the next fixup. Un-swept (resumed after the hold): the 11 ledger/dest corner cases, the dir leg on an independent fixture, dry-run parity + write-free proof, the backups dir's exclusion, the main-checkout path diff + its three suites, the live fleet dry-run sweep, ruff/mypy. Verdict pending the resumed sweep.
### Native finder (opus) — RESUMED and COMPLETE: all 7 round-4 classes swept by execution — 12/12 corner cases WARN-not-crash (corrupt/int/bogus-hash/legacy ledgers, absent row, both drifted, a symlink dest replaced without writing through its target, a dangling symlink replaced, dir-where-file left with its contents, file-where-dir untouched, 0-byte, chmod 000 dest and source); the dir leg's four branches + the prune of a retired file and its emptied dir; dry-run provably write-free (whole-tree md5 equal over 70 entries) with tallies equal to the real run; `Would seed` once per unseeded project — 45 visited, 41 lines, the 4 silent are non-git dirs; the main-checkout span byte-identical to base (md5 d17552a3…, `sync_single_file`/`_atomic_copy`/`create_backup`/`prune_retired_scripts`) + 40 neighbour tests; mypy delta 0; ruff clean; headers OK; 41/41; the 4-file diff; the fleet dry-run re-done from inside the worktree after catching a cwd reset that had run master's script (that first result discarded) — 83 worktrees re-compared, 0 changed, 0 ledgers or backups created in /opt. Pool claim (a) REFUTED (`main()` resets the tally at :1772; every reader takes a delta); (b) and (c) UPHELD as findings. 6 raised:
- [H] `:1003-1004` — `except OSError: continue` is scoped to a whole PATTERN; when `(src_dir / rel).exists()` (:758) raises on a locked subdir, nothing prints, the partial `authored` map is discarded and `_write_worktree_ledger` (:1010) REPLACES the ledger with the truncated map → the ledger is WIPED, the next run WARNs on files the sync itself wrote, the worktree never converges again, no visible cause (executed: run1 2 rows → run2 "Re-synced 0" while a2 was written, ledger [] → run3 refuses its own a.py) → FIXUP (1): the ledger is MERGED (seed `authored_this_run` from `previously_authored_rel`), the handler WARNs + counts.
- [M] `:675` — the first fleet run WARNs "edit in the worktree preserved" on 83 files and 5 of 5 hash-sampled are ledger GAPS (wt ≠ main == hub, ledger ABSENT — stale copies from an older branch, 0 ledgers exist yet), and since the WARN path writes no row all 83 (10 worktrees) stay frozen forever → FIXUP (2): split the message by cause; document the bootstrap. [M] `:381` + `:986-990` — an unwritable `info/exclude` withholds the secret correctly but the WARN sends the operator to the branch's `.gitignore`, which cannot help (the c22bd91c class on a sibling path) → FIXUP (3).
- [L] `<project>/.fabrik/backups/worktrees/` untracked, NOT IGNORED in 3 of 3 sampled projects (fires only under manual `--backup`; the wrapper passes `--force` only) → FIXUP (4, the manifest's gitignore block). [L] the doc preamble misplaces two of three steps (seeding at :1516 precedes the .gitignore patch :1522 and the lock :1614, and runs under dry-run) → FIXUP (5). [L] `force` is a dead parameter through two functions (documented "API symmetry") → FIXUP (6).
Round 5 verdict: 6 raised → 6 routed (1 H, 2 M, 3 L); pool: 2 CLEAN, deepseek 1 refuted (my diff cut) + 2 upheld + 1 refuted by the native finder. Not the no-op round.

## Round 6 — over `5fd58526..89b4252b` (5 files: the script +111/−28, its tests +204, the workflow doc, `scripts/fabrik_synced_manifest.py` +6 (`/.fabrik/backups/` in the gitignore block's "Local state" section), `.claude/settings.json` +6; the round-5 fixup: the ledger MERGED never rebuilt with the prune-loop OSError now WARN+counted and pruned paths popped; the WARN split by cause (live: 83 of 83 are ledger gaps, 0 "edit preserved"); the floor-write failure named at project and file level; the dead `force` dropped; the doc preamble corrected; 46 passed, 5 of 5 graders red on the round-4 revert, 40 neighbour tests green)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 6.
### Adjudication (pool layer)
- deepseek — 1 raised, UPHELD on the letter: the ticket's DO-NOT reads "touch the manifest or the tracked `.worktreeinclude` (T01a)" and `scripts/fabrik_synced_manifest.py` is in the diff. Disposition: ORCHESTRATOR-DIRECTED deviation — the round-5 fixup item 4 (the untracked `.fabrik/backups/` dir) was routed by the orchestrator to the manifest's gitignore block because that is the single source the projects' block is generated from; the DO-NOT was a coordination guard against T01a, merged at 7b71c336 before this round, so the overlap it prevented cannot occur. Recorded here and in the merge commit body; the manifest path is carried explicitly in the merge; the fleet-wide effect (every project's generated block gains one line on the next real sync) is stated for the native finder to size.
- gemini — CLEAN in substance (the ledger MERGE at the caller, prune gated by the worktree's own ledger AND tracked status, the settings key exact); its [L] on the zero-worktree summary matches the contract by its own reading; its [M] "`mkstemp` without an encoding" → adjudicated below by reading.
- qwen — CLEAN (4/4 rows; the merge/pop, the WARN wording, key order/seeding/idempotence, tally/dry-run, prune safety, backup placement, the secrets floor; 126 cases across 28 functions).
### Native finder (opus) — executed: the three-run merge chain (rows preserved through an injected mid-pattern OSError, WARN counted, run3 converged); the zombie pop; a stale surviving row never authorises an overwrite (both legs × ±3600, asserting bytes AND the row); the four-way decision on both legs; the deliberate delete re-created with its row refreshed; dry-run leaves the ledger bytes unchanged, whole-tree hash equal over 72 entries, tallies equal; the WARN split one fixture each (the gap path writes NO row); the documented bootstrap works and the next hub advance converges; no path writes a row without writing or byte-confirming the file; the seeder's bool + the right-cause WARN with the secret withheld; `/.fabrik/backups/` at line 81 of the block and `git check-ignore` IGNORED; `_sync_dir_into_worktree` without `force`; the doc's call order matches (:1599 → :1612 → :1706 → :1731); 13 corner cases WARN-not-crash; prune backups outside the tree; the main-checkout span byte-identical (md5 091d1af3…, 5,914 B); 46 + 40 green, settings ok, the settings diff purely additive; 5/5 graders red on revert; ruff/format clean; mypy delta 0; the live dry-run read-only over 84 trees (83 gaps / 0 edits / 41 Would seed / 82 Would set / 0 orphans; 0 ledgers or backups in /opt); exactly five files. Could not execute: the R1 `claude -p` `.worktreeinclude` probe (a receipt line, gates no code); a real fleet sync (forbidden). 7 raised:
- [H] REGRESSION from the merge: the prune predicate (:796-807) is MEMBERSHIP-only while the copy predicate (:678) demands the recorded hash equal the file — a WARN'd agent-edited path's surviving row is full prune authorisation, so when the hub retires that path the agent's edit is DELETED (executed; red-on-revert: 5919aa33 survives, 89b4252b deletes; production never passes `--backup`) → FIXUP (1): the prune requires the same hash proof as the copy.
- [M] `--backup` on the ledger-refresh COPY path (:685-689) writes `.backup.<ts>` litter INSIDE the worktree (round 4 fixed the prune path only) → FIXUP (2). [M] the ledger write (:564) is not atomic — a truncated ledger reads as `{}` and every advanced path becomes a permanent gap → FIXUP (3): tmp + `os.replace`. [M] the doc's decision flow (:218-219) still shows one WARN branch with a string the code no longer emits; :232 falsified by (1) → FIXUP (4). [M] the pattern-level `except` (:1078) discards `_sync_dir_into_worktree`'s partial `authored` map though its loop already wrote files → the old row survives with a hash matching neither disk nor source → the sync's OWN write reported as an agent edit, wedged forever (executed through run4) → FIXUP (5): return the partial map.
- [L] `scripts/fabrik_synced_manifest.py` has no `# AFTER-EDIT:` header (pre-existing; touched here) → FIXUP (6). [L] the `/.fabrik/backups/` line re-patches the TRACKED `.gitignore` of all 45 projects on the next real run (45 dirty trees; not flagged by `check_synced_unmodified` — `.gitignore` is not in synced.lock) and the default dry-run names it only under `--verbose` → FIXUP (7): a `Would patch .gitignore` preview line, per the round-4 precedent.
Round 6 verdict: 7 raised → 7 routed (1 H, 4 M, 2 L); pool 2 CLEAN + deepseek's DO-NOT letter upheld as an orchestrator-directed deviation; gemini's mkstemp [M] REFUTED by reading (`os.fdopen(fd, "w")` writes fixed ASCII patterns to a scratch file; the real seed uses utf-8). Not the no-op round.

## Round 7 — over `5fd58526..48b463fe` (5 files; the round-6 fixup as an untrailered checkpoint 9200310a under the quota hold + the trailered 48b463fe naming it: the prune requires the hash proof; the refresh-path backup relocated; the atomic ledger write; the doc fence split; per-item OSError isolation in both loops; the manifest header; the `Would patch .gitignore` preview — live: 45 of 45 on the first sweep; five fixtures moved to real hashes; 50 + 40 green; every grader red on 89b4252b)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 7.
### Adjudication (pool layer)
- deepseek — `status=error`, 0 chars (the zero-output unit class iterative_image_editor filed 01M1R…; the floor of one returned reader is met by the other two; not relaunched — the native layer is the authoritative sweep this round).
- gemini — CLEAN (the hash-match prune :2287; the atomic write :2130; backups under `.fabrik/backups/worktrees/` on both paths :2167/:2263; per-item isolation :2334; the two fleet-wide writes disclosed under dry-run :1704/:2407; the summary line survives the wrapper's `tail -3`; the settings block).
- qwen — CLEAN (4 rows to tests; DO-NOTs; the 67-test suite covering the 20+ historical defect scenarios incl. the round-6 prune regression and the atomic write).
### Native finder (opus) — executed: the round-6 regression chain holds (the agent's file survives the retire-prune with the new WARN); the prune-row matrix (matching row → pruned + popped; unreadable file → WARN, row kept; a directory with an agent child → left alone; agent-deleted file → nothing pruned); the binding decision over 16 of 16 combinations (both legs × row present/absent × ±3600 × force) — row present → refresh, absent → preserved + WARN, force and mtime never deciding; `--backup` on the refresh path leaves no litter inside the worktree; the atomic write under a raised `os.replace(ENOSPC)` AND a real SIGKILL (rc −9) → bytes intact, no tmp; per-item isolation in both loops; dry-run write-free (whole-tree md5) with tallies equal; `Would patch` idempotent; the secret floor's right-cause line with the secret withheld; `/.fabrik/backups/` IGNORED in the block and 3 of 3 live projects; the main-checkout span byte-identical (md5 d17552a3…, 142 lines); the live dry-run read-only over 84 worktrees (the 2-line diff = a sibling's edits in web-ecommerce-factory); 50 + 90 green across six suites; ruff/format clean; mypy delta 0; headers OK; settings ok; five files. Provenance stated plainly: 9200310a carries no trailers (the quota-hold checkpoint); 48b463fe names it — the squash merge (D5) is the only commit that reaches master. 12 raised, 11 upheld:
- [M] `.fabrik/worktree-synced.lock` — the design's only authority — is NOT gitignored (`?? .fabrik/` in every worktree; 3 of 3 live projects + a live seo worktree NOT IGNORED; a plain `git clean -fd` deletes it → every synced path back to the permanent gap); the change's own manifest test asserts this property for `/.fabrik/backups/` and never for the ledger → FIXUP (1). [M] the prune loop reports, counts and pops the row BEFORE `unlink()` (:881-886 → :1156-1163): an EPERM leaves the file, prints "Deleted orphan", tallies 3 for 2, pops the row → `recorded_hash is None` forever → FIXUP (2). [M] the REAL run patches all 45 tracked `.gitignore`s silently (`Would patch` is dry-run-only; `Results:` omits it; `check_synced_unmodified` compares synced.lock only) → FIXUP (3). [M] the ledger-gap bootstrap exists only in a docstring — 101 of 101 live first-run warnings land in the state the operator doc does not tell them how to clear → FIXUP (4).
- [L] the doc's flow omits the reachable `except OSError` branch → FIXUP (5). [L] a legacy `""` sentinel falls through `is None` to the "edit preserved" wording (the prune side uses `not recorded_hash`) → FIXUP (6). [L] the manifest comment names the renamed `_backup_worktree_deletion` → FIXUP (7). [L] a corrupt ledger reads as `{}` with no line naming the file → FIXUP (8). [L] `ledger_owns_dir`'s `p == rel_dir` accepts a FILE row as proof of DIRECTORY ownership; the empty-dir reason never printed → FIXUP (9). [L] a row whose source is retired and whose file the agent deleted is never popped — the zombie the :1157 comment forbids → FIXUP (10). [L] two refresh backups in one second overwrite → FIXUP (11, the coder's call to uniquify or state). [L] "no T01b review artifact" — REFUTED: this artifact, on master since round 1, carries every round's fire-rate measurement (the coder's branch predates it).
Round 7 verdict: 12 raised → 11 routed (4 M, 7 L) + 1 refuted; every round-6 fix reproduced green — the findings are the mirrors those fixes did not carry across (1, 3, 7) and edges the widened ledger newly reached. Not the no-op round.

## Round 8 — over `5fd58526..0e96a2e0` (5 files; the round-7 fixup 0e96a2e0: the ledger gitignored via the manifest; the prune unlinks before it reports/pops; the real run names the gitignore patch and `Results:` carries `gitignore patched: N` (live: 45 of 45 would be patched); the bootstrap in the operator doc; the OSError branch in the flow; `if not recorded_hash` on the copy side; the renamed helper in the manifest docstring; a corrupt ledger WARNs once; `ledger_owns_dir` without the file-row-as-dir arm; a zombie-row reap pass; microsecond backup stamps; seven new graders each red on 48b463fe; 57 + 40 green)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 8.
### Adjudication (pool layer) — a three-deepseek draw
- deepseek v4-flash (1) and (2) — the Touches/DO-NOT letter (the manifest and the workflow doc) — UPHELD on the letter as in rounds 4 and 6: both are ORCHESTRATOR-DIRECTED (the manifest under round 5's item 4 and round 7's item 1, T01a merged before; the doc under the script's own `# AFTER-EDIT:` header and the Doc Sync Matrix floor); recorded in the merge body. One coverage nit: `test_hub_settings_json_worktree_block_is_present_and_well_formed` asserts `hooks`/`permissions` present and non-empty, not byte-identical to base (the round-7 native finder verified the settings diff purely additive by execution) → put to the native finder; a pin against the base blob is cheap if it upholds.
- deepseek v3.2-exp — CLEAN (every round-7 item located by line; the 13 corner cases; the `Results:` line surviving `tail -3`).
### Native finder (opus) — executed: the ledger ignored where the block IS current; the prune's unlink-first order under EPERM/ENOENT/EISDIR/parent-000; `gitignore patched` parity dry vs real; the bootstrap paragraph executed verbatim; the doc flow six-for-six against the code; the legacy sentinel; the corrupt ledger (one WARN; a directory; a non-str value); `ledger_owns_dir` (a file row no longer authorises deleting a directory; an empty sync-owned dir pruned with its real reason); the zombie reap four ways incl. the deliberate-delete mirror; microsecond stamps; the atomic write under ENOSPC and a real SIGKILL at the swap; a 32-cell decision matrix with 0 violations; the hash-proof prune; the merged ledger; dry-run write-free + parity; the secrets legs incl. a `!.env` negation; the main-checkout span byte-identical (md5 091d1af3…, 141 lines); 55 of 55 `.worktreeinclude` patterns inspected; 57 + 89 green; ruff/format; mypy delta 0; five files; the trailers (9200310a untrailered, stated); the live dry-run read-only over 84 worktrees (104 of 104 gaps; `Would patch` 45 of 45). 6 raised:
- [M] the manifest's new `# AFTER-EDIT:` header's prose tail is parsed into a phantom coupled path `.gitignore` that WARNs on every future edit → FIXUP (1). [M] a NON-git `/opt` dir (4 of 45 — the four with no `Would seed`) is reported as "the floor could not be written (permission)" on every REAL sync, silent under dry-run — the wrong-cause class again plus a dry/real parity break → FIXUP (2): guard on `_is_git_repo`. [M] the round-7 ledger-gitignore fix lands in the MAIN checkout's tracked `.gitignore`, which a linked worktree never evaluates — 0 of 84 live worktrees ignore the ledger (84 of 84 ignore an older block's entries, so not an artifact); the mechanism that reaches every worktree is the shared `info/exclude` floor the script already writes → FIXUP (3): add the ledger to the floor's `block_lines` with a versioned marker. [M] the microsecond stamp is the only survivor of a 14-mutant sweep → FIXUP (4).
- [L] the corrupt-ledger WARN is on a line the wrapper's `tail -3` discards and absent from `Results:` → FIXUP (5). [L] the manifest DO-NOT — adjudicated orchestrator-directed (rounds 4/6/8); (3) moves the worktree coverage into the script → FIXUP (6): state it.
Round 8 verdict: 6 raised → 6 routed (4 M, 2 L); every round-7 fix reproduced green; pool 1 CLEAN + the letter items adjudicated. Not the no-op round.

## Round 9 — over `5fd58526..93d00357` (5 files; the round-8 fixup 93d00357: the manifest header without its prose tail (verified against `_coupled_tokens`); the floor leg gated on `_is_git_repo`; the ledger seeded into the shared `info/exclude` floor with a per-pattern append replacing the marker early-return, mirrored in the dry-run helper; the stamp grader; the corrupt-ledger WARN in `Results:`; the manifest lines kept and stated; five graders red on 0e96a2e0; 62 + 40 green; live: `Would seed` 41 of 45, `Would patch` 45 of 45, 107 gaps / 0 edits — the +6 diffed line by line as fleet drift)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 9.
### Adjudication (pool layer) — a three-deepseek draw
- deepseek v3.2-exp — CLEAN (the six round-8 items located; 65 tests; the two gates).
- deepseek v4-flash (1) and (2) — the Touches/DO-NOT letter (the manifest and the workflow doc) — UPHELD on the letter, ORCHESTRATOR-DIRECTED as adjudicated in rounds 4, 6 and 8 (T01a merged before any manifest edit; the doc under the script's own `# AFTER-EDIT:` header and the Doc Sync Matrix floor); stated in the merge body.
Native finder (opus): returned — its six items are the round-9 fixup brief recorded in the Round 10 heading (the disclosure list, the parity-mirror grader, doc step 2, the weld-guard grader, the tempfile reap, the R1 probe).


## Round 10 — over `5fd58526..6057202b` (the round-9 fixup 93d00357..6057202b: scripts/sync_enforcement_to_projects.py +22/−1, tests/test_sync_worktree_adoption.py +113/−1, docs/workflows/SYNC_ENFORCEMENT_WORKFLOW.md +14/−9 — all six prescribed items: the `Would seed` disclosure rendered from `(*_WORKTREE_SECRET_PATTERNS, _WORKTREE_LEDGER_REL)` with the upgrade wording; the dry-run parity leg on the legacy-marker fixture (mutant `all(...)→True` caught); doc step 2 "Shared floor" naming the third pattern + the marker-present upgrade path; the `:413` newline-guard grader (mutant `prefix=""` reproduces the weld); `_write_worktree_ledger` reaps stale `.ledger-tmp-*` siblings before minting (SIGKILL-orphan class) + grader; the R1 `claude -p` probe executed — `.worktreeinclude` does NOT fire on `EnterWorktree` (a throwaway repo under scratch, `--max-turns 2`; the worktree carried only tracked files), so the spec's stated default holds and the contract line names `--worktree` as the only launch form; no code change)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 (the reap is a delete on a fleet-synced path — it gets a native probe) — round 10.
### Orchestrator execution (in the worktree)
- `pytest tests/test_sync_worktree_adoption.py -q` → 65 passed; `ruff check` clean; `ruff format --check` → 2 files already formatted; `--dry-run` from the worktree → `DRY-RUN: No files were modified` (tail); the reap at `scripts/sync_enforcement_to_projects.py:663` (`lock_path.parent.glob(".ledger-tmp-*")`) sits inside `_write_worktree_ledger`, the mkstemp at `:669` with the same prefix; the three graders present at `tests/test_sync_worktree_adoption.py:1044`, `:1650`, `:1713`.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0585)
- v4-flash (1) — CLEAN. v3.2-exp — raised the reap race (= native 1) and the `Would seed` substring assertion (UPHELD [L], routed as item 8). v4-flash (2) — raised the reap race (= native 1); `.fabrik/synced.lock` copied into worktrees but not in the info/exclude floor, so an old-branch worktree shows `?? .fabrik/synced.lock` and a `git clean -fd` drops it (PLAUSIBLE — routed as item 3 for validation + fix); the manifest DO-NOT letter (adjudicated orchestrator-directed rounds 4/6/8/9 — not re-litigated).
### Native layer (opus×1, by execution)
- **[M] 1 — the r9 reap deletes a CONCURRENT sync's LIVE tempfile:** the `:661` premise ("an orphan has already lost its race") is refuted by the repo — `scripts/kilo-benchmarks/daily_refresh.sh:448` "the script has no internal lock"; only the cron takes the flock; `governance_sync_postcommit.sh`, fabrik-lib's `distribute_subagents.sh` and `watch_enforcement_changes.sh` take none (grep -c flock = 0 in 3 of 3). Reproduced on a real tmp repo: B's reap injected between A's `mkstemp` (`:669`) and `os.replace` → FileNotFoundError swallowed by `except OSError: pass` → `A's row landed: False`, no WARN, no tally. Direction conservative (the loser's ledger reverts; next run WARNs "no ledger record" and leaves files alone) but it is a delete on a path shipping to ~46 repos. FIXUP: age-guard (skip < 3600s) + rewrite the claim as stale-by-age + a two-file grader.
- [L] 2 — the floor list is a literal `(*_WORKTREE_SECRET_PATTERNS, _WORKTREE_LEDGER_REL)` at `:405`, `:479`, `:1166`, and `_worktree_would_ignore_after_seed:517` writes 2 of the 3 (latent — `:553` passes .env/.mcp.json only). FIXUP: one `_WORKTREE_FLOOR_PATTERNS`. [L] 4 — doc `:210` "under the existing marker" vs the executed end-of-file addendum after the user's own rules. [L] 5 — doc `:207` "one line per project" with no suppression condition; live 41 of 45 (the four non-git dirs, `_is_git_repo` gate `:1153`, unmentioned). [L] 6 — the addendum header `:417` hardcodes "(round 8)". [L] 7 — the reap has no dry-run grader (verified clean by execution — the orphan survives `dry_run=True` — resting only on the `:1330` call-site guard).
- Verified clean by execution: all six r9 items reproduce; four graders mutation-killed (the two-pattern revert, `return True`, `prefix = ""`, the reap body → pass); reap containment (sibling reaped; one-level-up, nested, and a directory named `.ledger-tmp-isadir` survive, non-fatal; ledger still written); the glob cannot escape `<worktree>/.fabrik/`; `.fabrik/` is not a worktreeinclude pattern (55 patterns, the only `.fabrik/` path is the code-appended `synced.lock`); the suspected `_is_git_repo` vs `--git-common-dir` divergence REFUTED (`:190` is `--is-inside-work-tree`, walks up identically); 65 passed; the six neighbour suites 89 passed in 197s; ruff/format clean; live `--dry-run` 45 synced / 0 failed with the tally folded in (two runs 10 min apart showed 45 then 90 files copied — a sibling's real sync from master in between; environmental); gate 2 `settings ok`, BC clause 4 purely additive vs base (hooks/permissions/enableAllProjectMcpServers identical); trailers parse; the r9 fixup is 3 files, DO-NOT respected; marker-present upgrade idempotent, trailing newline normalised, no duplicate append; tally tests delta-based (no order coupling).
### Verdict
**8 findings (1 M + 7 L) → FIXUP round 10 routed to the coder.** Ledger: reap safety (concurrency/age · dry-run · containment) · floor single-source · doc precision (step 2 wording, suppression condition) · header stamp · dry-run disclosure assertion · the converged classes (hash-vs-ledger, prune authorisation, secrets classification, tally — all clean).

## Round 11 — over `5fd58526..3a242852` (the round-10 fixup 6057202b..3a242852, incl. the untrailered WIP checkpoint 9ddda5e0 made under quota hold #3 — accepted as-is, named in the trailered successor: the `.ledger-tmp-*` reap age-guarded at `scripts/sync_enforcement_to_projects.py:711` (skip < 3600s by mtime; comment now says stale-by-age; grader with a fresh vs a 2h-old tempfile, red with the guard removed); `_WORKTREE_FLOOR_PATTERNS` defined once at `:360` and used at all four sites (8 references); the pool's `.fabrik/synced.lock` item VALIDATED — `?? .fabrik/` reproduced on a gitignore-less branch — and fixed by adding it to the floor tuple (the extended lock test asserts a clean `git status`, red with the pattern dropped); doc wording (end-of-file addendum; per GIT project, 41 of 45); the addendum header stamp no longer round-numbered; a grader that the reap never fires under --dry-run (red with the `if not dry_run` guard removed); the dry-run leg asserts the ledger pattern + the upgrade phrase (red on the pre-round-9 wording); 66 passed; neighbours 40; ruff/format clean; live --dry-run 45/0 with `Would seed … (.env, .mcp.json, .fabrik/worktree-synced.lock, .fabrik/synced.lock)`)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 (a fleet-wide ignore rule and a delete guard — native probe) — round 11.
### Orchestrator execution (in the worktree)
- `pytest tests/test_sync_worktree_adoption.py -q` → 66 passed; `ruff check` clean; `_WORKTREE_FLOOR_PATTERNS = (*_WORKTREE_SECRET_PATTERNS, _WORKTREE_LEDGER_REL, ".fabrik/synced.lock")` at `:360`, 8 references; the age check at `:711`.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0328)
- v4-flash (1) and (2): the manifest / workflow-doc Touches letter — adjudicated orchestrator-directed in rounds 4/6/8/9, not re-litigated. v3.2-exp and v4-flash (2): `stale.stat()` between the glob and the unlink can raise `FileNotFoundError` under a concurrent reaper, swallowed by the outer `except OSError: pass` — the ledger write silently lost (PLAUSIBLE, M — the native finder's question (a); adjudicated on its return). v3.2-exp: the 3600s floor vs a multi-hour sync — REFUTED (the tempfile lives for one worktree's ledger write, milliseconds, not the sync's span); `.fabrik/synced.lock` ignored in worktrees vs `check_synced_unmodified.py` — the reader uses `read_text`, not git status: not a defect (its own verdict). v4-flash (2): module-global tally pollution across tests — the tally tests are delta-based (round 10 native), not new.
Native: PENDING — appended when it returns.
