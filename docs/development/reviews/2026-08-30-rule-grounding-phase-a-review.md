# Review — plan-2 Phase A (check_rule_grounding + tests + registration + lockstep comment)

Surface: working-tree Phase-A diff (4 files: scripts/enforcement/check_rule_grounding.py ·
tests/enforcement/test_rule_grounding.py · scripts/final_gate.py · scripts/review_rubric.py),
pre-commit; base 4d848206-era master.
Mode: NO-POOL (operator standing directive) — solo native; closing pass re-derivation.
Prior-report anchor: none for this surface (new files) — full WIDE pass 1.

## Rubric (review_rubric.py --changed <the 4 paths> — verbatim section heads)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: scripts/enforcement/check_rule_grounding.py, scripts/final_gate.py, …)
### core/45-testing-strategy.md  (hit: tests/enforcement/test_rule_grounding.py)
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | security-auth floor (35) | CLEAN | read-only stdlib CLI; subprocess is the repo's own script by --root-resolved path, bounded args, no shell=True |
| 2 | data-postgres floor (25) | CLEAN | grep of the 4 changed files for sql/engine/psycopg/asyncpg: 0 DB-API hits (the word "session" appears once, in the check's docstring prose, not as a DB session) — check_rule_grounding.py reads only .md plan files |
| 3 | ops floor (30) | CLEAN | grep of the 4 changed files for compose/ports/docker/traefik: 0 hits — no service, no deploy artifact touched |
| 4 | 12-Factor | CLEAN | stdout-only, env-free, no daemon; timeout=120 bounds the one subprocess |
| 5 | python discipline (10) | CLEAN | sibling-mirrored frame (guards, _say, budget charged-up-front); AFTER-EDIT header present; module-level compiled regexes |
| 6 | testing (45) / behavior-without-a-test | CLEAN | 8 behaviors red-first (collection-error red watched, then 8/8 green); date-gate both sides (e), DRAFT skip (f), spine grading (g-set), CLI exit-0 (h) all pinned |
| 7 | fail-open vs fail-closed | CLEAN | warn_only always-0: SystemExit+Exception guards (tested via bad flag); a broken/missing rubric returns [] — completeness silently ungraded, never a red (correct advisory direction); date-gate fails toward NOT grading |
| 8 | boundary/sentinel/prefix | CLEAN | MATCHED_LINE_RE `^###\s+(\S+\.md)\s+\(hit:` matches the emission `### {rel}  (hit:` incl. its double space; digest header/separator rows skipped (rule-prefix + dash-set guards, exercised by the fixture header); `:NNN` suffix stripped before file read |
| 9 | enforcement-overclaim | CLEAN | docstring names the three blind spots (comprehension, implications, beyond-MATCHED packs) as the review's; census note says "reading quality is the review's" |
| 10 | lockstep integrity | CLEAN | review_rubric.py emission now carries the two-consumer comment; sensor at :287 re-verified parsing `"  (hit:"` — same shape both consumers |
| 11 | cost/limit edges | CLEAN | one bounded local subprocess per graded plan; no spend |
| 12 | live fire rate (measured before shipping) | CLEAN | live run: 2 in-window CONVERGED plans examined, both with findings — plan-1 NO-DIGEST (prose digest, the known dogfood target) + plan-2 PACK-NOT-IN-DIGEST ×4 (its digest skips its MATCHED packs) — both are the B4 dogfood, named in the plan; zero findings on the historical corpus (date gate holds) |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — all 12 classes, 4 files read line-by-line | citation | solo native (NO-POOL; dispatched: 0) | 0 | 0 | 0 | the live-run findings are the check WORKING on known dogfood targets, not defects in this surface |
| Pass 2 | CLOSING — re-derivation: suite re-run 8/8; live census re-run (2 examined / 2 with findings / labels re-listed); MATCHED regex re-tested against the emission string; guards re-exercised via the CLI matrix | method: re-derivation | 0 | 0 | 0 | TERMINAL |

Pass 2 = the no-op: `new: 0`, everything adjudicated. (Minimum-two-rounds satisfied; no fix pass
existed to confirm.)

## Phase A verdict

Phase A verdict: PASS — clean. The check's first live run correctly reds both in-window plans on
exactly the defects the plan's B4 dogfood step exists to fix — the floor grading its own author
first. Gate at commit time (verbatim summary of the `python scripts/final_gate.py --check --json`
run in this phase):

```json
{"status": "success", "tier": 2, "passed": 55, "failed": 0}
```
