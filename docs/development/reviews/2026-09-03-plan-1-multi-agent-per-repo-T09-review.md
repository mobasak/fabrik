# Acceptance review — T09 (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** IN-PROGRESS

**Surface:** the coder's worktree branch diff against the dispatch base 40673c55 (master after the T08b merge) — see the round sections below (one file per ticket, rounds APPENDED).

## Round 1 — over `40673c55..4fb6ae23` (22 rows: the 17 wrapper dirs under docs/orchestrator/_traycer-skills/ deleted (53 lines each); scripts/traycer_mirror.py deleted (93; a no-op without TRAYCER_EPIC_ID; census `git grep -l traycer_mirror -- . ':!docs/archive' ':!docs/orchestrator/_retired'` 20 files / 55 lines → 18 / 41 — the coder's "19" corrected by the native; the remainder history, plan artifacts, an absence-guard test, and the merge owner's link repairs); three docs renamed 2/0 into docs/orchestrator/_retired/traycer/*.RETIRED.md with tombstone headers; scripts/enforcement/check_traycer_chain.py (FLEET-SYNCED) 10/10 — `DIRS` two glob patterns expanded by `glob.glob`, cwd-relative, the docs/traycer + docs/orchestrator roots dropped, the [A]/[B]/[C] detectors unchanged; gates: PASS - 3 files exit 0; PASS - 0 files exit 0 in a bare project dir and in /opt/transdoc; fab-* skills 0 of 58; ls-files 0; watched-fail: the old DIRS scanned 2 files (the two mega reference docs), the new 3; kilo_selected_agents.md dependents 14 repo-wide − 3 plan-set = 11 (rules packs 0))
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 (a fleet-synced check + deletions) — round 1.
### Orchestrator execution (in the worktree)
- `check_traycer_chain.py` → PASS - 3 files, all 3 classes clean; the skills/tree gate ok; `git ls-files` on the deleted paths → 0; `ruff check` clean, `ruff format --check` 1 file already formatted; 22 numstat rows; the wiring tombstone's header follows the shape.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0227)
- Two CLEAN. One raised the referrer repairs and the tombstones' own relative links as unimplemented by the diff — the referrers are the merge owner's by the ticket's Docs line (adjudicated as for T10–T12b); the tombstones' own links are UPHELD as T09's (native 2).
### Native layer (opus×1, by execution — probes on copies)
- **[M] 1 — the check script silently dropped out of the capability catalog:** the header + the 5-line DIRS comment grew the file 4022 → 4219 chars, pushing `if __name__` past the 4000-char head window `scripts/generate_capability_index.py:76` reads, so `_classify_script` returns "manual" and the emitter drops the row (catalog 365 ok → 366 ok after shrinking, proven on a copy). FIXUP: `# AFTER-EDIT: none` + a ≤3-line comment (also closes 5). The window itself is a generator defect — routed hub-side.
- **[M] 2 — the three tombstones' OWN outbound relative links not re-based on the two-level-deeper move** (precedent `2026-07-20-plan-1-docs-truth-convergence.md:134`); 10 exact corrections listed (the `/opt/fabrik`-absolute mentions are not flagged — the coder's `:142,181,237,241` over-enumerated by four). FIXUP.
- [L] 3 — the census denominator 18 files, not 19. [L] 5 — the AFTER-EDIT header names the check's INPUTS, so every future edit WARNs (fold into 1). [L] 6 — `scripts/docs_updater.py:751-752` keeps INDEX description rows keyed on the old basenames (no ticket owns them) — orchestrator delta at merge. [L] 7 — NOTHING runs `check_traycer_chain.py` (0 of ~40 `run_optional_check` registrations; not in pre-commit; cwd-relative and fail-open from a wrong cwd) — routed to T16 as a register-or-record decision.
- Verified clean by execution: the four fleet shapes exit 0 and never raise (no commands/; commands/_sources/ without the three; a dir with a space; /opt/transdoc); the three detectors each fire on a planted violation (`FAIL - 3 violation(s)`); the watched-fail 2 → 3 confirmed; the glob contract judged RIGHT (the widening is required for fabrik-epics-review.md; a future `fabrik-epics-<x>.md` inheriting the detectors is the fail-safe direction; absence stays a no-op); the deletions complete with no live consumer (`tests/test_epic_order.py:861` is a forbidden-token assertion); the headers match the precedent; the dependent count re-derived 14 − 3 = 11, rules packs 0; 22 rows exactly; trailers parse; ruff/format/bandit clean; Touches exact; the stale corpus references are T14b's by its gate pathspec; the 16 T09-caused doc-link errors attributed exactly (the other 8 are worktree-only gitignored artifacts; the main checkout is 0 broken of 2323). Merge-owner queue: `check_doc_index.py`'s 5 errors (INDEX rows :884-885, :504-505, :406, :343 + three new rows), `docs/traycer/README.md:1110`, `docs/traycer/fabrik-workflow.md:1598`, the 03 mega tombstone `:148` and `:320`, the CAPABILITIES regeneration (carries unrelated sibling drift), CHANGELOG.
### Verdict
**2 findings (M) + 2 (L) → FIXUP round 1 routed to the coder**; three more routed (the generator window, the docs_updater rows, the unregistered check). Ledger: fleet shapes · detectors · glob contract · deletions/consumers · tombstone links (NEW) · catalog classification (NEW) · referrers.

## Round 2 — over `40673c55..e4642031` (the round-1 fixup 4fb6ae23..e4642031: `scripts/enforcement/check_traycer_chain.py` 4/6 — `# AFTER-EDIT: none`, the DIRS comment 5 → 3 lines, `if __name__` at offset 3901 (was 4171) so `generate_capability_index.py` classifies it `ok` again — proven on a rsync'd copy: 365 → 366 ok, the row back at `docs/CAPABILITIES.md:146`; the three tombstones' outbound links re-based verbatim (agile 4/4, refactoring 4/4, wiring 2/2 — headers untouched); `check_doc_links` 0 errors inside `docs/orchestrator/_retired/traycer/`; the census restated with its command: 18 files / 41 lines; gates re-run. Observation routed hub-side: `_first_docline` takes the AFTER-EDIT comment as a script's summary — pre-existing)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own execution (mechanical, prescribed verbatim; stated) — round 2.
### Orchestrator execution (in the worktree)
- `check_traycer_chain.py` → PASS - 3 files; `if __name__` offset 3901; line 2 `# AFTER-EDIT: none`; `ruff check` clean; `check_doc_links` errors inside `_retired/traycer/` → 0; the three re-based targets exist (`docs/archive/traycer-yolo-workflow.md`, `agents-fabrik.md`, `AGENTS.md`).
Pool: PENDING — appended when it returns.
