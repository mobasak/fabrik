# The WSL box leaves 172.16.0.0/12 — the docs and ledgers that record it (D-360)

**Status:** CONVERGED
**Surface:** `git rev-parse HEAD` = 3d1387845e68e9116fd95a1b612054b0d5fd5b5f; `git diff HEAD -- CHANGELOG.md docs/DECISIONS.md docs/STRATEGIC_BACKLOG.md docs/workstation/volkan-mac.md docs/reference/external-services-registry.md | md5sum` = 833e401964f00f1cf5110bd32c568a19
**Command:** /fabrik-review, ROUTED-UP from /fabrik-review-scoped step 0 (operator-named work) · **Changed:** `CHANGELOG.md`, `docs/DECISIONS.md` (D-360), `docs/STRATEGIC_BACKLOG.md`, `docs/workstation/volkan-mac.md`, `docs/reference/external-services-registry.md`
**Anchor:** no prior receipt covers this surface — a keyword search for the scope matched only unrelated uses of "mirrored", so the anchor cannot match and pass 1 is a full WIDE pass.

## Coverage Checklist

Rubric invocation (verbatim output — the gate reads the generated header, never a prose mention):

```text
$ python scripts/review_rubric.py --changed CHANGELOG.md docs/DECISIONS.md docs/STRATEGIC_BACKLOG.md docs/workstation/volkan-mac.md docs/reference/external-services-registry.md
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3; TOOLING surface)

### core/40-documentation.md
- > **⚠️ `docs/OPERATIONS.md` + `docs/DEPLOYMENT.md` are FLEET-AI INTERFACES, not just docs (D-065).**
- **Tier-1 (author → verify → converge; the author leg is NATIVE while the pool is OFF, D-181 — `scripts/doc_reconcile.py`'s pool author cannot dispatch):** for each **mechanically-detectable** doc whose Doc-Sync trigger fired (`docs/QUICKSTART.md` · `docs/CONFIGURATION.md` · `docs/data-contract.md` · `docs/SERVICES.md` · `docs/OPERATIONS.md` — the reliable-signal subset), `scripts/doc_reconcile.py` dispatches a cheap OpenRouter-pool author (`libs.subagents`, `pick_models("docs")`) to emit a **minimal structured patch**, **verifies it before applying** (a symbol cross-check catches invented endpoints; the orchestrator injects a higher-assurance native-Claude verify), and loops to a zero-edit round. Runs per phase in `/fabrik-execute-plan`; never blocks (fail-safe). The other docs (CHANGELOG, INDEX, FEATURES, RESILIENCE, PORTS, the READMEs, `db/schema.sql`, …) have no reliable mechanical content-signal → they rely on the touch-on-change backstop below + your own edit (force-update, not force-correct).
- The SSOT is the type-aware registry (`scripts/enforcement/_doc_registry.py::PROJECT_DOCS`) — this table is its project-facing rendering, kept in step, never a second truth. `/fabrik-plan-after-chat` (the plan set's spine + tickets — the ticket-format authority) injects these rows per ticket as its `Docs:` line.
- Standalone work (not plan execution) → `Agent-Role: primary`. Trailers go below a blank line, above `Co-Authored-By`. ⚠️ The trailer block must be its OWN paragraph with NO blank line inside it: git parses only the LAST paragraph, and only if it is all-trailers. A blank line before `Co-Authored-By:` demotes everything above it to prose; so does a prose line glued to the top of the block. Measured 2026-08-15: 200 of the last 200 hub commits carried `Agent-Role:` and only 10 parsed, because the old example here shipped the blank line.
- **⚠️ Link it or it is decoration.** *Measured:* requests for files that do NOT exist came ~zero from AI bots — agents never go looking. It follows (inference, not measurement) that a file only gets read when something points at it: reference it from the docs index or README.
- ⚠️ **In THIS repo `llms.txt` is GENERATED** (`scripts/generate_capability_index.py`, refreshed daily) — never hand-edit it; change the generator. A project writing one by hand owns it.
- either way. Cheap and reversible — never at the expense of `OPERATIONS.md`/`DEPLOYMENT.md`, which are the load-bearing agent interfaces (D-065).
- **No skipped heading levels** — `##` to `###`, never `##` to `####`
- **Fenced code blocks only** — never indented code (AI treats it inconsistently)

### 12-FACTOR (all twelve axes)
- I codebase: shared code → fabrik-lib, never two apps in one repo
- II deps: every shelled-out binary installed + pinned in the Dockerfile
- III config: granular env vars; no secrets in code; no grouped env sets
- IV backing services: swappable by DSN/config change only
- V build/release/run: releases immutable; never hot-patch a container
- VI processes: stateless; session state → redis-main; no sticky sessions
- VII port binding: bind in-container; Traefik routes; no host ports:
- VIII concurrency: scale out; never daemonize or write PID files
- IX disposability: SIGTERM returns in-flight jobs to the queue; jobs idempotent
- X dev/prod parity: same backing services everywhere; no SQLite-for-Postgres
- XI logs: unbuffered stdout only; the app never writes/rotates a logfile
- XII admin: migrations/one-offs run against the deployed release, never startup

## MATCHED — none (no pack glob hits the changed paths; the FLOOR still arms you)

# promote-to-check_*: 7 injected mandate(s) look deterministically greppable — their backtick literals, one line each (the full mandates are ABOVE, not repeated: re-emitting ~20 FLOOR lines verbatim doubled the rubric and got it skimmed — web-ecommerce-factory 01M1QEY5, 2026-09-05)
- `docs/OPERATIONS.md` `docs/DEPLOYMENT.md`
- `scripts/doc_reconcile.py` `docs/QUICKSTART.md` `docs/CONFIGURATION.md` `docs/data-contract.md` `docs/SERVICES.md` `docs/OPERATIONS.md`
- `scripts/enforcement/_doc_registry.py::PROJECT_DOCS` `/fabrik-plan-after-chat` `Docs:`
- `Agent-Role: primary` `Co-Authored-By` `Co-Authored-By:` `Agent-Role:`
- `llms.txt` `scripts/generate_capability_index.py`
- `OPERATIONS.md` `DEPLOYMENT.md`
- `##` `###` `##` `####`
```

| Class | Status |
|---|---|
| Hunt: `docs/DECISIONS.md` — the D-360 row, its 6 cells and every claim in them | FIXED r1 (A-S1: the row said the box "leaves `172.16.0.0/12` entirely" and that WSL "records rather than configures" the Lxss key; under mirrored mode WSL no longer writes it, so it still reads the stale `172.22.16.0/20` — reworded to "moves every live address off" with the stale key named; the row still parses through `scripts/decisions.py` as 6 cells, 1 of 1 D-360) |
| Hunt: `CHANGELOG.md` — the `### Changed` entry atop `[Unreleased]` | FIXED r1 (A-P1, a mirror site of A-S1 found by the round-zero class sweep and `check_review_hygiene.py --phrase`: the title said "leaves `172.16.0.0/12`" — same rewording; heading shape still `### Changed — Title (2026-09-23)` under `## [Unreleased]`) |
| Hunt: `docs/STRATEGIC_BACKLOG.md` — the resolution marker on the `:12` item | FIXED r1 (A-P2, found by the round-zero probe: the note ended "follows —" into the original entry's own " — " separator, printing a doubled dash) |
| Hunt: `docs/workstation/volkan-mac.md` — the replaced tether instruction | FIXED r1 (B-S1: the text said the portproxy and its firewall rule "now do nothing"; executed, Windows still listens on `0.0.0.0:2222` and the allow rule *WSL to Mac SSH (2222)* covers every profile, Public included — reworded to say they are live and forward only while the hotspot carries `192.168.137.68`) |
| Hunt: `docs/reference/external-services-registry.md` — the `:17` dashboard rationale | CLEAN (A-H1, B-H1 and B-S2 refuted: from Windows, `127.0.0.1:18779` returned HTTP 200 from a WSL listener bound `0.0.0.0` under mirrored mode, executed by the refuter and again by the orchestrator) |
| Recurrence: fail-open/fail-closed — a swallowed error or an absent check that reads as success | CLEAN (no gate or guard on this docs surface; the one security claim, the Hyper-V firewall, executed: `Enabled True`, `DefaultInboundAction Block`, network category Public) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted, a limit at its edges | CLEAN (pool OFF; 6 seats stamped with `command_run.py dispatch`, 6 returned, 0 over the 15-minute box) |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | CLEAN (every CIDR checked against `docker network inspect` and `ip -brief addr`: `docker0` `10.211.0.1/24`, 7 of 7 networks inside `10.212.0.0/16`, none inside `172.16.0.0/12`) |
| Recurrence: behavior-without-a-test — a contract row no test kills (mutation asserted) | CLEAN (docs and ledgers only — the surface adds no behaviour for a test to kill; every claim is instead executed against the live box) |
| Recurrence: denominator on every count — bounded searches state their bound | CLEAN (12 of 12 listening ports from `ss -ltnp`; 49 of 49 repo mailboxes in the send log, the 50th `/opt/fabrik-mail` entry `inbox` being no repo; 7 of 7 networks) |
| Recurrence: proxy-as-evidence — the real check EXECUTED, not read | CLEAN (every candidate executed by the slice refuter and re-executed by the orchestrator; one refuter verdict, B-S2, overruled on that execution) |

## Pass Ledger

| Pass | Finders | Counters | Method |
|---|---|---|---|
| Pass 1 | native sonnet×2 + haiku×2 (two slices, ONE `Workflow` call — 6 agents incl. 2 refuters), dispatched: 6, returned: 6 | found: 8, new: 8, confirmed: 4, fixed: 4, unexecuted: 0 | method: citation; round-zero probe — class sweep of totality and inertness wording plus `check_review_hygiene.py --phrase` · mirrors: 2 read; seats: A-sonnet 1/1 · A-haiku 0/2 · B-sonnet 1/2 · B-haiku 0/1; stop: confirmed 4; fix: +7 -5 lines for 4 confirmed |
| Pass 2 | native sonnet×2 + haiku×2 (the round-1 seats over their own slices, ONE `Workflow` call — 4 agents, no refuter owed at 0 candidates), dispatched: 4, returned: 4 | found: 0, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | method: re-derivation — every address, CIDR, count and path in the changed lines re-derived from the live box (`wslinfo`, `ip -brief addr`, `docker network inspect` over 10 of 10 networks, the Lxss registry key, `Get-NetTCPConnection`, `Get-NetFirewallRule`, `scripts/decisions.py D-360`, the 49-line send log); ledger: A-S1, A-P1, A-P2 NOW_FALSE from both slice-A seats, B-S1 NOW_FALSE from both slice-B seats; seats: A-sonnet 0/0 · A-haiku 0/0 · B-sonnet 0/0 · B-haiku 0/0; stop: closable |

## Confirmed findings

| Id | Where | Disposition |
|---|---|---|
| A-S1 | `docs/DECISIONS.md:17` | FIXED r1 — "leaves `172.16.0.0/12` entirely" and "records rather than configures" overclaimed under mirrored mode |
| A-P1 | `CHANGELOG.md:7` | FIXED r1 — the title repeated A-S1's "leaves `172.16.0.0/12`" |
| A-P2 | `docs/STRATEGIC_BACKLOG.md:12` | FIXED r1 — a doubled dash where the resolution note met the original entry |
| B-S1 | `docs/workstation/volkan-mac.md:71` | FIXED r1 — the live portproxy and its every-profile allow rule were described as doing nothing |

## Residual

| Candidate | Verdict |
|---|---|
| A-H1 | REFUTED (from Windows, `127.0.0.1:18779` returned HTTP 200 from a WSL listener bound `0.0.0.0` under mirrored mode) |
| B-H1 | REFUTED (the same test as A-H1; the refuter also reached a WSL listener bound only to `127.0.0.1` from Windows) |
| A-H2 | REFUTED (`ss -ltnp` lists 12 distinct wildcard TCP ports — 631 3001 5173 8016 8029 8031 8032 11235 18000 18013 38141 54322; the orchestrator's 13th was its own temporary test listener 18779) |
| B-S2 | REFUTED (the sentence is conditional on the server running; the dashboard has 0 listeners on 8770 now, and an identical `0.0.0.0` bind on another port was reached from Windows `127.0.0.1` with HTTP 200 — the refuter's confirm is overruled on that execution) |

## Per-phase verdicts

### Phase 0 — Establish scope: CLEAN — record opened on the routed-up surface; the five files pinned with a HEAD base copy and the diff digest; the hygiene check's 27 hits all sit on pre-existing lines, 0 on a changed line

### Phase 1 — Finders: CLEAN — two slices, two finders each, one `Workflow` call per pass; every seat returned and none left a coverage gap

### Phase 2 — Execute: FIXED(4) — 8 candidates, each re-executed by the orchestrator against the live box; 4 confirmed, 4 refuted, one refuter verdict (B-S2) overruled on that execution

### Phase 3 — Prove and fix: FIXED — the 4 doc defects reworded to state only what was executed; the surface adds no behaviour, so no grader is owed

### Phase 4 — Converge: CLEAN — pass 2, the round-1 seats over their own slices, confirmed 0 · fixed 0 with every ledger claim NOW_FALSE

## Gate

`final_gate.py --json --check`, pasted verbatim at the flip (check_convergence reads the fenced
`"status": "success"`):

```json
{"status": "success", "tier": 2, "passed": 66, "failed": 0, "skipped_checks": ["ruff-format (--check)", "ruff", "bandit", "semgrep", "pytest"]}
```

The skipped checks are the static and test tiers, which a `.md`-only diff does not run — this green asserts nothing about code, and the surface carries none. Every claim in the changed lines was instead executed against the live box, twice: by the slice refuters and by the orchestrator.
