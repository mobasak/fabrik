# Review — rotation-refresh-chain

**Status:** IN-PROGRESS
**Surface:** `git rev-parse HEAD` = fce9696457bebacd719f41ae367efdf84369073c; range tip fce9696457bebacd719f41ae367efdf84369073c; `git diff 7cb15def~1..fce96964 -- scripts/sysadmin/claude_rotate.py scripts/aro-wake/claude_rotate.py tests/test_claude_fleet.py tests/test_claude_rotate_v2.py docs/workstation/claude-account-rotation.md docs/LESSONS_LEARNT.md CHANGELOG.md` md5 67e3917dca58451b41de27e417f55794 (50365 bytes)
**Command:** /fabrik-review · **Changed:** `scripts/sysadmin/claude_rotate.py`, `scripts/aro-wake/claude_rotate.py`, `tests/test_claude_fleet.py`, `tests/test_claude_rotate_v2.py`, `docs/workstation/claude-account-rotation.md`, `docs/LESSONS_LEARNT.md`, `CHANGELOG.md`

**Anchor:** the newest prior receipt for this scope is `docs/development/reviews/2026-09-03-rotation-projected-trip-review.md` (6 of 297 receipts match `rotat|keepalive|refresh`); its `Surface:` hash is a different HEAD + diff md5, so it CANNOT match — full WIDE pass 1, no inherited rows. Routed UP from `/fabrik-review-scoped` step 1 (an auth surface — the OAuth refresh-chain tool — and operator-named work); the run record's surface names the trigger.
**Pin:** `<scratchpad>/rotation-review/surface.r1.diff` md5 `67e3917dca58451b41de27e417f55794` (50365 bytes) + the post-change files under `<scratchpad>/rotation-review/pin/` (each == `git show fce96964:<path>`); pre-change = `git show 7cb15def~1:<path>`. Attribution: the tool twins, the doc and the LESSONS entry were swept from this session's working tree into the sibling mail-triage commit 7cb15def; the remainder is fce96964 — the review surface is the union.

## Coverage Checklist

Rubric invocation (verbatim output — the gate reads the generated header, never a prose mention):

```text
$ python scripts/review_rubric.py --changed scripts/sysadmin/claude_rotate.py scripts/aro-wake/claude_rotate.py tests/test_claude_fleet.py tests/test_claude_rotate_v2.py docs/workstation/claude-account-rotation.md docs/LESSONS_LEARNT.md CHANGELOG.md
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3; TOOLING surface)

### core/10-python.md
**`uv`** is the mandated Python package manager. Never use raw `pip`, `pip install`, `poetry`, or `pipenv`.
- Dependencies live in `pyproject.toml` + `uv.lock`. Do not modify these files unless the ticket authorises it.
- its own reviewed commit, never as a side effect of unrelated work.
- The one RULE: use SQLAlchemy async consistently — never mix `async def` with sync `.query().all()` (the Banned table row; the full session pattern is `25-data-postgres.md`'s).
- The canonical `engine`, `async_session`, and `get_db` are defined in `src/database.py` — owned by `25-data-postgres.md`. Import from there, never redefine:
**Config convention:** apps read a complete `DATABASE_URL` (`postgresql+asyncpg://user:pass@host:port/db`) and `REDIS_URL` from env. Discrete `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD` for the app to assemble are **banned**. The env supplies the complete URL — `localhost` in WSL, `postgres-main` on VPS — so the host concern is an env-layer responsibility, never code logic. See `30-ops.md` compose template for how discrete vars are interpolated into `DATABASE_URL` at the compose level.
- volume** (`30-ops.md` § Volumes), never in `.tmp` and never in `/tmp`.
**GlitchTip discipline:** unhandled exceptions (FastAPI 500s) are auto-captured by GlitchTip with full stacktraces. In the `except Exception` branch, log a **short event name + correlation_id** — never `logger.exception()` (that duplicates the traceback in Loki AND GlitchTip). See `55-observability.md` § Error Reporting for the full rule.
**Note:** Use the scaffolded logger: `from {package}.logger import get_logger` (see `55-observability.md` § Pre-Scaffolded Logging). Do not use `structlog.get_logger()` directly or `logging.getLogger(__name__)`.
- **Never a bare `asyncio.create_task()`** — an unreferenced task is silently garbage-collected and its exceptions vanish. Hold the reference and await it, or use `asyncio.TaskGroup`.
- **`datetime.now(UTC)`, never `datetime.utcnow()`** — deprecated and naive; naive datetimes are a real cross-service defect class.
- Ruff's selected rule-sets MUST include `ASYNC` (blocking IO in async code — machine-enforces this pack's hardest-to-review rule), `B` (bugbear) and `S` (bandit) alongside the defaults; configured in `pyproject.toml`, emitted by the scaffolder.
- Production services run via `uvicorn` CLI in the Dockerfile, not `uvicorn.run()` in code. Base image is always the pinned Debian `-slim` variant on `linux/amd64` (the variant is pinned fleet-wide in `30-ops.md` § Container Base Images — change it THERE, never per-repo). Never use Alpine — musllinux wheels exist now (PEP 656) but coverage is still partial, source builds are dramatically slower, and musl's allocator/stack defaults degrade CPython; the trade never pays on this fleet.
- `uvicorn.run()` is for local development only. Never ship it in production code.
- a fleet scaling decision (more containers), never a per-app flag.
**BANNED: grouped/named env config sets.** 12F is explicit — *"env vars are granular controls, each fully orthogonal to other env vars"* — so a `config/production.yml`, a `settings.production` group, or a `config/{dev,staging,prod}.yaml` tree is a violation. Env vars are granular and set **per deploy**, never batched into a named "environment".
**BANNED:** `logging.FileHandler`, `logging.handlers.RotatingFileHandler`, `TimedRotatingFileHandler`, `loguru` file sinks, any `*.log` file write, any in-app log rotation/retention/cleanup. The app never decides where logs are stored or routed — Docker → Promtail → Loki does. Full rule: `55-observability.md` § Logs.
**Factor XII — Admin processes. NEVER migrate from app startup.**
**BANNED: `alembic upgrade head` in FastAPI's `lifespan`, in an `@app.on_event("startup")`, or as an import side-effect.** With more than one replica (or a restart storm) two containers run `upgrade head` **concurrently** → they race the Alembic version table → duplicate DDL → **wedged deploy**. Migrations are a **one-off admin process against the deployed release**: `docker compose run --rm <svc> alembic upgrade head` (see `30-ops.md` § Release & Admin Processes).

### 12-FACTOR (all twelve axes)
- I codebase: shared code → fabrik-lib, never two apps in one repo
- II deps: every shelled-out binary installed + pinned in the Dockerfile
- III config: granular env vars; no secrets in code; no grouped env sets
- IV backing services: swappable by DSN/config change only
- V build/release/run: releases immutable; never hot-patch a container
- VI processes: stateless; session state → redis-main; no sticky sessions
- VII port binding: bind in-container; Traefik routes; no host ports:
- VIII concurrency: scale out; never daemonize or write PID files
- IX disposability: SIGTERM returns in-flight jobs to the queue; jobs idempotent
- X dev/prod parity: same backing services everywhere; no SQLite-for-Postgres
- XI logs: unbuffered stdout only; the app never writes/rotates a logfile
- XII admin: migrations/one-offs run against the deployed release, never startup

## MATCHED — packs whose globs hit the changed paths

### core/40-documentation.md  (hit: CHANGELOG.md, docs/LESSONS_LEARNT.md, docs/workstation/claude-account-rotation.md)
- > **⚠️ `docs/OPERATIONS.md` + `docs/DEPLOYMENT.md` are FLEET-AI INTERFACES, not just docs (D-065).**
- **Tier-1 (author → verify → converge; the author leg is NATIVE while the pool is OFF, D-181 — `scripts/doc_reconcile.py`'s pool author cannot dispatch):** for each **mechanically-detectable** doc whose Doc-Sync trigger fired (`docs/QUICKSTART.md` · `docs/CONFIGURATION.md` · `docs/data-contract.md` · `docs/SERVICES.md` · `docs/OPERATIONS.md` — the reliable-signal subset), `scripts/doc_reconcile.py` dispatches a cheap OpenRouter-pool author (`libs.subagents`, `pick_models("docs")`) to emit a **minimal structured patch**, **verifies it before applying** (a symbol cross-check catches invented endpoints; the orchestrator injects a higher-assurance native-Claude verify), and loops to a zero-edit round. Runs per phase in `/fabrik-execute-plan`; never blocks (fail-safe). The other docs (CHANGELOG, INDEX, FEATURES, RESILIENCE, PORTS, the READMEs, `db/schema.sql`, …) have no reliable mechanical content-signal → they rely on the touch-on-change backstop below + your own edit (force-update, not force-correct).
- The SSOT is the type-aware registry (`scripts/enforcement/_doc_registry.py::PROJECT_DOCS`) — this table is its project-facing rendering, kept in step, never a second truth. `/fabrik-plan-after-chat` (the plan set's spine + tickets — the ticket-format authority) injects these rows per ticket as its `Docs:` line.
- Standalone work (not plan execution) → `Agent-Role: primary`. Trailers go below a blank line, above `Co-Authored-By`. ⚠️ The trailer block must be its OWN paragraph with NO blank line inside it: git parses only the LAST paragraph, and only if it is all-trailers. A blank line before `Co-Authored-By:` demotes everything above it to prose; so does a prose line glued to the top of the block. Measured 2026-08-15: 200 of the last 200 hub commits carried `Agent-Role:` and only 10 parsed, because the old example here shipped the blank line.
- **⚠️ Link it or it is decoration.** *Measured:* requests for files that do NOT exist came ~zero from AI bots — agents never go looking. It follows (inference, not measurement) that a file only gets read when something points at it: reference it from the docs index or README.
- ⚠️ **In THIS repo `llms.txt` is GENERATED** (`scripts/generate_capability_index.py`, refreshed daily) — never hand-edit it; change the generator. A project writing one by hand owns it.
- either way. Cheap and reversible — never at the expense of `OPERATIONS.md`/`DEPLOYMENT.md`, which are the load-bearing agent interfaces (D-065).
- **No skipped heading levels** — `##` to `###`, never `##` to `####`
- **Fenced code blocks only** — never indented code (AI treats it inconsistently)

### core/45-testing-strategy.md  (hit: tests/test_claude_fleet.py, tests/test_claude_rotate_v2.py)
- **Behavior Contract**: every ticket enumerates its distinct **user-observable behaviors / acceptance criteria** and tests **each one** — one high-value integration/E2E test per behavior, risk-ordered, TDD for the risky ones. Skip trivia (getters / framework glue / config): **lean-but-complete, NOT 100%-line-coverage dogma**. Do not chase line coverage — ensure every behavior has a test that would fail if that behavior regressed. (Cheap pool subagents can author the per-behavior tests — the suggest→curate→author→fix workflow in `62-using-subagents.md` § Dispatch policy + `~/.claude/commands/fabrik-review.md`.)
- **No cosmetic assertions**: never assert against CSS classes, Tailwind utility strings, pixel measurements, or snapshot hashes. Assert application state and user-visible outcomes only.
- **Watched-fail-first** (for tests this change adds or modifies; trivia stays skipped per the Behavior Contract): a non-trivial behavior's test proves something only if it has been SEEN RED — either write it first and watch it fail, or (after the fact) neuter the fix/feature, prove the test goes red, then RESTORE and re-run to green. The neutered state is never staged, committed, or left in the tree. A green test never seen red is unverified — a suite can pass with its guard deleted.
- **Run tests**: `uv run pytest tests/` (never bare `pytest` — Fabrik uses `uv`) — **when the project has a `pyproject.toml`/`uv.lock`**. A `requirements.txt`-only project (no manifest) runs `.venv/bin/python -m pytest tests/` — the manifest clause chooses the RUNNER, it never disarms the mandate to run the suite (web-ecommerce-factory 01M1QEY5, 2026-09-05: the clause read as "does not apply here"). ⚠️ **Gate this on the manifest, because this line is FLOOR-injected into finder prompts and a vendored fabrik-lib MODULE has neither by design**: the module recipe ships `requirements.txt` (`fabrik-lib/README.md` § Creating a Reference Implementation), so `uv run` cannot resolve it and `python3 -m pytest` is the only thing that works. Telling a finder the sole working … (wrapped further — read the pack)
- **Zero-mock database policy**: never mock SQLAlchemy, SQLModel, or database sessions. All backend tests execute against a real PostgreSQL instance.
- **`ASGITransport` never runs lifespan** — anything the app initializes at startup (scaffolded apps are lifespan-based) silently does not exist in tests; wrap with `asgi-lifespan`'s `LifespanManager` when a test needs startup state.
- Use `structlog` in test helpers if logging is needed — never `print()`. See `55-observability.md`.
- **Never stub a server action from Playwright** — the server is the E2E boundary; stubbing belongs in the unit lane where the action is a plain function.
- Run Playwright against the PRODUCTION build (`next build && next start`), never the dev server.
- All locators must be **semantic**: `page.getByRole('button', { name: /submit/i })`. Never use CSS selectors or XPath.
- Launch Playwright's **bundled Chromium** (`channel: 'chromium'`) — stable Chrome/Edge removed the `--load-extension` / `--disable-extensions-except` side-load flags (Chrome 137/139), so those args only work under bundled Chromium, never installed stable Chrome.
- Run `@axe-core/playwright` with **`bypassCSP: true`** (the non-relaxable extension CSP otherwise makes axe throw on `chrome-extension://` pages); keep `@axe-core/playwright` a **dev-dependency only** (MPL-2.0 — never bundled into the shipped artifact). Gate bundle size with `size-limit` **per surface** (popup / side-panel / content-script). Full loop: `chrome-ext/70-chrome-ext.md` § Testing & UI Verification.
- Keep the generated types committed and re-generate on schema changes (`uv run python -c "import json; from <package>.main import app; print(json.dumps(app.openapi()))" > openapi.json` — the scaffold emits `src/<package>/main.py`, never a flat `src/main.py`, so `src.main` imports nothing).
**BANNED in tests:**
| A test THIS change adds/modifies that was never seen red (no fail-first, no red-on-revert proof) | Watch it fail first, or neuter the change → prove red → restore → re-run green |
- [ ] Destructive DB tests call `require_throwaway(TEST_DATABASE_URL)` before connecting — never point them at a dev/shared DB.

# promote-to-check_*: 33 injected mandate(s) look deterministically greppable — their backtick literals, one line each (the full mandates are ABOVE, not repeated: re-emitting ~20 FLOOR lines verbatim doubled the rubric and got it skimmed — web-ecommerce-factory 01M1QEY5, 2026-09-05)
- `uv` `pip` `pip install` `poetry` `pipenv`
- `pyproject.toml` `uv.lock`
- `async def` `.query().all()` `25-data-postgres.md`
- `engine` `async_session` `get_db` `src/database.py` `25-data-postgres.md`
- `DATABASE_URL` `postgresql+asyncpg://user:pass@host:port/db` `REDIS_URL` `DB_HOST` `DB_PORT` `DB_NAME` `DB_USER` `DB_PASSWORD` `localhost` `postgres-main`
- `30-ops.md` `.tmp` `/tmp`
- `except Exception` `logger.exception()` `55-observability.md`
- `from {package}.logger import get_logger` `55-observability.md` `structlog.get_logger()` `logging.getLogger(__name__)`
- `asyncio.create_task()` `asyncio.TaskGroup`
- `datetime.now(UTC)` `datetime.utcnow()`
- `ASYNC` `B` `S` `pyproject.toml`
- `uvicorn` `uvicorn.run()` `-slim` `linux/amd64` `30-ops.md`
- `uvicorn.run()`
- `config/production.yml` `settings.production` `config/{dev,staging,prod}.yaml`
- `logging.FileHandler` `logging.handlers.RotatingFileHandler` `TimedRotatingFileHandler` `loguru` `*.log` `55-observability.md`
- `alembic upgrade head` `lifespan` `@app.on_event("startup")` `upgrade head` `docker compose run --rm <svc> alembic upgrade head` `30-ops.md`
- `docs/OPERATIONS.md` `docs/DEPLOYMENT.md`
- `scripts/doc_reconcile.py` `docs/QUICKSTART.md` `docs/CONFIGURATION.md` `docs/data-contract.md` `docs/SERVICES.md` `docs/OPERATIONS.md`
- `scripts/enforcement/_doc_registry.py::PROJECT_DOCS` `/fabrik-plan-after-chat` `Docs:`
- `Agent-Role: primary` `Co-Authored-By` `Co-Authored-By:` `Agent-Role:`
```

| Class | Status |
|---|---|
| Hunt: `scripts/sysadmin/claude_rotate.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (O1 delivery-gated stamp, O2 tolerant stamp read, O3 temp-dir fallback, O4 soonest-dir slug, O5 hashed stamp name, O7/O8 stale comments + docstring, O10 dead dir scan, O6 `--touch` retired; callers of `_tick_telegram` (8 sites) ignore the new bool; `_keepalive_ping`'s sole caller at the stale-reading refresh unchanged) |
| Hunt: `scripts/aro-wake/claude_rotate.py` — every changed hunk, its enclosing function, its callers | CLEAN (byte-identical twin of the sysadmin copy — `cmp` rc 0 by the Opus seat and by the orchestrator after every restore; `test_twin_copies_are_byte_identical` green) |
| Hunt: `tests/test_claude_fleet.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (T1 the 3 d boundary grader — 5 d mutant now killed; T2/T4 the credential-bytes guard retargeted at the LIVE `_keepalive_ping` — temp-copy mutant killed; T3 the EXPIRED-branch grader; six graders for O1–O5; the tick spies report delivery like the real notifier) |
| Hunt: `tests/test_claude_rotate_v2.py` — every changed hunk, its enclosing function, its callers | FIXED r1 (the six `--touch` graders replaced by `test_t8_touch_is_retired_spawns_nothing_and_mutates_nothing` — red on the pre-fix tool, green restored; the sweep grader's docstring no longer claims three weeks) |
| Hunt: `docs/workstation/claude-account-rotation.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (D1 the one-line form the tool prints, D2 two logged runs not three weeks, D3 the cron block byte-identical to `crontab -l` with the retirement stated below it, D4 `--touch` RETIRED, D6 the hashed stamp name; the delivery gate and the temp-dir fallback stated) |
| Hunt: `docs/LESSONS_LEARNT.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (D2 the two logged runs; D5 the three bullets wrapped like their neighbours) |
| Hunt: `CHANGELOG.md` — every changed hunk, its enclosing function, its callers | FIXED r1 (D2/D6: the entry re-cut for the round-1 fixes — delivery gate, hashed stamp, `--touch` retired, the thirteen graders by name — through the private index, the sibling's WIP hunks untouched) |
| Recurrence: fail-open/fail-closed — a swallowed error or an absent check that reads as success | FIXED r1 (O1: `_tick_telegram` returned None and the push stamped regardless — now the stamp waits for delivery; O3: a swallowed stamp-write OSError repeated the push every tick — now the temp-dir fallback; O2: `except OSError` let a `ValueError` escape a never-raises path) |
| Recurrence: cost/quota accounting — pool units scored, native seats counted, a limit at its edges | CLEAN (`refresh_expires_epoch is None` yields NO warning and NO push — unknown is never 0 or expired, `_fleet_row_warnings` + `_chain_expiry_push` isinstance guards; 3 native seats stamped and closed on the round record, no pool) |
| Recurrence: boundary/sentinel/prefix — an off-by-one, a sentinel value, a prefix-vs-exact match | FIXED r1 (T1: exactly 3 d is not pushed and 4 d is warned-not-pushed, graded — the 5 d mutant is killed; O5: the a-z0-9 slug folded `a.b@` and `a-b@` onto one stamp — hashed suffix, graded; ms→s conversion at the single source `_refresh_expiry_epoch`, both consumers read the converted epoch) |
| Recurrence: behavior-without-a-test — a contract row no test kills (mutation asserted) | FIXED r1 (O9/T2/T3: 9 graders red on the pre-fix tool + 2 named mutants killed — `_CHAIN_PUSH_S = 5 d` by `test_fleet_tick_pushes_once_per_chain_inside_three_days`, a temp-dir copy in `_keepalive_ping` by `test_keepalive_ping_runs_claude_in_place_and_never_reads_credential_bytes`; the Sonnet seat's 6-mutant battery: 4 killed, the 2 survivors are T1 and T3, both now graded) |
| Recurrence: denominator on every count — bounded searches state their bound | FIXED r1 (D2: "three weeks of 0 pinged" was asserted at 5 sites against a log holding 2 runs of 2 — re-derived by `grep -c pinged ~/.claude/keepalive.log` = 2, `ls keepalive.log*` = 1 file; every site now says two logged runs, 2026-08-31 and 2026-09-07) |
| Recurrence: proxy-as-evidence — the real check EXECUTED, not read | CLEAN (every CONFIRMED candidate reproduced by the orchestrator on the scratch copy `<scratchpad>/rotation-review/exec` or the live box read-only — `probes/phase2.py` output pinned; every REFUTED one carries the disproving line; the suites run on the tree, the graders seen red on the pre-fix tool) |

Verdict grammar (the gate refuses anything else): `CLEAN (<the paths/lines hunted>)` — a CLEAN row
must name a path and run past 70 characters · `FIXED r<n> (<what changed>)` · `REFUTED (<the
disproving line>)` · `RECORDED — unexecuted (<why>)` · `RECORDED — by design`, parenthesising the
owning row's first-cell id and the EARLIER round that adjudicated it, or a `D-nnn` with no round
(the § Residual block below shows the shape) · `RECORDED — measured (<why>)` ·
`RECORDED — hygiene false positive (<why>)` — every RECORDED reason is PARENTHESISED, never
colon-delimited (a colon would spell the `unexecuted: N` counter the ledger refuses). `UNCHECKED`
may survive only under a `## BLOCKED` escalation (a finding + 3 failed attempts).

## Pass Ledger

ONE table, one row per pass, counts punctuated (`found: F, new: N, confirmed: C, fixed: X,
unexecuted: U`) — the gate reads the LAST row as the exit round and refuses a second ledger group.
`found:` counts raw candidates, `new:` is prose the graders do not parse, `confirmed:` counts the
candidates EXECUTED and reproduced (the exit counter — a round is quiet at `confirmed: 0` and
`fixed: 0` with `unexecuted:` 0 or absent), `unexecuted:` counts code candidates RECORDED
unexecuted. Minimum two passes; the fixing pass is never the last; the closing pass re-derives
every count and anchor and says so in its Method cell, and its Finders cell names the seats that
read it by model token (`opus×1`, `sonnet×2`) — a round the orchestrator alone read cannot close.

| Pass | Finders | Counters | Method |
|---|---|---|---|
| Pass 16 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 8, new: 8, confirmed: 5, fixed: 5, unexecuted: 0 | method: re-derivation — delta over the round-15 diff `c25f63ae..8f442d68` (70 changed fix lines, 111 with the twin and the receipt → the partition, SEATS 3) + one hop; pin md5 9db75194; the Opus seat executed what the round-15 widening in `_drain_mail` CREATED: with the encode error swallowed per repo, every spawned `mail.py send` child sits on a never-closed pipe (`close()` skipped by the raise), reads EOF when the tick dies and publishes an EMPTY broadcast to every mailbox — before round 15 one child hung and the tick died loudly, after it ~N children and silence (executed with a fake `Popen`: 3 of 3 spawned, `close()` skipped, the child alive after the reference dropped); in `_tick_telegram` the same catch turned an unencodable chain-push message into a permanent silent miss whose printed reason names no such cause; the mirror's "retries it next tick" omitted the notifier's window (attempted each tick, one duplicate after 30 min); one hop out, `_keepalive_ping`'s `text=True` capture raises `UnicodeDecodeError` on one invalid byte of output; the docs seat found H19's "0 hits" false (1 hit, the tuple kept by design); the test seat found the graders unable to pin the tuple (a bare `except Exception` survives) and the drain grader gated by a skipif; round 16 re-grounds by DELETION (D-251): the round-15 `UnicodeError` catches are removed and the class is closed at its root — `_argv_safe` escapes a lone surrogate once at each sink (`\\ud800`, delivered as written), `_drain_mail` closes the write end in `finally` on every exit, `_keepalive_ping` decodes its unread text with `errors="replace"`; the three graders now pin delivery, the closed pipe (a broken pipe included) and the survived decode; the docstring, the doc and the CHANGELOG entry say re-attempted each tick with one duplicate after the window; round-zero probe — the three graders red on the pre-round-16 tool (the escaped message not delivered / the pipe left open / `UnicodeDecodeError` raised), green restored |
| Pass 15 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 5, new: 5, confirmed: 1, fixed: 1, unexecuted: 0 | method: re-derivation — delta over the round-14 diff `26b0a3f2..c25f63ae` (105 changed lines → the partition, SEATS 3) + one hop; pin md5 9210be4e; the test seat found nothing (4 of 4 mutants killed — two clocks restored, `now` taken after the call, `>=`, the decoupled clause); the docs seat found nothing in-hunk (16 claims re-derived, the red-on-revert reproduced, exit 126 reproduced); the Opus seat confirmed no code defect in the hunks and executed the fix's MIRROR: a forward clock step past the tolerance in the instant between `now = _now()` and the notifier's own `date +%s` reads a delivered send as unconfirmed (False, retried next tick — fail-closed, never a silent stamp; the window is the spawn latency, not the 30 s call, because the notifier stamps `date +%s` before curl) — the docstring omitted it and the receipt's "no legitimate write is refused" was unconditional; one hop out it executed a lone surrogate in the message escaping the except tuple as `UnicodeEncodeError` (neither `OSError` nor `SubprocessError`) through `_chain_expiry_push`'s "Never raises"; round 15 names the mirror in the docstring and qualifies the two receipt sentences (DDD1), catches `UnicodeError` as an unconfirmed send with the grader `test_tick_telegram_never_raises_on_an_unencodable_message` and sweeps the class to `_drain_mail`'s text-pipe write (`test_drain_mail_never_raises_on_an_unencodable_message`) — DDD2, one hop, swept with the contract; `_keepalive_ping`'s tuple stays (fixed argv, filesystem-derived env), and adds the one-clock clause to the doc paragraph the docs seat found silent (FFF1, one hop); round-zero probe — the new grader red on the pre-round-15 tool (`UnicodeEncodeError` raised), green restored |
| Pass 14 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 5, new: 5, confirmed: 4, fixed: 4, unexecuted: 0 | method: re-derivation — delta over the round-13 diff `dfa247b9..26b0a3f2` (101 changed lines → the partition, SEATS 3) + one hop; pin md5 395f45fd; the Opus seat executed the round-13 sentence "never as the future" (a planted epoch up to 60 s ahead reads as itself) and, behind it, a fail-open in `_tick_telegram`: its two artifact readings took separate clock values, so an artifact just past the tolerance (stale after a backward clock step of 61–90 s, when the notifier suppresses and writes nothing) read 0 before the call and as itself after — a send that never happened confirmed and its chain push stamped (executed on a copy: planted now+62, 3 s stub writing nothing → True); the test seat executed a mutant decoupling "a symlink" from "reads as nothing" that the split assertion let through; the docs seat re-added the round-13 ledger header (XX6 is FIXED one-hop, not RECORDED); round 14 fixes the mechanism in two lines — one `_now()` before the call, both `_stamp_epoch` readings judged against it (a write inside the 30 s call is at most 30 s ahead, inside the 60 s tolerance, so no legitimate write is refused — absent a forward clock step past the tolerance in the spawn instant, the fail-closed mirror DDD1 names in round 15) — with the executed grader `test_tick_telegram_reads_both_artifact_epochs_against_one_clock` (red on the pre-round-14 tool: True where False is asserted), states the tolerance sentence exactly ("no further ahead of *now* than that tolerance"), the parenthetical by genus ("an unrunnable or hand-broken script" — exit 126 on a mode-0 file was the omitted member), pins the whole artifact list clause in the invariance grader, and corrects the header; round-zero probe — the new grader red with both twins at the pre-round-14 file, green restored; the tightened assertion red against the decoupling mutant on a copy |
| Pass 13 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 10, new: 10, confirmed: 6, fixed: 6, unexecuted: 0 | method: re-derivation — delta over the round-12 diff `6d513ec4..dfa247b9` (59 changed lines → the partition, SEATS 3) + one hop; pin md5 f4bd4aac; the Opus seat executed the round-12 gloss "any non-empty write when there was no artifact yet — advances" against `_stamp_epoch` (4 of 6 non-empty shapes read 0: garbage, `0`, negative, future) and the strict `>` against a torn write EQUAL to the previous reading (a shared digit prefix — before `1789`, after `1789`, not advanced, and the notifier's window does not suppress it because `now - 1789` is far past 30 min) — a reachable delivered-but-unconfirmed shape the list omitted; the docs seat found the same equal-reading gap and that "one of them a send that went out" undercounts (the symlink and torn-at-or-below causes follow a delivered send too — the artifact write sits inside the success branch on both notifier paths); the test seat found nothing false (2 of 3 named mutants killed; the surviving one was the four-word consequence clause, dissolved by the rewrite); round 13 DELETES the round-12 gloss and states the mechanism instead — "its artifact did not read as a plausible epoch above the previous reading (a symlink, a non-file, an unreadable or non-numeric file, or a clock-implausible value reads as nothing; a torn write can land at or below it)" — on the line, the docstring, the doc and the CHANGELOG entry; "some of them a send that went out" at the four sites; "an error after a hand edit"; and the one-hop `_stamp_epoch` sentence SS2 recorded now reads "nothing or an epoch no later than that limit, never the future"; round-zero probe — the invariance grader (asserting the mechanism phrase, `a symlink` + `reads as nothing`, `at or below it`, and `"torn to empty" not in line`) red with both twins at the pre-round-13 file, green restored; the equal/garbage/future/below shapes executed by the orchestrator against the fixed tool (False / False / False / False) |
| Pass 12 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 5, new: 5, confirmed: 3, fixed: 3, unexecuted: 0 | method: re-derivation — delta over the round-11 diff `5c063e9b..6d513ec4` (123 changed lines → the partition, SEATS 3) + one hop; pin md5 d634662f; the test seat found nothing (3 named mutants killed, every asserted phrase once in the string); the docs seat found nothing in-hunk (14 claims re-derived, the Pass 10/11 counters re-added); the Opus seat executed the round-11 clause "torn short so it did not advance" against a stub notifier and falsified it: with no prior artifact (`before == 0` — the first chain push for every account, 0 of 2 live `.notified` files match `quota-rotation-chain*`) a torn non-empty write ADVANCES and confirms (probes A/B True, C/D False, E True — re-executed by the orchestrator, same five verdicts), executed a symlinked artifact as a permanent non-confirmation the clause list did not name, and read the timeout path (a raise, no status) against the parenthetical that paired it with "the return code is never read"; round 12 rewrites the cause as "a symlink (read as nothing), unreadable, clock-implausible, or torn to empty or below the previous reading, so it did not advance" on the line, the docstring, the doc and the CHANGELOG entry, and the parenthetical as ends-without-that-status (a parse error, a signal, the 30 s timeout which raises here) with no status read on any path; round-zero probe — the invariance grader (now asserting the new phrase, the symlink cause and `"torn short" not in line`) red with both twins at the pre-round-12 file, green restored |
| Pass 11 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 8, new: 8, confirmed: 7, fixed: 7, unexecuted: 0 | method: re-derivation — delta over the round-10 diff `d7c78895..5c063e9b` (126 changed lines → the partition, SEATS 3) + one hop; pin md5 ca71867b; the test seat found nothing (5 mutants killed, the invariance property re-proven); the Opus seat read the whole `mesh-notify` branch of the notifier and showed every expansion is guarded — the "abort under set -u" the round-10 wording asserted is unreachable there — and executed a torn short artifact reading as a plausible past epoch (a cause none of the clauses named) and the return contract ("delivered") vs the new "DELIVERS" sentence (the counter counts CONFIRMED pushes: 3 deliveries, `sent == 0`); round 11 removes the unreachable clause, names the torn-short cause, conditions the no-keys clause on no custom notifier, makes the exit-count parenthetical exact, says CONFIRMED in the return contract and "re-attempts … delivers again whenever the send itself succeeds"; the Pass 10 row now states its PP8/RR2 dedup; round-zero probe — the invariance grader red with both twins at the pre-round-11 file, green restored |
| Pass 10 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 6 / returned 3 (the first three seats died on API timeouts before reading; re-dispatched on the same pin — the readers are the second three) | found: 13, new: 13, confirmed: 10, fixed: 10, unexecuted: 0 (11 rows carry FIXED r10; PP8 and RR2 are ONE finding counted once; QQ1 refuted and PP9 recorded never count — D-206) | method: re-derivation — delta over the round-9 diff `e6e26ada..d7c78895` (131 changed lines → the partition, SEATS 3) + one hop; pin md5 1ba2abd3; the Opus seat EXECUTED two shell facts the round-9 wording had assumed: a notifier that aborts under `set -u` AFTER writing its artifact returns True (an abort is not unconditionally a failure cause and the artifact IS re-read on that path), and a failed `>` redirect is reported on stderr BEFORE `2>/dev/null` applies (the tick captures and discards it); the docs seat found a round-4 sentence ("judged against the wall clock: absent · suppressed · send failed") still inside the CHANGELOG entry I had been re-cutting every round — the hygiene sweeps never included CHANGELOG in their denominator; round 10 rewords: the abort out of the not-re-read parenthetical, no keys as never attempted, the stderr fact, the qualified exit-count claim, the causal chain for the lock dir, the existing-dir caveat, the CHANGELOG rebuilt; round-zero probe — the invariance grader red with both twins at the pre-round-10 file, green restored |
| Pass 9 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3 (all three reported under the fleet-quota hold that fired mid-pass; the run record was closed BLOCKED (missing infra) and a fresh record opened on the lift at 11:10) | found: 11, new: 11, confirmed: 9, fixed: 9, unexecuted: 0 | method: re-derivation — delta over the round-8 diff `10fa9da2..e6e26ada` (224 changed lines → the partition, SEATS 3) + one hop; pin md5 c3ef7c14; the Opus seat executed a notifier stub that delivers and then fails to write its artifact (`claude-sound.sh:295` swallows the write error): the line said "NOT delivered" three times while three Telegrams went out, and the 30-minute bound is read from the same unwritable artifact; round 9 renames the verdict to `push UNCONFIRMED`, names every cause including a delivered-but-unrecorded send, and states the lock-dir caveat on the bound; a contract grader pins that the dir, not the mode, is the trust boundary; round-zero probe — the changed graders red with both twins at the pre-round-9 file, green restored |
| Pass 8 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 16, new: 16, confirmed: 14, fixed: 14, unexecuted: 0 | method: re-derivation — delta over the round-7 fix diff `164a6fa7..10fa9da2` (179 changed lines → the partition, SEATS 3) + one hop; pin md5 5fbd6940; the Opus seat measured that round 7 had re-grown what D-251 removed (a path-based owner/mode guard in the reader — user cron, this uid's 0700 dir, single operator: no principal to defend against — with a permanent re-fire on a mount that cannot hold a mode and a TOCTOU window) and that the failure line asserted "did not advance" on the exception path where the artifact is never re-read (executed: the marker advanced, the line said it had not); the docs seat showed the receipt's own "43 of 45" had no derivation; round 8 REMOVES the guard and its grader, rewords the line to what was checked, drops the dead parameter, adds a zero-write guard, re-wraps the two long docstrings, and re-derives the count from the five counters (64 of 66); round-zero probe — the changed graders red with both twins at the pre-round-8 file, green restored |
| Pass 7 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring — the round resumed after § BLOCKED | found: 16, new: 16, confirmed: 14, fixed: 14, unexecuted: 0 | method: re-derivation — delta over the round-6 deletion `593dccb2..164a6fa7` (314 changed lines → the partition, SEATS 3) + one hop; pin md5 a1adea41; the deletion held under execution (every mechanical attack refuted by the Opus seat: fd cleanup over 400 refusals, ENXIO/S_ISREG in 0.00 s, ENOSPC direction, `--status` reaches the accessor and creates but never chmods); what remained was residue OF the deletion (four docstring/comment sentences and a stale citation), one removed guard with no read-side replacement (executed: a 0o666 planted stamp silenced the push), and the receipt's own overclaim (P3/P4 re-derived against `fce96964`); round-zero probe — the three new graders red against the pre-round-7 tool in the suite run that preceded the fix (3 failed, 309 passed), green after (312 passed) |
| Pass 6 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 19, new: 19, confirmed: 16, fixed: 16, unexecuted: 0 | method: re-derivation — delta over the round-5 fix diff `47787394..593dccb2` (230 changed lines → the partition, SEATS 3) + one hop; pin md5 7151bc12; the STALL BREAKER fired on this row (confirmed 11 → 13 → 16 over Passes 4–6, non-decreasing and nonzero) — § BLOCKED: NON-CONVERGENCE below names the foundation error and the re-grounding; re-derived by the orchestrator: the 15 callers of `_rotate_state_dir` include the `--status` read path, `chmod(d, 0o700)` strips setgid/sticky (the seat's probe re-read), the notifier sanitises a garbage artifact to 0 and SENDS (`claude-sound.sh:263-265`), `_fleet_active_wall_advisory` writes `_fleet_exhaustion_stamp()` (one stamp, two names); class rewrite — round 5's additions REMOVED (the accessor's repair, the discriminated verdict, the setting helper, the sibling enumeration) rather than patched; round-zero probe — the re-cut graders red with both twins at the pre-round-6 file, green restored |
| Pass 5 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 15, new: 15, confirmed: 13, fixed: 13, unexecuted: 0 | method: re-derivation — delta over the round-4 fix diff `b1762728..47787394` (398 changed lines → the partition, SEATS 3) + one hop; pin md5 a461fe3d; re-derived by the orchestrator: `mkdir(exist_ok=True)` leaves an existing 0775 dir at 0775 (probed), a readerless FIFO blocks `os.open` without O_NONBLOCK (the pre-fix writer hung the grader until a 15 s timeout — rc 124), the sibling stamps' temp-dir fallbacks read at `_drain_stamp_path`/`_fleet_exhaustion_stamp`/`_identity_probe_stamp` (my D-249 sentence was false — D-250 corrects it), the drain stamp's caller is `_tick_inner` not `_fleet_tick_inner`; round-zero probe — 4 of 5 round-5 graders red with both twins at the pre-round-5 file plus the FIFO hang, green restored |
| Pass 4 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 18, new: 18, confirmed: 11, fixed: 11, unexecuted: 0 | method: re-derivation — delta over the round-3 fix diff `d0eb9ba8..b1762728` (370 changed lines → the partition, SEATS 3) + one hop; pin md5 f4d93893; re-derived by the orchestrator: `pathlib.mkdir(parents=True)` creates intermediates at the default mode (the seat's probe on both commits, re-read), the decider's per-entry excepts (`:796-800`), `float(10**400)` OverflowError executed at the producer (`_refresh_expiry_epoch` → None after the fix), the four backlog line citations vs pin4; SCOPE-GROWTH STOP applied: rounds 2–4 confirmed defects only inside the fallback machinery the fix added, so the round-4 edit DELETES the fallback (one stamp home, the 0700 state dir) instead of designing a fourth one — D-249; round-zero probe — 7 of 10 round-4 graders red with both twins at the pre-round-4 file, green restored; hygiene at close: 0 references to the deleted symbols |
| Pass 3 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 16, new: 16, confirmed: 12, fixed: 12, unexecuted: 0 | method: re-derivation — delta over the round-2 fix diff `82f179c7..d0eb9ba8` (338 changed lines → the partition, SEATS 3) + one hop; pin md5 46bbce6e; re-derived by the orchestrator: the Stop decider's prune read at `claude-stop-decider.py:783-799` (top level only, 2 h, rmdir on empty dirs), `_file_refreshed_credentials` 42 lines def→next top-level and 4 call sites (grep with the def excluded), `_tick_telegram` 8 calls (9 mentions − the def); round-zero probe — 7 of 9 round-3 graders red with both twins at the pre-round-3 file, green restored; class rewrite — the notifier gate + the stamp block (`_stamp_epoch`, `_notify_marker`, `_tick_telegram`, `_notify_failure_reason`, `_chain_push_digest`, `_chain_push_stamps`, `_stamp_holds`, `_write_stamp`, `_chain_expiry_push`) rewritten in ONE batch from the probes' output after two consecutive residue rounds (D-231) |
| Pass 2 | native opus×1 (tool fix diff) + sonnet×2 (tests · docs), dispatched 3, returned 3, all fresh and non-authoring | found: 17, new: 17, confirmed: 14, fixed: 14, unexecuted: 0 | method: re-derivation — delta over the round-1 fix diff `fce96964..82f179c7` (873 changed lines → the round-1 partition, `dispatch_headroom.py --slices opus=1,sonnet=2 --delta 873` → SEATS 3) + one hop; pin md5 d34f03c2; every candidate re-executed by the orchestrator: `claude-sound.sh` read read-only (0 non-zero exits in 325 lines, `.notified` written only on a delivered send, window per key), the drain-stamp reader read (`stat().st_mtime`, never `read_text` — my round-1 R1 claim was FALSE), the 4 pre-existing ping graders counted at `7cb15def~1` (3 removed + 1 kept); round-zero probe — 5 of 7 round-2 graders red with both twins at the pre-round-2 file, green restored; class rewrite — `_tick_telegram`'s delivery paragraph rewritten from the notifier's own source (the artifact, not the rc) |
| Pass 1 | native opus×1 (tool) + sonnet×2 (tests · docs), dispatched 3, returned 3 (a first Opus dispatch died on an API timeout before reading anything and was re-dispatched — not a reader) | found: 22, new: 22, confirmed: 20, fixed: 20, unexecuted: 0 | method: re-derivation — every candidate executed on the scratch copy (`probes/phase2.py`: O2 raised UnicodeDecodeError, O3 pushed 5 of 5 ticks, O4 named `aa` for a two-dir row, O5 collided, T3 EXPIRED text lacked the clause, D1 the one-line form) or on the box read-only (D2 `keepalive.log` 2 runs of 2, D3 `crontab -l` vs the doc line); pin md5 67e3917d; hygiene at start (4 hits: H1–H4) and at close (3 hits, the same false positives at moved line numbers); round-zero probe — 9 graders red on the pre-fix tool, 2 named mutants killed (5 d push window; temp-dir copy in the kept ping) |

Row shapes (quoted here, so the gate does not read them as passes):

```text
| Pass 1 | native opus×1 + sonnet×2 | found: N, new: N, confirmed: C, fixed: X, unexecuted: U | citation |
| Pass 2 | native opus×1 + sonnet×2 | found: 0, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | method: re-derivation |
```

## Residual

Every candidate that did not enter `confirmed:` is recorded here, one row each, in the verdict
grammar of `/fabrik-review` § Phase 2 (the fenced block under the next heading is an EXAMPLE, never rows).

### Disposition ledger — round 1 (22 candidates → 20 FIXED + 2 REFUTED + 0 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| O1 | opus | the push stamped before delivery (`_tick_telegram` returned None, ignored rc, silent on a missing script) | FIXED r1 · LOCAL — `_tick_telegram` returns True only on rc 0; `_chain_expiry_push` stamps only on True, prints "NOT delivered … retried next tick" otherwise; graders `test_chain_push_stamps_only_a_delivered_notify`, `test_tick_telegram_reports_delivery` |
| O2 | opus | a non-UTF-8 stamp raised `UnicodeDecodeError` out of a "never raises" path (executed: `push#3 RAISED`) | FIXED r1 · LOCAL — `_stamp_holds` (`errors="replace"`, `except (OSError, ValueError)`); grader `test_chain_push_survives_a_garbage_stamp`; the sibling `_drain_stamp_path` readers are one hop out → backlog row (§ Residual R1) |
| O3 | opus | an existing read-only state dir passes `mkdir(exist_ok=True)`, the write OSError was swallowed, the push fired every tick (executed: 5 of 5) | FIXED r1 · LOCAL — `_chain_push_stamps` (state dir, then temp dir); read checks both, write falls through; grader `test_chain_push_falls_back_to_the_temp_dir_when_the_state_dir_is_read_only` |
| O4 | opus | `_relogin_block(slugs[0])` names the alphabetically first dir, not the one whose chain lapses (executed: `aa` for a row whose soonest dir is elsewhere; latent today — 5 of 5 fleet accounts hold one dir) | FIXED r1 · LOCAL — the row builder records `refresh_expires_slug`; warning + push use it; grader `test_chain_warning_and_push_name_the_dir_whose_chain_lapses_soonest` (sarp@ in `seo` + `youtube`) |
| O5 | opus | `a.b@x.com` and `a-b@x.com` share one stamp (executed: True) | FIXED r1 · LOCAL — 8-hex sha1 suffix; grader `test_chain_push_stamps_do_not_collide_across_lookalike_emails` |
| O6 | opus | `--touch` refreshes on a temp-dir COPY (pre-existing; premise falsified by the same measurement; a pair the liveness gate refuses leaves the store with a spent single-use token) | FIXED r1 · ARCHITECTURAL → D-247 (a flag retired; the alternatives — keep it, or make it in-place — rejected in the row): `_cmd_touch` prints `_TOUCH_RETIRED_LINE`, rc 0; `_touch_run_cli` removed; grader `test_t8_touch_is_retired_spawns_nothing_and_mutates_nothing` replaces six |
| O7 | opus | the orphan two-line comment with an unmatched `)` at the removed constant; `_CHAIN_EXPIRY_WARN_S` justified by "the keepalive cadence is 7 days" | FIXED r1 · LOCAL — comment removed; the 5 d rationale rewritten (two days of warnings before the 3 d push) |
| O8 | opus | `_fleet_row_warnings` docstring ended "a /login or a claude turn IN THAT DIR" | FIXED r1 · LOCAL — "a /login IN THAT DIR — never a claude turn (it does not move the chain), never a file copy" |
| O9 | opus | six branches of the new mechanism had no grader | FIXED r1 · LOCAL — the graders under O1–O5 + T3 (9 red on the pre-fix tool) |
| O10 | opus | `_cmd_keepalive` still scanned `_fleet_dirs()` for a function that ignores its arguments; rc 1→0 contract | FIXED r1 · LOCAL — no scan (`_keepalive_sweep([], …)`, graded by a raising `_fleet_dirs` in `test_keepalive_is_retired_spawns_nothing_and_says_why`); rc 0 is the deliberate retirement contract (doc § Re-login: "a no-op that prints why (rc 0)"), the cron line ignores rc |
| O11 | opus | `_CHAIN_PUSH_S`/`_CHAIN_EXPIRY_WARN_S` not env-tunable (12-Factor III) | REFUTED (III governs deployable app config; this is a box-local operator tool whose sibling threshold `_CHAIN_EXPIRY_WARN_S = 5 * 86400` has been a literal since it landed, the tests reach the constant by name (`cr._CHAIN_PUSH_S` in the boundary grader), and the seat itself marked the choice defensible) |
| O12 | opus | `now` captured at tick start is up to `KEEPALIVE_TIMEOUT` (150 s) stale at the push | REFUTED (the boundary is 259,200 s; a ≤150 s skew moves a push by one tick at most, and the pre-existing warning path uses the same `now` — `_fleet_tick_inner` computes it once by design so one tick prints one instant) |
| T1 | sonnet | `_CHAIN_PUSH_S = 5 d` mutant survived (no fixture between 3 d and 5 d, none at 3 d) | FIXED r1 · LOCAL — the push grader walks 4 d (warned, not pushed) → exactly 3 d (not pushed) → 2 d (pushed once); mutant killed (executed) |
| T2 | sonnet | `_keepalive_ping`'s in-place env shape lost its only direct grader with the removed sweep test | FIXED r1 · LOCAL — `test_keepalive_ping_runs_claude_in_place_and_never_reads_credential_bytes` (argv, CLAUDE_CONFIG_DIR == CLAUDE_QUOTA_HOME == the dir, no `mkdtemp`, no credential bytes); temp-copy mutant killed (executed) |
| T3 | sonnet | the EXPIRED branch of the warning and the push were untested; the shipped EXPIRED text omitted "does NOT extend" | FIXED r1 · LOCAL — the clause added to the EXPIRED text; grader `test_status_and_push_name_an_expired_chain_with_the_login_block` |
| T4 | sonnet | `test_keepalive_never_reads_credential_bytes` guards a stub that reads nothing | FIXED r1 · LOCAL — retargeted at the live ping (= T2's grader) |
| D1 | sonnet | the doc's illustrated 3-line block ≠ the one-line form `_relogin_block` prints (executed: `… claude → /login as ob@ocoron.com → /exit`) | FIXED r1 · LOCAL — the doc shows both and says which the tool prints |
| D2 | sonnet | "three weeks of 0 pinged" at 5 sites vs a log holding 2 runs (executed: `grep -c pinged` = 2 of 2 lines, 1 file) | FIXED r1 · LOCAL — every site now names the two logged runs (tool docstring, doc, LESSONS, CHANGELOG, v2 docstring; 0 of 5 files still say three weeks) |
| D3 | sonnet | the doc's "installed lines" cron block carried a trailing comment the live crontab lacks (executed: `crontab -l`) | FIXED r1 · LOCAL — the block is the crontab byte for byte; the retirement is prose below it |
| D4 | sonnet | the `--touch` bullet omitted that its own premise is the falsified one | FIXED r1 · LOCAL — the bullet states RETIRED 2026-09-13 with the premise and the hazard (with O6) |
| D5 | sonnet | the LESSONS bullets unwrapped, unlike their neighbours | FIXED r1 · LOCAL — wrapped at 100 columns with the file's two-space continuation |
| D6 | sonnet | `fleet-chain-push-<email>` presented as the literal filename | FIXED r1 · LOCAL — `<email slug>-<8 hex of the email>` (with O5) |

### Disposition ledger — round 16 (8 candidates → 5 FIXED + 1 FIXED one-hop + 2 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| GGG1 | opus | the round-15 `UnicodeError` catch in `_drain_mail` CREATED a leak with a fleet-visible side effect: the raise skipped `proc.stdin.close()`, the loop now ran to completion, so every repo's spawned `mail.py send` child sat on a never-closed pipe (the write end retained by the un-waited `Popen`), read EOF at the tick's death and published an EMPTY broadcast finding to every mailbox (`mail.py send` refuses no empty body) — before: one hung child, one empty mail, the tick died loudly; after: ~N of each and success reported (executed: 3 of 3 spawned, `close()` skipped, child alive after the reference dropped) | FIXED r16 · LOCAL — the catch is DELETED (D-251: the round's addition removed, not patched); the class closed at its root: `_argv_safe` escapes the message once (`backslashreplace` — a lone surrogate is delivered as `\\ud800`), and the write end is closed in `finally` on every exit (`contextlib.suppress(OSError)`); grader `test_drain_mail_never_raises_on_an_unencodable_message` pins the delivered bytes and the closed pipe, a broken pipe included, red on the pre-round-16 tool |
| GGG2 | opus | the round-15 docstring clause "(an unencodable message never spawns it)" contradicted its own neighbour: `_notify_failure_reason` names no such cause, so the operator read "may have been delivered" for a send this side KNEW never started | FIXED r16 · LOCAL — the clause is gone with the catch; the message is escaped before argv, the notifier runs and the send is confirmed; the docstring says so |
| GGG3 | opus | with `UnicodeError` caught, an unencodable chain-push message returned False on every tick forever — the chain-expiry push, the one thing the function exists to deliver, never reached the operator, silently | FIXED r16 · LOCAL — with GGG1's escape the push is delivered as written; grader `test_tick_telegram_never_raises_on_an_unencodable_message` asserts True and the escaped argument received by the notifier, red on the pre-round-16 tool (False) |
| GGG4 | opus | "the caller retries it next tick" — the notifier's own window suppresses every resend for 30 minutes, so the retry is ATTEMPTED each tick and the operator sees one duplicate once the window lapses | FIXED r16 · LOCAL — "re-attempts it each tick — the notifier's own window suppresses the resend until it lapses, so the operator may see one duplicate — never a silent stamp" in the docstring; the doc and the CHANGELOG entry say re-attempted each tick, one duplicate after the window |
| GGG5 | opus | ONE-HOP-OUT: the round-15 sweep covered the ENCODE side only — `_keepalive_ping` is the one `subprocess.run` with `text=True` and `capture_output=True`, so a single invalid UTF-8 byte from `claude -p ping` raises `UnicodeDecodeError` past its tuple, mid-liveness-pass (executed) | FIXED r16 · ONE-HOP (swept with the class, not counted) — `errors="replace"` on the run (the text is never read, only the status); grader `test_keepalive_ping_survives_undecodable_output` (a fake `claude` printing `\\xff\\xfe`), red on the pre-round-16 tool |
| HHH1 | sonnet (tests) | the round-15 graders asserted only "never raises", so a bare `except Exception` survived them (executed) | RECORDED — measured (moot with GGG1: the catches are deleted; the round-16 graders pin delivery, the closed pipe and the survived decode, and a bare catch would fail the delivery assertion) |
| HHH2 | sonnet (tests) | `test_drain_mail_never_raises_on_an_unencodable_message` is the only grader that calls the real `_drain_mail`, gated by a skipif on `/opt/fabrik/scripts/mail.py` — 0 assertions run where the file is absent | RECORDED — measured (the hub box carries the file, the skip has never fired; `_drain_mail` hardcodes the path, so running it elsewhere needs a seam this review will not add — D-251) |
| III1 | sonnet (docs) | H19 claimed "0 hits" for the old except tuple over 4 files while DDD2, one row above, says `_keepalive_ping` keeps that exact tuple — 1 hit in the tool | FIXED r16 · LOCAL — H19 reads "1 hit, `_keepalive_ping`'s tuple, kept by design" |

### Disposition ledger — round 15 (5 candidates → 1 FIXED + 2 FIXED one-hop + 2 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| DDD1 | opus | the one-clock fix has an undisclosed MIRROR: a forward clock step of more than the tolerance in the instant between `now = _now()` and the notifier's `date +%s` makes a delivered send read 0 against the pinned limit — False on a send that went out (executed: forward step +200 during the call, notifier wrote now₀+70 → False; pre-fix True); the direction is fail-closed (unconfirmed → retried next tick, the notifier's window bounding it), the exposure is the spawn latency, and the receipt's "no legitimate write is refused" was unconditional | FIXED r15 · LOCAL — the `_tick_telegram` docstring names the mirror and its direction; the Pass 14 row and AAA1 qualified; no code change (the trade is right: a spurious True stamped the push for good, a spurious False retries) |
| DDD2 | opus | ONE-HOP-OUT: a lone surrogate in `msg` (a JSON-parsed ledger row can carry one) makes `subprocess.run` raise `UnicodeEncodeError`, which is neither `OSError` nor `SubprocessError`, so it escapes `_tick_telegram` and `_chain_expiry_push`'s "Never raises" (executed) | FIXED r15 · ONE-HOP (swept with the contract, not counted) — `UnicodeError` joins the except tuple (an unconfirmed send, nothing spawned); grader `test_tick_telegram_never_raises_on_an_unencodable_message`, red on the pre-round-15 tool; the class swept over the tool's 3 `(OSError, SubprocessError)` sites: `_drain_mail`'s text-pipe write of the same JSON-derived text takes the same token with `test_drain_mail_never_raises_on_an_unencodable_message` (red on the pre-round-15 tool), `_keepalive_ping` keeps its tuple — fixed argv and filesystem-derived env, surrogateescape-safe |
| DDD3 | opus | ONE-HOP-OUT: "advanced during the call" is not "this call delivered" — another writer of the same key's marker inside the window (two ticks under the default key) reads True while this call's notifier suppressed | RECORDED — measured (pre-existing and unchanged by the fix; the chain push uses its own per-account key, and the default key's writers are the tick's own notifications under the tick lock; the operator-visible meaning "a send under this key went out" holds) |
| EEE1 | sonnet (tests) | the one-clock grader's second leg (a write at now+5 → True) passes on the pre-fix tool too — a guard against over-correction, not a red/green witness | RECORDED — measured (by construction: leg 1 is the witness, leg 2 pins the mirror's inner edge; stated in the grader's docstring) |
| FFF1 | sonnet (docs) | ONE-HOP-OUT: the doc paragraph (unchanged in round 14) describes the artifact confirmation without the one-clock rule — an omission, nothing false | FIXED r15 · ONE-HOP (swept with the claim, not counted) — one clause in the paragraph: both readings against one clock value; a backward step never confirms a send that did not happen; a forward step past the tolerance only reads a delivered send as unconfirmed, retried |

### Disposition ledger — round 14 (5 candidates → 4 FIXED + 1 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| AAA1 | opus | "a torn or planted file reads as nothing or as an epoch no later than that limit, never as the future" — a file up to 60 s ahead reads as itself (executed: now+5 … now+60 read as themselves, now+61 reads 0); and the claim it supported — "True ONLY when the artifact advanced during the call" — was false in the mechanism: `before` and the after-reading called `_stamp_epoch` at different wall clocks, so the tolerance limit moved by the call's duration and an artifact in the band `(now₀+60, now₁+60]` read 0 then positive with nothing written (executed: planted now+62, a 3 s stub writing nothing, `_tick_telegram` → True); non-adversarial trigger: a backward clock step of 61–90 s, under which the notifier computes a negative age, suppresses, and writes nothing — the chain push is stamped and never retried | FIXED r14 · LOCAL — `_tick_telegram` takes one `_now()` before the call and judges both readings against it (a write inside the 30 s call is ≤ 30 s ahead, inside the 60 s tolerance — no legitimate write refused, absent a forward clock step past the tolerance in the spawn instant: the fail-closed mirror DDD1 names); the `_stamp_epoch` sentence reads "no further ahead of *now* than that tolerance"; the `_tick_telegram` docstring states the one-clock rule; grader `test_tick_telegram_reads_both_artifact_epochs_against_one_clock` (planted now+61 + a stub writing nothing → False; a write at now+5 → True), red on the pre-round-14 tool; distinct from AA1 (a wrong reason) and XX7 (the notifier's own parse) |
| AAA2 | opus | "an error after a hand edit, a signal, or this call's 30 s timeout" omits a member: a script that passes `is_file()` but is mode 0 makes bash exit 126 — no edit, no signal, no timeout (executed) | FIXED r14 · LOCAL — "an unrunnable or hand-broken script, a signal, or this call's 30 s timeout" (the genus, not a longer list); the paragraph reflowed |
| AAA3 | opus | ONE-HOP-OUT: the docstring paragraph's 59-column line left ragged by the round-13 reflow | RECORDED — measured (the paragraph is rewrapped by AAA1/AAA2's edit; cosmetic, outside the hunk) |
| BBB1 | sonnet (tests) | round 13 split "a symlink (read as nothing)" into two independent substring checks, so a rewording detaching the symlink from its consequence passes (executed: "a symlink is refused outright, a non-file … reads as nothing" → 2 passed) | FIXED r14 · LOCAL — the grader pins the whole list clause as one phrase ("a symlink, a non-file, an unreadable or non-numeric file, or a" + "clock-implausible value reads as nothing"); red against the seat's decoupling mutant on a copy |
| CCC1 | sonnet (docs) | the round-13 ledger header counted XX6 ("FIXED r13 · ONE-HOP") as RECORDED: "6 FIXED … + 3 RECORDED" against 7 FIXED / 2 RECORDED in the rows | FIXED r14 · LOCAL — the header reads "7 FIXED, one of them the one-hop XX6 + 1 REFUTED + 2 cited + 2 RECORDED"; the Pass 13 counters (found 10, confirmed 6, fixed 6) exclude the one-hop items and were already consistent |

### Disposition ledger — round 13 (10 candidates + 2 one-hop-out observations → 7 FIXED, one of them the one-hop XX6 + 1 REFUTED + 2 cited + 2 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| XX1 | opus | the round-12 gloss "a torn write ABOVE the previous reading — any non-empty write when there was no artifact yet — advances and confirms" is over-broad and contradicts its own sentence: with `before == 0`, garbage, `0`, a negative and a future value are non-empty writes that read 0 and do NOT advance (executed: 4 of 6 shapes) | FIXED r13 · LOCAL — the gloss is DELETED (D-251: residue of a fix is removed, not refined); the docstring states the mechanism `_tick_telegram` reads — "did not read as a plausible epoch above the previous reading" |
| XX2 | opus | `_stamp_epoch(marker) > before` is strict, so a torn write EQUAL to the previous reading does not advance — reachable: a torn prefix `1789` as the previous artifact, the notifier's window computes `now - 1789 >= 1800` so the send goes out and is delivered, its write tears to the same prefix (executed: before `1789`, after `1789`, False) — neither "to empty" nor "below", and the line claims completeness | FIXED r13 · LOCAL — "a torn write can land at or below it" on the line, the docstring, the doc and the CHANGELOG entry; the grader asserts `at or below it` |
| XX3 | opus | "a parse error after a hand edit" — `set -u` without `set -e`: an unbound variable after a hand edit is a RUNTIME exit, not a parse error (a fourth non-zero end) | FIXED r13 · LOCAL — "an error after a hand edit" (the genus; no enumeration to grow) |
| XX4 | opus | "unreadable, clock-implausible" vs `_stamp_epoch`'s own five words (missing, unreadable, garbage, a symlink, not a plausible clock reading): a non-numeric artifact is garbage, a directory is neither | FIXED r13 · LOCAL — the shapes are named as `_stamp_epoch` names them: "a symlink, a non-file, an unreadable or non-numeric file, or a clock-implausible value reads as nothing" |
| XX5 | opus | "the send did go out" trusts a custom `MESH_NOTIFY_CMD`'s exit status (a custom notifier that exits 0 without sending advances the artifact) | cited — the sentence is gone with XX1's deletion; the line already names a custom `MESH_NOTIFY_CMD` as a send that can fail |
| YY1 | sonnet (tests) | `"torn short" not in line` would trip a FUTURE accurate wording such as "torn short of the previous reading" | REFUTED — the same shape as VV1: a hypothetical future sentence is not a failure scenario (D-230 asks for a concrete one); the ban pins the withdrawn claim, and the round-13 line has no "torn short" |
| YY2 | sonnet (tests) | the round-12 assertion pinned "torn to empty or below the previous reading" but not its consequence ", so it did not advance" — that mutant survived (executed) | FIXED r13 · LOCAL — the rewrite has no separate consequence clause (the mechanism sentence IS the consequence); the grader pins `plausible epoch above the previous reading` and `at or below it`, and `"torn to empty" not in line` |
| YY3 | sonnet (tests) | the torn-above-with-no-artifact and symlinked-artifact verdicts of `_tick_telegram` are pinned by wording only; the one executed grader covers absent / exit-0-without-artifact / advanced / stale | RECORDED — measured (ONE-HOP-OUT: round 12 changed wording, not `_tick_telegram`; backlog row "`_tick_telegram`'s torn-above and symlinked-artifact verdicts are asserted by wording only" in `docs/STRATEGIC_BACKLOG.md`, owner fleet) |
| ZZ1 | sonnet (docs) | "torn to empty or below" omits the EQUAL reading under the strict `>` | same finding as XX2 — cited, fixed there |
| ZZ2 | sonnet (docs) | "one of them a send that went out" undercounts — the symlink and torn-at-or-below causes also follow a delivered send (the artifact write sits inside the success branch of both notifier paths, `claude-sound.sh:268-271` and `:294-296`) | FIXED r13 · LOCAL — "some of them a send that went out" in the doc, "some of which are sends that DID go out" in the docstring, and the two grader docstrings that carried the same count (the 120-column line WW1 recorded is rewrapped with it) |
| XX6 | opus | ONE-HOP-OUT: `_stamp_epoch`'s docstring "a torn or planted file must read as nothing" (SS2) now stood in direct opposition to the round-12 text (a torn prefix `17` reads as 17) | FIXED r13 · ONE-HOP (SS2's line, the same claim one function away — swept with the claim, not counted): "a torn or planted file reads as nothing or as an epoch no later than that limit, never as the future" |
| XX7 | opus | ONE-HOP-OUT: the notifier's window parse (`claude-sound.sh:263-265`) accepts any all-digits `last`, so a planted far-future `.notified` suppresses every send for that key indefinitely while `_stamp_epoch` clamps the same value to 0 — the line's "suppressed by the notifier's 30-minute window" is right in kind, not in degree | RECORDED — measured (the notifier is production and infra's to edit — read-only here; noted in the backlog row with YY3; the class is a planted file in the lock dir on a single-operator box, fire rate 0 observed) |

### Disposition ledger — round 12 (5 candidates + 1 one-hop-out observation → 3 FIXED + 1 REFUTED + 2 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| UU1 | opus | "torn short so it did not advance" is exact only when a prior artifact exists: `_tick_telegram` confirms by `_stamp_epoch(marker) > before` and a torn prefix reads as a small plausible epoch, so at `before == 0` (no artifact yet — the first push of every chain key) a torn non-empty write advances and CONFIRMS (executed: A `'175'` → True, B `'1'` → True, C prior full epoch + `'175'` → False, D empty → False, E full epoch → True) | FIXED r12 · LOCAL — "torn to empty or below the previous reading, so it did not advance" on the line, in the docstring (which adds: a torn write above the previous reading confirms — the send did go out, only the recorded epoch is wrong), the doc and the CHANGELOG entry; the grader asserts the new phrase and `"torn short" not in line` |
| UU2 | opus | a SYMLINKED `.notified` is a permanent non-confirmation cause the rewritten clause does not name: `_stamp_epoch` returns 0 for a symlink while the notifier's `printf >` writes through it (executed: target `'1757900999'`, `_tick_telegram` False) — "unreadable" does not cover a readable file refused by policy | FIXED r12 · LOCAL — "a symlink (read as nothing)" leads the artifact clause on the line, the docstring, the doc ("a symlink") and the CHANGELOG entry; the grader asserts it |
| UU3 | opus | the `_tick_telegram` parenthetical pairs "killed by this call's 30 s timeout" with "the return code is never read here" — on the timeout path `subprocess.run` RAISES (`TimeoutExpired`, caught, `return False`) and no status exists; and the deleted `set -u` clause's TRUE half (a runtime abort, a signal, an OOM kill are further non-zero ends) went unrepresented in a list that kept its enumerative shape (10 `exit` statements in the notifier, all `exit 0`, of 325 lines) | FIXED r12 · LOCAL — "the process can still end without that status — a parse error after a hand edit, a signal, or this call's 30 s timeout, which raises here instead of returning a status — and no status is read on any path" |
| VV1 | sonnet (tests) | `assert "abort" not in line` is a raw substring ban that would trip a FUTURE accurate wording reusing the word (e.g. "aborted by signal N") | REFUTED — no such wording can be accurate from this side: `_tick_telegram` never reads a status (`:2311-2319`), so a signal-named clause would be a new unknowable claim, which is exactly what the ban guards; the seat itself judged the assertion right for today's property (3 of 3 named mutants killed) |
| WW1 | sonnet (docs) | `tests/test_claude_fleet.py:1686` (the invariance grader's docstring) is 120 columns | RECORDED — measured (ONE-HOP-OUT: the round-11 hunk is lines 1701–1711; the file carries 12 lines over 100 columns, none in a round-11 hunk; the repo's `ruff` line rule does not fire on docstrings) |
| UU4 | opus | ONE-HOP-OUT, reported not counted: `_chain_expiry_push`'s return value is discarded by its only caller (`_cmd_tick`), so the "Returns the number of pushes CONFIRMED" contract is grader-only | RECORDED — by design (SS1, round 11; the return is the grader's seam, the tick reads the printed lines) |

### Disposition ledger — round 11 (8 candidates → 7 FIXED + 0 REFUTED + 1 cited)

| id | seat | candidate | disposition |
|---|---|---|---|
| SS1 | opus | "every tick then DELIVERS the push again" beside "Returns the number of pushes delivered" — the counter counts CONFIRMED pushes (executed: 3 deliveries, `sent == 0` each tick) | FIXED r11 · LOCAL — "Returns the number of pushes CONFIRMED — a delivered push whose artifact never advanced is not counted"; the sentence says "re-attempts … delivers it again whenever the send itself succeeds" |
| SS2 | opus | a TORN SHORT artifact (a truncated all-digits epoch — the notifier's `printf` on ENOSPC) reads as a plausible past epoch, so the send is unconfirmed by a cause none of the seven clauses named (executed over six contents) | FIXED r11 · LOCAL — "unreadable, clock-implausible, or torn short so it did not advance" on the line, in the docstring, the doc and the CHANGELOG (the `_stamp_epoch` docstring's "torn … must read as nothing" is one hop out — RECORDED with U6's backlog class) |
| SS3 | opus | "an abort under its `set -u` is the one non-zero way out, and it can happen after the artifact was written" — every expansion in the notifier's `mesh-notify` branch is guarded (21 of 21), no `set -u` abort is reachable there; and SIGKILL by this call's 30 s timeout or a bash parse error are non-zero ways out | FIXED r11 · LOCAL — the parenthetical states the reachable non-zero ends and that the return code is never read; the unreachable "aborted before writing its artifact" clause removed from the line and the docstring |
| SS4 | opus | "no Telegram keys so it was never attempted" is false with a custom `MESH_NOTIFY_CMD` set (it runs before the key check) | FIXED r11 · LOCAL — "no Telegram keys and no custom notifier so it was never attempted" |
| SS5 | opus | "the artifact confirms the send" — a pre-existing artifact confirms nothing; the confirmation is the artifact ADVANCING | FIXED r11 · LOCAL — "after the artifact ADVANCED (that is the confirmation)" |
| SS6 | opus | "DELIVERS" unconditional — with the lock dir broken AND the send failing nothing is delivered | FIXED r11 · LOCAL — with SS1's wording |
| TT1 | sonnet | the Pass 10 row's `confirmed: 10` is reconcilable only by reading PP8's cell (its dedup with RR2) and the D-206 rule | FIXED r11 · LOCAL — the Pass 10 row states both in its counters cell |
| TT2 | sonnet | the abort claim in the docstrings is unreachable in the real notifier (the seat could not find an unguarded expansion) | same finding as SS3 — cited, fixed there |

### Disposition ledger — round 10 (13 candidates → 10 FIXED + 1 REFUTED + 2 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| PP1 | opus | "an abort" sat inside the parenthetical "on which path the artifact is not even re-read" — an abort exits non-zero, `subprocess.run` has no `check`, the artifact IS re-read, and an abort after the write returns True (executed with a `set -u` stub) | FIXED r10 · LOCAL — "the notifier did not finish (a timeout or a failed exec) or aborted before writing its artifact" |
| PP2 | opus | "(swallowed by its `2>/dev/null`)" — bash applies redirections left to right, so a failed `>` is reported on the notifier's real stderr (executed: `Permission denied` on stderr, rc 1); the tick's `capture_output=True` discards it | FIXED r10 · LOCAL — the docstring says where the evidence goes |
| PP3 | opus | "the send failed (… or no Telegram keys)" — with no keys the send is never attempted (`claude-sound.sh:293→301`) | FIXED r10 · LOCAL — "no Telegram keys so it was never attempted" |
| PP4 | opus | `_tick_telegram`'s "0 non-zero exits in the script" is a count of `exit` statements presented as a property of the process; an abort exits non-zero | FIXED r10 · LOCAL — qualified |
| PP5 | opus | the lock-dir caveat stated the effect without the chain and understated it (each repeat is a delivered Telegram) | FIXED r10 · LOCAL — the causal chain and "DELIVERS the push again" in the docstring and the doc |
| PP6 | opus | `_stamp_holds`' "created 0700 less the umask" omitted the accessor's "an existing dir keeps the mode it has" | FIXED r10 · LOCAL — the caveat added |
| PP7 | opus | one docstring line at 102 columns | FIXED r10 · LOCAL — re-wrapped (0 lines over 100 in the four touched functions) |
| PP8 | opus | the CHANGELOG bullet still said "undelivered" twice at the pin (and the entry is absent from the LIVE working tree — a sibling's uncommitted CHANGELOG edit was built from a blob without it) | FIXED r10 · LOCAL — the same finding as RR2 (cited); the working-tree hazard is the sibling's WIP: mailed to the mail-triage lane (§ Gate) |
| PP9 | opus | a sub-second TOCTOU between `_tick_telegram`'s and `_notify_failure_reason`'s independent `is_file()` checks of the notifier | RECORDED — measured (pre-existing, box-static file, sub-second window; no action) |
| QQ1 | sonnet | the contract grader's 0o644 iteration adds no kill power (only the 0o666 case discriminates) | REFUTED (the 0o644 case documents the legacy stamp the contract must keep honouring — a documentation assertion, deliberately; the seat's own verdict was cosmetic) |
| RR1 | sonnet | a round-4 sentence ("an undelivered push prints its real cause … judged against the wall clock: absent · suppressed by the window · send failed") survived inside the CHANGELOG entry through six re-cuts — three causes, a clock the function no longer reads; the hygiene sweeps' denominator never included CHANGELOG | FIXED r10 · LOCAL — the sentence removed from the entry; H14 sweeps CHANGELOG |
| RR2 | sonnet | "undelivered" twice in the CHANGELOG bullet after round 9 retired the word | FIXED r10 · LOCAL — "unconfirmed" |
| RR3 | sonnet | the CHANGELOG and the doc say "0700 when the tool creates it" without the umask hedge MM7 gave the code | FIXED r10 · LOCAL — "0700 less the umask when the tool creates it" in both |

### Disposition ledger — round 9 (11 candidates → 9 FIXED + 2 REFUTED + 0 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| MM1 | opus | one printed line asserted "NOT delivered" and admitted "could not be confirmed"; executed: a notifier that delivers but cannot write its artifact (lock dir refused, `2>/dev/null`) → three real Telegrams, three "NOT delivered" lines | FIXED r9 · LOCAL — `push UNCONFIRMED — …`; the docstring says "an unconfirmed push"; grader asserts the line never says NOT delivered |
| MM2 | opus | the enumeration omitted the one cause under which the message went out: delivered but the artifact could not be written | FIXED r9 · LOCAL — named on the line and in the docstring |
| MM3 | opus | in that state the "bounded by the notifier's window" claim fails too — the window is read from the same unwritable artifact (replicated: sends every tick) | FIXED r9 · LOCAL (wording; the behaviour is one hop out of the hunks and a code change would be new mechanism) — the docstring and the doc state the caveat: a lock dir that refuses the artifact defeats both the stamp and the window |
| MM4 | opus | `MESH_NOTIFY_CMD`'s own failed branch not named (unset on this box) | FIXED r9 · LOCAL — "(curl, a custom MESH_NOTIFY_CMD, or no Telegram keys)" |
| MM5 | opus | a `set -u` abort of the notifier is a third way to "not finish" | FIXED r9 · LOCAL — "(a timeout, an abort, or a failed exec)" |
| MM6 | opus | "unreadable" is the wrong word for a readable artifact the skew clamp zeroes | FIXED r9 · LOCAL — "unreadable or clock-implausible" |
| MM7 | opus | `_stamp_holds`' "created 0700" lacked the umask caveat JJ10 gave the accessor | FIXED r9 · LOCAL — "created 0700 less the umask" |
| NN1 | sonnet | no grader pins the current contract (a wide-mode stamp in this uid's dir holds); re-adding the removed guard passed the suite (executed: 183 passed with the guard back) | FIXED r9 · LOCAL — `test_stamp_holds_trusts_the_dir_not_the_mode` (0644 and 0666 both hold; the push stays quiet) — a contract grader, not a no-regrowth grader |
| NN2 | sonnet | the invariance loop's contents are tautological (inconclusive: the seat's own mutant hung on a bad glob) | REFUTED (HH1's disproof stands — a discriminating mutant fails iteration 1; the docstring states the property) |
| NN3 | sonnet | asserting "NOT delivered" and "could not be confirmed" on one line pins a contradiction | REFUTED as raised (the seat judged them consistent) — and moot: MM1 removed "NOT delivered" and the grader now asserts its absence |
| OO1 | sonnet | the CHANGELOG's "this uid's 0700 state dir" dropped the hedge the doc and code carry (an existing wider dir is still the boundary) | FIXED r9 · LOCAL — "this uid's state dir, 0700 when the tool creates it" |

### Disposition ledger — round 8 (16 candidates → 14 FIXED + 1 REFUTED + 1 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| JJ1 | opus | the line's prefix "its artifact did not advance" was asserted on `_tick_telegram`'s exception path too, where the marker is never re-read (executed: the marker advanced, the return was False, the line said it had not) | FIXED r8 · LOCAL — "the send could not be confirmed"; the docstring says on which path the artifact is not re-read; graders re-cut |
| JJ2 | opus | round 7 re-grew what D-251 removed: a uid/mode guard in the reader and a fourth discriminated clause — the pattern the breaker named | FIXED r8 · LOCAL — the guard REMOVED (the dir is the trust boundary; the reader is back to symlink + regular + content); the causes stay one undiscriminated list |
| JJ3 | opus | the guard defended against no principal this box has (user cron uid 1000, `~/.claude/state` created 0700 by the same accessor, single operator — measured) | FIXED r8 · LOCAL — with JJ2 |
| JJ4 | opus | on a mount that cannot hold 0600 the writer's `fchmod` and the reader's `0o022` refusal disagreed forever: a Telegram every 30 min for the life of the chain | FIXED r8 · LOCAL — with JJ2 (no mode read on the read side) |
| JJ5 | opus | `st_uid != os.getuid()` under a `sudo … --tick` | REFUTED (the seat's own verdict — user cron and the in-process dashboard trigger run as this uid; and the check is gone with JJ2) |
| JJ6 | opus | four path-based syscalls in the reader vs the writer's fd-based checks — a rename over the stamp between `stat` and `read_text` in a wider dir; the docstring's "refuses what a wider mode lets in" overstated a path-based `stat` | FIXED r8 · LOCAL — with JJ2; the docstring names the dir as the boundary |
| JJ7 | opus | the docstring promised "a timeout or a failed exec"; the line said only "(timeout)" | FIXED r8 · LOCAL — "(a timeout or a failed exec)" |
| JJ8 | opus | `while buf:` on a filesystem returning a 0-byte write spins forever inside the flock'd tick | FIXED r8 · LOCAL — a 0-byte write raises OSError |
| JJ9 | opus | `key` kept as ceremony (`del key`) | FIXED r8 · LOCAL — `_notify_failure_reason()` takes nothing |
| JJ10 | opus | "the leaf 0700" is also umask-masked | FIXED r8 · LOCAL — "0700 less the umask" |
| JJ11 | opus | two docstring lines over 100 columns (the file's line-length; E501 ignored) | FIXED r8 · LOCAL — both docstrings re-wrapped |
| KK1 | sonnet | `test_chain_push_names_the_notifier_verdict_it_can_know`'s docstring still said "three possible causes" | FIXED r8 · LOCAL — four, and the confirmed-send wording |
| KK2 | sonnet | the owner/mode grader's single 0o666 fixture let `0o002`/`0o077` mask mutants survive; nothing pinned that a legacy 0644 stamp is honoured | FIXED r8 · LOCAL — the guard and its grader are gone (JJ2); a legacy 0644 stamp is a regular readable file and holds |
| KK3 | sonnet | the uid half of the guard was unkillable in this suite | RECORDED — measured (moot with JJ2; noted for the record) |
| LL1 | sonnet | the doc said "a stamp that is not this uid's 0600 file is never trusted" — stricter than the code (a 0644 stamp was trusted) and inconsistent with the CHANGELOG hunk | FIXED r8 · LOCAL — the sentence replaced by "the dir is the trust boundary" (with JJ2) |
| LL2 | sonnet | the receipt's corrected "43 of the 45" matched no sum of its own counters (Passes 2–6 sum to 66) and listed no derivation | FIXED r8 · LOCAL — § BLOCKED states the population (the five `confirmed:` counters, 66) and the two original-change exceptions (64 of 66) |

### Disposition ledger — round 7 (16 candidates → 14 FIXED + 2 REFUTED + 0 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| GG1 | opus | `_tick_telegram`'s docstring still promised "`_notify_failure_reason` names the one that applies" — the discrimination round 6 deleted | FIXED r7 · LOCAL — the sentence states what the helper names |
| GG2 | opus | `_chain_expiry_push`'s docstring: "with the real cause printed" — false since round 6 | FIXED r7 · LOCAL — "every cause this side can know" |
| GG3 | opus | a dead two-line comment justifying a clock the reason helper no longer reads | FIXED r7 · LOCAL — removed |
| GG4 | opus | round 6 removed the mode repair and nothing on the READ side replaced it: `_stamp_holds` never checked owner or mode, so a 0o666 stamp planted through a wider state dir silenced the push (executed by the seat: 0 pushes with the plant, 1 without) | FIXED r7 · LOCAL — `_stamp_holds` refuses a stamp not owned by this uid or writable by group/other (read-only, no mutation — D-251's line held); grader `test_stamp_holds_refuses_a_stamp_others_could_have_written` |
| GG5 | opus | `_tick_telegram` also returns False when the notifier could not be run to completion (a 30 s timeout, a failed exec) — the line's closed set of causes omitted it | FIXED r7 · LOCAL — "the notifier did not finish (timeout)" added to the line and the contract sentence |
| GG6 | opus | `os.write` return value discarded (a short write is unreachable on a regular file except on error, the direction is re-push — latent) | FIXED r7 · LOCAL — a loop over the buffer |
| GG7 | opus | `{why}` on an argless `ValueError()`/`RuntimeError()` from `_STATE_DIR_ERRORS` prints nothing | FIXED r7 · LOCAL — `{type(why).__name__}: {why}`; the refusal grader asserts the class |
| GG8 | opus | the accessor's docstring "never mutates" — it does CREATE the dir (leaf 0700, parents at the umask) | FIXED r7 · LOCAL — the docstring says exactly that |
| GG9 | opus | `now` kept only to be `del`'d; the three graders were the only callers passing it | FIXED r7 · LOCAL — the parameter dropped, the graders updated (`key` is likewise unused and says so) |
| GG10 | opus | the stamp docstring cited D-249/D-250; D-251 authored its current text | FIXED r7 · LOCAL — D-249/D-251 (doc paragraph too) |
| HH1 | sonnet | the invariance grader's four artifact contents are "dead setup" (removing the writes leaves it green) | REFUTED (the property under test IS invariance — the line does not vary with the artifact, which is exactly what rounds 4–6 got wrong; the four contents are the input domain, and a discriminating mutant (a "suppressed"-only line for −600) fails iteration 1; the docstring now says so) |
| HH2 | sonnet | `FLEET_NOW - 450` residue from the deleted stale-clock grader | FIXED r7 · LOCAL — plain `FLEET_NOW` |
| HH3 | sonnet | the setgid fixture (0o2775) is mount-dependent (overlayfs strips setgid) | FIXED r7 · LOCAL — a plain 0o775 fixture, which still kills the chmod mutant |
| HH4 | sonnet | "File exists" is locale-dependent in principle (executed under `LC_ALL=C` and `tr_TR.utf8`: identical) | REFUTED (the seat's own execution: CPython does not localise the errno text; the grader now also pins the class name) |
| II1 | sonnet | the receipt's § BLOCKED and D-251 said "not one confirmed defect has sat in the ORIGINAL change" since Pass 2 — P3 (the `-0.0d` format) and P4 (the un-annotated `--touch` in the help block) were original-change lines round 1 left as context (re-derived against `fce96964`) | FIXED r7 · LOCAL — § BLOCKED carries the exact count (43 of 45) and names the two; D-251 is immutable and cites this receipt as its source — its "every" reads as the rounding of 43 of 45 stated here |
| II2 | sonnet | the doc cites D-249/D-250 for wording D-251 authored | FIXED r7 · LOCAL — D-249/D-251 |

### Disposition ledger — round 6 (19 candidates → 16 FIXED + 2 REFUTED + 1 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| DD1 | opus | the round-5 mode repair ran on EVERY `_rotate_state_dir()` call — 15 sites, the `--status` read among them — and `chmod(d, 0o700)` stripped setgid/sticky and followed a symlinked `ROTATE_STATE_DIR` (executed by the seat on three fixtures) | FIXED r6 · LOCAL — the repair REMOVED (re-grounding): the accessor never mutates an existing dir's mode; grader `test_rotate_state_dir_never_mutates_an_existing_dirs_mode` (a 0o2775 dir keeps its mode; a created dir is 0700) |
| DD2 | opus | the "unreadable or clock-skewed" branch sat before the suppressed/FAILED discrimination, but the notifier sanitises a garbage artifact to 0 and SENDS (`claude-sound.sh:263-265`), so the branch fired only when the send had actually failed — the operator never learned the keys were broken | FIXED r6 · LOCAL — the discrimination REMOVED: the line names the one knowable fact (the notifier absent, or its artifact did not advance) and all three causes of the latter; graders `test_notify_failure_reason_names_every_cause_it_cannot_tell_apart`, `test_chain_push_names_the_notifier_verdict_it_can_know` |
| DD3 | opus | the corrected rationale named four sibling fallbacks that map to three helpers ("advisory" = `_fleet_exhaustion_stamp`) and omitted `_fleet_refresh_stamp` (4 of 13 call sites carry a fallback) | FIXED r6 · LOCAL — the docstring, the doc and the CHANGELOG no longer enumerate siblings; D-251 records the erratum |
| DD4 | opus | the unwritable line ran the path into the errno text with no separator and printed the path twice (executed) | FIXED r6 · LOCAL — `stamp unwritable ({e})` |
| DD5 | opus | `{why!r}` drops the exception's filename | FIXED r6 · LOCAL — `{why}` |
| DD6 | opus | `_rotate_state_dir_setting()` (`Path.home()`, which can raise RuntimeError) was evaluated inside a `try` guarded only by `except OSError` — a "never raises" function could raise | FIXED r6 · LOCAL — the helper removed; nothing but `_chain_push_stamp` and `_write_stamp` runs inside the guards |
| DD7 | opus | `O_NONBLOCK` let a FIFO WITH a reader take the write "successfully" — a stamp `_stamp_holds` can never read → re-push every tick with no error line (executed by the seat: ENXIO readerless; success with a reader) | FIXED r6 · LOCAL — `S_ISREG` check on the opened fd, non-regular → OSError; grader `test_write_stamp_refuses_a_fifo_and_never_blocks_on_it` (both shapes, under its own alarm) |
| DD8 | opus | `_write_stamp`'s docstring asserted "no other uid can plant anything here", conditional in code on the uid check | FIXED r6 · LOCAL — the docstring states only what the flags guarantee |
| DD9 | opus | "their repeat is unbounded" did not hold for the advisory's telegram (same 30-minute window, default key) | FIXED r6 · LOCAL — the sentence is gone with the enumeration |
| DD10 | opus | the state-dir default expression now had five copies (two in-file) | FIXED r6 · LOCAL — the in-file duplicate removed with the helper (the three external copies are one hop out — hooks, ci probe — unchanged) |
| EE1 | sonnet | the repair grader's single fixture (0o775) let a `0o007` mask mutant survive | FIXED r6 · LOCAL — the repair is gone; the new grader asserts the read guarantee on a setgid 0o2775 dir and the 0700 of a created one |
| EE2 | sonnet | `marker.is_file()` → `exists()` mutant survives (a directory marker) | REFUTED (the seat's own verdict: no behaviour difference for the mutant; and the branch is removed in this round) |
| EE3 | sonnet | the FIFO grader HUNG on the regression instead of failing (no `pytest-timeout` installed: `ModuleNotFoundError`) | FIXED r6 · LOCAL — the grader carries a `signal.alarm(5)` guard that raises a named AssertionError |
| EE4 | sonnet | the symlink grader never asserted the "stamp unwritable" line for the push path | FIXED r6 · LOCAL — `capsys` assertion added |
| EE5 | sonnet | `assert cr._CLOCK_SKEW_TOLERANCE_S == 60.0` couples the test to the literal | REFUTED (by design — the round-5 seat proved a day-long tolerance survived without it; the boundary tests alone cannot pin the magnitude, which is the point) |
| EE6 | sonnet | `os.mkfifo` needs a FIFO-capable filesystem (ext4 here; DrvFs would ERROR) | RECORDED — measured (an environmental precondition of the grader, `stat -f -c %T /tmp` = ext2/ext3 on this box) |
| FF1 | sonnet | the doc and D-250 counted four sibling fallbacks — one stamp under two names (same finding as DD3, the doc/ledger half) | FIXED r6 · LOCAL — the doc paragraph enumerates nothing; D-251 is the erratum |
| FF2 | sonnet | a 137-character line in the doc paragraph (the round-5 sentence ran into the unchanged next one) | FIXED r6 · LOCAL — the paragraph re-wrapped at 100 columns |
| FF3 | sonnet | my backlog script dropped the blank line before the next `## [fleet]` heading | FIXED r6 · LOCAL — restored |

### Disposition ledger — round 5 (15 candidates → 13 FIXED + 1 REFUTED + 1 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| AA1 | opus | a backward clock step past the 60 s tolerance makes the artifact read 0 while the notifier still suppresses (negative age) → the line said "send FAILED" for the length of the skew (executed by the seat: +600 s → FAILED, +30 s → suppressed) | FIXED r5 · LOCAL — `_notify_failure_reason` reports an artifact that exists but reads as nothing as "unreadable or clock-skewed — delivery unknown"; grader `test_notify_failure_reason_names_an_unreadable_or_skewed_artifact` |
| AA2 | opus | round 4 deleted the file's only parent-mode repair and asserted "this uid's 0700 state dir" — `mkdir(mode=0o700, exist_ok=True)` never touches an existing dir's mode (probed: 0775 stays 0775); a wider `~/.claude/state` lets a group member plant the stamp and silence the push | FIXED r5 · LOCAL — `_rotate_state_dir` repairs a wider dir this uid owns (the one helper every stamp uses); grader `test_rotate_state_dir_repairs_a_wider_dir_it_owns` |
| AA3 | opus | the `_chain_push_stamp` docstring justified "no fallback" with "the ledger, drain and advisory stamps are already failing" — four of five sibling stamps fall back to the temp dir by design (`_drain_stamp_path`, `_identity_probe_stamp`, `_fleet_exhaustion_stamp`, `_fleet_refresh_stamp`); only the ledger has none | FIXED r5 · LOCAL — the docstring, the doc paragraph and D-250 state the honest trade (their repeat is unbounded, this one's is bounded by the notifier's window) |
| AA4 | opus | O_NOFOLLOW refuses a symlink but not a FIFO: a readerless FIFO at the stamp path blocks `os.open` forever — a hung 5-minute tick (executed: rc 124 on a 5 s timeout) | FIXED r5 · LOCAL — `O_NONBLOCK` (ENXIO, an OSError the caller handles); grader `test_write_stamp_never_blocks_on_a_fifo` (the pre-fix writer hangs it) |
| AA5 | opus | the state-dir refusal dropped its exception and printed a function name | FIXED r5 · LOCAL — the line carries the configured dir and the exception (`_rotate_state_dir_setting`); grader `test_chain_push_names_the_state_dir_refusal` |
| AA6 | opus | a lock-dir prune inside the 30 s send window reads a delivered send as undelivered → a duplicate next tick | RECORDED — by design (one hop out — the mesh prune, infra's beat; mail `01M2CH5CWPWV1SN62T8RP95QXV`; window ≤ 30 s, cost one duplicate telegram) |
| AA7 | opus (docs slice) | the CHANGELOG bullet ran two sentences together where the deleted fallback clause ended | FIXED r5 · LOCAL — the period restored with the round-5 wording |
| BB1 | sonnet | the epoch-boundary grader read the live tolerance constant, so a tolerance of a day passed it (executed by the seat: 86400 survives) | FIXED r5 · LOCAL — `assert cr._CLOCK_SKEW_TOLERANCE_S == 60.0` |
| BB2 | sonnet | "bounded by the notifier's window" is asserted as a string; no test drives the notifier's suppression during a state-dir outage | REFUTED (the bound is the notifier's, `claude-sound.sh:265` `[ $((now-last)) -lt 1800 ]`, outside this file and never mocked away by the claim — the receipt's Pass 2 and 5 rows quote it from source; the string asserts the tick SAYS so, which is the behaviour under test) |
| BB3 | sonnet | the symlink grader did not re-assert the victim after the push path's own write | FIXED r5 · LOCAL — one assertion added |
| CC1 | sonnet | D-249 and the doc: "the ledger, drain and advisory stamps already fail" — false for four of five stamps (same finding as AA3, the doc/ledger half) | FIXED r5 · LOCAL — the doc paragraph rewritten; D-250 supersedes D-249's wording |
| CC2 | sonnet | the backlog row cited the drain stamp in `_fleet_tick_inner` — it lives in `_tick_inner` (the legacy single-account tick); fleet mode runs the rewritten `_fleet_tick_inner`, which carries no drain stamp | FIXED r5 · LOCAL — the row names `_tick_inner` and says so |
| CC3 | sonnet | D-249: "every one inside the fallback machinery" — an overclaim (X2–X5, P4/P5/P9, W1–W4 were not) | FIXED r5 · LOCAL — D-250 lists what was and was not |
| CC4 | sonnet | "a bare `read_text().strip()`" — the identity-probe read is wrapped in `_STATE_DIR_ERRORS`; "bare" means "without the symlink refusal" | FIXED r5 · LOCAL — the row says exactly that |
| CC5 | sonnet | D-249 framed "`os.open`'s mode never repairs an existing file" as a lock-dir-specific defect class | FIXED r5 · LOCAL — D-250's wording (a general POSIX property, found in the fallback's write path) |

### Disposition ledger — round 4 (18 candidates → 11 FIXED + 1 REFUTED + 6 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| X1 | opus | `parent.mkdir(mode=0o700, parents=True)` creates the INTERMEDIATE lock dir at the default 0755 (pathlib drops `mode` for parents — probed on both commits: pre 0700, post 0755); the round-3 repair only touched `fleet-stamps/`; the docstring's "user-only 0700" was false | FIXED r4 · LOCAL — the fallback home is DELETED (one home, the state dir; `_write_stamp` no longer mkdirs anything); grader `test_chain_push_says_so_and_repeats_when_the_state_dir_refuses_the_stamp` |
| X2 | opus | `_notify_failure_reason(key, now)` was handed the tick's `now`, up to ~450 s stale after the pings leg, so a genuinely FAILED send inside the stale window read "suppressed" — the benign verdict exactly when the notifier is dead; the round-3 grader pinned the defect | FIXED r4 · LOCAL — the reason is judged against the wall clock (`_now()`); the grader pins `cr._now`, passes a stale tick `now` and an artifact at 1900 s, and expects FAILED |
| X3 | opus | `_STAMP_EPOCH_MAX_AHEAD_S = 86400` justified as "the same clamp the drain stamp applies" — the drain clamp is `_CLOCK_SKEW_TOLERANCE_S = 60.0` on an mtime; two future tolerances 1440× apart | FIXED r4 · LOCAL — `_stamp_epoch` uses `_CLOCK_SKEW_TOLERANCE_S` (the notifier writes the same wall clock); grader asserts the exact boundary |
| X4 | opus | `_fleet_row_warnings` reads the same field without the `isfinite` guard the push leg gained — a NaN expiry silently drops the warning | FIXED r4 · LOCAL — the guard mirrored; grader in `test_chain_push_skips_a_non_finite_expiry_and_never_raises` |
| X5 | opus | `math.isfinite(10**400)` raises OverflowError; the producer `_refresh_expiry_epoch`'s `float(exp)` sits OUTSIDE its try — a corrupt giant `refreshTokenExpiresAt` killed the whole tick (executed: OverflowError) | FIXED r4 · LOCAL — the conversion guarded (`OverflowError` → None, non-finite → None); grader asserts `None` for `10**400` |
| X6 | opus | O_TRUNC lands before any ownership check: a regular file owned by another uid in a group/other-writable stamp dir would be emptied | REFUTED (the one stamp home is this uid's 0700 state dir — `_rotate_state_dir()` mkdirs 0700 and no other uid can plant a file in it; the precondition (a wider dir) no longer exists once the shared-dir fallback is deleted; probed: a symlink is refused before any byte moves) |
| X7 | opus | an undelivered push retries every tick with no backoff: a dead notifier costs one spawn (up to `curl -m 15`) per account per tick | RECORDED — by design (D-230 bounded hop; the retry loop is round 1's design, not round 3's hunk; destination `docs/STRATEGIC_BACKLOG.md` "[fleet] An undelivered chain push retries every tick with no backoff", 2026-09-13) |
| X8 | opus | the decider's prune: `f.stat()` outside the per-entry try — a vanishing entry aborts the whole 2 h sweep (the abort-on-our-subdir hypothesis REFUTED by the seat: both excepts present, the loop continues) | RECORDED — by design (one hop out — the box mesh, infra's beat; mail `01M2CH5CWPWV1SN62T8RP95QXV`) |
| X9 | opus | the round-3 sweep grader never wrote the fallback file; the cause grader passed only because of X2 | RECORDED — measured (a restatement of Y1 and the X2 grader, both adjudicated on their own rows) |
| X10 | opus | every chain message is rendered by the notifier as a session death with a bogus `--resume` | RECORDED — by design (pre-existing for all 8 call sites; already in the `_file_refreshed_credentials` backlog row's second note, round 2) |
| X11 | opus | a state-dir outage BETWEEN ticks re-pushes a chain stamped in the state home (the fallback-only list held no stamp) | RECORDED — measured (the fallback design's inherent shape, deleted in this round; with one home the outage prints "stamp unwritable" and repeats by design, bounded by the notifier's window) |
| Y1 | sonnet | `test_chain_push_stamp_survives_the_lock_dir_sweep` never created the fallback file — its title claim was unproven (executed: the lock-dir path did not exist after the push) | FIXED r4 · LOCAL — the grader is deleted with the mechanism it named; the one-home repeat is graded in `…refuses_the_stamp` and `…cannot_be_made` |
| Y2 | sonnet | the digest test asserted only self-consistency — an `upper()` fold mutant survived (executed by the seat: 13 of 13 green) | FIXED r4 · LOCAL — the grader pins `sha1(b"sarp@ocoron.com")[:8]` |
| Y3 | sonnet | the exact `now + tolerance` boundary untested | FIXED r4 · LOCAL — `edge` accepted, `edge + 1` rejected |
| Y4 | sonnet | the decider's non-descent is asserted by no test in this module | RECORDED — measured (moot: the subdir is gone with the fallback) |
| Z1 | sonnet | the sibling-stamps backlog row cited line numbers +27 stale (the same commit moved the file) and one WRITE site as a read | FIXED r4 · LOCAL — the row cites FUNCTIONS, never lines, and names the identity-probe stamp's read correctly |
| Z2 | sonnet | the backlog row cited receipt row "P6-r3" — no such id; the finding is U6 | FIXED r4 · LOCAL — cites U6 |
| Z3 | sonnet | the doc attributed "0700, repaired" to the lock dir itself; the code repaired `fleet-stamps/` | FIXED r4 · LOCAL — the paragraph rewritten for the one-home design (no repair, no lock dir) |

### Disposition ledger — round 3 (16 candidates → 12 FIXED + 0 REFUTED + 4 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| U1 | opus | the round-2 fallback stamp sits in the mesh lock dir's TOP level, which every Stop hook prunes by mtime at 2 h (`claude-stop-decider.py:783-799`, re-read) — the push would repeat every two hours during a state-dir outage (~12/day vs the 288/day it replaced; the `/tmp` home it left was cleaned at 30 d) | FIXED r3 · LOCAL — the stamp lives in `<lockdir>/fleet-stamps/` (the prune never descends and rmdirs only an EMPTY dir) AND a stamp that holds the current key is re-touched every tick; grader `test_chain_push_stamp_survives_the_lock_dir_sweep` |
| U2 | opus | "NOT delivered (mesh-notify unavailable)" named the wrong cause for the two likely ones (suppressed by the window; send failed) | FIXED r3 · LOCAL — `_notify_failure_reason(key, now)` (absent · suppressed · FAILED); grader `test_chain_push_names_the_real_cause_of_a_failed_notify` |
| U3 | opus | `os.open(…, 0o600)` applies only to a file it creates: an existing 0644 stamp keeps 0644 across O_TRUNC (probed) — the "fresh 0600" docstring and the doc's "as a 0600 file" overclaimed | FIXED r3 · LOCAL — `os.fchmod(fd, 0o600)`; grader `test_write_stamp_enforces_0600_and_repairs_its_parent` |
| U4 | opus | `mkdir(mode=0o700, exist_ok=True)` never repairs an existing wider dir; the delivery gate trusts an artifact in it | FIXED r3 · LOCAL — the parent is chmod'ed to 0700 when this uid owns it and it is wider (same grader) |
| U5 | opus | the round-2 commit REPLACED the withdrawn round-1 backlog row instead of correcting it — the RECORDED item existed nowhere on disk | FIXED r3 · LOCAL — the corrected row (the sibling stamps follow symlinks / a bare `read_text` — U6's true content) added to `docs/STRATEGIC_BACKLOG.md` beside the `_file_refreshed_credentials` row |
| U6 | opus | the sibling stamps (drain `touch`+`utime` ~2599, fleet advisory `write_text` ~4794, identity-probe bare `read_text` ~2776/2791) do not follow the symlink-refusing standard | RECORDED — by design (D-230 bounded hop; one hop out of the fix hunks; destination `docs/STRATEGIC_BACKLOG.md` "[fleet] The tick's other stamps do not follow the symlink-refusing standard", 2026-09-13) |
| U7 | opus | `int(exp)` raises on NaN / −inf out of a "never raises" function (probed; producible only by a hand-built row — `_refresh_expiry_epoch` filters both) | RECORDED — measured (one hop out: the line was round 1's, not round 2's; guarded anyway with `math.isfinite`, grader `test_chain_push_skips_a_non_finite_expiry_and_never_raises`, not counted) |
| U8 | opus | an all-digits garbage or future artifact epoch reads as "the future" forever — no later send could advance it and the key is silenced on both sides; a backwards clock step reads every send as undelivered until wall-clock catches up | FIXED r3 · LOCAL — `_stamp_epoch(path, now)` clamps to `0 ≤ value ≤ now + 86400`; grader `test_stamp_epoch_rejects_garbage_and_future_values` |
| V1 | sonnet | the notify key was derived from `stamps[-1].name.rsplit('-', 1)[-1]` — correct only while both candidate paths share one name (a latent coupling no test pinned) | RECORDED — measured (one hop out per the seat; removed anyway: `_chain_push_digest(email)` is the one derivation for the stamp name and the key, grader `test_chain_push_key_and_stamp_share_one_digest`, not counted) |
| W1 | sonnet | backlog row: "52-line" — re-derived 42 lines from the `def` to the next top-level statement, blanks included | FIXED r3 · LOCAL — the row says 42 (and how it was counted) |
| W2 | sonnet | backlog row: "5 call sites" — `grep '_file_refreshed_credentials('` = 4 (two more mentions are docstring prose) | FIXED r3 · LOCAL — 4, lines named |
| W3 | sonnet | backlog row and the receipt's P1: "9 call sites" of `_tick_telegram` — 9 mentions include the def; 8 calls | FIXED r3 · LOCAL — 8 (the CLAUDE.md "structural line counted as a data row" shape, mine) |
| W4 | sonnet | backlog row cites "Opus seat C6" — the receipt files it as P6 | FIXED r3 · LOCAL — the row cites P6 |
| W5 | sonnet | D-247 says "with a temp-dir fallback"; the mechanism moved to the lock dir and delivery to the artifact — rows are immutable | RECORDED — by design (D-248 minted in this round supersedes D-247's mechanism detail; not a defect of the hunks) |
| W6 | sonnet | "a fixed path" overclaims — `CLAUDE_SOUND_LOCKDIR` moves it (the property that mattered is independence from `$TMPDIR`) | FIXED r3 · LOCAL — the doc, the CHANGELOG and the test docstring say "never `$TMPDIR`; `CLAUDE_SOUND_LOCKDIR` for the notifier and the tool alike" |
| W7 | sonnet | "as a 0600 file" true only for a freshly created stamp | FIXED r3 · LOCAL — true unconditionally after U3 (`fchmod`); the doc says so |

### Disposition ledger — round 2 (17 candidates → 14 FIXED + 1 REFUTED + 2 RECORDED)

| id | seat | candidate | disposition |
|---|---|---|---|
| P1 | opus | the delivery gate carried no information: `mesh-notify` exits 0 on every outcome (suppressed, curl failure, no keys — 0 of 325 lines exit non-zero; 227 of 511 logged outcomes delivered nothing) and all 9 call sites share one 30-min window under the constant key `quota-rotation`, so a flip in the same tick eats the chain push and it is stamped as sent (executed by the seat with a stub notifier; re-read by the orchestrator in `claude-sound.sh:249-306`) | FIXED r2 · LOCAL — `_tick_telegram(msg, key=…)` returns True only when `<lockdir>/<safe key>.notified` (written by the notifier solely on a delivered send) ADVANCED during the call; the chain push passes `quota-rotation-chain-<hash>` per account; graders `test_tick_telegram_reports_delivery_from_the_notifier_artifact`, `test_chain_push_uses_its_own_notify_key_per_account` |
| P2 | opus | the `/tmp` fallback stamp: a planted symlink is written through (`write_text` follows links) and a planted regular file holding the key suppresses the warning forever (executed by the seat on a copy) | FIXED r2 · LOCAL — the fallback moved to the resume mesh's lock dir (`_selfwatch_lock_dir()`, user-only 0700); `_write_stamp` opens `O_NOFOLLOW|O_CREAT|O_TRUNC` 0600; `_stamp_holds` refuses symlinks; grader `test_chain_push_refuses_a_planted_symlink_stamp` |
| P3 | opus | `EXPIRED -0.0d ago` on `left == 0.0` (executed: `f"{-0.0:.1f}"`) | FIXED r2 · LOCAL — `abs(left)` at both sites |
| P4 | opus | `--touch` still advertised in the usage/help block without the RETIRED annotation its sibling `--keepalive` carries | FIXED r2 · LOCAL — the help block's `--touch` RETIRED 2026-09-13 line |
| P5 | opus | `_email_for_token` dead (1 mention in 5,373 lines = its def) with a docstring naming the retired touch path | FIXED r2 · LOCAL — deleted (0 references on the surface, hygiene) |
| P6 | opus | `_file_refreshed_credentials` has no production caller (5 test call sites) | RECORDED — by design (D-230 bounded hop; one hop out of the fix hunks; destination `docs/STRATEGIC_BACKLOG.md` "[fleet] `_file_refreshed_credentials` is a 52-line credential writer with no production caller", 2026-09-13) |
| P7 | opus | `tempfile.gettempdir()` honours `$TMPDIR`: cron and a shell resolve different fallback paths for the same chain (duplicate push during a state-dir outage) | FIXED r2 · LOCAL — the fallback is the lock dir's fixed path (with P2) |
| P8 | opus | the `/tmp` stamp is 0644 under the umask in a 1777 dir (email slug + hash + expiry epoch world-readable) and never reaped | FIXED r2 · LOCAL — 0600 inside a 0700 dir (with P2); reaping: one ~10-byte file per account, the same lifetime as the mesh's own `.notified` files |
| P9 | opus | `hashlib.sha1(email)` is bandit B324 High/High and the hub gate never scans `scripts/` (`final_gate.py:926` walks `src/` only; ruff selects no `S`) | FIXED r2 · LOCAL — `usedforsecurity=False`; the gate scope filed to infra: mail `01M2CEWJQ2S0BWTT21XTZYFQ3T` |
| Q1 | sonnet | the "stamp unwritable … repeats next tick" branch (both homes refuse the write) had no grader (mutant: `else:` removed → 5 of 5 selected tests green) | FIXED r2 · LOCAL — `test_chain_push_says_so_and_repeats_when_no_stamp_can_be_written` |
| Q2 | sonnet | `test_chain_push_stamps_do_not_collide_across_lookalike_emails` reached the REAL `~/.claude/state` (no `ROTATE_STATE_DIR`; `_rotate_state_dir()` mkdirs unconditionally) | FIXED r2 · LOCAL — `ROTATE_STATE_DIR` + `CLAUDE_SOUND_LOCKDIR` pinned to `tmp_path` |
| Q3 | sonnet | the chmod-0500 tests misbehave as root (root ignores modes → `FileNotFoundError` on the fallback read) | FIXED r2 · LOCAL — `@pytest.mark.skipif(os.geteuid() == 0)` on the two mode-dependent graders (a guard for a reachable environment, not a skip of a behaviour) |
| S1 | sonnet | my round-1 backlog row claimed the `_drain_stamp_path` readers `read_text()` under `except OSError` — FALSE: the only reader stats `st_mtime` (`claude_rotate.py:2561-2565`, re-read by the orchestrator); the row invented follow-on work | FIXED r2 · LOCAL — the row replaced by P6's true one; R1 below re-dispositioned |
| S2 | sonnet | `wsl-startup-inventory.md` line 61 (retired no-op) contradicted lines 68-69 of the same bullet ("all live", "the rotation pings") | FIXED r2 · LOCAL — the bullet says `--tick`/dashboard live, `--keepalive` a retired no-op, the tick's stale-reading refresh resolves `claude` |
| S3 | sonnet | § Successor plan's M4 sweep still lists retiring `--touch` as future work | RECORDED — measured (one hop out of the fix hunks — a different section; the sentence was corrected in the same commit anyway, not counted) |
| S4 | sonnet | the doc says the stamp "lands in `/tmp`" — the code used `tempfile.gettempdir()` (and now the lock dir) | FIXED r2 · LOCAL — the doc names the lock dir path, mode and the fixed-path property (with P2) |
| S5 | sonnet | "the three ping graders retired" not verifiable from the delta alone | REFUTED (executed: `git show 7cb15def~1:tests/test_claude_fleet.py` held 4 `def test_keepalive_*` — 3 removed at 7cb15def, 1 kept and since retargeted; the CHANGELOG sentence is true) |

Round-1 START hygiene (`check_review_hygiene.py --surface <5 pinned files> --symbol _keepalive_sweep --symbol _KEEPALIVE_MAX_IDLE_S --symbol _chain_expiry_push --symbol _relogin_block --phrase "run one claude turn" --phrase "keepalive cadence"` → 4 hits over 5 files, 0 rows ungraded):

| H1 | stale-phrase `tests/test_claude_fleet.py:2027` 'run one claude turn' | RECORDED — hygiene false positive (the line is the NEGATIVE assertion `assert "run one claude turn" not in out` — the grader that proves the retired remedy is gone) |
| H2 | stale-phrase `tests/test_claude_fleet.py:2027` 'keepalive cadence' | RECORDED — hygiene false positive (same negative assertion, second phrase) |
| H3 | stale-phrase `docs/LESSONS_LEARNT.md:6624` 'run one claude turn' | RECORDED — hygiene false positive (the LESSONS entry QUOTES the retired remedy as the false claim it was: "run one claude turn" was printed for weeks and was false) |
| H4 | dead-symbol `_KEEPALIVE_MAX_IDLE_S` referenced 0 times across 5 files | RECORDED — measured (the probe symbol is the REMOVED constant; 0 of 5 files reference it is the removal being complete, not a dead symbol left behind) |
| H5 | round-1 CLOSE hygiene: the same three stale-phrase hits at moved lines (`tests/test_claude_fleet.py:2205` ×2, `docs/LESSONS_LEARNT.md:6639`); `three weeks` and `fleet-chain-push-<email>` 0 hits; `_touch_run_cli` / `_chain_push_stamp` 0 references | RECORDED — hygiene false positive (H1–H3's negative assertion and quoted false claim, unchanged) |
| R1 | O2's sibling: the `_drain_stamp_path` readers decode with the default codec — the round-1 record | REFUTED (round 2, S1: the only `_drain_stamp_path` reader is `stamp.stat().st_mtime` under `except OSError`, `claude_rotate.py:2561-2565`; no reader decodes stamp bytes except the one round 1 fixed; the backlog row was withdrawn) |
| H20 | round-16 CLOSE hygiene: `SubprocessError, UnicodeError`, `an unencodable message never spawns it`, `an unconfirmed send too`, `so it is retried` swept over the tool, the doc, the tests and the CHANGELOG entry (4 files) — 0 hits; `retried next tick;` 1 hit in the CHANGELOG entry, the stamp sentence's own (still true: an unconfirmed push is re-attempted) | RECORDED — measured (counts in the commit's hygiene run) |
| H19 | round-15 CLOSE hygiene: `except (OSError, subprocess.SubprocessError):` (the old tuple), `no legitimate write is refused)` (unqualified) swept over the tool, the doc, the tests and the CHANGELOG entry (4 files) — 1 hit, `_keepalive_ping`'s tuple, kept by design (DDD2); the receipt's own rows quote both phrases as history (first written "0 hits" — III1, round 16) | RECORDED — measured (counts in the commit's hygiene run) |
| H18 | round-14 CLOSE hygiene: `never as the future`, `no later than that limit`, `parse error after a hand edit`, `an error after a hand edit`, `_stamp_epoch(marker)` unqualified swept over the tool, the doc, the tests and the CHANGELOG entry (4 files) | RECORDED — measured (counts in the commit's hygiene run) |
| H17 | round-13 CLOSE hygiene: `torn to empty`, `read as nothing)`, `any non-empty write`, `must read as nothing`, `one of them a send`, `one of which is a send`, `torn short`, `parse error after a hand edit` swept over the tool, the doc, the tests and the CHANGELOG entry (4 files) — 2 hits, both the grader's NEGATIVE assertions (`torn short`, `torn to empty`) | RECORDED — measured (counts in the commit's hygiene run) |
| H16 | round-12 CLOSE hygiene: `torn short`, `return code is never read`, `killed by this call` swept over the tool, the doc, the tests and the CHANGELOG entry (4 files) — the one `torn short` hit is the grader's NEGATIVE assertion | RECORDED — measured (the hit is the negative assertion; counts in the commit's hygiene run) |
| H15 | round-11 CLOSE hygiene: `aborted before`, `an abort`, `DELIVERS the push`, `pushes delivered.` swept over the tool, the doc, the tests and the CHANGELOG entry (4 files) | RECORDED — measured (counts in the commit's hygiene run) |
| H14 | round-10 CLOSE hygiene: the withdrawn phrases (`judged against the wall clock`, `undelivered`, `NOT delivered` as a positive, `0700 when the tool creates it` unhedged, `an abort, or a failed exec`) swept over FOUR files — the tool, the doc, the tests AND the CHANGELOG entry (the prior sweeps' denominator omitted the CHANGELOG, which is how RR1 survived six re-cuts) | RECORDED — measured (see the commit's hygiene run for the counts) |
| H13 | round-9 CLOSE hygiene: `NOT delivered`, `undelivered push`, `four causes`, `0700 state dir)` — hits on the tool, the doc and the tests reported in the row's own text (3 files) | RECORDED — measured |
| H12 | round-8 CLOSE hygiene: `0600 file is never trusted`, `st_uid != os.getuid`, `three possible causes`, `43 of`, `D-249/D-251` — 0 hits; `did not advance` 3 hits, all legitimate (the `_tick_telegram` contract sentence and two test docstrings describing the artifact property, not the withdrawn line prefix) | RECORDED — hygiene false positive (the probe phrase on legitimate sites) |
| H11 | round-7 CLOSE hygiene: `the real cause`, `has three causes`, `judged against real time`, `three causes`, `D-249/D-250`, `FLEET_NOW - 450` — 0 hits on the tool, the doc and the tests (denominator: 3 files) | RECORDED — measured (the residue is gone) |
| H10 | round-6 CLOSE hygiene: `_rotate_state_dir_setting` 0 references (removed); `delivery unknown`, `clock-skewed`, `advisory, exhaustion` 0 hits; `repaired` 4 hits, all on pre-existing sites outside the round's hunks (the tool's mesh-heal wording at two sites, two identity-probe test docstrings) | RECORDED — hygiene false positive (the probe phrase on unrelated sites; the removals are complete) |
| H9 | round-5 CLOSE hygiene: the withdrawn phrases (`already failing`, `every one inside`, `_rotate_state_dir.__name__`, the `_fleet_tick_inner` drain citation) 0 hits on the tool, the doc and the tests | RECORDED — measured |
| H8 | round-4 CLOSE hygiene: 0 references to `_chain_push_stamps`, `_FLEET_STAMP_SUBDIR`, `_STAMP_EPOCH_MAX_AHEAD_S`; 0 hits for `fleet-stamps`, `re-touched`, `repaired when this uid owns it` on the tool, the doc and the tests | RECORDED — measured (the deletion is complete) |
| H7 | round-3 CLOSE hygiene: 4 hits over 4 files — `a fixed path` in a test docstring (reworded in-round), three `{{…}}` template-residue hits in `docs/STRATEGIC_BACKLOG.md` rows this review never touched (lines 65/199/897) | RECORDED — hygiene false positive (the residue sits in other rows of a shared backlog; the phrase hit was fixed) |
| H6 | round-2 CLOSE hygiene: 7 hits over 7 files — 6 stale-phrase hits are the orchestrator's own probe phrases landing on pre-existing sites (`gettempdir` in the drain/lock fallbacks at 2151/2734/3915/3925 and a v2 test, `returncode == 0` in `_keepalive_ping`'s own rc), 1 dead-symbol `_email_for_token` 0 of 7 = the deletion complete | RECORDED — hygiene false positive (probe phrases on legitimate sites; the dead-symbol hit is the removal measured) |

### Verdict grammar — an EXAMPLE, never rows

(quoted so no reader — human or scan — takes this template's own sample rows for the receipt's residuals):

```text
| F12 | RECORDED — unexecuted (3 probe attempts timed out) |
| F19 | RECORDED — by design (F4, round 3; D-203) |
| F31 | RECORDED — measured (a prevalence figure; makes no code or doc claim) |
```

`RECORDED — by design` names the OWNING row's first-cell id and the EARLIER round that adjudicated
it (or a `D-nnn` with no round); the gate refuses an absent owner and a round that is not below the
closing `Pass N`. `RECORDED — measured` and `RECORDED — unexecuted` never enter `confirmed:`;
`unexecuted:` on the closing row is what keeps an unexecuted CODE candidate from closing the loop.

## BLOCKED: NON-CONVERGENCE — the breaker fired at Pass 6, and the foundation error is the growth of the fix

The stall circuit-breaker's condition held at Pass 6: three consecutive delta rounds with non-decreasing, nonzero confirmed counts (Pass 4: 11, Pass 5: 13, Pass 6: 16). Per the contract this is a VERDICT, not a reason for a seventh patch. The suspected foundation error, named from the receipt's own rows: of the 66 defects confirmed in Passes 2–6 (the Pass rows' `confirmed:` counters, 14 + 12 + 11 + 13 + 16), 64 sat in what the PREVIOUS round's fix added — code, graders or prose — and two sat in the original change: Pass 2's P3 (the `-0.0d` format, a line of the original change that round 1 left as context) and P4 (the help block's un-annotated `--touch`, also original). (This sentence first read "not one", then "43 of 45" with no derivation; the round-7 and round-8 docs seats caught both — the population is the five counters above and the two exceptions are the only original-change rows in the five disposition ledgers.) D-251's summary sentence rounds 64 of 66 to "every"; this receipt is its cited source and carries the exact count. — a temp-dir fallback (P2/P7/P8), a lock-dir fallback (U1, X1), a mode repair on a read path (DD1), a discriminated failure verdict (X2, AA1, DD2), rationale prose enumerating sibling stamps (AA3, CC1, DD3, FF1), diagnostic helpers (DD4–DD6, DD10). The loop was converging a mechanism the fixes kept extending — the scope-growth stop's own shape — and the round-4 deletion of the fallback was followed by round-5 additions of the same kind. Re-grounding applied in Pass 6 (D-251): the additions are REMOVED, not patched — the accessor is a read again, the failure line names what this side can know and nothing more, no helper, no enumeration; only two hardenings with an executed reproduction stay (`O_NONBLOCK` + `S_ISREG`). Pass 7 resumes the loop as a delta over that deletion with fresh seats; the exit bar is unchanged (a fresh non-authoring seat confirming 0 on an unedited pin).

## Per-phase verdicts

### Phase 1 — Independent finders, round 1 (opus×1 + sonnet×2, pin 67e3917d): DONE — 22 candidates, 20 confirmed and fixed in-round, 2 refuted

### Phase 2 — Execute every candidate: DONE — every CONFIRMED reproduced on the scratch copy or the box read-only, every REFUTED carries its disproving line

### Phase 3 — Prove & fix: DONE — 9 graders red on the pre-fix tool, 2 named mutants killed, twins byte-identical, D-247 minted for the `--touch` retirement

### Phase 4 — Converge: IN PROGRESS — rounds 2–16 (delta) confirmed 14 → 12 → 11 → 13 → 16 → 14 → 14 → 9 → 10 → 7 → 3 → 6 → 4 → 1 → 5; round 16's five are what round 15's catches CREATED (the leak, the silent miss, the unnamed cause), the mirror's consequence, and a receipt count — re-grounded by deleting the catches and escaping at the sinks; round 17 (delta over the round-16 diff) is owed

## Gate

GATE-SCOPE: out-of-surface — Lint Ratchet (repo-wide, no new debt); findings naming this surface: 0 of 1; measured by: `uv run ruff check . --output-format concise` → `tests/test_check_convergence.py:1968:26: F401 subprocess imported but unused` (a sibling lane's committed wip 7dad5031, mail-triage Phase B — mailed to that lane; `uv run ruff check` on the review's four Python files → All checks passed)


`final_gate.py --check --json`, pasted verbatim at the flip (check_convergence reads the fenced
`"status": "success"`):

```json
UNCHECKED — paste the gate output here at the CONVERGED flip
```
