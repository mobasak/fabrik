# Acceptance review — T06a (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** IN-PROGRESS

**Surface:** the coder's worktree branch (worktree-agent-afe8783e4de220f3f, head 049e04dc) against its merge base 1abbc7dd — ONE new file `commands/_sources/fabrik-vision.md` (1,135 lines) moved from `docs/orchestrator/mega-epic-breakdown/00-trigger-mega-epic-fabrik.md` (962 lines; untouched). Coder: native Sonnet worktree. Gates: `check_traycer_chain.scan` → 0; retired-name grep → 0; `--check` exits 2 on the questionbar/subagents-core placeholders (EXPECTED until the assembler's `PARAMS["fabrik-vision"]` exists — T07a's file); `check_command_corpus` 30 refs: 28 to the sibling sources T06b/T06c, 2 the `validate_i18n.py` false-positive class (the checker's templates/ fallback is wired for orch docs only — infra beat, recorded).

**Merge disposition (orchestrator, recorded for T16 + a ledger row):** the three T06 sources render and pass the corpus check only together and only with the assembler's `PARAMS` entries — each is accepted on its own review, then all three merge in ONE D5 commit with the `PARAMS` entries added as the orchestrator's D2 mechanical fixup (T07a keeps the NEXT map and the rest).

## Round 1
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.
### Adjudication (pool layer)
- deepseek — CLEAN (4 rows; stale names 0; four includes on their own lines; Part B/C; Reads budget as prose; description 843 chars; no substance lost — the only omission the "row 102" label, which the ticket says stays as prose).
- qwen — 3 raised: "market-facing has no operational definition" — carried to the native finder (the source defines it at :268 as "a product with real external competitors/users, not an internal tool" — whether that is decidable is the finder's call); "no stale names" (its own CLEAN); "`{{include:questionbar}` at :1133 lacks its closing brace" — REFUTED by grep on the branch file: the four include lines are :25 run-record, :159 grounding-rules, :1107 questionbar, :1135 subagents-core, all well-formed.
- gemini — 5 raised: "wedge + white-space routing missing" — REFUTED by grep (:279 "its pricing wedge and white-space…" routes them to Technology Decisions / Out of Scope); "market-facing undefined" (carried, as above); "the Reads-budget row 102 dropped from a checklist structure" — the ticket mandates PROSE, the old doc had no checklist row either; "the 04 → /fabrik-epics-review rewrite missing" — UNVERIFIED by it (the coder's survival table lists the rename; the native finder greps it); "examples lost" — the old § Examples was a pointer to an archived file, condensed to a one-line pointer (the coder's declared drop) — carried to the native finder's substance-lost sweep.
### Native finder (opus) — 15 raised; the "move with two drops" framing FALSE: 543 of the old doc's 749 normalised lines had no match (15,709 → 12,039 words). Verified clean: the four includes alone on their lines (no other `{{…}}`), traycer-chain 0, stale names 0, no changelog prose, "market-facing" defined at :268-269 with internal tools + Retrofit skipping, the MATCH/BEAT/wedge/white-space vocabulary identical to fabrik-rivals.md.
- [H] no ticket in the set supplies `PARAMS["fabrik-vision"]` (0 `PARAMS` hits across 35 plan files) — the ticket's own `--check` gate is permanently red as written → PLAN-TEXT defect: T07a's brief gains the PARAMS entries (the coder leaves its intended questionbar text in a `<!-- PARAMS for T07a -->` comment).
- [H] NEW mode lost its orientation step (7 pre-flight checks + 12 planning constraints + the reconciliation rule; the only pre-flight sentence sat in EXISTING mode) → FIXUP (1).
- [M] two question bars render (inline prose + the fragment) → FIXUP (2). [M] the rivals wiring half-carried into the skeletons, no citation requirement, no acceptance row → FIXUP (3). [M] the market-facing STOP sited inside Path A only; the EXISTING-delta case unnamed → FIXUP (4). [M] the watchdog cost-cap rule lost in both skeletons → FIXUP (5). [M] Technology Decisions content stripped in four bullets → FIXUP (6). [M] `has_bearer_api`/`is_public` detail + citations lost → FIXUP (7). [M] Stage-1 missing-vs-modified + 5 governance files + "resolve from the index" lost → FIXUP (8).
- [L] compliance-row values thinned → FIXUP (9). [L] two hollow-citation guards + fleet-topology groundings dropped → FIXUP (10). [L] :1055 mis-assigns the verb (`/fabrik-epics` cuts; `/fabrik-epics-review` proves) and the stated successor → FIXUP (11). [L] description headroom 44 chars, not "857 of 1024" → FIXUP (12: trim ≤ 700). [L] the `grounding-rules` fragment's HTML comment names retired paths (T09/T12b's) → ROUTED.
Round 1 verdict: 15 raised → 12 fix classes routed (2 H, 7 M, 3 L), 1 plan-text defect (T07a's brief), 1 routed (fragment), 1 refuted-by-drop (Examples pointer). Not the no-op round. The fixup landed at 0888b880 (153/72; 1216 lines; description 626) — round 2 re-verifies.

## Round 2 — over the FINAL head `0888b880` (1,216 lines; the round-1 restoration: 153/72, twelve classes, description 626)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 2.
### Adjudication (pool layer)
- qwen — CLEAN (the four includes; forbidden strings 0; every restoration located; "old doc's 749 normalised lines fully accounted for").
- gemini — CLEAN with 3 nits: "cite `fabrik-rivals.md:2`" — the ticket's `(path:line)` suffixes are evidence pointers for the reviewer, never text to embed (no corpus source embeds a sibling's line number); "list the modules checked from the index" — the ladder's own evidence rule already binds; "PARAMS comment shape" — the comment is for T07a's coder to COPY, the assembler strips comments and parses nothing there.
- deepseek — 3 raised: "resolve modules from the index missing in NEW mode" — REFUTED: the old doc carried it only in the EXISTING skeleton (:823), which is where it was restored; "no reconciliation rule at :283-292" — REFUTED: the rule is the constraint-set reconciliation ("Apply both — the 12 at orientation, N3i's 20 at verification; the 5 genuinely absent named"), which is what :291-292 states — the finder read "reconcile-all"; a third (truncated) about Part B — qwen's sweep found the Architectural Mandates intact.
### Native finder (opus) — all 12 round-1 restorations CONFIRMED with lines quoted; substance re-measured on the RENDERED output (fragment substitution restores the rule-grounding gate): 446 of 749 old lines unmatched by reflow/rename, 20 by signal, of which 13 are the retired Traycer-mirror block — residue below; 116 table rows old = 116 new; traycer-chain 0; ten retired names 0 each; `_phase_count` 6; composed description 763/1024. 11 raised:
- [H] NOT this file: T07a's mandated `fabrik-rivals` NEXT addendum overflows `_emit_skill`'s 1024 cap (1022 today → 1069/1071) and SystemExits the corpus render → ROUTED to T07a's brief.
- [M] the `<!-- PARAMS for T07a -->` comment is NOT stripped at render (the assembler substitutes only include lines) — it would ship into every run → FIXUP (1: delete; the text goes in the report). [M] that block was incomplete (questionbar only; `subagents-core`'s 5 keys missing) → FIXUP (2: complete dict in the report). [M] :435 dangling "Phase 2 item 0" after the hoist → FIXUP (3). [M] :2/:53 still scope the gate to Path A; :294-311 applies it to both paths + EXISTING deltas → FIXUP (4). [M] :521-522 presents Kilo as a live gateway; the old doc's three retirement markers were dropped → FIXUP (5).
- [M] NOT this file: `check_traycer_chain.py`'s [C] detector cannot see the `file:NN` spelling (regex demands a `-command` suffix), so Behavior Contract row 3 is vacuous for class C → ROUTED to infra (after the plan / T09).
- [L] :205 rotted anchor `scaffold.py:186` (`I18N_ENABLED_TYPES` :258) → FIXUP (6). [L] the compliance matrix lost 7 of 8 pack citations and the bootstrap script names → FIXUP (7). [L] :1120-1127 a clause restated twice → FIXUP (8). [L] Path A/B specified twice in one section → FIXUP (9).
Round 2 verdict: 11 raised → 9 fix classes routed (5 M, 4 L), 2 routed outside the file (T07a's brief; infra). Not the no-op round.

