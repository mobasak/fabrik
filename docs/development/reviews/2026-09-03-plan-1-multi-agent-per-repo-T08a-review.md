# Acceptance review — T08a (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** CONVERGED

**Surface:** the coder's worktree branch diff against the dispatch base b1f7e675 — see the round sections below (one file per ticket, rounds APPENDED).

## Round 1 — over `b1f7e675..a994e02d` (scripts/enforcement/check_command_corpus.py 9/164 — `TRAYCER_SKILLS`, `_ORCH_DOC_RE`, `_orch_corpus()`, its `audit()` call site with the "wrapper tree missing in the hub" problem, the `traycer_skills` parameter and its 5 selftest callers, the selftest's wrapper fixture + two VACUOUS assertions deleted, all anchored by symbol (the ticket's line numbers had drifted: `:1001`, `:1123`, `:1616`); plus one consumer the ticket did not name, found by the first gate run: `orch_doc_set` and the orchestrator-only `templates/**/<script>` fallback in script-path resolution (mirror stated: command sources never received it); three history-only comments left; the denominator comment corrected; watched-fail: the importlib gate exit 1 and grep 20 BEFORE the edit; the check green over 63 files with `_traycer-skills/` parked out of the tree — the three new sources audited by the per-source predicates with no special case; `--selftest` 15 canaries over 8 predicates (was 17: exactly the two wrapper canaries); T08b's untouched test file 69 failed / 55 passed — 65 `traycer_skills` kwarg TypeErrors, 1 `_orch_corpus` AttributeError, 3 hard-coded canary counts each off by 2; the tests asserting the removed semantics named for T08b)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 (an enforcement gate) — round 1.
### Orchestrator execution (in the worktree)
- the check → `✓ … all sound across 63 file(s) read`; the importlib gate exit 0; `--selftest` → 15 canaries over 8 predicates; grep 0; `ruff check` clean; `pytest tests/test_check_command_corpus.py` → 69 failed, 55 passed (T08b's expected reds).
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0133)
- All three CLEAN: every deleted symbol gone, no remaining `docs/orchestrator` reference but the three history comments, the `templates/` fallback reachable only via `path in orch_doc_set` (dead once no orch doc is in `files`), the audited denominator 63 before/after, the three new sources under the per-source predicates, no fail-open for command sources, one file touched.
### Native layer (opus×1, by execution — mutations on copies, the before-version run in a symlinked mirror)
- [L] 1 — the `_SCRIPT_RE` comment (`scripts/enforcement/check_command_corpus.py:108-113`) justifies the absolute-form alternation by "the orchestrator docs cite almost exclusively in the absolute form" — a corpus the check no longer reads, while the branch is still load-bearing: 8 absolute-form citations live across the 63 files, 5 of them in `fabrik-epics-review.md`. ORCHESTRATOR FIXUP at merge (one sentence re-anchored to the live evidence, the incident kept as history).
- [L] 2 — the signature change's unnamed mirror: `agents` moved from 6th to 5th positional, so a five-positional caller would bind an orchestrator path to `agents` silently. Measured: 0 of 0 five-positional call sites across both files (AST; all 27 live sites pass the kwarg by keyword). REJECTED per FIX DIRECTIVE 5 — a zero fire rate; recorded so T08b's mechanical kwarg-strip knows the shape.
- [L] 3 — `:1481`'s "9 of 17 `problems.append` sites had no canary" is a pre-change population carrying an incident tag — historical record by this file's convention. No action.
- Verified clean by execution: the `templates/**` fallback removal is a behaviour no-op for every non-orchestrator input (gated on `path in orch_doc_set`; executed on its exact original case `scripts/validate_i18n.py` — identical verdict before/after; 0 of 144 live script citations missing at hub root); the audited denominator 97 → 63 — exactly `docs/orchestrator/**` (34) dropped, no hard-coded threshold; a fixture repo with a broken `_traycer-skills/` tree is silent after (problems 4 → 0), no crash; all seven applicable predicates fire on a mutated copy of each new source (P1 web-tool names, P2 chain targets, P3 script paths, P4 trailer model, P5 run record, P7 advertised close, P8 caller claims); the selftest's 3 uncovered emitters after are the same 3 of the 6 uncovered before (5 deleted: 2 covered, 3 uncovered; ratio 67% → 77%); no docstring describes the wrapper path (three `orchestrat` mentions: `:110` = finding 1, `:1146`/`:1268` incident history); gates green (63 files; importlib 0; selftest 15/8; ruff/format; mypy 17 errors in 3 files before AND after); trailers parse; one file 9/164; T08b feasible — a mechanical kwarg strip on a copy takes 69 failed → 13 genuine reds, all inside T08b's allowed edits (7 wrapper tests + `test_templates_fallback_is_orch_scoped_and_files_only` to delete — its non-wrapper first assertion is already covered by `test_template_shipped_scripts_resolve:507`, which passes; 5 count/expectation fixes incl. the kwarg inside a generated `driver.py` string at `:1896`); zero verdict changes on non-wrapper inputs. Context: `docs/reference/command-corpus-check.md:47-80` is now stale and is T14b's by Scope; between Merge Order 17 and 18 master's `tests/test_check_command_corpus.py` is red — declared by the ticket and the spine (the hub's pytest leg is off; the pre-commit hook runs `assemble_commands.py --check`).
### Verdict
**0 defects — no-op round** (three advisories: one orchestrator-applied comment fix, one measured-and-rejected mirror, one historical note). Ledger: symbol deletion · the unnamed fallback removal · denominator · silence on a wrapper tree · per-source predicates · selftest coverage · T08b feasibility — all swept. **Status: CONVERGED** at `a994e02d`; merge with the fleet sync (`scripts/enforcement/` is a trigger surface); T08b dispatched from the merged master.
