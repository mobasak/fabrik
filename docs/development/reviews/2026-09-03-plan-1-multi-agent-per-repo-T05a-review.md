# Acceptance review — T05a (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** CONVERGED

**Surface:** the coder's worktree branch (worktree-agent-aa202dd80ecd23540, head 6a065def) against its merge base 2f982a5f — `scripts/enforcement/check_plan_tickets.py` +269/−1 (FLEET-SYNCED), `tests/enforcement/test_plan_tickets_epic_scope.py` +287 (new, 8 tests). Coder: native Opus worktree (Execution Discipline). Gates: 8 passed; 321 across the five plan-ticket suites; ruff + format clean; mypy 0 new. Red-first: 5 of 8 red on the first run, the 3 vacuously-green rows proven by on-disk mutation (`_glob_covers` → `_covered_by`; an always-printed line mutated). Byte-identical proof on the live 33-ticket set (md5 `249e63c3…` both sides). Grounding measured: 10 live epics parsed (9 with `owned_paths`; the schema-less 2026-07-14 epic fails closed); 15-shape predicate table 15/15; `EPIC_HEADER_RE` fires on 1 archived plan fleet-wide. Coder's declared residual: a second `Epic:` line silently uses the first (0 spines carry two today).

## Round 1
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.
### Adjudication (pool layer)
- gemini — CLEAN (separator-awareness; the two-depth subtree probe distinguishes `src/**` from `src/*`; the parser's parity with T03a's incl. comment stripping and list-key restriction; fail-closed paths; the byte-identical baseline; `_carved_out`).
- qwen — CLEAN (7/7 classes; `src/**/x.py` matches `src/x.py` — `**` matches zero segments; `**` alone → `.*\Z`; trailing `/` stripped then probed; the parser compared line-by-line with T03a's branch; six fail-closed conditions; a second `Epic:` line acceptable at ≤1 fleet-wide).
- deepseek — 2+ raised (18,790 chars): "`(?:[^/]+/)*` requires at least one segment, so `src/**/config/**` rejects `src/config/settings.py`" — REFUTED by execution against the branch's own `_glob_covers` (a `*`-quantified group matches zero repetitions; probe output recorded below); "`**` alone compiles to `\Z`" — REFUTED (`**` alone → covers `src/a/x.py`: True). Its remaining items restate the passing rows.
Orchestrator probe on the branch's `_glob_covers` (executed in its worktree):
```
'src/**/config/**'                       covers 'src/config/settings.py'                  -> True
'src/**/config/**'                       covers 'src/a/b/config/x.py'                     -> True
'**'                                     covers 'src/a/x.py'                              -> True
'src/a/*'                                covers 'src/a/b/deep.py'                         -> False
'src/a/**'                               covers 'src/a/x.py'                              -> True
'libs/**/product_entitlements_bridge/**' covers 'libs/x/product_entitlements_bridge/y.py' -> True
'docs/**'                                covers 'docs/x/'                                 -> True
'docs/*'                                 covers 'docs/x/'                                 -> False
```
### Native finder (opus) — executed: 32 adversarial glob shapes correct (mid-`**` zero depth, `*` not crossing a separator where fnmatch would, `?`, `(admin)`/`(deploy)`/`[`/`\` literal, trailing `/`, bare `**`/`*`, glob-free → `_covered_by`); both links fire; 12 fail-closed shapes; base vs branch byte-identical over 68 plan dirs without `Epic:`; a `["**"]` epic against 43 live tickets → 0 findings; `_carved_out` condition-for-condition equal to the File-Scope skip set; stdlib-only, ruff/format clean, mypy = master's 3, 146 passed. 7 raised:
- [M] `_glob_regex`/`_glob_covers` have ZERO unit tests — 6 of 9 semantic mutants survive the 8 CLI tests (mid-`**` ≥1 segment; `?` crossing `/`; literals unescaped → `app/(admin)/**` becomes a group, a live web-ecommerce-factory shape; the 2nd subtree probe dropped; `_carved_out` ≡ False; trailing `**` → one level), two Behavior Contract rows guarded by pure-negative asserts (:199/:211) → FIXUP (1).
- [M] the copied `_parse_frontmatter` (:845-891) diverges from T03a's FINAL parser on 5 of 17 fixtures — a block with a blank line is silently TRUNCATED (the gate then emits a believable false accusation; the docstring's fail-CLOSED claim is wrong for it), a comment line empties the block, unindented block, `k: #x`, a `#` inside a quoted item; 0 of 10 live epics hit a divergent shape today → FIXUP (2): port the classifier + collector verbatim, hub-only parity test over the 17 fixtures.
- [L] the `EPIC_HEADER_RE` comment claims fence-stripping prevents a quoted example keying containment — false for `~~~`, 4-space indented code and prose (`Epic: this plan…` → `'this'`), each failing CLOSED; fire rate verified with its denominator: 1 of 1,003 files fleet-wide, an archived standalone the checker never enters → FIXUP (3, comment). [L] the "no repo root is resolvable" arm (:931-937) is unreachable → FIXUP (4). [L] `(?:[^/]+/)*` per mid-`**` backtracks ~4× per `**` (11 → 6.8 s on a non-match; a project-authored input; no timeout) → FIXUP (5). [L] `_glob_covers("src/a/**","src/a")` → True is the one permissive path (dir vs file undecidable without disk) — docstring must state the resolution → FIXUP (6). [L] no INDEX row for the new test → orchestrator Delta at merge (`delta_T05a_index.py`), not a coder item.
Round 1 verdict: 7 raised → 6 routed (2 M, 4 L) + 1 orchestrator Delta; pool layer's items subsumed. Not the no-op round.

## Round 2 — over `2f982a5f..33cfed6a` (46,630 B; the round-1 fixup 33cfed6a: direct predicate tests killing the six named mutants — `?` white-box via `_seg_regex` because the segment-wise matcher never feeds it a `/`; the frontmatter parser ported VERBATIM from T03a@544cf2ab with a hub-only parity test and a 17-fixture table (5 divergences from the predecessor, F07 measured NOT divergent, F10 the fifth); the `EPIC_HEADER_RE` comment corrected; the unreachable arm deleted; the mid-`**` regex replaced by a segment-wise FRONTIER matcher — 12 chained `**` 32 s → 0 ms; the slash-less resolution documented; 398 passed + 1 skipped across the enforcement suites)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 2.
### Adjudication (pool layer)
- deepseek — CLEAN (every contract row; the six mutant kills named; the verbatim port + parity test; the fence-strip comment; the frontier matcher; two files only).
- gemini — CLEAN (six confirmations by line: the `reach` frontier, the `_glob_covers` resolution order, `_carved_out` ≡ the File-Scope carve-out, the byte-identical port, the dual-link loop, the header-gated integration; 14 new tests, 17 fixtures, 25+ pairs).
- qwen — CLEAN (separator non-crossing, mid-`**` zero segments, the two-probe directory check, F05/F06/F10 parity, base byte-identity, `(admin)` escaped).
### Native finder (opus) — INTERRUPTED by the fleet-quota hold; executed before it: all six mutants applied on disk and each killed by exactly the named test (M9 by two; copy restored, asserted); the five ported symbols AST-extracted from `544cf2ab:scripts/epic_order.py` and the branch — 172 lines each, `diff -u` empty; the parity test's skip reason true (master's `epic_order.py` last changed at 1d89c618, pre-T03a); the `cast` reachability argument true (:1207-1208 vs :723-726); 32 passed + 1 skipped; exactly two files; stdlib; `tests/` is not fleet-synced so the hub-only parity target is by construction. 7 raised:
- [M] `EPIC_HEADER_RE`'s prefix `_F` (:204) has no `>` alternative (the status prefix at :1244 does) while the comment both says "no `>` blockquote" and lists it among the shapes that fail CLOSED — a `> Epic: …` header is silently IGNORED and containment never runs: the fail-open the check exists to close → FIXUP (1).
- [M] `FM_FIXTURES` (17) vary values, never the FENCE — a `----` opener, an indented `  ---` interior, an interior `--- note` are absent, so the parity guard would stay green through the divergence T03a's fence fixup introduces → FIXUP (2), sequenced behind T03a's next commit (re-port verbatim + three fence fixtures).
- [L] `_dup_keys` recorded by the parser, discarded by its consumer (:1142) — two `owned_paths:` lines resolve last-wins → a believable false accusation → FIXUP (3). [L] `_carved_out` (:564-578) duplicated verbatim at :1549-1560 on a synced file → FIXUP (4). [L] the :520-521 `reach` invariant comment is false where used (correct code, wrong premise) → FIXUP (5). [L] `[seq]` bracket classes silently literal and undocumented (`src/[ab]/**` false-REDs) → FIXUP (6). [L] `EPIC_HEADER_RE.search` first-wins vs the "one path per spine" hint → FIXUP (7).
Owed (not executed — the hold): the ≥30-pair frontier-vs-reference table + 4/8/12 timings; the fleet blast-radius sweep at base vs branch (the "1 line fleet-wide" count unconfirmed); ruff/format/mypy/headers/suite counts; the live set's `Agent-Task: T05A` vs Board-row ERROR. Round 3 re-sweeps these.
Round 2 verdict: 7 raised → 6 routed now (1 M, 5 L) + 1 sequenced behind T03a; pool 3/3 CLEAN missed the blockquote fail-open — the native layer found it by reading the prefix. Not the no-op round.

## Round 3 — over `2f982a5f..1e2ae413` (56,018 B; the round-2 fixup 1e2ae413: `_F` accepts `>` so a blockquoted header parses and fails closed; `_dup_keys` consumed; the inline carve-out replaced by `_carved_out` (M6 re-killed); the `reach` comment; `[seq]` documented + refused on the epic side only — measured 9 of 2,146 live Touches/File-Scope tokens are Next.js `[id]` routes kept literal, 0 of 45 live owned_paths carry a bracket; two headers → ERROR; a NameError in the untested unusable-path arm caught by ruff, fixed and covered; item (2) the re-port HELD for T03a's next commit; 404 passed + 1 skipped; live set byte-identical at the same HEAD; the `>`-tolerant regex still matches 1 of 508 live plan files)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 3 (the round-2 owed items required).
### Adjudication (pool layer)
- deepseek — CLEAN (2 files; 5 contract rows mapped to tests; DO-NOTs; every failure path fail-CLOSED incl. the `>` blockquote; the 17-fixture parity; the frontier matcher; the six mutants; `_carved_out` identity with the old inline block) — names the classes it cannot execute (the ≥30-pair table, the whole-box sweep, ruff/mypy/headers, suite counts): owned by the native finder.
- gemini — CLEAN (the blockquote prefix, `_dup_keys` consumed, the `raw_headers[0]` NameError fix, the `reach` comment, `[seq]` epic-side only with Next.js `[id]` literal, byte-for-byte port, stdlib only; the `Agent-Task: T05A` extra finding adjudicated as the staleness checker reacting to the coder's own commits — orchestrator-owned Board flip).
- qwen — CLEAN (7 rows to distinct red-fail tests; 6 mutants; the timed frontier test; the parity + hub sync test; the slash-less resolution; the unreachable arm; `[seq]` refusal).
### Native finder (opus) — pinned to 1e2ae413 (the branch advanced to 06d38837 mid-review: the held re-port landed — nine symbols byte-identical to T03a@2bd530fe, `# fmt: off` so the comparison stays byte-level, two of three new fence fixtures red on the old copy; findings re-confirmed at HEAD where noted; a first vacuous sweep — both checkers crashing at import, "0 divergences" asserting nothing — caught and redone with a crash guard). Executed: every documented header shape (9 parse, 3 fence forms stripped); the HELD fence shapes described at 1e2ae413 (a `----` opener silently passes; an interior `--- note` truncates → a false "carries no owned_paths" accusation through the CLI; both gone at 06d38837) and the 17 fixtures shown NOT to distinguish 544cf2ab from 2bd530fe (0 of 17) — why the re-port needed its own fence fixtures; 13 mutants applied on a copy, all killed; `_carved_out` end-to-end (every carved token keeps its own ERROR; the own-stem receipt the one exemption); the `reach` trace; both counts re-derived (0 of 45 exact; the 2,146 not reproducible — finding 2); the verbatim port byte-identical (172 lines); the frontier matcher differentially fuzzed — 60,000 random + 3,276 exhaustive pairs, 0 mismatches; the `cast` sound; the 58-pair table 0 mismatches with chained-`**` timings ≤0.05 ms; 42 plan-set dirs compared base vs branch, 0 divergences; the "1 line fleet-wide" claim confirmed over 993 files (an archived monolith); ruff/format clean; mypy delta 0; `check_script_headers.py` vacuous-green as staged-scoped (simulated: WARN only); 38+1 / 138 at 1e2ae413, 179+1 at HEAD; the `Agent-Task: T05A` ERROR the staleness class on the coder's own four branch commits (base and branch checkers emit the same 4; 0 from master) — orchestrator-owned Board flip. Could not execute: merged-world parity (T03a unmerged). 3 raised:
- [M] `_seg_regex` (:507) still backtracks catastrophically INSIDE a segment — one `[^/]*` per `*`: 8 stars 0.37 s, 12 stars 63.8 s; 74 s through the real CLI; the :521 "NO backtracking" claim false; 5 of 45 live owned_paths already use multi-`*` segments (max 2) → FIXUP (1): an iterative single-segment wildcard matcher + a timing grader.
- [L] the :502 "9 of 2146" ratio not reproducible at any bound (8 of 1,007 in the gate's own scope; 0 classes at every bound) → FIXUP (2). [L] `>> Epic:` / `> > Epic:` / `>Epic:` still parse to nothing → fail-open in the direction round 2 closed (0 of 993 live) → FIXUP (3). [L, adjacent, pre-existing] `_staleness` prints the uppercased trailer key so the error string cannot be grepped in the spine → FIXUP (4).
Round 3 verdict: 3 raised + 1 adjacent → 4 routed (1 M, 3 L); pool 3/3 CLEAN missed the intra-segment class. Not the no-op round.

## Round 4 — over `2f982a5f..1238d8db` (69,644 B; the re-port 06d38837 — nine symbols byte-identical to T03a@2bd530fe under `# fmt: off`, three fence fixtures F18–F20 (two red on the old copy), the parity test symbol by symbol — and the round-3 fixup 1238d8db: `_seg_regex` replaced by the two-pointer `_seg_matches` (74 s → 0 ms; the red run 128 s; 64,266 fuzz pairs + the 58-pair table clean), the ratio bounded (8 of 1,007 across 15 live sets in 7 repos, 0 alternation sets), nested/space-less blockquotes parse fail-closed, `_staleness` quotes the trailer as written; 410 passed + 1 skipped; 6/6 mutants dead with M4′ re-anchored) — T03a merged to master at 28de4900 during this round, so the parity guard must now run and pass
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 4.
### Adjudication (pool layer) — the first launch died at `model_start` on a network blip (connection resets + one DNS failure in the ledger; credits $4.26, not the cause); relaunched
- deepseek — 1 raised, REFUTED (third time in this ticket): "`_repo_root` may return None → `root / epic_rel` TypeError" — `check_plan_dir` returns `[]` before the call whenever `external_root is None and not _is_plans_layout(plan_dir)`, and `_repo_root` returns None iff not `_is_plans_layout` (a PATH-shape test — `docs/development/plans/<dated>/` — not a `.git` test, so a checkout without `.git` is not the counter-example); the round-2 and round-3 native finders each proved the `cast` sound by reading both sites. Otherwise CLEAN (5 rows, 15 tests, the port with 20 fixtures, the parity test, the Touches).
- gemini — CLEAN (`_seg_matches` single-backtrack two-pointer, `/` never swallowed, literals literal; the `reach` frontier incl. the trailing `+1`; the `fmt: off` port vs 28de4900/2bd530fe; the nested-blockquote prefix; the staleness quote/join split).
- qwen — CLEAN (5 rows; the predicate; byte-for-byte port with 20 fixtures; `_carved_out` shared; the trailer case; nested/tight blockquotes; two files; "the parity test runs against /opt/fabrik/scripts/epic_order.py and passes" — the headline check, to be confirmed by the native finder's own run).
### Native finder (opus) — HEADLINE: the parity test RUNS and PASSES against merged T03a (45 passed, 0 skipped; an independent comparator against `master:scripts/epic_order.py`@28de4900 — 9 symbols, DRIFT []; 20 fixtures behaviour-identical). Executed: the r3 harness (64,266 pairs clean) plus a wider r4 fuzz (130,049 pairs — finding 1's 62 mismatches); timings 12 stars 0.008 ms, 12 chained `**` 0.016 ms; `a**b` in a segment is two stars and the docstring says so; the ratio re-derived exactly (42 dated set dirs, 27 archived → 15 live sets in 7 repos, 1,007 tokens, 8 brackets all Next.js routes, 0 alternation sets; 45 owned_paths, 0 brackets, 5 multi-`*`); the widened prefix on 12 header shapes through the CLI; the trailer case ×4 quoted as written; 10 of 10 mutants killed on an isolated copy (R1's revert 81.98 s); a SIGTERM mid-run proved the isolation (the copy restored from `.bak`, the worktree clean throughout); 42 plan-set dirs base vs branch 0 divergences; ruff/format clean, the `# fmt: off` region intact; mypy delta 0; 45 / 138 / 67 green; two files; stdlib. Could not execute (the hold): the live-shape table over the 45 real owned_paths values; the un-normalised epic entry (`./src/a/**` — owned_paths are `.strip()`ed but never `_norm_path`ed) — routed to the coder to run. 4 raised:
- [M] `_seg_matches` (:518-521) tests the literal branch BEFORE the star branch, so a pattern `*` aligned with a literal `*` in the target is consumed as equal literals with no backtrack point — `("*","**")`, `("*","*x")`, `("a*","a*b")` all False against the docstring; unreachable today (both callers `continue` on `_carved_out`, which rejects `*`/`?`) but a latent trap on a synced file with no test → FIXUP (1).
- [L] four references to the deleted `_seg_regex`, two stating a false mechanism ("escapes `[`") → FIXUP (2). [L] the :275 count "1 line fleet-wide" without its bound (1 of 508 one level down; 1 of 992 recursive; 0 of 476 in dated set dirs; 0 of 301 non-archived — the one hit is archived, so the round-3 body's "live" was wrong) → FIXUP (3). [L] `- > Epic:` / `> - Epic:` mixed prefixes fail open (0 box-wide, no emitter) → FIXUP (4): name them out of contract or widen.
Round 4 verdict: 4 raised → 4 routed (1 M, 3 L); none changes the gate's behaviour on any live set; pool 2 CLEAN + deepseek's refuted arm. Not the no-op round.

## Round 5 — over `2f982a5f..ed2ee6f4` (the round-4 fixup ed2ee6f4, 50/16 + 56/3: the star branch first — the fuzz widened to `*` on the string side proves the class, 1,394 → 0 over 131,466 pairs; the dead references corrected; the fire-rate comment bounded at four populations; mixed bullet+quote prefixes widened fail-closed (the coder's stated call); the held items executed — 45/45 live shapes with a shape-aware probe, and the un-normalised epic entry REPRODUCED and fixed with a glob-safe `_norm_glob` (not `_norm_path`, which would eat `**/x/**`); 414 passed across six suites, 48 + 0 skipped in the epic-scope file — the parity test now runs green against merged T03a)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 5.
### Adjudication (pool layer)
- deepseek — CLEAN (5 rows to tests; the 9 mutant classes incl. the star-order fix; the 20-fixture parity; the staleness quoting; `_carved_out` identical; every fail-open path closed; two files; the counts consistent with the diff's annotations — the fuzz not runnable there, the star fix tested directly).
- gemini — CLEAN (`_norm_glob` preserves `**/x/**` while stripping `./` and edge quotes; the star branch first; the prefix repeating and mixing while bullets keep `\s+`; parser parity incl. the fence fix; no exponential path left; File-Scope link enforced; a local port, no import).
- qwen — CLEAN (23 cases; 45/45 shapes; normalisation admits quoted/backticked/`./`/`/./` but not `../` or absolute; the prefix; `_seg_matches` order; parity; 1/173 non-archived unaffected).
### Native finder (opus) — executed: the star-first matcher against a fresh independent recursive reference — 304,348 pairs, 0 mismatches (four sets incl. exhaustive `{a,b,*,?}^≤4` and 150,000 random both-sides-wildcard paths); the stale-mechanism mentions all tagged history; all three fire-rate bounds re-derived (508 vs the comment's 509 = same-day drift; the one hit archived); the prefix on 12 quote/bullet forms → silent pass, 3 prose forms → fail closed, the bulleted-prose admission pre-existing since 06d38837, linear at n=640; `_norm_glob` end-to-end (the table in finding 3; `**/x/**`, trailing `/`, `/**/`, whitespace, quotes pass; the `# comment` cut is the verbatim port's documented position-0 quote rule); 45 live shapes / 246 assertions / 0 violations with the segment-internal `**` name-bounded; the parity test RUNS (1 collected) and passes against master; 27 mutants on a copy — 20 killed, N10 a message-only equivalent, 6 survivors = findings 1–2; `_carved_out` fails closed per class; BC criterion 4 hard-reds via the pre-existing glob-in-File-Scope error; the `cast` proven by reading AND by `--allow-external` execution; epic findings appended before the DRAFT downgrade so a DRAFT set stays advisory; 69 plan-set dirs base vs branch 0 divergences (the plan's own dir differs only by the `T05A → T05a` class, 6 pairs); ruff/format clean, the `# fmt: off` region intact; mypy delta 0; headers OK; 48 + 0 skipped; 965 across tests/enforcement. 3 raised:
- [M] two fail-closed tests are satisfied by the FALSE-ACCUSATION fallback — mutant N11 (empty owned_paths silent → 4 false accusations) and B4 (the out-of-repo header guard dropped → an ABSOLUTE header naming an EXISTING file is read and enforced: a traversal) both leave the suite green, because :382 names a non-existent `/etc/epics/1-x.md` and :472 asserts only that some epic error mentions the string → FIXUP (1). [M] four tolerance arms ungraded (quote/backtick stripping :636-637; the identical repeated header dedupe :1271; the glob-free `_covered_by` fallback :1332; the scalar owned_paths wrap :615) — each deletion turns a silent PASS into a false accusation → FIXUP (2).
- [L] `_norm_glob` does not REFUSE unusable entries — `../`, absolute, `~`, `//`, `\`, bare `.` each false-accuse every ticket ×4 instead of naming the entry (0 of 45 live) → FIXUP (3): extend the `[seq]` refusal guard.
Round 5 verdict: 3 raised → 3 routed (2 M, 1 L) — graders and one fail-direction; the production logic held on every probe; pool 3/3 CLEAN. Not the no-op round.

## Round 6 — over `2f982a5f..e71b89fc` (the round-5 fixup e71b89fc, 47/14 + 89/12: the two fail-closed tests assert their specific messages with a real absolute-header traversal fixture — B4 and N11 die; four tolerance-arm graders — N4/B3/B5/B10 die; `_unusable_owned` refuses absolute/`~`/`..`/`\`/`//`/`.`/`[seq]` entries naming the entry and the reason (six shapes red-first through the CLI); 424 passed across six suites, 58 + 0 skipped; both harnesses 6/6 dead; 131,466 fuzz pairs clean; 45/45 live shapes, 0 unusable)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 6.
### Adjudication (pool layer)
- deepseek — CLEAN (5 rows; the six unusable shapes refused; the four tolerance arms covered; mutants killed; two files).
- gemini — CLEAN (`_unusable_owned` per shape with the "is unusable" message naming entry + reason incl. the json-escaped backslash; the specific fail-closed assertions killing N11 and B4; the four tolerance graders N4/B3/B5/B10; the matcher tests).
- qwen — CLEAN (44 scenarios: 5 behaviours + 17 tolerance/robustness + 20 fixtures + parity + byte-identity).
### Native finder (opus) — executed: 39 mutants on a copy, 35 killed (every ledger mutant dies; U9 — the refusal's early return — kills 7 tests, so "no ticket accusation" is graded); all 9 refusable shapes through the CLI with entry + reason and 0 accusations, incl. a REAL raw backslash in a hand-authored file; 15 look-alikes 0 refused (`src/..hidden/**`, `src/a..b/**`, `.github/**`, `src/v1.0/**`, `src/(group)/**`, `src/@scope/**`, `./src/**` normalised, trailing `/`, `**/x/**`, a backticked entry); the star-first matcher 304,348 fuzz pairs clean; the nine-symbol port byte-identical, the parity test collected and passing; 15 header forms parse, fences stripped, tilde/prose fail closed; the denominators reconciled (≤4-file drift; the one hit never read — a loose file under `plans/archived/`); 69 plan-set dirs 0 divergences; the `cast` claim; no `Epic: none` sentinel exists; 58 + 0 skipped, 371 across five suites, 969 across tests/enforcement; ruff/format/mypy clean; two files. 7 raised:
- [M] the `..` arm of the header guard (:1314) is UNGRADED — mutant B4b survives 58 green; a `Epic: ../outside/1-x.md` naming a REAL file outside the root runs containment against a FOREIGN epic (4 false accusations; a permissive foreign scope would PASS) — the sibling of the `/`/`~` arm round 5 graded → FIXUP (1).
- [L] the `..` owned_paths arm's segment-equality ungraded in its false-positive direction (the substring form diverges on three legitimate shapes) → FIXUP (2). [L] `test_unresolvable_epic_path_errors` unfailable for its arm (the OSError arm answers too) → FIXUP (3): assert "no such file under". [L] the refusal's REASON half ungraded (U8) → FIXUP (4). [L] `_norm_glob`'s paired-quote strip is a fifth ungraded tolerance arm, reachable only via backtick-wrapped quotes → FIXUP (5). [L] a VALUELESS `Epic:` header parses to nothing → silent skip (0 of 995 live; the emitter never writes it; the file's doctrine calls it the fail-open) → FIXUP (6, fail-closed preferred). [L] `owned_paths: ["./"]` normalises to `""` before the refusal guard sees it → "carries no owned_paths" against a file that declares one → FIXUP (7).
Round 6 verdict: 7 raised → 7 routed (1 M, 6 L) — graders and two messages; pool 3/3 CLEAN. Not the no-op round.

## Round 7 — over `2f982a5f..9d668a93` (the round-6 fixup 9d668a93, 18/2 + 93/15: seven graders each killing its named mutant — B4b via a `../` header naming an EXISTING outside file, U4b via three legitimate `..`-inside-a-segment entries, N10 via "no such file under", U8 via a per-shape reason table (the coder's first assert was satisfied by the message's tail and tightened), N4b via a backtick-wrapped quoted entry, V6 via `(\S*)` failing a valueless header closed (box-wide fire rate unchanged), V7 via `./` classified before the empty-drop; the paired-quote strip kept as a graded cheap tolerance; 434 passed across six suites, 68 + 0 skipped; all three harnesses dead)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 7.
### Adjudication (pool layer) — a three-deepseek draw
- deepseek v4-flash (1) — 1 raised: a hand-trace claiming the star-first branch "consumes" a literal `*` rather than matching it as a wildcard — the differential fuzz (304,348 pairs against an independent reference, 0 mismatches, incl. exhaustive `{a,b,*,?}^≤4` on both sides) settles the semantics; put to the native finder only for the record.
- deepseek v3.2-exp — CLEAN (rows, DO-NOTs, Touches, the port, the predicate, fail-closed paths).
- deepseek v4-flash (2) — CLEAN (the seven round-6 items located; the refusal reasons; the parity port; byte-identity vs base).
### Native finder (opus) — executed: 43 mutants re-anchored on a copy, 42 killed (every named class incl. V6/V7a/V7b; a stale `__pycache__` that would have false-survived every mutant caught by a sentinel mutation first); the V6 valueless-header shapes each exactly one ERROR, no traceback; the U8 reason table 8 of 8 through the CLI with 0 accusations and the `+N more` truncation; U4b's three legitimate `..` segments silent and a genuine `src/../x/**` refused; B4b's existing outside file "is unusable"; N10's message; N4b's five quote/backtick forms; 15 look-alikes 0 refused; the fuzz on a new seed 304,348 pairs clean; backtracking 0.01–0.03 ms at 24 stars / 24 chained `**`; the nine-symbol parity collected and passing; the `cast` sound; the advisory downgrade on lock-only dirs; base vs branch 0 divergences of 17 non-duplicate plan-set dirs (the earlier "69" not reproducible under any of its three enumerations — its bound stated); 68 + 381 green (the "six suites" reduced to the module's five reverse dependencies, bound stated); ruff/format/mypy clean; two files; stdlib + the two guarded sibling imports. 3 raised:
- [M] round 6's `(\S*)` widening admits `""` as a first-class token in the two-header set (:1312-1322): a well-formed spine plus any valueless `Epic:`-shaped prose line reads as "2 different Epic: headers (, …)", the early return skips containment, the hint misleads — a regression at the class (0 of 70,598 `*.md` box-wide today; a live near-miss in youtube's RESILIENCE.md) → FIXUP (1): count only non-empty tokens, fall through to the unusable arm when none survives.
- [L] the same set counts `X` and `X/` as two epics (other forms dedup) → FIXUP (2). [L] `:1378` `if str(o).strip()` drops a BLANK block-list item → the "carries no owned_paths" misreport the comment claims closed; the sole surviving mutant (V7c) → FIXUP (3): collapse only the scalar case.
Round 7 verdict: 3 raised → 3 routed (1 M, 2 L) — one a regression from the prior round's own fix; pool three-deepseek clean. Not the no-op round.

## Round 8 — over `2f982a5f..d878138e` (the round-7 fixup d878138e, 26/12 + 76/0: the two-header arm on non-empty tokens with the only-valueless spine still one ERROR (V6b dead); trailing-slash dedup (S2 dead); a blank scalar is no declaration and a blank block item is named as unusable while the four empty declarations still report correctly (V7c/V7d dead, parametrized); 441 across six suites, 75 + 0 skipped; a harness anchor-uniqueness assertion caught a stale anchor rather than reporting a false kill)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 8.
### Adjudication (pool layer) — a three-deepseek draw
- deepseek v4-flash (1) — CLEAN (5 rows; DO-NOTs; the predicate; containment refusals; 7 predicate tests, 20 fixtures, 18 integration tests).
- deepseek v3.2-exp — CLEAN (the round-8 ledger's every edge).
- deepseek v4-flash (2) — 1 raised: "when the epic is missing or has no owned_paths the early return skips the File-Scope link — a spine could widen its lock past the epic" — REFUTED: each of those early returns EMITS an ERROR ("no such file under", "carries no owned_paths", "is unusable"), which reds the gate; containment cannot run without the epic's paths and says so — fail CLOSED, not open (the plan's own spine carries no lock-widening path that bypasses an ERROR). Put to the native finder for the record.
### Native layer (opus×1, by execution — 46 mutants re-anchored at d878138e on a FULL repo copy with a green baseline first; the r5–r7 mutrepo stub's every "kill" had been a ModuleNotFoundError collection error — rebuilt, re-inoded, backup asserted before/after)
- **[M] 1 — the round-7 fix's ORDERING half is ungraded:** mutant V6c (`epic_rel = tokens[0]` for `real[0]` at `scripts/enforcement/check_plan_tickets.py:1332`) SURVIVES the suite (74 passed) — a valueless `Epic:`-shaped line FIRST and the real header second makes `tokens[0]` `""`, so the unusable-path ERROR fires and containment never runs: the shadow fail-open r7 closed, reached from the other ordering. Live code correct (probe `L1b valueless FIRST then real` rc=0); `tests/enforcement/test_plan_tickets_epic_scope.py:783` exercises real-first only. FIXUP: parametrize over both orderings.
- **[M] 2 — the unusable-path ERROR at `:1342` interpolates `raw_headers[0]` while the adjudicated token is `epic_rel = real[0]`:** with a valueless line first and an absolute/`..`/glob/residue token second the message reads `path '' is unusable` (M1–M4 reproduced; the control without the valueless line names `/etc/passwd`). Still hard-red; the author is sent to the wrong line — the class `_unusable_owned`'s first arm (`:665-667`) already fixed. FIXUP: `(epic_rel or raw_headers[0]).strip()` + a valueless-first fixture on `test_out_of_repo_epic_header_errors`.
- [L] 3 — whole-line emphasis `**Epic: <path>**` hard-reds (the value group's leading `\*{0,2}` has no tail twin; `…1-x.md**` trips the glob guard at `:1338`); fire rate 0 of 638 plan-tree `.md` files; the emitter teaches the unbolded form — judgment call routed (anchor the tail or document beside `:274-282`). [L] 4 — `:940` says "17-fixture contract"; `len(FM_FIXTURES) == 20` (the `:842` comment says 20). FIXUP 17 → 20.
- Verified clean by execution: 75 passed (worktree); the 5 suites referencing the module → 388 passed (the r6 "441 across six" is not reproducible from that bound — no sixth suite references it); 45/46 mutants killed incl. the r7-surface graders V6b/S2/V7c/V7d and a live-harness sentinel; ledger (1) two-header arm in both orders + only-valueless + shadow case; ledger (2) `rstrip("/")` three shapes; ledger (3) blank owned_paths seven shapes; `_unusable_owned` through the CLI 9 shapes 0 accusations; 12 prefix forms parse and containment RUNS, prose `Epic: this plan…` fails closed; differential fuzz seed 8080 304,348 pairs 0 mismatches; nine-symbol parity + fixtures + base identity 22 passed no skips; base-vs-branch over 31 plan-set dirs 0 divergences (the only `Epic:` header on the box is the archived monolith, refused by `PLAN_DIR_NAME_RE`); fire-rate comment bounds re-measured (1 of 994; 45 owned_paths values across 10 epic files; 8 bracket tokens across 7 repos; "1,007 tokens in 15 sets" measures 978 in 14 today — population drift, not ratio); regex backtracking 6 adversarial shapes 0.0000s; ruff/format clean; mypy zero delta; `check_script_headers` rc 0; stdlib only.
### Verdict
**4 findings (2 M + 2 L) → FIXUP round 8 routed to the coder.** Ledger: two-header arm ordering · error-message attribution · emphasis tolerance · fixture denominators · the r7 classes (V6b, S2, blank owned_paths — all clean) · parity/base identity (clean) · fuzz (clean).


## Round 9 — over `2f982a5f..ceda0adc` (the round-8 fixup d878138e..ceda0adc: check_plan_tickets.py 18/3, the epic-scope tests 54/10 — the valueless-line shadow test parametrized over `real-first`/`valueless-first` (mutant V6c killed by two tests); the unusable-path ERROR names the adjudicated token `(epic_rel or raw_headers[0]).strip()` + the `absolute-after-a-valueless-line` param (mutant M2 killed); whole-line emphasis read by peeling the tail with a LOOKAHEAD `(\S*?)\*{0,2}(?=[^\S\n]|$)` — the suggested end-anchor measured and REJECTED (it stopped matching the one live prose-tailed header on the box and the prose form: silent non-parses), 0 mismatches over 17 forms, two graders (mutant E3 killed); "17-fixture" → "20-fixture"; 81 passed (75 → 81), the five suites 394; prior harnesses re-run glob-set 6/6 · r5 6/6 · r6 7/7 (re-anchored — its V6 anchor was the pre-round-8 group) · r7 4/4)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own execution (the fixups were prescribed verbatim from round 8's executed findings; the one new mechanism — the lookahead peel — probed by the orchestrator below; stated) — round 9.
### Orchestrator execution (in the worktree)
- `pytest tests/enforcement/test_plan_tickets_epic_scope.py -q` → 81 passed; the five suites referencing the module → 394 passed; `ruff check` clean, `ruff format --check` 2 files already formatted; the `# fmt: off/on` region intact (3 marker lines); the message at `scripts/enforcement/check_plan_tickets.py:1357` reads `(epic_rel or raw_headers[0]).strip()`; the params at `tests/enforcement/test_plan_tickets_epic_scope.py:498` (`absolute-after-a-valueless-line`) and `:792` (`real-first`/`valueless-first`); `:984` says 20-fixture.
- Lookahead probe through `EPIC_HEADER_RE` (module loaded with `sys.modules` registered): `Epic: docs/**/x.md` → `docs/**/x.md` (interior stars intact); `**Epic: …1-x.md**`, `*Epic: …1-x.md*`, `**Epic:** …1-x.md`, the live prose-tailed form → `docs/development/epics/1-x.md`; `…1-x.md**extra` → whole token; **`Epic: epics/*` → `epics/`** — the trailing star is peeled as emphasis before the glob guard sees it (UPHELD below).
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0416)
- All three raise the same class: the lookahead peel `(\S*?)\*{0,2}(?=[^\S\n]|$)` eats a TRAILING `*`/`**` as closing emphasis before the glob guard — `Epic: epics/*` → `epics/` (the guard's "unusable — glob" never fires; containment refuses for the wrong reason, round 8's wrong-line class), and `Epic: …1-x.md*` → `…1-x.md` (a truncated path naming a real file is silently ACCEPTED — a fail-open shape). UPHELD by the orchestrator's execution. v3.2-exp additionally claims interior `**` truncates (`docs/**/x.md` → `docs/`) — REFUTED by execution (whole token). Everything else CLEAN: both orderings, the adjudicated-token message, the two emphasis graders, the 20 fixtures, the `_carved_out` extraction, 81 / 394, ruff/format, the `# fmt: off` region.
### Verdict
**1 finding (M) → FIXUP round 9 routed to the coder** (symmetric-emphasis peel via a backreference, or guard on the raw value before the peel; graders for `epics/*` and `…md*` naming the ORIGINAL token). Ledger: emphasis tolerance (the peel's mirror — trailing glob chars) · ordering arms (clean) · message attribution (clean) · fixture denominators (clean).

## Round 10 — over `2f982a5f..5549a1c4` (the round-9 fixup ceda0adc..5549a1c4: check_plan_tickets.py 23/12, the epic-scope tests 15/0 — the emphasis peel made SYMMETRIC: `(?P<em>\*{0,2})Epic … (?P<path>\S*?)(?:(?P=em))?(?=[^\S\n]|$)` — a tail run is removed only when the same run opened the line; the call site reads `m.group("path")` via `finditer` (a capturing group now exists, so `findall` would return tuples — documented); option (b) rejected as two tokens threaded through dedupe → real → adjudication; the coder's 22-form probe found a THIRD shape (`Epic: docs/x/**` → `docs/x/`) — all three red-first then green; the table 0 of 22 (the r8 lookahead 3, the end-anchor 2); mutant E4 (unpaired peel) killed by exactly the three new params; 84 passed (81 → 84); the five suites 397; two harness anchors re-anchored to the named group)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native: the orchestrator's own execution (one prescribed item; the new regex probed below; stated) — round 10.
### Orchestrator execution (in the worktree)
- `pytest tests/enforcement/test_plan_tickets_epic_scope.py -q` → 84 passed; the five suites → 397 passed; `ruff check` clean, `ruff format --check` 2 files already formatted. `EPIC_HEADER_RE` probed through `finditer(...).group("path")`: `Epic: docs/**/x.md` → whole; **`Epic: epics/*` → `epics/*`**, **`Epic: docs/x/**` → `docs/x/**`**, **`Epic: …1-x.md*` → `…1-x.md*`** (the glob guard now sees every trailing star); `**Epic: …**` / `*Epic: …*` / `**Epic:** …` / the live prose-tailed form → the bare path; the asymmetric `**Epic: …1-x.md*` → `…1-x.md*` (unpaired tail kept — refused loudly by the guard); `- **Epic:**` → `''`.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0149)
- Two CLEAN (the empty-`em` backreference is a no-op under Python `re` — confirmed; every non-parse or unusable shape errors, never silent; the 22-form table; the 397). One raised `*Epic: p**` — the opener `*` peels ONE of the two trailing stars, so the token is `p*` and the guard's message names `p*` rather than `p**`. ADJUDICATED: not a fail-open (the token still carries a `*`, so the glob guard refuses loudly — the reader's "no such file" claim is wrong: the guard fires first); a message imprecision on an input that is both emphasised AND a glob; 0 of the live population (1 header on the box, unbolded, no glob). REJECTED per FIX DIRECTIVE 5.
### Verdict
**0 findings — no-op round.** Ledger (this ticket's classes across ten rounds): two-header arm ordering · message attribution · emphasis tolerance (paired peel) · trailing-glob characters · fixture denominators · blank owned_paths · glob subsumption · parity/base identity · fuzz — all swept, all clean. **Status: CONVERGED** at `5549a1c4`; merge with the fleet sync (`scripts/enforcement/` is a trigger surface); INDEX row for the new test file + CHANGELOG are the merge owner's.
