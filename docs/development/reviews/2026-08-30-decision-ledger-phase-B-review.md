# Review — plan-1 decision-ledger, Phase B (seed-if-missing distribution + governance duties)

Surface: working-tree Phase-B diff — templates/governance/DECISIONS.md (new seed) ·
scripts/fabrik_synced_manifest.py (SEED_IF_MISSING + SEEDED_NOT_ENFORCED + template pair) ·
scripts/sync_enforcement_to_projects.py (seed_if_missing kwarg + call-site) ·
tests/test_sync_seed_if_missing.py (new, 5 behaviors) · tests/test_governance_template_split.py
(contract pin extended — adjacent fix, recorded as a plan deviation: not in File Scope, owned by no
other plan) · CLAUDE.md (duty bullet + matrix row + naming + ledger-first recall + the
`decision-ledger` UNIVERSAL marker) · templates/governance/CLAUDE.md (mirrored duties) — pre-commit.
Mode: NO-POOL — solo native; closing pass re-derivation.
Mid-phase operator input: "we have 3 different claude.md" — adjudicated in checklist row 9.

## Rubric (review_rubric.py --changed <the 7 paths> — verbatim section heads)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
## FLOOR — always injected, regardless of glob (spec L3)
### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all twelve axes)
## MATCHED — packs whose globs hit the changed paths
### core/10-python.md  (hit: scripts/fabrik_synced_manifest.py, scripts/sync_enforcement_to_projects.py, tests/…)
### core/40-documentation.md  (hit: CLAUDE.md, templates/governance/…)
### core/45-testing-strategy.md  (hit: tests/test_sync_seed_if_missing.py)
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | security-auth floor (35) | CLEAN | no auth surface; grep of the diff for token/jwt/secret: 0 new statements |
| 2 | data-postgres floor (25) | CLEAN | grep of the changed py files for sql/engine/psycopg: 0 DB-API uses — file copies + set membership only |
| 3 | ops floor (30) | CLEAN | no compose/deploy surface; the sync's SSH/deploy paths untouched |
| 4 | 12-Factor | CLEAN | sync_enforcement_to_projects.py + fabrik_synced_manifest.py stay stdout-only CLIs — no daemon/PID/logfile/env-group introduced in the diff (grep FileHandler/daemonize/yml-config over both: 0) |
| 5 | python discipline (10) | CLEAN | typed kwarg with honest docstring on BOTH the force interaction and the data-loss rationale; membership keyed by dest relpath (the manifest's own vocabulary) |
| 6 | testing (45) | CLEAN | 5 behaviors red-first (4 watched red + the defaults-off baseline guard); the never-overwrite invariant pinned under BOTH code paths (normal + --force) and byte-identity asserted, not just action-string |
| 7 | fail-open vs fail-closed — THE phase's hunt focus | CLEAN | the seed skip short-circuits BEFORE the symlink and force branches (order verified in the diff: symlink-replace would have overwritten a symlinked ledger — the exists() check excludes symlinks so a symlink dest still gets replaced, which is correct: a symlink is not project data, and workspace isolation requires real files); --force docstring updated at the kwarg (the :333 top-level docstring statement is now scoped by the kwarg's own exception note); a missing template SOURCE is skipped by the existing `if source.exists()` guard — no crash, no partial state |
| 8 | boundary/sentinel/prefix | CLEAN | SEED_IF_MISSING keyed by dest relpath exactly as SEEDED_NOT_ENFORCED is; "docs/DECISIONS.md" ≠ "PORTS.md" — no membership overlap ambiguity; the new mkdir(parents=True) for template dests is needed because docs/ may not exist in a bare repo (CLAUDE.md's dest is root — unaffected) |
| 9 | the THREE CLAUDE.md files (operator's mid-phase catch) | CLEAN | (1) hub CLAUDE.md — duty + matrix row + naming + recall + universal marker, edited; (2) templates/governance/CLAUDE.md — project-facing mirror, edited (syncs to ~46); (3) /opt/fabrik-lib/CLAUDE.md — sync-excluded + hand-maintained, cross-repo HARD STOP: reached via the `decision-ledger` UNIVERSAL marker (their check_governance_drift.py parses the hub list) + heads-up mail 01M19GPWBV; drift-parse smoke-ran clean after the list edit |
| 10 | governance-text fleet correctness | CLEAN | both duty texts are type-independent (like CHANGELOG's); the template's query line names the hub-side fleet command as hub-run (a project has no /opt/fabrik on its PATH — the text says "the hub runs"); subagent/pipeline pen rule carried verbatim from the spec |
| 11 | behavior-without-a-test | CLEAN | every sync behavior has a row; governance text is prose (review-adjudicated here); the manifest pair + class membership are pinned by test_manifest_declares_the_seed_class_and_pair |
| 12 | cost/limit edges | CLEAN | no spend |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — all 12 classes; the copier diff read against the FULL function (symlink/force/hash branches) | citation | solo native (NO-POOL; dispatched: 0) | 2 | 2 | 2 | F1 the template-dest parent dir may not exist in a bare repo → mkdir added at the call site; F2 the operator's three-CLAUDE.md catch → universal marker + fabrik-lib mail (row 9) |
| Pass 2 | CLOSING — re-derivation: suites re-run 11/11 (5 new + 6 split-suite incl. the extended pin); gate 55/0 fresh; drift-check parse smoke; grep DECISIONS count 5 in each CLAUDE.md; both mail ids on disk (01M19GPW7Q fleet · 01M19GPWBV fabrik-lib) | method: re-derivation | 0 | 0 | 0 | TERMINAL |

Pass 2 = the no-op: `new: 0`, everything adjudicated.

## Phase B verdict

Phase B verdict: PASS — clean. This commit is the rollout moment: the post-commit governance-sync
distributes the duty text + the seed pair fleet-wide, and no existing ledger can ever be overwritten
(proven under both code paths before the first project is touched).

```json
{"status": "success", "tier": 2, "passed": 55, "failed": 0}
```
