# Acceptance review — T05b (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** IN-PROGRESS

**Surface:** the coder's worktree branch diff against the dispatch base 6a5c5990 — see the round sections below (one file per ticket, rounds APPENDED).

## Round 1 — over the merge-base..fdb373a9 diff (scripts/final_gate.py +41, the legacy epic +14/0 frontmatter only, tests/enforcement/test_final_gate_epic_order.py +148, docs/workflows/FINAL_GATE_WORKFLOW.md +6/−1; 6 tests all seen red first; the tier pin mutant-proven; hub `epic_order.py --check` PASS)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.
### Adjudication (pool layer)
- deepseek — CLEAN (5 rows; DO-NOTs; 4 Touches; ` (N/A` collides with no row name in `scripts/final_gate.py` or `scripts/enforcement/*.py`; no fail-open path; no row at all in a project without the script; never at `--lean`/`--systemic`).
- gemini — CLEAN in substance; two items put to the native finder: `_epic_order_row` calls `run_cmd` with default parameters — is there a timeout (a hanging `epic_order.py` would hang the gate); stdout warnings at rc 0 are dropped (by contract: one finding must FAIL, so rc is the signal).
- qwen — 2 raised: the ` (N/A` marker is a substring match with no audit proving no other row name carries `(N/A` (deepseek's audit says none; the native finder greps every name); `test_hub_epics_dir_passes` calls `_epic_order_row()` directly rather than the subprocess gate, so "the hub is green" rests on the row alone while the worktree's full gate reports `Doc Link Integrity` red on 8 gitignored-file refs — UPHELD as a question: the native finder runs the doc-link check from the MAIN checkout to settle worktree-artifact vs real red.
### Native finder (opus) — executed: tier membership by the real subprocess gate (`--lean` 31 passed no epic row; default tier 2 the row in `failures`; `--systemic` none); the failure text reaches `failures[].output`; the N/A path lands in `skipped_checks` + `warnings`; the ` (N/A` marker name-scoped (`marker in name` over row names only; 9 `N/A` hits box-wide, none a row name; no dynamic row names); 0 of 49 project `scripts/` dirs carry `epic_order.py` and a broken epics dir without the script yields no row; `run_cmd` default 120 s (0.07 s measured), fail-closed on expiry; the rerun hint non-mutating; T03b's branch script keeps the hub green with epic 3 (`owned_paths: []`) in phase 1; the legacy body byte-identical (md5 42f3b8f4…), field order exact; the four Touches only, `epic_order.py` untouched; the doc's four finding classes map to real `findings.append` sites; ruff/format clean; mypy zero delta; 23 passed across four suites; `check_script_headers` OK; the worktree gate's `Doc Link Integrity` red is a WORKTREE ARTIFACT — 0 broken of 2,290 refs from the main checkout; the commit provenance is T05b's (the shared-scratchpad overwrite did not contaminate it). 4 raised, none in the gate code:
- [M] the doc's arithmetic: "35 checks total" beside "17 where the script exists" → 36 on the hub (measured by the doc's own stub method: tier 2 47 → 48 rows; `tier 2 → 38` was already wrong at base) → FIXUP (1). [M] the production file's own comment (:318) says a new skip shape MUST be added to `test_final_gate_advisory_display.py`'s pin; `EPIC_ORDER_NA` is absent and the test still passes — the silent gap its docstring names; outside T05b's Touches, unclaimed by any ticket → ORCHESTRATOR D2 fixup at merge (`fixup_T05b.py` written).
- [L] the list header "all … in `scripts/enforcement/`" falsified by the new path entry → FIXUP (2). [L] the inserted clause leaves the original parenthetical dangling → FIXUP (3).
Round 1 verdict: 4 raised → 3 routed (doc) + 1 orchestrator fixup; pool 3/3 CLEAN in substance (qwen's two questions settled: the marker audit; the hub-green claim proven from the main checkout). Not the no-op round.
