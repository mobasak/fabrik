# Acceptance review — T13 (plan set 2026-09-03-plan-1-multi-agent-per-repo)

**Status:** IN-PROGRESS

**Surface:** the coder's worktree branch (worktree-agent-aca08762aa41073a1, head 507df6ac) against its merge base 9c35928f — `scripts/wip_backup.sh` +79/−2, `tests/test_wip_backup.py` +118. Coder: native Sonnet worktree (the plan's Execution Discipline said pool for `complex`; the D6 record from T03a's rejected pool attempt made native the coding lane for every ticket); 9 passed (5 + 4); rows 1, 3, 4 red-first; row 2 honestly flagged green-on-both; shellcheck absent on the box, `bash -n` clean.

## Round 1
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 1.

### Native finder (opus) — verified: placement (worktree loop :99-114 above the main-tree `|| continue` at :117), 2 deletions both replaced by the prune widening, `KEEP_DAYS=7` and the main recipe untouched, index isolation proven (a file staged only in the worktree lands in the wt ref; a main-tree-only edit does not; the real worktree index/HEAD/stash unchanged). 10 raised:
- [H] `locked` worktrees are SKIPPED, and on this box `locked` means an agent is RUNNING (`locked claude agent agent-… (pid …)` on two live hub worktrees, one dirty) — the feature skips its own primary case; the recipe on a locked scratch worktree succeeds end to end. The ticket text itself prescribes the skip → ORCHESTRATOR DECISION (reversible; minted as a ledger row at merge): snapshot locked worktrees; skip only clean/missing/unenterable → FIXUP (1).
- [H] no dedup: 3 runs on an unchanged dirty worktree → 3 refs, 1 tree; at `*/15` = 96 refs/day/worktree; census 22 dirty `.claude/worktrees` worktrees across /opt (9 in the hub) → ~14,784 steady-state refs against 440 today → FIXUP (2): rolling `refs/wip/wt-<name>` + dated refs only on a tree change, mirroring `autobackup` + `bak-*`.
- [M] `git update-ref` exit unchecked; a basename git rejects (a space) logs "snapshotted" with no ref → FIXUP (3). [M] an unenterable (chmod 000) worktree is dropped with zero log lines → FIXUP (4). [M] the scope guard is a textual prefix; under a symlinked ROOT every worktree vanishes silently → FIXUP (5).
- [M] the `.claude/worktrees/` restriction leaves `/opt/youtube/.kilo/worktrees/frost-nightshade` (2,718 dirty paths, excluded from the main snapshot too) and the `.tmp/subagents/agent-*` worktrees uncovered → ORCHESTRATOR DECISION (reversible; ledger row at merge): any linked worktree under `$repo/` → FIXUP (6).
- [M] wt snapshots never leave the box (only `autobackup` is pushed, :146-150; the script header promises a push) → FIXUP (7): push the rolling refs like `autobackup`.
- [L] the prune's `${ref##*-}` deletes `wt-2020` blindly (tail sorts before the cutoff) → FIXUP (8): timestamp shape check; rolling refs exempt. [L] row-2 test passes with the feature absent (3 failed, 6 passed on the base script) → FIXUP (9): a dirty sibling in the same repo. [L] nothing enforces the precondition that the main tree ignores nested worktrees (hub: `.git/info/exclude:11`, not `.gitignore`; projects: T01a's synced line) — the fixture's `info/exclude` and the fleet's `.gitignore` line are equivalent for the snapshot → RECORDED, routed to T15's doc.
### Adjudication (pool layer)
- gemini — CLEAN (placement :100 above the main-tree check; isolated index in a subshell; `${ref##*-}` parse; skip paths; tests assert refs/content; quoting present; one [L] remark on `exit 0` inside the subshell vs `return 0` outside — consistent bash, no defect).
- qwen — CLEAN on placement, prune parse, malformed-ref `continue`, missing-dir log, locked skip (the last now overridden by decision (1)).
- deepseek — 1 raised: "the scope guard is built from the trailing-slash loop variable, so the pattern is `repo//.claude/worktrees/*` and no worktree is ever snapshotted" — REFUTED by execution: the suite's dirty-worktree test creates the ref (9 passed on the worktree; the native finder proved the ref's tree content by `cat-file`), so the guard matches; the textual-prefix fragility it points at is the native finder's finding (5), fixed by normalising both sides.
Round 1 verdict: 10 raised → 9 fix classes routed (2 H, 5 M, 2 L), 1 recorded/routed; pool 0 beyond the native set (1 refuted). Not the no-op round.

## Round 2 — over `9c35928f..666343d8` (23,819 B; the round-1 fixup: nine classes, 18 passed)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 2.
### Adjudication (pool layer)
- gemini — CLEAN (realpath normalisation; the tree-dedup; sanitisation + captured update-ref errors; the rolling ref pushed; the prune's explicit digit classes).
- qwen — 4 raised: the sanitised-name COLLISION (`a b`/`a-b` → one rolling ref) — CONFIRMED (also deepseek's) → FIXUP; one push per worktree per run — measured by the native finder: the dedup gate precedes the push, so only changed worktrees push (a one-off 23 × ~1.9 s first run) → folded into the fixup as a single batched push per repo; the `{8}` prune pattern — NOT a defect (explicit digit classes); the dangling-ref dedup — self-heals (the native finder proved it) → made explicit in the fixup.
- deepseek — the collision (same).
### Native finder (opus) — all 9 round-1 fixes FIXED by execution; the main-tree path byte-identical (39 lines) except the mandated widening; 13 of 13 T13 tests red against the base script. 5 raised + 1 out of scope:
- [M] the basename collision (sanitisation AND identical basenames under different parents — `.claude/worktrees/beta` vs `.tmp/subagents/beta` → one ref, the first worktree's WIP unrecoverable, both log "snapshotted"; 0 duplicates among 112 live worktrees today) → FIXUP (1: `<name>-<8 hex of sha1(realpath)>`).
- [M] four failure paths drop a dirty worktree with NO log line (unborn HEAD; one chmod-000 FILE loses the readable files too) → FIXUP (2). [M] the rolling ref is IMMORTAL for a removed worktree (locally and on origin; 17 live vs 30 ever-created hub worktrees) → FIXUP (3: a reaper). [L] `mktemp` failure leaks stderr and drops the worktree silently → FIXUP (4). [L] the isolated-index invariant unguarded on the worktree path → FIXUP (5: a test).
- [L] out of scope, pre-existing: `/opt/fabrik-lib{,-account,-review}` are one repo visited three times by the outer loop, each overwriting the shared autobackup → recorded for the backlog.
- Incident: a leaked `/tmp/wip-index-rn-kit-sandbox.*` found mid-review — the coder's runs were all under a scratch ROOT; the leak was the box's PRODUCTION cron (master's unfixed script, PID observed holding the lock) — evidence FOR the class, fixed by the trap in the fixup.
Round 2 verdict: 5 native + 3 pool → FIXUP routed (one batch, landed at 6feccf4e + 901fcb4b, 28 passed). Not the no-op round.

## Round 3 — over `9c35928f..901fcb4b` (55,086 B; 28 passed)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 3.
### Adjudication (pool layer)
- gemini — CLEAN (the reaper spares an unmounted-but-listed worktree via `wt_live_file` :77; the push file's lifetime under the trap; `--ignore-errors` captured as a "partial add" warning; realpath on both sides; quoting; `bash -n`).
- qwen — CLEAN (17 new tests; all failure paths log once; id8 collision prevention; batched push; traps).
- deepseek — 1 raised: when `mktemp` for `wt_live_file` fails the script falls back to `/dev/null`, so the reaper sees no worktree as live and could delete every rolling ref → carried to the native finder (its brief covers the reaper's false-orphan path; the quoted guard `[ "$wt_live_file" != "/dev/null" ] &&` suggests the reaper is skipped in that case — to be settled by execution).
### Native finder (opus) — 7 of 8 round-2/3 fixes VERIFIED by execution (collision ids; one batched push — `GIT_TRACE` shows 2 pushes: the worktree refspecs + autobackup; the dangling ref; the temp index gone on every path incl. a genuine SIGTERM mid-`git add` on a 95 MB tree, red-on-revert against master; one line per failure path; `--ignore-errors` cannot yield an empty tree — the index is seeded from HEAD; the reaper's happy path and the false-orphan probe (a prunable-but-listed worktree is recorded live before the `-d` check); the isolated-index invariant; the incident: the production cron runs master's script, byte-identical, with NO trap — the class confirmed and reproduced; no residue on the box now). 4 raised:
- [H] the reaper FAILS OPEN on an empty live-ids set — a linked worktree at ROOT level (`/opt/fabrik-lib-account`, `/opt/fabrik-lib-review` today, 2 of 57; fabrik-lib carries 7 live agent worktrees) enters the outer loop as a "repo" with no worktree beneath it, or `mktemp` fails for the live file → every `wt-<name>-<id8>` rolling ref of the SHARED store deleted locally and on origin (proven twice: a still-registered dirty worktree lost its only snapshot) → FIXUP (1): fail closed (empty/unavailable list → skip; never reap from a gitdir-file "repo").
- [H] the live-ids guard has zero coverage (the three-line guard deleted → 28 passed) → FIXUP (2).
- [M] the MAIN-tree `git add -A` has the same one-unreadable-file class, silently (no log line, no snapshot) — the ticket freezes main-tree semantics → RECORDED for the backlog (class completion after the plan).
- [L] the partial-add line embeds git's multi-line stderr → FIXUP (3).
Round 3 verdict: 4 raised → 3 fix classes routed (2 H, 1 L), 1 recorded; pool: deepseek's mktemp item CONFIRMED as trigger B. Not the no-op round.

## Round 4 — over `9c35928f..866c593a` (65,647 B; the round-3 fixup 866c593a: the reaper fails CLOSED on an empty/unavailable live-ids set and never reaps from a gitdir-file "repo"; the guard covered; the partial-add line single-line; 31 passed)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 4.
### Adjudication (pool layer)
- gemini — CLEAN (31 passed; the fail-closed reaper + the top-level-"repo" skip, the isolated-index invariant, push batching, collision ids, the widened scope; its three numbered items are confirmations — the accepted cost, the top-level worktree's own autobackup snapshot being correct under the shared ref-store, the lazily-quoted trap verified by `test_main_tree_temp_index_is_cleaned_up_on_signalled_interruption`).
- qwen — CLEAN (4/4 contract rows implemented and tested; DO-NOTs; Touches; the worktree snapshot placed above the main-tree dirty check).
- deepseek — 1 raised: a repo whose LAST linked worktree is removed keeps its orphan rolling ref pinned (the live-ids file is empty → reaper skipped) — this is the cost the round-3 fixup ACCEPTED by design (fail closed over greedy), stated in the script's own skip line; not a new class. Whether an enumeration that itself proves emptiness (`git worktree list` returning only the main tree, vs `mktemp` failing) may reap is put to the native finder.
### Native finder (opus) — the round-3 ledger re-verified CLEAN by execution in scratch repos (triggers A and B, the partial-add line with `chmod 000` files, collision ids, one batched push via a PATH shim, decoys, false-orphan, a dangling rolling ref, `--ignore-errors`, the isolated-index invariant incl. gitignored-tracked content, the porcelain parser on git 2.43, quoting; 31 passed; `bash -n` clean). 7 raised:
- [H] the reaper's DELETE scope is the whole SHARED ref store while the SNAPSHOT/live-ids scope is the repo subtree (:63-66 rejects, :84-86 never records) — a main repo reaps the rolling ref of a LIVE, DIRTY worktree nested under its root-level sibling worktree, locally and on origin, logging "(worktree removed)" (reproduced: `aa-sidecar` sorting before `parent`, nested dirty `beta` → `wt-beta` gone both sides while `?? dirty.txt` is live); live shape: /opt/fabrik-lib + fabrik-lib-account/-review → FIXUP (1): record EVERY enumerated worktree in the live file.
- [M] the round-4 `repo_is_worktree` guard is UNGRADED — deleted, 31 still green (the empty-live-file branch masks it; the fixture's sidecar has no nested worktree) → FIXUP (3). [M] the accepted cost is permanent (3 runs after removal: ref pinned locally and on origin) and the orchestrator's alternative — a positive enumeration WITNESS (bare emptiness is NOT proof: a non-repo yields 0 lines at exit 128; a healthy repo always lists its main worktree) plus "live file usable" — was implemented and probed by the finder: closes the cost, keeps triggers A and B closed, and makes the guard graded as a side effect → FIXUP (2), adopted. [M] the fail-closed advisory fires on the COMMON case: 39 of 45 visited repos have no nested worktree → ~3,744 lines/day against a 1 MB-capped / 256 KB-truncated log, evicting signal within a day → FIXUP (4): the line moves to the genuine anomaly. [M] PRE-EXISTING (298151c0, ancestor of the base): a repo and its top-level linked worktree share `refs/wip/autobackup` + the same-second `bak-<ts>` — last in glob order wins; live: fabrik-lib's autobackup parent is fabrik-lib-review's HEAD, fabrik-lib's 77 dirty files have no off-box copy → ROUTED (orchestrator: post-plan infra fix + backlog row; the :290-291 comment premise corrected in-ticket).
- [L] the header's "every exit path" trap claim overstates — SIGKILL leaves 3 temp files (SIGTERM 0) → FIXUP (5, wording + the `/tmp/wip-*` sweep). [L] the `:351` mktemp `2>/dev/null` + guard is a deviation beyond the mandated widening — verified benign (`GIT_INDEX_FILE=""` does not fall back to the real index) → acknowledged in the commit body.
Round 4 verdict: 7 raised → 5 routed (1 H, 3 M, 1 L) + 1 routed out (pre-existing) + 1 acknowledged; pool: deepseek's cost item RESOLVED by the witness fixup rather than restated. Not the no-op round.

## Round 5 — over `9c35928f..2ae4d5f6` (74,601 B; the round-4 fixup 81fadb02 + 2ae4d5f6: `_wip_record_live_id` records EVERY enumerated worktree so the reaper's delete scope equals its knowledge; the enumeration witness; the `repo_is_worktree` guard graded by a sidecar WITH a nested worktree (the guard now decides WHICH iteration reaps — owner, not sidecar); a plain repo logs nothing from the reaper; the trappable-exit wording; the reaper comment corrected; a self-caught vacuous first test fixed before reporting; 34 passed)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 5.
### Adjudication (pool layer)
- deepseek — CLEAN (4/4 rows; DO-NOTs — main-tree semantics and KEEP_DAYS unchanged; the reaper fails closed on an empty/absent live set, mktemp failures skip; 25 new test functions; `realpath -m` with fallback).
- gemini — CLEAN (rows 1–4 to their tests; `_wip_record_live_id` store-wide enumeration guarded by `test_sibling_nested_worktree_survives_store_wide_live_tracking`; the witness at :317-321/:330; the scope guard at :317; the lazy trap over all four temp files; quoting; collision ids).
- qwen — CLEAN (rows to lines and tests; the isolated-index invariant; temp-file traps; locked worktrees, collisions, the reaper edges).
### Native finder (opus) — the whole ledger re-swept by execution in scratch repos: sidecar sorting after the parent, a prunable-but-listed nested worktree (skipped with its one line, ref survives), two repos in one root, a worktree path with spaces, the witness (removal → reaped on run 2 and gone on origin; mktemp → one line, ref intact; exit 128 with 0 lines → skipped), the `repo_is_worktree` guard graded on OWNER attribution (mutant → the sidecar reaps → red), 0 bytes of reaper output on a clean plain repo and 3 lines over 12 repos (0 at steady state), collision ids, ONE batched push carrying 3 refspecs, decoys and false-orphans spared, the temp index gone on SIGTERM in the WORKTREE subshell, quoting, `bash -n`, 34 passed; the main-tree path proven equivalent to base on identical fixtures (same tree, commit, refs, log line) with its three declared line changes; a batched push mixing an update with a delete of an absent ref REFUTED as a failure (exit 0). 5 raised:
- [M] the reaper still fails OPEN on an INCOMPLETE enumeration: `:298` pipes the porcelain so its exit status is discarded and the witness counts lines SEEN — with real git 2.43, `chmod 000 .git/worktrees/beta` silently omits beta at exit 0 → run 2 reaps `wt-beta` locally and on origin while beta is on disk and dirty (also a shim exiting 128 after a partial list) → FIXUP (1): capture to a file, honour the exit status, gate on `worktree` lines == 1 + `.git/worktrees` entries.
- [M] `:290-291` a failed mktemp for the PUSH QUEUE falls back to `/dev/null` → every worktree push silently dropped, zero log lines (the sibling paths log) → FIXUP (2). [M] `docs/workstation/wip-backup-safety-net.md` documents only the main-tree namespace and recovery; the ticket's Docs row points at T15, which does not own it (0 hits in 34 files) → FIXUP (3, a third file; T16 records the mis-pointer). [M] the `:35` "sweep /tmp/wip-*" advice matches only the live flock lock and the cron log (0 stray files) → FIXUP (4): name `/tmp/wip-index-*` and `/tmp/wip-wt-*`. [L] main-tree log lines print `$repo`, worktree lines `$repo_real` → FIXUP (5, log text only).
Round 5 verdict: 5 raised → 5 routed (4 M, 1 L); pool 3/3 CLEAN missed the truncated-enumeration class — the native layer found it with a real-git reproduction. Not the no-op round.

## Round 6 — over `9c35928f..3d9c5b2c` (89,056 B; 3 files — `docs/workstation/wip-backup-safety-net.md` declared as the third; the round-5 fixup: the enumeration captured with git's exit status and a completeness check (real-git chmod-000 fixture → "enumeration incomplete: 1 of 2", ref survives; the exit-128 shim → "worktree list exited non-zero"), the push-queue mktemp failure logged and the guard testing `!= /dev/null`, the doc's worktree namespace + recovery + expiry + SIGKILL sweep, the header's two exact prefixes, `$repo_real` in main-tree log text; a first exit-128 fixture that passed on the baseline for the wrong reason (dated ref not aged) self-caught and fixed; 37 passed)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 6.
### Adjudication (pool layer) — the first launch died at `model_start` on the same network blip as T05a's; relaunched
- deepseek — 2 raised: "the doc is outside the Touches" — UPHELD on the letter, ORCHESTRATOR-DIRECTED (round 5 routed the wip-net doc into T13 under the Doc Sync Matrix floor because the ticket's Docs row pointed at T15, which does not own it; declared in the coder's commit body; T16 records the mis-pointer); "row 2's 'main snapshot byte-identical to a run without the worktree' has no test asserting the main snapshot" — a coverage claim put to the native finder (round 5 proved base-vs-branch identical trees/commits/refs on the main-tree path by execution; whether a TEST pins it is the question).
- gemini — CLEAN (the completeness guard :343-360 incl. the absent-vs-unreadable admin dir; the capture + `wt_enum_ok`; the push-queue guard `!= /dev/null` with one line; all five temp files in the top-level trap; the doc's namespace, recovery and sweep note; the two new fail-closed tests named).
- qwen — CLEAN (rows 1–4 by line and test; 22 new tests; the five-temp-file SIGTERM cleanup; symlinked roots; store-wide live tracking).
### Native finder (opus) — every probe executed in scratch repos: real-git chmod-000 on a worktree's admin dir → "enumeration incomplete: 2 of 3" on three runs, both refs intact; the exit-128 shim → "worktree list exited non-zero", the planted orphan spared; NO `.git/worktrees` at all → 1/1, the reaper runs and reaps a genuine orphan (correct); the dir gone after prune → same; a deleted worktree dir without prune → 2/2 + one "missing directory" line, main tree snapshotted; a `.git`-is-a-file sidecar skips silently before the completeness check; a dot-named worktree cannot hide (git sanitises ids); the push-queue mktemp failure → one line, local refs, origin empty; the doc's three recovery commands executed against a fixture; all six mktemp templates under the two prefixes; the main-tree diff exactly three justified lines (`$repo_real` ×2 text, `tmp_index=""`, the mktemp skip — base behaviour proven benign); the enum file gone on single-pid and group TERM; every earlier class (store-wide liveness, the graded guard, 0 bytes of reaper noise, collision ids, ONE batched push with the deletion in the same invocation, the dangling ref, decoys, `--ignore-errors`, the isolated-index invariant, quoting, one line per failure path); `bash -n`; 37 collected/passed; the round-5 guards non-vacuous. 5 raised:
- [M] the `.git/worktrees`-UNREADABLE guard (:356-358) has no grader — deleted, 37/37 green, and with the PARENT dir at chmod 000 `git worktree list` prints only the main entry at exit 0 while `ls -1` yields 0 → expected 1 = a compensating miscount → a live worktree's ref REAPED on both sides (the existing test chmods the CHILD and lands in the count-mismatch branch) → FIXUP (1). [M] the four `/tmp/wip-wt-*` temp files are in the trap but ungraded — the four rm lines removed → 4 leaks per signalled run, 37/37 green (the signalled test globs `wip-index-*` only) → FIXUP (2).
- [L] `grep -c … || echo 0` (:367) emits two lines when grep prints 0 and exits 1 → a prefix-less log line (the `wt_add_err` class) → FIXUP (3). [L] the `ls -1 | wc -l` denominator (:360) counts prunable admin entries → "incomplete" every tick, the reaper disabled for that repo until a manual prune → FIXUP (4): count only entries carrying a `gitdir` file. [L] the witness disjunct (:407) and the trailing else are unreachable once complete (every porcelain block ends with a blank line) → FIXUP (5): a leanness call stated in the commit body.
Round 6 verdict: 5 raised → 5 routed (2 M, 3 L) — graders and log hygiene; the production logic held on every probe; pool 3/3 CLEAN in substance (deepseek's doc-outside-Touches upheld as orchestrator-directed; its row-2 coverage claim subsumed by the finder's base-vs-branch main-tree proof). Not the no-op round.

## Round 7 — over `9c35928f..ddadd86f` (the round-6 fixup 68a50d40 + ddadd86f: both guards graded by mutation (the parent-dir-unreadable guard; the `/tmp/wip-wt-*` trap — with a self-caught flake from the live cron's identical prefix fixed by snapshot-and-diff); the double-line log fixed; the denominator counts only admin dirs carrying `gitdir` while an unreadable child is conservatively counted (a self-caught regression of the compensating-miscount class); the witness mechanism dropped as unreachable once the completeness gate runs first; 40 passed over 20+ runs)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 7.
### Adjudication (pool layer)
- deepseek — 1 raised: the doc outside the Touches — UPHELD on the letter, ORCHESTRATOR-DIRECTED (round 5, the Doc Sync Matrix floor; declared in the coder's commit body; T16 records the mis-pointer); otherwise CLEAN (27 test functions; the four hunks).
- gemini — CLEAN (each of the five round-6 items to its test; the witness absent from script and trap; the flake fix's residual risk stated — the test's own unique names + local origins bound it).
- qwen — CLEAN (the four rows; the reaper fail-closed on incomplete enumeration; the witness gone; the grep-c fix; main-tree semantics untouched except log text).
### Native finder (opus) — executed: 40 passed ×6 (`shellcheck` NOT INSTALLED — skipped, not passed); the parent-unreadable guard graded (git prints only the main entry at exit 0 under chmod 000 — the compensating miscount; mutant → a live ref reaped → `test_unreadable_worktrees_parent_admin_dir_reaps_nothing` red); the wt-prefix trap graded 3/3 (the snapshot-and-diff cannot false-PASS — mktemp never reuses a name); the grep-c single line; the denominator on all five admin-entry shapes with each fail direction stated; the witness gone, the reaper's four-way condition quoted, and "no path leaves the live file empty while complete" proven; every other guard red by mutation (scope, `--ignore-errors`, the prune arm, one push, the sanitiser, the id8 hash, placement → 30 failed); the doc's recovery commands executed; plain-repo silence; the main-tree diff exactly the justified lines; a batched push with an absent-ref deletion rc 0. 6 raised:
- [H] `:442` `grep -qxF "$wid"` without `--`: a worktree whose basename STARTS with a non-`[A-Za-z0-9._-]` character sanitises to `wid="-beta-<id8>"`, which grep parses as options (rc 1, no error) → a LIVE, dirty worktree's rolling ref reaped locally and queued for origin (executed end-to-end; the one-token fix verified on a copy) → FIXUP (1).
- [M] grep rc=2 (the live file vanished mid-run) reads as "not live" → reap; the :396 fail-closed invariant does not hold across the reaper loop (shim-emulated) → FIXUP (2). [M] the WORKTREE subshell's EXIT trap has no grader (deleted → 40 green; a real SIGTERM leaks the worktree index 3/3 under the mutant) → FIXUP (3). [M] the signalled test's `finally` deletes every `/tmp/wip-wt-*` file not snapshotted AFTER `Popen` — post-merge those are the live cron's; the commit's stated flake cause is false today (master's script has 0 such names) → FIXUP (4): hold the cron's flock; stop deleting foreign files. [M] `tests:1040-1043` claims a mutation proof that does not reproduce (the completeness check blocks the sidecar independently; the guard now only suppresses a skip line) → FIXUP (5). [L] three test comments still attribute behaviour to the deleted witness → FIXUP (6).
Round 7 verdict: 6 raised → 6 routed (1 H, 4 M, 1 L); pool 3/3 CLEAN missed the unguarded-grep class. Not the no-op round.

## Round 8 — over `9c35928f..8a77ed0c` (the round-7 fixup 8a77ed0c, 35/10 + 196/50: `grep -qxF --` (a `@beta` worktree survives; the other grep audited); the live-ids file re-checked before the loop and grep rc≥2 aborts with one "vanished" line (shim red on the baseline); the worktree subshell's trap graded by waiting on its own index file (mutation 5/5 after a self-caught wait-condition bug); the signalled test holds the cron's flock and never deletes foreign `/tmp/wip-wt-*` files; the guard test re-grounded to "no line names the sidecar" (reproduces under mutation); the witness comments reworded; 42 passed over 25+ runs incl. one 220 s run diagnosed as genuine flock contention with the production cron)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 8.
### Adjudication (pool layer) — a three-deepseek draw
- deepseek v4-flash (1) — 3 raised: the doc outside the Touches — UPHELD on the letter, ORCHESTRATOR-DIRECTED (rounds 5–7); the signalled test holds `/tmp/wip-backup.lock` with `fcntl.flock(LOCK_EX)` and NO timeout — a colliding cron run blocks the suite indefinitely (the coder measured one 220 s run as genuine contention) → put to the native finder to state the bound and the fix (`flock` with a bounded wait or a skip); a row-2 "main snapshot byte-identical" coverage claim (subsumed by the round-5 base-vs-branch proof).
- deepseek v3.2-exp — CLEAN (4/4 rows; the five round-7 fixes located with their tests; 39 new tests).
- deepseek v4-flash (2) — the doc outside the Touches only (adjudicated above).
### Native finder (opus) — executed: six option-shaped worktree names (`@beta`, `-`, `--`, `-n`, `-r`, `-e x`) all snapshot and all six rolling refs survive 3 runs with every dated ref aged; the `--` mutant red with the exact reap line; every positional use audited (all `refs/`/`/tmp/`/`:`-prefixed; bash `echo` never option-parses the ids); the vanish abort — one line, the ghost preserved, AND the already-queued batched push lands (origin holds the new v2); no binary/locale rc-2 path (the live file is ASCII by construction); the sidecar guard mutant red 3/3; the witness fully dead (2 hits, the adjudication comment); the porcelain's last entry blank-terminated; the gitdir denominator (a)–(d); a batched push with an absent-ref delete rc 0; subshells do not inherit the caught EXIT trap; the `case` glob-safe on a `r[ep]o` name; the doc's four recovery commands; 22 mutants red by the full suite and 4 proven equivalent by differential; the main-tree path exactly four declared deltas; 42 tests, 13 green full runs; `bash -n`; all 12 commits' trailers. 6 raised:
- [M] `tests:661` the blocking `flock(LOCK_EX)` on the production cron mutex has no bound — 164 s vs ~10 s baseline, a 30 s holder reproduced deterministically, caught live blocked behind the real cron → FIXUP (1). [M] `tests:368-376` the worktree-dedup grader is near-vacuous (same-second runs collide on the dated ref NAME; the dedup deleted survives 13 of 15) — round 1's [H] ungraded → FIXUP (2). [M] `tests:699-702` the round-7 subshell-trap grader escapes ~10% (the wait fires on the index file's first appearance, which `mktemp` creates and `:116` removes before `read-tree` recreates it; 18 of 20 caught, the two escapes with `add_started=False`) — round 7's "5/5" was under-sampled → FIXUP (3).
- [L] `:367` `[ -f gitdir ]` counts an EMPTY `gitdir` file git never enumerates → the reaper disabled for that repo forever (`[ -s ]` fixes it without re-opening the unreadable-child case) → FIXUP (4). [L] `wt_enum_ok` (:316-318) load-bearing but ungraded (a complete list + exit 1: pristine skips, the mutant reaps) → FIXUP (5). [L] two tests delete other processes' `/tmp` files (an unanchored `rm -f /tmp/wip-wt-live.*` in the vanish shim — a decoy deleted, proven; a basename-derived index glob) → FIXUP (6).
Round 8 verdict: 6 raised → 6 routed (3 M, 3 L) — graders and test hygiene; the production logic held on every probe; pool clean with the letter adjudicated. Not the no-op round.


## Round 9 — over `9c35928f..4922737b` (the round-8 fixup 8a77ed0c..4922737b: scripts/wip_backup.sh +19/−10, tests/test_wip_backup.py +260/−45 — all six prescribed items: the fixture's flock wait bounded (`LOCK_EX|LOCK_NB` polled to a 10s deadline, then `pytest.skip` naming the cron holder — measured 10.18s against a 15s holder; never proceed unlocked, since the flock is what rules out the real cron touching `/tmp/wip-wt-*` mid-test); the dedup grader asserts ONE `worktree beta snapshotted` stdout line across 3 runs (the old `len(dated_refs)==1` passed on the mutant — same-UTC-second collisions overwrote one ref name); the slow-add wrapper touches a sentinel when `add -A` is entered and the trap grader waits on it (18/20 → 20/20); `[ -f gitdir ]` → `[ -s gitdir ]` so a 0-byte ghost no longer wedges the reaper at "enumeration incomplete"; a `wt_enum_ok` grader (complete list + non-zero exit ⇒ no reap; the count-based sibling stays green under the same mutant — separate coverage); the tests snapshot the pre-existing `/tmp` set and delete only their own files (two decoys survive))
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 (the reaper deletes; the cron net is the box's safety floor — native probe) — round 9.
### Orchestrator execution (in the worktree)
- `bash -n scripts/wip_backup.sh` ok; `pytest tests/test_wip_backup.py -q` → 44 passed; the `-s` guard at `scripts/wip_backup.sh:376`, `wt_enum_ok` at `:315-317`/`:385`, the snapshotted line at `:182`; the dedup assertion at `tests/test_wip_backup.py:417-424` matches the script's literal `worktree <name> snapshotted`. shellcheck is not installed — skipped, not passed (every round).
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0464)
- v3.2-exp — CLEAN. v4-flash (1) and (2) — the doc outside Touches (the ticket's own Docs line names `docs/workstation/wip-backup-safety-net.md`; adjudicated in earlier rounds — not re-litigated); the live-ids echo-failure fail-open (PLAUSIBLE → routed as item 7 for validation); the whitespace-only `gitdir` re-wedge (REFUTED by the native finder's execution on git 2.43: a `"\n"` gitdir is enumerated as `prunable invalid gitdir file`, so `-s` and the porcelain count agree, 2 of 2, no wedge); the /tmp snapshot-diff race (UPHELD — native 5); the flock skip as a green gate (UPHELD — native 3); the SIGTERM main-index test hole (UPHELD — native 2); BC row-2 byte-identical coverage (settled in earlier rounds).
### Native layer (opus×1, by execution — mutations on copies, shims on PATH, production lock untouched)
- **[H] 1 — REGRESSION vs base: the worktree leg's `git push` (`scripts/wip_backup.sh:500-518`, `:509`) runs BEFORE the main-tree snapshot (`:520`) and is unbounded.** Same fixture (dirty main + dirty worktree, local origin, a push shim sleeping 600s), SIGTERM at 12s: branch `autobackup present? NO`; base 9c35928f `YES`. The main tree's dirt is unprotected and later repos never run; under `flock -n` a wedged push blocks later ticks (the real cron holds the lock 226s per 900s tick, measured in /proc/locks). FIXUP: `timeout 120` on both push sites + a shim grader.
- **[M] 2 — the main-tree temp-index trap (`:236`, T13's own addition) has no grader:** mutated to `:` survives 44/44 — the sentinel lands SIGTERM during the worktree's `add -A`, before the main index exists, so `tests/test_wip_backup.py:815`'s `remaining_main_index == []` is vacuous. FIXUP: a no-worktree TERM test.
- **[M] 3 — the flock skip fires ~24% of runs** (226/900 s; observed 4 of 6 runs `43 passed, 1 skipped`, exit 0, reason hidden under `-q`): the suite's only trap grader silently never runs and the gate is green. FIXUP: own lock path / window-scoped diff; never skip.
- **[M] 4 — `ruff check tests/test_wip_backup.py` red on the branch, clean at base:** UP017 `:353`, `:589`; F841 `:1559` — from the r1/r2/r6 fixups; unseen for eight rounds because `final_gate.py:2202 _RUFF_ROOTS` excludes `tests/` (routed hub-wide). FIXUP.
- [L] 5 — the round-8 snapshot-diff does not close the concurrent-sibling race (`repo.name` is the literal `wtleak` in every process; a decoy writer: 83 misread, 85 deleted) and `:750-762`/`:826-831` claim it does. FIXUP: pid-unique seed name + comments. [L] 6 — the newest-dated-ref pick at `:479` takes `head -1` before shape-filtering (worktrees `beta` and `beta-0e5aeec3` reproduced both fail directions). FIXUP: filter to the timestamp shape first.
- Verified clean by execution: the six round-8 items (bounded flock 10.26s with the reason naming the holder; dedup grader non-vacuous — guard deleted → 3 lines; sentinel under `tmp_path` with a 10s deadline; `-s` gitdir settles in both directions; `wt_enum_ok` mutant red while the count sibling stays green); detached + locked worktrees 3 of 3; bare repos never reach the loop; no `refs/wip` collision with the main leg (`wt-` vs `autobackup`/`bak-`); no `set -e` (`:40` is `set -u`), empty array safe under bash 5.2; `bash -n` clean; 44 collected; trailers parse; shellcheck NOT installed — skipped, not passed.
### Verdict
**7 findings (1 H + 3 M + 3 L, incl. the pool's live-ids item) → FIXUP round 9 routed to the coder.** Ledger: main-leg protection under worktree failure (NEW class) · trap graders · gate honesty (skip rate) · lint scope · test isolation · reaper ref selection · live-ids completeness · the converged classes (enumeration, dedup, secrets, namespace — clean).

## Round 10 — over `9c35928f..9bf9e67a` (the round-9 fixup 4922737b..9bf9e67a, incl. the untrailered checkpoint 9478a5d2 made under quota hold #3 — accepted as-is, named in the trailered successor: both `git push` sites bounded by `timeout $WIP_BACKUP_PUSH_TIMEOUT` (default 120) with a 600s-sleeping-shim grader (red: TimeoutExpired, no `refs/wip/autobackup`; green in 4.4s); the main-tree temp-index trap graded with a NO-worktree fixture (mutant `:` caught; the worktree-present sibling stays green under the same mutant — separate coverage); the production-lock flock borrow removed from the signalled-interruption test, non-interference carried by the pid-unique seed name + the pre-existing-diff — 6 full runs 48 passed, 0 skipped; `ruff check` clean incl. two F401s the removal exposed (format debt identical to base, left); `wtleak{pid}`; the newest-dated-ref pick shape-anchored before `head -1` + a foreign far-future-ref grader; the pool's live-ids item AGREED and fixed — the echo propagates its status, the caller counts records, the reaper refuses when recorded ≠ enumerated (grader: a read-only live-ids file))
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 (a cron-run safety net; the last native found an H the pool missed) — round 10.
### Orchestrator execution (in the worktree)
- `bash -n` ok; `pytest tests/test_wip_backup.py -q` → 48 passed; `ruff check tests/test_wip_backup.py` → All checks passed; `flock`/`LOCK_NB` references in the test file: 0; shellcheck not installed — skipped, not passed.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0237)
- v3.2-exp CLEAN. v4-flash (1): the "(offline/auth?)" line also printed on a timeout — UPHELD [L] (native 3); the missing-directory worktree recorded as live — its own conclusion: correct, protective (native (b) confirms: recorded=4 actual=4 expected=4). v4-flash (2): the doc outside Touches (the ticket's own Docs line — settled); the `/tmp/wip-wt-*` snapshot-diff race with the live cron — UPHELD (native 1); `timeout` availability — Ubuntu coreutils, box-local cron; [L] folded into native 3's validation.
### Native layer (opus×1, by execution — mutations on copies; one disclosed slip: the first probe ran the fixed script once against /opt without `WIP_BACKUP_ROOT` pinned — it snapshotted 5 dirty worktrees exactly as the cron does, reaped nothing, never touched the lock; every later probe pinned scratch)
- **[M] 1 — the flock removal's stated premise is false:** the pid-unique seed name reaches only `/tmp/wip-index-{repo}*`; the three files the flock protected — `wip-wt-push.XXXXXX`, `wip-wt-live.XXXXXX`, `wip-wt-enum.XXXXXX` (`scripts/wip_backup.sh:305/:310/:326`) — carry no repo name and no pid, and `tests/test_wip_backup.py:789/:836/:858-859` globs `/tmp/wip-wt-*` against a pre-existing snapshot, which excludes only files that existed BEFORE the window. Reproduced with a decoy created during the window: false red, then `os.remove`. A lost `wt_push_file` → `[ -s … ]` false → that repo's worktree refs never pushed off-box that tick. FIXUP: a `WIP_BACKUP_TMP_DIR` knob; the tests glob/delete under their own `tmp_path`; no global `/tmp` glob remains.
- [L] 2 — Doc Sync "New env var": `WIP_BACKUP_PUSH_TIMEOUT` absent from `docs/workstation/wip-backup-safety-net.md` (0 hits), as is the `live-ids incomplete` skip reason. [L] 3 — a malformed timeout value (`abc`, `-5`) degrades to NO push logged "(offline/auth?)"; `0` = GNU no-limit (silently unbounded again); an absent binary the same line — unreachable under cron, FIXUP: validate to the default + a distinct exit-124 line. [L] 4 — the DELETING sweep glob `wip-index-{repo.name}*` is prefix-only (pid 12345 matches pid 123456's files); the assert globs are anchored. [L] 5 — the stalled-push grader leaks an orphaned `sleep 600` on its failure path (no `start_new_session`).
- Verified clean by execution: the timeout wrapper at both sites with a 600s shim and a 2s override — the whole run 4.37s, nothing downstream skipped (dated ref, rolling ref, `autobackup`, `bak-<ts>` all minted), both failure lines name the repo; `set -u` under `env -i` exit 0; no `set -e`; `_wip_snapshot_worktree` outside the record `if`; item 7's refusal — missing-directory, locked and detached worktrees ALL recorded (`realpath -m`), two runs no `live-ids incomplete`, a genuine orphan reaped (REFUTES the pool's permanent-refusal worry); the index glob matches the pid-unique name; `${wid}` metacharacters — the sanitiser leaves `.` as the only ERE metachar; `a.b` vs `axb` worktrees probed, no exploitable hole; four mutants red on their intended graders (M1 timeout stripped, M2 the elif deleted, M3 the anchor reverted, M4 the trap line → `:` — the new test red while the sibling stays green); `bash -n` clean; 48 passed ×3, 0 skipped; ruff check clean (base clean, the intermediate 4922737b red — round 9 fixed it as claimed); trailers parse; exactly two files. Correction to the round-9 body: `ruff format --diff` is 49 lines at the ticket base, 1060 at round 8, 1195 at round 9 — pre-existing red but not "identical/unchanged"; final_gate auto-formats changed files, so the whole-plan gate would rewrite it — routed: format the file in the fixup.
### Verdict
**5 findings (1 M + 4 L) + the format debt → FIXUP round 10 routed to the coder.** Ledger: tmp-file isolation vs the live cron (NEW class — the flock's true purpose) · env-var validation + doc sync · deleting-glob anchoring · grader hygiene (orphaned child) · format · the converged classes (push bounding, trap graders, refusal counts, ref selection, wid metachars — clean).

## Round 11 — over `9c35928f..208223f6` (the round-10 fixup 9bf9e67a..208223f6: doc 24/0, script 44/9, tests 704/239 incl. a whole-file `ruff format` — `WIP_BACKUP_TMP_DIR` routes all five mktemp sites, the three affected tests under their own tmp_path with a decoy-survival grader, `"/tmp/` in the test file 0 executable (1: the deliberate cron decoy); the doc's Configuration + FAILS-CLOSED sections; the timeout validated (`''|*[!0-9]*|0` → 120) with graders; anchored delete globs; `start_new_session` + killpg; 50 passed ×2, 0 skipped)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 — round 11.
### Orchestrator execution (in the worktree)
- `bash -n` ok; 50 passed; `ruff check` clean; `ruff format --check` 1 file already formatted; `"/tmp/` 0; the knob at `scripts/wip_backup.sh:70`; the exit-124 lines at `:580`/`:637`; the doc names both env vars.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0233)
- Two CLEAN (the mktemp failure paths fail closed with a named line; the two-sided isolation; the validation order vs `set -u`; the FAILS-CLOSED list matches the script's strings; the reflow whitespace-only). One: the doc outside Touches (the ticket's Docs line — settled).
### Native layer (opus×1, by execution — mutations on copies with `.bak` assertions; every probe pinned to scratch)
- **[M] 1 — the exit-124 branch is DEAD at both push sites (`:574-575`, `:634-635`):** `$?` inside `if ! cmd; then` is the NEGATED status (0), so `-eq 124` never holds; end-to-end with the 600s shim both legs print "(offline/auth?)", `grep -c "timed out after"` = 0, and no test asserts the string — the round-10 body's claim is false. FIXUP: capture via `|| rc=$?` at both sites + graders for both lines.
- [L] 2 — a RELATIVE `WIP_BACKUP_TMP_DIR` resolves inside each repo and the temp index lands IN the snapshot (`git ls-tree refs/wip/autobackup` shows `tmp/wip-index-…`). FIXUP: require an absolute path. [L] 3 — the two rewritten interruption graders go vacuously green if the knob regresses (the class is caught only by an unrelated test). FIXUP: a non-vacuity assert each. [L] 4 — `mkstemp(...)[1]` leaks the fd at `:887`. FIXUP.
- Verified clean by execution: the knob fails CLOSED in every shape (trailing slash; non-existent; `chmod 500`; a relative non-existent) — named skip lines, RC 0, no partial refs, no repo pollution; `env -i` default `/tmp`, 5 of 5 mktemp sites routed; two-sided isolation empirically real (the live cron's `/tmp/wip-index-rn-kit-sandbox.*` appeared and vanished mid-session while every scratch dir stayed untouched); `set -u` ordering safe; the validation `case` mutation-killed (2 graders red); `start_new_session` behaviourally real (a nested grandchild orphaned without it, none with it); the doc's 6 reasons = the script's 6 (denominators quoted); the reflow normalised with the in-repo formatter leaves 181/68 real lines, every one mapped to a round-10 item; 50 collected, 0 skipped ×2; ruff/format clean; exactly 3 files; trailers parse; shellcheck absent — skipped.
### Verdict
**1 finding (M) + 3 (L) → FIXUP round 11 routed to the coder.** Ledger: env-var validation (REOPENED: the dead exit-124 branch, the relative path) · tmp-file isolation (clean, weakly graded → non-vacuity asserts) · grader hygiene (fd) · the rest clean.

## Round 12 — over `9c35928f..896a15bc` (the round-11 fixup 208223f6..896a15bc: script 31/4, tests 128/8 — both push sites capture the real status (`rc=0; … || rc=$?`), the exit-124 line reachable (a stalled-push run prints "timed out after 2s" once PER PUSH LEG — two lines on a repo whose main tree and worktree are both dirty (the header first said "once"; corrected on the native's measurement); the stalled-push grader asserts it present and the generic line absent — red on the dead-branch mutant; a bad-remote grader asserts the reverse); a relative `WIP_BACKUP_TMP_DIR` falls back to `/tmp` (grader with a pre-created repo `tmp/`: `git ls-tree` on the snapshot never contains `wip-index-`; red on the unvalidated mutant); one non-vacuity assert per interruption grader (red on a hardcoded-/tmp mutant); the fd closed; 52 passed ×2, 0 skipped; ruff/format clean)
Finders: pool deepseek/deepseek-v4-flash + google/gemini-3-flash-preview + qwen/qwen3-max + native opus×1 (the previous round's claim of this same fix was false — the closing round keeps the native) — round 12.
### Orchestrator execution (in the worktree)
- `bash -n` ok; 52 passed; `ruff check` clean; `ruff format --check` 1 file already formatted; two `|| …push_rc=$?` captures; the absolute-path `case` present; `MUTATION` markers 0.
### Pool layer (3 units returned — deepseek/deepseek-v4-flash, deepseek/deepseek-v3.2-exp, deepseek/deepseek-v4-flash; $0.0236)
- Two CLEAN (`|| rc=$?` safe under `set -u`; the snapshot and the dated ref are minted before the push, so a timed-out push costs nothing local; the relative-path validation before the first mktemp; the graders complementary). One: the doc outside Touches (the ticket's Docs line — settled since round 5).
### Native layer (opus×1, by execution — mutations on copies with `.bak` assertions; every probe pinned to scratch)
- [L] 1 — the worktree leg's generic line (`scripts/wip_backup.sh:605` `push failed for … worktrees (offline/auth?)`) is UNGRADED: silencing it leaves all 52 green, because the bad-remote grader's fixture has no linked worktree and exercises only the main leg at `:661`. Every OTHER line of the class is red on its grader when mutated (the worktree dead-branch shape, the main dead-branch shape, both generic lines together, the absolute-path `case`, the knob hardcoded to `/tmp` — both interruption graders red "WIP_BACKUP_TMP_DIR was not honoured"). FIXUP: a dirty worktree in that fixture + the worktree-line assert.
- [L] 2 — the round-12 header (and the fixup's commit body) claimed the timed-out line prints "once"; measured inside the grader: COUNT=2 — once per push leg. The grader is `in stdout` so it passes either way; the stated count was wrong — corrected in the header above (the commit body is immutable).
- Verified clean by execution: both legs with a 600s shim and a 2s timeout print distinct timed-out lines (`timed out after` = 2, `offline/auth` = 0); a bad remote prints both plain lines (0 / 2); a timed-out push costs no local state (autobackup, `bak-<ts>`, the worktree rolling + dated refs all minted; origin 0; the tmp dir empty) and a later repo in the same run gets all four of its refs — the loop never wedges; the relative-path validation over six shapes (`tmp`, `./tmp`, empty, `/tmp/`, `relative/nested`, unset) — `wip-index-` in the snapshot 0 every time, the pre-created repo dirs left empty, `/tmp/wip-*` delta none; the validation at `:80-83` precedes all five mktemp sites; neither `|| rc=$?` capture is inside the worktree subshell; `env -i` over five repo shapes → stderr 0 bytes, 0 unbound variables, and with a mktemp-failure shim fails closed with a named line; 52 collected, 52 passed ×2, 0 skipped; ruff/format clean; exactly two files; trailers parse; `"/tmp/` 0 in the test file (the one `dir="/tmp"` is the deliberate cron decoy, fd now closed); shellcheck ABSENT — skipped, not passed; no orphaned `sleep 600` left by any probe. Closing receipt — the four Behavior-Contract rows each named to its grader (`:336`, `:374`, `:401`, `:424`).
### Verdict
**2 findings (L) → FIXUP round 12 routed to the coder (the worktree-line grader); the header count corrected by the orchestrator.** Ledger (twelve rounds): main-leg protection under worktree failure · push bounding (both legs, both directions, now graded at every site once the fixup lands) · tmp-file isolation · env-var validation · trap graders · refusal counts · ref selection · wid metachars · doc sync · format · grader hygiene — the round-13 pool + the orchestrator's execution close it.
