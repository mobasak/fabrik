# Review — dup-id gate check landing (c14bb51b + fix round)

Status: CLOSED
Surface: commit c14bb51b (check_decisions_unique.py + final_gate wiring + tests + docs) and the
in-run fix round it triggered. Routed up from the scoped-review escalation rule (enforcement
path). Compact sizing: 1 native fabrik-reviewer finder + authority pass (pool skipped —
libs/subagents remains a sibling's live WIP, same disposition as the prior review).

## Rubric (verbatim head of `review_rubric.py --changed` over the surface — full output armed the finder)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
```

## Coverage Checklist

| Class | Verdict | Evidence |
|---|---|---|
| json-invisibility (the real find) | FIXED | registered `advisory=True` with `WARN:` output = invisible in `--json` (warnings filters on `⚠`, advisory_rows on warn_only registration) — wallpaper by construction in CI's mode; now `warn_only=True` + `⚠` prefix; pinned `test_warn_lines_carry_the_gate_prefix` + `test_gate_registers_the_check_warn_only` |
| same-class siblings | FIXED | `check_lint_ratchet.py` (2 skip-notices) + `check_imports_resolvable.py:639` printed bare `WARN:` under the same invisibility — prefixes fixed to `⚠`, their test pins updated to the convention (57/57 green) |
| regex-padding/bold false negatives | FIXED | `^\|\s*\**(D-\d+)\**\s*\|` tolerates formatter padding + bold cells; pinned `test_padded_and_bold_id_cells_still_match` |
| denominator-evidence | FIXED | the 0/49 claim now carries its verify one-liner in the backlog row |
| gate-wiring-scope | CLEAN | `changed` is `git diff --name-only` under pinned `cwd=PROJECT_ROOT` — repo-relative literal match sound (finder verified run_cmd:265-284) |
| advisory-display (human mode) | CLEAN | `print_step` shows exit-0 stdout; the defect was JSON-only |
| regex-vs-real-ledgers | CLEAN | finder ran extraction on 3 real ledgers (59/50/22 rows), 0 missed id rows, prose ignored; landing measurement covered all 49 |
| test-imports | CLEAN | `scripts/`+`scripts/enforcement/` are packages; 6/6 pass under repo pytest config; tests/ not synced |
| fail-open/fail-closed | CLEAN | check exits 0 by contract (warn_only claim); a broken whitelist ledger read raises nothing — missing file = silent by design (most repos' ledgers are healthy; detection is advisory at landing per D-057) |
| behavior-without-a-test | FIXED | every adjudicated fix carries its pin; the visibility contract itself is now tested |
| cost/quota accounting | CLEAN | no cost surface; single finder returned without quota death |

## Pass Ledger

| Pass 1 | method: gate | found: 4 | new: 4 | fixed: 4 | finders: dup-id-gate(native, returned: 1 CONFIRMED + 3 plausible, all adjudicated real) · authority(self, 4 seams clean + live lean-gate proof) |
| Pass 2 | method: re-derivation | found: 0 | new: 0 | fixed: 0 | finders: authority re-sweep — live hub run silent, dup fixture prints ⚠ lines, 6/6 + 57/57 sibling tests green |

## Gate

`python scripts/final_gate.py --lean --check --json` → `success` (this turn, wiring registered);
full `--check` green earlier this session pre-fix-round — the fix round's files re-verified by
the 57-test sweep + the live check executions above.

## Terminal

The exit round is the ledger's last row (Pass 2 — the quiet round). Totals: 4 raised, 4 fixed,
0 refuted. The headline find —
a WARN check invisible in exactly the mode CI reads — also closed the same latent class in two
sibling checks that had shipped it earlier.
