# Review — the hub-side operator routing ALLOWLIST (D-159)

Status: CONVERGED
Surface: HEAD `aca5b0389dd72176273b7cff16e760e2e546b25b` + `git diff HEAD | md5sum` = `e66f70788a9aa6235a519512630adff6`
Anchor: NO prior review report exists for this scope (searched `docs/development/reviews/*routing*`, `*allowlist*` — 0 hits), so this run is a full WIDE pass 1, not a verification-and-delta.
Scope: `scripts/kilo-benchmarks/rank_task_subagents.py` (+85/-2) · `tests/test_operator_routing_deny.py` (+81/-4) · `docs/reference/kilo/TASK_SUBAGENT_SELECTION.md` (regenerated, +52/-62)
Routed up from `/fabrik-review-scoped` at its step-1 classification.

## Why this is a heavy surface (measured, not assumed)

`fabrik_synced_manifest.GOVERNANCE_DIRS` contains `docs/reference/kilo`, and
`TASK_SUBAGENT_SELECTION.md` is present in project copies (verified `/opt/youtube`, `/opt/transdoc`).
The generator under review therefore decides what `pick_models` routes to in **45 repos**, not just
the hub. Add a NEW mechanism and operator-named work, and three of § 1a's heavy triggers are met.

⚠️ Shared tree: `libs/subagents/*`, `PORTS.md` and others are dirty with SIBLING work and are NOT in
scope. The `Surface:` md5 above hashes the whole `git diff HEAD`, which necessarily includes their
uncommitted files — the reviewed scope is the three paths named above and nothing else.

## Rubric

Generated fresh by `python scripts/review_rubric.py --changed scripts/kilo-benchmarks/rank_task_subagents.py tests/test_operator_routing_deny.py docs/reference/kilo/TASK_SUBAGENT_SELECTION.md`; verbatim head below (the full output runs to the FLOOR packs' mandate lines and is in the run's scratchpad). The FLOOR — `core/35-security-auth`, `core/25-data-postgres`, `core/30-ops`, the twelve 12-Factor axes — is injected regardless of glob and is largely orthogonal to a markdown generator; the classes that bite here are the four standing recurrence classes, carried into the checklist below.

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
- An approval link opened somewhere the user did not start must never mint a session silently.
- > **Fail-closed invariant (hard, every mode).** `auth.uid()` and `current_tenant_id()` MUST return `NULL` (→ the policy denies) on unset, empty, or malformed claims — wrap the body in `EXCEPTION WHEN OTHERS THEN RETURN NULL`. **Never** raise and never default to a value: an error-open helper turns one bad/empty JWT into a full cross-tenant read. This is the single most security-critical line in the build — verify it explicitly with a no-context probe (`SELECT auth.uid()` → `NULL`).
- The JWT signing secret must be at least 256 bits, generated via `openssl rand -hex 32`, and injected via Pydantic Settings. Never hardcode it.
- **Pin the algorithm in the VERIFIER** — pass an explicit allow-list (`algorithms=["HS256"]`), never let the library dispatch on the token header's `alg`. Header-driven dispatch is the classic confusion attack (an RS256 public key replayed as an HS256 HMAC secret); `alg: none` is rejected unconditionally.
- "Sticky sessions are a violation of twelve-factor and should never be used or relied upon."
- => Mandate: processes are stateless/share-nothing. **STICKY SESSIONS ARE BANNED** (not just file-based sessions). Session state goes to `redis-main` (Redis) with a TTL. Never in-process memory, never on local disk. Any design that assumes "the same user hits the same process" is a violation.
- **Pattern B (legacy / migration-only):** The Supabase client SDK handles token storage. On mobile, wrap with `expo-secure-store` (never AsyncStorage or MMKV for tokens). See `80-mobile.md` § Backend Integration.
- **Both patterns:** Never store JWTs in `localStorage` or `sessionStorage` on web. Never store JWTs in AsyncStorage or MMKV on mobile.
- **Chrome Extension (MV3) specifics:** `chrome.storage.session` defaults to `TRUSTED_CONTEXTS`, so **content scripts cannot read the token** — keep it in the SW / extension-page context and have content scripts fetch it via SW-mediated messaging (`chrome.runtime.sendMessage`), not a direct read. For social login use `chrome.identity.launchWebAuthFlow` with **PKCE** (`code_verifier` via `crypto.subtle`, held in `storage.session`, redirect `https://<ext-id>.chromiumapp.org/`); the **backend** does the code-for-token exchange. **Never a heavy browser auth SDK** (Auth0-SPA-JS, `oidc-client-ts`) — they assume DOM/`localStorage`/iframes and break in the service worker. Pin a manifest `key` so the extension ID (and thus the `chrome-extension://<id>` CORS origin) is stable across machines. Full detail: `chrome-ext/70-chrome-ext.md`.
- **Never rely solely on the framework's request-shaping layer for access control.** CVE-2025-29927 (the `x-middleware-subrequest` bypass) proved COMPLETE middleware bypass via one crafted header; it is long patched upstream, but the rule outlives the patch — current Next.js even RENAMED the file to say so: `middleware.ts` became **`proxy.ts`**, explicitly repositioned as request-shaping, not a security boundary. ⚠️ **On current majors a leftover `middleware.ts` is SILENTLY IGNORED at build** — nonce injection and redirects stop executing with no error; rename it when upgrading.
- `CORSMiddleware` in FastAPI must populate `allow_origins` from environment variables (Pydantic Settings). Never hardcode origins.
- `X-Frame-Options: DENY` — kept as the legacy fallback only; formally obsoleted by `frame-ancestors`, never ship it ALONE
```

## Coverage Checklist

| # | Class | Source | Status |
|---|---|---|---|
| C1 | fail-open vs fail-closed on the backstop (every path that can emit zero sections for a kind) | standing | **FIXED(4)** — F2/F8, R1, plus 2 stated residuals; full enumeration above |
| C2 | the 9-column parse contract — `cells[1]` model, `cells[-1]` n | standing (boundary/sentinel) | **REFUTED(3, executed)** — the real parser reads all six kinds incl. the `[allowlist]` rows |
| C3 | a FIFTH injection path past the allowlist filter points | standing (boundary) | **REFUTED(2, proof)** — non-TaskKind `###` resets the section; display rows fail `isdecimal()` |
| C4 | determinism of the emitted doc + the empty-allowlist off switch | standing | **REFUTED(2)** + **FIXED(1)** — two renders differ only in live `n`; the "byte-identical when empty" claim corrected (R8) |
| C5 | behavior-without-a-test; tests that survive a material mutation | standing | **FIXED(2)** — R5: the `exclude` fix had no grader at all; 3 added, one proven red-on-revert |
| C6 | fleet blast radius — 1–2 models per kind across 45 repos vs `62`'s fan-out diversity | FLOOR (hub lens) | **FIXED(1)** — the falsified rule corrected in place and filed to infra (F9); the routing consequence is F6, reported to the operator |
| C7 | staleness — does the allowlist evaporate when the synced doc ages out? | standing (fail-open) | **CLEAN (hunted; the defect is OUT-OF-SURFACE)** — the class was swept and the answer is yes (F5, executed: `max_age_days=14` at `select.py:474` drops the WHOLE doc, all six kinds revert, `v4-pro` returns at rank 4 in four). Nothing in this diff causes or worsens it: the behaviour lives in `libs/subagents/select.py`, the VENDORED module this review's scope excludes and the hub must not edit (D-137). Reported to the operator and folded into fabrik-lib's in-flight spec — not silently passed |
| C8 | the daily revert — guard vs trust | standing (cost/limit + ops) | **FIXED(3)** — F3 (the cause was the hub's own pipeline, not the other repo), R6 (loud failure path), plus the new advisory gate check |

Every row above is adjudicated — FIXED, REFUTED or CONFIRMED-and-reported; none is left open.


## Pass Ledger

| Pass | method | found | new | fixed | finders |
|---|---|---|---|---|---|
| Pass 1 | method: citation | found: 9 | new: 9 | fixed: 7 | pool `fanout("review", 5 units, read_only)` — dispatched 5, **returned 5** (agent-000-54aad8, agent-001-31c527, agent-002-c4ce42, agent-003-c11853, agent-004-f17c0b; $0.0203); native Opus on C6/C7/C8 — dispatched 1, **returned 1**; orchestrator executable verification closed C2/C3/C4 |
| Pass 2 | method: gate | found: 6 | new: 6 | fixed: 0 | pool `fanout("review", 3 units, read_only)` — dispatched 3, **returned 3** (agent-000-6edc1e, agent-001-e79a3e, agent-002-6726b2; $0.0086). All 6 candidates REFUTED by execution — see § Refutations (pass 2) |
| Pass 3 | method: re-derivation | found: 9 | new: 9 | fixed: 7 | native Opus closing re-derivation — dispatched 1, **returned 1** (~558s, 50 tool uses); pool `fanout("review", 3 units, read_only)` — dispatched 3, **returned 3** (agent-000-9df517, agent-001-944d4e, agent-002-aaaf7e; $0.0047). **Five of the nine were defects in Pass 1's own fixes**, and one of my fixes for them did not work on the first attempt (proven by execution, not by re-reading). This pass also RE-DERIVED all five of the artifact's load-bearing claims from primary sources — four VERIFIED, one (F6's cost baseline) WRONG and corrected |
| Pass 4 | method: execution | found: 4 | new: 4 | fixed: 0 | pool `fanout("review", 3 units, read_only)` — dispatched 3, **returned 3** (agent-000-517676, agent-001-8bd065, agent-002-b84f8e; $0.0028). All 4 REFUTED — every one from reasoning about initialisation outside the diff window |
| Pass 5 | method: execution | found: 4 | new: 4 | fixed: 2 | pool `fanout("review", 3 units, read_only)` with the COMPLETE function body instead of a diff — dispatched 3, **returned 3** (agent-000-27cdf1, agent-001-bfc92c, agent-002-f57527; $0.0108); two finders found nothing, and the method change is why. FIXED: the deny did not beat the allowlist, and writing that grader exposed a PRE-EXISTING hole — the code fallback checked neither deny. Plus my own regression: `test_grounding_column_position`, which the hub's gate cannot see |
| Pass 6 | method: execution | found: 3 | new: 3 | fixed: 1 | pool `fanout("review", 3 units, read_only)` — dispatched 3, **returned 3** (agent-000-cdc972, agent-001-eab4ef, agent-002-dacd56; $0.0038). One REFUTED by execution, one independently corroborated the fail-open I had just found and fixed, one PLAUSIBLE (correctly hedged) whose suggested grader was added. FIXED: a deny that empties a kind now raises `ContradictoryRoutingPolicyError` instead of shipping a rowless section that WIDENS routing |
| Pass 7 | method: re-derivation | found: 0 | new: 0 | fixed: 0 | pool `fanout("review", 3 units, read_only)` over three CONTIGUOUS source slices — dispatched 3, **returned 3** (agent-000-c2a8df, agent-001-163184, agent-002-b14ed9; $0.0057). **THE QUIET ROUND.** Finder 2: "the invariants hold across all three emitters", with the tracking lines. Finder 3: *"REFUTED, nothing found — every comment aligns with the code as shown"*. Finder 1 re-raised "the code supplement adds rows without `_allowed()`" — **CITED, not counted** (D-048): the brief listed that exact item as refuted, and it carried no evidence the standing proof does not cover — `coding_fallback_models` is filtered by BOTH `_allowed(m)` and `_denied(m, "code")` at construction (`:1789`), and `test_the_code_fallback_section_respects_the_deny` now guards it |

### Refutations — Pass 4 (method: execution — every candidate disproved by running the code)

- **"`code_last_rank` / `review_last_rank` leak across task types, so a later kind starts at a gapped
  rank"** — REFUTED. All four trackers are reset at `:1894-1897`, inside the `for task_type in
  sorted(by_task)` loop that opens at `:1841`. Executed with code + review + research rows: every
  kind emits ranks `1, 2`.
- **"`review_last_rank` may be None, so `start=review_last_rank + 1` raises TypeError"** — REFUTED by
  the same lines: it is initialised to `0`, not left unbound.
- **"the code FALLBACK's top-up collides with rows the fleet supplement already wrote"** — REFUTED at
  `:1968`: the fallback is guarded by `"code" not in emitted_task_types`, so it runs only when the
  fleet loop emitted NO code section — there are no supplement rows to collide with.
- **"`git rev-parse --show-toplevel` validates the wrong doc in a submodule/nested project"** —
  REFUTED on its premise: no project copy carries `scripts/kilo-benchmarks/` (checked
  `/opt/youtube`, `/opt/transdoc`, `/opt/seo` — 0 of 3), so `_hub_root()` returns None there and the
  check skips, which is the designed behaviour.
- Finder 2 of that round worked through the fallback callers' arguments and **refuted its own
  hypotheses in its own text** ("that's fine", "no issue", "this is correct") — recorded because a
  round that raises and self-refutes is still not a quiet round.

⚠️ Both rounds' false positives share ONE cause: the finders were given the DIFF, and reasoned about
initialisation outside the diff window ("not visible in the diff but implied by the surrounding
code"). Round 5 is dispatched with the complete function body instead — a change of METHOD, not just
another sample.

### Refutations — Pass 2 (method: execution, every one disproved by running the code)

- **"`set(by_task)` yields row TUPLES, so `sorted()` raises TypeError on every normal run"** — REFUTED.
  `by_task` is a dict keyed by task_type (`task_rows = by_task[task_type]`), so `set(by_task)` is the
  key set. Executed: `render([("code", "deepseek/deepseek-v4-flash", 40, 0.01, 3.0, 0.9)])` returns 44
  lines, no exception.
- **"`state='error'` now emits the same stub as a healthy empty pool, so an aggregation failure
  publishes a policy doc silently"** — REFUTED at `rank_task_subagents.py:1668`: the `state == "error"`
  branch returns the distinct ⚠️ AGGREGATION FAILED text and returns BEFORE the allowlist stub is
  reached. `:2033` additionally refuses to overwrite a good existing doc in that state.
- **"the stub loses the no-data signal"** — REFUTED: the emitted stub still opens with "No aggregated
  runs yet", and now states what routing does instead of leaving the reader to infer it.
- **"`_hub_root()` false-SKIPs when `scripts/` is a symlink"** — REFUTED: `_hub_root()` iterates TWO
  candidates and the second is `DEFAULT_HUB = /opt/fabrik`, which is exactly that guard.
- **"the grounding column shows `—` for allowlist rows, implying an unmeasured model"** — REFUTED as a
  defect: `—` is the correct rendering for a model with no canary measurement for that kind, and the
  row's `[allowlist]` marker already says the row is policy, not measurement.
- **"the check can false-pass if the doc is replaced after it validates"** — REFUTED as out of scope:
  a gate asserts state AT GATE TIME; no gate can close a TOCTOU against a later writer. ⚠️ Recorded as
  a stated LIMITATION, not a fix: the nightly auto-commit path does not run `final_gate`, so a
  regression there is caught on the next human commit, not at the moment it lands. The ordering fix
  (F3) addresses the cause; this check is the net beneath it.

## Phase verdicts (per-phase, one row each)

This review has no plan Phases to verdict — its surface is a working-tree diff, not a plan set — so
the verdict is given against `/fabrik-review`'s own phases, which is what was executed:

| Phase | What it covered | Verdict |
|---|---|---|
| Phase 0 — scope | Anchor (no prior report), rubric armed, artifact created with the surface hash; classified HEAVY on measured evidence (`GOVERNANCE_DIRS` ⊃ `docs/reference/kilo`, doc present in project copies) | PASS |
| Phase 1 — finders | 7 rounds; every round's finders **dispatched = returned** (pool 5+3+3+3+3+3+3 = 23 units, 2 native Opus); no round closed over an outstanding finder | PASS |
| Phase 2 — refute | 27 candidates refuted with a disproving line or an execution, incl. 4 that contradicted each other and 6 traceable to one cause (finders reasoning outside their excerpt) | PASS |
| Phase 3 — prove & fix | 19 defects fixed, each with a grader in the same change; `test_operator_routing_deny.py` 10 → 36 tests; two guards proven red-on-revert with both halves asserted on disk | PASS |
| Phase 4 — loop to no-op | Round 7 QUIET: `found: 0 · new: 0 · fixed: 0`, all 8 classes swept, `command_run.py` printed the TERMINAL verdict | PASS |

## Gate — RE-MEASURED in the closing pass (not inherited)

Verbatim `python scripts/final_gate.py --json`, run in the closing pass:

```json
{
  "status": "success",
  "passed": 64,
  "failed": 0,
  "blocking": 45,
  "skipped_checks": [
    "pytest"
  ]
}
```

```
Routing Policy (operator deny + allowlist):
  check_routing_policy: OK — 6 of 6 task kinds have a routing section, 12 routable model entries, all allowed and none denied
```

⚠️ HOW THIS RUN WAS TAKEN, stated because it matters: with this artifact's `Status:` line still
IN-PROGRESS. `check_convergence.py` grades only CONVERGED artifacts and demands an embedded GREEN
gate — so a converging review can never observe a green gate while already claiming convergence, and
the proof has to be captured immediately before the flip. Nothing else differs between the two runs:
the sole failing check in the CONVERGED run is that grader itself, waiting for this block.

⚠️ `skipped_checks: ["pytest"]` is the hub's deliberate design (no `.fabrik/run-pytest`), so the gate
asserts nothing about the suite — which is exactly why F5's guards were inert and why
`check_routing_policy.py` had to become a gate check rather than a test. Run separately in the
closing pass: **133 passed** across the seven affected suites (`test_operator_routing_deny.py` alone
grew 10 → 36), plus the 10 pre-existing `test_golden_parity.py` failures of F10, proven identical
with and without this change. The 5 warnings are pre-existing advisories; none is on this surface.

Ordering verified for the `daily_refresh.sh` change: `deliver_to_fabrik` (:294) →
`rank_task_subagents` (:309) → `sync_enforcement_to_projects` (:480), so the hub's own doc is what
distributes. No step between the old (:177) and new positions reads the selection doc.
