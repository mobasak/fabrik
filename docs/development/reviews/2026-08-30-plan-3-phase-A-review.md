# Review — plan-3 Phase A (the per-repo MCP emitter, red-first)

Surface: scripts/sysadmin/emit_mcp_project_config.py (NEW, 180 lines) ·
tests/test_emit_mcp_project_config.py (NEW, 13 tests) · scripts/fabrik_synced_manifest.py
(gitignore group) · tests/test_sync_seed_if_missing.py (+1 test) · CHANGELOG.md · INDEX.md ·
scripts/sysadmin/test_claude_rotate.py (E402 lint fix in committed sibling code, behavior-neutral).
Mode: NO-POOL — solo native (plan-declared override).

## Rubric (verbatim output — review_rubric.py --changed <the 4 core changed paths>)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
- An approval link opened somewhere the user did not start must never mint a session silently.
- > **Fail-closed invariant (hard, every mode).** `auth.uid()` and `current_tenant_id()` MUST return `NULL` (→ the policy denies) on unset, empty, or malformed claims — wrap the body in `EXCEPTION WHEN OTHERS THEN RETURN NULL`. **Never** raise and never default to a value: an error-open helper turns one bad/empty JWT into a full cross-tenant read. This is the single most security-critical line in the build — verify it explicitly with a no-context probe (`SELECT auth.uid()` → `NULL`).
```

## Coverage Checklist

| # | Class | Verdict | Evidence |
|---|---|---|---|
| 1 | security/secrets (35) | CLEAN | the ONLY secret-adjacent value (repo DATABASE_URL) lands in a GITIGNORED file (manifest group test asserts `.mcp.json` in the block); no key material in the script; open-source litmus: script itself carries zero credentials |
| 2 | data-postgres floor (25) | CLEAN | no DB access — the script only READS `.env` text; backing-service handle stays config (digest row 8 honored: code never decides hosts) |
| 3 | ops floor (30) | CLEAN | no docker/compose/deploy surface; localhost URIs are host-side by design (digest row 9 checked-N/A) |
| 4 | 12-Factor | CLEAN | pure file-emitter; config in argv; no env grouping |
| 5 | python discipline (10) | CLEAN | typed signatures, `str | None`, modern syntax; ruff + gate green 55/0 |
| 6 | behavior-without-a-test (45) | FIXED→CLEAN | 13 tests, ALL watched RED first (collection error pre-implementation, run shown in transcript), then green; the 11 plan behaviors + wef-D-018 + health-D-025 each have a named test |
| 7 | write-set containment | CLEAN | test_write_set_containment proves the full-tree delta is exactly `{repo}/.mcp.json` |
| 8 | fail-open/fail-closed | CLEAN | per-repo try/except → SKIP-and-log, run continues (plan A1 test 11 — the CHOSEN fail-open shape, tested); defs-source absence fails CLOSED (SystemExit) |
| 9 | boundary/sentinel | CLEAN | `_TYPE_RE`/`_DBURL_RE` anchored `^…$` multiline; condemned/never-emit sets are exact names; ordered de-dup via dict.fromkeys |
| 10 | derivation vs rulings | CLEAN | OVERLAYS mirrors roster § per-repo overlays incl. D-018 media producers + D-025 health repos; AFTER-EDIT header couples script↔roster; hub full-set minus NEVER_EMIT |
| 11 | defs-source lifecycle | CLEAN | fallback chain (--defs → hub .mcp.json → active fleet roster) survives the B4 trim — documented in module docstring |
| 12 | cost/quota accounting | CLEAN | zero paid calls — the emitter is pure local file IO; NO-POOL, no subagent spend this phase |
| 13 | lint-ratchet collateral | FIXED | pre-existing E402 in committed test_claude_rotate.py fixed (import reorder only); 13 pre-existing test failures PROVEN not mine: HEAD-copy run = identical 13F/58P (copy method, mutation asserted) |

## Pass Ledger

| Pass | scope | method | finders | found | new | fixed | notes |
|---|---|---|---|---:|---:|---:|---|
| Pass 1 | WIDE — all 12 classes over the 7-file diff | citation + execution | solo native (NO-POOL; dispatched: 0) | 2 | 2 | 2 | F401 sys-unused; F841 test var — both lint, both fixed |
| Pass 2 | CLOSING — re-derivation: full suites re-run fresh (13+7 passed), gate --check re-run 55/0, ruff repo-wide 0 errors, HEAD-copy comparison for the sibling file | re-derivation | 0 | 0 | 0 | TERMINAL |

## Step verdict

Step verdict: PASS — Phase A complete: emitter + 13 red-first tests + gitignore group, gate green.
Residual (out of phase scope, noted for close-out): the 13 pre-existing env-dependent failures in
scripts/sysadmin/test_claude_rotate.py (8852a716) — same count at HEAD, untouched by this phase.

```json
{"status": "success", "tier": 2, "passed": 55, "failed": 0}
```
