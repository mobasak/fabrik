# Review — external-services chain fix (2026-09-02)

**Scope:** the STAGED diff on `/opt/fabrik` master — 21 paths, +873/−290: the external-services chain
rewired into `daily_refresh.sh`, the code call-site scan in `gather_envs.py`, the classify bound, the
liveness surface, the retired orchestrator, the fleet-index blocks + CLAIMS rows + docs. Operator: "fix
them then review". Working-tree review (finders read the live tree; the only sibling-dirty path is
`PORTS.md`, excluded from scope).
**Status:** IN-PROGRESS — committed mid-loop on the operator's ruling (the review's ~30 modified files were redding siblings' gates); the final quiet round (Pass 5: the pending non-author Sonnet report + a fresh pool sweep) closes on the committed code and flips this line.
**Committed:** `c2b1ad2f` (2026-09-02 14:53, pushed) — Pass 5 runs on `git diff c2b1ad2f~1 c2b1ad2f`. Every later pass commits its own round (one commit each; pass 39 two: `10db7e01` + `cf0abcac`) and the Pass rows from 39 on name the SHAs they produced; the cumulative surface is `c2b1ad2f~1..<latest>` over the review's paths.
**Surface:** HEAD `68afb4c66e9fc152f95fcd9015bf9eee5050971b` + `git diff HEAD | md5sum` = `acd8d3e33cfc66339ef98f8d069fb715`
**Anchor:** no prior `*-review.md` covers this scope (newest reports: mcp-watch-session-start, mail-handling-enforcement, review-triage; `2026-07-26-catalog-autofreshen-review.md` touched `gather_envs.py` but its Surface predates this diff and cannot match) → full WIDE pass 1.

## Rubric (verbatim `python scripts/review_rubric.py --changed <21 staged paths>`)

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
- An approval link opened somewhere the user did not start must never mint a session silently.
- > **Fail-closed invariant (hard, every mode).** `auth.uid()` and `current_tenant_id()` MUST return `NULL` (→ the policy denies) on unset, empty, or malformed claims — wrap the body in `EXCEPTION WHEN OTHERS THEN RETURN NULL`. **Never** raise and never default to a value: an error-open helper turns one bad/empty JWT into a full cross-tenant read. This is the single most security-critical line in the build — verify it explicitly with a no-context probe (`SELECT auth.uid()` → `NULL`).
- The JWT signing secret must be at least 256 bits, generated via `openssl rand -hex 32`, and injected via Pydantic Settings. Never hardcode it.
- **Pin the algorithm in the VERIFIER** — pass an explicit allow-list (`algorithms=["HS256"]`), never let the library dispatch on the token header's `alg`. Header-driven dispatch is the classic confusion attack (an RS256 public key replayed as an HS256 HMAC secret); `alg: none` is rejected unconditionally.
- "Sticky sessions are a violation of twelve-factor and should never be used or relied upon."
- => Mandate: processes are stateless/share-nothing. **STICKY SESSIONS ARE BANNED** (not just file-based sessions). Session state goes to `redis-main` (Redis) with a TTL. Never in-process memory, never on local disk. Any design that assumes "the same user hits the same process" is a violation.
- **Pattern B (legacy / migration-only):** The Supabase client SDK handles token storage. On mobile, wrap with `expo-secure-store` (never AsyncStorage or MMKV for tokens). See `80-mobile.md` § Backend Integration.
- **Both patterns:** Never store JWTs in `localStorage` or `sessionStorage` on web. Never store JWTs in AsyncStorage or MMKV on mobile.
- **Chrome Extension (MV3) specifics:** `chrome.storage.session` defaults to `TRUSTED_CONTEXTS`, so **content scripts cannot read the token** — keep it in the SW / extension-page context and have content scripts fetch it via SW-mediated messaging (`chrome.runtime.sendMessage`), not a direct read. For social login use `chrome.identity.launchWebAuthFlow` with **PKCE** (`code_verifier` via `crypto.subtle`, held in `storage.session`, redirect `https://<ext-id>.chromiumapp.org/`); the **backend** does the code-for-token exchange. **Never a heavy browser auth SDK** (Auth0-SPA-JS, `oidc-client-ts`) — they assume DOM/`localStorage`/iframes and break in the service worker. Pin a manifest `key` so the extension ID (and thus the `chrome-extension://<id>` CORS origin) is stable across machines. Full detail: `chrome-ext/70-chrome-ext.md`.
- **Never rely solely on the framework's request-shaping layer for access control.** CVE-2025-29927 (the `x-middleware-subrequest` bypass) proved COMPLETE middleware bypass via one crafted header; it is long patched upstream, but the rule outlives the patch — current Next.js even RENAMED the file to say so: `middleware.ts` became **`proxy.ts`**, explicitly repositioned as request-shaping, not a security boundary. ⚠️ **On current majors a leftover `middleware.ts` is SILENTLY IGNORED at build** — nonce injection and redirects stop executing with no error; rename it when upgrading.
- `CORSMiddleware` in FastAPI must populate `allow_origins` from environment variables (Pydantic Settings). Never hardcode origins.
- `X-Frame-Options: DENY` — kept as the legacy fallback only; formally obsoleted by `frame-ancestors`, never ship it ALONE
**Never** write inline `APIKeyHeader` / `require_api_key`. **Never** use per-service key names (`SERVICE_API_KEY`, `PROXY_API_KEY`). Scaffold `python-api` auto-emits `internal_auth.py`, `metrics.py` (REQUEST_COUNT / ERROR_COUNT / ACTIVE_JOBS / PROCESSING_COUNT), `/metrics` endpoint (Authelia-bypassed), and `SERVICE_INTERNAL_SECRET_KEY` in `.env.example`.
- => Mandate: config via env vars only (`os.getenv("KEY", "default")`); **ZERO secrets/constants in code**. Apply the open-source litmus test to every change. **BANNED**: grouped/named env config sets (e.g. a `config/production.yml` or a `settings.production` group) — env vars are granular and orthogonal, set per deploy. (The pack already covers secret handling — cross-reference existing secret patterns and extend with config orthogonality.)
- [ ] Mobile tokens stored in `expo-secure-store` — never AsyncStorage or MMKV.
- > **⚠️ Bearer bypass scope — security-critical.** The bypass defaults to `^/api/`, which makes the **entire** `/api/*` surface public (un-2FA'd). If the application authenticates only a **sub-prefix** (e.g. `/api/v1` carries the bearer/internal-token check) while OTHER `/api/*` routes are unauthenticated (legacy / admin / destructive), you **MUST** narrow the bypass with `shape.bearer_bypass_prefix: "^/api/v1"` — otherwise `fabrik apply` exposes those routes to the public internet. **Bypass ONLY the path the app itself authenticates.** Value must start with `^/`; the verifier (`orchestrator/verifier.check_api_bypass`) probes the configured prefix on deploy. When unsure whether a service has un-auth'd `/api/*` routes, ask the app owner before relying on the `^/api/` default.

### core/25-data-postgres.md
| Vector search | pgvector on `postgres-main` + `fabrik-lib/rag` — ⚠️ the extension is NOT currently installed there (probed 2026-09-01: `postgres:16-alpine`, `plpgsql` only); a project needing vectors REQUESTS the fleet infra change first, never assumes it | same `postgres-main` DSN |
- Use Pydantic `BaseSettings` (per `10-python.md` § Config Loading) — never raw `os.getenv` **for an
- ⚠️ **Scope, stated here because this LINE is what `review_rubric.py` injects — without its section.**
- Never blindly trust `--autogenerate`. Always review `upgrade()` and `downgrade()` for unintended column drops, rename misinterpretations, and ENUM alterations before committing.
- > **Older pythons only** (services pinned below stdlib-uuid7 — which today includes SCAFFOLDED services: the scaffold still emits an older interpreter and ships `uuid-utils`; alignment tracked in the backlog): import `uuid7` from `uuid_utils.compat`, never `uuid_utils.uuid7()` directly — the latter returns `uuid_utils.UUID`, which asyncpg rejects (not a stdlib `uuid.UUID`). **DB-side:** newer PostgreSQL majors ship native `uuidv7()` (probe: `SELECT uuidv7()`); prefer `DEFAULT uuidv7()` at schema level where it exists. `postgres-main` currently runs major <!--v:postgres_major-->16<!--/v-->, which predates it — generate app-side on the fleet.
- Foreign keys must declare `ON DELETE` behaviour explicitly — `CASCADE` if children cannot exist without the parent, `RESTRICT` to protect audit trails. Never rely on the implicit default.
- This section owns the **canonical** engine, session, and `get_db`. `10-python.md` imports from here — never redefines its own.
- Database `AsyncSession` must be scoped to the route handler via `Depends()`. Never open sessions or transactions in global middleware — this holds connections during serialisation and I/O, exhausting the pool.
**BANNED as a server-side backing service** (dev, test, and prod alike):
**⚠️ SCOPE — this ban is about BACKING SERVICES, not client-local storage.** It does **NOT** apply to:
- **`desktop-app`** — SQLite is the **mandated** engine there (`desktop-app/72-desktop.md` § Local Persistence: `better-sqlite3` + SQLCipher; *"Production builds MUST encrypt the local SQLite file"*).
**12-Factor IV (Backing Services) — generalised:** swapping ANY attached backing service (DB, cache, object storage) is a **config change, never a code change**. The handle lives in `DATABASE_URL` / `REDIS_URL` / storage env — the code *reads* it, the code does not *decide* it. Never `if ENV == "prod":` branching to pick a host. (See § PostgreSQL Host Selection, which already mandates this for the DB.)
- [ ] All primary keys use UUIDv7 — stdlib `uuid.uuid7` on current Python (older pythons: `uuid_utils.compat.uuid7`, never direct `uuid_utils.uuid7()`); no `uuid4()`.

### core/30-ops.md
- the pinned release leaves full security support, never per-pack.
- All services deploy via `fabrik apply` (SSH + Docker Compose) on the `fabrik` network. Traefik routes external traffic — services do NOT bind host ports.
- **No `ports:` section.** All external traffic routes through Traefik. Never bind host ports. See Docker Port Security below. **12‑Factor VII (Port binding):** "the app is self‑contained and exports HTTP by binding to a port; it does not rely on runtime injection of a webserver" — which is exactly WHY no host `ports:`.
- **`container_name: <name>` is mandatory.** Same `_validate_compose()` gate refuses any service without it. Stable names are required so Gatus endpoints, inter-service URLs, and `docker exec`/`docker inspect` keys don't drift per redeploy. Use the bare service name (`browserless`, `gotenberg`, `meilisearch`, `glitchtip-web`, `site-provisioner`, etc.) — never UUID-suffixed names.
- gets one (ruling D-052) — see `core/60-watchdog.md`. Do not author a `watchdog: { enabled: false }`
- path before the flag goes in the spec, and assert target health (`/api/v1/targets` → `up`), never a
- VOLUME gets a plan pointed at a directory that never exists — a paper backup that reads green and
- plan; never let a service-named plan be mistaken for the protection.
- health-enabled service can NEVER pass `up -d --wait` on a fresh database, and the deploy hangs to
- a bare `exec "$@"`.* An init the deploy cannot perform itself is a runbook step the plan MUST own.
- `fabrik redeploy <app>` SSHes to the VPS and runs `git pull` + `docker compose up -d --wait` against the **GitHub remote**, NOT the local `/opt/<app>` clone. Skipping `git push` redeploys the previous remote commit — the VPS never sees local changes.
**Mandate:** build → release → run are strictly separated. Releases are IMMUTABLE; the git SHA is the release ID. NEVER hot‑patch a running container (no `docker exec` to edit code/config in place, no in‑place code mutation on the VPS). Any change = a new build + a new release via `fabrik apply` / `fabrik redeploy`.
- Runtime database migrations that modify the app container (migrations MUST be run as separate deploy‑time steps)
**Mandate:** WSL dev and the VPS run the SAME backing services (PostgreSQL + Redis), same major version. NEVER substitute a different backing service in dev (no SQLite standing in for Postgres, no in‑memory dict standing in for Redis). The same code must run unmodified in both environments.
- WSL runs PostgreSQL + Redis at the SAME MAJOR as the VPS containers — probe the live truth, never copy a tag from a doc: `ssh vps "sudo docker inspect postgres-main redis-main --format '{{.Config.Image}}'"` (2026-09-01: `postgres:16-alpine` · `redis:7-alpine` — upstream official images, outside OUR-image Alpine ban per § Banned Patterns)
**Invariant:** Never use `ports:` in compose.yaml to expose internal services to the host. All external traffic must go through Traefik.
**Health endpoints (`/health`, `/healthz`, `/metrics`, `/api/health`) bypass Authelia on all services** — required for Gatus and Prometheus monitoring. The bypass is **resource-based, not domain-bound** — applies on every domain routed through Authelia (hub direct + spokes via `authelia-vps1@file` middleware). Never protect these paths.
**CRITICAL:** Use `web`/`websecure` in Traefik labels — never `http`/`https` (those entrypoints do not exist). The scaffolder emits the correct entrypoint names; if you hand-write labels, match these exactly.
**Mandate:** migrations and admin tasks run as a ONE‑OFF process against the DEPLOYED image + env — identical environment to regular processes. NEVER run admin tasks from a laptop against prod, NEVER via `docker exec` into a live container, and **ABSOLUTELY NEVER auto-run migrations from app startup/`lifespan`** (concurrent replicas race the Alembic version table → wedged deploy).
- > sees a file that looks exactly like a migration step, and ships a deploy where migrations never run —
- > the rule producing the very defect it exists to prevent. Do not re-add either without a `path:line` in
**Processes are share-nothing:** any state shared across requests MUST go to Redis (`redis-main`) with a TTL. A project using Redis for sessions MUST declare `shape.needs_cache: true` in `specs/services/<id>.yaml`, or `fabrik apply` skips the Redis registrar and the deploy is silently broken.
- "A twelve-factor app never relies on implicit existence of system-wide packages"
**Mandate:** any binary the app shells out to (ffmpeg, yt-dlp, poppler, tesseract…) MUST be `apt-get install`-ed in the Dockerfile, with a `shutil.which()` startup probe that fails fast. **The pinned base image is the version boundary** — exact `=version` apt pins are banned: they break on every Debian point release as old debs leave the mirrors (the "works then mysteriously breaks" class this section exists to prevent); the codename pin + image digest give the reproducibility. Never assume `curl`/ImageMagick/ffmpeg exist in the image — they don't by default.

### 12-FACTOR (all twelve axes)
- I codebase: shared code → fabrik-lib, never two apps in one repo
- II deps: every shelled-out binary installed + pinned in the Dockerfile
- III config: granular env vars; no secrets in code; no grouped env sets
- IV backing services: swappable by DSN/config change only
- V build/release/run: releases immutable; never hot-patch a container
- VI processes: stateless; session state → redis-main; no sticky sessions
- VII port binding: bind in-container; Traefik routes; no host ports:
- VIII concurrency: scale out; never daemonize or write PID files
- IX disposability: SIGTERM returns in-flight jobs to the queue; jobs idempotent
- X dev/prod parity: same backing services everywhere; no SQLite-for-Postgres
- XI logs: unbuffered stdout only; the app never writes/rotates a logfile
- XII admin: migrations/one-offs run against the deployed release, never startup

## MATCHED — packs whose globs hit the changed paths

### core/10-python.md  (hit: scripts/classify_services.py, scripts/credit_fetchers/__init__.py, scripts/gather_envs.py)
**`uv`** is the mandated Python package manager. Never use raw `pip`, `pip install`, `poetry`, or `pipenv`.
- Dependencies live in `pyproject.toml` + `uv.lock`. Do not modify these files unless the ticket authorises it.
- its own reviewed commit, never as a side effect of unrelated work.
- The one RULE: use SQLAlchemy async consistently — never mix `async def` with sync
- The canonical `engine`, `async_session`, and `get_db` are defined in `src/database.py` — owned by `25-data-postgres.md`. Import from there, never redefine:
**Config convention:** apps read a complete `DATABASE_URL` (`postgresql+asyncpg://user:pass@host:port/db`) and `REDIS_URL` from env. Discrete `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD` for the app to assemble are **banned**. The env supplies the complete URL — `localhost` in WSL, `postgres-main` on VPS — so the host concern is an env-layer responsibility, never code logic. See `30-ops.md` compose template for how discrete vars are interpolated into `DATABASE_URL` at the compose level.
- volume** (`30-ops.md` § Volumes), never in `.tmp` and never in `/tmp`.
**GlitchTip discipline:** unhandled exceptions (FastAPI 500s) are auto-captured by GlitchTip with full stacktraces. In the `except Exception` branch, log a **short event name + correlation_id** — never `logger.exception()` (that duplicates the traceback in Loki AND GlitchTip). See `55-observability.md` § Error Reporting for the full rule.
**Note:** Use the scaffolded logger: `from {package}.logger import get_logger` (see `55-observability.md` § Pre-Scaffolded Logging). Do not use `structlog.get_logger()` directly or `logging.getLogger(__name__)`.
- **Never a bare `asyncio.create_task()`** — an unreferenced task is silently garbage-collected
- **`datetime.now(UTC)`, never `datetime.utcnow()`** — deprecated and naive; naive datetimes
- Ruff's selected rule-sets MUST include `ASYNC` (blocking IO in async code — machine-enforces
- Production services run via `uvicorn` CLI in the Dockerfile, not `uvicorn.run()` in code. Base image is always the pinned Debian `-slim` variant on `linux/amd64` (the variant is pinned fleet-wide in `30-ops.md` § Container Base Images — change it THERE, never per-repo). Never use Alpine — musllinux wheels exist now (PEP 656) but coverage is still partial, source builds are dramatically slower, and musl's allocator/stack defaults degrade CPython; the trade never pays on this fleet.
- `uvicorn.run()` is for local development only. Never ship it in production code.
- a fleet scaling decision (more containers), never a per-app flag.
**BANNED: grouped/named env config sets.** 12F is explicit — *"env vars are granular controls, each fully orthogonal to other env vars"* — so a `config/production.yml`, a `settings.production` group, or a `config/{dev,staging,prod}.yaml` tree is a violation. Env vars are granular and set **per deploy**, never batched into a named "environment".
**BANNED:** `logging.FileHandler`, `logging.handlers.RotatingFileHandler`, `TimedRotatingFileHandler`, `loguru` file sinks, any `*.log` file write, any in-app log rotation/retention/cleanup. The app never decides where logs are stored or routed — Docker → Promtail → Loki does. Full rule: `55-observability.md` § Logs.
**Factor XII — Admin processes. NEVER migrate from app startup.**
**BANNED: `alembic upgrade head` in FastAPI's `lifespan`, in an `@app.on_event("startup")`, or as an import side-effect.** With more than one replica (or a restart storm) two containers run `upgrade head` **concurrently** → they race the Alembic version table → duplicate DDL → **wedged deploy**. Migrations are a **one-off admin process against the deployed release**: `docker compose run --rm <svc> alembic upgrade head` (see `30-ops.md` § Release & Admin Processes).

### core/40-documentation.md  (hit: CHANGELOG.md, INDEX.md, docs/CAPABILITIES.md)
- > **⚠️ `docs/OPERATIONS.md` + `docs/DEPLOYMENT.md` are FLEET-AI INTERFACES, not just docs (D-065).**
- **Tier-1 (cheap-pool author → verify → converge):** for each **mechanically-detectable** doc whose Doc-Sync trigger fired (`docs/QUICKSTART.md` · `docs/CONFIGURATION.md` · `docs/data-contract.md` · `docs/SERVICES.md` · `docs/OPERATIONS.md` — the reliable-signal subset), `scripts/doc_reconcile.py` dispatches a cheap OpenRouter-pool author (`libs.subagents`, `pick_models("docs")`) to emit a **minimal structured patch**, **verifies it before applying** (a symbol cross-check catches invented endpoints; the orchestrator injects a higher-assurance native-Claude verify), and loops to a zero-edit round. Runs per phase in `/fabrik-execute-plan`; never blocks (fail-safe). The other docs (CHANGELOG, INDEX, FEATURES, RESILIENCE, PORTS, the READMEs, `db/schema.sql`, …) have no reliable mechanical content-signal → they rely on the touch-on-change backstop below + your own edit (force-update, not force-correct).
- The SSOT is the type-aware registry (`scripts/enforcement/_doc_registry.py::PROJECT_DOCS`) — this table is its project-facing rendering, kept in step, never a second truth. The hub's epic-to-ticket workflow (`/opt/fabrik/docs/orchestrator/epic-to-ticket-workflow/06-ticket-breakdown-fabrik.md`) injects these rows per ticket.
- Standalone work (not plan execution) → `Agent-Role: primary`. Trailers go below a blank line, above `Co-Authored-By`. ⚠️ The trailer block must be its OWN paragraph with NO blank line inside it: git parses only the LAST paragraph, and only if it is all-trailers. A blank line before `Co-Authored-By:` demotes everything above it to prose; so does a prose line glued to the top of the block. Measured 2026-08-15: 200 of the last 200 hub commits carried `Agent-Role:` and only 10 parsed, because the old example here shipped the blank line.
- **⚠️ Link it or it is decoration.** *Measured:* requests for files that do NOT exist came ~zero
- from AI bots — agents never go looking. It follows (inference, not measurement) that a file only
- ⚠️ **In THIS repo `llms.txt` is GENERATED** (`scripts/generate_capability_index.py`, refreshed
- daily) — never hand-edit it; change the generator. A project writing one by hand owns it.
- either way. Cheap and reversible — never at the expense of `OPERATIONS.md`/`DEPLOYMENT.md`, which
- **No skipped heading levels** — `##` to `###`, never `##` to `####`
- **Fenced code blocks only** — never indented code (AI treats it inconsistently)

### core/45-testing-strategy.md  (hit: scripts/tests/test_gather_envs.py, scripts/tests/test_refresh.py, tests/test_external_services_chain.py)
- **Behavior Contract**: every ticket enumerates its distinct **user-observable behaviors / acceptance criteria** and tests **each one** — one high-value integration/E2E test per behavior, risk-ordered, TDD for the risky ones. Skip trivia (getters / framework glue / config): **lean-but-complete, NOT 100%-line-coverage dogma**. Do not chase line coverage — ensure every behavior has a test that would fail if that behavior regressed. (Cheap pool subagents can author the per-behavior tests — the suggest→curate→author→fix workflow in `62-using-subagents.md` § Dispatch policy + `~/.claude/commands/fabrik-review.md`.)
- **No cosmetic assertions**: never assert against CSS classes, Tailwind utility strings, pixel measurements, or snapshot hashes. Assert application state and user-visible outcomes only.
- **Watched-fail-first** (for tests this change adds or modifies; trivia stays skipped per the Behavior Contract): a non-trivial behavior's test proves something only if it has been SEEN RED — either write it first and watch it fail, or (after the fact) neuter the fix/feature, prove the test goes red, then RESTORE and re-run to green. The neutered state is never staged, committed, or left in the tree. A green test never seen red is unverified — a suite can pass with its guard deleted.
- **Run tests**: `uv run pytest tests/` (never bare `pytest` — Fabrik uses `uv`) — **when the project has
- a `pyproject.toml`/`uv.lock`**. ⚠️ **Gate this on the manifest, because this line is FLOOR-injected into
- **Zero-mock database policy**: never mock SQLAlchemy, SQLModel, or database sessions. All backend tests execute against a real PostgreSQL instance.
- **`ASGITransport` never runs lifespan** — anything the app initializes at startup (scaffolded apps are lifespan-based) silently does not exist in tests; wrap with `asgi-lifespan`'s `LifespanManager` when a test needs startup state.
- Use `structlog` in test helpers if logging is needed — never `print()`. See `55-observability.md`.
- **Never stub a server action from Playwright** — the server is the E2E boundary; stubbing belongs in the unit lane where the action is a plain function.
- Run Playwright against the PRODUCTION build (`next build && next start`), never the dev server.
- All locators must be **semantic**: `page.getByRole('button', { name: /submit/i })`. Never use CSS selectors or XPath.
- Launch Playwright's **bundled Chromium** (`channel: 'chromium'`) — stable Chrome/Edge removed the `--load-extension` / `--disable-extensions-except` side-load flags (Chrome 137/139), so those args only work under bundled Chromium, never installed stable Chrome.
- Run `@axe-core/playwright` with **`bypassCSP: true`** (the non-relaxable extension CSP otherwise makes axe throw on `chrome-extension://` pages); keep `@axe-core/playwright` a **dev-dependency only** (MPL-2.0 — never bundled into the shipped artifact). Gate bundle size with `size-limit` **per surface** (popup / side-panel / content-script). Full loop: `chrome-ext/70-chrome-ext.md` § Testing & UI Verification.
- Keep the generated types committed and re-generate on schema changes (`uv run python -c "import json; from <package>.main import app; print(json.dumps(app.openapi()))" > openapi.json` — the scaffold emits `src/<package>/main.py`, never a flat `src/main.py`, so `src.main` imports nothing).
**BANNED in tests:**
| A test THIS change adds/modifies that was never seen red (no fail-first, no red-on-revert proof) | Watch it fail first, or neuter the change → prove red → restore → re-run green |
- [ ] Destructive DB tests call `require_throwaway(TEST_DATABASE_URL)` before connecting — never point them at a dev/shared DB.

### core/57-external-data-sourcing.md  (hit: docs/reference/apis/EXTERNAL_SYSTEMS.md)
- API clients, connectors, webhook handlers). ⚠️ `58-resilience` does NOT auto-load on
**Never scrape when an API exists** — an API is a contract you can PROFILE (limits, resume,
- ⚠️ **Two lanes, and conflating them is the mistake this ladder invites.** An MCP server is an
- ⚠️ **Check the roster before naming one — do not cite from memory**: the roster is SPLIT
- (pluggable, Anti-Captcha — ⚠️ a BYPASS capability, not a default; see § Hard constraints,
- · `proxy-pool` (Postgres-backed proxy pool — ⚠️ same caveat as `captcha-solve`: a bypass
- capability, never a default) · `api-quota` (`QuotaTracker` parses rate-limit headers, persists
- for the real API. ⚠️ **A module that already retries IS the retry layer** — `web-scrape`
- (⚠️ OpenRouter-only, see § Hard constraints), `ai-image`, `ai-audio`, `ai-translate`,
**Derive the count and the category set from the FILE, never from this list** — the file has
- `docs/reference/apis/**`. Key via env var, never in code.
**An UNKNOWN never waives the design decision — design to the conservative reading**: unknown cap
| 4 | **Identity posture** — limits per-key, per-account or per-IP? does the ToS permit multiple accounts/orgs — **cite the clause + URL, or UNKNOWN** | decides whether horizontal scale is even available — see the ⚠️ below |
| 9 | **Credential lifecycle** — does the key/token EXPIRE, on what schedule; can it be rotated with an overlap (two keys valid at once) or only by a hard cut; what does an expired key LOOK like (401? 403? a silent empty 200?) | decides whether rotation can be automated at all, and whether the connector can even tell a dead credential from a dead vendor — an expired key is the failure a retry loop can never fix |
- ⚠️ **Field 4 is where an operational question becomes a legal one.** Asking "are multiple accounts
- constraints, and it is an operator decision with the ToS quoted — never an engineering convenience.
- expiry never happens in production; the `58` last-resort rung that died of an expired credential
- model (pydantic `strict=True` / zod `.strict()`, unknown keys rejected deliberately), never
- signature outside the timestamp tolerance. ⚠️ **And it runs a reconciliation poll** — a scheduled
- row 22 alarms on, never the fetch count. A **bot-block** (`403`, a challenge page) is a pause
- condition, not a retry and not a bypass: `58` says never retry a 403, and § Hard constraints
- says never solve the challenge — the legal move is `adaptive-dispatch`'s strategy switch
- ONCE, never hot-looping, and named as human-gated in §2b so nobody expects the queue to wake itself.
- ⚠️ **One subject, one home.** The profile records the VENDOR's contract (measured, dated). YOUR
- card in `docs/RESILIENCE.md`; that card LINKS the profile and never copies its numbers.
- ⚠️ **Not gate-enforced yet, deliberately — and measured 2026-09-02: 0 of the 6 hub vendor docs in
- **LLM data = OpenRouter ONLY** — never a direct vendor LLM SDK (`openai`, `@anthropic-ai/sdk`).
- **Vendor keys are env vars** (`os.getenv`), never constants; the repo must stay open-sourceable (12F-III).
- Honour a TDM reservation even when your own UA is not the one named. ⚠️ **The token set churns
- **⚠️ THE BYPASS LINE IS THE LEGAL LINE, and it runs straight through our own toolbox.**
- catalog's `proxy` and `captcha` categories are therefore **capabilities, never defaults**:
- on a page you never agreed to are weak against a logged-out fetcher; terms you click through,
- ⚠️ **Where the profile lives differs between hub and project** — `EXTERNAL_SYSTEMS.md` is a
- `cost` · `capability` · `url` · `status` · `match`), never the envelope — the twelve profile
- the handler, never onto Authelia. It does not ride the health-path bypass either. **The

# promote-to-check_*: 97 injected mandate(s) look deterministically greppable
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
- > **Fail-closed invariant (hard, every mode).** `auth.uid()` and `current_tenant_id()` MUST return `NULL` (→ the policy denies) on unset, empty, or malformed claims — wrap the body in `EXCEPTION WHEN OTHERS THEN RETURN NULL`. **Never** raise and never default to a value: an error-open helper turns one bad/empty JWT into a full cross-tenant read. This is the single most security-critical line in the build — verify it explicitly with a no-context probe (`SELECT auth.uid()` → `NULL`).
- The JWT signing secret must be at least 256 bits, generated via `openssl rand -hex 32`, and injected via Pydantic Settings. Never hardcode it.
- **Pin the algorithm in the VERIFIER** — pass an explicit allow-list (`algorithms=["HS256"]`), never let the library dispatch on the token header's `alg`. Header-driven dispatch is the classic confusion attack (an RS256 public key replayed as an HS256 HMAC secret); `alg: none` is rejected unconditionally.
- => Mandate: processes are stateless/share-nothing. **STICKY SESSIONS ARE BANNED** (not just file-based sessions). Session state goes to `redis-main` (Redis) with a TTL. Never in-process memory, never on local disk. Any design that assumes "the same user hits the same process" is a violation.
- **Pattern B (legacy / migration-only):** The Supabase client SDK handles token storage. On mobile, wrap with `expo-secure-store` (never AsyncStorage or MMKV for tokens). See `80-mobile.md` § Backend Integration.
- **Both patterns:** Never store JWTs in `localStorage` or `sessionStorage` on web. Never store JWTs in AsyncStorage or MMKV on mobile.
- **Chrome Extension (MV3) specifics:** `chrome.storage.session` defaults to `TRUSTED_CONTEXTS`, so **content scripts cannot read the token** — keep it in the SW / extension-page context and have content scripts fetch it via SW-mediated messaging (`chrome.runtime.sendMessage`), not a direct read. For social login use `chrome.identity.launchWebAuthFlow` with **PKCE** (`code_verifier` via `crypto.subtle`, held in `storage.session`, redirect `https://<ext-id>.chromiumapp.org/`); the **backend** does the code-for-token exchange. **Never a heavy browser auth SDK** (Auth0-SPA-JS, `oidc-client-ts`) — they assume DOM/`localStorage`/iframes and break in the service worker. Pin a manifest `key` so the extension ID (and thus the `chrome-extension://<id>` CORS origin) is stable across machines. Full detail: `chrome-ext/70-chrome-ext.md`.
- **Never rely solely on the framework's request-shaping layer for access control.** CVE-2025-29927 (the `x-middleware-subrequest` bypass) proved COMPLETE middleware bypass via one crafted header; it is long patched upstream, but the rule outlives the patch — current Next.js even RENAMED the file to say so: `middleware.ts` became **`proxy.ts`**, explicitly repositioned as request-shaping, not a security boundary. ⚠️ **On current majors a leftover `middleware.ts` is SILENTLY IGNORED at build** — nonce injection and redirects stop executing with no error; rename it when upgrading.
- `CORSMiddleware` in FastAPI must populate `allow_origins` from environment variables (Pydantic Settings). Never hardcode origins.
- `X-Frame-Options: DENY` — kept as the legacy fallback only; formally obsoleted by `frame-ancestors`, never ship it ALONE
**Never** write inline `APIKeyHeader` / `require_api_key`. **Never** use per-service key names (`SERVICE_API_KEY`, `PROXY_API_KEY`). Scaffold `python-api` auto-emits `internal_auth.py`, `metrics.py` (REQUEST_COUNT / ERROR_COUNT / ACTIVE_JOBS / PROCESSING_COUNT), `/metrics` endpoint (Authelia-bypassed), and `SERVICE_INTERNAL_SECRET_KEY` in `.env.example`.
- => Mandate: config via env vars only (`os.getenv("KEY", "default")`); **ZERO secrets/constants in code**. Apply the open-source litmus test to every change. **BANNED**: grouped/named env config sets (e.g. a `config/production.yml` or a `settings.production` group) — env vars are granular and orthogonal, set per deploy. (The pack already covers secret handling — cross-reference existing secret patterns and extend with config orthogonality.)
- [ ] Mobile tokens stored in `expo-secure-store` — never AsyncStorage or MMKV.
- > **⚠️ Bearer bypass scope — security-critical.** The bypass defaults to `^/api/`, which makes the **entire** `/api/*` surface public (un-2FA'd). If the application authenticates only a **sub-prefix** (e.g. `/api/v1` carries the bearer/internal-token check) while OTHER `/api/*` routes are unauthenticated (legacy / admin / destructive), you **MUST** narrow the bypass with `shape.bearer_bypass_prefix: "^/api/v1"` — otherwise `fabrik apply` exposes those routes to the public internet. **Bypass ONLY the path the app itself authenticates.** Value must start with `^/`; the verifier (`orchestrator/verifier.check_api_bypass`) probes the configured prefix on deploy. When unsure whether a service has un-auth'd `/api/*` routes, ask the app owner before relying on the `^/api/` default.
| Vector search | pgvector on `postgres-main` + `fabrik-lib/rag` — ⚠️ the extension is NOT currently installed there (probed 2026-09-01: `postgres:16-alpine`, `plpgsql` only); a project needing vectors REQUESTS the fleet infra change first, never assumes it | same `postgres-main` DSN |
- Use Pydantic `BaseSettings` (per `10-python.md` § Config Loading) — never raw `os.getenv` **for an
```

## Coverage Checklist

| # | Class | Verdict | Evidence (what/where hunted) |
|---|---|---|---|
| 1 | FLOOR core/35-security-auth (secrets in code / leak paths — `CODE_HOST_URL` values, pool input) | FIXED(2) | CODE_HOST_URL values are host-only by construction (P22 REFUTED); tmp file 0600-first (O13); pool receives scheme://host only (Opus trace) — hunted: gather_envs render, classify flagged_providers/unit_prompt, chain script alert one-liner |
| 2 | FLOOR core/25-data-postgres (registry_sync writes: prune guard, transaction atomicity) | FIXED(2) | prune bounded by name, mass INSERT unguarded by design (Opus measured 0 prunes on first run); `kind` column via idempotent ensure_schema (O4); credential by key role (O5, N1) — hunted: registry_sync sync_registry/parse, schema.sql, gen_dashboard.load |
| 3 | FLOOR core/30-ops (cron-run script error mode, no daemon/PID, logfile discipline) | FIXED(3) | chain script: timeouts, alerts, gated heartbeat, one definition for both entry points (O2, O9, O10, O11); no daemon/PID; logs to the caller's LOG_FILE — hunted: daily_refresh.sh, wsl_startup_hook.sh, external_services_chain.sh |
| 4 | 12-Factor (all twelve axes — one finder owns) | CLEAN | docs finder owned the class: no FileHandler/daemon/sticky/sqlite/alembic-at-startup/host ports/docker exec; `rg` spawn is a box-local cron script, out of II's container scope — hunted: every changed .py/.sh |
| 5 | core/10-python (argparse, subprocess, typing, fail-soft paths in gather_envs/classify/registry_sync/gen_dashboard) | FIXED(4) | argparse on gen_dashboard/registry_sync; subprocess handling of rg exit codes (O3); fail-closed scan (P3); rotation → cursor (O6) — hunted: the four scripts |
| 6 | core/40-documentation (Doc Sync: INDEX/CHANGELOG/CAPABILITIES/new reference doc/README) | FIXED(6) | INDEX/CHANGELOG/CAPABILITIES/capabilities.json/reference doc/README-less reference row; counts corrected (D2, D6); new chain script + schema rows — hunted: every doc in the diff vs the code |
| 7 | core/45-testing-strategy (watched-fail-first; test quality: would tests pass on revert, over-mocking, time bombs) | FIXED(3) | red-on-revert proven for the pass-1 tests (9 fail → 21 pass), N1 red→green; S7 churn test replaces the fixed-N test; `needs_rg` skip is honest not silent (P6) — hunted: 41 tests |
| 8 | core/57-external-data-sourcing (the 12-field profile shape + cell tiers in the 10 spliced blocks; header denominator) | FIXED(5) | 10 spliced blocks: 12+1 rows each (checker), entities unescaped, pipes fixed (D3), Env-keys wording (P5/P26), derivable header (D1), taxonomy row (D7) — hunted: EXTERNAL_SYSTEMS.md whole file via rederive checker |
| 9 | Logic / off-by-one / None-empty handling in the scan (host_sld, ignored_host, PLATFORM_SLD, host_label, provider_for_host) | FIXED(7) | S1, S2, S4, S5, S6, O1, D4 + P4 (2-letter labels); host_sld/HOST_RE/platform root refuted (P10–P15) — hunted: gather_envs scan functions with live data |
| 10 | Idempotency & determinism (daily no-op: sorted output, read_existing_body compare, rg under cron PATH, timeout) | FIXED(1) | sorted rendering, set-keyed hosts → byte-stable body (scan finder traced every path); rg on cron PATH verified; partial-scan rule (O3) — hunted: consolidate/render/read_existing_body |
| 11 | Fail-open vs fail-closed on every gate/guard (bounded prune direction, scan fail-soft, `\|\| echo` non-fatal steps, liveness evidence) | FIXED(4) | fail-closed scan (P3), gated heartbeat (O2), alerts on every failure (P2), prune guards unchanged; race REFUTED (O9) — hunted: every guard in the diff |
| 12 | Cost / quota / limit accounting edges (--max-per-run, sorted head pinned by transport errors, tombstone loop termination, unknown≠0) | FIXED(3) | cursor walk (O6/S3), error budget (O8), platform-label catalog hit (O1) — the spend is bounded by construction now — hunted: classify main/bound_flagged/build_proposals |
| 13 | Boundary / sentinel / prefix collisions (`CODE_HOST_URL` vs SVC_RE/KV_RE/URL_KEY_RE, `\x00` slot key, `   # ` note delimiter, name collisions between env-derived and host-derived providers) | CLEAN | `\x00` internal (P21), `   # ` impossible in a host (P24), CODE_HOST_URL vs SVC_RE/KV_RE/URL_KEY_RE parse (Opus: 305/305 parsed, 0 dropped) — hunted: registry_sync.parse, classify.flagged_providers, gather_envs render |
| 14 | Removed-guard / removed-behavior regressions (orchestrator retired: flock, seen-set, alerting — what is lost) | FIXED(4) | flock/timeout (O9), seen-set → error budget (O8), new-provider alert (O10), gather-after-classify (O7); prune tests survive the deletion (Opus) — hunted: HEAD:refresh_service_inventory.py vs the chain |
| 15 | Cross-file contract breaks (registry_sync main signature, gen_dashboard main(argv), consolidate(code_dirs), daily_refresh step args, capabilities.json regen divergence, CLAIMS `kind` enum) | FIXED(3) | bound_flagged 3-tuple with callers updated; consolidate(code_dirs=None) preserved; registry_sync main is the only entry; capabilities.json regen would not re-add (retired file gone from disk); CLAIMS kind normalised (D5) — hunted: every changed signature + its callers |
| 16 | Behavior-without-a-test (every new behaviour named in the CHANGELOG has a guard) | FIXED(1) | every CHANGELOG-named behaviour has a test (chain order/gating/both entry points, cursor, budget, kind, key role, 0600, partial scan, ambiguity, platform label, Dockerfile/__tests__) — hunted: CHANGELOG entry line by line |
| 17 | Fleet blast radius (CLAIMS.yaml syncs to ~46 repos; hub-only surfaces stay hub-only) | CLEAN | CLAIMS.yaml rows + a header comment only (synced); everything else is hub-only (scripts/, db/, docs/, .fabrik/) — hunted: governance-sync filter vs the staged paths |

## Pass Ledger

| Pass | method | found | new | fixed | finders |
|---|---|---|---|---|---|
| Pass 1 | method: citation | found: 55 | new: 55 | fixed: 35 | finders: pool fanout ×4 (deepseek-v3.2-exp scan · gemini-3-flash chain · qwen3-max contracts · deepseek-v4-flash docs — all scored) + native fabrik-reviewer ×3 (Opus cron/prune/secrets · Sonnet scan+tests · Sonnet docs/CLAIMS/12F; first dispatch of all three died on a session-limit 429 — re-dispatched after the account rotation, dispatched 6 / returned 3, partitions re-covered by the retries) + orchestrator (N1) · adjudicated: 35 FIXED / 20 REFUTED (P7–P25, O12) · → not done (changed code) |
| Pass 2 | method: citation | found: 39 | new: 39 | fixed: 21 | finders: SCOPED on the fix delta (pass-1 state → live, reconstructed from the pinned base sha): pool fanout ×3 (deepseek-v4-flash code · gemini-3-flash ops-tests · qwen3-max docs — 26 raw candidates merged into 18 rows Q1–Q18 (`found:` counts rows needing adjudication, not raw duplicates), all scored) + native fabrik-reviewer Opus ×1 (G1–G20; executed rg, timeout and $? semantics, live SQL) + orchestrator (F1, executed rg glob semantics) · adjudicated: 21 FIXED (F1, Q10, G1–G12, G14–G20) / 18 REFUTED (Q1–Q9, Q11–Q18, G13) · → not done (changed code) |
| Pass 3 | method: re-derivation | found: 30 | new: 30 | fixed: 21 | finders: CLOSING WIDE sweep, non-author: pool fanout ×3 (deepseek-v4-flash code · gemini-3-flash ops-tests · qwen3-max docs — 8 candidates W1–W8, all scored) + native fabrik-reviewer Opus ×1 (H1–H17; executed rg, pg_locks, a reverted copy of gen_dashboard, the capability generator) + native Sonnet ×1 (C1–C3; counted tests live vs HEAD, re-derived the header) + orchestrator re-derivation of every count and anchor from primary sources (`rederivation.txt`: 45 repos / 371 hosts / 143 blocks + 7 retired = 150 / 116 fleet-used / 109 catalog vendors / 51→53 tests / D-081 anchors all exist / both entry points run the chain) · adjudicated: 21 FIXED (C1–C3, H2–H17 minus H1, N2, N3) / 8 REFUTED (W1–W8) / 1 ROUTED (H1 → G12) · → not done (changed code) |
| Pass 4 | method: re-derivation | found: 4 | new: 4 | fixed: 2 | finders: FINAL fresh sweep on the worktree-vs-HEAD surface (the shared index held a sibling's set): pool fanout ×3 (deepseek-v4-flash code · gemini-3-flash ops-tests · qwen3-max docs — 26 raw candidates, 23 cite standing rows → V4, 3 rows V1–V3; all scored) + native Sonnet ×1 (non-author; report pending at the time of the operator's commit-now ruling — adjudicated in Pass 5) · adjudicated: 2 FIXED (V1, V3) / 2 REFUTED (V2, V4) · → not done (changed a shared file + a ruling landed) |
| Pass 5 | method: re-derivation | found: 12 | new: 12 | fixed: 6 | finders: on commit `c2b1ad2f` + the post-commit delta: pool fanout ×3 wide (X1: 14 candidates, all citing standing rows; scored) + pool fanout ×3 scoped on the guard delta (Y1–Y6; scored) + native Sonnet ×1 (non-author, T1–T5: the H3 mirror measured live) + native Opus ×1 (non-author; report adjudicated in Pass 6 if it raises anything) + orchestrator (N4) · adjudicated: 6 FIXED (N4, T1–T5) / 6 REFUTED (Y1–Y6) — X1's 14 cite standing rows and are not counted · → not done (changed code: T1/T2/T4 + N4) |
| Pass 6 | method: citation | found: 16 | new: 16 | fixed: 16 | finders: native Opus ×1 on c2b1ad2f (Z1–Z15; executed a live env sweep, an in-process `--only` run, the capability generator, `git log -S`) + native Sonnet ×1 on the post-commit delta (S6; two red-on-revert experiments: the mirror fix RED when reverted, the dead branch a no-op) + pool fanout ×3 scoped on the delta (Y-class residue: none new) · adjudicated: 16 FIXED / 0 REFUTED · → not done (changed code) |
| Pass 7 | method: re-derivation | found: 18 | new: 18 | fixed: 15 | finders: full fresh sweep on `c2b1ad2f~1..f2370abd`: pool fanout ×3 wide (AA1: 10 candidates, all standing rows / outside the surface; scored) + native Sonnet ×1 (AB1–AB3; executed the live consolidation and hand-simulated laps) + native Opus ×1 (AC1–AC14; executed a live env sweep, an isolated revert of Z2, the capability generator, `final_gate --check` → success) — both non-author · adjudicated: 15 FIXED (AB1–AB3, AC1, AC2, AC3=AB1, AC4=AB2, AC5, AC6, AC7, AC10, AC11, AC13, AC14) / 1 REFUTED (AC12) / 2 ROUTED (AC8, AC9 → intel) · → not done (changed code) |
| Pass 8 | method: re-derivation | found: 20 | new: 20 | fixed: 18 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..8fec476e -- <my paths>`: pool fanout ×3 wide (AE1: 5 candidates, sibling CHANGELOG context + G9 by design; scored) + native Sonnet ×1 (AD1–AD2; live scan, live SELECTs) + native Opus ×1 (AF1–AF15; three sandboxed neutered-copy proofs of vacuous graders, a live 21-file sweep, `liveness_audit`, `crontab`) — both non-author · adjudicated: 18 FIXED (AD1, AD2, AF1–AF14) / 2 REFUTED (AE1, AF15) · → not done (changed code) |
| Pass 9 | method: re-derivation | found: 19 | new: 19 | fixed: 15 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..fae20651 -- <my paths>`: pool fanout ×3 wide (AI1: 4 candidates — sibling CHANGELOG context + G9 by design; scored) + native Sonnet ×1 (AH1–AH2; live registry SELECTs) + native Opus ×1 (AJ1–AJ13; a live 21-env `consolidate()` under BOTH the committed and the worktree `is_credential`, four sandboxed neutered-copy proofs, a `timeout -k` exit-code probe, an independent re-derivation of 143/7/116/109/45) — all non-author · adjudicated: 15 FIXED (AH1, AH2, AJ1–AJ13) / 4 REFUTED (AI1) · AF6 re-ruled NOT FIXED → re-fixed under AJ5 · → not done (changed code) |
| Pass 10 | method: re-derivation | found: 17 | new: 17 | fixed: 12 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..b5e5ab8f -- <my paths>`, every finder pinned to the sha (sandbox extract + provenance assert — the pass-9 machinery lesson applied): pool fanout ×3 wide (AK1: 5 candidates, all sibling context / G9; code + docs units empty; scored 3/2/3) + native Sonnet ×1 (AL1; 4 sandbox red-on-revert proofs, 7 counts re-derived, 0 doc/code mismatches) + native Opus ×1 (AM1–AM10; a 1084-name × 9-shape `is_credential` matrix old-vs-new, a rendered-page XSS proof, 241 shell lines diffed against a quote-aware stripper) — all non-author · adjudicated: 12 FIXED (AL1, AM1–AM4, AM6–AM11, AM5 cited to AL1) / 5 REFUTED (AK1) · → not done (changed code) |
| Pass 11 | method: re-derivation | found: 15 | new: 15 | fixed: 9 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..c6b6916d -- <my paths>`, finders pinned to the sha in sandbox extracts (`scripts tests libs .fabrik`): pool fanout ×3 wide (AN1: 5 candidates, sibling context / G9 / a pre-existing localhost viewer; code + docs units empty; scored 3/2/3) + native Sonnet ×1 (AO1–AO2; 4 sandbox red-on-revert proofs, 6 counts re-derived live) + native Opus ×1 (AP1–AP8; an end-to-end `cpill` injection proof through `svc_line`→`SVC_RE`→`render`, a fail-open parse reproduced with a three-provider fixture, a 24-provider tie census, 200 000-value regex measurements) — all non-author · adjudicated: 9 FIXED (AO1, AP1–AP6, AP8 + AP7 cited to AO1) / 1 ACCEPTED-AS-STATED (AO2, an immutable commit message) / 5 REFUTED (AN1) · → not done (changed code) |
| Pass 12 | method: re-derivation | found: 16 | new: 16 | fixed: 11 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..4ac17e0f -- <my paths>`, finders pinned in sandbox extracts: pool fanout ×3 wide (AR1: 5 candidates — sibling context / G9 / an unevidenced inference; code + docs units empty; scored 3/2/3; the first two dispatches errored in every unit on a shadow `h11.py` a pass-11 finder had left in the scratchpad) + native Sonnet ×1 (AQ1–AQ2; 4 sandbox red-on-revert proofs, 7 counts re-derived incl. a self-refuted 17→12 tie census) + native Opus ×1 (AS1–AS9; a stubbed-fanout daily run reproducing the re-billing loop, a 649-pair anchor diff, a 65-value `#svc` fuzz, five node/regex executions) — all non-author · adjudicated: 11 FIXED (AQ1, AQ2, AS1–AS9) / 5 REFUTED (AR1) · → not done (changed code) |
| Pass 13 | method: re-derivation | found: 23 | new: 23 | fixed: 14 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..ef1146fa -- <my paths>`, finders pinned in sandbox extracts: pool fanout ×3 wide, RE-DISPATCHED with per-partition routed hunks after its own MACHINERY line exposed that the inlined diff had been cut inside `capabilities.json` since pass 10 (AV1: 3 code candidates refuted by execution, 5 ops candidates sibling context / by design / inferred, 1 docs candidate stale; scored 2/2/2) + native Sonnet ×1 (AT1–AT3; 6 sandbox red-on-revert proofs, a node-absent skip probe, `tar -tv` on the symlink claim) + native Opus ×1 (AU1–AU11; a stubbed-fanout daily run through the merge path, a 649-pair anchor diff, 56 regex probes, a 711-line stripper diff) — all non-author · adjudicated: 14 FIXED (AT1–AT3, AU1–AU11 — AU8 partially) / 9 REFUTED (AV1) — the row first said 24/15, an off-by-one the pass-14 Sonnet finder caught (AX2) · → not done (changed code) |
| Pass 14 | method: re-derivation | found: 28 | new: 28 | fixed: 12 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..adae4169 -- <my paths>`, finders pinned in sandbox extracts: pool fanout ×3 wide, routed (AW1 — the code unit's first REAL finding since routing: the shared secrets tmp race; AZ1: 4 code / 7 ops / 5 docs refuted; scored 4/2/1) + native Sonnet ×1 (AX1–AX2; 10 of 10 pass-13 graders proven red-on-revert, 6 counts re-derived) + native Opus ×1 (AY1–AY9; an `esc`-removed run of the node graders, a cell-bypass mutation, an end-to-end `__API_KEY` re-bill proof, a two-writer tmp probe) — all non-author · adjudicated: 12 FIXED (AW1, AX1, AX2, AY1–AY4, AY7, AY8 — AY5/AY6 folded into AW1, AY9 removed) / 16 REFUTED (AZ1's 4 + 7 + 5; AY5/AY6's lock halves are noted inside their FIXED rows, not counted twice) — the row first said 25 found; 12 + 16 = 28, corrected in place (BB3, the AX2 class again) · → not done (changed code) |
| Pass 15 | method: re-derivation | found: 27 | new: 27 | fixed: 12 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..398fefe9 -- <my paths>`, finders pinned in sandbox extracts: pool fanout ×3 wide, routed (BB4 status default + the BA1 sweep race found independently; BC1: 4 code / 6 ops / 4 docs refuted; scored 4/2/1) + native Sonnet ×1 (BA1–BA3; 6 red-on-revert proofs, a two-writer sweep reproduction, a no-change-day reproduction) + native Opus ×1 (BB1–BB9; a 24 h-old same-pid leftover, a broken-symlink/directory sweep probe, a two-loopback-server redirect proof with a sentinel key, a `http://a]b` classify crash reproduced through `main`) — all non-author · adjudicated: 12 FIXED (BA1, BA2, BA3, BB2–BB9; BB1 = BA2) / 15 REFUTED (BC1 + the parts of BB3 that asked to skip a directory silently — a directory IS skipped, by the guard) · → not done (changed code) |
| Pass 16 | method: re-derivation | found: 22 | new: 22 | fixed: 11 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..7d6d80ac -- <my paths>`, finders pinned in sandbox extracts: pool fanout ×3 wide, routed (BF1: 4 code / 6 ops refuted, docs unit empty — its first candidate was an artefact of the 70k cap cutting a regex, so the cap is now 140k; scored 2/1/3) + native Sonnet ×1 (BD1–BD3; 8 red-on-revert proofs, a standalone symlink repro) + native Opus ×1 (BE1–BE9; a `urlopen` call-site revert left the redirect grader green, a 1-minute-old same-pid leftover, scalar `match` on both sides, five redirect codes measured) — all non-author · adjudicated: 11 FIXED (BD1, BD2, BE2, BE3, BE4, BE5, BE7, BE8, BE9 + BE1 = BD1, BE6 = BD4 — BD4 itself is an OWN re-derivation row, not a finder candidate) / 1 ACCEPTED (BD3) / 10 REFUTED (BF1) — the row first said 25/11/14, which its own id list did not support (BG2, the AX2/BA3 class a third time; the recount rule from here: candidates = finder ids + pool items, own rows excluded) · → not done (changed code) |
| Pass 17 | method: re-derivation | found: 23 | new: 23 | fixed: 10 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..3347dd6c -- <my paths>` (the deleted orchestrator dropped from the list), finders pinned in sandbox extracts: pool fanout ×3 wide, routed at 140k (BI1: 2 code / 6 ops / 4 docs refuted; scored 1/1/1) + native Sonnet ×1 (BG1–BG2; 7 of 7 pass-16 graders proven red-on-revert) + native Opus ×1 (BH1–BH8; an end-to-end merged-key-to-wrong-vendor reproduction with a stubbed fanout, a catalog-after-dispatch reproduction, 14 URL shapes, six status codes against a loopback server) — all non-author · adjudicated: 10 FIXED (BG1, BH1–BH8 + BG2 corrected in place) / 1 ACCEPTED (BH7's directory half) / 12 REFUTED (BI1) · → not done (changed code) |
| Pass 18 | method: re-derivation | found: 29 | new: 29 | fixed: 12 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..27923d70 -- <my paths>`, finders pinned in sandbox extracts (`db` + the reference doc archived too; ledger materialised once): pool fanout ×3 wide, routed (BL1: 5 code / 6 ops / 5 docs refuted; scored 2/1/1) + native Sonnet ×1 (BJ1–BJ3; the shipped `merged_match` reader neutered → 94 passed) + native Opus ×1 (BK1–BK10; a fake-DB probe proving the gate fails OPEN on an unreadable catalog, a same-batch merge-target reproduction against both shas, a winning-prefix collision matrix, cursor-before-probe timing) — all non-author · adjudicated: 12 FIXED (BJ1, BJ2, BJ3, BK1–BK6, BK8, BK9, BK10 + BJ4 folded into BK3, BK7 into BK1) / 1 ACCEPTED (BK5's NaN half is redundant with the argument order — kept explicit) / 16 REFUTED (BL1) · → not done (changed code) |
| Pass 19 | method: re-derivation | found: 30 | new: 30 | fixed: 14 | finders: full fresh sweep on the PATH-FILTERED cumulative surface `c2b1ad2f~1..ff3af096 -- <my paths>` (the deleted `test_refresh.py` dropped from the list): pool fanout ×3 wide, routed (BO1: 10 code — FIVE real: typed state files, the TLD-blind wildcard, `--only` exit, a comma in a url, a directory at the dashboard tmp; 8 ops + 3 docs refuted; scored 4/1/1) + native Sonnet ×1 (BN1 = BM1 found independently; 7 of 7 pass-18 graders proven red-on-revert) + native Opus ×1 (BM1–BM9; `owned_by` probed on the shipped grader's own fixture, an order-dependence matrix, a 129-warning degradation reproduction) — all non-author · adjudicated: 14 FIXED (BM1–BM9, BO1's five) / 16 REFUTED (BO1's rest) · → not done (changed code) |
| Pass 20 | method: re-derivation | found: 27 | new: 26 | fixed: 10 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..25eabb28 -- <my paths>`, finders pinned in sandbox extracts: pool fanout ×3 routed (BP1: 11 code / 4 ops / 0 docs — all 15 refuted, one after a fleet-wide measurement; scored 1/1/2) + native Sonnet ×1 (BQ1–BQ3; the pass-19 graders mutation-tested — BQ1's escape found by running the shipped and a fixed tie-break through the same test; BQ2 found as 4 live rows in the DB) + native Opus ×1 (BR1–BR9; an e2e `ValueError` through `consolidate()`, the order matrix on a neutered pre-BM1 copy, 18 fuzzed urls through `svc_line` → `SVC_RE`, C5 recomputed over 533 hosts = 0 changes, `owned_by` over 128 live credentials = 0 cross-vendor wins, a 5-of-434 digest-collision census) — all non-author · adjudicated: 10 FIXED (BQ1–BQ3, BR1, BR3–BR8) + 1 DUPLICATE (BR2 = BQ1, folded — the one non-new row) / 1 ACCEPTED (BR9, standing BH7) / 15 REFUTED (BP1) — 27 rows (corrected in pass 21, BS1: the row first said found 26 / fixed 11, counting BR2 in one tally and not the other) · live after: 0 of 109 prefixes tie, 0 merged, `gather_envs --apply` regenerated the stale 12:00 file (434 services / 802 key rows — 128 credential · 141 config · 533 code-host · 0 unattributed; the sync's own `808 api_keys` stat counts INSERTS, six same-value name pairs collapsing to one row each — corrected in pass 21, BS1), deepl's cost tier restored · 130 tests green · → not done (changed code) |
| Pass 21 | method: re-derivation | found: 38 | new: 36 | fixed: 17 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..19868ff0 -- <my paths>` (the review ledger dropped from the finder path list), finders pinned in sandbox extracts: pool fanout ×3 routed (BT1: 14 code / 5 ops / 0 docs — all 19 refuted; scored 1/1/2) + native Sonnet ×1 (BS1: two denominators in the pass-20 row, re-derived from the row itself; 3 of the pass-20 graders mutation-tested red, the DB graders traced) + native Opus ×1 (BS2–BS17 + the BS1 duplicate; executed: all four unreadable-catalog shapes through `main()` with the 22-of-87 prune blast radius, the label fallback with real matchers over 109 vendors, the `EXA`/`EXA_` matrix in both orders, the tie's tombstone/merge inertness, BR8's floor with node removed from PATH, 820-vs-814 on a fresh consolidation, 6 catalog states through `catalog_provenance`, `date` vs `date -u`) — all non-author · adjudicated: 17 FIXED (BS1 in place + BS2–BS17) + 2 DUPLICATES (Opus's copies of BS1's two items) / 19 REFUTED (BT1) — 38 candidates (corrected in pass 22, BV1: the row first said 34/33, counting BS1's two items as one and one duplicate instead of two) / BS14's concurrency half ACCEPTED after measuring · live after: 0 of 109 prefixes collide after token normalisation, `load_catalog` warns on 0 ties, `gather_envs --apply` lost 0 attributions of 555 code hosts, 133 tests green · → not done (changed code) |
| Pass 22 | method: re-derivation | found: 31 | new: 30 | fixed: 16 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..47cd3c64 -- <my paths>`, finders pinned in sandbox extracts: pool fanout ×3 routed (BT2: 10 code / 5 ops / 0 docs — 14 refuted, BU1 real; scored 1/2/2) + native Sonnet ×1 (BV1–BV5; 5 pass-21 graders mutation-tested red, the DB graders traced; two vacuous graders and two un-derivable numbers found) + native Opus ×1 (BW1–BW11 + the BV1/BV4 duplicates; executed: old-vs-new attribution over 555 live hosts = 14 changed, a synthetic youtube-only catalog to NEEDS-TRIAGE, the `TypeError` on list columns, 22 env files from a relocated copy, the curated/merged `EXA_`/`EXA` matrix, 6 catalog shapes × 3 consumers, 4 new prints × 2 sinks for content leaks = 0) — all non-author · adjudicated: 16 FIXED (BU1, BV1–BV5, BW1–BW9, BW11) + 1 DUPLICATE (BW10 = BV4) / 14 REFUTED (BT2) — 31 candidates: 15 pool + 5 Sonnet + 11 Opus (corrected in pass 23, BX2: the row first said 35/33/18 with a phantom duplicate — the same defect as BV1, one pass later) · live after: 0 attributions lost with the 8 vendors' `hosts` curated (14 of 564 hosts at 7442058b rescued by them; the host population drifts run to run — 555 → 561 → 564 across passes 21–23 — so every count carries its sha, BX7), 0 ties, 140 tests green · → not done (changed code). **Convergence note (operator asked at 12 h):** rounds 19–22 found 30 → 27 → 38 → 35 and fixed 14 → 10 → 17 → 18 — not decaying; about half of each round's fixes are latent classes with a measured 0 live fire-rate. From round 23 the bar is the FIX DIRECTIVE's own verb 5: CONFIRMED or live-impact findings are fixed; PLAUSIBLE latent findings with a measured zero fire-rate get an ACCEPTED row with the measurement, not code |
| Pass 23 | method: re-derivation, under the tight bar | found: 59 | new: 57 | fixed: 9 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..7442058b -- <my paths>`, finders pinned in sandbox extracts, briefs demanding a MEASURED fire-rate per candidate and a LIVE/LATENT split: pool fanout ×3 routed (BY1: 34 code — the deepseek provider stalled and the batch ran partial-tolerant — / 8 ops / 0 docs, all 42 refuted; scored 1/1/2) + native Sonnet ×1 (2 LIVE doc-drift items + 1 LATENT; 9 of 10 pass-22 graders mutation-tested or traced clean) + native Opus ×1 (7 LIVE: BX1–BX7; 7 LATENT: BX8 fixed for its consequence, BX9 fixed for its size, 5 accepted; executed: the `--only` paid no-op through `main()`, the merge-minted tie end to end through `cs.main()` then `ge.main() == 1`, the core-step one-word mutation against BU1, 563/564 host population, 0 of 346 triage names colliding with curated tokens) — all non-author · adjudicated: 9 FIXED rows (BX1–BX9, covering 11 candidates: BX4 folds Sonnet's two doc items with Opus's one) / 6 ACCEPTED with measurements (BZ1) / 42 REFUTED (BY1) — 59 candidates, 2 duplicates (Sonnet's two doc items = Opus's BX4) · live after: 0 alien-domain warnings of 564 hosts, 0 ties, 141 tests green · code-changing fixes this round: **3** (BX1 + BX8 in `classify_services.py`, BX5 in the chain script — down from 16/17/18 in rounds 20–22; BX9 is grader-only, corrected in pass 24, CA6); the rest are docs, ledger and graders · → not done (changed code) |
| Pass 24 | method: re-derivation, tight bar | found: 22 | new: 19 | fixed: 8 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..dd55ca81 -- <my paths>`, finders pinned in sandbox extracts: pool fanout ×3 routed (CB1: 5 code / 6 ops / 0 docs, all 11 refuted; scored 1/1/2) + native Sonnet ×1 (3 LIVE: the exit-1 decode, the Pass 23 composition, the CHANGELOG omission; 1 LATENT: BY1's denominator; 6 pass-23 graders mutation-tested red) + native Opus ×1 (3 LIVE + 4 LATENT, all executed: `FAIL_SYNC` under bash, `--only kilo` refused, the curated `captcha` deletion with its red-on-revert at 7442058b, the merged-holder tie end to end, 16-of-109 / 3-of-109 enabling measurements, `-` in 0 of 109 prefixes and 0 of 537 keys) — all non-author · adjudicated: 8 FIXED (CA1–CA8, covering 11 candidates: CA1 folds three, CA3+CA5 fold Opus's latent pair and its grader note) / 11 REFUTED (CB1) — 22 candidates (11 pool + 4 Sonnet + 7 Opus), 3 duplicates counted inside them (corrected in pass 25, CC6: the row first said 25/22) · **the verdict on round 23:** its premise (BX1) was false and its pop (BX8) over-reached — both reverted to the narrow, correct shape this round; net code-changing fixes: 2 files (`classify_services.py`, the chain script) · live: 0 of 346 triage names are catalog keys, 0 merged prefixes, 141 tests green · → not done (changed code) |
| Pass 25 | method: re-derivation, tight bar, every fix's stated caller executed | found: 36 | new: 34 | fixed: 6 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..32493a98 -- <my paths>`, finders pinned in sandbox extracts with the round's SINGLE commit as the delta: pool fanout ×3 routed (CD1: 7 / 6 / 6, all 19 refuted; scored 1/1/1) + native Sonnet ×1 (1 LIVE — the ungraded pop guard; 1 LATENT — CA2's numbers; 7 mutations run, 5 red) + native Opus ×1 (8 LIVE + 7 LATENT, all executed: the two-lap re-bill on a curated-in-triage entry, `--only` on it paid and byte-identical, the CA5 orphan's `url_index == {}`, ENOSPC through `main()`, the alert argv 896/1/11, six derivations of CA2's numbers) — all non-author · adjudicated: 6 FIXED (CC1–CC6, covering 15 candidates: CC1 folds Opus #2/#9, CC2 both finders' pop-guard items, CC3 #10/#11, CC4 #13, CC5 #3/#4/#7/#8/#15, CC6 #5/#6 and Sonnet's latent) / 2 ACCEPTED (CE1's first two; its third item was pass 23's) / 19 REFUTED (CC7) — 36 candidates, 2 duplicates (the pop guard ×2, CA2's numbers ×2) (corrected in pass 26, CD6: the row first said 13 / 3 / 3 duplicates / new 33) · **the root of rounds 22–25's oscillation is closed one branch upstream**: the classifier never receives a curated name, so the keep/pop argument has no subject · code-changing files: 3 (`gather_envs.py` +1 branch, `classify_services.py` −1 keep −1 orphan, the chain script's body) · live: 107 catalogued / 346 triage on disk (a fresh dry-run said 347 — a sibling's new host; 0 of 346 blocks named for a catalogued vendor today), 142 tests green · → not done (changed code) |
| Pass 26 | method: re-derivation, tight bar, stated callers executed | found: 33 | new: 30 | fixed: 6 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..993872de -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta: pool fanout ×3 routed (CF1: 7 / 5 / 4, all 16 refuted; scored 1/1/1) + native Sonnet ×1 (2 LIVE + 5 LATENT; 5 mutations red) + native Opus ×1 (6 LIVE + 4 LATENT, all executed: the tie on the firing shape with its red-on-revert, the model overwriting a placeholder's operator fields, `DEEPL_TOKEN` adopted end to end, the write failure's traceback, 5 rendered body lengths, 1082 live keys → 6 derive-path → 0 adopt) — all non-author · adjudicated: 6 FIXED (CD1–CD6, covering 14 candidates: CD4, CD5 and CD6 each fold both finders' copies, CD6 also the two docstring/comment rewordings) / 3 ACCEPTED (CG1) / 16 REFUTED (CF1) — 33 candidates, 3 duplicates (corrected in pass 27, CE8: the row first split 12 / 5) (the write traceback, the body length, the 347/arithmetic pair), new 30 · code-changing files: 3 (`classify_services.py` one rule + the keep, `gather_envs.py` the summary line + the write wrap, the chain body) · live: 107 / 346 unchanged, 144 tests green · → not done (changed code) |
| Pass 27 | method: re-derivation, tight bar, stated callers executed | found: 32 | new: 30 | fixed: 8 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..f11022ca -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta: pool fanout ×3 routed (CH1: 5 / 7 / 4, all 16 refuted; scored 1/1/1) + native Sonnet ×1 (1 LIVE — the duplicated row id; 2 LATENT; 6 mutations red, all 13 pass-26 graders real) + native Opus ×1 (5 LIVE — all ledger/grader hygiene, no code defect; 8 LATENT — four latent re-bill/erase loops in CD2's own class, all executed over 3 laps) — all non-author · **no LIVE code defect this round** (the first such round): the live items were a duplicated row id, an ungraded dry-run line, a stale number, two false "reworded" claims and a wording; the code changes close latent loops CD2 created (a `?` placeholder merged away, a list category copied back, the tombstone branch erasing the operator's words) · adjudicated: 8 FIXED (CE2–CE9, covering 14 candidates: CE7 folds both finders' copies and Opus's doc claim, CE8 folds five ledger items) / 2 ACCEPTED (CI1) / 16 REFUTED (CH1) — 32 candidates, 2 duplicates (the 495 and the `newvendor` claim, each by both finders) · code-changing files: 2 (`classify_services.py`, `gather_envs.py`; the chain script untouched) · live: 107 / 346 unchanged, 145 tests green · → not done (changed code) |
| Pass 28 | method: re-derivation, tight bar, every class swept with a denominator | found: 21 | new: 21 | fixed: 5 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..a2a6c2f9 -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta and every one of 12 classes reported clean-or-candidate with its denominator: pool fanout ×3 routed (CK1: 0 / 5 / 3 — the code finder stalled; all 8 refuted; scored 0/1/1) + native Sonnet ×1 (1 LIVE — the doc's "one of five"; 1 LATENT; 7 mutations red) + native Opus ×1 (5 LIVE — one real code gap (CJ1), one ungraded line, two grader/text mismatches, one CHANGELOG qualifier; 6 LATENT — one fixed for its cost (CJ3), one folded into CJ2 (#7), four accepted; Sonnet's latent doc wording fixed under CJ4; 12 classes tabulated with denominators: two live consolidations byte-identical, 4 three-lap classify scenarios, 6 catalog refusals + 4 other exit paths, 7 mutations) — all non-author · adjudicated: 5 FIXED (CJ1–CJ5, covering 9 candidates: CJ2 folds Opus #2/#7, CJ4 folds Sonnet's two doc items and Opus #4, CJ5 folds Opus #3/#5) / 4 ACCEPTED (CL1) / 8 REFUTED (CK1) — 21 candidates (8 pool + 2 Sonnet + 11 Opus), 0 duplicates (corrected in pass 29, CM1: the row first said 25 / 23 / 11 / 2) · code-changing files: 2 (`gather_envs.py` one handler, `classify_services.py` one predicate + one guard; the chain body one char longer at 496 rendered) · live: 107 / 346 unchanged, 145 tests green · → not done (changed code) |
| Pass 29 | method: re-derivation, tight bar, every class swept with a denominator | found: 38 | new: 36 | fixed: 7 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..68918881 -- <my paths>`, finders pinned in sandbox extracts (the liveness registry linked) with the round's single commit as the delta: pool fanout ×3 routed (CN1: 11 / 7 / 5 — 22 refuted, 1 real (the INDEX `kind` row); scored 1/1/1) + native Sonnet ×1 (2 LIVE — both ledger arithmetic; 1 LATENT; 5 mutations red) + native Opus ×1 (5 LIVE — three grader gaps, the doc's curated-shape sentence and the same arithmetic; 7 LATENT — a six-site predicate matrix, 8 exit paths through `main()`, 2000 property trials on the cursor, 32 print sites audited, 0 of 252 live values in the dry-run output; 12 classes tabulated) — all non-author · **no LIVE code defect** (pass 28's CJ1 broke pass 27's — this is the first clean round since); the live items were grader coverage and ledger arithmetic; the code changes unify one predicate and type one exception · adjudicated: 7 FIXED (CM1–CM7, covering 15 candidates) / 1 ACCEPTED (CO1) / 22 REFUTED (CN1) — 38 candidates (23 pool + 3 Sonnet + 12 Opus), 2 duplicates (the arithmetic, both finders; the doc-count pin, both finders) · code-changing files: 2 (`gather_envs.py`: one helper, one tuple, one handler split, one anchored regex; `classify_services.py`: one predicate) · live: 107 / 346 unchanged, 147 tests green · → not done (changed code) |
| Pass 30 | method: re-derivation, tight bar, every class swept with a denominator | found: 23 | new: 21 | fixed: 5 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..8379491f -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta: pool fanout ×3 routed (CQ1: 1 / 6 / 0, all 7 refuted; scored 1/1/2) + native Sonnet ×1 (0 LIVE, 3 LATENT — all text; 8 mutations red) + native Opus ×1 (9 LIVE — one re-opened exit path, one ungraded copy, two weak grader loops, five texts; 4 LATENT — the predicate family's last three sites and the guard anchor; 24 shape×surface combinations, 12 classes tabulated, 120 renders max 496) — all non-author · **no LIVE production defect for the second round running**; every fix this round is a consequence of the previous round's fix (the predicate family closed at its last three sites, the fifth exit-1 cause CM5 created, the anchor CM6 left loose) or a grader/text · adjudicated: 5 FIXED (CP1–CP5, covering 16 candidates) / 0 ACCEPTED / 7 REFUTED (CQ1) — 23 candidates (7 pool + 3 Sonnet + 13 Opus), 2 duplicates (the Pass 29 arithmetic; the stale "ONE"), new 21 · code-changing files: 2 (`gather_envs.py`: the predicate, two call sites (a third, the adoption path, was a uniformity edit with no observable behaviour — declared inert, CS4), one handler, one anchor; `classify_services.py`: the copy) · live: 107 / 346 unchanged, 147 tests green · → not done (changed code) |
| Pass 31 | method: re-derivation, tight bar, every class swept with a denominator | found: 18 | new: 16 | fixed: 4 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..7f9fd4a5 -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta: pool fanout ×3 routed (CT1: 2 / 6 / 0, all 8 refuted; scored 1/1/2) + native Sonnet ×1 (0 LIVE, 2 LATENT — both text; 8 mutations red) + native Opus ×1 (3 LIVE — all text; 5 LATENT — the `" ? "` shape, the swallowed permission error, two grader escapes, one inert site; 22 mutants, 12 classes tabulated, 30 renders max 496) — all non-author · **no LIVE production defect for the third round running**; the code changes: a stripped comparison (two sites), one `raise`; the rest graders and text · adjudicated: 4 FIXED (CS1–CS4, covering 10 candidates) / 0 ACCEPTED / 8 REFUTED (CT1) — 18 candidates (8 pool + 2 Sonnet + 8 Opus), 2 duplicates (the CP2 attribution, both finders; the ledger arithmetic, both) · code-changing files: 2 (`gather_envs.py`: two lines + one `raise`; `classify_services.py`: one line) · live: 107 / 346 unchanged, 148 tests green · → not done (changed code) |
| Pass 32 | method: re-derivation, tight bar, every class swept with a denominator | found: 22 | new: 19 | fixed: 5 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..f2eead0f -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta: pool fanout ×3 routed (CV1: 5 / 6 / 0, all 11 refuted; scored 1/1/2) + native Sonnet ×1 (0 LIVE, 0 LATENT, 3 TEXT; 10 mutations red) + native Opus ×1 (1 LIVE-reachable — CS2's `IsADirectoryError` mirror; 4 LATENT — the vanished-file drop, the seventh predicate copy in `tombstone_of`, the ungraded bucket strip, the `(` escape; 2 TEXT; 54 shape×surface combinations, 100 renders max 496, 12 classes tabulated) — all non-author · **one live-reachable regression of the previous round (CU1); CU3's list/blank divergence dates to passes 29–30 (`real_category` minted at pass 29 with `tombstone_of` untouched), only its padded-`?` shapes to pass 31 (corrected at pass 33, CW3)** · adjudicated: 5 FIXED (CU1–CU5, covering 11 candidates) / 0 ACCEPTED / 11 REFUTED (CV1) — 22 candidates (11 pool + 3 Sonnet + 8 Opus — 8 tagged candidates re-counted from the finder's message at pass 33; the class breakdown above enumerates 7 of them), 3 duplicates (the doc's third clause, both finders; the padded `?` sentence, both; the streak, Sonnet only — counted once), new 19 · code-changing files: 2 (`gather_envs.py`: the `is_file` guard, one `except`; `classify_services.py`: one import, `tombstone_of` and the loop on the shared predicate) · live: 107 / 346 unchanged, 148 tests green · → not done (changed code) |
| Pass 33 | method: re-derivation, tight bar, every class swept with a denominator | found: 35 | new: 34 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..31983d88 -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta: pool fanout ×3 routed (CX1: 9 / 7 / 4, all 20 refuted; scored 1/1/1) + native Sonnet ×1 (0 LIVE, 2 LATENT, 4 TEXT; 5 mutations red, row ids unique (the finder's '68' was its bounded grep; 311 of 311 re-derived at pass 34), 107 / 346 re-derived live) + native Opus ×1 (2 CONFIRMED holes in CU1's own guard — `is_file()` swallowing ENOENT and re-raising EACCES as a traceback — + 1 PLAUSIBLE Telegram parity; 4 LATENT; 2 TEXT; 13 category shapes × 5 path shapes executed, 2 consolidations byte-identical over 21 files, 117 tests (the three DB-free suites at 31983d88; the finder's '4 orderings' is not reproducible — pytest-randomly is not installed — 2 orderings re-verified at pass 34), 34 print sites in the two changed files 0 value-bearing, 10 renders max 496) — all non-author · **two confirmed defects in the previous round's fix (CW1), nothing older** · adjudicated: 3 FIXED (CW1–CW3, covering 10 candidates) / 4 ACCEPTED with measurements (CW4) / 21 REFUTED (CX1) — 35 candidates (20 pool + 6 Sonnet + 9 Opus), 1 duplicate (the root-skip test, both natives — counted once), new 34 · code-changing files: 1 (`gather_envs.py`: the stat guard, the one-line helper at three sites) + a comment in `classify_services.py` · live: 107 / 346 unchanged, 151 tests green (96 → 99: one grader split into four) · → not done (changed code) |
| Pass 34 | method: re-derivation, tight bar, every class swept with a denominator | found: 27 | new: 26 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..471125ab -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta; the pool brief now carries the ledger's REFUTED/ACCEPTED rows (156 rows, 14 k chars): pool fanout ×3 routed (CZ1: 3 / 5 / 4, all 12 refuted — 4 restated ledger rows; scored 1/1/1) + native Sonnet ×1 (0 LIVE, 1 LATENT, 3 TEXT; 16 mutation×test combinations, 311 row ids unique) + native Opus ×1 (1 LIVE CONFIRMED — the heartbeat; 5 LATENT; 5 TEXT; 5 path shapes, 2 consolidations byte-identical, 41 print sites 0 value-bearing, 25 alert renders max 496) — all non-author · **one live design defect older than the review (the heartbeat's evidence path, from the original staged diff) and one consequence of CW1's comment** · adjudicated: 3 FIXED (CY1–CY3, covering 13 candidates) / 1 ACCEPTED with measurement (CY4) / 13 REFUTED (CZ1) — 27 candidates (12 pool + 4 Sonnet + 11 Opus), 1 duplicate (the call-site count, both natives — counted once), new 26 · code-changing files: 2 (`external_services_chain.sh`: the stamp; `gather_envs.py`: the no-files line) + the registry's evidence path + a comment · live: 107 / 346 unchanged (348 flagged on a fresh consolidation — drift), 153 tests green (99 → 101; the chain's 13 gained assertions) · → not done (changed code) |
| Pass 35 | method: re-derivation, tight bar, every class swept with a denominator | found: 21 | new: 20 | fixed: 4 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..ccd19323 -- <my paths>`, finders pinned in sandbox extracts with the round's single commit as the delta, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DB1: 1 / 4 / 1, all 6 refuted; scored 1/1/1) + native Sonnet ×1 (1 LIVE — the doc's header line; 1 LATENT; 16 mutation×test combinations red, the CY1 box facts re-derived) + native Opus ×1 (3 LIVE CONFIRMED — the auditor's cron line, the per-label alert clause, the 0-unattended-runs denominator; 6 LATENT; 4 TEXT; 11 mutations, 17 return sites, 25 alert renders max 496, 3 orderings) — all non-author · **one live defect older than the review (the weekly liveness cron, 3 weeks dark), one false alert clause older than the review, and one class defect in CY1's own stamp (a truncating write)** · adjudicated: 4 FIXED (DA1–DA4, covering 13 candidates) / 2 ACCEPTED with measurements (DA5) / 6 REFUTED (DB1) — 21 candidates (6 pool + 2 Sonnet + 13 Opus), 1 duplicate (the un-alerted stamp, both natives — counted once), new 20 · code-changing files: 2 (`external_services_chain.sh`: the per-label clause, the atomic + alerted stamp; `liveness_audit.py`: the stamped stderr line) + the registry's new surface + `gen_dashboard.py`'s comment/header · live: 107 / 348 (re-derived by a dry-run consolidation on 2026-09-03 — 2 new triage names since the file's last write), 168 tests green across the four DB-free suites (13 + 101 + 46 + 8; the DB-bound 31 unchanged) · **0 unattended chain runs to date — the first is due 2026-09-03 06:00 +03** · → not done (changed code; the operator's crontab line outstanding) |
| Pass 36 | method: re-derivation, tight bar, every class swept with a denominator | found: 45 | new: 36 | fixed: 4 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..1933fa8b -- <my paths>` (now including `liveness_audit.py`, its test, `gen_dashboard.py`, INDEX), finders pinned in sandbox extracts with the round's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DD1: 19 / 6 / 0 — 6 self-duplicates, all refuted; scored 1/1/1) + native Sonnet ×1 (2 LIVE — both known/true, 4 TEXT; 17 mutations red, 478 row ids unique) + native Opus ×1 (4 LIVE CONFIRMED — the glued stamp, the vacuous `cd` grader, the ungraded heartbeat body, the audit's coupling header; 3 LATENT; 7 TEXT; 6 mutations, 2 survived → fixed; 12 renders max 496; 3 orderings) — all non-author · **one live defect in DA1's own fix (the stamp glued under the cron's merged streams — the fix's grader could not see the cron's shape) and two vacuous/missing graders from pass 35** · adjudicated: 4 FIXED (DC1–DC4, covering 16 candidates) / 2 ACCEPTED with measurements (DC5) / 27 REFUTED (DD1) — 45 candidates (25 pool + 6 Sonnet + 14 Opus), 9 duplicates (6 inside the pool, the registry `why` and the liveness doc by both natives, the crontab line by Sonnet vs the ledger's own NEXT), new 36 · code-changing files: 2 (`liveness_audit.py`: `flush=True`; `external_services_chain.sh`: `mv -fT`) + graders + the registry's two `why` fields + four docs · live: 107 / 348 (dry run 2026-09-03; 346 on disk — the column's referent, stated from here on), 169 tests green (13 + 101 + 47 + 8) · 0 unattended chain runs to date (the 06:00 cron is 35 minutes away at this row's writing) · → not done (changed code; the operator's crontab line outstanding) |
| Pass 37 | method: re-derivation, tight bar, every class swept with a denominator | found: 31 | new: 29 | fixed: 4 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..e06c6fb9 -- <my paths>` (+ `docs/workstation/liveness.md`), finders pinned in sandbox extracts with the round's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DF1: 7 / 3 / 1, all 11 refuted; scored 1/1/1) + native Sonnet ×1 (0 LIVE, 2 LATENT, 3 TEXT; 9 mutations, the flush mutation red 5 of 5) + native Opus ×1 (2 LIVE — the template-level parity grader, the doc's universal DEAD clause; 5 LATENT; 8 TEXT; 9 mutation runs, 40 alert renders max 496, 7 `mv -fT` destination shapes, 4 stream shapes × 2 modes) — all non-author · **no live production defect; one grader gap and one mechanism defect in the previous round's own fixes (the parity unit, the echoed marker), one doc sentence older than the review** · adjudicated: 4 FIXED (DE1–DE4, covering 19 candidates) / 1 ACCEPTED with measurement (DE5) / 11 REFUTED (DF1) — 31 candidates (11 pool + 5 Sonnet + 15 Opus), 2 duplicates (the parity unit and the DD1 count, both natives — counted once), new 29 · code-changing files: 1 (`liveness_audit.py`: stamped-only marker evidence, the closed-stdout guard) + graders + the registry `why` + four docs · live: 107 / 348 (dry run; 346 on disk), 171 tests green (13 + 101 + 49 + 8) · → not done (changed code; the operator's crontab line outstanding) |
| Pass 38 | method: re-derivation, tight bar, every class swept with a denominator | found: 16 | new: 16 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..319782e2 -- <my paths>`, finders pinned in sandbox extracts with the round's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DH1: 1 / 5 / 0, all 6 refuted; scored 1/1/1) + native Sonnet ×1 (0 LIVE, 0 LATENT, 1 TEXT; 13 mutations, 491 row ids unique, 0 skips of 171) + native Opus ×1 (3 LIVE — the merged-pipe/full-disk raise, the inert `dup2`, the unguarded `[-1]`; 3 LATENT; 3 TEXT; 22 mutations executed 19 red 3 survived → all three fixed; 2 of 2 `log_marker` surfaces executed against the real logs; 10 graded renders 0 odd; 22 tokens 0 credential-shaped) — all non-author · **no live production defect; one fail-closed gap and one inert line in the previous round's own fix, two unguarded contracts** · adjudicated: 3 FIXED (DG1–DG3, covering 10 candidates) / 0 ACCEPTED / 6 REFUTED (DH1) — 16 candidates (6 pool + 1 Sonnet + 9 Opus), 0 duplicates, new 16 · code-changing files: 1 (`liveness_audit.py`: the two `OSError` guards, `dup2` on stderr only) + graders + three docs · live: 117 / 338 on disk after the 06:00 reconsolidate (the row first said 107 / 348 — the pre-run figures, corrected at pass 39), 172 tests green (13 + 101 + 50 + 8) · **the first unattended chain run: `daily_refresh.sh` fired from cron 2026-09-03 06:00:01, the chain started ~06:00:06 and succeeded (23 s, exit 0, stamp written, LIVE) — 1 of 1** · **the commit also carries `scripts/service_catalog.json` (+80 lines: that run's 10 classifier entries — 4 identified, 6 tombstoned), swept in by the commit script's stale path list, disclosed at pass 39; the list is corrected** · (row ids: 491 by the pass-38 finder's grep, 495 by pass 39's — two regexes, both 0 duplicates) · → not done (changed code; the operator's crontab line outstanding) |
| Pass 39 | method: re-derivation, tight bar, every class swept with a denominator — and the chain's first PRODUCTION output read as class-1/8 evidence | found: 36 | new: 32 | fixed: 5 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..f3aaeac4 -- <my paths>`, finders pinned in sandbox extracts with the round's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DJ1: 10 / 4 / 1 — deepseek's INDEX count was real; scored 1/1/2) + native Sonnet ×1 (0 LIVE, 1 LATENT, 4 TEXT; 3 mutations ×3 runs, 0 skips of 172, 495 row ids unique, the live counts re-derived two ways) + native Opus ×1 (4 LIVE — the silent full disk, `2>&-`, the catalog auto-commit, the queue composition + misfile; 6 LATENT; 6 TEXT; 8 shell shapes × 2 builds, 7 mutations 6 red 1 survived (a redundant `flush` on stderr), 0 of 1049 secret values in the report, 20 alert renders max 496) — all non-author · **one unnamed mirror in the previous round's fix (a full disk now silent), one unguarded stream state, and the first live output: 6 of 10 units on scrape targets (by design, tombstoned once) and 1 of 4 identifications misfiled** · adjudicated: 5 FIXED (DI1–DI5, covering 17 candidates) / 4 ACCEPTED with measurements (DI6) / 15 REFUTED (DJ1) — 36 candidates (15 pool + 5 Sonnet + 16 Opus), 4 duplicates (the INDEX count ×2, the undisclosed catalog hunk, the stale live counts — natives and the pool), new 32 · code-changing files: 2 (`liveness_audit.py`: the undeliverable exit, the None guard; `classify_services.py`: the cost line + field) + one hand-curated catalog row (`argusmedia`, the classifier's own format, 1 line) + graders + four docs · live: 117 / 338 on disk (455 services, 21 projects), 174 tests green (13 + 102 + 51 + 8) · **1 unattended run to date, succeeded; crontab line 96 still the operator's** · produced `10db7e01` + `cf0abcac` (the two grader files the commit script's list had dropped, committed 46 s later) · → not done (changed code) |
| Pass 40 | method: re-derivation, tight bar, every class swept with a denominator | found: 40 | new: 39 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..cf0abcac -- <my paths>`, finders pinned in sandbox extracts with pass 39's two commits split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DL1: 15 / 4 / 0, all 19 refuted; scored 1/1/1) + native Sonnet ×1 (0 LIVE, 0 LATENT, 4 TEXT; 9 mutations, the closed-stderr child probed `sys.stderr is None`, 5× determinism, 345 row definitions unique) + native Opus ×1 (6 LIVE — the tool-less classifier, the 10-of-10 count, the asserted unit cost, the closed stdout, the derived surfaces, the unconsumed exit; 7 LATENT; 4 TEXT; 9 mutations 6 red, the flywheel ledger read, 4 exit-flush shapes, 36 `_Res` call sites) — all non-author · **one root-cause defect older than the review, found through the first production output (the paid units never had a web tool), and the mirror of the previous round's stream guard** · adjudicated: 3 FIXED (DK1–DK3, covering 17 candidates) / 4 ACCEPTED with measurements (DK4) / 19 REFUTED (DL1) — 40 candidates (19 pool + 4 Sonnet + 17 Opus), 1 duplicate (the asserted unit cost, both natives), new 39 · code-changing files: 2 (`classify_services.py`: the tool names, the finite-only cost sum; `liveness_audit.py`: the closed-stdout guard) + graders + four docs · live: 117 / 338 on disk, 177 tests green (14 + 103 + 52 + 8) · produced `22fee37c` · → not done (changed code) |
| Pass 41 | method: re-derivation, tight bar, every class swept with a denominator | found: 25 | new: 25 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..22fee37c -- <my paths>`, finders pinned in sandbox extracts with the round's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DN1: 5 / 4 / 0, all 9 refuted; scored 1/1/1) + native Sonnet ×1 (0 LIVE, 1 LATENT, 2 TEXT; 7 mutations, 253 files swept, the ledger's 10 rows re-summed) + native Opus ×1 (5 LIVE — the capped strike, the greedy parse, DK1's unnamed mirrors, the coupling wallpaper, the INDEX count; 6 LATENT; 2 TEXT; 7 mutations 6 red 1 survivor, the advertised-tools probe 2 of 2 / 0 of 5, 34 surfaces twice, 20 alert renders) — all non-author · **no live production defect on the shipped runs; two consequences of the previous round's fix reachable from the NEXT run, caught before it** · adjudicated: 3 FIXED (DM1–DM3, covering 15 candidates) / 1 ACCEPTED (DM4) / 9 REFUTED (DN1) — 25 candidates (9 pool + 3 Sonnet + 13 Opus), 0 duplicates, new 25 · code-changing files: 4 (`classify_services.py`: the capped split, the balanced parser; `external_services_chain.sh`: the classify budget; `liveness_audit.py`: the undelivered marker; `scripts/enforcement/check_command_corpus.py`: the set-form regex + canary, and its self-test's canaries made non-vacuous — a SYNCED surface, correct fleet-wide) + graders + docs · live: 117 catalogued `#svc` blocks (106 real + 11 `unidentified`) / 338 triage of 455 in the consolidated file on disk, 179 tests green (14 + 105 + 52 + 8; the gate's `--selftest` green over its 9 predicates and its bare run green over 55 corpus files — two runs) · produced `f0b47c47` + `048c830a` (the chain script the commit script's list had dropped, committed 5 min later) · pre-existing, out of scope, recorded: `tests/test_gate_check_canaries.py` fails at HEAD on 12 registered checks without a canary (this corpus check among them) — a backlog row · → not done (changed code) |
| Pass 42 | method: re-derivation, tight bar, every class swept with a denominator | found: 43 | new: 36 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..048c830a -- <my paths>` (+ the corpus gate), finders pinned in sandbox extracts with pass 41's two commits split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DP1: 8 / 3 / 2, one real; scored 1/1/2) + native Sonnet ×1 (0 LIVE, 4 LATENT, 4 TEXT; 9 mutations, the gate's canary compound executed, 511 row ids unique) + native Opus ×1 (1 LIVE — the vacuous 9th canary; 10 LATENT; 11 TEXT; 8 mutations 6 red 2 survived → both fixed, an 11-shape parser probe, the library's wall clock/concurrency/recovery read, 0 of 5 leaks through the real prompt, 10 alert renders max 487) — all non-author · **no live production defect; two of the previous round's fixes half-right (the cap rule, the budget), its graders half-blind (the self-test, the sweep)** · adjudicated: 3 FIXED (DO1–DO3, covering 29 candidates) / 2 ACCEPTED with measurements (DO4) / 12 REFUTED (DP1) — 43 candidates (13 pool + 8 Sonnet + 22 Opus; the row first split them 30 / 12 → 11, corrected at pass 43), 7 duplicates (the self-test, the bare set, the venv, the budget pin, INDEX ×2, "3 of 4" — natives and the pool), new 36 · code-changing files: 3 (`classify_services.py`: the parsed-cap rule, the enum-preferring scan, `recover_caps=False`; `external_services_chain.sh`: 2100 s; `scripts/enforcement/check_command_corpus.py`: the signature-asserting self-test, the argument-end regex, a union canary — SYNCED) + graders (an AST sweep, a fake `timeout`) + docs · live: 117 catalogued blocks / 338 triage of 455 on disk, 180 tests green (15 + 105 + 52 + 8; the gate's `--selftest` green over 9 predicates) · first web-tooled run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 43 | method: re-derivation, tight bar, every class swept with a denominator | found: 25 | new: 23 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..e8606f1d -- <my paths>`, finders pinned in sandbox extracts (now with `templates` and `CLAUDE.md`, git-initialised for the sweep) with the round's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DR1: 0 / 3 / 4, two real; scored 1/1/2) + native Sonnet ×1 (0 LIVE, 0 LATENT, 3 TEXT; 11 mutations 11 red, 180 tests + `-W error::ResourceWarning`, the library's three citations re-read, 117/338/455 re-derived) + native Opus ×1 (1 LIVE — the collapsed budget key; 9 LATENT; 5 TEXT; 10 mutations 8 red 2 survived → both fixed, 11 shapes through the real `main()`, 46 ledger units 0 capped, 20 alert renders max 496 — the row first said 484, the `registry_sync`/`gen_dashboard` length; corrected at pass 44) — all non-author · **no live production defect; the previous round's extractor rule was wrong in design (a later object could override the answer) and its graders had two blind spots** · adjudicated: 3 FIXED (DQ1–DQ3, covering 19 candidates) / 1 ACCEPTED (DQ4) / 5 REFUTED (DR1) — 25 candidates (7 pool + 3 Sonnet + 15 Opus), 2 duplicates (DP1's count and the 254, both natives), new 23 · code-changing files: 2 (`classify_services.py`: the first-genuine-answer scan, the template-aware cap; `scripts/enforcement/check_command_corpus.py`: the nested-call/tail regex, quoted-only names, two canaries, the good fixture's ternary — SYNCED) + graders + docs + two INDEX rows · live: 117 catalogued blocks / 338 triage of 455 on disk, 180 tests green (15 + 105 + 52 + 8; `--selftest` 12 canaries over 9 predicates) · first web-tooled run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 44 | method: re-derivation, tight bar, every class swept with a denominator | found: 45 | new: 41 | fixed: 4 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..84fb4137 -- <my paths>`, finders pinned in sandbox extracts (templates + CLAUDE.md, git-initialised) with the round's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DT1: 0 / 4 / 15, all 19 refuted; scored 1/1/1) + native Sonnet ×1 (0 LIVE, 3 LATENT, 2 TEXT; 11 mutations, 180 tests + `-W error::ResourceWarning`, 455/338/117 re-derived) + native Opus ×1 (5 LIVE — the un-run self-test, the 55-vs-93 denominator, the ungraded `\|` arm, the indistinguishable pin, the inert archive exclusion; 7 LATENT; 9 TEXT; 8 mutations 5 red 3 survived → all three fixed, 16 regex shapes, 47 of 47 fleet copies byte-identical to HEAD) — all non-author · **no live production defect; the previous round's gate widening was only half-graded (its own self-test ran nowhere) and mis-counted its denominator** · adjudicated: 4 FIXED (DS1–DS4, covering 24 candidates) / 2 ACCEPTED with measurements (DS5) / 19 REFUTED (DT1) — 45 candidates (19 pool + 5 Sonnet + 21 Opus), 4 duplicates (the prose tail, the `\|` arm, the 254, the eight — both natives), new 41 · code-changing files: 2 (`classify_services.py`: the template mark, the completed-template strike; `scripts/enforcement/check_command_corpus.py`: the negative-lookahead capture + keyword cut, `AUDITED`, the `sys.path` guard, a prose canary + a good-fixture line — SYNCED) + graders (the self-test as a test, the exact-set denominator, the decoy pin, the first-`.py` key) + the coupled gate doc + three INDEX rows · live: 117 catalogued blocks / 338 triage of 455 on disk, 229 tests green (16 + 105 + 52 + 8 + 48; `--selftest` 13 canaries over the eight predicates; the gate reads 93 files) · a sibling's uncommitted deploy-verify WIP is dirty in the tree (`commands/_sources/fabrik-deploy-*.md`, `docs/DECISIONS.md`, `templates/scaffold/scripts/verify_prod_parity.py`) — untouched, and one of their tests reads it live · → not done (changed code) |
| Pass 45 | method: re-derivation, tight bar, every class swept with a denominator | found: 34 | new: 33 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..88031247 -- <my paths>` (+ the gate's test and doc), finders pinned in sandbox extracts (docs archived, git-initialised) with the round's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DV1: 5 / 4 / 9, all 18 refuted; scored 1/1/1) + native Sonnet ×1 (0 LIVE, 1 LATENT, 0 TEXT; 9 mutations, 229 tests + `-W error::ResourceWarning`, the console block byte-compared, 47 of 47 fleet copies) + native Opus ×1 (5 LIVE — the label, the four surviving add sites, the silent predicate skip, the `none` phantom, the doc's error-budget sentences; 6 LATENT; 4 TEXT; 9 mutations, 58 `web_tools=` lines swept, the check run in all 47 copies) — all non-author · **no live production defect; the previous round's denominator fix was half-graded and mislabelled its own advisory, and one pre-existing enforcement defect surfaced through the file under review** · adjudicated: 3 FIXED (DU1–DU3, covering 13 candidates) / 3 ACCEPTED with measurements (DU4) / 18 REFUTED (DV1) — 34 candidates (18 pool + 1 Sonnet + 15 Opus), 1 duplicate (the add sites, both natives), new 33 · code-changing files: 2 (`scripts/enforcement/check_command_corpus.py`: the label, `SKIPPED_PREDICATES` + the advisory, comments — SYNCED; `scripts/enforcement/check_script_headers.py`: the sentinel filter — SYNCED) + graders (a four-site fixture, the advisory test, the label assertion, a new `tests/test_check_script_headers.py`) + the registry doc + the classifier's comment + INDEX · live: 117 catalogued blocks / 338 triage of 455 on disk, 232 tests green (16 + 105 + 52 + 8 + 49 + 2), the first web-tooled classify run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 46 | method: re-derivation, tight bar, every class swept with a denominator | found: 34 | new: 29 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..132d74a9 -- <my paths>` (+ the gate's test and doc), finders pinned in sandbox extracts, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DX1: 5 / 3 / 1; 8 refuted, 1 accepted with a live measurement; scored 1/1/1) + native Sonnet ×1 + native Opus ×1 — all non-author · **no live production defect; round 45's fixes were half-visible (the gate runner swallowed the advisory they added) and half-graded (the print, the canonical doc, the broken-module path), and round 45's `none` fix closed one token of a class** · adjudicated: 3 FIXED (DW1–DW3) / 1 ACCEPTED with measurement (inside DX1) / REFUTED: 8 pool (DX1) + the native remainder — ⚠ the native finders' per-candidate texts were lost to a context compaction between adjudication and this write; the FIXED set was carried in full, the native refuted/duplicate remainder is recorded by count only (34 − 9 pool − the 13 candidates DW1–DW3 cover = 12) · code-changing files: 3 (`scripts/final_gate.py`: `advisory=True` on the corpus-gate row; `scripts/enforcement/check_command_corpus.py`: the import guard + `_IMPORT_FAILURE`, `(attempted N)` — SYNCED; `scripts/enforcement/check_script_headers.py`: the path-shape predicate + the OSError guard — SYNCED) + graders (a `final_gate.py` source pin; the stdout, broken-module, spoke and exact-set assertions; three header shapes + the unreadable-script test; the duplicate `tests/test_check_script_headers.py` deleted) + INDEX · sandbox mutations M1–M7 (below) · live: 117 catalogued blocks / 338 triage of 455 on disk, 253 tests green (16 + 105 + 52 + 8 + 53 + 13 + 6), the first web-tooled classify run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 47 | method: re-derivation, tight bar, every class swept with a denominator | found: 32 | new: 28 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..b98b6249 -- <my paths>` (+ the gate's test and doc), finders pinned in sandbox extracts with round 46's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (DZ1: 5 / 4 / 7; 15 refuted, 1 accepted with a live measurement; scored 1/1/1 — gemini re-raised DX1's heartbeat row, deepseek's seven aim at sibling entries in the cumulative CHANGELOG diff) + native Sonnet ×1 (4: 2 LATENT, 2 TEXT; 253 tests + every DW mutation re-executed, 106/10 headers recounted) + native Opus ×1 (12: 6 LIVE, 3 LATENT, 3 TEXT; the full human gate run in the extract, 241/208/106/217/39/13/17 header denominators, a spoke-shaped run) — all non-author · **no live production defect; round 46's import guard was a FAIL-OPEN regression in the hub (a broken `web_tools.py` went from a blocking red to a green whose advisory the `--json` mode never showed), its grader was a text pin that bled into the next registration, its `advisory=True` shipped a content-free ✓ line into every fleet gate, and DW2's "closes the class" was false (8 of 106 headers still unclosable)** · adjudicated: 3 FIXED (DY1–DY3, covering 13 candidates) / 3 ACCEPTED with measurements (inside DY1 and DZ1) / 15 REFUTED (DZ1) + 1 duplicate (Sonnet's regex bleed = Opus's pin finding) + 1 verified-by-me (Sonnet's UNVERIFIED-BY-YOU on the fabrik-lib claim: `/opt/fabrik-lib/scripts/enforcement/check_script_headers.py:86` carries the path-shape rule) — 32 candidates (16 pool + 4 Sonnet + 12 Opus), new 28 · code-changing files: 3 (`scripts/enforcement/check_command_corpus.py`: the hub problem, the empty-constant guard, `--quiet` — SYNCED; `scripts/enforcement/check_script_headers.py`: `_coupled_tokens` + `_satisfied`, the dangling symlink, the `inspected` denominator — SYNCED; `scripts/final_gate.py`: `--quiet` on the corpus row) + graders (a three-hub subprocess driver, the `--quiet` test, an AST pin + an executed runner stub, 8 header cases) + INDEX · sandbox R1–R7b (14 mutations, every one → red; R6c only after its test stopped creating the file on disk) · live: 117 catalogued blocks / 338 triage of 455 on disk, 263 tests green (16 + 105 + 52 + 8 + 54 + 21 + 7), 47 of 47 synced copies of both enforcement scripts byte-identical to hub HEAD after round 46's post-commit sync (fabrik-lib's is its own), the first web-tooled classify run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 48 | method: re-derivation, tight bar, every class swept with a denominator | found: 27 | new: 21 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..7f758c37 -- <my paths>` (+ the gate's test and doc), finders pinned in a sandbox extract that now carries `.claude/ .windsurf/ src/ PORTS.md` (pass-47 machinery note) with round 47's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (EB1: 0 / 4 / 2 — qwen returned an empty list, the first quiet finder of the run, scored 3; gemini re-raised DX1's heartbeat row and DE5, deepseek re-raised the CLAIMS `kind` row a third time; scored 1/1) + native Sonnet ×1 (6: 2 LATENT, 4 TEXT; 263 tests, forward + reversed order, every DY figure recounted — four of five reproduced exactly) + native Opus ×1 (15: 5 LIVE, 6 LATENT, 3 TEXT, 1 measurement; the broken-sibling and missing-dep repros through the real `run_optional_check`, 12 multi-hub audit calls in 6 processes, 8 header repros in 4 temp repos, a 2494-file satisfiability sweep) — all non-author · **no live production defect; round 47's hub escalation had a BLAST-RADIUS defect (any failure in the `libs.subagents` import graph — a peer's half-saved sibling module on the shared tree — would have redded every session's Tier-2 gate under `web_tools.py`'s name), the ⚠ coverage lines were still absent from the FAILING exit, and the header check's script-relative reading could close a root coupling with a same-named local file** · adjudicated: 3 FIXED (EA1–EA3 — 19 clauses across the three rows; one finder's recount of the same prose) / 4 ACCEPTED with measurements (inside EA1 and EB1) / 6 REFUTED (EB1) — 27 candidates (6 pool + 6 Sonnet + 15 Opus), new 21 (6 re-raises/duplicates) · code-changing files: 3 (`scripts/enforcement/check_command_corpus.py`: `_IMPORT_BLAME` + the blame gate, `_print_coverage_warnings` on both exits, the comment — SYNCED; `scripts/enforcement/check_script_headers.py`: root-first candidates, the kept `/`, self-glob exclusion + `*?[`, `is_file`, the early-return parity — SYNCED) + `docs/reference/command-corpus-check.md` (the coupled doc the round-47 commit had left stale — the header check's own WARN, executed by the finder against round 47's staged set) + graders (a fourth hub in the driver, the failing-run test, the AST slot pin, 5 header cases, the symlink test's decorative clause dropped, the leak clear) + INDEX · sandbox Q1–Q8 (8 mutations, every one → red) · live: 117 catalogued blocks / 338 triage of 455 on disk, 269 tests green (16 + 105 + 52 + 8 + 55 + 26 + 7), the first web-tooled classify run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 49 | method: re-derivation, tight bar, every class swept with a denominator | found: 30 | new: 24 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..14880440 -- <my paths>` (+ the gate's test and doc), finders pinned in a sandbox extract whose pathspec is now stated in the brief (`… README.md .env.example …`), round 48's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (ED1: 7 / 4 / 6 — all refuted or outside this surface; scored 1/1/1) + native Sonnet ×1 (4: 3 LATENT, 1 TEXT; 269 tests, 10 mutations re-executed — Q7's `?[` half found ungraded; forward + reversed order with a state probe) + native Opus ×1 (9: 4 LIVE, 3 LATENT, 2 TEXT; 12 fixture hubs across 9 processes + 4 mutations of the real extract, a 5-shape blame matrix, 4 header repros) — all non-author · **no live production defect; round 48's blame attribution had a HOLE for the exact shape it was written about — a half-saved file is a SyntaxError, which carries no `path` and whose import frames CPython strips, so a broken `web_tools.py` was blamed on its importer and fell OPEN while a broken sibling that `web_tools.py` imports directly was blamed on `web_tools.py` and BLOCKED** · adjudicated: 3 FIXED (EC1–EC3) / 3 ACCEPTED with measurements (inside EC1 and EC2) / 17 REFUTED (ED1) + 6 verified-exact denominators + 1 machinery note — 30 candidates (17 pool + 4 Sonnet + 9 Opus), new 24 · code-changing files: 1 (`scripts/enforcement/check_command_corpus.py`: `exc.filename` for a SyntaxError, the out-of-repo blame by file name, the comment — SYNCED) + the reference doc (three sentences the code did not honour) + graders (four blame hubs in the driver — syntax-in-web_tools, renamed constant, syntax-in-a-direct-sibling, an out-of-repo dependency; the `?` glob case; the `0 of 0` assertion; the `AUDITED` clear) + INDEX · sandbox S1–S5 (5 mutations, every one → red) · live: 117 catalogued blocks / 338 triage of 455 on disk, 270 tests green (16 + 105 + 52 + 8 + 55 + 27 + 7), the first web-tooled classify run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 50 | method: re-derivation, tight bar, every class swept with a denominator | found: 30 | new: 25 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..f6afa2e1 -- <my paths>` (+ the gate's test and doc), finders pinned in a sandbox extract (pathspec stated), round 49's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (EF1: 8 / 5 / 4 — all refuted or outside this surface; scored 1/1/1) + native Sonnet ×1 (3: 1 LATENT, 2 TEXT; 270 tests, 6 mutations re-executed, forward + reversed order, every EC figure recounted — one wrong) + native Opus ×1 (10: 7 LIVE, 1 LATENT, 2 TEXT; an 18-hub blame matrix across 18 processes, 5 real-gate-row runs, the constant's shape swept) — all non-author · **no live production defect; the loader I have rewritten for four rounds still had a CLASS open — the constant's SHAPE (a bare str is a set of characters and inverts predicate 1 fleet-wide; None crashes the blocking row with a traceback; a non-str member crashes the remediation join), a name-SUFFIX identity (`libs/web_tools.py` is a real standalone module and would have blocked under the wrong name), a runtime SyntaxError's `<string>` filename (fail-open), an out-of-repo PACKAGE shown as a bare `__init__.py`, and a symlinked `libs/` mislabelled "(outside the repo)"** · adjudicated: 3 FIXED (EE1–EE3) / 2 ACCEPTED (inside EE2) / 17 REFUTED (EF1) + 3 refuted-by-execution native items (IndentationError/TabError, a missing sibling, a raise in the top package — all already right) — 30 candidates (17 pool + 3 Sonnet + 10 Opus), new 25 · code-changing files: 1 (`scripts/enforcement/check_command_corpus.py`: `_blame_for`, `_shape_problem`, `_same_file`, `_display_path` — SYNCED) + the reference doc + graders (seven hubs added to the driver — an out-of-repo package, a bare-str / None / non-str-member constant, a runtime SyntaxError, a suffix collision, a symlinked `libs/`; the dependency dir scoped to the two hubs that need it) + INDEX · sandbox U1–U7 (7 mutations, every one → red) · live: 117 catalogued blocks / 338 triage of 455 on disk, 270 tests green (16 + 105 + 52 + 8 + 55 + 27 + 7), the first web-tooled classify run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 51 | method: re-derivation, tight bar, every class swept with a denominator | found: 25 | new: 21 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..48e28f93 -- <my paths>` (+ the gate's test and doc), finders pinned in a sandbox extract, round 50's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (EH1: 7 / 4 / 3 — one real text defect, the INDEX pipe; scored 1/1/2) + native Sonnet ×1 (2: 1 LATENT, 1 TEXT; 270 tests, 5 mutations re-executed, forward + reversed, the identity rule found half-graded) + native Opus ×1 (9: 4 LIVE, 4 LATENT, 1 TEXT; 3 mutations of the REAL extract's `web_tools.py` — NUL-truncated, mode 000, replaced by a directory — each through the real CLI and the gate row; a 10-hub adversarial set; 4 helper mutants) — all non-author · **no live production defect; the traceback-inference approach of rounds 48–50 could not be made complete — a NUL-corrupted file raises a SyntaxError with NO filename, an unreadable one a PermissionError inside the import machinery, a directory a ModuleNotFoundError in the importer — every one blamed an innocent sibling and went GREEN on the blocking row; the fix is structural: ASK THE FILE (read + compile) before importing** · adjudicated: 3 FIXED (EG1–EG3) / 2 ACCEPTED (inside EG1) / 14 REFUTED (EH1) + 1 fixed text defect from the pool (the INDEX pipes, EG3) — 25 candidates (14 pool + 2 Sonnet + 9 Opus), new 21 · code-changing files: 1 (`scripts/enforcement/check_command_corpus.py`: `_target_is_broken` compile-first, `_blame_for` on any real-file `filename`, `_shape_problem` over `Collection`, `_same_file` guarded, `_display_path` cwd-independent — SYNCED) + the reference doc + graders (five hubs — `parentwt`, `nulbytes`, `noperm`, `dirmod`, `dictkeys`; sixteen direct unit tests over the five helpers; the dead disjunct dropped) + INDEX (three code spans' pipes escaped) · sandbox V1–V6 (6 mutations, every one → red — V3/V6 twice, driver and unit) · live: 117 catalogued blocks / 338 triage of 455 on disk, 286 tests green (16 + 105 + 52 + 8 + 71 + 27 + 7), the first web-tooled classify run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 52 | method: re-derivation, tight bar, every class swept with a denominator | found: 20 | new: 17 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..72626afd -- <my paths>` (+ the gate's test and doc), finders pinned in a sandbox extract, round 51's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (EJ1: 0 / 4 / 1 — qwen quiet again, scored 3; gemini and deepseek 1) + native Sonnet ×1 (4: 3 LATENT, 1 TEXT; 286 tests, 10 mutations re-executed incl. V1–V6, forward + reversed) + native Opus ×1 (11: 2 LIVE, 7 LATENT, 2 TEXT; 20 adversarial hubs, 7 raw-byte compile cases, 13 shape values, 7 mutants — two survivors) — all non-author · **no live production defect; compile-first (EG1) left two unguarded edges — the PRESENCE probe itself (`Path.exists()` raises EACCES on an unreadable parent, pathlib ignores only ENOENT/ENOTDIR/EBADF/ELOOP → a raw traceback out of the blocking row) and a dangling/looping symlink read as "absent"; and the widened `filename` blame of EG1 had a mirror — a module failing to open a DATA file at import was blamed on the data file and fell open** · adjudicated: 3 FIXED (EI1–EI3) / 3 ACCEPTED with measurements (inside EI1) / 5 REFUTED (EJ1) + 2 sibling-WIP notes — 20 candidates (5 pool + 4 Sonnet + 11 Opus), new 17 · code-changing files: 1 (`scripts/enforcement/check_command_corpus.py`: the guarded presence probe, the checker-frame attribution, the last REAL frame, `.py`-only filename blame, the wider compile catch, `_scrub` — SYNCED) + the reference doc + graders (four hubs — `dangling`, `nopermdir`, `corruptpyc`, `missingcfg`; unit rows for a missing and a non-`.py` filename, the resolved display branch, the message) · sandbox X1–X6 (6 mutations, every one → red; X4 only after its assertion was narrowed to the unusable line — the fixture's own orchestrator-tree message carries an absolute path by design) · live: 117 catalogued blocks / 338 triage of 455 on disk, 286 tests green (16 + 105 + 52 + 8 + 71 + 27 + 7) with one SIBLING test deselected (`tests/test_final_gate_advisory_display.py` is dirty in the tree with a new test whose `final_gate` counterpart is their unsaved WIP — not mine to touch or commit), the first web-tooled classify run due 2026-09-04 06:00 · machinery: the round-51 post-commit governance sync reported SYNC FAILED (its cause scrolled past my filtered output; the forced re-run distributed 47 of 47 cleanly) · → not done (changed code) |
| Pass 53 | method: re-derivation, tight bar, every class swept with a denominator | found: 29 | new: 22 | fixed: 3 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..bd9b6368 -- <my paths>` (+ the gate's test and doc), finders pinned in a sandbox extract, round 52's commit split per file — THREE of its eight diffs a sibling's swept-in work, tagged `sibling` in the brief — the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (EL1: 5 / 4 / 5 — all refuted; scored 1/1/1) + native Sonnet ×1 (5: 1 LATENT, 3 TEXT, 1 sibling; 286 tests forward + reversed, 6 mutations, an AST recount of two denominators — both wrong by one) + native Opus ×1 (10: 2 LIVE, 3 sibling, 3 LATENT, 2 TEXT; 11 fixture hubs, a LIVE red-and-restore on the real extract, 5 mutations, the Tier-2 gate JSON) — all non-author · **no live production defect in MY surface's shipped behaviour; round 52's checker-frame rule over-reached — a SIBLING's torn bytecode cache produces the same frameless EOFError, so it BLOCKED the gate under `web_tools.py`'s name with a remediation ("fix the module") that cannot work (executed LIVE on the real extract: exit 1); the sibling hunks my commit swept in carry three defects of their own, routed to their author** · adjudicated: 3 FIXED (EK1–EK3) / 2 ACCEPTED (inside EK1) / 14 REFUTED (EL1) / 3 ROUTED to fleet by mail (`01M1KMF66S0HCR1XCC0QASMEQP`: `_summarize_skipped` keys on 1 of ~7 skip shapes — 1 of 3 did-not-run rows reported; INDEX documents two files absent from HEAD — the Doc Link Integrity row reds on a fresh checkout; the new JSON keys ship ungraded) + 2 machinery notes — 29 candidates (14 pool + 5 Sonnet + 10 Opus), new 22 · code-changing files: 1 (`scripts/enforcement/check_command_corpus.py`: `_bad_cache_owner` — the caches asked, the target's first, then its package siblings, then the parent package; frames must name an EXISTING file; `_scrub` on every branch and of the operator's home — SYNCED) + the reference doc + graders (hub `sibpyc`; the `noperm` scrub assertion; a vanished-frame + home-scrub unit) · sandbox Y1–Y5 (5 mutations, every one → red) · live: 117 catalogued blocks / 338 triage of 455 on disk, 288 tests green (16 + 105 + 52 + 8 + 72 + 27 + 8 — the sibling's in-flight advisory-display test now passes against HEAD and is no longer deselected), the first web-tooled classify run due 2026-09-04 06:00 · process: from this round the commit is made by a per-round script with a content SNAPSHOT of every file I edited (a sibling's later edit to the same file refuses the commit), no whole-index reset, the staged numstat printed unfiltered (operator directive 2026-09-03: "please be respectful" — the pass-52 incident) · → not done (changed code) |
| Pass 54 | method: re-derivation, tight bar, every class swept with a denominator | found: 13 | new: 12 | fixed: 2 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..cae2716f -- <my paths>` (+ the gate's test and doc), finders pinned in a sandbox extract that now carries a generated id→verdict index of the ledger (`.ledger_index.txt`, 561 rows — the pass-53 machinery note), round 53's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (EN1: 5 / 3 / 1 — all refuted; scored 1/1/1) + native Sonnet ×1 tests/docs (3: 1 LATENT, 2 TEXT; 288 tests, 4 mutations, the zero-byte-cache shape executed through `_live_web_tool_names`) + native adversarial ×1 run on SONNET as a substitute (1 LIVE; the Opus finder was refused four times by the API — the weekly limit, then 500/529/529 after the operator switched the account) — all non-author; the run record carries this pass as rounds 54 (pool only, before the switch) and 55 (the natives) · **no live production defect; two attribution edges of round 53's cache-owner rule — a zero-byte target cache (which Python ignores) was counted as the torn one before a truly torn parent cache was reached, and a torn cache of the sibling `web_tools.py` imports DIRECTLY (`from .tools import`, the production shape) was blamed on the target because its own import line is a real frame** · adjudicated: 2 FIXED (EM1–EM2) / 9 REFUTED (EN1) + 2 count corrections — 13 candidates (9 pool + 3 Sonnet + 1 adversarial), new 12 · code-changing files: 1 (`scripts/enforcement/check_command_corpus.py`: only a cache Python would LOAD counts — a 16-byte header with the current magic and, in timestamp mode, an mtime/size matching the source; the caches are asked whenever the failure ended inside the import machinery, the frame's attribution kept when none is torn — SYNCED) + graders (hubs `zerotarget`, `sibtoolspyc`; a header unit test) · sandbox W1–W3 (3 mutations, every one → red) · live: 117 catalogued blocks / 338 triage of 455 on disk, 289 tests green (16 + 105 + 52 + 8 + 73 + 27 + 8), the first web-tooled classify run due 2026-09-04 06:00 · → not done (changed code) |
| Pass 55 | method: re-derivation, tight bar, every class swept with a denominator | found: 23 | new: 20 | fixed: 2 | finders: full fresh sweep on the path-filtered cumulative surface `c2b1ad2f~1..ee6926b6 -- <my paths>` (+ the gate's test and doc), finders pinned in a sandbox extract with the ledger index, round 54's commit split per file, the pool brief carrying the ledger's REFUTED/ACCEPTED rows: pool fanout ×3 routed (EP1: 4 / 5 / 1 — all refuted; scored 1/1/1) + native Sonnet ×1 tests/docs (4: 3 LATENT, 1 TEXT; 289 tests forward + reversed, 4 mutations) + native Opus ×1 adversarial (9: 2 LIVE, 6 LATENT, 1 TEXT; 15 hubs, CPython's own `_bootstrap_external` read as ground truth, 4 mutations) — all non-author; the run record carries this pass as round 56 (round 55 was the pass-54 natives) · **no live production defect; the hand-written cache-header gate of round 54 implemented three of CPython's five validation conditions (no hash check for hash-based pycs, no reserved-flag rejection, no non-code-body check), the cache search ran target-first against the import's execution order so a two-cache tear printed a remedy that did not fix the failure, and the sibling set lacked the compile-first certainty the target has (a NUL-padded or directory-shaped sibling blocked under `web_tools.py`'s name)** · adjudicated: 2 FIXED (EO1–EO2) / 10 REFUTED (EP1) + 1 doc + 4 graders folded into EO1/EO2 — 23 candidates (10 pool + 4 Sonnet + 9 Opus), new 20 · code-changing files: 1 (`scripts/enforcement/check_command_corpus.py`: the cache verdict delegated to CPython's own validators, every torn cache reported in import order, sibling sources checked compile-first, a `.pyc` in `exc.path` a cache trigger — SYNCED) + the reference doc + graders (hubs `raiseplustorn`, `nulsib`, `dirsib`, `nopermsib`, `staletarget`, `noncode`, `bothtorn`; the unit rows for a size-only stale header, a 17-byte cache, reserved flags, a checked-hash cache current/stale, a non-code body) · sandbox V1–V3 + U1–U6 (9 mutations, every one → red) · live: 117 catalogued blocks / 338 triage of 455 on disk, 289 tests green (16 + 105 + 52 + 8 + 73 + 27 + 8), the first web-tooled classify run due 2026-09-04 06:00 · operator directive this pass: "use maximum count of subagents and converge asap" — pass 56 runs six partitioned native finders in parallel plus the pool · → not done (changed code) |

## Machinery observed this run

- **Pass 19 (Sonnet): `my_paths.txt` still named the deleted `scripts/tests/test_refresh.py`** — dropped. **Own machinery: the TLD-aware attribution (C5) was measured against the live scan BEFORE landing — 3 of 525 hosts would have lost their vendor, all three legitimately the same vendor on a sibling domain — and curated through the designed `hosts` field rather than by weakening the rule.** FIX-DIRECTIVE verb 1 again: measure, then decide the class.
- **Pass 18 (Opus): the brief carried a typo'd delta base sha** (`3347dc6c` for `3347dd6c`) — a finder trusting it would have diffed nothing; caught by git's own error. Briefs now paste shas from `git log`, never retyped. **Own machinery: BK1 is the second time a security gate shipped fail-OPEN on the unreadable-input path (BH1 → BK1); the grader that would have caught it is the one Sonnet asked for in BJ1 — 'exercise the SHIPPED reader'.** Both landed this pass.
- **Pass 17 (Opus): the sandbox pathspec omitted `db/` (a briefed path), and the 583-line ledger is cheaper materialised once into the sandbox than `git show`-grepped repeatedly** — both adopted in the next brief. **Own machinery: the first BH1 cut was measured against the live registry BEFORE landing and found to refuse ten legitimately-aliased blocks — the measurement changed the design (provenance instead of name ownership).** That is the FIX-DIRECTIVE verb 1 doing its job on a security fix.
- **Pass 16 (both native finders): the brief's `rm -f $D/scripts/verify_prod_parity.py` line is now a no-op for a different reason — the symlink is no longer in the tree at `7d6d80ac` (a sibling moved it); and `my_paths.txt` still names the deleted orchestrator.** Both dropped from the next brief. **Own machinery: the pass-15 `except` narrowing (BB6) was a `str.replace` on a line ruff had wrapped — it matched nothing and the commit shipped the docstring without the code.** Every anchored edit since pass 9 that missed did so loudly (an assert); this one was a bare `replace` with no assert. Rule applied from here: every patch edit asserts its match count.
- **Pass 15 (Opus): `my_paths.txt` still lists `scripts/refresh_service_inventory.py`, deleted at `c2b1ad2f`** — `git archive` omits it silently; harmless (it contributes 0 subjects), noted so the next brief says so.
- **Pass 13: the pool's code unit had been VACUOUS since pass 10.** `pool_pass.py` inlined the first 70 000 characters of the whole cumulative diff; from pass 10 that cut fell inside `capabilities.json`, so the code partition never saw a `scripts/*.py` hunk and its `[]` verdicts (AK1/AN1/AR1 "code + docs units empty") were not evidence. The unit's own MACHINERY line said so this pass. Fixed in the helper (each partition gets its own files' hunks) and re-dispatched: the code unit then returned 3 candidates (all refuted by execution). The Pass 10–12 rows' pool claims are hereby re-read as "pool: ops unit only" — the native finders carried those passes.
- **Pass 13: the Bash safety classifier was rate-limited for ~2 minutes** (`claude-sonnet-5 … temporarily unavailable`); read-only tools carried the reads and the patch resumed — no work lost, one turn of latency.
- **Pass 12: a finder's helper file poisoned the pool — `h11.py` written into the SHARED scratchpad root by the pass-11 Opus finder** (17:18) shadowed the real `h11` for the pool helper, whose own directory is first on `sys.path`: two full dispatches errored in every unit (`module 'h11' has no attribute 'Request'`), recorded nothing, and read like a venv break until `find` found the file. Rule for the finder brief: helpers go under the finder's OWN sandbox dir, never the scratchpad root — backlog row.
- **Pass 12 (Opus) said the brief's `rm -f $D/scripts/verify_prod_parity.py` is a no-op because `git archive` does not extract the symlink — REFUTED by the pass-13 Sonnet finder with `tar -tv` + `test -L`: the symlink IS extracted (dangling). The pass-12 claim was accepted unverified and copied into the pass-13 brief and a backlog row (AT2); both corrected. Two finders asserted opposite mechanisms about the same file — the lesson is that a MACHINERY claim gets the same executable check as a code claim.
- **Peer message (seo session, 17:5x): the hub `fabrik` inbox is 81 deep, oldest 2026-09-01 00:40** — relay is not draining while three sessions run long reviews; an ack-required finding was dropped in both directions for ~6 h. Out of this run's scope (mailbox handling is a separate duty), recorded here and as a backlog row; the sender-visible failure signal they ask for is a fabrik-mail design gap.
- **Pass 11 (Opus): the sandbox extract set is undocumented at the repo level** — `scripts/verify_prod_parity.py` is a symlink into `templates/` and extracts dangling from `git archive`; nothing in the repo says so. Recorded here and in the backlog row for the finder brief (the brief now names it).
- **Pass 11 (Opus): AM3's framing "17 of 18 registry tests skip without the local DB" reads as if the DB-backed asserts never run** — in this environment the local Postgres IS reachable and 18 of 19 ran (the single skip is a missing `all-envs.env` in a sandbox). AM3's point stands (a pure grader survives a DB-less box); its wording is conditional, not a description of this box.
- **Own machinery, pass 11: three anchored-edit patches aborted on `ruff format` rewraps** (a long assertion, an import line, a strip block) — every one recovered by re-reading the live line, but the pattern is a cost: format BEFORE computing anchors, or anchor on a unique short literal (the value string, the test name) rather than a wrapped statement.
- **Pass 10 (both native finders): the sandbox recipe needs `libs/`, `.fabrik/`, and a note on the `scripts/verify_prod_parity.py` SYMLINK** (`git archive <sha> scripts tests` extracts it dangling; `scripts/tests/test_gather_envs.py` → `classify_services` → `libs.subagents`; the chain test reads `.fabrik/liveness-registry.json`). Both finders self-served it in seconds; the brief should name the full extract set.
- **Pass 10 (Opus): "read live envs for NAMES only" makes a (name, value) classifier unmeasurable** — the finder substituted a 9-shape synthetic value matrix over 1084 live names (9756 subjects), which is sound but not the live flip set. The dispatcher (this session) can measure the live set itself without printing values, and did (278 rows, 0 flips); a brief should say "values may be READ in-process, never printed" rather than forbid the read.
- **A finder that imports from the live tree measures the tree, not the commit (pass 9, Opus):** `sys.path.insert(0, "/opt/fabrik/scripts")` loaded a sibling's uncommitted AH1/AH2 for ~20 min of a run briefed on `fae20651`; caught only because `inspect.getsource()` disagreed with the diff. Finder briefs that pin READS to `git show <sha>:<path>` must pin IMPORTS the same way (extract to a sandbox, assert provenance) — backlog row, infra beat. And the same trap bit THIS session's own flip-set measurement: a module loaded from a temp dir resolved `CATALOG_PATH` relative to itself, found no catalog, and reported "0 keys move" for both sides — a vacuous denominator (150 name-derived keys, 0 matcher-derived) until the paths were pinned (279 keys, 1 moves).
- **The disposition ledger advanced past the dispatch mid-run (pass 9, Opus):** the brief scoped standing rows as "P…AF, AG1" while the worktree already carried AH1/AH2, so the finder spent a pass re-deriving a row already fixed. A dispatch should state the ledger's high-water mark as a `git show`-able sha or timestamp, not a letter — same backlog row.
- **Shared-index absorption (twice):** sibling commits `72020e88` (13:25), `87f6002a` (13:34) and `3bf64937` (13:53) carried THIS review's staged blobs of `CHANGELOG.md` and `.windsurf/rules/CLAIMS.yaml` (80 rows) into master under their subjects — a bare `git commit` takes the whole shared index. Content is on master and correct; attribution is theirs. The mirror hazard (a stale blob of mine reverting their lines) was audited before every stage: 0 removed sibling lines in every shared doc.
- **`check_subagent_flywheel` cycle boundary = HEAD on shared master:** four pool review rows recorded at 13:19 UTC were "not this cycle" after a sibling's 13:34 commit; the pass-2 pool dispatch re-satisfied it (backlog row filed, infra beat).
- **Native finder deaths on the session limit (429):** the first three dispatches died before reading anything; re-dispatched after the operator rotated the account — the ledger row says dispatched 6 / returned 3 / partitions re-covered.
- **Researcher brief said `awk …`** — the grounding agent has no shell; 10 of 10 substituted Read/Grep (backlog row, infra beat). Two grounding-fragment claims refuted live: exa fetches small JSON endpoints fine; WebFetch does not follow cross-host redirects and truncates then asserts negatives.
- **A finder handed a saved diff on a 3-session tree has no drift signal:** the pass-1 snapshot went stale for five files within 40 min; every finder was told to re-read live files and two caught would-be false findings that way. The pass-2 delta was reconstructed from the pinned base sha, not from HEAD.
- **Pass 20 (Sonnet):** the sandbox extract isolates CODE, not the DATABASE — `test_registry_sync.py`'s `fixture_env` tests connect to the real local `fabrik_services` from any copy of the code, so a finder running the brief's own named graders mutated the shared registry (the pollution BQ2 then fixed at the root: no test names a real vendor, and an autouse guard fails any test that changes a real row). The next brief names it: registry graders run only through the guard, never "from the sandbox".
- **Pass 20 (Opus):** `my_paths.txt` carried the review ledger itself inside the reviewed surface, mixing the ledger's delta into the code diff a finder must read — dropped from the finder path list (it stays a committed path). The brief's `git archive … 2>/dev/null` masks a mistyped pathspec as success — the next brief drops the redirect.
- **Pass 20 (self):** a red-on-revert mutation can pass by LUCK — my first `owned_by` revert (`sorted(top)[0]`) picked the merged tuple and kept the grader green; the faithful revert (first hit in list order) went red. A revert must reproduce the ORIGINAL defect's mechanism, not merely differ from the fix.
- **Pass 21 (Opus):** the sandbox extract did not isolate the SECRETS FILE either — `gather_envs.py` hardcoded the hub's `secrets/all-envs.env`, so a sandboxed `--apply` would have written the real fleet file (BS17, fixed at the root: every hub path now derives from `__file__`). The auto-mode classifier also denied the finder a read-only `SELECT count(*)`, so three findings were grounded on an in-process consolidation instead of live rows (~10 min lost); and the brief named two test files without their `scripts/tests/` directory.
- **Pass 21 (self):** two of sixteen red-on-revert mutations silently missed their anchor after `ruff format` reflowed the line — the asserted match count caught both (one would otherwise have reported `1 passed` as a proof). The rule holds for mutations as for patches: assert the count, never trust "mutated".
- **Pass 22 (Opus):** the brief claimed the sandbox "reads AND writes only under $D" — false for the SCAN ROOT: `OPT` is the fleet root by design, and the hub-env exclusion had moved with `REPO` (BW3), so a relocated copy measured 22 projects. The next brief says: the scan root is the real fleet, only the write target and the catalog are the copy's. The `.fabrik` pathspec in `git archive` prints tar noise on stderr; the brief now says so instead of redirecting it.
- **Pass 22 (self):** the oscillation the run-record tool flagged since round 20 is real (30 → 27 → 38 → 35 found); the response is the bar, not the brief — see the Pass 22 row.
- **Pass 23 (pool):** the deepseek code finder STALLED past the 600 s wall clock (the fanout's own error text) yet its partial text carried 34 candidates — the batch is partial-tolerant, so the run counted; `set_quality` returned a bare False for all three pass-23 pool agents once and True on the retry minutes later (with `reason_sink=[]` empty) — a transient DB write, the function's documented fail-open; nothing to file, but the bare-False path is why `reason_sink` exists: pass it on every call.
- **Pass 23 (self):** my sandbox mutation helper reported `1 error` (a pytest collection error) for four proofs whose direct re-runs were clean `1 failed` — a helper that cannot tell "the mutation broke the import" from "the grader went red" is not a proof; the four were re-run directly and their failure lines are in the rows.
- **Pass 24 (Sonnet):** the brief's `pass23.diff` range `7442058b..dd55ca81` spanned three sibling flywheel commits — a finder counting "code-changing files" from it got 3 where the round's own commit has 2; the next brief pins the round's single commit (`<sha>~1..<sha>`). **Pass 24 (self):** two round-23 fixes were built on an unexecuted premise (that `--only` could dispatch a curated name) — the very seam the finders had been told to execute; a fix's stated caller must be run, not assumed, before the fix ships.
- **Pass 25 (Opus):** the sandbox extract has no `secrets/`, so `cs.ALL_ENVS`/`ge.OUTPUT` in the copy point nowhere — every live measurement reads the real file and catalog (names only); the brief now says so instead of implying the copy is self-sufficient. The brief's test-count gloss had the chain and credit-fetcher labels swapped (the sum was right).
- **Pass 26 (Opus):** `scripts/tests/test_gather_envs.py` rebinds `sys.modules["gather_envs"/"classify_services"/"registry_sync"]` on import, so a probe that imports the scripts BEFORE the suite gets different module objects and every `monkeypatch.setattr` in `_classify_env` no-ops — the next brief says "import `cs`/`ge` FROM the suite". And the sandbox's `ge.OUTPUT` is not "nowhere": `--apply` there would CREATE `<sandbox>/secrets/all-envs.env` (0600) — the ban on `--apply` in a sandbox stands for that reason.
- **Pass 27 (Opus):** the sandbox root must be on `sys.path` for `from scripts.tests.test_gather_envs import ge, cs` — the brief named the import without the insert; and the DB-free subset the brief authorises runs to 113, not the 144 headline (31 registry tests are DB-bound) — the next brief states both. `git archive … .fabrik` silently drops the path when it is absent at the sha (`ls -a` shows it at later shas); a check keyed on `.fabrik/liveness-registry.json` must read the tree, not the sandbox.
- **Pass 27 (self):** a red-on-revert mutation that yields a `SyntaxError` proves nothing (CE7's first attempt) — a mutation must be valid code that restores the ORIGINAL mechanism; re-done as a hoisted read.
- **Pass 28 (Opus):** `tests/test_external_services_chain.py` resolves `.fabrik/liveness-registry.json` relative to the TEST FILE, so a sandbox without `.fabrik` loses that test (113, not 114) — the brief now says `ln -s /opt/fabrik/.fabrik $D/.fabrik`, and the proof script does it. The pool's deepseek finder stalled past the wall clock for the second time in three passes (scored 0; the batch is partial-tolerant).
- **Pass 29 (Opus):** the brief's "114 = 93 + 8 + 13" named only two of the three DB-free suites for the probe path — the next brief lists all three by path. `git archive … scripts` carries the dangling `verify_prod_parity.py` symlink (a known extract artefact since pass 13): `grep -r` over the sandbox prints one spurious line.
- **Pass 29 (self):** a proof fixture must use the FIRING shape of its class (CM6's first fixture put the literal on a `?` line, where both predicates agree) — the same lesson as CD1's grader, one round later.

## Disposition ledger

| # | Finding | Raised by | Disposition |
|---|---|---|---|
| P1 | classify: sorted `--max-per-run` head pinned forever by providers that only transport-error (never tombstoned) → queue starves | pool chain (gemini-3-flash) + orchestrator seam (j) | FIXED · LOCAL — `bound_flagged` is a day-rotating window (`seed`), `test_classify_bound_rotates_so_no_provider_is_pinned` |
| P2 | daily_refresh: registry_sync/classify failure only echoes "(non-fatal)" — the retired orchestrator's `send_alert` on sync/classify failure is LOST (bounded-prune refusal, exhausted pool, dead DB become log lines) | pool chain | FIXED · LOCAL — `_svc_step_alert` on every chain step (executed against a stub `alerting` with exit 1 and 3: both captured, quoting valid); chain test asserts each step's alert guard |
| P3 | gather_envs: rg missing/timeout → scan returns {} → file rewritten WITHOUT every code host → registry_sync mass-delete → prune guard refuses → only then visible | pool scan + orchestrator | FIXED · LOCAL — `CodeScanError` (OSError/timeout/rg exit 2) → `main()` exits 1, nothing written; `test_scan_failure_is_fail_closed_nothing_written` |
| P4 | ignored_host: `len(sld) < 3` drops real 2-letter vendors (`qq.com`, `hp.com`) | pool scan | FIXED · LOCAL — bound is `< 2`; `t.co` still ignored (1-letter), `qq.com` asserted kept |
| P5 | EXTERNAL_SYSTEMS Google APIs block says `Env keys: none` while `GOOGLE_CLIENT_*` exists (tojlo-mail) | pool docs (deepseek-v4-flash) | FIXED · LOCAL — meta names `GOOGLE_CLIENT_*` shared with `### Google OAuth (Sign-in)`; the other 7 code-only blocks reworded "none in the fleet today (… the posture row names the knob a consumer would add)" |
| P6 | tests call the real ripgrep → red where rg is absent | pool scan | FIXED · LOCAL — `needs_rg` skipif marker on the three rg tests (honest skip, never silent green) |
| P7 | registry_sync `main()` drops `prune` → "defaults to False" | pool chain | REFUTED — `sync_registry(dsn=None, fetch_credits=False, prune: bool = True)` (registry_sync.py:67); prune stays True |
| P8 | daily_refresh lost the orchestrator's flock → overlapping runs corrupt state | pool chain | REFUTED — gather_envs writes via `os.replace` (atomic); `sync_registry` is ONE transaction with row locks; a manual run concurrent with the 06:00 cron serialises on the DB and never observes a torn file (gather_envs.py:main tmp→replace; registry_sync.py:67-80). Overlap of two daily crons needs a >24 h run — the 600 s scan timeout and per-step timeouts bound it |
| P9 | liveness heartbeat = local file mtime is a "12-Factor logfile dependency" | pool chain | REFUTED — the surface is a BOX-LOCAL cron on WSL, not a container; the liveness registry is defined as box-local evidence (`docs/reference/liveness.md`); XI governs app logs in containers |
| P10 | PLATFORM first-label `TRUSTSTORE_API_KEY` could match a catalog `match` prefix | pool scan | REFUTED — measured: no catalog `match` token equals a platform first label (API/WWW/S3/EMAIL/MAIL/OAUTH2/GMAIL/TRUSTSTORE… → none; the only ≤3-char tokens are HF_/BFL/B2_/R2_/N8N/TCO) |
| P11 | host_label inconsistency for the platform ROOT host (`amazonaws.com` → `amazonaws`) | pool scan | REFUTED — documented branch (`first == sld → sld`); a bare platform root is a placeholder link, and it lands in triage under its own label, never credited to a vendor |
| P12 | provider_for_host returns None for platform hosts → flagged | pool scan | REFUTED — that IS the rule (never credit a cloud to one vendor); the service label goes to triage where the classifier names the product |
| P13 | HOST_RE misses hosts with ports/underscores | pool scan | REFUTED — `https://api.x.com:8080/` captures `api.x.com` (the regex stops at `:`); underscores are not valid hostnames |
| P14 | project_dirs skips symlinked / non-git dirs | pool scan | REFUTED — by design and documented ("every git repo under /opt"); `/opt` repos are real directories (measured: 45) |
| P15 | single-label hosts (`https://internal-api`) mishandled by host_sld | pool scan | REFUTED — HOST_RE requires ≥1 dot; a single-label host never reaches host_sld |
| P16 | a real env var named `CODE_HOST_URL` collides with the synthetic slot | pool scan | REFUTED — non-secret slots key on `key\x00value`; same key + different value = distinct lines, same value = one line (the existing dedupe contract) |
| P17 | `test_scan_skips_…` vacuously proves `.md` exclusion | pool scan | REFUTED — the `.md` line documents the type filter; the load-bearing assertions are `docs/notes.py` (glob) and `tests/test_x.py` (glob) and `github.com` vs `api.github.com` (rule) |
| P18 | ARG_MAX with many repo dirs | pool scan | REFUTED — 45 dirs ≈ 1 KB of argv against a 2 MB ARG_MAX |
| P19 | `code_only` counts a catalogued provider absent from every .env | pool scan | REFUTED — that is the definition ("providers seen ONLY in code") |
| P20 | `sld in catalog` credits an unrelated vendor sharing a label | pool scan | REFUTED — catalog keys are vendor names (`core` → core.ac.uk is the CORE API); a label equal to a catalog key IS that vendor's registrable domain by construction of the catalog |
| P21 | `\x00` in the slot key reaches registry_sync / KV_RE | pool contracts (qwen3-max) | REFUTED — the slot key is internal; `render_provider` writes `primary=value` where `value = _slot.split("\x00", 1)[-1]` (gather_envs.py) — the file line is `CODE_HOST_URL=https://<host>   # used by: …`, which KV_RE parses like any other |
| P22 | a secret VALUE can reach the pool through a CODE_HOST_URL line | pool contracts | REFUTED — the value is constructed as `f"https://{host}"` from HOST_RE's host group only (no path, query, userinfo); `flagged_providers` further reduces `_URL` values to `scheme://hostname` via `urlsplit` (classify_services.py:40-66) |
| P23 | code-only providers pruned for having zero real keys | pool contracts | REFUTED — prune is `DELETE … WHERE provider <> ALL(seen)` by NAME (registry_sync.py:63); key count is irrelevant |
| P24 | `   # ` note delimiter inside a scanned URL | pool contracts | REFUTED — hosts cannot contain spaces (HOST_RE `[A-Za-z0-9-.]` only) |
| P25 | truncated sentence "…is i" in the Google APIs research note | pool docs | REFUTED — the finder's hunk was cut at the 60 000-char unit limit; the live line ends "…row 9 here is the token lifecycle both share." (EXTERNAL_SYSTEMS.md:1153) |
| N1 | registry_sync: `first_value` = first non-empty line VALUE; `CODE_HOST_URL` sorts before `DEEPL_API_KEY`, so DeepL's credit fetcher would be called with `https://api-free.deepl.com` as the key → 403 → snapshot lost daily | orchestrator (own hunt while finders ran; grounded: live scan attributes api-free.deepl.com → deepl) | FIXED · LOCAL — a URL-shaped value is never the credential (registry_sync.py); `test_credit_fetcher_gets_the_credential_never_a_code_host_url` red on the pre-fix code, green after |
| S1 | `REFERENCE_ONLY_SLD` named vendors (`microsoft`, `docker`, `sentry`, `grafana`, `apple`, `gitlab`, `postgresql`, `redis`) → `graph.microsoft.com` (email-reader) and `registry-1.docker.io` (fabrik) silently dropped from the scan | native scan (Sonnet), live call sites cited | FIXED · LOCAL — the list names only registries/schemas/CDNs/Q&A/front-end libs; doc subdomains ignored by `DOC_HOST_PREFIXES`; `api.*` always survives; asserted for graph.microsoft.com, registry-1.docker.io, sentry.io vs learn.microsoft.com, docs.docker.com |
| S2 | `catalog_url_index` resolves a shared registrable label by JSON key order (`backrest`'s repo link owns `github`; `google-safe-browsing` owns `google`) | native scan | FIXED · LOCAL — two-level index: exact hostname (unique only) then `*.<domain>` (unique owner only); shared labels/hosts are dropped as ambiguous; `test_catalog_index_drops_ambiguous_labels_and_keeps_exact_hosts` |
| S3 | rotation offset `(day × k) % N` loses continuity when N changes daily (claimed guarantee unproven) — same as O6 | native scan + native Opus | FIXED · LOCAL — see O6 (persisted cursor) |
| S4 | mixed-source bucket: env-derived `url=?` header never gains the code host's URL | native scan | FIXED · LOCAL — an env-derived `?` header adopts the first code host URL; `test_env_derived_unknown_gains_the_code_host_url` |
| S5 | Dockerfiles (`RUN curl https://…`) never scanned | native scan | FIXED · LOCAL — `-t docker` added; `test_jest_dirs_and_dockerfiles` |
| S6 | bare `!build` / `!cache` globs exclude a repo's own source dirs at any depth | native scan | FIXED · LOCAL — root-only `!/build` `!/cache`; `src/build/x.py` asserted scanned |
| S7 | `test_classify_bound_rotates…` never exercised a changing N | native scan (test quality) | FIXED · LOCAL — replaced by `test_classify_bound_walks_the_queue_from_a_cursor_under_churn` (providers leave/arrive between runs) |
| O1 | platform-labelled providers (`gmail.googleapis`, 11 live) can never leave triage — `provider_for_host` never consults the catalog on the platform branch → re-billed every lap | native Opus (Sonnet-verified live) | FIXED · LOCAL — a platform label present in the catalog is returned; `test_platform_service_already_in_catalog_leaves_triage` |
| O2 | the heartbeat proved only `gen_dashboard` ran: with every step non-fatal, a failed `gather_envs` still produced a fresh mtime → LIVE while half-dead (the 46-day class re-created) | native Opus | FIXED · ARCHITECTURAL → D-081 (amended in place, uncommitted row) — `external_services_chain.sh` writes the dashboard ONLY when `chain_failed=0`; `test_the_chain_is_one_script_in_order_with_a_gated_heartbeat` |
| O3 | `--no-messages` blanked rg's stderr for exactly the rc=2 errors; one unreadable dir aborted all 45 repos with an empty diagnostic | native Opus (measured on rg 14.1.0) | FIXED · LOCAL — flag dropped; rc=2 WITH matches = partial scan (warn with rg's path), rc=2 WITHOUT = `CodeScanError` carrying the text; `test_scan_partial_error_keeps_matches_and_names_the_path` |
| O4 | 376 of 657 `api_keys` rows would be SHA-256 of a public URL; dashboard `keys` inflates (281 → 657) | native Opus (measured on live data) | FIXED · ARCHITECTURAL → D-081 — `api_keys.kind` ('credential' \| 'code-host') via idempotent `ensure_schema` + schema.sql; `gen_dashboard` counts credentials only, unions projects from all rows; `test_code_host_rows_are_kind_code_host_and_never_counted_as_keys` (live registry) |
| O5 | N1's value-shape test still hands `AZURE_ACCOUNT_NAME` (sorts before `AZURE_API_KEY`) to the fetcher | native Opus | FIXED · LOCAL — credential chosen by KEY role (`CREDENTIAL_KEY_RE`); `test_credential_chosen_by_key_role_not_line_order` |
| O6 | day-keyed rotation is arbitrary under churn; `date.today()` is local while the pipeline is UTC | native Opus | FIXED · LOCAL — persisted cursor (`.tmp/external-services/classify_cursor.json`), no date dependence; churn test |
| O7 | chain order gather → classify → sync leaves registry/dashboard one day behind the catalog (orchestrator re-ran gather after classify) | native Opus | FIXED · LOCAL — `gather_envs_reconsolidate` step after classify (cheap no-op when unchanged) |
| O8 | no seen-set: a provider that only transport-errors is re-billed every lap forever (C4 never tombstones errored) | native Opus | FIXED · LOCAL — `apply_error_budget`: 3 consecutive erroring runs → tombstoned (`classify_errors.json`); `test_classify_error_budget_tombstones_after_three_consecutive_errors` |
| O9 | orchestrator's `flock`/`timeout 600`/CPU-mem scope gone; race assessed bounded (atomic writes, one transaction) | native Opus | FIXED · LOCAL (timeouts: every step under `timeout 900`, exit 124 alerted) · race REFUTED — `os.replace` + single transaction = stale-but-complete, never torn |
| O10 | "new providers found" alert lost | native Opus | FIXED · LOCAL — classify sends an info alert + writes `classify_last.json` when it identifies providers |
| O11 | `wsl_startup_hook.sh` shares the daily lock and did not carry the chain — a boot before 03:00 UTC skips it for the day | native Opus (measured: 0 skips in 21,110 log lines today, class real) | FIXED · ARCHITECTURAL → D-081 — ONE chain script, run by both entry points; `test_both_entry_points_run_the_same_chain_script_and_inline_no_step` |
| O12 | liveness runs weekly (`40 6 * * 1`) so a 30 h budget is sampled weekly — an intermittent miss is invisible | native Opus | REFUTED — the two failure shapes are covered by two mechanisms: a step failure alerts the SAME day (chain script) and now also ages the heartbeat (O2); a cron that never fires is caught by the weekly probe within 7 days; the audit cadence is a box-wide setting outside this diff |
| O13 | secrets tmp file written 0644 then chmod'd — a world-readable window with every fleet credential | native Opus (measured umask 0022) | FIXED · LOCAL — `os.open(…, 0o600)` before the first byte; `test_output_file_is_0600_from_creation` spies the mode |
| O14 | `unit_prompt` calls `CODE_HOST_URL` an "environment variable NAME" for code-only providers | native Opus | FIXED · LOCAL — endpoint-led prompt when no env names exist; `test_unit_prompt_branches_for_code_only_providers` |
| D1 | header "124 fleet-used" not derivable from the doc (blocks support 116); "166" asserted | native docs (Sonnet), parsed all 143 blocks | FIXED · LOCAL — header states the derivation rule and reads 150 = 143 blocks + 7 retired, 116 fleet-used; closing checker CLEAN |
| D2 | CHANGELOG "+6" tests; actual +8 (10 → 18, now 27) | native docs | FIXED · LOCAL — counts restated |
| D3 | two unescaped `\|` in prose cells (Gmail row 11, SES row 11) split the tables | native docs | FIXED · LOCAL — `/`-separated |
| D4 | `__tests__/` not excluded — `tojlo-mail/apps/web/__tests__/integration/helpers.ts` scanned as a call site | native docs (live) | FIXED · LOCAL — `__tests__`, `__mocks__`, `mocks`, `e2e`, `cypress` excluded; asserted |
| D5 | CLAIMS `kind: legal` undocumented; `pack:` pointing at a doc undocumented in the header contract | native docs | FIXED · LOCAL — `kind` normalised to `standard`; header note documents `pack:` may name the fleet index |
| D6 | CHANGELOG "9/12 UNKNOWN" for SES — pre-change block had 10 | native docs (re-measured from the base sha) | FIXED · LOCAL — 10/12 |
| D7 | taxonomy table has no row for keyless, call-site-discovered systems | native docs | FIXED · LOCAL — "Keyless call site" row |
| F1 | the S6 fix (`!/build`, `!/cache`) excluded NOTHING: rg anchors a leading `/` to the CWD (`/opt/fabrik`), not to each search root (`/opt/<repo>`) — measured with a temp repo: all four build/cache files matched; the S6 test was too weak to notice (it asserted only that `src/build` survived) | orchestrator, pass 2 (executed `rg` in a temp tree instead of reading the glob doc) | FIXED · LOCAL — the scan runs with `cwd=/opt` and RELATIVE repo names, globs `!/*/build` `!/*/cache` (verified: root `build/` + `cache/` excluded, `src/build` kept, other repos unaffected); test hardened with root-level files, red on the old form → green; live scan unchanged at 371 hosts / 35 projects |
| Q1 | cursor names a provider that left the queue → `index()` ValueError | pool code (deepseek-v4-flash), pass 2 | REFUTED — the cursor is resolved by counting names ≤ it (`sum(1 for n in names if n <= after) % len(names)`), never by `index()` (classify_services.py::bound_flagged) |
| Q2 | window larger than the queue double-picks in one run | pool code | REFUTED — `len(provs) <= max_per_run → return provs` before any wrap |
| Q3 | first run (no cursor) crashes or skips the head | pool code | REFUTED — `after is None → start = 0` |
| Q4 | `$?` masked by the alert helper | pool code + pool ops | REFUTED — `_step` captures `rc=$?` on the line after the command, alerts afterwards, returns rc; executed with exit 1 and 3 (pass 1) |
| Q5 | python -c one-liner injectable via `$1`/stderr | pool code + pool ops | REFUTED — the chain script passes title/body/severity as `sys.argv`, nothing is interpolated into Python source |
| Q6 | `ADD COLUMN … DEFAULT 'credential'` mislabels pre-existing CODE_HOST_URL rows | pool code | REFUTED — no CODE_HOST_URL row predates the column (the first code-host sync ran after `ensure_schema`), and every INSERT carries `kind` with `ON CONFLICT … kind=EXCLUDED.kind`; measured after the real run: 250 credential / 404 code-host |
| Q7 | URL-only provider still feeds the fetcher | pool code + pool ops + pool docs | REFUTED — `first_value` stays None → `if fetch_credits and first_value` skips it (registry_sync.py); the reference doc says so |
| Q8 | two credential keys: lexicographic-first may be the wrong one | pool code | REFUTED as not introduced — the pre-existing rule was "first line"; this change only excludes non-credentials; the fetcher/key-name contract is the project's env convention (residual class (a), pre-existing) |
| Q9 | `CodeScanError` breaks callers expecting `{}` | pool code + pool ops + pool docs | REFUTED — `consolidate`/`scan_code_hosts` have no caller outside `gather_envs.main` (grep over scripts/, src/, tests/, libs/: pandas noise only) |
| Q10 | cursor read-modify-write race between a manual run and the cron | pool code | FIXED · LOCAL — classify holds `flock(LOCK_EX\|LOCK_NB)` on `.tmp/external-services/classify.lock` for the run; the loser skips with exit 0; `test_classify_skips_when_another_run_holds_the_lock` |
| Q11 | 2-letter labels admit paid-classifier noise (`ai`, `db`) | pool code + pool ops | REFUTED by measurement — the live scan has exactly 2 two-letter labels in 371 hosts: `gc` (Health Canada, webprod.hc-sc.gc.ca) and `un` (UN Comtrade API) — both real data sources the fleet calls |
| Q12 | `ensure_schema` as CREATE-only never migrates an existing DB | pool code | REFUTED — it is `ALTER TABLE … ADD COLUMN IF NOT EXISTS`, run every sync (registry_sync.py::ensure_schema); the live registry (pre-existing) gained the column on the real run |
| Q13 | `kind: standard` hides the LinkedIn claim from `kind: legal` consumers | pool code | REFUTED — no consumer reads `kind` (rules_currency_watch keys on last_verified/window_days; measured pass 1) |
| Q14 | heartbeat DEAD on partial success raises alert fatigue | pool ops (gemini) | REFUTED — that asymmetry is the O2 design: a chain that did not complete must not read LIVE; the step alert already names the failed step |
| Q15 | 2-letter TLD hosts (`io.ad`) dropped | pool ops | REFUTED — the rule is on the registrable LABEL, not the TLD; `ignored_host` requires an alphabetic TLD of ≥2 chars only |
| Q16 | earlier "166" mentions contradict the derived header | pool docs (qwen3-max) | REFUTED — `grep -n '166\b'` over the doc: 0 residues |
| Q17 | reference doc still says "shorter than 3" | pool docs | REFUTED — `grep 'shorter than 3'`: 0 in both docs (updated in pass 1) |
| Q18 | seed=None + empty queue crash | pool docs | REFUTED — `if not provs: return 0` precedes the bound |
| G1 | a code-only tombstone (`allowed`, from a URL's userinfo in a comment) minted `match: ["ALLOWED"]` and `match_provider` runs before `is_internal_config`, so `ALLOWED_ORIGINS=*` became a service with a "credential" for two projects — LIVE in the registry after the real run | native Opus, pass 2 (SQL on the live registry) | FIXED · LOCAL — code-only identified/tombstoned entries carry `match: []` (`code_only_provider`); an `INTERNAL_EXACT` name is never matched to a provider; the live `allowed` entry repaired to `match: []` (next sync returns ALLOWED_ORIGINS to internal-config); tests `test_code_only_entries_carry_no_match_prefix`, `test_internal_config_name_beats_a_catalog_match_prefix` |
| G2 | `HOST_RE` captures URL userinfo (`https://allowed.com@evil.com` → `allowed.com`) | native Opus, pass 2 | FIXED · LOCAL — optional `[^/\s@]+@` skipped before the host group; `test_host_re_skips_userinfo` |
| G3 | `kind` by VALUE shape labels password-bearing proxy URLs (`NAMECHEAP_PROXY_URL=http://u:pw@…`) as public `code-host` | native Opus, pass 2 (live rows) | FIXED · LOCAL — `kind` = `code-host` only for the synthetic `CODE_HOST_URL` key; `test_kind_is_the_synthetic_key_never_a_value_shape` |
| G4 | `CREDENTIAL_KEY_RE` is `$`-anchored: `GROQ_API_KEY_2` never feeds the fetcher (latent: all four fetcher providers match today) | native Opus, pass 2 | FIXED · LOCAL — first non-URL value is the fallback when no key name matches; same test |
| G5 | error-budget counts persist for providers that left the queue (bypasses the 3-run budget on re-flag; file grows unbounded) | native Opus, pass 2 | FIXED · LOCAL — counts pruned to the currently flagged set; `test_error_budget_forgets_providers_that_left_the_queue` |
| G6 | `_read_json` returns any JSON shape → a bare-string cursor file crashes classify → chain DEAD | native Opus, pass 2 | FIXED · LOCAL — typed default (`isinstance(obj, type(default))`); `test_state_reads_are_typed` |
| G7 | `os.open(O_CREAT, 0o600)` keeps a leftover tmp file's old 0644 mode → the O13 window returns after a crash | native Opus, pass 2 | FIXED · LOCAL — `unlink(missing_ok)` + `O_EXCL`; `test_leftover_tmp_from_a_crashed_run_is_recreated_0600` |
| G8 | `?`-header adoption gated on `not provider`: an env-derived unknown whose host names a CATALOGUED vendor stays in triage and is re-billed per lap | native Opus, pass 2 | FIXED · LOCAL — such a bucket adopts the catalog meta and leaves triage; `test_env_derived_unknown_adopts_the_catalogued_vendor_its_host_names` |
| G9 | all-or-nothing heartbeat gate: a failed classify (credits, pool) reports a FRESH registry as DEAD | native Opus, pass 2 | FIXED · ARCHITECTURAL (one correct form; covered by D-081's amended text) — gate on the DATA steps (`core_failed`: gather, reconsolidate, registry_sync); classify failure alerts only; chain test asserts the core set |
| G10 | `gen_dashboard` writes non-atomically → a half file with a fresh mtime reads LIVE | native Opus, pass 2 | FIXED · LOCAL — tmp + `os.replace`; `test_gen_dashboard_write_is_atomic` |
| G11 | standalone `gen_dashboard` before any new sync → `UndefinedColumn kind` | native Opus, pass 2 | FIXED · LOCAL — `information_schema` probe; every row counts as a credential when the column is absent |
| G12 | the hook runs the chain before `autocommit_pipeline_outputs.sh`, whose stage list lacks the tracked `service_catalog.json` → daily dirt in the shared tree | native Opus, pass 2 | REFUTED by ruling — the intel session (via the operator, 2026-09-02) rejected auto-committing the catalog: a blind stage would push a session's staged-but-unreviewed lines at boot. The daily dirt is the pre-existing behaviour (classify --apply has run daily since 2026-07-26) and stays the committing session's job; hand-off mail withdrawn (D-081 amended) |
| G13 | sync tests issue DDL (`ensure_schema`) on the real local registry | native Opus, pass 2 (test quality) | REFUTED — the fixture pattern (real local peer-auth registry, skip when unreachable, teardown deletes only the test provider) pre-exists this change; the DDL is the same idempotent `ADD COLUMN IF NOT EXISTS` the daily run performs, destroys nothing, and no-ops after the first run; a throwaway registry is a fixture-level change outside this diff (residual class (a)) |
| G14 | partial-scan test used an ABSOLUTE fixture path (project attribution never asserted) | native Opus, pass 2 (test quality) | FIXED · LOCAL — relative `app/x.py` line + `projects == {"app"}` asserted |
| G15 | churn test's "never picked twice per lap" was asserted only on `seen[:20]`; wrap re-picks are inherent | native Opus, pass 2 (test quality) | FIXED · LOCAL — property restated and asserted: no repeat before the first wrap, every start-set survivor reached before any repeat |
| G16 | inlining check is a substring test that a future COMMENT would trip | native Opus, pass 2 (test quality) | FIXED · LOCAL — scoped to non-comment lines |
| G17 | INDEX.md tree row still described the chain as inside `daily_refresh.sh` without the reconsolidate step or the hook | native Opus, pass 2 | FIXED · LOCAL |
| G18 | `capabilities.json` still named the deleted orchestrator in `credit_fetchers`' summary | native Opus, pass 2 | FIXED · LOCAL — summary text corrected in place (byte-faithful) |
| G19 | `LAST_RUN` assigned and unused in the chain script | native Opus, pass 2 | FIXED · LOCAL — dropped |
| G20 | "tombstoning" printed even without `--tombstone-unresolved`; `classify_last.json`'s `tombstoned` counted already-catalogued skips | native Opus, pass 2 | FIXED · LOCAL — conditional wording; the list is built from the actual tombstone loop |
| W1 | unquoted `$FABRIK_ROOT` breaks on a path with spaces | pool wide ops-tests (gemini-3-flash), closing pass | REFUTED — `daily_refresh.sh` quotes every expansion in the chain call; the hook's `bash -c` block uses the unquoted convention of every other line in it (pre-existing); `/opt/fabrik` has no spaces and the fleet's `/opt/<repo>` layout is an invariant |
| W2 | atomic dashboard write via `os.rename` across filesystems → EXDEV | pool wide ops-tests | REFUTED — the tmp is `out.with_name(…)`, same directory by construction (gen_dashboard.py::main) |
| W3 | `value_sha256` uniqueness too narrow | pool wide ops-tests | REFUTED — the constraint `UNIQUE (service_id, value_sha256)` and its upsert are unchanged by this diff (schema.sql:27) |
| W4 | liveness fails open on a MISSING dashboard file | pool wide ops-tests | REFUTED — `evidence_age` on a missing path returns DEAD/absent (liveness_audit.py:298-302, re-verified by the pass-1 Opus finder); the file is gitignored so a fresh checkout reads honest DEAD |
| W5 | per-file `open()` leak in the scan loop | pool wide ops-tests | REFUTED — the scan is one ripgrep subprocess; no file is opened in Python |
| W6 | hardcoded absolute evidence path is a 12-Factor violation | pool wide ops-tests | REFUTED — `.fabrik/liveness-registry.json` is the BOX-LOCAL audit registry; every one of its 33 surfaces names an absolute or `~` path by design (`docs/reference/liveness.md`) |
| W7 | shell injection through `shell=True` | pool wide ops-tests | REFUTED — `subprocess.run` with a list and `cwd=OPT`, never `shell=True` (gather_envs.py::scan_code_hosts) |
| W8 | `--help` test passes for lack of write permission | pool wide ops-tests | REFUTED — the test `chdir`s into a writable `tmp_path` and the sibling test proves a write there succeeds |
| C1 | CHANGELOG test counts stale against the same staged diff (chain 5→7, gather 27→34, registry +3→+4) | native Sonnet, closing pass (counted `def test_` live vs HEAD) | FIXED · LOCAL — counts restated from the primary source |
| C2 | the D-081 row (uncommitted) still cited 166/124 for a doc that now derives 150/116, and pre-dated the chain-script/core-gate design | native Sonnet, closing pass | FIXED · LOCAL — row amended before its first commit (rows are immutable once committed; this one never was) |
| C3 | `scripts/service_catalog.json` in the blind auto-commit stage list could bundle a sibling's uncommitted hand-edit under the bot's commit | native Sonnet, closing pass | REFUTED by ruling — the intel session rejected auto-committing the catalog at all (G12/H1): no stage-list change exists; D-081 records the ruling |
| H1 | `autocommit_pipeline_outputs.sh`: the G12 line exists only in a stale index blob; the worktree carries a SIBLING's in-flight rewrite (their untracked review names the file) that dropped it — committing either way destroys one session's work | native Opus, closing pass (index vs worktree) | REFUTED by ruling (see G12) — the file is the sibling's live WIP and the addition is withdrawn; that path is NOT in this commit |
| H2 | the nine code-only providers identified by the 13:49 run were minted with bare-word `match` prefixes before the G1 guard landed | native Opus, closing pass (parsed all-envs.env) | FIXED · LOCAL — the nine rows set to `match: []` in the same change; the identified path now guarded by `test_identified_code_only_providers_get_no_match_prefix` (H10) |
| H3 | the internal-config precedence checked only `INTERNAL_EXACT`, case-sensitively: `ANTHROPIC_READ_TIMEOUT` was a live "credential" of `anthropic` in the registry | native Opus, closing pass (live SQL) | FIXED · LOCAL — `is_internal_config(key)` (upper-cased) wins over any prefix; test extended with the SUBSTR and lowercase cases; re-synced: the timeout line is back in internal-config and anthropic's rows are code-host only |
| H4 | five table rows with unescaped `\|` (SES row 11 had 15 cells in a 3-column table) — D3's spot fix missed three and mis-fixed one | native Opus, closing pass | FIXED · LOCAL — whole-file grader (cells per row vs header) escaped 5 rows; grader reports 0 |
| H5 | `ensure_schema`'s ALTER inside the sync transaction held ACCESS EXCLUSIVE on `api_keys` for the whole run (measured in pg_locks) | native Opus, closing pass | FIXED · LOCAL — schema first, in its own short transaction, before the sync transaction; re-synced live |
| H6 | six statements said the heartbeat needs "every step"; the reference doc's § Liveness said the inverse ("always writes") | native Opus, closing pass | FIXED · LOCAL — all seven statements say DATA steps (gather, reconsolidate, registry_sync) |
| H7 | INDEX row + D-081 clause (c) still said "day-rotating window" | native Opus, closing pass | FIXED · LOCAL — cursor-walked, with the re-measured queue (254 of 371 unresolved on 2026-09-02) |
| H8 | INDEX test row "6 tests" vs 7 collected | native Opus, closing pass | FIXED · LOCAL |
| H9 | `test_gen_dashboard_write_is_atomic` was vacuous (passed on the pre-fix code) | native Opus, closing pass (ran it against a reverted copy) | FIXED · LOCAL — rewritten: `os.replace` spied with the exact (tmp, out) pair + a mid-write failure leaves the old page; proven red on the pre-fix write, green after |
| H10 | the identified path of the `match: []` rule was untested | native Opus, closing pass | FIXED · LOCAL — `test_identified_code_only_providers_get_no_match_prefix` |
| H11 | `capabilities.json`/`docs/CAPABILITIES.md` removed the orchestrator but never gained the chain script | native Opus, closing pass (ran the generator to a temp dir) | FIXED · LOCAL — entries added by text in the generator's shape (a full regeneration would churn three sibling-owned names) |
| H12 | Pass 2 row's REFUTED enumeration summed to 18, not 25 | native Opus, closing pass | FIXED · LOCAL — 18 |
| H13 | the report embeds no `final_gate.py --json` success block yet | native Opus, closing pass | FIXED · LOCAL — embedded at the exit under § Gate evidence (the committed mid-loop copy lacked it — Z5) |
| H14 | the `kind` probe was not schema-qualified | native Opus, closing pass | FIXED · LOCAL — `table_schema = current_schema()` |
| H15 | a manual `--only` run with >10 names moved the shared daily cursor | native Opus, closing pass | FIXED · LOCAL — the cursor is never written on `--only`; `test_only_run_never_moves_the_shared_cursor` |
| H16 | `OWN_HOST_SUFFIXES` matched `notocoron.com` by bare `endswith` | native Opus, closing pass | FIXED · LOCAL — dot-prefixed entries, apex kept; asserted |
| H17 | D-081's "~215 unknown" was undated | native Opus, closing pass | FIXED · LOCAL — re-derived and dated (254 of 371, 2026-09-02) in the uncommitted row |
| N2 | `api_keys` rows were upsert-only: a key that leaves a provider (a code host no longer referenced, a var reclassified as internal-config) survived as a phantom — `anthropic` kept a "credential" row after H3 moved its timeout var back to internal-config | orchestrator, closing pass (live SQL after the H3 re-sync) | FIXED · LOCAL — per-service prune of rows whose digest is not in this run's set (never global; GUARD 1 still protects an empty parse); `test_stale_key_rows_are_pruned_per_service`; live: anthropic → code-host rows only |
| N3 | `ensure_schema`'s unconditional `ADD COLUMN IF NOT EXISTS` takes ACCESS EXCLUSIVE and waits behind ANY open reader transaction — a measurement connection with one SELECT open stalled the sync past 120 s; an idle dashboard-server read would do the same to the daily run | orchestrator, closing pass (measured: the hang, then pg_stat_activity) | FIXED · LOCAL — the column is probed (ACCESS SHARE, schema-qualified) and the ALTER runs only when absent, i.e. once ever; `test_ensure_schema_never_alters_when_the_column_exists` |
| V1 | my liveness-registry rewrite re-serialized 13 sibling comment lines (`\u2014` escapes → literal em-dashes), churning a shared file | pool wide code (deepseek-v4-flash), pass 4 | FIXED · LOCAL — re-serialized with the file's own `ensure_ascii` escaping; the diff is the one added surface again |
| V2 | the chain script's capability `status: manual` while cron runs it | pool wide docs (qwen3-max), pass 4 | REFUTED — the generator's vocabulary is `ok`/`manual`/`retired`; `daily_refresh.sh` itself is `manual` (capabilities.json:2569); the value means "not liveness-verified by the generator", not "run by hand" |
| V3 | D-081 said the catalog "rides" the auto-commit stage list — a claim now false (routed, then rejected) | pool wide docs, pass 4 | FIXED · LOCAL — row states the intel ruling: the catalog stays off the list, the daily dirt is the committing session's job |
| V4 | 23 further pass-4 pool candidates | pool wide ×3, pass 4 | REFUTED — each cites a standing row: fresh-checkout evidence (W4), `--help` (P-era argparse in the diff), credit_fetchers invoke line (pre-existing), schema default (Q6/Q12), retired script in INDEX (deleted in the diff), Vercel/Azure/M365 cells (dated vendor citations by contract; handling is 58's), scan parse errors (W5: ripgrep, no Python reads), kaizen stamp (outside the diff), unquoted vars (Q4), zombie references (grep: none), `http://` (HOST_RE is `https?`; dynamic hosts are out of scope by design, documented), cursor corruption (G6), daily-lock atomicity (pre-existing mechanism outside the diff), JWT rubric (no such code in the diff), heartbeat on partial failure (O2/G9), `kind: legal` (Q13), 150 vs 158 (D1), CLI-only Vercel use (documented inputs), schema backfill (Q6), INDEX wording (opinion), stage-list test (moot after the ruling) |
| N4 | the per-service key prune (N2) with an EMPTY digest list: `value_sha256 <> ALL('{}')` is TRUE for every row, so a block whose values are all empty would wipe the provider's keys | orchestrator, pass 5 (raised while briefing the finder; executed the SQL semantics) | FIXED · LOCAL — the DELETE runs only when the run produced ≥1 digest; `test_key_prune_skips_a_provider_with_no_values`; post-commit delta covered by a scoped pool unit + the Pass 5 finders |
| X1 | 14 pass-5 wide-pool candidates (code unit: none) | pool wide ×3, pass 5 on c2b1ad2f | REFUTED — each cites a standing row or lies outside the commit: chain fail-open (O2/G9 gate), cursor head pinned by an erroring provider (O8: tombstoned after 3 laps), `tests/test_quota_dashboard.py` / `tests/test_claude_rotate_v2.py` / `scripts/sysadmin/quota_dashboard.py` / `scripts/dashboard_server.py` (not in this commit — CHANGELOG context lines), daily-lock race (pre-existing mechanism, outside the diff), f-string hosts (documented scope: literals), classify failure vs heartbeat (G9 by design), schema default/backfill (Q6/Q12), CHANGELOG 158→166 (the REMOVED line in the diff, D1), CLAIMS kind (Q13), chain-test coverage of flock (pre-existing lock), retired-script residue in INDEX (grep: 0) |
| T1 | H3's precedence demoted REAL vendor secrets that carry a config token — `SUPABASE_DB_PASSWORD` / `SUPABASE_DB_URL` (via `_DB_PASSWORD`/`_DB_URL` in INTERNAL_SUBSTR) left the `supabase` block for internal-config — the MIRROR of H3, measured live in the generated file | native Sonnet, pass 4 (exhaustive cross-check of the 12 internal-substring hits) | FIXED · LOCAL — a vendor-prefixed key with a config token is internal only when its VALUE is not secret-shaped (`is_secret`); `ANTHROPIC_READ_TIMEOUT=120` → internal, `SUPABASE_DB_PASSWORD=…` → supabase; both asserted; live file re-generated: Supabase keys back under their block |
| T2 | the classify tests' fake `fanout` broadcast `results[0]` to every unit — a names/results misalignment regression would be invisible | native Sonnet, pass 4 (test quality) | FIXED · LOCAL — positional fixture with a length assertion |
| T3 | CHANGELOG test counts re-drifted (34/10 vs 36/13) | native Sonnet, pass 4 | FIXED · LOCAL — 36 / 13 (re-derived by `grep -c` at the exit) |
| T4 | `conn` leaks if `ensure_schema` raises before the sync's try/finally | native Sonnet, pass 4 | FIXED · LOCAL — closed on failure, re-raised |
| T5 | D-081's "254 of 371" outpaced by same-day classify laps | native Sonnet, pass 4 | FIXED · LOCAL — timestamped to the measurement ("at 2026-09-02 14:30, before the first classify laps") |
| Y1 | the empty-digest guard is "fail-open": clearing every value of a provider no longer revokes its keys | pool scoped code (deepseek-v4-flash) + ops + docs, pass 5 | REFUTED — an all-empty block is indistinguishable from a torn parse, and prune-nothing is the same safe direction as GUARD 1 (empty parse never prunes); a key removed while others remain still prunes (digests non-empty — N2's test) |
| Y2 | the N4 test passes `prune=False` so the DELETE "is not exercised" | pool scoped code + ops, pass 5 | REFUTED — the per-service key prune is independent of the SERVICE prune flag (registry_sync.py: the DELETE sits inside the provider loop, gated only on `digests`); `test_stale_key_rows_are_pruned_per_service` runs with `prune=False` and observes the DELETE |
| Y3 | a service removed from the file yields an empty digest list | pool scoped ops (gemini), pass 5 | REFUTED — a removed provider is never iterated (the loop is over parsed blocks); the SERVICE prune handles it (bounded) |
| Y4 | URL-only provider skips the credit fetch | pool scoped ops + code, pass 5 | REFUTED — by design (Q7) |
| Y5 | the test leaks a connection | pool scoped ops, pass 5 | REFUTED — `conn.close()` is called (test body) |
| Y6 | the guard test does not cover a later non-empty prune | pool scoped docs (qwen3-max), pass 5 | REFUTED — `test_stale_key_rows_are_pruned_per_service` (N2) is exactly that case |
| Z1 | H3's precedence flip demoted 8 catalogued-vendor credentials (MINIO_ROOT_PASSWORD, GRAFANA_ADMIN_PASSWORD, N8N_ENCRYPTION_KEY, M365_CERT_*, …) to internal-config and N2 then pruned their key rows | native Opus, pass 5 on c2b1ad2f (live sweep over the 21 .env files) | FIXED · LOCAL — same root as T1: the secret-shaped tiebreak (`is_secret`) keeps every credential with its vendor; verified live after regeneration: MINIO/GRAFANA/N8N/M365 keys back under their blocks, timeouts internal; the pass-6 Sonnet finder re-classified all 11 collisions on the real inputs: all on the right side |
| Z2 | a `--only` run computed `all_flagged` AFTER the filter → wiped every other provider's error-budget counts and clobbered `classify_last.json` | native Opus, pass 5 (executed in-process) | FIXED · LOCAL — `all_flagged` taken before the filter; a hand-picked run writes neither the budget nor the cursor and keeps its own last-run file; `test_only_run_leaves_the_error_budget_of_other_providers_alone` |
| Z3 | the hand-written capability entries were not in the generator's shape (MD rows exist only for `status: ok`) — tomorrow's generator run would undo them | native Opus, pass 5 (ran `_classify_script`) | FIXED · LOCAL — `generate_capability_index.py` run for real; both files now carry the generator's own output (the chain script's entry with its AFTER-EDIT summary; siblings' committed scripts included as the generator sees them) |
| Z4 | commit c2b1ad2f bundled the sibling's UNCOMMITTED CHANGELOG entries (rotation/quota work, none of whose files were in the commit) — a HARD STOP violation, irreversible on a shared tree | native Opus, pass 5 (`git log -S`) | FIXED-by-record · LOCAL — no rewrite (a sibling commit sits on top); LESSONS row added; every later commit of this run stages shared docs SURGICALLY (HEAD + my hunks only) and verifies with `git diff --cached` that no foreign hunk rides along |
| Z5 | H13 claimed a gate-evidence block "below" that the committed report lacked; C3 claimed a stage-list comment that never existed | native Opus, pass 5 | FIXED · LOCAL — C3 re-ruled (D-081 only, no stage-list change, per the intel ruling); H13's block is embedded at the exit under § Gate evidence |
| Z6 | committed CHANGELOG counts (34/10) contradicted the commit's own "55 tests" | native Opus, pass 5 | FIXED · LOCAL — T3 (re-derived at the exit) |
| Z7 | the per-service key prune ignored `prune=False` (the partial-file contract) | native Opus, pass 5 | FIXED · LOCAL — `prune_keys` (default = `prune`); `test_partial_file_sync_keeps_a_providers_other_keys`; the N2/N4 tests pass `prune_keys=True` explicitly |
| Z8 | connection leak when `ensure_schema` raises | native Opus, pass 5 | FIXED · LOCAL — T4 |
| Z9 | after a failed scan the paid classify step still ran against yesterday's queue | native Opus, pass 5 | FIXED · LOCAL — classify gated on `core_failed == 0`; chain test asserts it |
| Z10 | a catalog entry left at `category: "?"` was skipped by the tombstone loop and re-billed every lap (latent: 0 of 109) | native Opus, pass 5 | FIXED · LOCAL — such an entry is re-stubbed; `test_a_catalog_entry_left_at_question_mark_is_restubbed` |
| Z11 | the 0600 spy asserted the process-wide `os.open` call list | native Opus, pass 5 (test quality) | FIXED · LOCAL — filtered to the `.tmp` path |
| Z12 | fake `fanout` broadcast `results[0]` | native Opus, pass 5 | FIXED · LOCAL — T2 |
| Z13 | `daily_refresh.sh` header inventory numbered 1, 2, 4, 5, 6 | native Opus, pass 5 | FIXED · LOCAL — renumbered |
| Z14 | reference doc's two bullets disagreed on scanned types (Dockerfiles) | native Opus, pass 5 | FIXED · LOCAL |
| Z15 | the `url` adoption mutated a bucket meta dict that may alias a catalog entry | native Opus, pass 5 | FIXED · LOCAL — copy-on-write |
| S6b | dead `elif not provider and is_internal_config(key): provider = None` (proven no-op by deletion + 36 green) | native Sonnet, pass 6 | FIXED · LOCAL — removed |
| AA1 | 10 pass-7 wide-pool candidates (code unit's JSON arrived fenced — one item) | pool wide ×3, pass 7 on c2b1ad2f~1..f2370abd | REFUTED — liveness evidence path on a fresh checkout (W4), unquoted vars (Q4), `kind: standard` (Q13), the doc path (`docs/reference/external-services-registry.md` exists; the finder misread INDEX's tree row), CHANGELOG "earlier says 34 / 158→166" (those are the REMOVED lines of the diff — D1/T3), and four items about `guard_selection_freshness.py` / `quota_dashboard.py` / `claude_rotate.py` — sibling commits interleaved inside the `c2b1ad2f~1..f2370abd` range, outside this review's surface (a range diff on shared master includes siblings' commits; the native finders were told the two commit hashes explicitly) |
| AB1 | `--only` with more names than `--max-per-run` silently dropped the extras (the cap applied to an operator-typed list, cursor-seeded) | native Sonnet, pass 7 (executed) | FIXED · LOCAL — `--only` is never bounded; `test_only_list_is_never_capped` |
| AB2 | CHANGELOG test counts drifted again with the commit's own additions | native Sonnet, pass 7 | FIXED · LOCAL — re-derived by `grep -c` at the exit only (every mid-loop restatement went stale within the same commit — six times), staged surgically |
| AB3 | a platform host already catalogued under ANOTHER key (`safebrowsing.googleapis` → `google-safe-browsing`, `generativelanguage.googleapis` → `gemini`) became a duplicate triage identity re-billed every lap | native Sonnet, pass 7 (live consolidation) | FIXED · LOCAL — when the classifier's returned name IS an existing catalog key, the host is recorded on that entry's `hosts` list (no duplicate key); `catalog_url_index` indexes `hosts` exactly, so the scan attributes it and it leaves triage; `test_identified_platform_host_merges_into_the_existing_catalog_entry` |
| AC1 | `derive_provider` split numbered keys into pseudo-vendors (`groq`, `groq_api_key_2`, `groq_api_key_3` — three paid classify units and three registry rows for one account, self-reinforcing once a `GROQ_API_KEY_2` prefix lands in the catalog) | native Opus, pass 7 (live) | FIXED · LOCAL — a trailing `_<n>` is stripped first; `test_numbered_keys_belong_to_one_provider` |
| AC2 | the Z2 grader was vacuous: `--only` never writes the budget file, so moving `all_flagged` back after the filter still passed (proven by an isolated revert) | native Opus, pass 7 (executed) | FIXED · LOCAL — the test now asserts the DAILY path: a 10-of-15 slice keeps the deferred providers' counts; `all_flagged` is live on that path (it prunes counts to the whole queue, not the slice) |
| AC3 | `--only` selection was seeded by the shared cursor and could skip a named provider forever | native Opus, pass 7 (executed) | FIXED · LOCAL — AB1 (never capped, never cursor-seeded) |
| AC4 | CHANGELOG counts stale again (5th recurrence) | native Opus, pass 7 | FIXED · LOCAL — re-derived once at the exit (AB2) |
| AC5 | a `timeout` kill of classify persisted nothing → the identical slice re-billed every day forever | native Opus, pass 7 | FIXED · LOCAL — the cursor is persisted BEFORE the paid dispatch; `test_cursor_is_persisted_before_the_paid_dispatch` (fanout raising → cursor advanced) |
| AC6 | `kind` closed only the CODE_HOST_URL half: 97 of 278 "credential" rows were config knobs (`IPROYAL_HOST`, `RAPIDAPI_DELAY`, `SONIOX_MODEL`, …) still inflating the dashboard's key count | native Opus, pass 7 (measured live) | FIXED · LOCAL — a third `kind` = `config` when `is_secret` says no (one classifier, shared with the scan); the dashboard counts credentials only; `test_config_knobs_under_a_vendor_are_kind_config_not_credential` |
| AC7 | the value tiebreak overrode an EXPLICIT `INTERNAL_PREFIX` declaration: `M365_CERT_THUMBPRINT` (a 40-hex public fingerprint) became a Microsoft credential | native Opus, pass 7 (live) | FIXED · LOCAL — `internal_reason`: exact/prefix declarations always win; only generic substr tokens use the value tiebreak; `test_explicit_internal_prefix_wins_even_for_a_secret_shaped_value` |
| AC8 | (sibling file) the freshness-guard test counts its denominator off the shared worktree | native Opus, pass 7 | ROUTED → intel (mail filed, see FEEDBACK) — `scripts/kilo-benchmarks/tests/test_autocommit_freshness_wiring.py` is commit `cfa906bc`, outside this review's surface (swept in by the range diff) |
| AC9 | (sibling file) the wiring tests exercise a hand-copied string | native Opus, pass 7 | ROUTED → intel (same mail) |
| AC10 | an `--only` run consumed error budget it refused to record and could tombstone from it | native Opus, pass 7 | FIXED · LOCAL — on `--only` the budget is neither spent nor recorded (`exhausted = ∅`); asserted in the AC2 test |
| AC11 | the `?` re-stub overwrote a curated `match` list | native Opus, pass 7 | FIXED · LOCAL — `match`/`hosts` preserved on re-stub; `test_restub_keeps_a_curated_match_list` |
| AC12 | identical-value collisions decide `kind` last-writer (a CODE_HOST_URL equal to a base-URL var) | native Opus, pass 7 | FIXED · LOCAL via AF1 — `kind` is decided by the key NAME, so a `CODE_HOST_URL` row and a `*_URL` config row are both non-credential whatever the value's entropy (the pass-7 refutation's premise was wrong — AF10) |
| AC13 | the reconsolidate ran after a failed scan — two identical alerts, a second 900 s window | native Opus, pass 7 | FIXED · LOCAL — gated on `core_failed` like classify; chain test asserts it |
| AC14 | a failed dashboard render left `…html.tmp` behind | native Opus, pass 7 | FIXED · LOCAL — unlinked before and after; the atomic test asserts no tmp remains |
| AD1 | `kind` by `is_secret` value entropy called public identifiers credentials (`M365_CLIENT_ID`, `M365_TENANT_ID`, `GOOGLE_CLIENT_ID`, `R2_ACCOUNT_ID` — 26 live pairs) — the AC6 class half-closed | native Sonnet, pass 8 (live) | FIXED · LOCAL — `is_credential`: credential-shaped NAME, else identifier-suffixed names (`_ID`, `_NUMBER`, `_ARN`, `_REGION`, `_HOST`, `_PORT`, `_MODEL`, `_VERSION`) are config, else value entropy; `test_public_identifiers_are_config_not_credentials` |
| AD2 | the AB3 merge covered only code-only providers: an env-keyed triage provider identified as an existing vendor still minted a second catalog identity | native Sonnet, pass 8 | FIXED · LOCAL — such a provider lends its key prefix to the existing entry's `match` list; `test_identified_env_keyed_provider_joins_the_existing_vendors_match_list` |
| AE1 | 5 pass-8 wide-pool candidates (code + docs units: none) | pool wide ×3, pass 8 | REFUTED — four describe sibling CHANGELOG entries that ride in the CHANGELOG hunk's context (quota hook, rotation probe, dashboard flip, `node --check` — not this review's code); one is the G9 DATA-steps gate by design |
| AF1 | `kind` by value entropy still called 48 live vendor rows credentials (23 plain API URLs, 13 tenant/account ids) — AC6/AD1 closed only half | native Opus, pass 8 (measured over the 21 .env files) | FIXED · LOCAL — `is_credential` is NAME-driven (credential-shaped name → yes; URL/identifier-shaped name → no unless the value embeds `user:pw@`; else the value); registry test extended with an API URL (config) and a DSN with a password (credential) |
| AF2 | a host merged into its vendor (AB3) was ALSO tombstoned on the daily argv (`--tombstone-unresolved`) — reproduced in a sandbox | native Opus, pass 8 (executed) | FIXED · LOCAL — merged providers skip the stub loop; the AB3 test now runs the daily argv |
| AF3 | AC5's persist-before-dispatch made the documented DRY RUN move the shared cursor | native Opus, pass 8 (executed) | FIXED · LOCAL — the cursor is written only on `--apply`; `test_dry_run_moves_no_cursor` |
| AF4 | the AC14 grader was vacuous (the fake write raised before creating the tmp) | native Opus, pass 8 (executed on a neutered copy) | FIXED · LOCAL — the fake write creates the tmp then dies; the assertion now discriminates |
| AF5 | the AC10 grader was vacuous (`_Res.error` always None → `errored` always empty) | native Opus, pass 8 (executed on a neutered copy) | FIXED · LOCAL — `_Res(error=…)`; `test_only_run_with_a_transport_error_records_and_tombstones_nothing` (three erroring `--only` runs: no budget row, no stub) |
| AF6 | CHANGELOG counts stale at HEAD (6th recurrence) | native Opus, pass 8 | re-ruled NOT FIXED at pass 9 (AJ5: the exit re-derivation ran before the pass-8 tests were written — 44/15 written, 49/16 at fae20651); closed under AJ5 — the counts are computed in-script from `^def test_` as the doc patch's last act |
| AF7 | five places enumerated `kind` as two values while the code writes three | native Opus, pass 8 (live: 181 / 97 / 371) | FIXED · LOCAL — schema comment, INDEX, reference doc, registry_sync docstrings, CHANGELOG name `config` |
| AF8 | the IDENTIFIED path overwrote a curated entry's `match`/`hosts` (AC11 covered only the stub path) | native Opus, pass 8 | FIXED · LOCAL — preserved on both paths; `test_identified_curated_entry_keeps_its_match_and_hosts` |
| AF9 | AC7's "prefix always wins" could hide a credential-shaped secret under a generic INTERNAL_PREFIX token (`PROXY_`, `VPS_`…; 0 live) | native Opus, pass 8 | FIXED · LOCAL — a prefix declaration yields only when BOTH the name and the value say credential; `test_generic_internal_prefix_never_hides_a_credential_shaped_secret` |
| AF10 | AC12's refutation rested on a false premise (`https://registry-1.docker.io` IS secret-shaped by entropy) | native Opus, pass 8 | FIXED · LOCAL — closed by AF1: `kind` is name-driven, so a `CODE_HOST_URL` row never competes with a credential-named row; AC12 re-ruled FIXED via AF1 |
| AF11 | process-wide spies on `os.replace`/`Path.write_text` | native Opus, pass 8 | FIXED · LOCAL — the replace assertion is membership, not equality; the write patch is restored by pytest |
| AF12 | `--only foo` with an unflagged name is silently dropped | native Opus, pass 8 | FIXED · LOCAL — missing names printed to stderr with the queue size |
| AF13 | `timeout` without `--kill-after` lets a SIGTERM-ignoring child outlive the budget | native Opus, pass 8 | FIXED · LOCAL — `timeout -k 30`; chain test asserts it |
| AF14 | `hosts` trusted to be a list (a hand-edited scalar breaks the merge and seeds one-char index keys) | native Opus, pass 8 | FIXED · LOCAL — scalar-safe on both sides |
| AF15 | the archived plan-lock still lists the retired orchestrator | native Opus, pass 8 | REFUTED — `.fabrik/plan-locks/2026-07-18-…json` is the historical lock of an ARCHIVED plan (D-079's own precedent: plan sets are immutable history), outside the review's paths; no check resolves it |
| AG1 | commit `fae20651` bundled a sibling's 26-line uncommitted CHANGELOG entry (scaffold deploy-verifiability, D-082) — the Z4 class again; the "surgical index" method was a no-op because a PATHSPEC commit takes the worktree, not the index | orchestrator, pass 9 (found by reading `git diff fae20651~1 fae20651 -- CHANGELOG.md`) | FIXED-by-record · LOCAL — no rewrite (pushed; content correct); mail to fleet so it is not re-added; LESSONS row corrected to name the mechanism; every later shared-doc commit uses the worktree-swap method and is proven by `git show HEAD -- <file>` |
| AH1 | `is_credential`'s unanchored name branch (`gather_envs.SECRET_KEY_RE`: `_AUTH`, `DSN`, `SIGNING`…) made `WEBSHARE_IP_AUTH=false` a credential — live in the registry | native Sonnet, pass 9 (live) | FIXED · LOCAL — only the ANCHORED suffix regex short-circuits; an unanchored token counts only with a secret-shaped value; `test_unanchored_secret_token_needs_a_secret_shaped_value` |
| AH2 | a credential that is a userinfo DSN (`NAMECHEAP_PROXY_URL=http://u:pw@…`) was never eligible as the credit-fetcher input (both branches excluded URL-shaped values) — masked live by `NAMECHEAP_API_KEY` | native Sonnet, pass 9 | FIXED · LOCAL — the fetcher gets the first `credential`-kind row whatever its shape (the `fallback_value` split removed); same test asserts a DSN-only provider feeds the fetcher |
| AI1 | 4 pass-9 wide-pool candidates (code + docs units: none) | pool wide ×3, pass 9 | REFUTED — three describe sibling CHANGELOG entries riding in the hunk's context (quota-stop hook, parity stub, dashboard reload); one is the G9 gate by design (a failed gen_dashboard leaves the OLD mtime — exactly the ageing heartbeat) |
| AJ1 | AF9 made the explicit-prefix tiebreak ONE-factor: `is_secret` is true on the name alone, so `M365_CERT_KEY_FILE=/opt/…/x.pem` (a path under `M365_CERT`) became a microsoft-365 credential — reproduced live: with the real catalog pinned, exactly 1 of 279 service keys across 22 `/opt/*/.env` files moves (that one, out to internal-config) between HEAD and the fix, 0 enter | native Opus, pass 9 | FIXED — the value half is `credential_grade`, and `credential_grade` now rejects a POSIX path (`PATH_VALUE_RE`, ≥2 word segments — a base64 secret carries `+`/`=`); comment at `gather_envs.py` + `external-services-registry.md` reworded; grader `test_generic_internal_prefix_never_hides_a_credential_shaped_secret` now asserts BOTH sides (PROXY_API_KEY stays the vendor's, M365_CERT_KEY_FILE goes internal) — red-on-revert in a sandbox copy (mutated back to `is_secret` → 1 failed; restored → passed) |
| AJ2 | AF2's `or prov in merged_into` guard had no grader: reverted in a sandbox, 49/49 stayed green; the AB3 test ran `--apply`, not the daily `--apply --tombstone-unresolved` | native Opus, pass 9 | FIXED — `test_identified_platform_host_merges_into_the_existing_catalog_entry` runs the daily argv; sandbox: guard reverted → 1 failed, restored → passed |
| AJ3 | AF14's two scalar-`hosts` guards had no grader (both reverted → 49/49 green) | native Opus, pass 9 | FIXED — `test_a_hand_edited_scalar_hosts_entry_is_tolerated_on_both_paths` (index: one key, no one-char keys; classify merge appends to the scalar); sandbox: index guard reverted → 1 failed; merge guard reverted → 1 failed; restored → passed |
| AJ4 | `IDENTIFIER_KEY_RE` is `_ID$`-anchored, so a tenant qualifier escapes it: `CLOUDFLARE_ZONE_ID_OCORON` ×3 (public zone ids), `RESTIC_REPOSITORY`, `B2_PROXY`, `DNA_USER`, `N8N_WEBHOOK_{CONTENT,DEPLOY}` were `kind='credential'` by value entropy — survives AH1 | native Opus, pass 9 | FIXED — the identifier token may carry ONE trailing qualifier (`(?P<qualifier>_[A-Z0-9]+)?$`), a qualifier that is itself a secret token (`_HOST_AUTH`) never demotes; locators (`_FILE`, `_PATH`, `_THUMBPRINT`, `_REPOSITORY`, `_REPO`, `_WEBHOOK`, `_PROXY`) and account names (`_USER`, `_USERNAME`) added; `DATAFORSEO_API_BASE64` stays a credential (it IS one — base64 `login:password`); grader `test_identifier_survives_a_qualifier_and_a_locator_is_never_a_credential` — sandbox: qualifier group emptied → 1 failed, restored → passed |
| AJ5 | CHANGELOG test counts stale at HEAD for the 7th time (44/15 vs 49/16 at fae20651) — AF6's "re-derived" ran before the pass-8 tests were written | native Opus, pass 9 | FIXED — AF6 re-ruled NOT FIXED; counts are now computed by `grep -c '^def test_'` INSIDE the doc-patch script as its last act (50 / 18 / 7 at this write) — no hand-typed count remains |
| AJ6 | fleet-index denominator "150 = 143 blocks + 7 Retired rows" double-counts `Supabase` and `Promtail` (each has a block AND a Retired row): distinct = 148; propagated into immutable D-081 and the CHANGELOG | native Opus, pass 9 | FIXED — header sentence names the overlap (148 = 143 + the 5 block-less Retired rows), CHANGELOG corrected, ledger row `D-085` supersedes D-081 for the count only; re-derived in-script: 143 blocks, 7 rows, 2 with a block → 148; checker CLEAN |
| AJ7 | `Let's Encrypt` listed under "Removed — zero signal" while `### Let's Encrypt (via Traefik)` is a live block | native Opus, pass 9 | FIXED — removed from the Removed list; in-script cross-check of every Removed name against the block list: 0 leaks in 26 names |
| AJ8 | `test_both_entry_points_run_the_same_chain_script_and_inline_no_step` asserted the path against RAW text — a comment naming the script passed | native Opus, pass 9 | FIXED — asserts against CODE with full-line AND trailing comments stripped; proven on the real file with the backup method: line 195 → `true  # …chain.sh` → 1 failed; own-line comment + `true` → 1 failed; restored byte-identical (`git diff --quiet`) → 7 passed |
| AJ9 | the alert decodes only `124 = timeout`, but AF13's `-k 30` makes the SIGTERM-ignoring case exit 137 (probed) | native Opus, pass 9 | FIXED — alert body + `external-services-registry.md` decode 137; grader `assert "137 = SIGKILL" in text` — backup-method mutation → 1 failed, restored → 7 passed |
| AJ10 | AF9's conjunct `SECRET_KEY_RE.search(key) and is_secret(key, value)` is tautological (`is_secret` returns True on the name) | native Opus, pass 9 | FIXED — same edit as AJ1 (`credential_grade(value)` is the value factor; the comment says so) |
| AJ11 | AFTER-EDIT headers under-declared: `gen_dashboard.py: none` (its output is the liveness heartbeat), `registry_sync.py` omits its test and its `gather_envs` dependency | native Opus, pass 9 | FIXED (direction corrected) — the header lists files to UPDATE when the script changes, so the `gather_envs`→`registry_sync` dependency goes on `gather_envs.py` (`scripts/registry_sync.py scripts/tests/test_gather_envs.py` added), `registry_sync.py` adds its test, `gen_dashboard.py` names the liveness registry, the subsystem doc and the chain test |
| AJ12 | the AF9 grader's docstring named `M365_CERT_THUMBPRINT` but never built it, and its `INTERNAL_PREFIX` monkeypatch added a prefix already present (no-op) | native Opus, pass 9 | FIXED — the no-op patch became an assertion that both prefixes ARE in `INTERNAL_PREFIX`; the docstring names what the body asserts (AJ1 grader) |
| AJ13 | AF12's `--only` missing-name report had no grader | native Opus, pass 9 | FIXED — `test_only_list_is_never_capped` passes `,nope` and asserts the stderr line with its `(15 flagged)` denominator via `capsys` |
| AK1 | 5 pass-10 wide-pool candidates (code + docs units: none) | pool wide ×3, pass 10 (deepseek-v4-flash code · gemini-3-flash ops-tests · qwen3-max docs; scored 3/2/3) | REFUTED — four describe sibling CHANGELOG entries riding in the path-filtered hunk's context (the parity-contract stub, the deploy-checklist denominators, the quota-stop hook), outside this surface; the fifth is the G9 heartbeat gate by design (a chain that did not complete MUST age the heartbeat — that is the liveness contract, not alert fatigue) |
| AL1 | `PATH_VALUE_RE` (AJ1) also fullmatched a base64 secret that starts with `/`, carries a second `/` and no `+`/`=` — ≈0.37 % of 40-char values (Opus measured 746/200000; AWS's own doc key with a leading `/` is one) — so a real credential could be graded a locator | native Sonnet, pass 10 (+ Opus AM5) | FIXED — every segment must be a file/dir name (lowercase after its first character): 34 of 34 live path values still fit, 0 of 200 000 synthetic base64 values do; grader `test_path_rule_rejects_paths_but_not_a_slash_bearing_base64_secret` — sandbox: old regex restored → 1 failed, fixed → passed. The first attempt (first segment only) failed its OWN grader on `/Zq8/…` and was tightened before landing |
| AM1 | AH2 made the credit-fetcher input POSITIONAL (`first_value or value` for any credential row) — the N1/O5 key-role guarantee was gone and the comment above the code still asserted it; live: 0 of the 4 providers with a fetcher affected, 2 of 304 blocks (`duplicati`, `google-cloud`) had a non-key credential sorting first | native Opus, pass 10 | FIXED — `credential_rank`: 0 = anchored credential NAME (numbered `_API_KEY_2` included), 1 = non-URL credential value, 2 = userinfo DSN (AH2's only-a-DSN case still feeds); the O5 test's competitor is now credential-KIND (a 26-char account name) so it discriminates — sandbox: rank neutered → O5 test 1 failed, restored → passed. Live: 1 of 304 providers changes input (`dataforseo`: base64 login:pw → `_PASSWORD`; no fetcher exists for it) |
| AM2 | the G4 test had been made green by REORDERING its fixture (`_API_KEY_2` before `_PROXY_URL`) instead of preserving the behaviour; its docstring still said "as the fallback" | native Opus, pass 10 | FIXED — fixture order restored to the discriminating one (DSN line sorts FIRST), docstring says what is asserted; sandbox: rank → constant → 1 failed, restored → passed |
| AM3 | AH1's value conjunct had no grader that survives an unreachable Postgres — 17 of 18 registry tests skip without the local DB; both revert-shaped mutations passed in a DB-less sandbox | native Opus, pass 10 | FIXED — `test_kind_by_name_is_decidable_without_a_database` (pure): AH1, AM6, AM7, AM8, AM11 and the rank order; sandbox: AH1 branch reverted → 1 failed |
| AM4 | `gen_dashboard` injected registry strings into an inline `<script>` via bare `json.dumps` — `</script>` is not escaped, and `provider`/`url` are MODEL-authored upstream (`classify_services` writes the pool model's `url` with no shape check) — a rendered-page proof executed the break-out | native Opus, pass 10 | FIXED (security) — `json_for_script` escapes `<`/`>`/`&` as `\u003c`-style JSON escapes (round-trips as the same value); `classify_services` accepts only `http(s)` model URLs, else `?`; graders `test_dashboard_data_cannot_break_out_of_its_script_tag` + `test_a_model_authored_url_must_be_http_or_https` — sandbox: escaping removed → 1 failed; scheme check removed → 1 failed |
| AM5 | `PATH_VALUE_RE` false-positives on base64 (746/200000 measured) | native Opus, pass 10 | FIXED under AL1 (same finding, Sonnet raised it first) |
| AM6 | the AH1 value conjunct (`credential_grade`) demoted a WEAK but real secret: `DUPLICATI_PASSPHRASE=1234`, `X_SECRET=changeme` → `config`, uncounted on the dashboard | native Opus, pass 10 | FIXED — `PASSPHRASE`/`PASSWD` join the anchored suffixes (name wins); the unanchored branch demotes only a KNOB-shaped value (`KNOB_VALUE_RE`: a bare word ≤12 or a number — `false`, `basic`, `120`), never by credential grade; live: 0 of 278 rows change kind vs b5e5ab8f |
| AM7 | the new `_PROXY` identifier token demoted a scheme-less proxy credential (`B2_PROXY=user:pw@host:port`) — `USERINFO_RE` matched only after `://` | native Opus, pass 10 | FIXED — `USERINFO_RE` accepts the scheme-less form (`(?:^|://)user:pw@`); pure grader; sandbox: old regex → 1 failed |
| AM8 | `_PW`/`_CREDS` are in neither credential regex, so a qualifier `SMTP_HOST_PW` demoted through the identifier escape (latent: 0 of 1084 live names) | native Opus, pass 10 | FIXED — `PW`, `PWD`, `CREDS`, `CREDENTIALS` anchored; pure grader; sandbox: old regex → 1 failed |
| AM9 | the chain test's trailing-comment strip (`\s+#.*$`) is not quote-aware — latent (0 of 241 shell lines diverge today), but `echo "step #1"; python gather_envs.py` would hide an inlined step | native Opus, pass 10 | FIXED — `_shell_code`, a quote-aware scan, self-asserted in the test on both directions; sandbox: quote tracking neutered → 1 failed |
| AM10 | `external-services-registry.md` step-3 row still described the fae20651 fetcher algorithm ("first key whose NAME is a credential, else the first non-URL value"); `registry_sync.py`'s AFTER-EDIT omitted `gen_dashboard.py` | native Opus, pass 10 | FIXED — row describes the rank order (AM1); header lists `gen_dashboard.py` |
| AM11 | (found by this session's live re-measurement of AM6/AM8) `GOOGLE_APPLICATION_CREDENTIALS=/…/sa.json` flipped config→credential once `CREDENTIALS` was anchored — the value is a path | own re-derivation, pass 10 | FIXED — a path-shaped value is a locator BEFORE any name rule; pure grader; sandbox: path check neutered → 1 failed; live re-run: 0 flips in either direction across 278 rows |
| AN1 | 5 pass-11 wide-pool candidates (code + docs units: none) | pool wide ×3, pass 11 (scored 3/2/3) | REFUTED — three are sibling CHANGELOG entries in the hunk's context (quota-stop hook, parity symlink, health-probe vendoring); one is the G9 heartbeat gate by design (a failed CLASSIFY must NOT age the heartbeat — the data steps did run; the alert is the classify signal); one names `dashboard_server.py` from an INDEX row — a pre-existing localhost-bound viewer outside this surface (`git log -S dashboard_server` predates 2026-08) |
| AO1 | AL1's tightened `PATH_VALUE_RE` silently changed `{2,}` to `+`: a ONE-segment lowercase value starting with `/` (`/xk9m2p7q4z8w1n5aa`) fullmatched as a path and, because AM11 checks the path rule before any name rule, `SOME_API_TOKEN=/xk9m…` became config; latent (0 single-segment slash values in 278 live rows) | native Sonnet, pass 11 | FIXED — `{2,}` restored (the comment's own contract); mirror named and closed: a one-segment real DIR value (`UPLOAD_DIR=/uploads…`) now fails the path rule and would be credential-KIND by entropy, so `_DIR`/`_DIRS`/`_FOLDER` join `IDENTIFIER_KEY_RE`; graders in both suites — sandbox: `+` restored → gather test 1 failed AND registry pure test 1 failed; `_DIR` token removed → 1 failed (after the first grader value proved non-discriminating — too short to be secret-shaped — and was lengthened); live: 0 kind flips across 278 rows |
| AO2 | commit `c6b6916d` says "8 new/changed graders proven red-on-revert" while the test-function delta is 7 (4 new + 3 changed) | native Sonnet, pass 11 | ACCEPTED as stated (a commit message is immutable): the 8 are PROOFS, not functions — AM1 and AM2 are two mutations against two tests of the same rank rule, and AM9's stripper is self-asserted inside an existing test; the Pass 10 row now carries that reconciliation |
| AP1 | second injection sink: `cpill` builds a CLASS attribute from `r.cost`/`r.status` unescaped (`json_for_script` protects the literal, not the runtime concatenation into `innerHTML`); `cost`/`status`/`category` are model-authored with no enum check — proven end-to-end: a `cost` of `x"><svg/onload=alert(1)>` survives `svc_line`, `SVC_RE` and `render` and parses as a real `<svg onload>` element; live 0 of 110 catalog entries carry `"`/`<`/`>` | native Opus, pass 11 | FIXED (security) — the class token is restricted to `[A-Za-z0-9-]` in the script, AND `classify_services` accepts only the enum values (`COSTS`, `STATUSES`, `CATEGORIES.split()` — a space-joined STRING, so membership is on the split list, never a substring test) with `?`/`active` defaults; graders: the AM4 classify test now feeds an out-of-enum cost/status/category and asserts the defaults, the dashboard test asserts the sanitizer is in `SCRIPT` and the raw form is not — sandbox: enum guard removed → 1 failed; sanitizer reverted → 1 failed |
| AP2 | an unparseable `#svc` line FAILS OPEN in `registry_sync.parse`: the provider vanishes (pruned, with its credit history) and its keys are upserted under the PREVIOUS provider — a rank-0 key then feeds THAT vendor's credit fetcher with another vendor's secret; reproduced with a three-provider fixture (`cost=free tier`); `svc_line` emits `category`/`cost`/`url`/`status` raw, and a model's `free tier` / `active (beta)` / a newline in `capability` are ordinary answers; live 0 of 110 entries carry whitespace | native Opus, pass 11 | FIXED at both roots — `parse` ends the block on a `#svc ` line `SVC_RE` cannot read and RAISES at the end (fail closed: the sync aborts, the chain alerts, nothing is misattributed); `svc_line` emits one whitespace-free token per field (`free_tier`) and a one-line capability; graders `test_parse_fails_closed_on_an_unreadable_svc_line` + `test_svc_line_never_emits_a_token_the_consumer_cannot_parse` — sandbox: the `#svc ` branch removed → 1 failed; the token normaliser removed → 1 failed |
| AP3 | `credential_rank` left 21 of 24 multi-credential providers tied at rank 0, so line order decided — `supabase` fed its PUBLIC anon key over `SUPABASE_SECRET_KEY`, `backblaze-b2` its id half, `coolify`/`grafana` an admin password over the API token; the docstring still said "never by line order" | native Opus, pass 11 | FIXED — five tiers: fetcher-grade anchored name (`_API_KEY`, `_TOKEN`, `_SECRET_KEY`, `_SERVICE_ROLE_KEY`, `_APPLICATION_KEY`, numbered) → other anchored → PUBLIC/ANON/PUBLISHABLE-marked → non-URL value → DSN; equal ranks break by the file's name-sorted order, now STATED in the docstring; live: 4 of 304 providers change input (b2 → `B2_APPLICATION_KEY`, supabase → `SUPABASE_SECRET_KEY`, coolify → `COOLIFY_API_TOKEN`, grafana → `GRAFANA_SERVICE_ACCOUNT_TOKEN`), 0 kind flips in 278 rows, 12 of 24 multi-credential providers still tie across DIFFERENT fetcher-grade names (`CLOUDFLARE_API_TOKEN` vs `CLOUDFLARE_GLOBAL_API_KEY`) and resolve by the stated rule; grader `test_fetcher_rank_prefers_a_key_over_a_password_and_demotes_public_names` — sandbox: tiers collapsed → 1 failed; public demotion removed → 1 failed (after the first assertion proved non-discriminating and a PUBLIC fetcher-grade name was added) |
| AP4 | `_shell_code` ignored backslash escapes: `echo "a \" # b"; python gather_envs.py` lost the invocation and the no-inlined-step guard failed OPEN (latent: 0 of 493 + 3 of 218 escaped-quote lines, none divergent) | native Opus, pass 11 | FIXED — an escape flag inside double quotes; self-asserted in the test; sandbox: flag removed → 1 failed |
| AP5 | `USERINFO_RE` required a non-empty username, so `redis://:pw@host` (the canonical Redis/AMQP form) was `config` (pre-existing, re-exposed by pass 10; 0 of 2 099 live pairs) | native Opus, pass 11 | FIXED — `[^/@\s:]*` before the colon in the scheme form; grader in the pure rank test; sandbox: old regex → 1 failed |
| AP6 | the scheme-less userinfo form accepted ANY `x:y@…` (`sip:alice@example.com`, `team:oncall@company.com`) as a credential and a fetcher input (0 of 649 live rows) | native Opus, pass 11 | FIXED — the scheme-less form is the proxy shape only (`user:pw@host:port`, AM7's case), grader asserts both `sip:` and `team:` are config and `B2_PROXY` stays a credential |
| AP7 | `PATH_VALUE_RE` accepts a single segment (36 112 of 50 000 `/`+lowercase values) | native Opus, pass 11 | FIXED under AO1 (Sonnet raised it first this pass; `{2,}` restored, `_DIR` identifiers added for the mirror) |
| AP8 | the short anchored tokens (`PASS`, `PW`, `PWD`, `CREDS`, `KEY`) had no left boundary — `TRAEFIK_BYPASS=true`, `SURVEY_MONKEY` would be anchored credentials (0 of 582 live names) | native Opus, pass 11 | FIXED — `(?:^|_)` boundary on the short tokens; grader; sandbox: boundary removed → 1 failed |
| AR1 | 5 pass-12 wide-pool candidates (code + docs units: none) | pool wide ×3, pass 12 (scored 3/2/3; the first two dispatches errored in every unit — see machinery) | REFUTED — a fresh install with no dashboard reads DEAD (correct: a chain that has never completed IS dead, G9); "missing shell quoting" is INFERRED from the CHANGELOG with no line cited (`external_services_chain.sh` passes fixed argv, no catalog value ever reaches a shell); the quota-stop hook and the deploy-verify test are sibling CHANGELOG entries in the hunk's context |
| AQ1 | the AP1 grader for `cpill` was a SOURCE-TEXT substring match on `SCRIPT`, never an execution (node is on the box) | native Sonnet, pass 12 | FIXED — `test_cpill_class_token_is_sanitized_when_the_script_runs` lifts the one-liner and runs it under node with the hostile cost; the whole output must be one span with a `[A-Za-z0-9-]` class and no raw angle bracket (the first assertion captured up to the injected quote and passed — tightened before landing); sandbox: sanitizer reverted → 1 failed. The substring assertion stays as the floor when node is absent |
| AQ2 | the rank docstring's tie clause ("equal ranks break by file order") was asserted nowhere | native Sonnet, pass 12 | FIXED — `test_equal_fetcher_ranks_break_by_file_order` (two rank-0 keys, the first in file order feeds); sandbox: `<` → `<=` → 1 failed |
| AS1 | the AP1 enum guard re-opened the C2 paid loop: `identified` was computed from the RAW answer, the catalog from the GUARDED one — a case/whitespace variant (`AI-LLM`, `Freemium `) was "identified", written as `category: ?`, excluded from the tombstone path, re-flagged by `gather_envs` and re-billed every day; reproduced with a stubbed fanout on the daily argv | native Opus, pass 12 | FIXED — values are `strip().lower()`-normalised before the enum test (so the variants ARE the enum), and a provider the enum still flattens is not identified: it takes the tombstone path and leaves triage; grader `test_a_model_answer_the_enum_flattens_is_not_identified` (both halves) — sandbox: normalisation removed → 1 failed; the not-identified branch removed → 1 failed. The AM4 URL test's fixture moved to a valid category so it tests only the cost/status/url flattening |
| AS2 | AP8's `(?:^|_)` boundary un-anchored every GLUED credential token (`SEDO_SIGNKEY` — 1 of 538 live names — `MASTERKEY`, `DBPASS`): the AM6 class re-opened on the mirror direction AP8 never measured | native Opus, pass 12 | FIXED — `KEY` and `PASS` are anchored glued again, minus the measured English noise as fixed-width negative lookbehinds (MONKEY, TURKEY, HOCKEY, JOCKEY, DONKEY; BYPASS, COMPASS, SURPASS, TRESPASS); the 2–3-letter tokens (`PW`, `PWD`, `CREDS`) keep the boundary; graders for both directions; live: 0 kind flips in 278 rows (the live `SEDO_SIGNKEY` value is credential-shaped either way — the class, not the instance, was open); sandbox: bounded form restored → 1 failed |
| AS3 | `name` was the one `#svc` field `_svc_token` did not cover — a whitespace catalog key would now fail the sync CLOSED every day (AP2) with no self-healing (0 of the catalog's 109 vendor entries today (110 keys incl. `_README`, AT3)) | native Opus, pass 12 | FIXED — `name` tokenised; grader extended (`pro v` → `pro_v`); sandbox: → 1 failed |
| AS4 | `used_by` members were not tokenised and `SVC_RE` has no end anchor, so `used_by=b,pro j` parsed as `['b','pro']` — a wrong project attributed, silently (0 of 59 `/opt` dirs today) | native Opus, pass 12 | FIXED — each member tokenised; grader asserts `b,pro_j`; sandbox: → 1 failed |
| AS5 | the provider link's `href` had no render-time scheme gate: a hand-edited or pre-AM4 catalog url `javascript:…` renders as a live link in the operator's origin (0 of the catalog's 109 vendor entries live — 110 keys incl. `_README`, AT3) | native Opus, pass 12 | FIXED — `href()` admits `http(s)` only, else the provider renders as text; node-executed grader `test_href_gate_rejects_non_http_schemes_when_the_script_runs`; sandbox: gate loosened → 1 failed |
| AS6 | `_shell_code`'s escape flag was double-quote-only: ANSI-C `$'don\'t # care'` closed early and hid an inlined step (0 of 2 entry files use `$'`) | native Opus, pass 12 | FIXED — a `dollar` flag on a `'` opened after `$`; self-asserted; sandbox: → 1 failed |
| AS7 | the tie docstring named NAME order for two values under ONE name, where the discriminator is the deduped value itself (`gather_envs` sorts by `(name, slot)`) — rotating a key can change which value the fetcher reports (deepl, apify, groq, webshare, soniox carry two) | native Opus, pass 12 | FIXED — docstring and the reference doc state the real rule (name-sorted, then value-sorted) and its consequence |
| AS8 | `PUBLIC_NAME_RE` was an unbounded substring test (`CANONICAL_API_KEY` → public; 4 of 649 live matches, all genuinely public) | native Opus, pass 12 | FIXED — bounded `(?:^|_)…(?:_|$)`; grader; sandbox: → 1 failed |
| AS9 | rank inversion: a PUBLIC anon key (2) outranked a real non-anchored secret (3) and a DSN (4) — the "last resort" intent was not what the tiers encoded (0 of 304 providers affected) | native Opus, pass 12 | FIXED — public is tier 4, below the non-URL value (2) and the DSN (3); graders; sandbox: → 1 failed; live: 0 fetcher-input changes across 304 providers |
| AT1 | the AS5 href gate had no always-on floor: without node both node tests SKIP and a defeated gate passes the suite | native Sonnet, pass 13 | FIXED — the script-tag test asserts the exact gate one-liner; sandbox: gate defeated → the floor test 1 failed |
| AT2 | the pass-12 machinery note (and the backlog row, and the pass-13 brief) said `git archive` does not extract the parity symlink — `tar -tv` + `test -L` show it IS extracted, dangling | native Sonnet, pass 13 | FIXED — note, backlog row and briefs corrected; lesson recorded: a MACHINERY claim gets the same executable check as a code claim |
| AT3 | AS3/AS5 cited "0 of 109" — the catalog has 110 keys incl. `_README`, 109 vendor entries | native Sonnet, pass 13 | FIXED — rows say "109 vendor entries (110 keys incl. `_README`)" |
| AU1 | the AS1 enum gate applied only to the direct-entry path: the MERGE path (`name` = an existing vendor) validated nothing, so an enum-rejected answer still appended a match prefix to a CURATED entry, took that vendor's fetcher slot by name order, and left triage unidentified (reproduced with a stubbed fanout) | native Opus, pass 13 | FIXED — `identified` is enum-gated by ONE helper (`_enum_category`) that every consumer reads; the merge loop only sees accepted answers; grader `test_an_enum_rejected_answer_never_merges_reports_or_scores_as_identified` — sandbox: gate reverted → 1 failed |
| AU2 | enum-flattened providers stayed in `identified`, so `classify_last.json`, the alert body and the "N/N identified" print all reported a provider that was tombstoned in the same run | native Opus, pass 13 | FIXED — same gate (the report reads the gated dict); the grader asserts `identified == []` and `tombstoned == ["gamma"]` in the state file |
| AU3 | AS5's grader tested the `href` helper in isolation — nothing asserted `render()` CALLS it (call site reverted → suite stayed green) | native Opus, pass 13 | FIXED — the node test now lifts the `const url=` call-site line and executes it with a hostile url (output is plain text, no `<a`); sandbox: call site reverted → 1 failed |
| AU4 | `derive_provider("_MYVENDOR_API_KEY")` minted `_myvendor`, which `load_catalog` drops as metadata — tombstoned once, re-flagged and re-billed forever (0 of 582 live names start with `_`) | native Opus, pass 13 | FIXED — the leading `_` is stripped; grader; sandbox: → 1 failed |
| AU5 | the flywheel score read the RAW category: an enum-rejected answer scored 5 and fed `pick_models` fleet-wide | native Opus, pass 13 | FIXED — the score reads the enum verdict; grader captures `set_quality` → `[2]`; sandbox: → 1 failed |
| AU6 | the per-key `used by:` note (which `registry_sync` prefers over the `#svc` field) was not tokenised — a whitespace project attributed differently on the two paths (0 of 59 dirs) | native Opus, pass 13 | FIXED — same `_svc_token`; grader via `consolidate` on a `pro j` project dir; sandbox: → 1 failed |
| AU7 | tokenising `name` (AS3) broke the file→catalog round trip for a whitespace catalog KEY (`pro v` emitted as `pro_v`, read back as a new provider, re-billed daily) — 0 of 109 live | native Opus, pass 13 | FIXED at the source — `load_catalog` REFUSES a key that is not a single token (loud, hand-edit only); grader; sandbox: → 1 failed. `svc_line`'s tokenising stays as defence in depth |
| AU8 | the lookbehind noise list is open-ended (`VALKEY`, `HOTKEY`, `WHISKEY`… all anchored) and a glued public key (`SSH_PUBKEY`) ranked as a real secret | native Opus, pass 13 | PARTIALLY FIXED — `PUBKEY`/`PUB` join the bounded public marker (`SSH_PUBKEY` → last resort; grader; sandbox → 1 failed); the noise list stays a measured denylist (0 live flips in 649 pairs; a positive suffix list would trade a known false-positive class for an unknown false-negative one — recorded, not built) |
| AU9 | `classify_services` read the catalog RAW: `_README` (a string) reached `catalog[target].setdefault` on a model answer naming it — an `AttributeError` that aborts the daily classify | native Opus, pass 13 | FIXED — metadata keys are kept aside and written back FIRST (the re-serialisation is byte-identical to the checked-in file, measured); grader; sandbox: → 1 failed |
| AU10 | the secrets write had no cleanup: an exception between `os.open` and `os.replace` left `all-envs.env.tmp` (the full credential set, 0600) beside the target until the next success | native Opus, pass 13 | FIXED — `write_secret_file` unlinks the tmp on any failure; grader monkeypatches `os.replace` to raise; sandbox: → 1 failed |
| AU11 | `flagged_providers` opened the triage block on ANY line containing `NEEDS-TRIAGE` — a value with that literal would send every later curated vendor to the paid pool and overwrite it (1 of 1 live occurrence is the header) | native Opus, pass 13 | FIXED — header lines only; grader (a curated vendor between the poisoned value and the header stays unflagged — the first fixture put nothing between them and proved non-discriminating); sandbox: → 1 failed |
| AV1 | pass-13 pool candidates (after the routed re-dispatch: 3 code, 5 ops, 1 docs) | pool wide ×3, pass 13 (scored 2/2/2) | REFUTED — `host_sld` handles `com.au` (`_CC_SECOND_LEVEL`, executed), the `\x00` slot is split at render (`gather_envs.py` render loop; 0 NUL bytes in the live file), ripgrep has a built-in `docker` type (`rg --type-list`, and the live scan ran); the heartbeat-by-mtime and manual-run-race items are G9/atomic-write by design (a concurrent tmp is `O_EXCL`, the loser errors loudly), the 137 and `ALLOWED_TOKEN` items are inferences the unit could not see the code for, the partial-scan item is the documented AF choice guarded by the bounded prune; the docs unit re-raised the 148 count already corrected under AJ6 |
| AW1 | `write_secret_file` used ONE shared tmp name: a manual run racing the cron unlinked the other writer's in-progress file, and the first writer's `os.replace` then installed the SECOND writer's partial file as the secrets file (the `Re-run gather_envs.py --apply` hint in classify's own output makes the race reachable) | pool code unit (deepseek), pass 14 — the first real code finding since the brief was routed | FIXED — a per-process tmp name (`.tmp.<pid>`, `O_EXCL` — a planted symlink is refused too); a live sibling's tmp is never touched, a leftover older than an hour (SIGKILL — uncatchable) or under the legacy shared name is swept; grader `test_secret_file_tmp_is_per_process_and_a_live_siblings_tmp_is_never_touched` — sandbox: shared name restored → 1 failed; the two legacy 0600 graders adapted to the new name; live: 0 leftovers |
| AX1 | the new Catalog-conventions sentence said `category`/`cost`/`status` all gate identification — only `category` does; `cost`/`status` default to `?`/`active` | native Sonnet, pass 14 | FIXED — reworded |
| AX2 | the Pass 13 row said found 24 / fixed 15 — the distinct ids reconcile to 23 / 14 (AU8 was counted inside its own range and again as "partially") | native Sonnet, pass 14 | FIXED — row corrected in place with the correction named |
| AY1 | both node-executed graders substituted a hand-written `esc` for the SHIPPED one, and no floor pinned its definition — `esc` deleted → 10 passed | native Opus, pass 14 | FIXED (security) — both graders lift the shipped `const esc=` line; the script-tag test pins its four escapes; sandbox: `esc` neutered → the node grader 1 failed AND the floor 1 failed |
| AY2 | the AU3 call-site grader executed `const url=` but nothing asserted the provider CELL uses it (cell rewritten to a raw `<a href=r.url>` → suite green) | native Opus, pass 14 | FIXED — the grader lifts and executes the cell fragment and asserts the rendered `<td>` (plain text, no `<a`) — sandbox: cell bypass → 1 failed |
| AY3 | `derive_provider`'s `or key` fallback restored the `_` prefix when the strip emptied the stem (`__API_KEY` → `__api_key`), and with AU9's meta split such a provider is re-tombstoned and re-billed EVERY run (end-to-end proof; 0 of 582 live names) | native Opus, pass 14 | FIXED — `or key.lstrip("_") or "unnamed"`; grader over `_`, `__`, `___TOKEN`, `__API_KEY`; sandbox: → 1 failed |
| AY4 | `load_catalog`'s docstring still promised fail-soft while AU7 made it raise, and the `ValueError` escaped `main` as a raw traceback (its neighbour `CodeScanError` gets a one-liner) | native Opus, pass 14 | FIXED — docstring states both contracts (unreadable → fail-soft; unround-trippable key → fail-closed); `main` catches both as the same one-line exit-1 shape; grader `test_a_bad_catalog_key_is_a_one_line_exit_1_never_a_traceback`; sandbox: → 1 failed. The blast radius (one bad hand-edited key stops the daily chain until fixed, with an alert) is the chosen fail-closed direction — a silently dropped vendor would be re-billed daily instead |
| AY5 | no lock around the secrets write; the failure-path unlink could delete a concurrent run's tmp | native Opus, pass 14 | FIXED under AW1 (per-process names make both unlinks private); the requested `flock` is REFUTED as unnecessary — two writers now produce two private tmps and the last rename wins with identical content |
| AY6 | AU10's "never outlives a failure" overclaims — SIGKILL is uncatchable | native Opus, pass 14 | FIXED under AW1 — docstring says "catchable"; a killed run's per-process leftover is swept after an hour |
| AY7 | `_svc_token` collapsed whitespace but not `,`, the consumer's list delimiter — a project dir with a comma would attribute as two projects (0 of 45 dirs) | native Opus, pass 14 | FIXED — `[\s,]+`; grader extended with an `a,b` project; sandbox: → 1 failed |
| AY8 | AU9 fixed the `_README` instance, not the class: a string value under a NON-underscore key still reached `.setdefault` in classify and `.get` in `load_catalog` | native Opus, pass 14 | FIXED — both loaders keep only dict values as providers (classify carries the rest as metadata and writes it back); graders on both sides — sandbox: each filter removed → 1 failed (the classify grader first passed under mutation because the model answer named `_readme`; it now names the string key) |
| AY9 | `assert entry["category"] != "?"` was dead by construction and a production `assert` | native Opus, pass 14 | FIXED — replaced by the comment it was |
| AZ1 | pass-14 pool candidates other than AW1: 4 code (non-consecutive error budget — a non-dispatched lap is neither a success nor a failure, by the docstring; a corrupt cursor re-bills at most one 10-unit slice, by design; the deleted orchestrator's cron never existed, D-081; `MYKEY` is the AS2 glued class), 7 ops (a quoted `$LOG_FILE`; `Pro V` = AU7; a planted tmp symlink is refused by `O_EXCL`; the AU10 cleanup; the partial-scan choice and bounded prune; the chain runs once a day under its lock; a JWT rubric item with no file), 5 docs (`kind: legal` was never an enum value — pass-1 row; 148 = AJ6; backticked `<host>` in prose; no broken rows; superseded ledger rows are the ledger's contract) | pool wide ×3, pass 14 | REFUTED |
| BA1 | the AW1 sweep called `stat()` on a glob result with no guard: a sibling run unlinking the same leftover between glob and stat raised `FileNotFoundError` OUTSIDE the write's `try` — a raw traceback under the exact two-writer scenario AW1 was written for (reproduced; the pool code unit found it independently) | native Sonnet + pool code, pass 15 | FIXED — the sweep is its own function: `lstat` (never follows a symlink), regular files only, every `OSError` is a skipped entry; grader reproduces the race with a racing `lstat`; sandbox: guard narrowed → 1 failed |
| BA2 | the sweep lived inside `write_secret_file`, which `main` skips on a NO-CHANGE day — the common day — so a SIGKILLed run's credential leftover stayed until the next content change; the docstring's "swept once it is an hour old" was conditional (Opus BB1 reproduced the same through `main`) | native Sonnet + native Opus, pass 15 | FIXED — `sweep_stale_tmp(OUTPUT)` runs on every `--apply` BEFORE the no-change short-circuit; grader runs `main` twice with an unchanged body and a planted 2 h-old leftover; sandbox: the call removed → 1 failed |
| BA3 | the Pass 14 row said found 25 / fixed 12 / refuted 16 — 12 + 16 = 28 (the AX2 class again, one row later) | native Sonnet, pass 15 | FIXED — 28, corrected in place; the lock halves of AY5/AY6 live inside their FIXED rows and are not counted twice |
| BB2 | the sweep's `stale != tmp` guard excluded exactly the one file it must remove — a DEAD run's leftover under a REUSED pid (WSL reboot + boot-time hook) — and `O_EXCL` then failed the whole run (reproduced with a 24 h-old same-pid leftover) | native Opus, pass 15 | FIXED — the sweep keys on age, not identity (our own tmp does not exist yet at sweep time); grader plants a leftover under `os.getpid()`; sandbox: pid skip restored → 1 failed |
| BB3 | the sweep followed symlinks and choked on a directory (`IsADirectoryError`, `FileNotFoundError` on a dangling link) — three reproduced crash shapes before `os.open` | native Opus, pass 15 | FIXED — `lstat` + `S_ISREG` + the OSError guard; grader plants a dangling link and a directory; sandbox: the guard narrowed → 1 failed (the `S_ISREG` line alone is defence in depth — a directory's `unlink` is caught by the same guard, so its removal is not independently red) |
| BB4 | an out-of-enum model `status` defaulted to `active` — `retiring_soon` shown as live | pool code unit, pass 15 | FIXED — defaults to `?` (unknown), the AP1 grader asserts it |
| BB5 | `urlsplit` on a model-authored url (`http://a]b` → `Invalid IPv6 URL`) was unguarded in the direct path, the merge path AND `flagged_providers` (a project's own malformed `*_URL`) — one bad answer discarded the whole paid batch; a bad project value would kill classify every day | native Opus, pass 15 | FIXED — `_scheme`/`_host` helpers swallow the `ValueError` (an unparseable url is `?`, a hostless one is skipped); grader covers all three sites; sandbox: helper un-guarded → 1 failed |
| BB6 | `gather_envs.catalog_url_index` had the same unguarded `urlsplit`, and AY4's widened `except ValueError` would report it as "a bad catalog key" with no entry named | native Opus, pass 15 | FIXED — the index skips an unparseable url; `load_catalog` raises a typed `CatalogError(ValueError)` and `main` catches only that (a bug keeps its traceback); grader; sandbox: index guard removed → 1 failed |
| BB7 | the legacy shared tmp name was swept UNCONDITIONALLY on the claim "no writer uses it" — true only of the merged tree; a pre-AW1 writer on an old checkout is a live sibling during the transition | native Opus, pass 15 | FIXED — the legacy name is age-gated like every other; grader plants a fresh legacy file and expects it kept; sandbox: legacy special-case restored → 1 failed. The pre-existing legacy-leftover grader now ages its fixture |
| BB8 | `credit_fetchers` used `urlopen`'s default redirect handler, which re-sends `Authorization` — the live vendor key — to whatever host a 3xx names, over plain http too; the module docstring claimed "never sent anywhere but the vendor's own API" (proven with two loopback servers and a sentinel) | native Opus, pass 15 | FIXED (security) — a `_NoRedirect` opener: a redirect is an `HTTPError` → the fetch returns None; grader runs a local 302 server and asserts the target path is never requested; sandbox: default opener → 1 failed |
| BB9 | the shipped `esc` did not escape `'` (the pre-AY1 hand-written grader escaper had, so the fidelity fix silently narrowed the exercised class); every attribute is double-quoted TODAY (7 of 7 call sites) | native Opus, pass 15 | FIXED — `'` → `&#39;` in `esc` and in the floor tuple; sandbox: the quote escape removed → the floor 1 failed. The AY2 cell grader lifts a statement PREFIX — recorded: a sink appended after it is not covered |
| BC1 | pass-15 pool candidates other than BA1/BB4: 4 code (the error budget is written after the dry-run return; a removed cursor provider still positions the window by `n <= after`; a `letmein`-shaped value under an UNANCHORED secret token is the AH1 knob trade-off, 0 live; the deleted orchestrator's cron never existed), 6 ops (fixed step labels; `env VAR="$x"` never re-parses; the dashboard write is tmp+replace, AF11; `_alert || true` is best-effort by design under the heartbeat; module-level `load_catalog` lookups; the cursor-before-dispatch trade is AC5), 4 docs (`kind: legal`, 148, a sibling's INDEX row, ledger supersession) | pool wide ×3, pass 15 | REFUTED |
| BD1 | BB6 was half-implemented: `main` still caught bare `ValueError` — the `CatalogError` type discriminated nothing and any bug inside the scan was still a one-liner (both native finders, independently) | native Sonnet + native Opus, pass 16 | FIXED — `except (CodeScanError, CatalogError)`; grader `test_only_the_typed_catalog_error_is_a_one_liner` (an unrelated ValueError propagates); sandbox: widened back → 1 failed. Cause: the pass-15 replace targeted a line ruff had already wrapped and silently matched nothing — the class is "an unasserted edit"; the grader is the fix |
| BD2 | the BB3 symlink half of the sweep grader did not discriminate: a FRESH dangling link is skipped by the age gate alone, so `lstat`-vs-`stat` and `S_ISREG` had no independent red | native Sonnet, pass 16 | FIXED — the grader links to an hours-old real file (kept: the LINK is fresh — only `lstat` gives that), then ages the link itself (kept: not a regular file); sandbox: `stat` → 1 failed; `S_ISREG` removed → 1 failed |
| BD3 | commit `7d6d80ac` says "13 graders proven red-on-revert" — the ledger derives 9 sandbox lines + 3 join/fixture proofs | native Sonnet, pass 16 | ACCEPTED as stated (immutable message); this row is the reconciliation: BA1, BA2, BB2, BB3, BB4, BB5 (×3 sites), BB6, BB7, BB8, BB9 + the two node-join repairs = 13 proof runs, 10 distinct graders |
| BD4 | a leftover under OUR pid younger than the sweep's hour (a crash then a pid reuse within it) made `O_EXCL` fail the run (Opus BE6 reproduced it with a 1-minute-old file) | own re-derivation + native Opus, pass 16 | FIXED — `write_secret_file` unlinks a same-pid leftover before `O_EXCL` (no other LIVE process can hold our pid); grader; sandbox: unlink removed → 1 failed. BE6's other half (drop the age gate for our pid in the sweep) is REFUTED as redundant once the write handles it |
| BE2 | the BB8 redirect grader drove `_OPENER` directly — reverting the shipped call site to `urlopen` left it green | native Opus, pass 16 | FIXED — the grader goes through `_get_json` (the shipped path) and asserts the fetch returns None and the redirect target is never requested (the helper retries `/a`, so the path SET is asserted); sandbox: call site reverted → 1 failed. The listening socket is closed too (BE7) |
| BE3 | the merge path's `match` had no scalar guard (AF14 covered `hosts`): a hand-edited `"match": "BRAVE"` crashed classify AFTER the paid batch, or a substring hit silently skipped the merge (0 of 109 live) | native Opus, pass 16 | FIXED — coerced like `hosts`; grader; sandbox: guard removed → 1 failed |
| BE4 | `load_catalog` iterated a scalar `match` letter by letter — six one-letter prefixes that hijack `N_KEY` and un-match the vendor's own keys (0 of 109 live) | native Opus, pass 16 | FIXED — a scalar is one prefix, every element stringified; grader asserts the matcher table and both lookups; sandbox: → 1 failed |
| BE5 | BB4 was half-implemented: an OMITTED `status` still defaulted to `active` (0 of 109 live entries lack a status) | native Opus, pass 16 | FIXED — `?`; grader with an answer that has no status; sandbox: → 1 failed |
| BE7 | the redirect grader never closed its listening socket | native Opus, pass 16 | FIXED — `server_close()` |
| BE8 | the pool-prompt `scheme://host` reconstruction dropped IPv6 brackets, and `_host` then rejected the value on the merge path (0 of 421 live http urls are bracketed) | native Opus, pass 16 | FIXED — brackets kept; grader round-trips `https://[::1]`; sandbox: → 1 failed |
| BE9 | the sweep's return count was discarded — a repeatedly killed run was cleaned up with zero visibility | native Opus, pass 16 | FIXED — a stderr WARNING names the count and the path (style; no grader — it is a print) |
| BF1 | pass-16 pool candidates: 4 code (a "missing parenthesis" that was the 70k cap cutting the regex — helper cap raised to 140k; a same-pid leftover < 1 h = BD4; error counts pruned to the flagged set, AF4; `subprocess` list argv needs no quoting), 6 ops (a double-quoted `$LOG_FILE`; classify outside the heartbeat is G9 by design; the static `.tmp` suffix = AW1, fixed; the partial-scan choice; `credential_grade` segments = AO1; the 30 h liveness budget vs the 36 h daily-refresh freshness check are two surfaces with two cadences) | pool wide ×3, pass 16 | REFUTED |
| BG1 | the reference doc still said `cost`/`status` default to `?` / `active` — BB4 and BE5 had made both `?` | native Sonnet, pass 17 | FIXED — reworded |
| BG2 | the Pass 16 row's 25/11/14 did not reconcile with its own id list (the AX2/BA3 class a third time) | native Sonnet, pass 17 | FIXED — 22/11/1/10, corrected in place; the recount rule stated: candidates = finder ids + pool items, own re-derivation rows excluded |
| BH1 | (security) one wrong model `name` merged a flagged provider's prefix into an existing vendor's `match`; its key then sorted first at rank 0 and `registry_sync` handed it to THAT vendor's credit fetcher (`AAA_API_KEY` → `api.deepl.com`, reproduced end-to-end with a stubbed fanout) | native Opus, pass 17 | FIXED at the root of the class — PROVENANCE: classify writes a merged prefix to `merged_match`, never to the curated `match`; the scan matches on both; `registry_sync` ranks a key that reached the block through a merged prefix below every owned one and refuses the fetch when the best credential is unowned (warned only where a fetcher exists). The first cut keyed ownership on the key's own name and would have refused ten live blocks whose CURATED aliases (`HF_TOKEN`, `B2_…`, `R2_…`) are ownership — measured before landing and redesigned. Graders on all sides (rank, gate, segregation, matching on the merged field); sandbox: each → 1 failed. Live: 0 merged prefixes in the catalog today, 0 fetcher-input changes, 0 blocks refused |
| BH2 | classify read the catalog AFTER the paid dispatch — an unreadable file lost the whole slice and the cursor had moved (reproduced) | native Opus, pass 17 | FIXED — read + parsed before `fanout`, exit 1 with nothing dispatched; grader spies on `fanout`; sandbox: → 1 failed |
| BH3 | `UnicodeDecodeError` (a ValueError) from an undecodable catalog escaped `load_catalog`'s fail-soft tuple and, after BD1, `main`'s — a raw traceback where the docstring promised fail-soft | native Opus, pass 17 | FIXED — in the fail-soft tuple; grader; sandbox: → 1 failed |
| BH4 | `_get_json` retried EVERY failure three times with no backoff — a revoked key (401) hammered thrice, a 429 ignoring `Retry-After`, every 3xx since BB8 (measured on six statuses) | native Opus, pass 17 | FIXED — only `core/58`'s RETRYABLE_STATUS is retried, `Retry-After` honoured and capped at 30 s; grader against a loopback server (401 → 1 request, 503 → 3, hostile header capped); sandbox: → 1 failed |
| BH5 | the scalar coercion turned `"match": null` into the literal prefix `"None"` and persisted it (`NONE_API_KEY` → brave) | native Opus, pass 17 | FIXED — `null` → `[]` on both `match`/`merged_match` and `hosts`; grader; sandbox: → 1 failed |
| BH6 | a top-level JSON list/string catalog and a list/`null` category crashed the scan or rendered a section titled `None` | native Opus, pass 17 | FIXED — non-object → `{}` with the fail-soft warning; a non-string category buckets as `?`; graders; sandbox: each → 1 failed |
| BH7 | the BD4 pre-unlink sat outside the `try`; a DIRECTORY at our own tmp name is an unsweepable landmine | native Opus, pass 17 | FIXED (unlink guarded) / ACCEPTED (a directory planted at that exact name is not a data condition — `O_EXCL` reports it loudly) |
| BH8 | an empty `match` prefix `""` matched every `_`-prefixed key | native Opus, pass 17 | FIXED — skipped; grader; sandbox: → 1 failed |
| BI1 | pass-17 pool candidates: 2 code (the `\x00` slot again — split at render, 0 NUL bytes live; a `CODE_HOST_URL` exact-match "never matches" — it matches 371 live rows), 6 ops (`_alert` passes its three fields as argv — read, not recalled; the dashboard write is tmp+replace; pid-tagged leftovers are swept; 0600 from creation; an unparseable `#svc` alerts through `_step`; `$LOG_FILE` is quoted), 4 docs (148 with its stated overlap; `kind: legal` was never an enum value; the pipes were fixed; 116 is the derived count) | pool wide ×3, pass 17 | REFUTED |
| BJ1 | the shipped `merged_match` reader had no grader — every consumer test monkeypatched the list; neutering the reader left 94 passing | native Sonnet, pass 18 | FIXED — `test_the_shipped_merged_prefix_reader_feeds_the_fetcher_gate` drives the real catalog file through the shipped reader; sandbox: reader neutered → 1 failed |
| BJ2 | the null-match grader never reached the coercion (the fixture had no `merged_match` key) | native Sonnet, pass 18 | FIXED — the fixture pre-sets `merged_match: null`; sandbox: coercion removed → 1 failed |
| BJ3 | `_retry_after` clamped only the upper bound — `Retry-After: -5` would crash `time.sleep` | native Sonnet, pass 18 | FIXED — `max(0, …)`; grader; sandbox: → 1 failed |
| BJ4 | (pool code unit) `owned_by` semantics: a key claimed by another provider's merged prefix read as owned | pool, pass 18 | FIXED under BK3 (the WINNING prefix decides) |
| BK1 | (security) the BH1 gate FAILED OPEN when the catalog was unreadable: `load_catalog` fail-softs to `{}`, so every key looked owned and the fetch proceeded — while the chain still runs `registry_sync --fetch-credits` after a failed scan (fake-DB probe: the victim secret reached `fetch_balance`) | native Opus, pass 18 | FIXED — `catalog_provenance()` returns None on an unreadable / non-object / unround-trippable catalog; unknown provenance is never ownership (no fetch), while the DB sync still runs (BK7); grader `test_an_unreadable_catalog_refuses_every_fetch_but_still_syncs`; sandbox: `None` → ownership → 1 failed |
| BK2 | the two preserve lists (identified rewrite, tombstone re-stub) kept `match`/`hosts` but not `merged_match` — a merge whose target was in the same batch lost the prefix and the merged provider was re-billed forever (REGRESSION vs the pre-provenance design; 0 of 109 live `?` entries) | native Opus, pass 18 | FIXED — both lists; grader `test_a_merged_prefix_survives_a_rewrite_of_its_target`; sandbox: both lists reverted → 1 failed |
| BK3 | `owned_by` asked "does ANY merged prefix match" instead of "which prefix WON" — a short merged prefix (`HF`) shadowed the longer curated alias (`HF_TOKEN`) that actually routed the key (the doc's own example judged unowned) | native Opus, pass 18 | FIXED — the longest match over the full matcher list decides, and ownership is "that pair is not merged"; grader with the HF fixture; sandbox: → 1 failed |
| BK4 | the BH2 catalog probe ran AFTER the cursor write — an unreadable catalog aborted with "cursor unmoved"-shaped wording while the slice had been skipped for a lap | native Opus, pass 18 | FIXED — the probe precedes the cursor write; grader asserts no cursor file after the abort; sandbox: probe moved back → 1 failed |
| BK5 | `_retry_after("nan")` → `time.sleep(nan)` raises | native Opus, pass 18 | FIXED — explicit NaN guard; grader. ACCEPTED-as-redundant: `max(0.0, nan)` already yields 0.0 by argument order, so the sandbox mutation stayed green — the guard is documentation |
| BK6 | the BH2 pre-dispatch snapshot was written back after the minutes-long paid dispatch — a hand edit made meanwhile was silently reverted | native Opus, pass 18 | FIXED — the pre-dispatch parse is a readability PROBE; the merge re-reads the file fresh (unreadable then → exit 1, nothing written); grader edits the catalog inside the stubbed fanout; sandbox: snapshot reused → 1 failed |
| BK7 | the sync now depended on the catalog loading — a whitespace key aborted the DB sync entirely | native Opus, pass 18 | FIXED under BK1 (unknown provenance, not an abort; grader asserts `catalog_provenance()` is None for such a key) |
| BK8 | the gate stopped the FETCH but the unattributed key was still stored as the vendor's `credential`, and the warning was gated on a fetcher existing (4 of 109) | native Opus, pass 18 | FIXED — stored as `kind = credential-unattributed` (never a key on the dashboard, never a fetcher candidate) and warned by NAME always; schema comment + doc; grader asserts the row's kind by this run's digest; sandbox: kind restored → 1 failed |
| BK9 | a code comment cited the REJECTED first cut's "ten live blocks" measurement | native Opus, pass 18 | FIXED — comment rewritten with the shipped design's fact (0 merged prefixes live) |
| BK10 | the fleet-synced rule pack `core/57` enumerated the catalog fields without `hosts`/`merged_match` | native Opus, pass 18 | FIXED — enumeration extended (a synced surface; correct for every project since it documents the hub catalog) |
| BL1 | pass-18 pool candidates: 5 code (rg's `path:match` separator IS `:http` — the live scan produced 371 rows; cursor-before-dispatch = AC5; no lock = AY5; hostnames cannot carry `_`; `owned_by` = BJ4), 6 ops (`$@` under `timeout` is quoted; the dashboard tmp is unlinked in `finally`, AC14; `_alert || true` best-effort under the heartbeat; three items restate fixed rows as findings), 5 docs (`kind: legal`; 148/116 vs pre-correction history; D-081 is immutable and superseded by D-085; `APIs/Gmail` is a slash; the schema comment lists every kind) | pool wide ×3, pass 18 | REFUTED |
| BM1 | (security) `owned_by` never checked that the WINNING prefix belongs to THIS block's provider — a stale block carrying another vendor's key (`DEEPL_API_KEY` under `exa`) read as owned and would feed exa's fetcher; equal-length prefixes of two providers flipped the verdict by JSON key order (both native finders, on the shipped grader's own fixture) | native Opus + native Sonnet, pass 19 | FIXED — `win[1] == provider and win not in merged`; graders for the cross-provider curated case and both key orders; sandbox: → 1 failed; live: 0 of 129 credentials have a winning prefix of another provider | · CORRECTED by BQ1 (pass 20): the equal-length half was claimed, not closed — the grader's `or order[0][1] == "zzz"` escape was true for the exposing order and the stable length-sort kept the tie JSON-order-dependent
| BM2 | unknown provenance degraded SILENTLY: 129 identical warnings, every dashboard row at 0 keys, `registry_sync` exit 0, the heartbeat GREEN | native Opus, pass 19 | FIXED — one summary line with the count, `main` exits 2 so `_step` alerts and the dashboard is not rewritten (a degraded registry never reads LIVE); graders; sandbox: each → 1 failed |
| BM3 | the BK6 fresh re-read added a pay-and-lose path: a catalog corrupted DURING the dispatch aborted after the cursor moved and the money was spent | native Opus, pass 19 | FIXED — the merge falls back to the pre-dispatch probe (the last good state) with a warning; grader corrupts the catalog inside the stubbed fanout; sandbox: → 1 failed |
| BM4 | `has_fetcher` orphaned with a docstring describing deleted behaviour | native Opus, pass 19 | FIXED — deleted |
| BM5 | two key NAMES sharing one value collapse to one row whose `kind` was decided by file order (5 of 304 live blocks collide, 0 credential-grade) | native Opus, pass 19 | FIXED — the restrictive kind (`credential-unattributed`) wins whatever the order; grader runs both orders against the registry; sandbox: → 1 failed |
| BM6 | `ensure_schema`'s docstring enumerated three kinds | native Opus, pass 19 | FIXED |
| BM7 | `_catalog_snapshot` was write-only after BK6 | native Opus, pass 19 | FIXED — renamed `_catalog_probe` and now read by BM3's fallback |
| BM8 | a grader docstring named the deleted `merged_prefixes()` | native Opus, pass 19 | FIXED under the BJ1 grader's rewrite to `catalog_provenance()` |
| BM9 | an unattributed credential was invisible on the dashboard (same as no key at all) | native Opus, pass 19 | FIXED — the keys cell shows `n unattributed` with a tooltip; floor grader; sandbox: → 1 failed |
| BN1 | (Sonnet) `owned_by` ignores `provider` — the same defect as BM1, PLAUSIBLE-rated with a 0-of-109 duplicate-prefix measurement | native Sonnet, pass 19 | FIXED under BM1 |
| BO1 | pass-19 pool candidates: 10 code — FIXED: a wrong-typed cursor/count in a state file crashed classify (C2, typed on read), the url-index wildcard dropped the TLD and the label fallback ignored the entry's domain (`api.foo.org` credited to `foo.com` — C5, fixed on both routes; live: 3 of 525 scanned hosts changed attribution, all three the SAME vendor on a sibling domain — `docker.io` for Docker Hub, `aliyuncs.com` for Alibaba — curated via `hosts`, 0 changes after), `--only` naming nothing flagged exited 0 (C6), `_svc_token` rewrote a legal `,` in a url (C7), a directory at the dashboard tmp name raised outside the try (C10); REFUTED: the cleanup unlink on a directory (BH7 accepted), uppercase path segments (the AL1 trade-off, documented), a live writer stopped > 1 h (the chain kills a step at 15 min), the partial-scan prune (AF, documented), the cursor before merge (= BM3) · 8 ops (posthog code-host attribution is the designed G8 feature; pid namespaces — one host; `_alert || true` by design; four restate fixed rows) · 3 docs (150 in D-081 is immutable and superseded; `kind: legal`; table pipes) | pool wide ×3, pass 19 | 5 FIXED / 16 REFUTED |
| BP1 | pass-20 pool candidates: 11 code (deepseek-v4-flash) / 4 ops-tests (gemini-3-flash) / 0 docs (qwen3-max: `[]`) | pool fanout ×3 routed, pass 20 | REFUTED (15 of 15, 6 self-refuted "no defect"): cursor persisted before the paid dispatch is AC5 by design (a raising `fanout` costs one lap, never a re-bill), `owned_by` with no matching prefix returns True (`not hits → True`, read the code), `HTTPS://` uppercase literals — measured fleet-wide: 13 hits in 8 files of every `/opt` repo, all test fixtures or a URL normaliser, 0 real vendors — a case-insensitive `HOST_RE` would ADD noise (verb 5, rejected after measuring), a `,` in `used_by` is `_svc_token`'d (AY7), `svc_bucket` is defined at gather_envs.py:809, `ensure_schema` on a missing table fails loudly (the schema is an install precondition, `db/services_registry_schema.sql`), a failed scan leaves yesterday's file (atomic write) so the sync re-asserts yesterday's state under GUARD 2 (O2/G9), the dashboard tmp is unlinked in `finally` (AC14), a sibling deleting a stale tmp between glob and lstat is the desired end state (OSError skipped), the pill TEXT is `esc(v)` (gen_dashboard.py:165) |
| BQ1 | (security) `owned_by`'s "equal-length prefixes cannot flip the verdict by JSON order" (BM1) was FALSE: `next()` took the first hit of a STABLE length-sort, so two providers claiming a same-length prefix resolved by catalog key order — and the pass-19 grader was vacuous for the exposing order (`not X or order[0][1] == "zzz"`); mutation-proven by the finder against both the shipped and a fixed tie-break | native Sonnet, pass 20 (executed) | FIXED · LOCAL — both routers take the LONGEST hit set: `owned_by` owns only when that set is one provider (this block's) and none of it merged; `gather_envs.match_provider` (the mirror — the same first-hit walk routed keys into blocks) attributes a cross-vendor tie to NEITHER (the key falls to `derive_provider`); live: 0 of 109 catalog prefixes tie, 0 merged, `gather_envs --apply` no-op; graders assert both orders on both sides + longer-still-wins + one-vendor-twice; sandbox (first-hit tie-break restored): → 1 failed each side; the code comment, the Round-19 sentence and row BM1 corrected |
| BQ2 | four pass-18/19 graders wrote `name=deepl` / `name=exa` — REAL vendors — through `sync_registry` into the shared local registry: the services upsert flipped deepl's cost tier (`freemium` → the fixture's `paid`, live on the dashboard) and left 4 fake `projA` key rows the teardown never deleted (it only knows `TEST_PROVIDER`) | native Sonnet, pass 20 (executed; found the rows in the live DB) | FIXED · LOCAL — the four tests use `TEST_PROVIDER`/`TEST_PROVIDER2` (`test_zzz_regsync2`), the teardown deletes both, and an autouse `real_registry_untouched` fixture snapshots every non-`test_zzz` service + key row before and after EACH test and fails the test that changed one (the class guard, not the instance); the registry restored by the real `registry_sync.py` run (`deepl` freemium again, `4 stale keys` pruned — the projA rows); sandbox (one test renamed to a non-test provider `zzz_probe_bq2`): → guard fails, probe row removed |
| BQ3 | BM9's grader was a substring check (`"r.unattributed" in SCRIPT`) where its siblings execute under node | native Sonnet, pass 20 | FIXED · LOCAL — the keys cell is lifted from the row template and run under node: `{keys:2, unattributed:1}` renders `2 <span … >1 unattributed</span>`, `unattributed:0` renders `2` alone; the source-text floor stays; sandbox (`r.unattributed?` → `false?`): → 1 failed |
| BR1 | the C5 label fallback added a SECOND unguarded `urlsplit` on a catalog url (BB6 guarded the url-index site for exactly this input): a hand-edited `"url": "https://[foo].com"` raised a plain `ValueError` past `main`'s except → chain DEAD every day until repaired (executed through `consolidate()`) | native Opus, pass 20 (executed) | FIXED · LOCAL — guarded; `test_a_malformed_catalog_url_never_crashes_the_label_fallback`; sandbox (guard narrowed to an unrelated exception): → 1 failed |
| BR2 | the BM1 order-loop grader cannot fail; the code comment is measurably false (`[False, True]` across the two orders on the shipped code) — the same finding as BQ1, found independently | native Opus, pass 20 (executed against a neutered pre-BM1 copy too) | FIXED under BQ1 |
| BR3 | an EMPTY but valid catalog was indistinguishable from an unreadable one (`not catalog → None`), and pass 19 upgraded that conflation to exit 2 + a dead heartbeat forever on a bootstrap registry, with an alert text naming three causes that are not it | native Opus, pass 20 | FIXED · LOCAL — `load_catalog(strict=True)` raises `CatalogError` on unreadable/non-object instead of degrading, so provenance is `([], set())` for `{}` (nothing curated, nothing merged, every key its block's own) and `None` only when unknown; `test_an_empty_catalog_is_known_provenance_not_unknown`; sandbox (`if catalog else None` restored): → 1 failed |
| BR4 | one root cause, two alerts (an AC13 regression): a `CatalogError` fails `gather_envs` (alert 1), then the ungated `registry_sync` runs on the STALE file, hits the same error inside `catalog_provenance`, exits 2 (alert 2, new in pass 19) and flips 128 live credential rows to unattributed | native Opus, pass 20 | FIXED · LOCAL — `_step registry_sync` gated on `core_failed` like classify and the reconsolidate (mirror named: a scan-failure day records no credit snapshot — the flip was the worse outcome); chain grader `test_registry_sync_is_gated_on_the_scan_and_the_doc_names_every_kind`; reference doc says so; sandbox (gate removed): → 1 failed |
| BR5 | `docs/reference/external-services-registry.md` untouched by pass 19: the step-3 `kind` enumeration named 3 of 4 values, "counts only credentials" no longer described the row, and the exit-code sentence did not name `registry_sync`'s exit 2 (`grep -c "exit 2"` → 1, ripgrep's) | native Opus, pass 20 (denominators executed) | FIXED · LOCAL — both sentences; the chain grader parses the schema's `kind` comment (4 values) and asserts each is named in the doc, plus `2 = \`registry_sync\``; sandbox (one backtick pair removed): → 1 failed |
| BR6 | BM5 closed only the `kind` half of the one-value-two-names collision: `aliases`/`used_by_projects` stayed last-write-wins in the upsert — measured 5 of 434 blocks with a digest collision, 2 with diverging per-name attribution (`rapidapi`, `namecheap`), 0 lost at service level because sibling rows carry the projects | native Opus, pass 20 (measured on a fresh in-process consolidation) | FIXED · LOCAL — per-digest union of `used_by` (sorted) and aliases inside the block loop, the last upsert carrying the full union; `test_two_names_one_value_union_their_projects_whatever_the_order` (both file orders); sandbox (last-name tuple restored): → 1 failed |
| BR7 | BM3's failure-path fallback merges onto the PRE-dispatch probe and writes it back: any catalog edit landed during the dispatch window is reverted (a deleted entry resurrected) and the warning did not say so — BK6's "never reverted" holds only on the success path | native Opus, pass 20 | FIXED · LOCAL (the honest minimum): the warning dates the probe copy and says edits since are DISCARDED, with the repair step; the stronger "write only this run's entries onto disk" is impossible on that path (the disk copy is what is unreadable); grader extended; sandbox (text reverted): → 1 failed |
| BR8 | the `n unattributed` tooltip blamed a model-merged prefix, but the unknown-provenance branch sets the kind too — and the live catalog has 0 merged prefixes across 109 providers, so the pill could only ever fire for the cause the tooltip denied (standalone `gen_dashboard.py` after a degraded sync) | native Opus, pass 20 | FIXED · LOCAL — the tooltip names both causes; asserted by the BQ3 node grader; sandbox: → 1 failed |
| BR9 | (cited, not filed) the `finally: tmp.unlink` on a directory at the tmp name surfaces `IsADirectoryError` with the write error as `__context__` — same path in both messages | native Opus, pass 20 | ACCEPTED — BH7 (standing), refuted again in BO1 |
| BS1 | two denominators in the Pass 20 row: `808 keys` against a census summing to 802 (the sync's `api_keys` stat counts INSERTS — six same-value name pairs; BS7 fixes the counter), and `found: 26 / fixed: 11 / 27 dispositions` (BR2 counted in one tally and not the other); Opus found the same two independently | native Sonnet, pass 21 (re-derived from the row's own numbers) | FIXED · LOCAL — corrected in place, the correction named: found 27 / new 26 / fixed 10 + 1 duplicate, 802 rows |
| BS2 | (security) BR4's gate closed 1 of 4 causes of unknown provenance: the daily scan's `load_catalog()` still fail-SOFTED an unreadable / non-object / missing catalog (BH3/BH6) to "every provider flagged" and exited 0, so `registry_sync` ran on a fresh all-degraded file — every vendor's metadata blanked to `?`, all 128 credentials stored unattributed, 22 providers pruned under a bound of 87 with their credit snapshots and hand-entered subscriptions cascading — and the exit-2 alert came AFTER the commit (executed dry through `main()` for all four shapes) | native Opus, pass 21 (executed) | FIXED · LOCAL — `load_catalog` fails CLOSED on every untrusted shape (`CatalogError`; the `strict` flag from BR3 is gone — there is one behaviour), `main` reports one line and exits 1, the chain gate skips every later step, the previous consolidation stands; graders: the two fail-soft tests rewritten to `pytest.raises` (+ a missing file); reference doc says which shapes fail the scan; sandbox (fail-soft restored): → 2 failed |
| BS3 | (security) the C5 registrable-domain guard was undone by the next line: an alien-domain host whose label is a catalogued vendor fell through to `match_provider(<SLD>_API_KEY)` and re-credited the SAME vendor through its own prefix — 82 of 109 vendors curate that prefix, so most would take `api.<vendor>.org` (pass 22 re-derived 74–81 depending on the filter; the exact-literal 82 is the number that reproduces); the grader passed `matchers=[]`, the one shape production never has | native Opus, pass 21 (executed with the real matchers) | FIXED · LOCAL — a catalogued label whose url sits on another domain returns None (triage, never that vendor); the grader passes `[("FOO", "foo")]`; live `--apply`: 0 attributions lost of 555 code hosts (38 new lines were a sibling project's new hosts); sandbox (fallthrough restored): → 1 failed |
| BS4 | (security) BQ1's tie check compared raw prefix LENGTH while the matcher normalises the token (`EXA` and `EXA_` are one token at match time, two lengths at tie time): a one-character catalog edit claimed another vendor's keys in every order with no ambiguity signal, and merged, took the real vendor's own key away; reachable without a hand edit (`derive_provider("EXA__API_KEY") == "exa_"`, a live double-underscore convention, and both classify mint paths write `match: [prov.upper()]`) | native Opus, pass 21 (executed, both orders, merged variant) | FIXED · LOCAL — one `gather_envs.prefix_hits()` normalises the token (`rstrip("_")`) for BOTH routers, `owned_by` normalises the merged set too; live: 0 of 109 collide after normalisation, 8 of 109 end in `_`; graders on both sides + the merged-token case; sandbox (`_` kept): → 1 failed each side |
| BS5 | BQ1's None-on-tie made a tied key permanently un-routable AND silent: the derived block is re-flagged for PAID triage every run, tombstone and merge are both inert for it (the derive branch never consults the catalog), nothing warns — the re-billed-forever class (C2/AU4/Z10) reopened for ties | native Opus, pass 21 (executed: day 1 → tombstone → merge, all None) | FIXED · LOCAL — `load_catalog` names the prefix and every claimant in one WARNING at load (an ambiguity that costs money daily is never invisible); grader; sandbox (warning removed): → 1 failed |
| BS6 | BR8's fix (the tooltip naming both causes) was guarded ONLY behind `pytest.skip("node not on PATH")` — the file's own AT1 rule; mutation-proven: 9 passed, 3 skipped with node removed from PATH | native Opus, pass 21 (executed with and without node) | FIXED · LOCAL — an always-on source-text floor beside BM9's; sandbox (tooltip reverted): → 1 failed |
| BS7 | `stats["api_keys"] += 1` counted INSERT executions, not rows — the daily `synced N api_keys` line over-reports by the digest-collision count (820 vs 814 on a fresh consolidation), the root of BS1's 808-vs-802 | native Opus, pass 21 (executed) | FIXED · LOCAL — counted once per digest; the BR6 union grader asserts `api_keys == 1` for two names one value; sandbox (`if True`): → 1 failed |
| BS8 | BR3's strict load raised BEFORE the fail-soft print and `catalog_provenance` swallowed the exception text: 3 of 4 unknown-provenance causes were never said on the sync path, the 4th printed a message that was false there — the exit-2 page named nothing | native Opus, pass 21 (executed, 6 catalog states) | FIXED · LOCAL — `catalog_provenance` prints `WARNING: catalog provenance UNKNOWN — <reason with the path>`; grader (capsys); sandbox (reason dropped): → 1 failed |
| BS9 | `db/services_registry_schema.sql`'s `kind` comment kept the half-truth BR8/BR5 removed elsewhere (`credential-unattributed` = "a model-merged prefix" only) — the more likely cause, unknown provenance, missing; the chain grader parsed only the four values | native Opus, pass 21 | FIXED · LOCAL — comment names both causes; the chain grader asserts `provenance unknown` on the kind line; sandbox: → 1 failed |
| BS10 | BR7's `probe_at` was a bare LOCAL time while every other chain stamp is UTC and says so (box at UTC+03): comparing "read at 21:36" with the UTC `Generated:` header mis-sizes the discard window by 3 h in the unsafe direction — the O6 class; the grader matched the shape, blind to the zone | native Opus, pass 21 (measured `date` vs `date -u`) | FIXED · LOCAL — `datetime.now(UTC)…" UTC"`; grader demands the suffix; sandbox (bare time): → 1 failed |
| BS11 | the chain's `_alert` body decoded 124 and 137 only — the exit-2 page listed three causes ("bounded-prune refusal, exhausted pool, dead registry DB"), none of which is it | native Opus, pass 21 | FIXED · LOCAL — the body decodes `2 = registry_sync could not read the catalog: provenance UNKNOWN…`; chain grader; sandbox: → 1 failed |
| BS12 | (security, latent) the credit-fetcher input was chosen inside the loop BEFORE BM5's restrictive-kind downgrade: a value carried by an owned name AND an unattributed name was stored `credential-unattributed` yet SENT to the vendor's usage API — the invariant "never a fetcher candidate" broken in three documented places; live: 0 of 443 blocks have a credential-grade digest collision | native Opus, pass 21 (traced both orders) | FIXED · LOCAL — fetch CANDIDATES `(rank, value, digest)` collected in the loop, the input chosen after it (by role then file order — AM1/AH2 kept) skipping any digest stored unattributed; BM5's grader now runs with `fetch_credits=True` and asserts no fetch in either order; sandbox (`if True`): → 1 failed |
| BS13 | BQ2's guard keyed on `test_zzz` while three fixtures still named `alpha`/`bravo`/`charlie` (parse-only today, one edit from a sync onto the shared registry the teardown never cleans), nothing enforced the naming class, and `NOT LIKE 'test_zzz%'` left `_` an unescaped SQL wildcard | native Opus, pass 21 | FIXED · LOCAL — fixtures renamed under the prefix, `ESCAPE '\'`, and a meta-grader that reads this file and rejects any `#svc name=` outside `TEST_PREFIX`/`{TEST_PROVIDER…}`; sandbox (one name reverted): → 1 failed |
| BS14 | the autouse guard: an unreachable DB at teardown mis-diagnosed as "a test wrote a real row"; `_real_rows` leaked the connection on a query error; the snapshot omitted `aliases` (which BR6 itself writes) and `account_email`; `test_bounded_prune_refuses_mass_delete`'s self-heal syncs the REAL file without the opt-out; a failure dumped ~800 rows | native Opus, pass 21 | FIXED · LOCAL — unreachable-at-teardown stands down, `try/finally` close, 10 columns, the opt-out on the heal path, a capped symmetric-difference message; grader asserts the 10-column shape; sandbox (two columns dropped): → 1 failed · ACCEPTED (measured, not built): a concurrent real sync during a test run (one cron at 06:00, three sessions) can fail an innocent test — the message names the rows, the re-run is green |
| BS15 | the reference doc's step table said only step 4 is gated ("SKIPPED when a DATA step failed") while steps 2, 2b and 3 are gated identically; BR5 fixed the prose and left the table asymmetric | native Opus, pass 21 | FIXED · LOCAL — every gated row says "skipped after a failed scan"; the chain grader asserts it per row; sandbox (one cell reverted): → 1 failed |
| BS16 | the dashboard had no aggregate `unattributed` statcard: in the degraded case 128 credentials flip and the only signal is a per-row pill in a sorted table of 439 | native Opus, pass 21 | FIXED · LOCAL — one card; grader renders `[]` and finds the label; sandbox (card removed): → 1 failed |
| BS17 | `gather_envs.py` hardcoded `OUTPUT`/`FABRIK_ENV` as absolute hub paths while `CATALOG_PATH` is file-relative: a sandboxed copy reads the SANDBOX catalog and would write the REAL fleet secrets file on `--apply` — every finder of these 21 passes ran from exactly such a copy | native Opus, pass 21 (read `sb34o/scripts/gather_envs.py:63`) | FIXED · LOCAL — `REPO = Path(__file__).resolve().parent.parent`, both paths derive from it, `OPT` stays the fleet root; source-text + value grader; sandbox (absolute path restored): → 1 failed |
| BT1 | pass-21 pool candidates: 14 code (deepseek-v4-flash, 5 self-refuted "no defect") / 5 ops-tests (gemini-3-flash) / 0 docs (qwen3-max: `[]`) | pool fanout ×3 routed, pass 21 | REFUTED (19 of 19): `sch.uk`-class second levels only matter when a catalog url sits there (0 of 109), `MONKEY_KEY` ends in the anchored `_KEY` suffix and IS a credential (the lookbehind guards the glued form), a merged prefix longer than the curated one is the restrictive contract by design (BH1) and cannot be minted from a vendor's own stem (`derive_provider` strips `_TOKEN`), a cursor past every name wraps by design (Q1/Q3 — only still-flagged names are in the list, nothing classified is re-billed), a directory at the tmp name (C10/BH7), a partial rg scan (W5), the error budget reset (self-refuted), `_svc_token` commas (AY7), the internal-prefix override (self-refuted), a credential/code-host digest collision (0 credential-grade of 5 live, BM5), `json_for_script` backslashes (json.dumps escapes them; AM4 round-trip grader), `bad[:120]` on `#svc` lines carries no value (Opus's secrets sweep: 0 of 6 prints), the outbox flush ordering in `daily_refresh.sh` (unrelated data), `$FABRIK_ROOT` with spaces (Q4, a fixed path), PID-reuse in the sweep (it keys on mtime, not PID), a failed classify keeping the heartbeat fresh (G9 by design), `ensure_schema`'s ALTER (probe-first, one-time) |
| BU1 | (pool, the one real one) every chain grader regex-matched the script — a bash syntax error or a gate that only LOOKED right passed them all | pool ops-tests (gemini-3-flash), pass 22 | FIXED · LOCAL — `test_the_chain_script_executes_its_gating` runs the script under bash with a fake interpreter: a failed scan skips classify / reconsolidate / sync / dashboard, alerts once, exits 1; a clean run writes the dashboard in the five-step order, exits 0; sandbox (dashboard gate removed): → 1 failed |
| BT2 | pass-22 pool candidates: 10 code (deepseek-v4-flash, 5 self-refuted / unverifiable) / 5 ops-tests (gemini-3-flash, BU1 among them) / 0 docs (qwen3-max: `[]`) | pool fanout ×3 routed, pass 22 | REFUTED (14 of 15): a no-url entry's label wins by design (C5, documented), base64-as-path (PATH_VALUE_RE demands lowercase segments; 2 of 808 fullmatch, both real paths), cursor past a removed name (Q1/Q3), a clickable model url (AS5 scheme gate + `rel=noopener`; the url is the scan's own host), an unparseable `#svc` line fails closed by design (AP2 — `svc_line` guarantees the shape), exit 2 acted on by the chain (`_step` → `core_failed`, graded), an empty digest list never prunes (`if digests and …`), owned+merged same value stays unattributed by design (BM5/BS12), cursor/error-budget/scan graders exist by name, the DSN default (pre-existing), a first-run stale dashboard ages to DEAD by design, no child processes to orphan (fanout is HTTP in-process), `$FABRIK_ROOT` quoting (Q4), `daily_refresh`'s `_step` prints timing and never alerts (read it) |
| BV1 | Pass 21's row: `found: 34` against dispositions summing to 37 (17 + 1 + 19) — BS1's two items counted as one finding and one duplicate instead of two | native Sonnet, pass 22 (re-derived from the row) | FIXED · LOCAL — 38 / 36 / 17 + 2 duplicates + 19, corrected in place |
| BV2 | BS13's meta-grader regex demanded a literal right after `name=` — a fixture built by concatenation (`"#svc name=" + vendor`) captured nothing and sailed through | native Sonnet, pass 22 (executed) | FIXED · LOCAL — line-wise `#svc name=(\S*)` over every non-comment source line, so a concatenation captures `"` and FAILS; sandbox (a concatenated `deepl` fixture added): → 1 failed |
| BV3 | BS17's value assertions were vacuous in the real checkout (`REPO/…` and the old hardcode are the same Path there) and its source-text regex missed four spellings | native Sonnet, pass 22 (executed all four) | FIXED · LOCAL — the invariant EXECUTED on a relocated copy (`importlib` from a temp root: `OUTPUT` under that root, the hub env still excluded); the regex stays as a floor; folded with BW3 |
| BV4 | the "75 of 109 live vendors" in the BS3 comment and row does not re-derive (74 / 76 / 81 by filter) | native Sonnet + native Opus, pass 22 | FIXED · LOCAL — reworded to the reproducible 82 of 109 |
| BV5 | CHANGELOG Round 21 omitted BS6 (the tooltip's always-on floor) and BS14 (the guard hardening) | native Sonnet, pass 22 | FIXED · LOCAL — one clause added |
| BW1 | (security/cost) BS3's `return None` killed the LEGITIMATE label credit too: 14 of 564 live hosts (at 7442058b; 555 when first measured) of 8 vendors sit on a sibling domain (`fal.run`/`fal.ai`, `replicate.delivery`, `youtube.com`/`developers.google.com`, `supabase.co`, `siliconflow.com`/`.cn`, `bfl.ai`, `kilo.ai`, `openpagerank.com`) — today they land on the right block only because the label equals the key AND the vendor has env keys; drop the env key and the host becomes a NEEDS-TRIAGE block under the vendor's own NAME, the classifier pays for it and its answer OVERWRITES the curated entry (`status=retired`, url, capability) | native Opus, pass 22 (executed: old vs new over 555 hosts; a synthetic youtube-only catalog → NEEDS-TRIAGE) | FIXED · LOCAL (three parts, each graded): the refusal is said aloud naming the `hosts` fix; the 14 hosts are curated into the 8 entries' `hosts` (data grader over the live catalog); a refused host is filed under its registrable domain (`fal-run`), never the vendor's name (`code_only_block_name`), and the classifier keeps a curated entry's fields when it re-classifies one; live `--apply`: 0 attributions lost; sandbox: → 1 failed ×4 (warning, name, no-overwrite, catalog) |
| BW2 | BS14's guard diagnostic raised `TypeError` (array columns come back as lists, unhashable in a set) instead of naming the changed rows — the mitigation BS14 bought did not exist | native Opus, pass 22 (executed) | FIXED · LOCAL — `_row_diff` hashes list columns as tuples; grader; sandbox (raw sets): → 1 failed |
| BW3 | BS17 moved the HUB-env exclusion to `REPO/.env`: a relocated copy scanned the real `/opt/fabrik/.env` as a 22nd project (every finder's sandbox measured 451 services where the census says 434) | native Opus, pass 22 (executed) | FIXED · LOCAL — the exclusion is the FLEET path `OPT/fabrik/.env` (+ the copy's own `.env` and `OUTPUT`); the relocated-copy grader asserts it; sandbox (`REPO/.env`): → 1 failed |
| BW4 | BS4's token normalisation collapses a curated `HF_` and a merged `HF` into one token, so `top & merged` disowns the vendor's OWN keys — and classify's merge path mints exactly that (it checks `merged_match`, never `match`); live: 8 of 109 curated prefixes end in `_`, 0 merged today | native Opus, pass 22 (executed both orders) | FIXED · LOCAL — `merged_matchers` drops a merged copy of a curated token; the mint site never appends one; graders on both sides (the classify one through the real merge path); sandbox: → 1 failed ×2 |
| BW5 | BS5's tie WARNING was a stderr line in a cron log the repo itself documents as unread, printed 3× per run — "never invisible" rested on no channel | native Opus, pass 22 | FIXED · LOCAL — a cross-provider tie FAILS the scan (`CatalogError` naming prefix + claimants; the chain alerts; the previous consolidation stands — the AU7 posture); grader rewritten; sandbox (warning restored): → 1 failed |
| BW6 | an all-`_` prefix produced an unactionable warning about an empty token, and BS4 silently changed `("_", p)` from "matches every `_`-key" to "matches nothing" with no grader | native Opus, pass 22 | FIXED · LOCAL — empty tokens claim nothing; grader asserts the all-`_` catalog loads and routes nothing; sandbox: → 1 failed |
| BW7 | BS8's "reason with the path" held for 3 of 4 fail-closed shapes — the whitespace-key error carried none | native Opus, pass 22 | FIXED · LOCAL — the path is in the message; grader; sandbox: → 1 failed |
| BW8 | one test carried BS13's source-text grader AND BS14's DB-shape grader behind one PG skip: DB-free runs reported the first as skipped and the second never ran | native Opus, pass 22 | FIXED · LOCAL — split into two tests |
| BW9 | the BS16 card summed KEY counts beside four provider counts and was labelled `unattributed` | native Opus, pass 22 | FIXED · LOCAL — `unattributed keys`; grader; sandbox: → 1 failed |
| BW10 | the same 75-of-109 as BV4 | native Opus, pass 22 | FIXED under BV4 |
| BW11 | a catalog truncated to `{}` / `{"_README": …}` is KNOWN-empty by design (BR3) and exits 0 — every vendor blanks to `?`, the whole file goes to paid triage, and only the bounded prune's `RuntimeError` downstream saves the registry; the BS2 row read as if the class were closed | native Opus, pass 22 (executed) | FIXED · LOCAL — `refuse_emptied_catalog()` in `main`: when the previous consolidation carries catalogued `#svc` lines and the catalog now has no providers, the scan fails closed (a genuine bootstrap — no catalogued line before — still proceeds); grader through `main()` (nothing written); sandbox (guard removed): → 1 failed |
| BX1 | **SUPERSEDED by CA2 — the premise was FALSE** (`--only <curated>` cannot dispatch: it is filtered against NEEDS-TRIAGE; the keep's real caller is a curated entry whose block is in triage, 16 of 109 one key away). Original text: (correctness/cost) BW1's curated-keep fired ONLY where it was wrong: the daily path can no longer send a curated name to the classifier (0 of 346 NEEDS-TRIAGE names are catalog keys, by BW1's rename), so its only caller was the operator's own `--only <vendor>` refresh — which became a PAID no-op that printed `Wrote 1 identified providers` while the catalog stayed byte-identical (executed through `main()`) | native Opus, pass 23 (executed; 100 % of `--only <curated>` runs, 0 of 346 daily) | FIXED · LOCAL — the keep is gone; the model's answer wins the metadata, `match`/`merged_match`/`hosts` survive as before (AF8); the grader that had locked the wrong branch in now asserts the refresh lands; sandbox (keep restored): → 1 failed |
| BX2 | the Pass 22 row's arithmetic did not close (`found 35 / fixed 18` against 16 named fixes and 31 derivable candidates) — BV1's defect one pass later | native Opus, pass 23 (re-derived) | FIXED · LOCAL — 31 / 30 / 16 + 1 duplicate + 14, corrected in place |
| BX3 | CHANGELOG Round 22 omitted BW6, BW7 and BW8 (3 of 14 code-changing fixes) — BV5's defect one pass later | native Opus, pass 23 (8 clauses matched against 14 rows) | FIXED · LOCAL — one clause |
| BX4 | the reference doc named none of pass 22's behaviours: the tie that fails the scan, the emptied-catalog refusal, the sibling-domain warning, and `hosts` as the OPERATOR's field (the warning tells them to edit it; the doc described it as classifier-written only) — its exit-code enumeration read as exhaustive and was two causes short | native Sonnet ×2 + native Opus, pass 23 (grep denominators) | FIXED · LOCAL — the failure-visibility sentence lists every refusal; the conventions paragraph says what `hosts` is for and how a refused host is filed; the chain grader still checks the step table |
| BX5 | the alert body decoded 124 / 137 / 2 but not **1** — the exit of EVERY catalog refusal, including the two BW5/BW11 added; the cause sat only in the untailed log BW5 had declared no channel | native Opus, pass 23 | FIXED · LOCAL — the body decodes 1 with the six causes and points at the first stderr line; chain grader; sandbox: → 1 failed |
| BX6 | BU1's executing grader covered 2 of 3 branches: adding `classify_services` to the `core_failed` case list (a one-word edit that ages the heartbeat on every paid-step failure) passed it — that branch was still proven only by regex | native Opus, pass 23 (mutation-executed) | FIXED · LOCAL — a third scenario: classify fails → alerted, exit 1, data steps run, dashboard written; sandbox (classify made a core step): → 1 failed |
| BX7 | one population, three denominators: "14 of 555" (BW1), "0 of 561" (Pass 22 row), 564 measured twice at 7442058b — the host population drifts under the measurer | native Opus, pass 23 (measured 563/564) | FIXED · LOCAL — every host count carries its sha and the drift is stated |
| BX8 | (fail-closed cascade, latent 0 of 346 but reachable by the classifier's own normal operation) a tombstoned provider re-tried and answered `name: <other vendor>` writes its token into the target's `merged_match` while its own `match: [TOKEN]` stays behind → a cross-provider tie → BW5 fails EVERY later scan, permanently, with tombstone and merge both inert (executed end to end: `cs.main()` then `ge.main() == 1`) | native Opus, pass 23 (executed; 0 of 346 live triage names collide with a curated token) | FIXED · LOCAL — the merge removes the source entry, and a token ANY other provider curates is never minted as a merged copy; grader through the real merge path (both halves); sandbox: → 1 failed ×2. Fixed despite the zero fire-rate: the consequence is a permanent chain death that BW5 itself created |
| BX9 | BV2's comment-skip (`lstrip().startswith("#")`) would skip a `#svc` fixture written inside a triple-quoted block; 0 of 25 lines affected today | native Opus, pass 23 (measured 2 of 25 skipped, both the grader's own) | FIXED · LOCAL (a one-line removal + a reworded comment); sandbox (a comment-line fixture naming a real vendor): → 1 failed |
| BY1 | pass-23 pool candidates: 34 code (deepseek-v4-flash — the provider stalled past the wall clock and the batch was partial-tolerant; 1 CONFIRMED, 33 PLAUSIBLE, most self-refuted) / 8 ops-tests (gemini-3-flash) / 0 docs (qwen3-max: `[]`) — 34 + 8 + 0 = 42, counted from `pool_wide_results.json` by `"title":` (CA8) | pool fanout ×3 routed, pass 23 | REFUTED (42 of 42, many-to-one onto ~21 grounds): the "backward cursor" is the wrap (Q1/Q3; the cursor after a wrap IS the last processed name), IPv6 brackets (BB5 by construction), `code_only_provider` on empty names (self-refuted), the error budget reset (self-refuted, O8), typed state reads (G6), the tmp directory (C10/BH7), the `NEEDS-TRIAGE` header substring (the header is machine-written), out-of-enum status → `?` (BB4, by design), merged prefix onto a code-only target (AD2 — a key-carrying source merged into a host-only vendor is that vendor's key by the model's word, warned/unattributed by BH1 on the fetch side), the flock fd (released on close/exit), tombstone `match: []` for code-only (documented, by design), the stale probe (BM3/BR7 documented), non-dict provider values (AY8), `_alert`'s `|| true` (best-effort alerting by design; the heartbeat is the backstop), a truncated dashboard on SIGKILL (atomic `os.replace`), `VENV_PY` default (env-overridable), rg exit 2 partial scan (W5, accepted), dashboard secrets (digests only, AM4), tmp symlink (`O_EXCL`), SIGKILL mid-transaction (Postgres rolls back; the env file is atomic), `_alert` argument injection (`"$1"` is one argv; labels are literals) |
| BZ1 | pass-23 LATENT findings, each with a measured zero fire-rate — recorded, not built (the bar from the Pass 22 row): the sibling-domain DATA grader depends on today's curated set (a RULE grader exists beside it: `test_wildcard_attribution_keys_on_the_registrable_domain`); `code_only_block_name`'s domain branch is unreachable after the backfill (0 of 564) and a derived name that is itself a catalog key would render `category=?` (0 of 564; 0 collisions); the block namer does not `lstrip("_")` like `derive_provider` (0 of 564 hosts start with `_`; 12 platform hosts well-formed); `refuse_emptied_catalog` counts `category=?` by substring over the whole `#svc` line (0 of 453 misparses; `known = 107 = stats["catalogued"]`) and counts the 6 tombstones as known — stated here; the guard now reads the 0600 secrets file on a dry run and an `OSError` there is a traceback (0 observed; the file is ours); the guard's diagnostic `of {len(diff)}` counts a symmetric difference (≈2× changed rows; only visible on a real guard failure) | native Sonnet + native Opus, pass 23 (each measured) | ACCEPTED ×6 — verb 5: a detector or fix with a measured zero fire-rate is surface, not safety |
| CA1 | the exit-1 clause BX5 put in the ONE shared alert body was asserted for every step, and incomplete for the scan: `registry_sync` exit 1 (a missing `all-envs.env`, an unhandled DB error) paged "the scan REFUSED the catalog" while the same body's next sentence said "a dead registry DB needs a hand"; and `gather_envs` returns 1 for `CodeScanError` and for "no `/opt/*/.env`" too — 1 of 6 explicit exit-1 sites is a catalog refusal; the doc's line 11 and line 41 gave two exhaustive-sounding meanings | native Opus + native Sonnet, pass 24 (executed: `FAIL_SYNC=1` under bash; `ge.main()` with no env files and with a dead ripgrep, both rc=1) | FIXED · LOCAL — the body decodes 1 PER STEP: for `gather_envs` the three input refusals (the catalog's six shapes, ripgrep, no env files), for any other step "its own failure — read its first stderr line"; the doc's two lines agree; chain grader asserts both halves; sandbox (old clause restored): → 1 failed |
| CA2 | **BX1's premise was FALSE**: `--only <curated>` cannot dispatch at all (it is filtered against NEEDS-TRIAGE — executed: `--only kilo` on a curated kilo → "none of the named providers is in NEEDS-TRIAGE", rc 1, catalog unchanged), so the keep BX1 removed had never made an operator run a no-op; its REAL caller is a curated entry whose block is in triage — its own key not one of its `match` tokens and no code host rescuing it (the enabling population re-derives as 15–17 of 109 depending on the rule — own key not routed / routed to None / block named after the key — and 1–3 with no host; pass 25 closed the whole class at the scan, CC1) — and with the keep gone one model answer overwrote the operator's category/cost/capability/url/status, a wrong url then refusing the vendor's own hosts into a new paid block daily (executed on `huggingface`) | native Opus, pass 24 (executed; 0 of 346 today, the enabling condition measured) | FIXED · LOCAL — the narrow keep is back on its true premise: a curated entry in triage keeps the operator's fields, the model fills only `?`; the grader drives the DAILY argv and proves `--only <curated>` is refused; BX1 marked SUPERSEDED; sandbox (keep removed): → 1 failed |
| CA3 | BX8's `catalog.pop(prov)` was unconditional on the entry's shape: a CURATED source in triage whose model answer named another vendor was deleted — operator `match`/`hosts`/url gone, every key its `match` routed re-attributed to the other vendor (executed: `CAPTCHA_API_KEY` → anticaptcha at dd55ca81; red-on-revert at 7442058b) | native Opus, pass 24 (executed; 0 of 346 today, 16 of 109 one key away, 3 with no host) | FIXED · LOCAL — a surviving curated source counts among the token holders, so its token is never minted onto the target and the entry is never popped (`tombstone_of` guards the pop as well); grader case 2; sandbox (pass-23 counting restored): → 1 failed (the tie itself: `GEMINI (gemini, google-ai)`) |
| CA4 | `_curated_tokens` read `match` only while `load_catalog`'s tie claims read `match` AND `merged_match`: a token another entry carried as a MERGED prefix was minted again → tie → every later scan dead (executed end to end) | native Opus, pass 24 (executed; 0 of 109 entries carry `merged_match` today) | FIXED · LOCAL — both fields; grader case 3b; sandbox (`match` only): → 1 failed (`GEMINI (google-ai, vertex)`) |
| CA5 | the skip branch (token curated elsewhere) still ran `merged_into` + `identified.pop`: no entry, no tombstone, no routing, exit 0 — the C2/AF2 re-dispatch loop, silently, plus a cosmetic `merged_match: []` on the target; and the grader's fixture was a state the scan cannot produce (a key whose token any entry curates is routed, never flagged) | native Opus, pass 24 (executed) | FIXED · LOCAL — not a merge: the provider keeps its own identified entry with `match: []` (its key is routed by the curator) and is never re-dispatched, said by name; the target's own curated token stays the BW4 merge (nothing minted, consumed); grader case 3 asserts the entry, the empty prefix and the routing; the fixture's unreachability is stated in the row (classify reads the file it is given); sandbox (skip consumes): → 1 failed |
| CA6 | the Pass 23 row named BX9 (grader-only) as code-changing and left out BX5 (the chain script) | native Sonnet, pass 24 (single-commit `--stat`) | FIXED · LOCAL — corrected in place |
| CA7 | CHANGELOG Round 23 omitted BX9 — BX3's defect one pass later | native Sonnet, pass 24 | FIXED · LOCAL — one clause |
| CA8 | BY1's "42 of 42" was not derivable from the row (≈21 grounds for 42 candidates) | native Sonnet, pass 24 | FIXED · LOCAL — the per-partition counts (34 + 8 + 0) and the many-to-one are stated |
| CB1 | pass-24 pool candidates: 5 code (deepseek-v4-flash, all PLAUSIBLE) / 6 ops-tests (gemini-3-flash, all "CONFIRMED" — five describe existing graders proving existing behaviour, one is a rules-pack claim outside this surface) / 0 docs (qwen3-max: `[]`) | pool fanout ×3 routed, pass 24 | REFUTED (11 of 11): cursor with a removed name (Q1/Q3), rg partial (W5), `KNOB_VALUE_RE` demoting a weak unanchored value (AQ-era by design — an unanchored name needs a secret-shaped value), pid reuse (`O_EXCL` + the hourly sweep), a code-only tombstone's `match: []` (documented), the orchestrator bypass prefix (a rules pack, not this chain), the static-tmp race / unreadable-catalog exit 2 / bounded prune / `javascript:` url / XY vs XY_ (each the behaviour a shipped grader already proves) |
| CC1 | the whole rounds-22–25 argument over the classifier's keep was one branch upstream: a curated entry whose own key is not one of its `match` tokens (`HUGGINGFACE_API_KEY` under `match: ["HF_"]`) had its block filed as NEEDS-TRIAGE, so every lap paid a pool unit for it and `--only <name>` on it was a paid no-op (executed two-lap sequence: flagged 1 → identified → keep wrote `match` unchanged → flagged 1 again) | native Opus, pass 25 (executed; 0 of 346 today, 15–17 of 109 one key away) | FIXED · LOCAL at the SCAN — a key whose derived name is a catalog key files under that entry (the name is the evidence a model answer `name: <key>` would be; a tombstone is adopted too, so it is never re-billed); the classifier never sees a curated name; the keep is gone as dead code; grader through `consolidate()`; live: 107 catalogued / 346 triage unchanged (0 such blocks today); sandbox (adoption removed): → 1 failed |
| CC2 | CA3's `tombstone_of` pop-guard was ungraded: case 2's curated source has its own name as its token, so the CA5 branch fired first and the pop was never reached — 89 of 89 tests green with the guard removed (both native finders) | native Sonnet + native Opus, pass 25 (mutation-executed) | FIXED · LOCAL — case 4: a curated source whose token is NOT its prefix (`match: ["GOOGLE_GEMINI"]`) is minted onto the target and KEPT; sandbox (guard removed): → 1 failed |
| CC3 | CA5's own-entry orphan carried the TARGET's url, so `catalog_url_index` dropped the domain both entries claimed and the target's host de-attributed into paid triage every lap (executed on the grader's own fixture: `url_index == {}`); its stdout line reached no record and `classify_last.json` counted it as identified; the branch is unreachable from the scan (a routed key is never flagged) | native Opus, pass 25 (executed) | FIXED · LOCAL — merged WITHOUT a prefix (the curator keeps routing the key), no orphan entry, said by name; grader case 3 asserts no entry, no mint, routing to the curator; sandbox (orphan restored): → 1 failed (the tie) |
| CC4 | `tombstone_of` keyed on `category` alone: a hand-written `?` placeholder with a curated `match`/`hosts`/`url` was a tombstone and popped by the merge | native Opus, pass 25 (0 of 109 live entries carry `?`) | FIXED · LOCAL — a `?` entry with any curated routing field is the operator's; grader case 5; sandbox: → 1 failed |
| CC5 | the pass-24 alert body: 896 chars against the alerting contract's ~500, an unbalanced `*` (`/opt/*/.env`) and 11 `_` for Telegram's legacy-Markdown fallback (its own error map names bad Markdown as a 400), "for gather_envs" while the second scan's label is `gather_envs_reconsolidate`, "one of three" while `write_secret_file`'s OSError is a fourth exit-1 cause; and CA1's doc half was ungraded (both doc sentences falsifiable with 13 of 13 green) | native Opus, pass 25 (executed: argv dump, 896/1/11; ENOSPC through `main()`) | FIXED · LOCAL — a shorter body with no `*` or `_` in its own text (pass 25 called it "480 chars"; it was 534 literal and 582–596 rendered — pass 26 measured it and cut it under 500 rendered for every label, CD5), "the two gather steps", the fourth cause, the legend pointed at the doc; the doc says "one of four" on both lines; the chain grader lifts the body and asserts parity, length and both doc sentences; the label/log-path underscores remain (alerting's to escape — STRATEGIC_BACKLOG row filed); sandbox (`*` restored / doc reverted): → 1 failed each |
| CC6 | the Pass 24 row's `found 25 / new 22` did not derive from its own composition (11 + 4 + 7 = 22, duplicates counted inside) — BV1/BX2's defect a third time; and CA2's "16 of 109 / 3 with no host" re-derived as 15–17 / 1–3 by three finders | native Opus + native Sonnet, pass 25 | FIXED · LOCAL — 22 / 19, and the range stated with its rules |
| CC7 | pass-25 pool candidates: 7 code (deepseek) / 6 ops (gemini) / 6 docs (qwen — its first non-empty list, all about files outside this delta) | pool fanout ×3 routed, pass 25 | REFUTED (19 of 19): cursor-before-dispatch (AC5 by design), the deferred error budget (O8 — consecutive PROCESSED runs by design), `ignored_host` suffixes (dotted by construction), the label fallthrough (BS3 — it returns None, the finder read pre-BS3 code), a single-segment path value (2 of 808 measured), `SOME_ID` (self-refuted), U+2028 in a JS string literal (legal since ES2019, the page's own script parses it), provenance None → unattributed (BK1 + exit 2 by design), the sweep race (mtime-keyed), a new step outside the `case` list (the list is the contract and is graded), `$LOG_FILE` (quoted), `_alert`'s `|| true` (best-effort by design), orphaned children (none spawned), the CLAIMS kind (Q13), 148/116 (D-085), INDEX's removed script (deleted by design), D-081 vs D-085 (rows immutable, supersede-by-row by design), the EXTERNAL_SYSTEMS derivation (D1), a fresh heartbeat after a post-write alert failure (the data was written) |
| CE1 | pass-25 LATENT findings recorded, not built: the "tombstoning" print for an exhausted provider the catalog already carries (now says so; reachable only for a `?` placeholder with routing — CD2's class); a `'` in `FABRIK_ROOT` inside the `-c` string (env-controlled, default `/opt/fabrik`); the sibling-domain DATA grader (stated in BZ1) | native Opus + native Sonnet, pass 25 | ACCEPTED ×3 |
| CD1 | (fail-closed cascade) CC4 tightened `tombstone_of` but the merge path's INLINE tombstone test stayed on the old rule: a `?` placeholder with its own name as its curated prefix (`{"category": "?", "match": ["GEMINI"]}`) was excluded from the token holders (old rule) and minted onto the target, yet NOT popped (new rule) → a cross-provider tie → every later scan dead; CC4's own grader used the one shape of its class that does not fire (`GOOGLE_GEMINI`) | native Opus, pass 26 (executed; red-on-revert at 993872de; 0 of 109 live `?` entries) | FIXED · LOCAL — one rule (`tombstone_of`) at both sites; grader case 6 (the firing shape); sandbox (old rule restored): → 1 failed (the tie) |
| CD2 | (removed guard) CC1 deleted the keep on the premise "a curated name never reaches here" — false for the class CC4 created in the same commit: a `?` placeholder with curated routing routes its own key, its block is `category=?` → triage → dispatched, and the model overwrote the operator's cost/capability/url/status (`retired` → `active`, the url replaced — executed); `--only` on it too | native Opus, pass 26 (executed; 0 of 109 live) | FIXED · LOCAL — the keep restored on CC4's predicate (the operator's fields stand, the model fills `?`); grader on the daily argv AND `--only`; CE1's "unreachable" corrected; sandbox (keep removed): → 1 failed |
| CD3 | CC1's adoption was the chain's only routing decision never said aloud — a misfile leaves triage on the first lap with no line, no stat, no alert; `owned_by` then treats the adopted key as attributed (`not hits → True`) and a fetch candidate | native Opus, pass 26 (executed end to end: `DEEPL_TOKEN` under a `DEEPL_API`-only match → rank 0) | FIXED · LOCAL — `stats["adopted"]` and one summary line naming the adoptions (names only) on both the dry-run and the apply path; grader; sandbox (line removed): → 1 failed |
| CD4 | "the first stderr line names the cause" was false for the fourth exit-1 cause CC5 itself added: `write_secret_file` re-raises and `main` caught only the typed errors — a disk-full/permission failure exited 1 with a traceback (executed: `PermissionError` first line `Traceback`) | native Opus + native Sonnet, pass 26 (executed) | FIXED · LOCAL — the write is wrapped: one ERROR line, exit 1, the previous file stands; the body and the doc say "the step's stderr names the cause"; grader; sandbox (unwrapped): → 1 failed (the OSError escapes) |
| CD5 | the "480-char" body was 534 literal and 582–596 RENDERED (label + production log path) — 82–96 over the alerting contract's ~500, and the grader's `<= 520` on the variable-stripped template was calibrated to the shipped string, proving the opposite of its comment | native Opus + native Sonnet, pass 26 (measured 5 of 5 labels) | FIXED · LOCAL — the body cut to render under 500 for every label (longest: `gather_envs_reconsolidate` at 495), the `REGISTRY_PRUNE_FORCE` remedy back as "a prune refusal needs the force knob"; the grader renders all five labels with the production log path; the CHANGELOG and CC5 rows corrected; sandbox (one clause added): → 1 failed (551) |
| CD6 | the Pass 25 row: `347` triage (the header line counted) vs `346` on disk in the same sentence; 13 + 3 + 19 = 35 ≠ 36 (adjudication) and "3 duplicates" for two `×2` pairs (new 33 → 34) — CC6's class in CC6's own row; `--only` grader docstring overclaimed CC1; the `newvendor` assertion documents unrelated behaviour | native Opus + native Sonnet, pass 26 | FIXED · LOCAL — 346, 15 candidates covered / 2 accepted / 19 refuted = 36, 2 duplicates, new 34; the `--only` docstring reworded (the `newvendor` comment was NOT — claimed here, done in pass 27, CE8); the doc says tombstones are adopted too |
| CF1 | pass-26 pool candidates: 7 code (deepseek) / 5 ops (gemini) / 4 docs (qwen) | pool fanout ×3 routed, pass 26 | REFUTED (16 of 16): `sch.uk` (BT1), the platform-SLD `EMAIL` prefix (AB3 by design, 0 live), cursor with a removed name (Q1), `owned_by` on no hits (by design), an empty catalog at bootstrap (BW11: no previous file → proceeds), cursor-before-dispatch (AC5), rg paths relative to `OPT` (by construction), the daily-lock race (pre-existing mechanism), `case` label quoting (literals), tmp on SIGTERM (`BaseException` unlink + sweep), a test-fixture connection on an execute error (test-only teardown, accepted), `daily_refresh`'s outbox flush (fail-open by design, unrelated data), CLAIMS kind (Q13), CHANGELOG entities (none present), INDEX's "46 days" (historical by design), CLAIMS 150 (unrelated) |
| CG1 | pass-26 LATENT findings recorded, not built: a generic derived name adopting a third-party vendor (0 of 6 live derive-path keys adopt; 12 of 109 entries would; every fetcher vendor curates its own name today — the corroboration is catalog state, now said aloud by CD3); a non-str `category` counted catalogued (0 of 109); hyphenated catalog keys unreachable by adoption (0 of 1082 non-empty project env lines — consolidate's INPUT, not the 1150-line output file) | native Opus + native Sonnet, pass 26 (each measured) | ACCEPTED ×3 (pass 27 corrected this row: two further items — the `--only` docstring and the `newvendor` comment — were REWORDINGS under CD6, not acceptances, and one of them had not actually landed, CE8) |
| CE2 | (cost, latent 0 of 109) a `?` placeholder with curated routing whose model answer named ANOTHER vendor was merged (token minted onto the target) but never popped (operator's) and never stubbed (merged ones skip the tombstone loop) — its category stayed `?`, so it was re-dispatched every lap forever (executed 3 laps on CD1's own grader shapes); the same paid loop for a curated real-category source in the same fixture | native Opus, pass 27 (executed; 0 of 109 live `?` entries) | FIXED · LOCAL — a catalogued non-tombstone source is never folded into another vendor by a model's word: the identified path keeps the operator's fields and fills its `?`, so it leaves triage; grader cases 4 and 5 assert the category lands and nothing is minted; sandbox (merge condition restored): → 1 failed |
| CE3 | (cost, latent 0 of 109, a regression of CD2) the keep copied a hand-edited NON-str `category` back over the classifier's verdict; `by_cat` maps it to `?` → triage → dispatched again, and the tombstone loop skips it — a paid unit every lap with no way out (executed 3 laps; 1 lap with the keep removed) | native Opus, pass 27 (executed) | FIXED · LOCAL — the keep never copies `category` (the enum verdict is the classifier's); grader; sandbox: → 1 failed |
| CE4 | (removed-guard mirror, latent 0 of 109) CD2 restored the keep on the identified branch only — on the TOMBSTONE branch an unidentifiable answer erased the operator's cost/capability/url/status (executed: `retired` → `unidentified`, url → `?`, and a lost url loses the C5 wildcard credit) | native Opus, pass 27 (executed) | FIXED · LOCAL — the stub carries the operator's non-`?` words too; grader; sandbox: → 1 failed |
| CE5 | (observability, latent) the run printed the model's proposal and wrote something else (4 of 5 fields discarded by the keep) with no line saying so — a paid unit whose only effect is `category`, reported as a full re-classification | native Opus, pass 27 (executed) | FIXED · LOCAL — `kept the operator's <fields>` printed beside the proposal (names of fields only) |
| CE6 | CD3's dry-run line was ungraded (113 of 113 green with it deleted) and the line was absent on the no-change early return — the steady-state daily run would print it zero times | native Opus, pass 27 (mutation-executed) | FIXED · LOCAL — one line, printed on EVERY path before the apply branch, worded as what it is (the ENTRIES a key named for them adopted — Opus #5's wording point); grader covers the dry-run; sandbox (line moved back under the write): → 1 failed |
| CE7 | CD4 wrapped the write only: the previous file unreadable (mode 0000) or the directory unwritable still exited 1 with a traceback (executed both), falsifying the doc's "a one-liner for every gather refusal" and its "one of four" | native Opus, pass 27 (executed; 0 of 1 live — the file and dir are ours) | FIXED · LOCAL — mkdir, sweep, the previous-file read and the write share one `except OSError` → one ERROR line; the doc says "one of five" and "its own output path cannot be read or written"; grader (a `PermissionError` from the read); sandbox (read hoisted out of the handler): → 1 failed |
| CE8 | ledger hygiene: the id `CD1` named two rows (pass 25's pool refutations and pass 26's tombstone rule) and `S6` two (pass 1 and pass 6); CD6/CG1 claimed the `newvendor` comment was reworded (the diff shows it was not) and CG1 listed two rewordings as "not built"; CD5 said 494 for a body that renders at 495; CG1's "1082" is consolidate's INPUT (non-empty project env lines), not the 1150-line output | native Sonnet + native Opus, pass 27 | FIXED · LOCAL — `CC7`, `S6b`; the comment reworded now; CG1 ×3 with the population named; 495; the Pass 26 split 14 / 3 |
| CE9 | the CD5 grader hardcoded the production log path (5 chars of headroom; a longer `FABRIK_ROOT` would pass the grader and break the contract) | native Opus, pass 27 (measured 495 of 500) | FIXED · LOCAL — the grader reads `LOG_FILE=` out of `daily_refresh.sh` |
| CH1 | pass-27 pool candidates: 5 code (deepseek) / 7 ops (gemini — five describe existing graders proving existing behaviour) / 4 docs (qwen — all outside this delta) | pool fanout ×3 routed, pass 27 | REFUTED (16 of 16): `me.uk` second levels (BT1), a code-host-only provider's keys never pruned when `digests` is empty (`if digests and …` is the Z7 partial-file guard; a provider with NO rows in the file is pruned as a service), the synthetic `<SLD>_API_KEY` probe (documented, AB3), the cursor wrap at `max_per_run == len` (Q1/Q3), a 6-letter word as a secret under an unanchored name (AQ-era by design), the malformed catalog / shared tmp / `javascript:` url / prefix tie / `ensure_schema` lock (each the behaviour a shipped grader proves), `CERT_PATH` values (PATH_VALUE_RE), `load_dotenv` in `_alert` (reads the hub `.env` by design; the body is one argv), CLAIMS kind (Q13), 148/116 (D-085), INDEX cross-refs (outside the surface), D-085 vs CLAIMS (unrelated) |
| CI1 | pass-27 LATENT findings recorded, not built: a `write_secret_file` failure on step 2b sets `core_failed` and skips the sync/dashboard although step 1's same-day file is intact (0 daily runs on record — "one cause, one alert" stands); the adoption line has no consumer beyond the log (0 of 3 candidate consumers) | native Opus, pass 27 | ACCEPTED ×2 |
| CJ1 | CE7's handler covered the apply block, but `refuse_emptied_catalog()` reads the previous file BEFORE it — a mode-0000 `all-envs.env` was still a traceback (executed), the CE7 grader could not see it (its `out` did not exist when it patched the read, so the guard short-circuited), and the doc/ledger/CHANGELOG had already claimed "every OSError on the output path" — BZ1's latent recorded in pass 23, marked FIXED in pass 27 without touching it | native Opus, pass 28 (executed; 0 of 1 live — the file is ours) | FIXED · LOCAL — the guard's read shares the one-line handler; the grader writes `out` first and then patches the read; sandbox (handler narrowed): → 1 failed (the traceback) |
| CJ2 | CE5's `kept the operator's …` line was ungraded (114 of 114 green with it deleted — CE6's defect, in the same commit) and printed a claim of a keep with NOTHING kept when the pop left `kept` empty | native Opus, pass 28 (mutation-executed) | FIXED · LOCAL — printed only when something was kept; graded on both the placeholder run and the empty case; sandbox (`if True`): → 1 failed |
| CJ3 | (cost, latent 0 of 109, CE3's mirror) the tombstone loop's "a real entry" test was `not in (None, "", "?")` while the scan buckets a non-str/blank category to `?`: a list category was dispatched every lap and never stubbed (executed 3 laps under the daily argv) | native Opus, pass 28 (executed; 0 of 109 non-str categories live) | FIXED · LOCAL — the loop applies the scan's own predicate (`isinstance(str) and strip() and != "?"`); grader (a list category + an unidentifiable answer → stubbed); sandbox (old predicate): → 1 failed |
| CJ4 | the doc said "one of five" over four top-level clauses (the count bumped in CE7 without a fifth clause; the grader pinned the phrase, not the count) and "read or written" while the body said "output not writable"; the CD2 doc sentence read as if `category` were kept | native Sonnet + native Opus, pass 28 | FIXED · LOCAL — "one of four" (the output-path clause is one cause with three sites), the body says "output path unusable" (one char LONGER than "output not writable" — 496 rendered at most; pass 28 wrote "4 chars shorter — 491", corrected in pass 29, CM1), the CD2 sentence says the verdict lands on `category`; the grader pins the new phrases; sandbox (old body): → 1 failed |
| CJ5 | CE9 read the log path's tail but hardcoded `/opt/fabrik` as its root — a longer `FABRIK_ROOT` rendered 507 with the grader green (executed); and the CHANGELOG's Round 27 clause said "a catalogued source" where the code says NON-tombstone | native Opus, pass 28 (executed) | FIXED · LOCAL — both halves read from `daily_refresh.sh`, once; the clause qualified; sandbox (`FABRIK_ROOT` lengthened in the copy): → 1 failed (508) |
| CK1 | pass-28 pool candidates: 0 code (deepseek STALLED past the wall clock — scored 0) / 5 ops (gemini) / 3 docs (qwen) | pool fanout ×3 routed, pass 28 | REFUTED (8 of 8): a DSN-shaped value at rank 3 (by design, AM1 tiers), path-like values (2 of 808 measured), rg partial (W5), the sweep (mtime-keyed), the label fallback "falls back" (it returns None — BS3; the finder read pre-BS3 code), CLAIMS kind (Q13), 148/116 (D-085), a doc path outside this delta |
| CL1 | pass-28 LATENT findings recorded, not built: `parse_env` swallowing `OSError` (0 of 22 project env files unreadable; the torn-file tolerance is documented — a typed error for the permission case is a design change, not a fix); the classifier's catalog write without the AU10 tmp-unlink twin (0 stray tmp observed); a whitespace-only category getting its own header (0 of 109); CE4's kept `url` on a stub making the stub a domain claimant (0 of 109 `?` placeholders live) | native Opus, pass 28 (each measured) | ACCEPTED ×4 |
| CM1 | the Pass 28 row's numbers: "4 chars shorter — 491" for a phrase one char LONGER (496 rendered at most) and "25 candidates (8 + 2 + 11)" for 21 — the ledger's own class (BV1/BX2/CC6/CD6/CE8), a fifth time | native Sonnet + native Opus, pass 29 (re-derived) | FIXED · LOCAL — 496 / 21 / 0 duplicates, corrected in place; the finder attribution names every fold (Opus #7 → CJ2, Sonnet's doc wording → CJ4) |
| CM2 | the ≤500 grader iterated a HAND-KEPT five-label tuple while `_steps()` in the same file derives the labels (a renamed step rendered 513 with 13 green); the "one of four" grader pinned the PHRASE (a fifth clause under it passed; a fifth `return 1` passed); the log path came from one of the chain's two entry points | native Opus + native Sonnet, pass 29 (mutation-executed) | FIXED · LOCAL — labels from `_steps(text)`, both entry points' `LOG_FILE` literals rendered, the doc's ordinal COUNTED against its depth-0 clauses; sandbox (a fifth clause): → 1 failed |
| CM3 | (latent 0 of 109) three hand-written category predicates: the bucketing (`isinstance(str) and cat`), the `catalogued` stat (`!= "?"`) and the classifier loop (`+ .strip()`) — a `null`/list/blank category rendered under NEEDS-TRIAGE while the header counted it catalogued, and the CJ3 comment's "the SAME predicate" was false for blank; the CJ3 grader covered only the list shape (dropping `.strip()` was green) | native Opus, pass 29 (executed over four shapes × six sites) | FIXED · LOCAL — one `gather_envs.real_category()` behind the bucketing and the stat, the loop identical to it; graders over all shapes at both sites; sandbox (old stat predicate / blank clause): → 1 failed each |
| CM4 | (latent 0 of 487 834 paths) rg output that cannot be decoded raised `UnicodeDecodeError` — a `ValueError` past every handler — so the chain paged the four-cause legend for a traceback matching none | native Opus, pass 29 (executed; 0 non-UTF-8 paths under /opt) | FIXED · LOCAL — it is the documented "ripgrep cannot run" cause (`CodeScanError`); grader; sandbox: → 1 failed |
| CM5 | (latent 0 of 1) CJ1's handler wrapped `consolidate()` too, so an `OSError` from anywhere in the scan was blamed on the output file (executed: `project_dirs` denied → "cannot read or write …/all-envs.env: … '/opt'") | native Opus, pass 29 (executed) | FIXED · LOCAL — the guard has its own handler (its `CatalogError` and its `OSError`), `consolidate` keeps its typed errors; grader; sandbox (widened): → 1 failed |
| CM6 | (latent 0 of 453) `refuse_emptied_catalog` tested the literal `category=?` anywhere on the `#svc` line, not the field — a capability carrying it hid a real vendor from the guard | native Opus, pass 29 (measured) | FIXED · LOCAL — the field, anchored; grader (the literal in the capability, the field real → refused); sandbox (substring): → 1 failed — the first fixture used the non-firing shape and was flipped |
| CM7 | the doc's "the ONE curated shape that is dispatched" undercounted (an entry whose `category` is `null`/a list/blank is dispatched too — the project's own grader proves it); INDEX.md's schema row named 3 of the 4 `kind` values | native Opus + pool docs, pass 29 | FIXED · LOCAL — both sentences |
| CN1 | pass-29 pool candidates: 11 code (deepseek) / 7 ops (gemini) / 5 docs (qwen) | pool fanout ×3 routed, pass 29 | REFUTED (23 of 23 — the INDEX `kind` drift in CM7 was this session's own reading of the pool's INDEX item, not the item): the error budget (O8), `host_domain` (BT1), cursor-before-dispatch (AC5), `--max-per-run 0` (a no-op run by argparse), `_svc_token` on keys (AU7 refuses them), `IDENTIFIER_KEY_RE`'s qualifier (`or ""`), the header's provenance (prose), sweep vs clock skew (1 h margin), a hand-edited previous file (the guard counts fields), the unattributed rows after the catalog returns (`kind=EXCLUDED.kind` on every sync), the dashboard tmp race (C10/BH7 class), rg partial (W5), sweep race (mtime), `_alert` argv (one each), flock on SIGKILL (kernel-released), EXA/EXA_ (BS4), a rules pack, `FABRIK_ROOT` (env-overridable), CLAIMS kind (Q13), 148/116 (D-085), INDEX pipes (a bullet, not a table), D-085 vs CHANGELOG (rows immutable), EXTERNAL_SYSTEMS regeneration (D1) |
| CO1 | pass-29 LATENT findings recorded, not built: a blank category (`"   "`) rendering under a blank header and never dispatched (0 of 109; the predicate now treats blank as real everywhere, the limbo stays a hand-edit); the doc's "one of four" grader once pinned the phrase (now counted, CM2) | native Opus + native Sonnet, pass 29 | ACCEPTED ×1 (the blank limbo) — closed in pass 30: blank is `?` everywhere (CP1) |
| CP1 | (latent 0 of 109/453) the predicate family once more: `real_category` called blank REAL (it rendered under a blank header, never dispatched, `category=?` on its own line — a file contradicting itself), the `#svc` field rendered a list category's REPR straight to the DB while the block sat in triage, the code-host adoption path kept a fourth hand-written variant, and the classifier's copy was ungraded for blank (`.strip()` back → 116 green) | native Opus, pass 30 (executed: 6 shapes × 4 surfaces, 3 laps each) | FIXED · LOCAL — blank is `?` everywhere (`bool(cat.strip())`), the field and the adoption path call `real_category`, the classifier's copy is graded against it (blank stubbed); the blank limbo CO1 accepted is closed; sandbox: → 1 failed ×3 |
| CP2 | (latent 0 of 1) CM5's split re-opened a FIFTH exit-1 cause: an `OSError` from the scan itself (`/opt` unlistable) escaped `main()` as a traceback while the doc said "one of four" and the legend named none of it — CM4's defect one row later | native Opus, pass 30 (executed) | FIXED · LOCAL — one line naming the input file (the "env files" cause; the doc's third clause says so, parenthesised), never a traceback; grader (the CM5 one inverted: exit 1, one line, not blamed on OUTPUT); sandbox (handler narrowed to another type): → 1 failed |
| CP3 | (latent 0 of 453) CM6's anchor was a leading space + trailing space — a model-authored capability carrying `category=? ` still hid a real vendor from the emptied-catalog guard (the BW11 fail-open), and the CHANGELOG's "not the literal anywhere on the line" was false | native Opus, pass 30 (executed) | FIXED · LOCAL — the regex matches the field at its fixed position after `#svc name=<token>`; the grader's fixture is the firing shape; sandbox (search restored): → 1 failed |
| CP4 | graders: the depth-0 clause counter split on commas only (a fifth cause joined by a bare `or`/`;` passed); the entry-point loop was aggregate-guarded (an entry point that stopped setting `LOG_FILE` passed silently); CM7's INDEX half had no grader | native Opus, pass 30 (mutation-executed: 2 of 5 doc mutants and 1 of 3 entry mutants escaped) | FIXED · LOCAL — boundaries at depth 0 on `,`/`;`/`or` (the doc parenthesises an in-clause alternative and writes "read/written"), a per-entry assertion, INDEX's schema row checked for every `kind`; sandbox: → 1 failed ×3 |
| CP5 | texts: the Pass 29 row credited Opus 12 items over an enumeration of 11 (CM7's doc sentence unlisted); CN1's "22 of 23" over a 23-item list; the classify comment and a test docstring still said "the ONE curated shape" after CM7 made it plural; two grader comments cited CM1 for CM2; the doc called a whitespace-only category "dispatched" while CO1 said the opposite | native Sonnet + native Opus, pass 30 | FIXED · LOCAL — all six texts; with CP1 a whitespace-only category IS dispatched now, so the doc's sentence became true and CO1 closed |
| CQ1 | pass-30 pool candidates: 1 code (deepseek) / 6 ops (gemini) / 0 docs (qwen) | pool fanout ×3 routed, pass 30 | REFUTED (7 of 7): "the final internal `else` is missing" (it is there, after the `SECRET_KEY_RE`/`SERVICE_SHAPE_RE` branch), `$FABRIK_ROOT` with spaces (Q4), the `case` list as a contract (graded by `test_the_chain_is_one_script…`), alert storms (AC13/BR4 gating), `env LOG_FILE=$LOG_FILE` (it is `"$LOG_FILE"`, quoted), test fixtures catching `Exception` to skip (by design — an unreachable PG skips), "the test file ends mid-line" (the pool's own diff truncation) |
| CS1 | (latent 0 of 109) CP1 moved the limbo from `"   "` to `" ? "`: the predicate stripped for emptiness but compared the UNSTRIPPED value with `?`, so `" ? "` was real for the header (catalogued 1, no triage block, never dispatched) while its own `#svc` line and the dashboard said `?`; an untrimmed real category minted a duplicate section | native Opus, pass 31 (executed: 13 shapes × 3 surfaces) | FIXED · LOCAL — compare and bucket on the stripped value, in the predicate and the classifier's copy; graders (`" ? "`, `"\t?\n"`) at both sites; sandbox: → 1 failed ×2 |
| CS2 | (latent 0 of 22) an unreadable project `.env` was silently dropped by `parse_env` (`except OSError: return out`) — exit 0, the secrets file rewritten without that project, its keys then DELETEs against the sync — while the doc promised "never a degraded file" and CP2's comment claimed the case | native Opus, pass 31 (executed: chmod 000 → `projects=1`, silent) | FIXED · LOCAL — only a vanished file (a mid-save race) is tolerated; permissions raise into CP2's one line naming the file; the comment names the reachable triggers (`Path.glob` swallows an unlistable `/opt`, which the no-env-files exit already covers — both finders); grader (chmod 000 → exit 1, one line; unlink → tolerated); sandbox (swallow restored): → 1 failed |
| CS3 | graders: the INDEX `kind` check was a substring (`credential` satisfied by `credential-unattributed` — the 3-of-4 drift CM7 exists to catch passed); the clause counter missed a fifth cause joined by `and`, a cause hidden behind an unbalanced `)` (depth went negative and every later boundary died) and a newline before `or` | native Opus + native Sonnet, pass 31 (9 doc mutants: 4 killed, 5 escaped; INDEX mutant escaped) | FIXED · LOCAL — word-boundary match; depth clamped at 0, whitespace normalised, `or`/`and` both boundaries; sandbox: → 1 failed ×3 |
| CS4 | texts: the Pass 30 row said "three call sites" (two; the adoption-path edit is a uniformity refactor with no observable behaviour — declared inert, no grader owed), CP1's "455" (453), the CHANGELOG's "six stale texts" (seven) | native Opus + native Sonnet, pass 31 | FIXED · LOCAL |
| CT1 | pass-31 pool candidates: 2 code (deepseek) / 6 ops (gemini) / 0 docs (qwen) | pool fanout ×3 routed, pass 31 | REFUTED (8 of 8): the triage flag (it ends at the next section header — `break` on `# ═`), `MONKEY_KEY` (anchored `_KEY` IS a credential), SIGKILL mid-write (`os.replace` atomic), `$LOG_FILE` quoting (quoted), the sweep race (mtime), a `CODE_HOST_URL` fetch bypass (kind `code-host` is never a fetch candidate, graded), `open()+chmod` (it is `O_CREAT|O_EXCL` 0600, graded), merge data loss (the tombstone/curated matrix, graded) |
| CU1 | (LIVE-reachable regression of CS2, 0 of 21 today) the `raise` also caught `IsADirectoryError`: a `.env` DIRECTORY (`python -m venv .env`, a mainstream convention in any of the 45 repos) failed the scan daily — classify, reconsolidate, sync and dashboard all skipped, liveness DEAD until a human noticed; before CS2 it was silently skipped | native Opus, pass 32 (executed) | FIXED · LOCAL — `project_env_files` lists REGULAR files only (a directory or a dangling symlink is no env file); the mirror named: a project whose `.env` is a directory has no env, as before CS2; grader; sandbox (`is_file` check removed): → 1 failed |
| CU2 | (latent 0 of 21) CS2's `FileNotFoundError` tolerance was justified by a claim the code does not honour ("the previous consolidation carries the project") — `main` rewrites the whole file, so a `.env` vanished between the glob and the read was DROPPED silently (exit 0, rc, its keys then deletes under the sync's bound: 18 of 19 projects sit under 90) — CS2's own harm through another door | native Opus, pass 32 (executed two laps) | FIXED · LOCAL — the glob proved the path existed this run: any `OSError` on the read is a refusal through the one-line handler; grader (unlink → exit 1, one line); sandbox (tolerance restored): → 1 failed |
| CU3 | (latent 0 of 110) `tombstone_of` was a SEVENTH copy of the category predicate — unstripped — disagreeing with `real_category` on five shapes: a padded/blank category was dispatched (not real) yet "curated" for the merge path (not a tombstone) → the merge refused, a duplicate identity minted, and with a prior merged copy on the target a TIE → chain dead from tomorrow (executed) | native Opus, pass 32 (executed: 9-shape matrix, both consequences) | FIXED · LOCAL — `classify_services` imports the ONE predicate (`gather_envs.real_category`) for `tombstone_of` and the tombstone loop (the private copy deleted); grader: `tombstone_of` ≡ `not real_category` over eight shapes, plus the curated-routing and padded-`unidentified` cases; sandbox: → 1 failed ×2 |
| CU4 | graders: CS1's bucket-key strip had no grader (reverting it left 109 green — two sections for ` ai-llm ` and `ai-llm`); the counter's clamp closed `)` but an unbalanced `(` still hid a cause silently | native Opus, pass 32 (mutation-executed) | FIXED · LOCAL — a two-entry grader asserts one section (its first fixture wrote a literal `\n` and was fixed); `assert depth == 0` after the loop; sandbox: → 1 failed ×2 |
| CU5 | texts: the doc's third exit-1 clause did not name an unreadable/vanished/non-regular project env file (CS2's whole point — kept inside the parentheses, the ordinal stays four); the curated-shapes sentence omitted the padded `?` CS1 moved into that class; the ledger's "no live defect for the Nth round" streak counted through pass 28's CJ1 (a real gap) — it is the third clean round, not the fifth | native Opus + native Sonnet, pass 32 | FIXED · LOCAL — all three |
| CV1 | pass-32 pool candidates: 5 code (deepseek) / 6 ops (gemini) / 0 docs (qwen) | pool fanout ×3 routed, pass 32 | REFUTED (11 of 11): cursor-before-dispatch (AC5), the budget without `--tombstone-unresolved` (the daily argv sets it; without it the print says "eligible"), U+2028 (legal in ES2019 string literals; the page parses its own JSON), merged prefixes routing hosts (AD2 by design), a malformed catalog (BS2), the shared tmp (per-pid `O_EXCL`), tmp perms (0600 from creation, graded), `_alert` argv (one each), a connection per sync (one), `FABRIK_ROOT` (env-overridable), the 12-factor claim (local state by design for a hub-local chain) |
| CW1 | (CONFIRMED, 0 of 22 live — all regular files, 0 symlinks) CU1's `is_file()` was the wrong primitive twice: it swallows `ENOENT`, so a `.env` vanished (or a dangling symlink) between the glob and the stat was SKIPPED silently — CU2's harm through a third door, one line after CU2 closed the second — and it re-raises `EACCES`/`EIO`/`ESTALE` out of `project_env_files`, whose call in `main` sat outside every `try`: a `.env` symlinked into a vault the chain cannot traverse (or `/opt` itself unlistable) died as a raw traceback, breaking the one-line contract the doc promises for every gather refusal | native Opus, pass 33 (executed both: rc=0 silent · uncaught `PermissionError`) | FIXED · LOCAL — the listing stats explicitly (`stat.S_ISREG`; a directory or FIFO is skipped, any `OSError` on the stat is raised) and the call sits under the same one-line "env files" refusal as the read, through one helper (`inputs_refusal`: the def and two call sites); the mirror named: a dangling `.env` symlink now REFUSES the scan daily until removed (CU1 skipped it, pre-CU1 dropped the project), a venv `.env` directory is still skipped; graders: a dangling symlink → exit 1 one line; a symlink under an untraversable dir → exit 1 one line, no traceback; sandbox (`is_file` restored): → 1 failed; (the try removed): → 2 failed |
| CW2 | graders: one `pytest.skip("running as root")` sat above the CU1 and CU2 assertions in the same function — under root (any container/CI run) the CU1 guard could be deleted and the suite stayed green (0 of 1 known runner; executed: `1 skipped`) | native Opus + native Sonnet, pass 33 (executed) | FIXED · LOCAL — the bundled test split into four (permission · vanished/unstat-able · untraversable · directory) on one fixture; only the two permission concerns skip under root; sandbox with root simulated: old shape 1 skipped (nothing else ran), new shape 3 passed + 2 skipped |
| CW3 | texts: the Pass 32 row claimed "no defect older than pass 31" while CU3's list/blank divergence dates to passes 29–30 (`real_category` minted at pass 29 with `tombstone_of` untouched; only the padded-`?` shapes are pass 31), and its breakdown enumerated 7 Opus items against 8 tagged; the CS2 test docstring still said a vanished file is "tolerated"; the merge-path comment still said "the one such shape"; the doc's "not a real string" mislabelled three string shapes; the doc's third exit-1 clause named "not a regular file" as a read failure (after CU1 it is skipped at the listing, and a FIFO in the race window would hang, not exit 1) | native Opus + native Sonnet, pass 33 | FIXED · LOCAL — all five, the clause kept inside its parentheses (the ordinal stays four; `depth == 0` grader green) |
| CW4 | ACCEPTED with the measurement (latent, hand-edit-only or bounded): the three sibling `?` comparisons (`match`/`hosts`/`url` at the tombstone predicate, the keep filter, the stub) are unstripped — 0 of 109 catalog entries carry a padded string field, the classifier writes the bare literal; a hand-edited `UNIDENTIFIED` counts as real (0 of 109; `tombstone_entry` mints lowercase); the `--only` info body is unbounded (0 of 1 daily invocation — the argv bounds it at 10; `telegram.py` truncates at 4096, never fails); the classify info alert's provider list can carry an odd `_` count, which Telegram legacy Markdown rejects — 1 of 346 triage names (`bridge_internal`), an INFO alert whose loss leaves the classification written and logged, the ssh-apprise leg tried first — the class fix is escaping at `libs/alerting/telegram.py`'s boundary (every caller exposed), recorded in `docs/STRATEGIC_BACKLOG.md`, not a chain edit | native Opus, pass 33 | ACCEPTED (4) |
| CX1 | pass-33 pool candidates: 9 code (deepseek) / 7 ops (gemini) / 4 docs (qwen); Sonnet's "bundled asserts hide later concerns" (a failing assert fails the suite — detection, not diagnosis; each pass-32 mutation was proven to fail at its own assert) | pool fanout ×3 routed + native Sonnet, pass 33 | REFUTED (21 of 21 — the 20 pool candidates under 17 reasons, two of which fold 3 and 2 candidates, plus Sonnet's): `com.uk` (handled by `_CC_SECOND_LEVEL` by design), the rg timeout (`CodeScanError` → a one-line refusal, alerted), `FOO_SECRET_ID` (executed: `is_credential` False — an identifier, by design), `bound_flagged` duplicates (the early return hands back the dict itself, no slice), `parse_env` raising (CU2 by design), the emptied-catalog guard (BW11 by design, the force knob), the tie (refused at `load_catalog`, BW5), a trailing-underscore key (not a key name), the SIGKILL tmp (`sweep_stale_tmp`), `env LOG_FILE=$LOG_FILE` (QUOTED in `daily_refresh.sh:171`; the hook's copy at `wsl_startup_hook.sh:206` is unquoted but `LOG_FILE` is a literal with no whitespace — 0 fire), `credential_grade`/`rg` argv/transaction scope (not visible to the finder; list-argv subprocess, one transaction — reviewed in passes 3–9), PID collision (one box), `_alert \|\| true` and `_step \|\| echo` (by design: an alert failure never kills the chain, siblings' pattern), the CLAIMS path (the file exists at that path), the 148 denominator (D-085 IS 148), the INDEX pipe (renders — the row has none), archived plan paths (outside the surface) |
| CY1 | (CONFIRMED, 1 of 1 live NOW) the chain's liveness evidence was the DASHBOARD file's mtime — an artifact any manual `gen_dashboard.py <path>` run refreshes — so `liveness_audit` read `LIVE … 14.3h old` while the scheduled chain had not run since 2026-09-01 06:01 (+03): syslog carries the 06:00 CRON line for daily_refresh on Aug 30, 31 and Sep 1 and NONE on Sep 2 (cron itself alive, pid 315), `daily_refresh_last_success.txt` = 2026-09-01T03:01:56Z, `update.log` = one "Log rotated" line since Sep 1 11:08; the dashboard was written 2026-09-02 13:50 by a manual run that logged nowhere — the header comment's promise ("a half-dead chain … reads DEAD") held for the chain and not for the heartbeat, which could not tell "the chain ran end-to-end" from "someone ran the last step" | native Opus, pass 34 (executed `liveness_audit.py` + the log/syslog census) | FIXED · LOCAL — the heartbeat is a stamp (`.tmp/external-services/chain-heartbeat`, gitignored) written by the chain script ALONE, inside the gated branch, after `_step gen_dashboard` succeeded; the registry's evidence path points at the stamp and the grader cross-checks it against the script's `HEARTBEAT` line (one writer, `text.count == 1`); the executing test asserts the stamp after a clean run and after a failed paid step, and its absence after a failed scan; header comment + doc rewritten; the mirror named: until the next scheduled run stamps it the surface reads UNKNOWN/DEAD — which is the TRUE verdict for a chain the cron skipped yesterday; sandbox (stamp line deleted): → 2 failed — the gated regex and the executing test (a third failure in that sandbox was its doc-less extract, not the mutation; corrected at pass 35); (registry path reverted): → 1 failed |
| CY2 | graders + one message: (a) `/opt/p` mode 0 — the glob cannot stat `p/.env`, so the LISTING drops the project silently and only `project_dirs`'s `.git` probe refuses (a coincidence nothing pinned; had it swallowed EACCES the CS2 silent drop returned for every unenterable project — 0 of 45 live dirs, `/opt/archived` has no `.env`); (b) the FIFO shape named in the comment and the doc was graded 1 of 4 (directory only); (c) the new `except` comment claimed "(or `/opt` itself)" while an unlistable `/opt` never raises — the glob yields nothing and the "no files" line named the wrong cause | native Opus, pass 34 (executed all three) | FIXED · LOCAL — the unenterable-dir contract is a test (two projects, one mode 0 → exit 1, one line naming the path; root skips); the FIFO joins the directory test; the no-files line says "(or /opt cannot be listed)" and is graded (mode-0 `/opt`); the comment corrected; sandbox (`project_dirs` swallowing `OSError`): → 1 failed; (`S_ISDIR` instead of `S_ISREG`): → 1 failed; (wording reverted): → 1 failed |
| CY3 | texts: the classify comment said "real string" where the same commit's doc says "real category string"; CW1 said "three sites" (the def + two calls); the Pass 33 row carried the finders' bounded counts as bare claims ("68 row ids" — 311; "34 print sites" without its scope; "4 orderings" — pytest-randomly is not installed, not reproducible); CX1 said "21 of 21" over 17 listed reasons (two fold 3 and 2 candidates); the backlog row omitted the ssh-apprise precondition and counted 8 call sites where there are 7 direct + 1 indirect (`mail_escalate.py`); the doc's "skipped, never an error" lacked "at the listing" (a FIFO swapped in during the stat→read window would hang to the step timeout, not exit 1); the Pass 32 row's 7-of-8 gap stays stated as unreconciled | native Opus + native Sonnet, pass 34 | FIXED · LOCAL — all but the last, which the row already discloses |
| CY4 | ACCEPTED with the measurement: the listing's exclusion tuple compares paths, so `OUTPUT` is a dead member (the glob can never yield it) and a `/opt/x/.env` SYMLINKED at the hub's own `.env` or the consolidated secrets file would be followed and folded in as project `x` — 0 of 22 live `.env` are symlinks; a hand-made link into the hub's secrets is not a shape the scan is asked to defend against | native Opus, pass 34 (executed: symlink followed, guards precede the stat) | ACCEPTED (1) |
| CZ1 | pass-34 pool candidates: 3 code (deepseek) / 5 ops (gemini) / 4 docs (qwen) — 4 of the 12 restate CX1/CU2 rows verbatim despite the ledger excerpt now in the brief (`parse_env` raising, the QUOTED `LOG_FILE`, the dashboard after a failed paid step (G9), the sibling-race unlink); Opus's "a permanently dangling `.env` symlink takes the chain down daily — distinguish it by `is_symlink()` and skip" | pool fanout ×3 routed + native Opus, pass 34 | REFUTED (13 of 13): the pool's — the merged-prefix key is unattributed BY DESIGN (BH1), `PLACEHOLDER_SLD` is a reviewed list, rg exit 2 keeps matches and WARNS by path (adjudicated), `lexists`+`unlink` race is caught (`except OSError`), `load_dotenv` on the hub `.env` is the hub-local chain by design, the CLAIMS row parses (the gate's YAML load), the CHANGELOG line is prose not a table, the INDEX row is a tree line not a schema row, D-081's retired script is retired — and the dangling symlink: a `.env` pointing at an unmounted vault IS the CS2 loss if skipped (its keys deleted under the sync's bound), the daily alert names the file, 0 of 22 live — the refusal is the deliberate fail-closed mirror CW1 named, not an inconsistency with the venv-directory rule (a directory is by convention never an env; a symlink is a reference to one) |
| DA1 | (CONFIRMED, 1 of 1 scheduled runs failed — older than the review) the CY1 heartbeat had no working consumer: the installed weekly line is `40 6 * * 1 .venv/bin/python scripts/sysadmin/liveness_audit.py --json >> $HOME/.claude/liveness.log 2>&1` — no `cd /opt/fabrik` (the script's own `PROPOSED_CRON` has it), so cron's `$HOME` cwd resolves `.venv/bin/python` to nothing: `~/.claude/liveness.log` holds ONE report (2026-08-16, a manual run) and ends `/bin/sh: 1: .venv/bin/python: not found` (mtime Aug 31 06:40, the last Monday); 0 of 33 registry surfaces watched the watcher, so a 1-of-1 failure was invisible for three weeks — ledger row O12's "a cron that never fires is caught by the weekly probe within 7 days" rested on a probe that never produced a report | native Opus, pass 35 (executed: the log census + `_heartbeat_one` on the live surface) | FIXED · LOCAL (the in-repo half) + OPERATOR (the crontab line): the audit prints ONE `LOG_STAMP`-shaped stderr line on completion (`SELF_MARKER`; a failed cron line appends only an error, never the marker) and the registry's new `liveness-audit` surface ages it (`log_marker`, 180 h — reads DEAD (absent) today, the true verdict); graders: the chain test pins the surface + marker ↔ `SELF_MARKER` and `PROPOSED_CRON`'s shape, `test_liveness_audit` runs `main` and ages the line with `_stamp_age`; the crontab write is classifier-blocked for agents — the operator replaces line 96 with `PROPOSED_CRON` verbatim (NEXT); sandbox (surface removed): → 1 failed; (stderr line removed): → 1 failed |
| DA2 | (CONFIRMED, 1 of 5 labels) the step alert said "Dashboard not rewritten; liveness DEAD" for EVERY label — false for `classify_services`, the paid step G9 exists for: executed `FAIL_CLASSIFY=1` → exit 1, dashboard written, heartbeat stamped, and the body sent the operator hunting a dead chain that is fresh | native Opus, pass 35 (executed) | FIXED · LOCAL — a per-label `hb` clause ("Dashboard and heartbeat unaffected (paid step)." for classify, the DEAD line otherwise) spliced into the body; the ≤500 renderer substitutes it per label (classify 496 — the +8 clause on the −8 label TIES the max with `gather_envs_reconsolidate`; corrected at pass 36); the executing test asserts "liveness DEAD" on a failed scan and "heartbeat unaffected" on a failed classify; sandbox (override removed): → 2 failed |
| DA3 | (CONFIRMED mechanism, 0 of 1 live) the stamp was `> "$HEARTBEAT"` — the redirect TRUNCATES before `date` runs, so a failed write (ENOSPC, a stub `date`) left a 0-byte stamp with a FRESH mtime that `_heartbeat_one` read LIVE while the script printed "liveness will read DEAD" (CY1's own class); the failed stamp was exit 0 and the only failure in the script with no alert; the `\|\| echo` branch and the gated dashboard-FAILURE branch were proven by regex only (deleting the notice left 122 of 122 green) | native Opus + native Sonnet, pass 35 (executed: the `date` stub → 0 bytes/now → `Verdict.LIVE`; `.tmp` as a file → exit 0, 0 alerts) | FIXED · LOCAL — tmp + `mv -f` (the previous stamp survives a failed write, no tmp left), a failed stamp sets `chain_failed=1` and ALERTS; the executing test gains three scenarios (`FAIL_DASHBOARD` → exit 1, one alert, no stamp; `.tmp` a FILE → exit 1, "heartbeat NOT stamped" alerted, dashboard stands; a failing `date` on PATH → the old stamp's content and mtime untouched, no tmp); the static grader keys on the rename and refuses a direct writer in code (comments excluded); sandbox (direct write restored): → 2 failed; (alert + flag removed): → 1 failed; (dashboard step ungated, stamp unconditional): → 2 failed |
| DA4 | texts: the doc's header line 3 and § Liveness still said "the dashboard file's mtime" (the same commit rewrote § Failure visibility — READ-BEFORE-YOU-EDIT, two sources of truth in one document); `INDEX.md`'s dashboard row said "its mtime is the liveness heartbeat"; `gen_dashboard.py`'s atomic-write comment said the same and its `AFTER-EDIT` header still coupled the registry; CY1's "→ 3 failed" was 2 (the third was the doc-less sandbox); and the denominator every scheduled-behaviour claim rests on: the chain script (created 2026-09-02) has run under a scheduled entry point **0 times** — syslog's last `daily_refresh` CRON line is 2026-09-01 06:00, the Sep 2 06:00–06:14 batch is missing box-wide (every job, ~15 min, cron did not catch up) — so CY1 is correct by construction and validated by nothing yet; the first unattended run is due 2026-09-03 06:00 +03 — **and it ran: cron fired `daily_refresh.sh` at 06:00:01 (the chain itself from ~06:00:06), `gather_envs` 1 s · `classify_services` 20 s · `gather_envs_reconsolidate` 0 s · `registry_sync` 2 s · `gen_dashboard` 0 s (455 services), chain 23 s exit 0, stamp `2026-09-03T03:00:29Z`, the audit reads LIVE (read at pass 38); the kilo pipeline's own `[heartbeat] STALE (48.0h > 36h)` alert fired in the same run — the Sep 2 miss WAS paged by that surface** | native Opus + native Sonnet, pass 35 | FIXED · LOCAL — all five texts; the denominator recorded here and on the Pass 35 row |
| DA5 | ACCEPTED with the measurement: `.tmp/` is gitignored, so a `git clean -fdx` deletes the stamp → a false DEAD until the next run (0 of 1 — no sweeper touches `.tmp/external-services`, 3 files coexist there with 0 deleters (re-counted at pass 36: `ls -1 | wc -l` on 2026-09-03); DEAD is the fail-closed direction); the registry's `cron_match` names only `daily_refresh.sh` while the boot hook is a second producer of the same stamp — by design: the cron IS the schedule, the hook the catch-up, and `kind: cron` reads DEAD/unscheduled the day the line leaves the crontab (0 of 1) | native Opus, pass 35 | ACCEPTED (2) |
| DB1 | pass-35 pool candidates: 1 code (deepseek) / 4 ops (gemini) / 1 docs (qwen) | pool fanout ×3 routed, pass 35 | REFUTED (6 of 6): a value carrying the literal `NEEDS-TRIAGE` (the block header test is `line.startswith("# ═")`, a `KEY=value` line never qualifies), positional credential choice (an `ACCOUNT_NAME` is an identifier — `is_credential` False, adjudicated), the dangling symlink (CZ1, restated), IPv6 brackets (`urlsplit().hostname` strips them by design; dashboard ids derive from provider names, not hosts), a `javascript:` model URL (the named test IS the guard), the CLAIMS `kind` value (pass 1 (h), out of surface) |
| DC1 | (CONFIRMED, 3 of 3 `--json` runs) DA1's self-marker was printed to stderr AFTER a block-buffered stdout write: under the cron's own `>> log 2>&1` a report larger than the stdout buffer went through with its trailing newline still buffered, so the stamp landed GLUED to the JSON's last brace (`}2026-09-03 05:03:51 liveness-audit: …`) — never `LOG_STAMP`-shaped — and the `liveness-audit` surface read UNKNOWN, which `--strict` never fails: the watcher-of-the-watcher was unfalsifiable by construction, the shape DA1 was opened to close; DA1's grader used `capsys` (separate streams, an empty registry) and could not see it | native Opus, pass 36 (executed the installed line's redirect + `marker_age` + `_heartbeat_one`) | FIXED · LOCAL — the report print is `flush=True`; grader: a subprocess run of the real CLI into ONE merged file with an 80-surface registry (>16 KB, the buffer overflowed — asserted), the marker line found once and aged by `_stamp_age`; sandbox (flush removed): → 1 failed |
| DC2 | graders: (a) DA1's `PROPOSED_CRON` grader was vacuous — its regex stopped before the `cd` it exists for (the broken installed form passed it, 1 of 1 mutation survived); (b) the heartbeat alert body (DA3) sat outside the step-alert regex, so neither the ≤500 budget nor the Markdown-parity check reached it (an odd `_` + 560 chars passed, 1 of 1); (c) a DIRECTORY at the stamp path made `mv -f` move the tmp INSIDE it and the audit age the directory's own mtime — LIVE forever, no alert (executed; 0 of 1 live) | native Opus, pass 36 (executed all three) | FIXED · LOCAL — the regex pins `.venv/bin/python`; every `_alert` body in the script is rendered (both, asserted) for length and parity, `$HEARTBEAT` substituted; the rename is `mv -fT` (a directory refuses → alerted, exit 1) with executing scenario 7; sandbox (`cd` removed from `PROPOSED_CRON`): → 1 failed; (an odd `_` + padding in the heartbeat body): → 1 failed; (`-T` dropped): → 2 failed |
| DC3 | texts + coupling: the registry's `external-services-chain` `why` still described the refuted dashboard-mtime model (the audit's own input, a fifth stale copy DA4 missed) and `test_gen_dashboard_write_is_atomic`'s docstring the sixth ("the mtime IS the heartbeat"); the doc's § Failure visibility never named the new stamp-failure alert (two sources of truth again); `docs/workstation/liveness.md` — the `doc:` pointer of the new surface and a coupled file in the audit's AFTER-EDIT header — said nothing about the stderr stamp or the self-surface | native Opus + native Sonnet, pass 36 (repo-wide grep for the claim: 2 survivors of 6) | FIXED · LOCAL — all four; the audit's other coupled file (`scripts/sysadmin/archived/kaizen_metrics.py`) is archived and captures the streams separately — left, WARN accepted |
| DC4 | denominators + tense: "failed on every scheduled run for three weeks / since 2026-08-16" overstated the log — ONE `not found` line (the only scheduled attempt, 2026-08-31; the 2026-08-16 report was manual, and the crontab was wiped and reinstalled on 2026-08-19); "180 h = a weekly run plus one missed Monday" is wrong arithmetic (168 + 12 h of late slack — a missed Monday is 336 and reads DEAD); DA2's "classify 488" is 496 (a tie at the 4-char margin); DA5's "6 files" is 3; the Pass 35 row carried "107 / 346" verbatim (348 today); and every operator-facing text described the crontab defect in the PAST tense while line 96 is still installed broken | native Opus + native Sonnet, pass 36 | FIXED · LOCAL — six texts across the audit comment, both registry `why` fields, the doc, the chain test comment, CHANGELOG Round 35 and three ledger rows; present tense until the operator installs the line (NEXT) |
| DC5 | ACCEPTED with the measurement: `chain-heartbeat.tmp.<pid>` has no sweeper — a SIGKILL between `date` and `mv` (microseconds) leaves one file per crashed pid (0 leftovers of 3 files in the directory; DA5's sub-class); on the `/tmp/fabrik_daily_refresh_YYYYMMDD.log` FALLBACK log path (3 `_`) every alert body's underscore count is odd → Telegram legacy-Markdown 400 unless the apprise leg delivers (0 of 1 — the cache log is writable; the CW4 class, recorded on the backlog row) | native Opus, pass 36 | ACCEPTED (2) |
| DD1 | pass-36 pool candidates: 19 code (deepseek) / 6 ops (gemini) / 0 docs (qwen); Sonnet's two LIVE items | pool fanout ×3 routed + native Sonnet, pass 36 | REFUTED (27 of 27 — deepseek's 19 map to 12 subjects, counts in brackets): cursor-before-dispatch [2] (AC5 by design), a stale cursor [3] (the walk resumes at the first name above it — wraps, never skips), `--max-per-run 0` [1] (not on the daily argv), `apply_error_budget` [1] and `Path.parts` [1] (the finder's own text concludes "not a bug"), `:http` in a file NAME [4] (a path containing `:http` is not a live shape, 0 of 569 hosts), `gen_dashboard`'s tmp after SIGKILL [2] (the next run unlinks it first — no accumulation), `M365_TOKEN_ENDPOINT` [2] (executed: `is_credential` False), `owned_by` [1] / `credential_rank` [1] / `ensure_schema` [1] (the finder's own text concludes "correct"); gemini's 6: `code_only_block_name` (BS3/BW1), the curated `?` keep (CD2/CE3), `qq.com` (two-letter SLDs by design), the outbox flush (kilo pipeline, out of surface), a `date` that "returns empty" (rc 0 with empty output is not a `date` failure mode; rc≠0 is alerted, DA3), a lone `.env` directory → "no files" exit 1 (correct: no env at all); Sonnet's 2: "the stamp does not exist → DEAD 'never written'" (TRUE — no run of the chain script has completed since the stamp was defined; the old inline daily_refresh runs were not the chain) and "crontab line 96 still broken" (known, operator-owned, NEXT) — 19 + 6 + 2 = 27 |
| DE1 | (CONFIRMED grader gap, 0 of 5 today) DC2's parity check measured the TEMPLATE with every variable erased, and only the BODY — while `telegram.py` sends `*{title}*\n{body}` as ONE legacy-Markdown message: today parity holds by coincidence (`$label` once in the title, once in the body — the two underscores cancel), and a plausible copy-edit dropping `$label` from the body made 4 of 5 labels odd (HTTP 400, the alert lost when the apprise leg also fails) with 13 of 13 tests green | native Opus + native Sonnet, pass 37 (executed: 1 of 1 mutation survived) | FIXED · LOCAL — the grader renders both alerts' title AND body per label × production log path and asserts an even `_` count and exactly two `*` on the message Telegram builds (the template-level check kept); sandbox (`$label` dropped from the body): → 1 failed |
| DE2 | (CONFIRMED mechanism, 0 of 1 live) the audit's own `--json` report echoes `SELF_MARKER` verbatim in its `detail` text into the very log the cron appends it to: `marker_age` counted substring hits (2 per run, inflated ~2×) and a run that emitted the report but died before its stamp left ONLY untimestamped hits → UNKNOWN, which `--strict` never fails — DC1's shape by another route (live log: 0 marker lines, 1215 lines); and DC1's `flush=True` moved the stdout write inside `main`, so `--json \| head` raised `BrokenPipeError` out of `main` (traceback, exit 1, NO stamp) under a doc that says "exits 0 by default and never raises" (0 of 2 code consumers — both `capture_output`); two graders were over- or mis-pinned: the `PROPOSED_CRON` regex false-redded a correct `exec` variant and pinned the schedule, and the one-writer count ran on raw text while its sibling stripped comments | native Opus + native Sonnet, pass 37 (executed all four) | FIXED · LOCAL — `marker_age` ages and counts only `LOG_STAMP`-shaped hits (untimestamped-only stays UNKNOWN — the mirror named: a log whose marker lines are ALL unstamped is unchanged); a closed stdout parks on `/dev/null` and the stamp still lands (grader: a 300-surface report into a reader that leaves after 16 bytes → exit 0, no traceback, one stamped line); the cron regex pins `cd /opt/fabrik`, the `&&` joiner and the relative `.venv` path, nothing else (`exec` variant green, `cd` removed red — both executed); the writer count runs on comment-stripped code; sandbox (substring counting restored): → 1 failed; (the pipe guard removed): → 1 failed |
| DE3 | texts + coupling: the doc's § Failure visibility FIRST sentence still said every non-zero exit — "an exhausted pool" included — leaves the dashboard unwritten and the heartbeat unstamped (false for classify, the very clause DA2 fixed in the alert, 1 of 5 labels; the same doc's step table says the opposite); `mv -fT`'s mirror was unnamed (executed: a SYMLINK at the stamp path is REPLACED, never followed — 0 of 1); the chain `why` rewrite had dropped the G9 carve-out and the founding 46-day incident; the liveness doc's new paragraph sat orphaned after the `---` that closes § Running it, in the past tense, without the "stays broken until the operator installs it" clause; `test_liveness_audit.py` (47 tests) had no INDEX row while the audit's AFTER-EDIT header couples INDEX | native Opus + native Sonnet, pass 37 | FIXED · LOCAL — the sentence scoped to DATA steps with the G9 carve-out; the mirror in the script comment; the two facts back in the `why`; the paragraph moved under § Running it in the present tense; the INDEX row |
| DE4 | ledger/CHANGELOG/backlog texts: the audit test's docstring still carried "three weeks / had no `cd`" (7th and 8th instances of the claim DC4 fixed six of); the registry test's bound `<= 336 # one missed Monday` was the wrong arithmetic DC4 declared fixed — and 336 h reads LIVE at `age <= limit`, exactly what the `why` says must read DEAD; the backlog row said every chain alert BODY goes odd on the fallback path (2 of 6 bodies do — every MESSAGE does, the title carries `$label`); the Pass 36 row's "107 / 348" dropped its provenance while every earlier row meant the on-disk 346; DD1 said "27 of 27" over 18 named subjects (deepseek's 19 fold to 12 — now bracketed and summed); CHANGELOG said `mv -T` for `mv -fT` and its counts line omitted `tests/test_liveness_audit.py` (45 → 49) | native Opus + native Sonnet, pass 37 | FIXED · LOCAL — all eight (`< 336` with the DEAD comment) |
| DE5 | ACCEPTED with the measurement: `mv -T` is GNU-only (busybox/BSD lack it) — 0 of 2 entry points run anywhere but this Ubuntu box (`mv (GNU coreutils) 9.4`), no container path invokes the chain | native Opus, pass 37 | ACCEPTED (1) |
| DF1 | pass-37 pool candidates: 7 code (deepseek) / 3 ops (gemini) / 1 docs (qwen) | pool fanout ×3 routed, pass 37 | REFUTED (11 of 11): a wrong-typed cursor file (`_read_json` returns the dict-or-default the caller `.get`s — a hand-corrupted state file, 0 of 1), `bound_flagged` with `max_per_run=0` [2] (not on the daily argv — the 7 deepseek items are 6 subjects, this one twice), a cursor name absent from the queue (the walk resumes at the first name above it), `parse_env` "caught too broadly" (the one-line refusal IS the contract, CS2/CW1), `code_only_block_name` (BS3/BW1), `owned_by` with an empty provenance (executed: True — a key no prefix routed is not a merged-route risk, BH1's rule; an EMPTY catalog never reaches the sync, gather refuses it first, BW11), `json_for_script` and `&` (script content is raw text, never entity-decoded), a "truncated test file" (the pool's own 140 k-char partition cut, not the file — 31 tests collect), `_alert` quoting with a `'` in `FABRIK_ROOT` (both entry points hardcode `/opt/fabrik`, 0 of 2), a hostile `cost` value (the named test IS the guard), the CLAIMS `kind` (pass 1 (h), out of surface) |
| DG1 | (CONFIRMED, 4 of 7 shell shapes + 1 of 5 stdout failure modes) DE2 guarded only the stdout leg and only `BrokenPipeError`: `--json 2>&1 \| head` (stderr the same dead pipe) still exited 120 with the stamp lost, and `> /dev/full` (ENOSPC — the cron's own `>> log` shape on a full disk) raised out of `main` with a traceback and NO stamp, under a doc that says "exits 0 by default and never raises"; DE2's `os.dup2` line was inert (its deletion left 62 of 62 green — the report's buffer is dropped on the first EPIPE, so the exit flush never fails on stdout) and would have rewired the PROCESS's fd 1 for any in-process caller; only `BrokenPipeError` was caught (ENOSPC/EIO are siblings on the same write) | native Opus, pass 38 (executed 7 shapes × 2 builds, 5 failure modes) | FIXED · LOCAL — both prints are guarded for any `OSError`: the report is undeliverable, the stamp is still owed; a dead stderr parks fd 2 on `/dev/null` so the exit-time flush cannot fail (measured: `\| head` 0 + stamp, `2>&1 \| head` 0, `/dev/full` 0 + stamp); the inert `dup2` on stdout is gone; graders: a merged dead pipe → exit 0, a full disk → exit 0 with one stamped line on stderr; sandbox (the stderr guard removed): → 1 failed; (the report guard narrowed back to `BrokenPipeError`): → 1 failed |
| DG2 | graders: `stamped[-1]` (the NEWEST dated line — the docstring's contract) was unguarded (`[0]` left 62 green: `reboot-sweep` would age the oldest of 49 hits against 720 h and read DEAD); the doc's fenced "install this verbatim" cron line and `PROPOSED_CRON` were two unbound sources of truth for the one line DA1 is still open on (a drifted schedule left 62 green); the closed-stdout docstring mis-stated the mechanism ("far larger than the pipe buffer" — a 19 KB report forces the EPIPE too; the determinant is a write that is not one syscall, `F_GETPIPE_SZ` = 8192 here) | native Opus + native Sonnet, pass 38 (executed) | FIXED · LOCAL — the echo test asserts the age is the 2026-09-02 line's; the registry test binds the doc's fenced line to the constant; the docstring says what forces the EPIPE; sandbox (`[0]`): → 1 failed; (the doc's schedule drifted): → 1 failed |
| DG3 | texts: DE3's scoped clause dropped the fourth case — a failed `gen_dashboard` step also leaves the heartbeat unstamped (the stamp sits behind it, the script header says so, the doc did not); DF1 said "11 of 11" with one item "twice restated" in prose (the bracket form DE4 adopted one row above); INDEX's row said "`\| head` never raises" while the merged-stream form still raised | native Opus, pass 38 | FIXED · LOCAL — all three |
| DH1 | pass-38 pool candidates: 1 code (deepseek) / 5 ops (gemini) / 0 docs (qwen) | pool fanout ×3 routed, pass 38 | REFUTED (6 of 6): a "connection leak" in `sync_registry` (`conn.close()` at three sites, read), symlink loops under `rg` (ripgrep does not follow symlinks by default), a `mkdir -p` race (`-p` is idempotent under concurrency by contract), a hand-echoed marker in the operator's own log (single-operator threat model; an unstamped echo is not evidence since DE2), internal TLDs in the host regex (a host without a public suffix is not an external vendor — by design), the DB suite's teardown helper (out of the DB-free scope; the suite is not run here) |
| DI1 | (CONFIRMED, 1 of 1 executed shapes; 0 of 1 real ENOSPC events) DG1 turned the audit's own production failure shape — the cron's `>> log 2>&1` on a FULL DISK — from a loud exit 120 into a silent exit 0 with zero output (both prints swallowed), a mirror DG1 did not name; and the handler's own `os.open`/`os.dup2` were unguarded (EMFILE/EBADF would raise the traceback the doc forbids) | native Opus, pass 39 (executed both builds) | FIXED · LOCAL — a reader that LEAVES (`BrokenPipeError`) is its choice (exit 0); a box that cannot STORE the report (any other `OSError`) sets `undeliverable` and `main` returns 1 — the one non-zero exit outside `--strict`, never a raise (correct exit semantics, currently UNCONSUMED: the installed line has no `\|\|`, there is no MTA on the box, and both shell-out callers ignore the return code — measured at pass 40), the stamp still landing wherever stderr works; the handler is guarded; doc + INDEX say so; graders: `/dev/full` → exit 1 + one stamped stderr line, both streams on `/dev/full` → exit 1, the merged dead pipe → exit 0; sandbox (the flag never set): → 1 failed |
| DI2 | (CONFIRMED, 2 of 8 shell shapes) `2>&-` leaves `sys.stderr` None: `print(file=None)` falls back to STDOUT, so the stamp was appended to the JSON report (`json.load` → Extra data) and the dead-stderr guard itself raised `AttributeError` on `None.fileno()` — the guard added to close DG1's class was unguarded against the one stream state it exists for (0 of 2 installed cron shapes) | native Opus, pass 39 (executed) | FIXED · LOCAL — the stamp is skipped when stderr is None; grader: fd 2 closed in the child → exit 0, the report parses, no marker in it; sandbox (the None guard removed): → 1 failed |
| DI3 | (the chain's FIRST production output, 1 of 1 runs) ALL 10 of the 06:00 run's paid units were code-only hosts with no credential (corrected at pass 40 — the row first said 6: that is the number that came back `unidentified` — `asyaport`, `assanport`, `ardot`, `archive`, `asphaltinstitute`, `asphaltpavement`, trade-intelligence's scrape targets — while the other 4 became permanent `status: active` entries from a zero-search guess, see DK1); of the 4 identified, `argusmedia` (a commodities price-reporting agency) was filed `media-stock` on the word "media" — `status: active`, out of triage, and `--only` cannot re-dispatch a curated name, so wrong forever without a hand edit; no cost figure is printed anywhere | native Opus, pass 39 (executed against the live catalog + secrets names) | FIXED · LOCAL (the misfile — `argusmedia` curated to `research-data` by hand, one line in the classifier's own format) + a cost line (the run's pool spend printed and stored in `classify_last.json`, graded — the LOG half of "no log, alert or dashboard"; the alert and the dashboard still carry no cost figure, and the curated `argusmedia` row reaches `secrets/all-envs.env` and the dashboard only at the next chain run's reconsolidate) + ACCEPTED (the queue composition, with the measurement): code-only hosts ARE the input the code scan exists for (a vendor called only from code has no key to find), a scrape target costs one unit ONCE and is tombstoned (never re-billed — 6 of 6 tombstoned), 338 queued at 10/run with the cap at $0.20/unit is ≤ $68 worst-case and the unit MEASURED from the flywheel ledger's 10 rows of that run is $0.000088 (sum $0.00088 — the row first said "~$0.02", an assertion; corrected at pass 40, and the figure will rise now that the units actually search, DK1); a confidence gate between one model answer and a curated file is recorded on the backlog |
| DI4 | graders: the doc↔`PROPOSED_CRON` binding compared bytes (trailing whitespace from an editor false-redded it) and took the FIRST ```cron fence (a decoy fence above the proposed one — e.g. quoting the broken line for contrast — would bind the wrong line, 0 of 1 today) | native Sonnet + native Opus, pass 39 (executed) | FIXED · LOCAL — both sides stripped; the fence is the one under `### Proposed cron`; sandbox (trailing spaces): → green; (a decoy fence above): → green; (the schedule drifted): → 1 failed |
| DI5 | texts: INDEX said "49 tests" for the audit suite (50 collected — one parametrized test; 51 after this round); the Pass 38 row's `107 / 348` were the pre-run figures (117 / 338 on disk after the 06:00 reconsolidate), its file tally omitted the swept-in catalog hunk, and "06:00:01" is when `daily_refresh.sh` fired (the chain ~06:00:06); CHANGELOG said "the inert `dup2` is gone" without naming the fd (the stderr one is load-bearing — its removal exits 120); the liveness doc's exit-contract paragraph never said the report can be undeliverable; the audit's AFTER-EDIT header coupled `docs/workstation/liveness.md`, which pass 38 did not touch (the substantive miss); the pass-38 finder's "491 row ids" vs pass 39's 495 — two regexes, both 0 duplicates | native Opus + native Sonnet + pool (deepseek, the INDEX count), pass 39 | FIXED · LOCAL — all of them |
| DI6 | ACCEPTED with the measurement: fd 2 parked on `/dev/null` for an in-process caller with a dead stderr + one leaked fd (0 of 2 live callers — both shell out); `stamped[-1]` is file order, "newest" only for an append-ordered log (0 of 2 surfaces out of order); `"unidentified"` is not in `CATEGORIES` — the tombstone vocabulary is a second predicate (12 of 120 entries, by design: `real_category` blesses it and keeps it out of the paid queue); the `/dev/full` leg errors instead of skipping where `/dev/full` exists but is not writable (now guarded by `os.access`, 0 of 1) | native Opus, pass 39 | ACCEPTED (4) |
| DJ1 | pass-39 pool candidates: 10 code (qwen) / 4 ops (gemini) / 1 docs (deepseek — the INDEX count, counted under DI5); Opus's "add `scripts/service_catalog.json` to the pipeline's auto-commit `PATHS`" | pool fanout ×3 routed + native Opus, pass 39 | REFUTED (15 of 15): the catalog auto-commit — the operator REMOVED it from `PATHS` this run (D-081: curated data is committed by hand; the daily dirty file is the accepted cost, and a review commit sweeping it in — f3aaeac4 — was the commit script's stale path list, now corrected); enum strip (BB4/BE5), IPv6 (DB1), cursor-before-dispatch (AC5), an empty tombstone name (names come from the parsed block), the tmp path (under `secrets/`, `O_EXCL`), whitespace keys (`load_catalog` refuses them, BW5), `flock` on crash (released with the process), `href` scheme (adjudicated), vendor-specific ranks (design), "path traversal" in `project_dirs` (names from the `/opt` listing, no input), a manipulated `FABRIK_ROOT` (hardcoded in both entry points), partial state on a failed `date` (DA3: tmp removed, alerted), the fallback-path Markdown and the SIGKILL tmp (DC5, restated) |
| DK1 | (CONFIRMED, 10 of 10 paid units — the root cause, older than the review) the classifier's `web_tools=frozenset({"exa", "brave"})` names two PROVIDERS; the pool loop advertises only TOOL names (`WEB_TOOL_NAMES` = `web_search`, `web_search_brave`, `web_scrape`, `web_crawl`, `docs_lookup`) and pops the tool list when none match — so every "research" unit ran with NO web tools: the flywheel ledger's 10 rows of the 06:00 run are `turns=1`, `tool_calls={}`, while the prompt demands "Search the web to confirm the vendor … do NOT guess" — a contract the dispatch made unsatisfiable; 4 units returned a confident vendor + `status: active` having searched nothing (the `argusmedia` misfile among them); the corpus's own gate for this class (`check_command_corpus.py`, founded on `web_tools=["exa","brave"]`) scans only `commands/` markdown and only `[...]` literals — the box's one production caller was outside it on both axes, and `commands/assemble_commands.py:361` says "tool names, NOT provider names" in a comment | native Opus, pass 40 (executed: the advertised list `[]`, the ledger rows) | FIXED · LOCAL — `web_search` + `web_search_brave` (the two engines the provider names meant); grader: every `web_tools=` literal in `scripts/**` and `libs/**` names members of `WEB_TOOL_NAMES`, ≥1 literal swept (a regex at pass 40 — blind to a bare `{…}` set and a nested call, found at pass 42; an AST walk over tracked files since DO2); the mirror named: units now search — turns and cost per unit rise (re-measure after the next run), and, found at pass 41 (DM1): a unit can now be CAPPED (its answer must be kept, not struck), multi-turn answers can carry a second brace (the parser must be balanced), fleet env-var NAMES and hosts now reach Exa and Brave through model-authored queries (names only — the same names already reach OpenRouter), and a unit's 1800 s wall clock exceeded the step's 900 s budget (the classify budget is 1800 s); sandbox (provider names restored): → 1 failed |
| DK2 | (CONFIRMED mechanism, 0 of 2 installed cron shapes / 0 of 10 units) the mirror DI2 left: a CLOSED stdout (`>&-`) makes `print()` a silent no-op — no `OSError`, `undeliverable` False, exit 0 and a stamp asserting "report generated" for a report that went nowhere (vacuous evidence, the module's founding class); and the new cost line: an unguarded `float()` on a malformed `cost_usd` raised out of `main` AFTER the paid dispatch and before the merge (batch lost, cursor already moved — the BK4/BH2 class re-opened), the library's unknown cost (`None`) was reported as $0.00, and a NaN wrote a bare `NaN` into `classify_last.json` | native Opus, pass 40 (executed all four) | FIXED · LOCAL — `undeliverable = sys.stdout is None` (exit 1, the stamp still lands; grader: fd 1 closed in the child); only finite numbers are summed and unknowns are counted (`cost_unknown_units`, printed as "N unknown"; grader: a string, a `None` and a NaN in one batch → the batch lands, $0.0000 with 3 unknown, the JSON parses); sandbox (the stdout guard removed): → 1 failed; (a bare `float()` restored): → 1 failed |
| DK3 | texts: DI3 said "6 of 10 units were scrape-target domains" — all 10 were code-only hosts (6 tombstoned, 4 became permanent entries from zero-search guesses); DI3's "~$0.02 measured" was asserted — the flywheel ledger's 10 rows of that run sum to $0.00088, $0.000088/unit (and will rise now that units search); DI3's "no log, alert or dashboard" goal is met for the log only, and the curated `argusmedia` row reaches the secrets file and the dashboard only at the next reconsolidate; DI1's exit 1 is currently consumed by nothing (no `\|\|` on the installed line, no MTA on the box, both shell-out callers ignore the return code); the Pass rows named the surface reviewed but never the SHAs produced and the header's `Committed:` was 34 rounds stale; DI5 said "all seven" over a list that parses to 6 or 8; the row-id count had three answers (491/495/501 — three regexes); `classify_services.py`'s AFTER-EDIT header lacked its grader file and the doc; the liveness doc and INDEX did not name `>&-`/`2>&-` | native Opus + native Sonnet, pass 40 | FIXED · LOCAL — all of them — eleven texts counted as: [1] 6-of-10, [1] the unit cost, [2] log-only + the derived surfaces, [1] the unconsumed exit, [2] the SHAs + the header, [1] DI5's seven, [1] the row-id bound, [1] the coupling header, [1] the liveness doc + INDEX `>&-` (bracketed at pass 41) — the header now names the produced SHAs from pass 39 on; the row-id count is stated with its regex |
| DK4 | ACCEPTED with the measurement: a dry run (no `--apply`) PAYS, prints the cost and persists nothing — the dry-run contract, 0 of 1 scheduled runs; `preexec_fn` is thread-unsafe by contract — 1 thread measured under pytest, unsafe only under xdist/threaded plugins; a decoy ```cron fence placed BETWEEN the heading and the real one would bind the decoy (0 of 1 fences); `--strict` failures and an undeliverable report share exit 1 (0 `--strict` consumers on the box) | native Opus, pass 40 | ACCEPTED (4) |
| DL1 | pass-40 pool candidates: 15 code (qwen) / 4 ops (gemini) / 0 docs (deepseek) | pool fanout ×3 routed, pass 40 | REFUTED (19 of 19): the cursor wrap (adjudicated), the error budget across slices (design), `code_only_provider` with no `names` (False is right — no key lines means not code-only), `host:port` bracketing (`urlsplit().hostname` carries no port), a `README` vs `_README` collision (non-dict values are refused), `_read_json` typing / a `.git` FILE in a worktree / IDN TLDs / whitespace categories / empty tokens / `json_for_script` quoting / the lock on SIGKILL — the finder's own text concludes "correct" on seven of them, `:http` in a path (0 of 569 hosts), `owned_by` with no hits (DF1), the pid-reuse sweep (`O_EXCL` + the hour gate), an empty heartbeat dir on SIGKILL (harmless), the quoted `"$2"` body, the stamp at completion time (by design), and a "missing `rs` import" that the finder's own text shows at line 36 |
| DM1 | (CONFIRMED, reachable from the next run — the mirror of DK1) with web tools advertised a unit can hit `max_turns`: the library's finalize call returns a real answer but stamps its hint on `error`, and `build_proposals` keyed on `error is not None` — the answer discarded AND an error-budget strike, three laps → a permanent `unidentified` tombstone for a real vendor (0 of 20 pre-fix units, which never tool-called; the population is 10 units/day from now); `extract_json` was a greedy `{.*}` from the first `{` to the LAST `}` — a search-grounded answer with any second brace (a cited `{"prices": [...]}`, a `{region}` placeholder) parsed to None and was tombstoned, never retried (4 of 4 post-search shapes — the row first said 3, corrected at pass 42; 0 of 20 pre-fix); and the classify step's 900 s `STEP_TIMEOUT` was shorter than one unit's 1800 s wall clock — a `timeout` kill after the cursor moved loses the slice and the money (AC5 by design) | native Opus, pass 41 (executed offline with an injected run function: capped → struck → exhausted in 3 laps; the four parse shapes; the budget arithmetic) | FIXED · LOCAL — a capped unit with text is not an error (a true transport failure still is, C4); the parser is a balanced `raw_decode` scan for the first object carrying `category`; `CLASSIFY_TIMEOUT` (default 1800 s) budgets the paid step alone, graded in the chain test; graders for both parse shapes and the capped/errored split; sandbox (capped treated as error): → 1 failed; (greedy regex restored): → 1 failed; (the budget removed): → 1 failed |
| DM2 | (CONFIRMED mechanism, 0 of 1 live) an undeliverable report (DI1/DK2) still stamped the literal "report generated" — the registry's marker would read the surface LIVE for 180 h on a report that went nowhere, and the only differentiator (exit 1) has no consumer; the sweep grader's capture stopped at the first `}` (`frozenset({"a"} | {"exa"})` hid its second half) and an EMPTY classifier literal escaped the per-literal check; the corpus gate's `_WEB_TOOLS_RE` was list-only and never saw a set/frozenset form; `if not undeliverable:` around the print was dead code (`print()` already no-ops on a None stdout — its removal left 52 of 52 green) | native Opus + native Sonnet, pass 41 (executed) | FIXED · LOCAL — an undeliverable run stamps `liveness-audit: report UNDELIVERED` (graded: the full-disk and closed-stdout runs carry it and not the live marker; `marker_age` counts 0 hits for it); the sweep captures to the end of a LITERAL argument and pins that the classifier's literal names `web_search`; the gate's regex accepts list/set/frozenset with a set-form canary in its self-test (fleet-synced surface — correct for every project) — and the self-test's own canaries were VACUOUS: every probe body lacked `{{include:run-record}}`, so the run-record predicate failed each probe and masked the one under test (a list-only regex passed the set-form canary; found by this proof) — each case now opens a run record, so only its predicate can fire; the dead branch removed; sandbox (marker swap reverted): → 2 failed; (the gate regex back to list-only): `--selftest` reports `VACUOUS: the web-tool name (set form) predicate did not fire` |
| DM3 | texts + coupling: `classify_services.py`'s AFTER-EDIT header coupled the hand-curated catalog (D-081) and not the chain test that grades its `web_tools` literal; `liveness_audit.py`'s coupled an `archived/` script — 6 of 6 header WARNs across the last two commits were non-actionable (the FIX DIRECTIVE §5 wallpaper class); INDEX said "51 tests" for a 52-test file — this row CLAIMED the fix at pass 41 and INDEX was not in that commit (corrected for real at pass 42, the fourth recurrence); DK3's "eleven texts" admitted three parses (bracketed); CHANGELOG's "repo-wide grader" — the sentence names `scripts/` + `libs/` (the tracked population is 299 `.py` at HEAD, 254 swept; the row first said 253 of 298); the registry doc did not name the new egress (names and hosts to Exa/Brave); the liveness doc's "the one non-zero exit" reads as one cause (two: a full disk, a closed stdout) | native Opus + native Sonnet, pass 41 | FIXED · LOCAL — all of them |
| DM4 | ACCEPTED with the note: the cost tests pin the exact `.4f` print (a cosmetic reformat reds them — brittle in the safe direction) | native Sonnet, pass 41 | ACCEPTED (1) |
| DN1 | pass-41 pool candidates: 5 code (qwen) / 4 ops (gemini) / 0 docs (deepseek) | pool fanout ×3 routed, pass 41 | REFUTED (9 of 9): the cursor before the dispatch (AC5 by design), the last-record write when the catalog is unreadable post-dispatch (the probe path still writes it), non-roundtrippable code-only names (BS3/BW1), provenance UNKNOWN → every key unattributed (BH1 — the fail-closed direction, by design), `gen_dashboard`'s tmp on `BaseException` (the `finally` removes it), two instances racing the rename (the daily lock; last-wins benign, DC5), the quoted `"$2"` body (DD1), a failed dashboard leaving the stamp unwritten (the point of the gate, DG3), "missing imports" in the DB suite (the pool's own partition cut) |
| DO1 | (CONFIRMED, 0 of 20 pre-fix units; population 10/day from 2026-09-04) DM1's cap exemption keyed on `bool(text)`: a cap whose finalize is PROSE (the loop's finalize directive is prose-shaped and restates no JSON contract — the LIKELY cap outcome) parsed to None, took no strike, and fell straight into `--tombstone-unresolved` — a first-lap PERMANENT tombstone where pre-fix it needed 3 laps; the balanced scan returned the FIRST object carrying `category`, so an echoed format template (`<one of: …>`) before the answer won and the enum guard tombstoned the vendor with the right answer sitting below; and `CLASSIFY_TIMEOUT` was set EQUAL to one unit's wall clock (1800) with 30 s of outer grace on top and `recover_caps=True` re-dispatching every zero-output cap SEQUENTIALLY with its own 1800 s — the kill DM1 meant to prevent stayed reachable; the units run ALL 10 in parallel (the pool caps concurrency at the unit count), not "4 at a time" | native Opus, pass 42 (executed through the real `build_proposals` and an 11-shape probe; the library read at `agent.py:209`, `:2111`, `:2126`) | FIXED · LOCAL — a cap is exempt only when its answer PARSED (prose/empty caps are retries, graded); the scan prefers the first object whose category is a real enum member and falls back to the first (graded: echo before the answer, two enum objects, a lone template); `recover_caps=False` (pinned) and `CLASSIFY_TIMEOUT` 2100 with the grader asserting budget > the library's `wall_clock_s` + 30 and, by EXECUTION, a fake `timeout` on PATH logging the budget each step ran under (classify 4321 from the env, the rest 30); the comment says all 10 in parallel; sandbox (any-text exemption): → 1 failed; (first object wins): → 1 failed; (`recover_caps` pin dropped): → 1 failed; (default 1800): → 1 failed; (`_step` ignoring the budget): → 1 failed |
| DO2 | (CONFIRMED graders — the fleet-synced gate and the sweep) the corpus gate's `--selftest` asserted "SOME problem fired", never the NAMED predicate: the `agent definition` placeholder body in the loop was graded by the run-record predicate (1 of 9 canaries vacuous at HEAD), and a dead predicate whose canary lost its include stayed green; `_WEB_TOOLS_RE` still stopped at the first `}`/`)` (`{"a"} | {"exa"}` hid `exa`; `sorted({…})` no match) — DM2 fixed the test's copy, not the synced gate's; the sweep regex was blind to a bare `{…}` set and a nested call, false-positived on a single-line `f(web_tools=[…], system="research")` (harvesting `research`), and swept an untracked vendored venv (`scripts/kilo-benchmarks/.lcb-venv`, 1712 of the 2013 files it opened); the budget assertion was a text pin not scoped to `_step` (a second `CLASSIFY_TIMEOUT=900` line passed) | native Opus + native Sonnet, pass 42 (executed) | FIXED · LOCAL — each canary is (body, signature) and the loop asserts the signature's message (the agent-definition fixture likewise, out of the loop; a union-form canary added); the gate's regex captures to the argument's end (a closer followed by `)`, `,` or end of line); the sweep is an AST walk over `git ls-files scripts libs` (dot-dirs excluded off-git; archives excluded since pass 43) resolving list/set/tuple/`frozenset(…)`/`sorted(…)`/unions and skipping non-literals, with its own shape test; the budget assertion is scoped inside `_step()` and proven by execution; sandbox (the chain predicate DEAD + its canary's include dropped): the self-test reports VACUOUS for `chain target` — the pass-41 shape stayed green under the same compound; (the gate regex reverted): VACUOUS for the union form; (the literal reverted): → 1 failed |
| DO3 | texts: INDEX still said "51 tests" (DM3 claimed the fix; INDEX was not in pass 41's commits — the fourth recurrence) and DM3's "253 of 298 — the sentence says so now" was also unapplied (the CHANGELOG said "253 files"); the doc's alert legend said "124 = the 900 s timeout" after the classify budget changed; "4 at a time" in the script comment and the test; DK1's "list/set/frozenset forms" was false for a bare set; DM1 said "3 of 4 shapes" (4 of 4); the Pass 41 row conflated the gate's two runs ("self-test green over 55 files") and carried "117 / 338" without its subject (117 catalogued `#svc` blocks = 106 real + 11 `unidentified`, of 455 in the consolidated file); Pass 40 and 41 rows omitted their produced SHAs despite the header's promise; CHANGELOG Round 41 overstated "no longer vacuous" and "captures whole literals"; the "253 of 298" count had three right answers (299 tracked at HEAD, 280 swept by the shipped sweep — the row first said 254, the pre-DO2 exclusion set, corrected at pass 43 — 2013 opened in place by the old walk) | native Opus + native Sonnet + pool (deepseek, the INDEX count), pass 42 | FIXED · LOCAL — all of them (deepseek's "three `api_keys.kind` values" sentence does not exist in the CHANGELOG — refuted, DP1) |
| DO4 | ACCEPTED with the measurement: `daily_refresh.sh`'s own `_step` applies no timeout and neither entry point wraps the chain, so the classify step can now hold the daily pipeline for 35 min worst case instead of 15 (0 of 1 runs — 23 s today; the tradeoff is deliberate: a kill loses the slice and the money); the archived `kaizen_metrics.py` dropped from the audit's coupling header (it references the audit 3 times; FIX DIRECTIVE §5 — a coupling that can never be actioned is wallpaper) | native Opus, pass 42 | ACCEPTED (2) |
| DP1 | pass-42 pool candidates: 8 code (qwen) / 3 ops (gemini) / 2 docs (deepseek — one real, the INDEX count, counted under DO3) | pool fanout ×3 routed, pass 42 | REFUTED (12 of 12): the cursor "infinite loop" (a bounded walk, adjudicated), a negative `--max-per-run` (hand-typed argv; the daily line is 10), the lock file race (`flock`), `tombstone_of` on a list (CU3), the pid in the tmp name (DC5), adoption of internal keys (only a catalog entry with that name adopts, 0 of 119), `--help` writing a file (fixed at round 1, the finder describes the pre-fix), the prune default (adjudicated), the quoted `"$2"` (DD1), the rename race (DC5), the alert-then-exit-code (the flag is set BEFORE the alert), and "three `api_keys.kind` values in the CHANGELOG" (no such sentence — the file lists four) |
| DQ1 | (CONFIRMED, 0 of 46 units; population 10/day from 2026-09-04) DO1's enum preference let a LATER object override the model's own answer: an answer `{"name": "argusmedia", "category": "llm"}` followed by a cited `{"name": "exa", "category": "search"}` was filed under `exa` — executed through the real `main()`: `merged into existing entries: argusmedia → exa`, `exa.merged_match: ["ARGUSMEDIA"]` — and the prompt's own sanctioned refusal `{"category": "?"}` followed by a hedge picked the hedge, inverting the "do NOT guess" guardrail (pre-DO1 the first object won, safely); and DO1's cap rule keyed on "an object parsed": a capped unit whose only object was the ECHOED TEMPLATE parsed, took no strike and was tombstoned on the first lap (a behaviour a pass-42 assertion pinned) | native Opus, pass 43 (executed: the merge path, the refusal, the template cap) | FIXED · LOCAL — the scan returns the model's FIRST genuine answer (a `?` refusal or a near-miss included) and skips only an echoed format template (`<one of: …>`, `a|b`); a cap is exempt only when it ANSWERED (a parsed non-template object); graders: refusal-then-hedge → `?`, answer-then-cited-object → the answer, near-miss-then-enum → the near-miss, a template-only cap → a retry; sandbox (enum preference restored): → 1 failed; (a template counted as an answer): → 1 failed |
| DQ2 | graders + the synced gate: the fake-`timeout` grader keyed `by_step` on the LAST argv word — both gather runs end in `--apply`, so a wrong budget on the first collapsed into the second's (executed: 1 of 1 mutation survived); the gate's lookahead `(?=\s*(?:\)|,|$))` made an inline-code span (`` `…web_tools=["exa"]` ``) and a prose tail invisible (regressions vs the pass-41 form; 0 of 5 live occurrences), its `(?:frozenset\(|set\()?` did not admit `sorted(…)` while its own comment claimed so, `_NAME_RE`'s zero-or-more quotes harvested `if`/`else`/`sorted` as tool names on the widened capture, and `re.M` was inert on a per-line scan; the AST sweep skipped an EMPTY literal and pinned the classifier by POSITION (`[0]`), had dropped the `.archive`/`archived` exclusion (26 tracked archive files newly swept), and decoded `git ls-files` outside its `except` | native Opus, pass 43 (executed: 9 shapes × old/new, 5 of 5 live occurrences both — 6 with the orchestrator doc `docs/orchestrator/mega-epic-breakdown/00-trigger-mega-epic-fabrik.md:399`, which `audit()` appends; re-counted at pass 44) | FIXED · LOCAL — the budget grader keys each step on its SCRIPT and asserts both gather runs (`["30", "30"]`) and classify alone; the gate's regex admits nested calls (`frozenset|set|sorted|list|tuple`) and a backtick/EOL tail, `_NAME_RE` requires quotes on both sides (a ternary line in the GOOD fixture pins the false-positive side), `sorted` and inline-code canaries, `re.M` dropped and the per-line scope stated; the sweep excludes archives, decodes with `surrogateescape`, and pins the classifier by its `fanout(` call; sandbox (first gather given the classify budget): → 1 failed; (the backtick tail removed): the inline-code canary VACUOUS; (`sorted` not admitted): the sorted canary VACUOUS; (quotes optional again): the good fixture FALSE-POSITIVES; (a stale literal under a tracked `.archive` file): green |
| DQ3 | texts: CHANGELOG Round 40 still said "(253 files)" — claimed fixed by DM3 and again by DO3, applied neither time (fifth recurrence of the class; 280 of 299 tracked at HEAD now); DO3 said "254 swept" (the pre-DO2 exclusion set — the shipped sweep opened 280); the Pass 42 row credited DP1 with 11 refuted while DP1 enumerates 12 (29 fixed / 12 refuted now); the sweep test's docstring and rows DK1/DO2 said the gate checks "only `commands/` markdown" — it appends the assembler and the orchestrator docs, where 4 of its 6 occurrences live; the test's budget comment still said "4 at a time"; and two INDEX rows a sibling's quota commit (b8a09612) omitted — `tests/test_quota_stop_hook.py` (15) and `tests/test_quota_dashboard.py` (34): a defect in COMMITTED code is the repo's | native Opus + native Sonnet + pool (deepseek, the INDEX rows), pass 43 | FIXED · LOCAL — all of them |
| DQ4 | ACCEPTED with the note: the agent-definition fixture's signature assertion is indistinguishable by mutation from the "anything fired" form it replaced (the fixture's `src` is clean, so only that predicate can fire) — belt-and-braces, ungraded on purpose | native Opus, pass 43 | ACCEPTED (1) |
| DR1 | pass-43 pool candidates: 0 code (qwen) / 3 ops (gemini) / 4 docs (deepseek — two real, the INDEX rows, counted under DQ3) | pool fanout ×3 routed, pass 43 | REFUTED (5 of 5): a "truncated test file" (the pool's own partition cut), the quoted `"$2"` (DD1), `mkdir -p` (idempotent), the CLAIMS `kind` (pass 1 (h)), D-085's line reference (the finder lacked the file; `EXTERNAL_SYSTEMS.md:4` is the header line it names) |
| DS1 | (CONFIRMED mechanism, 0 of 46 units) DQ1's `_TEMPLATE_RE` carried a bare `\|` arm — a pipe ANYWHERE in the category — so a hedged answer (`research-data\|media-stock`) was classed as a template, skipped, and a later cited object won (the DQ1 class through another door; the prompt's `\|`-joined placeholder appears only in the COST field, never inspected) and the arm was ungraded (deleting it left the suite green); a COMPLETED template-only answer was tombstoned on the first lap while a CAPPED one retried (asymmetric, undocumented); `_live_web_tool_names` inserted the repo on `sys.path` on every call (15 copies after one `--selftest`) | native Opus + native Sonnet, pass 44 (executed) | FIXED · LOCAL — the template mark is the angle-bracket placeholder alone (a hedge is the answer: it fails the enum, it is never overridden); a template-only answer strikes the error budget capped or completed; the insert runs once; graders: hedge-then-cited → the hedge, a completed template → errored; sandbox (the `\|` arm restored): → 1 failed; (a completed template no longer striking): → 1 failed |
| DS2 | (CONFIRMED — the fleet-synced gate) the gate's `--selftest` was invoked by NOTHING on the box (0 of 4 executable surfaces: pre-commit, `final_gate.py`, its test file, the liveness canaries) — three regex mutations its canaries catch left every suite green; its success line counted the 55 collected files while the predicates opened 93 (orchestrator docs, wrappers, agent defs), a hazard its own comment named; DQ2's positive lookahead made a bare prose tail (`web_tools=["exa"] for facts`, 4 of 4 un-delimited shapes) invisible while the comment claimed it, and a rejected first closer let the lazy capture run to a later closer and harvest a following argument (`system="exa"` → a false BLOCKING failure, 4 of 7 shapes); UNQUOTED names became invisible (the mirror of `_NAME_RE`'s `+`, unnamed — 0 of 6 live occurrences are unquoted); `re.M` was inert on a per-line scan; the summary said "9 predicates" where the module's docstring says eight | native Opus + native Sonnet, pass 44 (executed: 3 mutations through the whole suite; the capture over 9 + 7 shapes; the file count instrumented) | FIXED · LOCAL — `--selftest` runs as a subprocess in the gate's own test file; the success line counts `AUDITED` (every file a predicate opened — 93 read today), graded by an exact-set test; the capture stops at the first closer NOT followed by an operator or ternary keyword and is cut at the first keyword-argument boundary (a prose-tail canary; `system="exa"` after a ternary in the GOOD fixture); the unquoted mirror is named in the comment; `re.M` gone, the per-line scope stated; "the eight predicates"; the coupled reference doc's console block and "Five" header brought to eight; sandbox (the sorted canary made vacuous): the subprocess test → 1 failed; (the positive lookahead restored): the prose-tail canary VACUOUS; (the keyword cut removed): the good fixture FALSE-POSITIVES on `exa`; (every `AUDITED.add` removed): → 1 failed |
| DS3 | graders: `_fanout_web_tools`'s "pin by the `fanout(` call" was indistinguishable from the positional `[0]` it replaced (1 `fanout(`, 1 literal in the file); the fake `timeout` keyed rows on `$5`, which is the script only because all 5 `_step` sites happen to be `"$VENV_PY" script …` | native Opus, pass 44 (executed: 1 of 1 mutation survived; 5 of 5 sites conform) | FIXED · LOCAL — a decoy-literal test (a `dict(web_tools=["exa"])` before the call must not be the one pinned); the fake keys on the FIRST `.py` argv word; sandbox (the pin back to `[0]`): → 1 failed |
| DS4 | texts: the CHANGELOG's "280 of 299 swept" and DQ3's "280" were computed BEFORE DQ2 restored the archive exclusion in the same round — the shipped sweep opens 254 (299 − 26 archive − 18 tests − 1); DQ2/DQ3's "5 of 5 live occurrences" — 6 with the orchestrator doc `audit()` appends (4 of 6 outside `commands/` markdown); the Pass 43 row's "max 484" — 496 (the `registry_sync`/`gen_dashboard` length was quoted); CHANGELOG Round 43 overstated "inline-code/prose tails" and "skips only a template"; the coupled `docs/reference/command-corpus-check.md` still showed "all 5 predicates" and "Five" (the gate's AFTER-EDIT header names it — 0 of 3 coupled files were staged at pass 43); INDEX: the chain test row carried no count, the quota-dashboard row missed 2 of the file's 3 named risks, and `tests/test_check_command_corpus.py` had no row | native Opus + native Sonnet, pass 44 | FIXED · LOCAL — all of them |
| DS5 | ACCEPTED with the measurement: the archive exclusion in the sweep is a declared-inert narrowing (0 of 26 tracked archive `.py` files carry `web_tools`; its only proof was a hand-planted file at pass 43); `classify.lock` is opened for the run and never closed (39 `ResourceWarning`s under `-W error` in the suite, downgraded by pytest's unraisable hook — one-shot cron semantics, the flock dies with the process) | native Opus + native Sonnet, pass 44 | ACCEPTED (2) |
| DT1 | pass-44 pool candidates: 0 code (qwen) / 4 ops (gemini) / 15 docs (deepseek) | pool fanout ×3 routed, pass 44 | REFUTED (19 of 19): `$LOG_FILE` in the body (the body is one quoted `"$2"`, DD1), the two budgets ("inconsistent" — by design, DM1), cleanup on a failed rename (the `rm -f`), a space in a project dir name (kebab dirs), and deepseek's 15 — out-of-surface INDEX/DECISIONS prose, immutable superseded rows (D-081 vs D-085 by design), a "(7)" count that does not exist (the CHANGELOG says 16), the Round 42 sentence superseded by Round 43's, quota-hook claims outside the chain |
| DU1 | (CONFIRMED — the fleet-synced gate) DS2's `(collected N)` label on the SKIPPED advisory printed the READ count (`audited` is `len(AUDITED)` now), so under three vanished files it said "collected 90" of 93 — the population the label exists to name; all four `AUDITED.add` sites survived removal with 229 green because the exact-set fixture zeroed out the wrapper and agent-def paths (dropping those two sites moved the live denominator 93 → 76 / 89 silently); with `libs/subagents/web_tools.py` absent the founding predicate ran zero times, left NO trace, and the success line named "web-tool names" at the same 93 (the provider-name defect it exists for passed green — executed; pre-existing, re-exposed by DS2's stronger line; 0 of 48 repos lack the module today); two comments and a test docstring still described the dead `max()`/COLLECTED arithmetic | native Opus + native Sonnet, pass 45 (executed all four) | FIXED · LOCAL — the label is `collected {read + skipped}` (graded: 3 skipped → "collected 3"); the fixture exercises every read site with a DISJOINT file (a wrapper naming a real orchestrator doc, an agent definition) — the two corpus-loop sites read the same files and cover each other by design, declared; a hub whose module is absent records `SKIPPED_PREDICATES` and prints `⚠ predicate skipped — …` (graded); the three texts; sandbox (label reverted): → 1 failed; (the wrapper site removed): → 1 failed; (the agent site removed): → 1 failed; (the advisory block removed): → 1 failed |
| DU2 | (CONFIRMED, 27 of 106 inspectable headers (129 carry the line) — a sibling enforcement script, in-beat) `check_script_headers.py` tested `NONE_VALUES` against the WHOLE header only, so the sentinel form CLAUDE.md § Pointers mandates — `# AFTER-EDIT: <files … \| none>` — split into a phantom coupled file literally named `none` that can never be staged: a permanently unclosable WARN on every edit of 27 scripts, this gate among them (executed on the file under review) | native Opus, pass 45 (executed: 3 header shapes) | FIXED · LOCAL — the sentinel is dropped from the split; `tests/test_check_script_headers.py` (new — the spec over three shapes and a pin on the shipped tokeniser); sandbox (filter reverted): → 1 failed |
| DU3 | texts: the registry doc attributed the error budget to transport failures alone (DQ1/DS1 made a template echo strike the same way — the doc is named in the classifier's own AFTER-EDIT header and was not staged); the regex comment did not name two residual shapes (a tail starting with an operator or ternary keyword and no later closer; a keyword argument inside a ternary condition — 0 of 49 tracked `web_tools=` lines outside this ledger); the classifier's KNOWN-TRADEOFF block did not name the cost mirror (an echoing provider costs up to 3 units over 3 laps, same per-run bound) nor that a hedged/near-miss category is tombstoned on lap 1; INDEX's gate-test row had no count | native Opus, pass 45 | FIXED · LOCAL — all of them |
| DU4 | ACCEPTED with the measurement: a hedged answer (`a\|b`) is now a lap-1 tombstone where pass 43 gave it three retries — it is the model's answer and fails the enum like any malformed one (0 of 455 blocks carry a piped category; named in the doc and the comment); the fake `timeout`'s first-`.py` key is declared inert hardening (`$5` is the script at all 5 sites; reverting it stays green); `sys.modules` caching means a second audit of a DIFFERENT repo in one process reuses the first's `WEB_TOOL_NAMES` (0 of 48 — one repo per process) | native Opus, pass 45 | ACCEPTED (3) |
| DV1 | pass-45 pool candidates: 5 code (qwen) / 4 ops (gemini) / 9 docs (deepseek — one withdrawn by the finder itself) | pool fanout ×3 routed, pass 45 | REFUTED (18 of 18): rg exit 2 with matches (adjudicated: kept + warned), an empty queue (`len(provs) <= max_per_run` returns first), a negative `--max-per-run` (DP1), `os.replace` exceptions (`except BaseException`), the two catalog readers (the probe is the sync's, BS2), the rename race / `rm -f` quoting / the alert-then-exit-code (DC5/DD1/the flag precedes the alert), the FAIL_GATHER scenario already executes the catalog refusal, and deepseek's out-of-surface CLAIMS/DECISIONS/INDEX drift notes ("no mechanism keeps CHANGELOG counts current" — the counts are the entry's record, not a live figure) |
| DW1 | (CONFIRMED — the fleet-synced gate and the runner that hosts it) `final_gate.py` registered `check_command_corpus.py` without `advisory=True`, so the runner discarded its stdout on success — DU1's `⚠ predicate skipped` line (and every ⚠ the check prints) never reached the gate that runs it (executed: the row's captured output); `_live_web_tool_names` guarded `exists()` only — a PRESENT but broken `libs/subagents/web_tools.py` (a raise on import, a renamed constant, a missing dependency after a vendor sync) took the BLOCKING gate down with a raw traceback instead of the advisory (executed in a fresh interpreter against a fake hub); DU1's print half was ungraded; the four-site fixture's `<=` upper bound let `files = files + orch_docs` vanish with the canonical orchestrator doc silently (executed: removed → green); the hub test pinned neither that the bait `["exa"]` stays silent nor that a spoke leaves `SKIPPED_PREDICATES` empty; and the underflow test's patched-out `audit()` inherited an earlier test's advisory (order-dependent) | native Opus + native Sonnet, pass 46 (executed) | FIXED · LOCAL — `advisory=True` on the corpus-gate row, pinned by a source regex in `tests/test_final_gate_advisory_display.py`; a `try/except` around the import records `_IMPORT_FAILURE` (cleared per audit) and the advisory says `failed to import (<Exc>: <msg>)` beside `absent`; the label reads `(attempted N)`; a stdout test through `main`, a subprocess broken-module test, exact-set equality on the fixture, the silent-bait and spoke assertions, `SKIPPED_PREDICATES.clear()` in the underflow test; sandbox M1 (`advisory=True` dropped): → 1 failed; M2 (the print removed): → 1 failed; M3 (the audit-side `SKIPPED_PREDICATES.clear()` removed — the DU1 line; round 46's `_IMPORT_FAILURE.clear()` and the test-side clear were NOT what reddened here, see DY3): → 1 failed; M4 (the guard removed): → 1 failed; M5 (`orch_docs` no longer joined): → 1 failed |
| DW2 | (CONFIRMED, 10 of 106 inspectable headers — a sibling enforcement script, SYNCED, in-beat) DU2 dropped the token `none` and left the CLASS: `(none)`, a `·` separator and prose words such as `(§ fix-first)` still split into phantom coupled files (10 of the 106 inspectable headers, 129 carry the line); and a staged path that exists but cannot be read raised out of `main` — a warn-only gate FAILING with a traceback that names the wrong cause | native Opus, pass 46 (executed: 3 header shapes + a `chmod 0` script) | FIXED · LOCAL — a coupled file has a path SHAPE (`/` or `.`), the rule fabrik-lib's sync-excluded copy already carries; an unreadable header is a WARN `cannot read the header (<Exc>) — not checked`; graders: the three shapes parametrized + the unreadable-script test (root-skipped) in the existing main-driven suite; sandbox M6 (predicate reverted to token-`none`): → 2 failed, 1 passed (the `| none` case is DU2's and stays green under the old predicate — expected); M7 (the guard removed): → 1 failed |
| DW3 | texts: round 45 added `tests/test_check_script_headers.py` beside the existing main-driven `tests/enforcement/test_check_script_headers.py` (9 tests) — two suites for one script, INDEX carrying both and calling the older one "5 tests"; INDEX's corpus-test count (50 → 53 collected); the Pass 45 row's test total and the DU2/DU3 denominators (233 → 232, 50 → 49, "28 of 128" → 27 of 106 inspectable, "0 of 58" → 0 of 49 tracked outside this ledger); the CHANGELOG's "all four survived removal" (only the wrapper and agent sites had); the regex comment's stale count | native Sonnet, pass 46 | FIXED · LOCAL — the duplicate file deleted (`git rm`), its two tests folded into the main-driven suite (13 tests), every count re-derived from the producing tool (`pytest --collect-only`, `grep -c`) |
| DX1 | pass-46 pool candidates: 5 code (qwen) / 3 ops (gemini) / 1 docs (deepseek, self-labelled UNVERIFIABLE) | pool fanout ×3 routed, pass 46 | REFUTED (8 of 9) + ACCEPTED (1): an unreadable `/opt/<dir>` crashing `project_dirs()` — ACCEPTED with the measurement: `Path.exists` (3.12) swallows only ENOENT/EBADF/ENOTDIR/ELOOP, so a `d---------` entry WOULD raise; live: 59 dirs under `/opt`, 1 not readable (`/opt/containerd`, root, `--x--x`) and executed against it `.git exists → False`, `project_dirs()` → 45 without raising; 0 of 59 non-executable · credits fetched for an unattributed credential (the `next(...)` generator filters `kinds_by_digest != credential-unattributed`, `registry_sync.py:364` — the check the finder placed "after" IS the selection) · `n <= after` with `after=None` (`if after is not None`, `classify_services.py:184`) · a tombstone adopted as catalogued (`real_category` gates it at `gather_envs.py:866/997`, CP1) · curated + merged same-length tokens (`PROV_TOKEN` is not a prefix of `PROV_API_KEY`; `prefix_hits` yields the curated hit alone → owned) · the heartbeat rename race (per-PID tmp + `rename(2)` on one mount; both entry points share the daily lock, `wsl_startup_hook.sh:72/188`; a failed redirect keeps the previous stamp — DA3) · `sweep_stale_tmp` without an `except OSError` (it is there, `gather_envs.py:1223` — the finder said the source was "not fully visible") · `set -u` on `LOG_FILE` (defaulted at line 28 before its first use at 51; `:-` also substitutes an empty string) · `kind: standard` on the linkedin CLAIMS row breaking a legal-review consumer (0 of 141 hub scripts read a CLAIMS `kind`; the watcher's two `kind` hits are mail `--kind`) |
| DY1 | (CONFIRMED, HIGH — MY round-46 regression on the fleet-synced gate, plus its runner) the `try/except` in `_live_web_tool_names` turned a PRESENT-but-broken hub module (a raise on import, a renamed constant after a vendor sync) from a BLOCKING red (`ImportError … EXIT=1`, executed pre-round-46) into `✓ … all sound across 94 file(s)` + `⚠ predicate skipped — … failed to import (…)` at EXIT=0 — and that ⚠ never reached `--json`, whose `warnings` filter admits only ⚠-FIRST output while the check prints its ✓ line first (executed through `run_optional_check`: `would land in --json warnings?: False`); an EMPTY `WEB_TOOL_NAMES` (a stub) flagged every name in the corpus with `valid: ` (0 fire — the literal has 5 names); the DW1 grader was a text-regex pin that a second `advisory=True` within 600 chars satisfied (executed by both natives); `advisory=True` without `--quiet` put the ✓ denominator line under a green row on every human-mode gate fleet-wide (`… all sound across 0 file(s) read` in a spoke-shaped tree) — the class the sibling row solved with `--quiet`; round 46's `_IMPORT_FAILURE.clear()` was ungraded (removal → 53 passed; an absent module after a broken one reported the previous repo's exception) | native Opus + native Sonnet, pass 47 (executed) | FIXED · LOCAL — in the hub a present-but-unusable module is a PROBLEM (`present but unusable (<Exc>) — fix the module, do not skip`), absent stays the DU1 advisory (a project never vendors it); an empty constant is recorded as `WEB_TOOL_NAMES is empty` and takes the same path; `--quiet` suppresses the ✓ line only — the ⚠ lines always print, and print FIRST — and the gate passes it; graders: a three-hub subprocess driver (broken → problem; empty → problem; absent-after-broken → the `absent` advisory, proving the clear), the `--quiet` stdout test, an AST pin on the registration (`--quiet` + `advisory=True`, no text bleed), an executed stub through `run_optional_check` (⚠-first stdout kept under `advisory=True`, dropped without); sandbox R1 (hub broken → advisory again): → 1 failed; R2 (empty guard removed): → 1 failed; R3 (`--quiet` ignored): → 1 failed; R4a (`--quiet` dropped from the row): → 1 failed; R4b (`advisory=True` dropped): → 1 failed; R4c (the runner discards advisory stdout): → 1 failed · ACCEPTED with measurements: the `sys.modules` package cache means a second repo in one process reuses the first's names and never reaches the guard (0 of 48 callers — one repo per process; `web_tools.py` has a package-relative import, so a by-path load is not a drop-in — the graders shell out instead; DU4 already held this; ⚠ the COST of that accepted state grew this round — in one process without a purge a GOOD repo after a broken one now gets a false BLOCKING problem where round 46 gave it an advisory, and a revisited repo's `sys.path` entry no longer moves to the front (EA measurement, pass 48) — still 0 of 48 callers); the runner's ⚠-FIRST `warnings` filter as a class (5 of 72 enforcement scripts print both a ✓/✅ and a ⚠ shape statically; 0 ⚠ lines followed a PASS row in the pass-46 human run — left as is, this check now satisfies it via `--quiet`) |
| DY2 | (CONFIRMED — the sibling synced check; DW2's "this closes the class" was false) 8 of 106 inspectable headers (13–14 of 217 kept tokens — two independent recounts disagree by one; the 8 headers agree) named a coupled file NO staging action could satisfy — script-relative names (`tests/test_x.py` beside `scripts/kilo-benchmarks/x.py`, 6 headers), a glob (`docs/orchestrator/**`), a directory (`tests/fixtures/kaizen-golden/`) — executed with exactly the named files staged → WARNING; the `.` half of the shape rule was ungraded (mutation → 13 passed; it guards 17 of 217 tokens: `CHANGELOG.md`, `INDEX.md`, `CLAUDE.md`, `PORTS.md`, `.env.example`); `n/a` (a `NONE_VALUES` member carrying a `/`) minted a phantom again (0 of 106 today); extension-less files (`Makefile`) were dropped (0 of 39 dropped hub tokens name a real path; across `/opt/*/scripts` the only shape-less tokens are prose); a dangling symlink fell through the deletion branch and was counted as inspected; the clean line counted COLLECTED scripts (a staged deletion → `1 staged script(s) inspected` for 0 read — the collected-vs-attempted overstatement of DW3, in the sibling); the OSError comment named a case (the dead symlink) that never reaches it | native Opus, pass 47 (executed: two staging repros, a deletion, a dangling symlink, a `chmod 0` file) | FIXED · LOCAL — `_coupled_tokens` (sentinel-excluded; path shape OR exists repo-rooted / script-relative) + `_satisfied` (exact, script-relative, glob via `fnmatch`, directory prefix); a dangling symlink is a WARN; `inspected` counts what was READ (`N of M staged script(s) inspected`); the comment; graders: 8 new cases in the main-driven suite; sandbox R5a (script-relative removed) / R5b (glob removed) / R5c (directory removed): → 1 failed each; R6a (`exists()` removed): → 1 failed; R6b (sentinel filter removed): → 1 failed; R6c (the `.` half removed): → 1 failed — only after the test stopped creating the file on disk (an existing file is coupled by `exists()` whatever its shape; the first cut of the test was vacuous for that half and was caught by its own revert); R7a (symlink branch removed): → 1 failed; R7b (counter → collected): → 1 failed |
| DY3 | texts: DW1's `M3 (the clear removed)` vouched for the wrong line — the red was the pre-existing audit-side `SKIPPED_PREDICATES.clear()` (DU1), not round 46's `_IMPORT_FAILURE.clear()` (graded now, DY1) nor the test-side clear (hygiene only: collection order is deterministic here and the underflow test precedes the populating one — no order defect was demonstrable); the subprocess test's first assertion `startswith("None [")` also matched a silent swallow (`None []`) — the exact failure list is asserted; INDEX counts (53 → 54, 13 → 21, 5 → 7); CHANGELOG Round 47 | native Opus + native Sonnet, pass 47 | FIXED · LOCAL — the M3 line qualified in place, the assertions, the counts |
| DZ1 | pass-47 pool candidates: 5 code (qwen) / 4 ops (gemini) / 7 docs (deepseek) | pool fanout ×3 routed, pass 47 | REFUTED (15 of 16) + ACCEPTED (1): the `classify.lock` fd held for the run (the fd IS the lock — closing it releases it; DS5 measured the ResourceWarning) · catalog unreadable mid-dispatch merged onto the pre-dispatch probe (LOUD by design, BM3: the paid batch is not lost and the WARNING says any edit since is DISCARDED) · `project_dirs` `.git`-only (the docstring's contract; env files are enumerated by `OPT.glob("*/.env")` independently, `gather_envs.py:711`) · gen_dashboard's tmp on a failed replace (`finally: tmp.unlink(missing_ok=True)`, line 280) · IPv6 brackets (0 of 119 `url` values in `scripts/service_catalog.json` bracketed) · the heartbeat rename race (DX1's refutation, re-raised — the brief carried the row) · alert failure under disk-full masking the status (exit 1 is preserved; `_alert`'s `2>&1 || true` sends the interpreter's own stderr to the log, so a broken alerter IS logged — which refutes the `$VENV_PY` one too) · ACCEPTED with the measurement: a SIGKILL between `date >` and `mv` orphaning `chain-heartbeat.tmp.<pid>` (0 orphans on disk; `timeout -k` kills the child STEPS, never this script; a trap for a microsecond window is wallpaper) · deepseek's seven: six aim at sibling entries in the cumulative CHANGELOG/DECISIONS diff outside this surface (the deploy-triad block, D-091 "the modes are gone" — the SOURCE has one mode and the parser tolerates old headers, both true; `flush_subagent_outboxes`; D-085 vs `EXTERNAL_SYSTEMS.md` — the header reads 148 at HEAD, line 4) and the CLAIMS `kind` re-raise (0 of 141 consumers, DX1) |
| EA1 | (CONFIRMED, HIGH — MY round-47 escalation on the fleet-synced gate) the hub PROBLEM fired on ANY failure raised while importing `libs.subagents.web_tools` — the whole `libs.subagents` package graph and `httpx` — and blamed `web_tools.py`: a `def broken(:` appended to `libs/subagents/lanes.py` → `✗ … web_tools.py is present but unusable (SyntaxError … lanes.py, line 658)`, EXIT=1, and through the real `run_optional_check` `passed: False` on the BLOCKING row (executed) — on this shared tree a peer session's half-saved sibling module would red every other session's Tier-2 gate on a file CLAUDE.md forbids them to touch, with the remediation pointing at the wrong file; the ⚠ coverage lines (`could NOT be read`, `predicate skipped`) were printed on the SUCCESS exit only — the failing run, the one that most needs to say its verdict was computed over an incomplete corpus, dropped them (executed) while the new `--quiet` help text claimed they always print; the except-clause comment still said "degrade like an absent one"; the coupled reference doc `docs/reference/command-corpus-check.md` was left stale by round 47 (no `--quiet`, no hub-blocking mode; lines 78–82 read as the reverse contract) — the header check's own WARN, executed against round 47's staged set; round 46's stdout test left `SKIPPED_PREDICATES` populated (a leak, benign in file order); the AST pin accepted `--quiet` in ANY constant slot (a registration whose display name is `--quiet` and passes no flag satisfied it) | native Opus + native Sonnet, pass 48 (executed) | FIXED · LOCAL — `_IMPORT_BLAME` records the file the failure was raised in (`exc.path` for a renamed constant, the last traceback frame otherwise; the empty constant is blamed on `web_tools.py` explicitly); the hub blocks only when the blame is `web_tools.py` itself, a sibling's failure is an advisory naming that file (`<file> failed to import (<Exc>) while loading libs/subagents/web_tools.py — predicate 1 did not run; not this check's surface`); `_print_coverage_warnings()` runs on both exits; the comment; the reference doc's hub/project split + flags paragraph; the leak cleared; the pin reads `call.args[2:]`; graders: a fourth `sibling` hub in the driver (init raises, web_tools fine → no problem, one advisory naming `__init__.py`), the failing-run test; sandbox Q1 (blame gate removed): → 1 failed; Q2 (failing path drops the ⚠ lines): → 1 failed; Q8 (`--quiet` in the display-name slot): → 1 failed · ACCEPTED with measurements: the multi-repo-in-one-process cost delta (a good repo after a broken one, no purge → a false BLOCKING problem; a revisited repo's `sys.path` entry stays behind a later one — both 0 of 48 callers, one repo per process; recorded on the DY1 acceptance); `--quiet` hides DU1's `all sound across 0 file(s)` line in a project's gate run — a tension, not a defect: with nothing printed nothing misreads as coverage, and the sibling row made the same call |
| EA2 | (CONFIRMED — the sibling synced check; DY2's own regression + its residue) `_satisfied`'s script-relative candidate closed a ROOT coupling with a same-named local file: `# AFTER-EDIT: README.md` on `scripts/x.py`, `scripts/README.md` staged, root `README.md` untouched → silence where the pre-round-47 rule warned (executed; 3 same-named doc files already live under `scripts/`, 0 of 217 tokens ambiguous today); `as_posix()` dropped the trailing `/`, so a script-relative DIRECTORY token could never be closed (executed: `fixtures/` → an unclosable WARN; 0 of 217 hub tokens are script-relative directories); a glob matching the script's own path was self-satisfying (`scripts/**` staged alone → OK; 0 of 2 hub globs self-match) and only `*` counted as a glob character; the `exists()` halves would promote a prose word that names a DIRECTORY (`docs`, `src`) — 0 of 283 hub tokens today, unmeasurable across the ~46 synced trees; the early-return comment claimed a wording parity round 47 broke; the symlink test's second clause was decoration (the `inspected` line never prints beside a WARN); the glob INCLUSION half of `_coupled_tokens` was ungraded (dropping `*` tokens as prose passed every case) | native Opus + native Sonnet, pass 48 (executed: 8 header repros, 4 mutations) | FIXED · LOCAL — repo-rooted candidate first, the script-relative one only when the repo-rooted path does not exist; the `/` kept; the script itself excluded from glob/prefix matches, `*?[` all glob characters; `is_file()` (a `Makefile` still counts, a bare directory name never); the early-return line reads `0 of 0 …`; the symlink test asserts the WARN only; graders: the wrong-root case, the glob-inclusion case, the self-glob case, the script-relative directory case, the prose-directory case; sandbox Q3 (root not preferred): → 1 failed; Q4 (slash dropped): → 1 failed; Q5 (self-glob allowed): → 1 failed; Q6 (`is_file` → `exists`): → 1 failed; Q7 (globs dropped as prose): → 1 failed · residual, measured: 7 of 217 kept tokens across 6 headers are still unsatisfiable in the EXTRACT — 4 name files the extract lacks (`.env.example`, `docs/DEPLOYMENT.md`, `docs/OPERATIONS.md`, a test file), 3 are stale-target headers in `scripts/kilo-benchmarks/` naming files that do not exist (closable by creating the file or fixing the header — a different class, out of this surface) |
| EA3 | texts: the Pass 47 row and CHANGELOG Round 47 said "13 mutations / thirteen graders" for 14 labelled R-entries (R1–R3, R4a–c, R5a–c, R6a–c, R7a–b); DY2's "13 of 217" against an independent recount of 14 (the 8 headers agree — both figures now stated); DZ1's "0 of 117 catalogued URLs" used the `#svc` block count as the denominator for a catalog-JSON grep (119 `url` values); the DY1 acceptance's cost changed under round 47 and is restated; INDEX counts (54 → 55, 21 → 26); CHANGELOG Round 48 | native Sonnet + native Opus, pass 48 | FIXED · LOCAL — all of them |
| EB1 | pass-48 pool candidates: 0 code (qwen — an empty list) / 4 ops (gemini) / 2 docs (deepseek) | pool fanout ×3 routed, pass 48 | REFUTED (6 of 6): the heartbeat rename race (DX1/DZ1, third raise — the script runs under the callers' shared daily lock, `wsl_startup_hook.sh:72/188`) · `$SECONDS` including a suspension (wall-clock is the intended timing of a cron step; a suspended step's wall time IS what the log should show) · `_alert` quoting ("currently safe" by the finder's own text — argv, never interpolation) · missing `mv -T` tests (DE5, accepted) · the CLAIMS `kind` row (DX1, third raise; 0 of 141 consumers) · unescaped pipes in DECISIONS rows D-094/D-093/D-092/D-087/D-080 (executed: 0 of 97 `D-` rows deviate from the separator's 7 pipes once escaped pipes and code spans are removed) |
| EC1 | (CONFIRMED, HIGH — MY round-48 blame attribution on the fleet-synced gate) a SyntaxError carries no `path` and CPython 3.12 strips the import-machinery frames, so `tb[-1].filename` is the IMPORTER: `def broken(:` appended to `libs/subagents/web_tools.py` → `✓ … all sound` + `⚠ predicate skipped — … libs/subagents/loop.py failed to import (SyntaxError … (web_tools.py, line 420))`, EXIT=0, `run_optional_check` → `PASSED: True` — the DY1 contract defeated for the whole compile-error class, the advisory contradicting itself in-band (executed); the MIRROR: a SyntaxError in a sibling that `web_tools.py` imports DIRECTLY lands on `web_tools.py`'s own `from .tools import` frame → the hub PROBLEM under the wrong name (executed on a fixture; 0 of 1 direct import today — `tools.py` is imported earlier by `mcp_tools.py`, so a half-saved `agent.py` was blamed on `__init__.py`: verdict right, attribution wrong, the doc's own advertised example); the `exc.path` preference — the only thing that makes a renamed constant BLOCK — had no grader (mutation → 88 passed while the verdict flipped); the advisory printed an absolute out-of-repo path (a site-packages dependency under the operator's home) into a synced check's stdout; two doc sentences the code does not honour ("in a project … prints `⚠ predicate skipped`" — a project has no corpus and never reaches the branch; "⚠ first on both exits" — after the `✗` block on the failing one, by the round's own grader) and the hub-branch comment said "a project … skips by design" | native Opus + native Sonnet, pass 49 (executed: 12 fixture hubs, 4 real-tree mutations, a 5-shape blame matrix) | FIXED · LOCAL — `blame = exc.path or (exc.filename if SyntaxError) or tb[-1].filename`; an out-of-repo blame is named by file (`<name> (outside the repo)`); the comment and the two doc sentences; graders: four hubs added to the subprocess driver — `syntax` (a `def broken(:` web_tools → BLOCK), `renamed` (`WEB_TOOL_NAMES_V2` → BLOCK via `exc.path`), `sibtools` (`from .tools import H` + a broken tools.py → an advisory naming `tools.py`, never `__init__`), `extdep` (a raising dependency on `PYTHONPATH` → an advisory naming `broken_dep_xyz.py (outside the repo)`, no `/`); sandbox S1 (the SyntaxError clause removed): → 1 failed; S2 (the `exc.path` preference removed): → 1 failed; S3 (the absolute path kept): → 1 failed · ACCEPTED with measurements: a module-level `SystemExit` at import escaping `except Exception` (0 of 21 `libs/subagents` modules exit at import outside a `__main__` guard — AST sweep; `except BaseException` would swallow Ctrl-C); the driver purges `sys.modules` but not `sys.path`, so a namespace-package fixture (no `__init__.py`) would resolve `libs` from an EARLIER hub — one hub per process for that shape; every hub the driver carries that HAS a `libs/` tree has its `__init__.py` (7 of 8 — the `absent` hub has no `libs/` at all) |
| EC2 | (CONFIRMED — the sibling synced check's graders) the `?`/`[` glob-character widening of EA2 had no grader (reverting it to `*`-only → 269 passed; 0 of 217 hub tokens use `?`/`[`); the `0 of 0 staged script(s) inspected` early-return wording of EA2 was asserted nowhere (reverting → 26 passed); `test_live_corpus_is_clean` leaves the module-global `AUDITED` populated — a leak visible only in reversed collection order (the other four globals stay clean) | native Sonnet, pass 49 (executed) | FIXED · LOCAL — a `docs/coupled.m?` case, the `0 of 0` assertion in the nothing-staged test, `AUDITED.clear()` after the live audit; sandbox S4 (`*` only): → 1 failed; S5 (the old wording): → 1 failed · ACCEPTED with the measurement: root-first candidates make a SCRIPT-RELATIVE directory token unclosable the day a same-named ROOT directory appears (`Path(token).exists()` is true for a directory) — 0 of 217 hub tokens are script-relative directories, 11 script-relative file tokens now depend on the ABSENCE of a same-named root path (0 of 217 ambiguous today); unmeasurable across the ~46 synced trees |
| EC3 | texts: the Pass 48 row's "17 candidates" for EA1–EA3 against a clause recount of 19 (a grouping difference — restated as the clause count); INDEX (26 → 27); CHANGELOG Round 49 | native Sonnet, pass 49 | FIXED · LOCAL |
| ED1 | pass-49 pool candidates: 7 code (qwen) / 4 ops (gemini) / 6 docs (deepseek) | pool fanout ×3 routed, pass 49 | REFUTED (17 of 17): `sync_registry(prune_keys=None)` deleting keys from a PARTIAL file (`prune if prune_keys is None else prune_keys`, `registry_sync.py:353` — `prune` is the Z7 partial-file guard's own verdict) · a negative `--max-per-run` (DP1) · a symlinked `/opt/<repo>` (`Path.exists` follows symlinks; only a DANGLING one fails, which is not a repo) · gen_dashboard's `finally` ("the current code doesn't have this issue" — the finder's own text) · `_read_json` memory on a large error file (per-provider entries, ERROR_BUDGET tombstones bound the growth) · a symlink planted in the `unlink`→`O_EXCL` window (`O_CREAT|O_EXCL` refuses an existing path of ANY kind, a symlink included) · alert-body quoting (argv, never interpolation — "UNVERIFIABLE" by the finder) · `rm -f "$HEARTBEAT.tmp.$$"` unquoted (it is quoted, line 79) · `mv -T` (DE5) · the SIGKILL orphan (DZ1) · `core_failed` bypass ("verified: chain_failed=1 is set" — the finder's own text) · deepseek's six aim at sibling DECISIONS/CHANGELOG/INDEX rows outside this surface (D-088/D-082/D-095/D-094 prose, the quota-dashboard entries) |
| EE1 | (CONFIRMED, HIGH — MY loader on the fleet-synced gate, the class DY1/EA1/EC1 left open) `WEB_TOOL_NAMES = None` (or an int) → `TypeError: 'NoneType' object is not iterable` at the `frozenset`, a raw traceback as the BLOCKING row's message (executed through the real gate row: `PASSED: False` with a traceback); `WEB_TOOL_NAMES = "web_search"` → `frozenset` of nine CHARACTERS, the DY1 empty-guard never fires, and every real name in the corpus is flagged — `valid: _, a, b, c, e, h, r, s, w` (executed; `libs/` is excluded from ruff AND mypy, so this check is the constant's only reader); a non-str member reaches the remediation join and crashes there; the hub gate was a bare `endswith("web_tools.py")` — `libs/web_tools.py` (a real standalone module in this tree, 2 of 2 files end with the name) and any `*web_tools.py` sibling blocked under `libs/subagents/web_tools.py`'s name (executed); a RUNTIME SyntaxError (`compile()` of a string) says `<string>` as its filename, which the EC1 rule trusted → `'<string>'.endswith(…)` False → an ADVISORY, `PASSED: True` for an unusable module (executed; 0 of 21 vendored modules call `compile`/`eval`/`exec` today); an out-of-repo PACKAGE was shown as a bare `__init__.py`, and an in-repo file behind a symlinked `libs/` was labelled "(outside the repo)" because `resolve()` ran first; the doc's SyntaxError sentence stated the rule unconditionally | native Opus, pass 50 (executed: 18 hubs in 18 processes, 5 real-gate-row runs) | FIXED · LOCAL — four helpers close the class: `_blame_for` (`exc.path` → a SyntaxError's `filename` only when it names a REAL file → the last frame), `_shape_problem` (a non-empty collection of non-empty str, or the shape is named), `_same_file` (identity by `resolve()`, never a suffix), `_display_path` (the literal repo-relative path first, then resolved, else the file name with its package for an `__init__.py`); the doc paragraph; graders: seven hubs added to the driver (`extpkg`, `strconst`, `noneconst`, `badmember`, `rtsyntax`, `suffix`, `symlibs`); sandbox U1 (a bare str accepted): → 1 failed; U2 (None accepted): → 1 failed; U3 (non-str members accepted): → 1 failed; U4 (suffix identity): → 1 failed; U5 (`<string>` trusted): → 1 failed; U6 (resolved-only display): → 1 failed; U7 (bare `__init__.py`): → 1 failed |
| EE2 | (CONFIRMED — grader hygiene on the driver and the texts) the driver's `PYTHONPATH=<site>` reached every hub — a regular `libs` package there would have beaten a namespace-shaped hub (executed: a namespace hub flipped to `valid: SITE_LEAK_NAME`; 0 of 8 driver hubs were namespace-shaped, so 0 fire); the `extdep` hub's `"/" not in …` assertion was redundant (the literal-substring assertion before it already pins the string — executed under S3: the earlier assertion is the one that reds); the EC1 acceptance said "8 of 8 hubs have `__init__.py`" — the `absent` hub has no `libs/` tree at all (7 of 8) | native Opus + native Sonnet, pass 50 (executed) | FIXED · LOCAL — the dependency dir is appended to `sys.path` inside the driver for the two hubs that need it only; the redundant assertion replaced by the package-hub assertion; the count corrected · ACCEPTED with measurements: `test_live_corpus_is_clean`'s `AUDITED.clear()` has no grader (every other reader clears or re-audits first; `audit()` itself clears at its top — hygiene, not enforcement); the `0 of 0` early-return literal is a spelled constant, not a computed denominator (nothing is staged — there is nothing to compute; its comment now says so) |
| EE3 | texts: the reference doc's SyntaxError sentence (now conditional on a real file, with the runtime shape, the identity rule, the shape rule and the symlink case); the header check's early-return comment (it claimed a parity the clean line's third count does not have); CHANGELOG Round 50 | native Opus, pass 50 | FIXED · LOCAL |
| EF1 | pass-50 pool candidates: 8 code (qwen) / 5 ops (gemini) / 4 docs (deepseek) | pool fanout ×3 routed, pass 50 | REFUTED (17 of 17): `bound_flagged` with one provider and `after` at it (`len(provs) <= max_per_run` returns everything before the cursor arithmetic) · IPv6 brackets (DZ1, 0 of 119) · an error budget persisting for a provider that left the flagged set (`if v > 0 and k in keep` DROPS it — the finder's own quoted line) · PLATFORM_SLD attribution "loss" (by design: a platform host is attributed by key name or flagged, BR/CP rows) · two concurrent scans (the daily lock + `classify.lock`; the lost-update window is a settled class) · an XSS through a single quote in `json_for_script` (JSON strings are double-quoted and `json.dumps` escapes `"`; `<` is escaped — AM4; a `'` closes nothing) · `credential_rank` "inconsistency" (the rank is BY ROLE, AM1/AH2) · a directory at the tmp name (`except OSError: let O_EXCL say so`, BH7) · `$LOG_FILE` unquoted in `_alert` (it sits inside a double-quoted argument, line 51) · `mv -T` (DE5) · the SIGKILL orphan (DZ1) · a PID-predicted symlink under the operator's own `.tmp/` (single-operator threat model; `O_EXCL` on the Python side) · `check_daily_refresh_freshness.py` missing from the chain (it guards `daily_refresh.sh`'s own pipeline, not this chain) · deepseek's four: a "truncated" D-083 (7 pipes, the separator's count), the CLAIMS `kind` row (fourth raise), an INDEX D-082/D-086 pointer on a sibling surface, and EXTERNAL_SYSTEMS.md "not in the diff" (no hunk of mine touches it since pass 9) |
| EG1 | (CONFIRMED, HIGH — MY loader on the fleet-synced gate: the traceback-inference class of EA1/EC1/EE1, closed structurally) the REAL extract's `libs/subagents/web_tools.py` truncated with trailing NUL bytes → `SyntaxError: source code string cannot contain null bytes` with `exc.filename is None` → the last frame is `loop.py` → an advisory naming an innocent file, EXIT=0, the gate row `passed: True` (executed — verbatim the EC1 headline of pass 49, reproduced after two rounds of fixes); `chmod 000` on it → a PermissionError raised INSIDE the import machinery, the frame `<frozen importlib._bootstrap_external>`, an advisory declaring the hub's own broken file "not this check's surface", EXIT=0 (executed); a DIRECTORY at the module path passed the `.exists()` presence guard and produced a ModuleNotFoundError blamed on the importer, EXIT=0 (executed); `_shape_problem` rejected `dict` keys and a `dict` — valid collections the consumer's `name in WEB_TOOL_NAMES` accepts — as "not a collection of str", a fleet-wide false BLOCK the day the names are derived from a registry (0 of 2 definitions today); `_same_file`'s `except OSError` could not catch pathlib's ELOOP (re-raised as RuntimeError) nor an embedded NUL (ValueError), and `_display_path` resolved unguarded — a raw traceback out of the gate on a crafted `exc.path` (0 of 21 vendored modules construct one); `_display_path` resolved a RELATIVE or pseudo blame against the cwd (`<frozen importlib._bootstrap>` rendered as a repo file from the repo, "(outside the repo)" from `/tmp`); the identity rule was half-graded — no hub had a DIFFERENT file with the SAME basename, so `Path(blame).name == target.name` passed 15 of 15 hubs (Sonnet, executed); the doc's attribution sentence omitted the load-bearing `exc.path` branch (0 of 226 lines named it) | native Opus + native Sonnet, pass 51 (executed: 3 real-tree mutations through the CLI and the gate row, 10 adversarial hubs, 4 helper mutants, 2 cwds) | FIXED · LOCAL — `_target_is_broken(target)`: not a regular file / unreadable / NUL bytes / a compile-time SyntaxError are settled by ONE read and ONE `compile()` of the target BEFORE the import, blamed on the target with certainty; `_blame_for` reads any exception's `filename` that names a real file (a PermissionError carries it), then the frame; `_shape_problem` accepts any `collections.abc.Collection` except str/bytes (a generator, None, an int rejected; the dead `isinstance(value, str) or` disjunct dropped); `_same_file` catches `(OSError, RuntimeError, ValueError)`; `_display_path` resolves ABSOLUTE paths only, names a pseudo path `(not a file)` and an empty blame `an unknown file`; the doc paragraph rewritten around the compile-first rule and `exc.path`; graders: hubs `parentwt` (`libs/web_tools.py` raising, imported via `from ..web_tools` → an advisory naming IT), `nulbytes`, `noperm` (root-skipped), `dirmod` (each → BLOCK naming the cause), `dictkeys` (ACCEPTED — predicate 1 runs and flags the bait); sixteen direct unit tests over `_shape_problem` (12 shapes), `_display_path` (two cwds), `_same_file` (a symlink loop, a NUL), `_blame_for` (a PermissionError, a `<string>`), `_target_is_broken` (four states); sandbox V1 (compile-first bypassed): → 1 failed; V2 (filename narrowed to SyntaxError): → 1 failed; V3 (the four literal types): → 1 failed (driver) + 2 failed (unit); V4 (`except OSError` only): → 1 failed; V5 (a relative path resolved): → 1 failed; V6 (a directory not broken): → 1 failed (driver) + 1 failed (unit) · ACCEPTED with measurements: `_blame_for`'s `is_file()` is TOCTOU-racy for a SIBLING that vanishes between the raise and the blame (the target's own health no longer depends on it; a vanished sibling falls to the frame — an advisory either way, the right verdict for a sibling); the sixteen-hub driver grades through one assertion chain (a red names the first failing hub only — the direct unit tests now give per-helper resolution) |
| EG2 | (CONFIRMED — grader precision) the four helpers had ZERO direct tests (`grep -c` → 0 of 1132 lines) and one monolithic assertion chain; the identity rule's grader could not distinguish identity from basename | native Opus + native Sonnet, pass 51 (executed) | FIXED · LOCAL — folded into EG1's graders (sixteen unit tests + the `parentwt` hub) |
| EG3 | texts: three INDEX code spans carried a bare `\|` inside a table row (my `test_check_script_headers.py` row's `a.py \| b.md` and `\|` — GFM splits a cell on a pipe inside a code span; 3 code spans across 2 of 240 rows, executed by counting bare pipes against the separator); the doc's SyntaxError sentence; CHANGELOG Round 51 | deepseek (pool) + native Opus, pass 51 | FIXED · LOCAL — the pipes escaped, the paragraph rewritten |
| EH1 | pass-51 pool candidates: 7 code (qwen) / 4 ops (gemini) / 3 docs (deepseek) | pool fanout ×3 routed, pass 51 | REFUTED (13 of 14) + FIXED (1 — the INDEX pipes, EG3): qwen's seven are self-refuted confirmations ("the code explicitly checks", "matches the intended behavior", "correct fail-closed behavior" — its own text; scored 1 for zero discrimination) · `_alert`'s body unquoted (`"$1" "$2" "$3"`, line 35 — quoted) · the heartbeat race (sixth raise; the daily lock) · `flush_subagent_outboxes.py`/`rank_task_subagents.py` "unverifiable" (not this surface) · `mkdir -p` on a non-directory path component (alerted and exit 1 — DA3, by design) · a CHANGELOG mailbox sum on a SIBLING's entry (not this surface) · a "truncated" D-088 `why` cell ending "the blind pass found num" (the phrase does not occur in the row; 7 pipes, the separator's count — fabricated) |
| EI1 | (CONFIRMED, HIGH — MY loader on the fleet-synced gate: the two edges compile-first left open) `libs/subagents/` mode 000 → `target.exists()` itself raised `PermissionError` (pathlib ignores only ENOENT/ENOTDIR/EBADF/ELOOP) — a raw traceback as the BLOCKING row's message through the real CLI and the gate row (executed); a DANGLING or self-looping symlink at the module path read `exists()` False → "absent — predicate 1 did not run", the project-by-design wording, for a broken vendored link (executed); a corrupt `__pycache__` pyc with a healthy source → `EOFError: marshal data too short` with no path and no filename → the last frame `<frozen importlib._bootstrap_external>` → an advisory "(not a file) … not this check's surface", exit 0 (executed — the EG1 headline one artifact over); the widened `filename` rule's MIRROR: a module opening an unreadable/missing DATA file at import carries that file's name → blamed on `secret.txt`, an advisory, where the pre-EG1 rule blocked (executed; 0 of 16 `open`/`read_text` sites in the vendored package run at import today); `compile()` of a pathological source raised RecursionError past the catch tuple (1 of 3 shapes; 0 of 21 modules); the BLOCK line quoted the exception's absolute path (`Permission denied: '/home/…'`) in a synced check's stdout; the doc's "with certainty" covered health, not the names (a same-second same-size rewrite keeps a stale pyc header — `copy2` preserves mtimes fleet-wide; 0 today) | native Opus, pass 52 (executed: 20 hubs, 3 real-extract mutations through the CLI and the gate row) | FIXED · LOCAL — the presence probe is `exists() or is_symlink()` under `try/except OSError` (a raise is the target's own failure); a dangling link is present and "not a regular file"; `_blame_for` takes the last frame that names a REAL file and a failure whose blame is the checker itself is the target's (the only import in the checker is the target's); the `filename` branch requires a `.py`; the compile catch adds RecursionError/MemoryError; `_scrub` strips the repo prefix from the failure text; the doc qualified; graders: hubs `dangling`, `nopermdir` (root-skipped; readability captured BEFORE the mode is restored), `corruptpyc` (a `py_compile`d pyc truncated to its header), `missingcfg`; unit rows: a missing filename and a real non-`.py` fall to the frame; sandbox X1 (a bare `exists()`): → 1 failed; X2 (the checker-frame rule removed): → 1 failed; X3 (`.py` requirement dropped): → 1 failed + 1 failed (unit); X4 (no scrub): → 1 failed; X5 (the pseudo frame taken): → 1 failed · ACCEPTED with measurements: a same-second same-size source rewrite leaves a matching pyc header — the names come from the import, the health from the source (0 today; `git checkout` stamps mtime=now); a RELATIVE in-repo blame renders "(outside the repo)" (0 of 4 `audit()` call sites pass a relative repo; frame filenames are absolute under `sys.path.insert(0, str(repo))`); the `_IMPORT_BLAME[-1] == str(target)` disjunct is load-bearing exactly when `resolve()` fails on the target (the unreadable-parent hub) — kept, graded by that hub |
| EI2 | (CONFIRMED — grader precision) `_display_path`'s resolved branch had no test that made the literal `relative_to` fail and the resolved one succeed (deleting the branch → 71 passed); `_blame_for`'s `is_file()` guard was ungraded (removal → 71 passed; it flips a missing-config BLOCK to an advisory); `_target_is_broken`'s message hard-coded a path it was not given | native Sonnet + native Opus, pass 52 (executed) | FIXED · LOCAL — a symlinked-alias case for the resolved branch (X6: → 1 failed); the two unit rows; the message names the shape, not a path |
| EI3 | texts: EG3's "3 of 240 rows" (3 code spans across 2 of 240 rows); the doc's attribution and certainty sentences; CHANGELOG Round 52 | native Sonnet + native Opus, pass 52 | FIXED · LOCAL |
| EJ1 | pass-52 pool candidates: 0 code (qwen — an empty list) / 4 ops (gemini) / 1 docs (deepseek) | pool fanout ×3 routed, pass 52 | REFUTED (5 of 5): the heartbeat race (seventh raise) · a non-object `cursor.json` crashing `state.get` (`_read_json` returns the default for a wrong SHAPE, `classify_services.py:190–195`) · the catalog merge-back overwriting a human edit (BM3 — loud, by design) · "the diff ends abruptly at line 981" (the pool brief's own truncation, the settled `pool-brief-truncation` class; the file parses — 286 tests ran) · D-093's "50 live vendored copies" vs D-098's 47 + hub + fabrik-lib (two populations: vendored POOL copies incl. two structurally unreachable ones, vs enforcement-SCRIPT copies) |
| EK1 | (CONFIRMED, HIGH — MY round-52 rule on the fleet-synced gate) "a failure whose last real frame is the checker is the target's" was true of the PREMISE (the checker's only import is the target's) and false of the CONCLUSION: the import executes the whole `libs.subagents` graph, and ANY member's torn `.pyc` (>16 bytes, a current header) raises the same frameless EOFError — `libs/subagents/__init__.py`'s cache truncated on the REAL extract → `✗ … web_tools.py is present but unusable (EOFError: marshal data too short) — fix the module`, exit 1, with `web_tools.py` byte-perfect; restoring the cache → green (executed; a zero-byte or unreadable cache falls back to source — 2 of 2 shapes clean); `_scrub` was applied on 2 of 3 `_IMPORT_FAILURE` sites — `_target_is_broken`'s return (a mode-000 FILE) leaked the absolute path, and the `noperm` grader asserted only the exception name; a sourceless sibling `.pyc` made `_blame_for` name a `.py` that no longer exists; the trailing `tb[-1]` fallback was dead; "(a directory, or a dangling symlink)" enumerated two of at least four non-file shapes (a FIFO executed); the doc stated the frame rule as a certainty | native Opus, pass 53 (executed: 11 hubs, one LIVE red-and-restore on the real extract, 5 mutants) | FIXED · LOCAL — `_bad_cache_owner(target)`: with no source frame the CACHES are asked — the target's first, then its package siblings, then the parent package (`marshal.loads` of the body) — so the target's own torn cache BLOCKS and a sibling's is an advisory naming that module, both carrying `delete <package>/__pycache__`; none unloadable → "an unknown file"; the frame loop takes only an EXISTING file, the fallback is `""`; `_scrub` on every branch and of `Path.home()` (`~/…`); the message is "not a regular file"; the doc sentence; graders: hub `sibpyc` (a torn `__init__.py` cache → an advisory naming it, never a block), the `corruptpyc` assertion names the cache and the remediation, the `noperm` hub asserts the scrub, a vanished-frame + home-scrub unit; sandbox Y1 (the caches not asked): → 1 failed; Y2 (the target branch unscrubbed): → 1 failed; Y3 (a vanished frame trusted): → 1 failed; Y4 (the home not scrubbed): → 1 failed; Y5 (the owner search skips the target): → 1 failed · ACCEPTED with measurements: `_scrub` leaves the bare repo root (no trailing slash) and non-home absolute paths (0 of 21 vendored modules construct an ImportError with `path=`; the contract is the home and the repo); the gone-`.py` unit row exercises the `is_file()` clause, not the `.py` clause (its comment now says so; the `.py` clause is graded by the `cfg.yaml` row) |
| EK2 | (ROUTED — the sibling hunks my pass-52 commit swept in, their author mid-flight) `_summarize_skipped` keys on the `NOT INSTALLED` name marker — 3 of the gate's skip shapes carry it, the script-absent / `not installed, skipping` / `(NOT RUN)` rows do not: the Tier-2 JSON on the extract says `skipped: 1` beside 3 did-not-run rows, one of them the sibling's own citations row; INDEX documents `check_citations_resolve.py` + its test, both absent from HEAD — `Doc Link Integrity (live tree)` reds on a fresh checkout; the JSON keys ship with zero graders (1 of 57 test files referencing `final_gate` calls the function — the sibling's own in-flight test — and 0 assert the new keys) | native Opus, pass 53 (executed: the gate JSON, `git ls-files`) | ROUTED — mail `01M1KMF66S0HCR1XCC0QASMEQP` to fleet with the executed evidence and the suggested predicate; not edited here (the author is mid-flight on that surface — CLAUDE.md § Shared repo) |
| EK3 | texts: EI1's "0 of 15 `open`/`read_text` sites" (16 by grep) and "0 of 5 `audit()` call sites" (4 by AST); the doc's checker-frame sentence and a `_scrub` clause; CHANGELOG Round 53; INDEX (71 → 72) | native Sonnet + native Opus, pass 53 | FIXED · LOCAL |
| EL1 | pass-53 pool candidates: 5 code (qwen) / 4 ops (gemini) / 5 docs (deepseek) | pool fanout ×3 routed, pass 53 | REFUTED (14 of 14): IPv6 brackets (DZ1) · `_TEMPLATE_RE` "too broad" (0 of 455 categories start with `<`; the enum has none) · the `classify.lock` fd (DS5) · `bound_flagged` arithmetic (DP1) · a non-string `after` in `cursor.json` (`if not isinstance(cursor, str): cursor = None`, executed in DV1) · the heartbeat race (eighth raise) · `$LOG_FILE` "injection" (argv, quoted) · a reconsolidate failure after a paid classify (the paid results are in the catalog; the dashboard is withheld and the alert names the step — by design, DA-rows) · a SIGKILL mid-write partial 0600 tmp for ≤1 h (G7/BA1: PID-named, `sweep_stale_tmp`) · deepseek's five "point-in-time claims" in sibling DECISIONS/INDEX/CHANGELOG rows (a ledger row IS a dated measurement by contract — rows immutable) |
| EM1 | (CONFIRMED — MY round-53 cache-owner rule on the fleet-synced gate, two attribution edges) `marshal.loads(b"")` raises the same EOFError as a torn body, so a ZERO-BYTE target cache — which the import machinery ignores and recompiles from source — was named the owner before a truly torn PARENT cache (`libs/__pycache__`) was reached: a BLOCK on `web_tools.py` with `delete subagents/__pycache__`, a remediation that cannot fix the real cache (executed through `_live_web_tool_names` on a synthetic hub); a torn cache of the sibling `web_tools.py` imports DIRECTLY (`from .tools import` — the production import at `web_tools.py:36`) never reached the cache search: the target's own import line is a real, existing frame, so the frame rule returned the target → a BLOCK under `web_tools.py`'s name — exactly the class round 53 claimed closed, invisible to its `sibpyc` grader because `__init__.py` is imported before any target frame exists (executed through `audit()` on a hub mirroring the production shape) | native Sonnet ×2 (tests/docs + the adversarial substitute), pass 54 (executed) | FIXED · LOCAL — `_bad_cache_owner` considers only a cache Python would LOAD (a 16-byte header, the current `MAGIC_NUMBER`, and in timestamp mode a header mtime/size equal to the source's — a zero-byte, foreign or stale cache is skipped); the caches are asked whenever the failure's innermost frame is inside `<frozen importlib` (no source frame, the checker's frame, OR the target's own import line), and the frame's attribution is kept when no cache is torn (a missing sibling stays the target's BLOCK); graders: hub `zerotarget` (a zero-byte target cache beside a torn parent → an advisory naming `libs/__init__.py` with `delete libs/__pycache__`), hub `sibtoolspyc` (a torn direct-sibling cache → an advisory naming `tools.py`), `test_bad_cache_owner_ignores_caches_python_would_not_load` (zero-byte, foreign magic, stale header → None; current header + torn body → the source); sandbox W1 (the header gate removed): → 1 failed; W2 (the staleness gate removed): → 1 failed; W3 (the import-machinery trigger removed): → 1 failed · refuted in passing by the adversarial finder (executed): a CHECKED_HASH cache torn → found and Python's import does fail; 21 of 21 valid caches under `libs/` load — 0 false positives; the 21 modules `import libs.subagents.web_tools` loads all sit within the target's package or its parent — the search population is complete; both caches torn → the target wins; `Path.home()` with HOME unset falls back to the passwd entry |
| EM2 | texts: the Pass 53 row and CHANGELOG Round 53 said "287 tests (… + 7)" — the credit-fetchers suite collects 8 (288); EK2's "0 of 58 test files" — 57 reference `final_gate`, and 1 (the sibling's own in-flight test) calls `_summarize_skipped` without asserting the new keys; CHANGELOG Round 54 | native Sonnet, pass 54 | FIXED · LOCAL |
| EN1 | pass-54 pool candidates: 5 code (qwen) / 3 ops (gemini) / 1 docs (deepseek) | pool fanout ×3 routed, pass 54 | REFUTED (9 of 9): the `classify.lock` fd (DS5) · IPv6 brackets (DZ1; the finder's own text: "handled safely — no defect") · `bound_flagged` (DP1; "adjudicated as correct in prior passes" — its own text) · non-dict catalog metadata ("acceptable" — its own text) · a partial ripgrep exit ("documented as intentional" — its own text) · "the diff ends abruptly at line 2830" (the settled `pool-brief-truncation` class) · a failing alerter masking a stale heartbeat (exit 1 is preserved; DA-rows) · `_alert` without a `timeout` wrapper (the senders carry their own network timeouts: `libs/alerting/apprise.py:102` 12 s, `telegram.py:99` `TIMEOUT`; the daily lock bounds the run) · a "truncated" D-093 row (7 pipes, the separator's count — the same fabrication as EL1's D-088) |
| EO1 | (CONFIRMED — MY cache-owner rule on the fleet-synced gate, its remaining divergences from CPython) the header gate checked 3 of CPython's 5 conditions (`_classify_pyc` / `_validate_hash_pyc` / `_validate_timestamp_pyc` / `_compile_bytecode`, read at `/usr/lib/python3.12/importlib/_bootstrap_external.py`): a CHECKED_HASH cache whose source moved on (recompiled by CPython) was named the owner; reserved flag bits (rejected by CPython) were accepted; a body that unmarshals to a NON-code object (`ImportError: Non-code object in <pyc>`, `path` = the pyc) was passed as loadable — the target's own broken cache became an advisory on the pyc's path, exit 0 (executed, each); the search ran `[target, siblings, parents]` while the import executes parents first — with both `libs/__init__` and the target torn the remedy named `subagents/__pycache__` alone and, followed, left `rc=1 EOFError` (executed); a NUL-padded SIBLING (a SyntaxError with no filename, frames stripped) and a DIRECTORY at a sibling's path both blocked under `web_tools.py`'s name — the target compiles clean, the importer is the last frame (executed); a torn cache of the DIRECTLY imported sibling and a zero-byte target cache (EM1, pass 54) were the same class one edge over | native Opus + native Sonnet ×2 (pass 54's substitute, pass 55), (executed: 15 hubs, CPython read, 4+4 mutations) | FIXED · LOCAL — `_cache_would_fail` asks CPython's own validators (`_classify_pyc`, the hash for `flags & 0b10`, `_validate_timestamp_pyc`), then `marshal.loads` and the code-object check; a manual-rules fallback for an interpreter without those names; `_bad_cache_owners` returns EVERY torn cache in `_import_order` (parents, siblings, target) — the target among them ⇒ BLOCK, else an advisory naming the first, the remedy naming every `__pycache__`; `_bad_source_owner` gives siblings the target's compile-first check when the frame says the target; a `.pyc` in `exc.path` triggers the cache search; the docstrings; the doc paragraph; graders: hubs `nulsib`, `dirsib`, `nopermsib` (root-skipped; the frame kept, never "unknown"), `staletarget` (a stale header end-to-end), `noncode`, `bothtorn` (both named, both dirs), `raiseplustorn` (a raise beside a coincidental torn cache — the raise named); unit rows: size-only stale, a 17-byte cache, reserved flags, a checked-hash cache current and stale, a non-code body; sandbox U1 (the hash comparison removed): → 1 failed; U2 (manual rules without the reserved-bits check): → 1 failed; U3 (a non-code body accepted): → 2 failed; U4 (the sibling source check removed): → 1 failed; U5 (first-torn, target-first): → 1 failed; U6 (the `.pyc` trigger removed): → 1 failed; V1 (the caches always consulted): → 1 failed; V2 (the size half removed): → 1 failed; V3 (the header threshold widened): → 1 failed |
| EO2 | texts: the reference doc's cache sentence (loadable-cache rule, the widened trigger, every torn cache named) — round 54 had changed the behaviour without the doc named in the script's own `AFTER-EDIT` header; the `_bad_cache_owner` docstring's "the order the import executes them" (it was the reverse) and "only a cache Python would load" (false for hash-based and reserved-flag caches); CHANGELOG Round 55 | native Sonnet + native Opus, pass 55 | FIXED · LOCAL |
| EP1 | pass-55 pool candidates: 4 code (qwen) / 5 ops (gemini) / 1 docs (deepseek) | pool fanout ×3 routed, pass 55 | REFUTED (10 of 10): the `classify.lock` fd (DS5) · an IPv6 zone id (0 of 119 `url` values) · curated-entry merge losing a model field (AD2/AU9 — the accepted-category merge keeps operator fields and takes the model's for `?` ones, by design) · a truncated fourth item · `$LOG_FILE` "injection" (the body is `sys.argv[2]`; the only interpolation is the fixed `$FABRIK_ROOT`) · the 30 h liveness budget "tight" (DA rows; the run takes 23 s) · a disk-full EMPTY stamp (DA3 — `date > tmp && mv`: a failed redirect fails the chain and keeps the previous stamp) · the orphaned tmp (DZ1) · "unverifiable imports" · the CLAIMS pack comment naming `docs/reference/apis/EXTERNAL_SYSTEMS.md` "which lives at docs/EXTERNAL_SYSTEMS.md" (it lives exactly where the comment says — read at pass 9, line 4; the finder invented the diff header) |
| P26 | Vercel block `Env keys: none` vs posture naming `VERCEL_DEPLOY_HOOK_URL` | pool docs | FIXED · LOCAL — folded into P5's rewording ("none in the fleet today … the posture row names the knob a consumer would add") |
