# Review — docusaurus governance-doc publish exclude (fleet+infra finding 01M19JJNWK)

**Surface:** HEAD 818f88bc · diff-md5 ff0927c0f961608e3244443642eed706 (final surface: src/fabrik/scaffold.py + tests/test_scaffold_doc_seeding.py; the .j2 edit was reverted, see F4)
**Scope:** operator-named + security-relevant generator fix — internal Fabrik docs stay PRESENT in docs/ but content-docs-EXCLUDED from the docusaurus published site.
**Status:** DONE — round 2 returned new: 0, every class adjudicated.

## Rubric

Armed with `python3 scripts/review_rubric.py --changed src/fabrik/scaffold.py tests/test_scaffold_doc_seeding.py` (verbatim output header):

```
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
### core/25-data-postgres.md
### core/30-ops.md
### 12-FACTOR (all axes)
```

FLOOR: core/35-security-auth · core/25-data-postgres · core/30-ops · 12-FACTOR (all axes). The security-relevant class here is the LEAK (internal docs → world-readable site).

## Finders

- **Pool breadth** (`fanout("review", …, mode="read_only")`, 2 units, flywheel-recorded + `set_quality`): raised the two-source drift, glob-nesting, and a "5 defaults / .spec." default-set claim.
- **Native Opus** (`fabrik-reviewer`, `model: opus`): authoritative pass — `node --check` on a generated config (rc 0), integration-seeded a docusaurus project (DECISIONS/LESSONS/BACKLOG land), grounded the docusaurus 3.x `DEFAULT_OPTIONS.exclude` (4 patterns), surfaced the pipeline-generated-doc gap (F1).

## Pass Ledger

- **Pass 1** (finders: pool ×2 + native Opus ×1 — leak-closure, seeding-regression, JS-validity, default-completeness, alternate-leak-paths, missing-doc, test-quality): found: 9 | new: 9 | fixed: 5 → not done (changed code). Refuted 4 (below); fixed 5 (F1–F5).
- **Pass 2** (method: **re-derivation** — regenerated the config and re-counted every `_DOCUSAURUS_UNPUBLISHED_DOCS` entry present as `**/<name>` inside the exclude ARRAY [12 = 4 defaults + 8 internal, 0 missing]; re-ran `_type_seeds_doc(reg, "docusaurus", …)` for the seeded governance docs [still seed]; confirmed the .j2 reverted to HEAD [single source of truth]; `node --check` rc 0; pytest 13/13): found: 0 | new: 0 | fixed: 0 → EXIT.

## Coverage Checklist

| Class | Verdict |
|---|---|
| leak-actually-closed | FIXED — content-docs `exclude` removes the internal docs from the only publish path (sidebars are `autogenerated` from the same content set; no static/ copy). Opus read `_scaffold_docusaurus` in full. |
| ledger-still-seeded | CLEAN — change touches only `config_js`; seeding loop untouched. Test asserts `_type_seeds_doc(reg,"docusaurus","docs/DECISIONS.md")` (+LESSONS/BACKLOG) still True. Opus integration-seeded and confirmed the files land. |
| pipeline-generated-docs-leak | FIXED (F1) — docusaurus is UI-bearing → /fabrik-flows + /fabrik-ui-design write docs/flows.md, ui-design.md, design-system.md into the published tree; added all three to `_DOCUSAURUS_UNPUBLISHED_DOCS`. |
| glob-nesting | FIXED (F3) — bare `'DECISIONS.md'` matched only the docs root; rendered as `'**/<name>'` so a nested copy is caught. |
| two-sources-agree | FIXED (F4) — the docusaurus.config.js.j2 is confirmed NEVER rendered (grep; `_scaffold_docusaurus` reads only package.json.j2/Dockerfile.j2). Reverted the .j2 edit → one source of truth (the constant → inline config_js), eliminating the drift trap. |
| default-excludes-preserved | CLEAN — the 4 re-listed defaults exactly match docusaurus 3.x `@docusaurus/plugin-content-docs` `DEFAULT_OPTIONS.exclude` (Opus-grounded; the pool's "5th `.spec.` pattern" claim REFUTED — not in the plugin default). |
| generated-JS-valid | CLEAN — generated a real config, `node --check` rc 0; f-string interpolation well-formed (quotes/commas; trailing comma legal in JS arrays). |
| test-scope (behavior-without-a-test, standing) | FIXED (F5) — test asserts entries INSIDE the exclude array (not merely in-file), defaults preserved, AND the PRESENT half (docs still seed). Red-on-revert proven. |
| fail-open/fail-closed (standing) | CLEAN — no gate/guard logic in this change; a generator config edit. |
| cost/quota/limit edges (standing) | CLEAN — n/a. |
| boundary/sentinel/prefix (standing) | CLEAN — the `**/<name>` globs are exact per-file; the exclude-array membership is asserted by position, not a loose in-file substring. |

## Refuted (proof)

- **"docusaurus default has 5 excludes / a `.spec.` pattern"** (pool) — REFUTED: docusaurus 3.x `DEFAULT_OPTIONS.exclude` is exactly the 4 patterns re-listed (Opus grounded against the plugin default; official docs example shows the same 4).
- **".j2 uses no quotes around filenames"** (pool finder-0) — REFUTED: the .j2 used single quotes (and is now reverted anyway).
- **"two-source drift is a live leak"** — REFUTED as a leak: the .j2 is never rendered, so it produces no real site config; addressed structurally by reverting it (F4).
- **"governance doc leaks via static/ or an MDX import"** (pool) — REFUTED for the scaffolder: `_scaffold_docusaurus` writes no static/ copy of docs and no MDX importing them; user-authored imports are out of the generator's scope.

## Gate

```
$ pytest tests/test_scaffold_doc_seeding.py  →  13 passed
$ ruff check src/fabrik/scaffold.py tests/test_scaffold_doc_seeding.py  →  All checks passed
$ mypy src/fabrik/scaffold.py  →  Success: no issues found
$ node --check <generated docusaurus.config.js>  →  rc 0
```
(Full `final_gate --json` not cited on the whole tree — it carries other sessions' uncommitted WIP; no red cites my two files.)

Exit: 9 candidates → 5 FIXED + 4 REFUTED (sum = 9); round 2 re-derivation raised new: 0; every class adjudicated. Measured 0 live docusaurus projects fleet-wide, so the generator fix closes the class with no retro-fix.
