# T26 — /fabrik-service-test: 63b manifesto conformance

Status: DONE

Surface: commands/_sources/fabrik-service-test.md (281 lines, read in full; grep-derived anchors) + the RENDERED command `~/.claude/commands/fabrik-service-test.md` (668 lines: composed from run-record · autonomy-run · term-coverage (D-048-current post-T22) · injection · cert-board-contract · cert-visual-deliverable · cert-execution · cert-handoff-grammar + close-feedback — all fragments T01-swept, term-coverage T22-fixed; re-rendered at merge).
Outcome: 2 fixes verifier-driven — the P0-P3 severity-assignment criteria landed at the cert-handoff-grammar FRAGMENT root (heals /fabrik-user-test too: blast-radius tiers, take-the-higher-when-torn, the waiver-mints-down rule), and the credential carve-out landed in-source (pool workers never hold credential material — native or scoped-throwaway-revoked; the T21/T25 class, third occurrence).

## 63b Verdict Table

| intersection | verdict (grep-derived SOURCE anchors) |
|---|---|
| (a) checkable gates | FIXED (fragment root) — the HANDOFF P<0-3> digit had NO assignment criteria anywhere in the 668-line render while /fabrik-release BLOCKS on it and the grader parses-never-validates it (check_review_coverage.py:1288 discards the captured severity); cert-handoff-grammar now tiers by blast radius (P0 data-loss/tenant-crossing/auth-bypass/money · P1 journey-blocking/fail-open · P2 workaroundable/latency/error-shape · P3 doc-drift/cosmetic) with take-the-higher-when-torn and the operator-waives-DOWN rule (their waiver mints, per T20). Otherwise conformed — the term-coverage fragment supplies the coverage-adjudicated exit with D-048 counting (post-T22); Phase 5 is discovery-until-dry with the four modes re-run against the CURRENT service (:233-239); coverage fractions each carry the scenario IDs proving them (:272-275); the handoff grammar is machine-graded (cert-handoff-grammar fragment; /fabrik-release enforces rows by SEVERITY, :230); the mechanical path-gate auto-reclassifies — "no judgment call, the diff decides" (:193-195) |
| (b) ledger routing + one-way field block | N/A for mints, CONFORMS on routing discipline — "Never decide a product question inside a test run" (:216): a missing endpoint is NEW WORK routed to the spec pipeline whose approval mints (the T18→T28 chain); contract-mismatch behavior is refuted OR a contract-change proposal, "say which" (:179-180); rulings received ride close-feedback. One-way field block N/A — the gauntlet never touches prod (HARD STOP) and its artifacts are tests + reports |
| (c) rigor scales with irreversibility | FIXED — the credential-to-pool vector was open: pool workers drive authed journeys, so credential material (sandbox keys are real paid credentials) entered pool prompts; now a credentialed journey runs NATIVE or on a scoped throwaway credential minted-and-revoked per round, "never paste a shared fixture token into a pool unit's task text" (:252-259 — the T21/T25 class, third occurrence in this plan). Otherwise conformed — the isolated-dataset HARD STOP: never against production, the VPS fleet, or shared postgres-main/redis-main, "destructive and abuse-limit scenarios are in scope here, so an isolated DB/queue/bucket is non-negotiable" (:48-54); native agents reserved for auth/tenant-isolation/migrations/concurrency/destructive-degradation legs (:250-251, :260-261); cost-bearing externals run sandbox/recorded or become BLOCKED-env findings (:52-54); "never degrade shared or paid infrastructure to manufacture a red" (:198-201) |
| (d) labeled verified/assumption evidence | CONFORMS — payloads are READ, not just schema-checked: "schema conformance proves shape, never truth" (:159-164); every FAIL reproduced ×2 before CONFIRMED, "'Should work' is a void verdict" (:165-168); RESPONSE truth vs SYSTEM truth — a 200 over a missing side-effect is a CONFIRMED fail-open defect, and the inverse too (:169-173); rig-first refutation with the live near-miss recorded (:180-187); code-wrong rows carry WIRE/STATE evidence, "never the assertion text alone" (:218-222); ledger freshness re-runs the repro — "its current color decides, not the ledger's" (:223-228); runtime.md SHA discipline separates "the fix didn't work" from "the service predates the fix" (:55-62) |
| (e) captured disorder | CONFORMS — BLOCKED-env findings are reported, never silently skipped (:52-54); pool-unavailable degrades HONESTLY: recorded, breadth done inline, zero-flywheel-rows noted with why — "the obligation degrades honestly; it never just vanishes" (:256-259); evidence copied to the board's evidence/ dir BEFORE recording so a sibling's cleanup can't flip an earned row (:151-157); SKIPPED list with reasons in the report (:277-278); close-feedback rides |
| (f) most-reversible default under ambiguity | CONFORMS — fix-or-handoff with NO silent bucket (:177); RIG-FIXED may never linger as a permanently-red committed test (:205-207); handoffs ship a committed RED repro + a routed owner, and routes EXECUTE in Phase 6 of the same run — "a handoff defers sequencing, it never exports the work" (:229-230); "a certification that detours into a plan abandons its own coverage loop" (:209-210); type-vs-surface mismatches are findings, not guesses (:22-29) |

6/6 adjudicated: 3 CONFORMS, 2 FIXED, 1 N/A.

## Scoped verification review (nested /fabrik-review)

| round | findings | disposition |
|---|---|---|
| 1 — author-blind fabrik-reviewer verifier | 4 candidates: **2 CONFIRMED** (severity criteria absent from source + all 8 fragments while release gates on the digit and the grader discards it — fixed at the cert-handoff-grammar fragment root, healing /fabrik-user-test too; the credential-to-pool carve-out missing while pool workers drive authed journeys — the exact class T21/T25 fixed on sibling surfaces, missed by my all-CONFORM stamp a third time → in-source carve-out) · **1 explored-and-REFUTED** (the term-coverage vs cert-execution dual vocabulary is a DELIBERATE carve-out — term-coverage.md:12 names both gauntlets as keeping discovery-until-dry unchanged) · **1 clean sweep** (all 24 anchors exact, both counts exact, api-smoke-test exists, grader machinery confirmed, release § Precondition matches T20, board-vs-report namespaces distinct by design, DESIGN-GAP disposition adequately routed via cert-execution + close-feedback). The all-CONFORM stamp fell for the 26th time | 2 fixes (1 fragment-root, 1 in-source) + artifact re-adjudication |
| 2 — closing re-derivation sweep | found: 0, fixed: 0 — the fragment tiering re-read (cert-handoff-grammar :4-11), the carve-out re-read (:252-259), remaining anchors re-confirmed | TERMINAL no-op |

Verifier falsification streak: 26-for-26 — headline: the severity digit that gates releases had no assignment criteria anywhere in the corpus, and the credential class fell for a third time on a fresh surface.

## Per-finding disposition ledger

1. Severity criteria absent (CONFIRMED, systemic) → fragment-root fix in cert-handoff-grammar (tiers + take-higher + waiver-down); /fabrik-user-test inherits on render.
2. Credential-to-pool vector (CONFIRMED, T21/T25 class) → in-source carve-out: native or scoped-throwaway-revoked (:252-259).
3. Dual termination vocabulary (explored) → REFUTED: deliberate carve-out, term-coverage.md:12 names both gauntlets.
4. All other angles → clean, verifier-enumerated.
