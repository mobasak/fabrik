# T06 acceptance review — the code half of Integration (scaffold markers + docs) (plan 2026-09-06-plan-2-multi-agent-adoption)

**Status:** CONVERGED
**Surface:** `git diff 5cd2ff55..worktree-agent-ad6f11a764e3a69ac -- src/fabrik/scaffold.py tests/test_scaffold_doc_seeding.py docs/reference/multi-agent-operating-model.md templates/governance/CLAUDE.md` (+5 scaffold literal, +94/−3 tests, +16/−2 operating model, 1/1 template; native Sonnet coder 95d42942). The receipt half of T06 (fire-rate proof, seam run, doc receipts, whole-plan gate, docs-review) is the orchestrator's — `2026-09-06-plan-2-multi-agent-adoption-review.md`.

## Coverage Checklist

Rubric invocation (verbatim head of the output):

```text
$ python scripts/review_rubric.py --changed src/fabrik/scaffold.py tests/test_scaffold_doc_seeding.py docs/reference/multi-agent-operating-model.md templates/governance/CLAUDE.md
# REVIEW RUBRIC — inject into EVERY finder prompt (generated by review_rubric.py)
# Honesty (L1): this arms the review — it raises compliance probability, it does not guarantee it.

## FLOOR — always injected, regardless of glob (spec L3)

### core/35-security-auth.md
**The default for ALL new projects, including user-facing SaaS + mobile.** Vendor `fabrik-lib/fastapi-user-auth`: the app issues its own JWTs — **Argon2id** (the vendored argon2-cffi defaults meet OWASP minimums; never Argon2i) + timing-equalized login, atomic refresh-token rotation (`DELETE … RETURNING`), JWT `jti` denylist revocation, and dual-mode tenant-isolation RLS. Supabase is retired as a default (see `agents-fabrik.md § Supabase`); reach for Pattern B only for a project that *already* runs on Supabase Auth.
- Do not use NextAuth.js, Clerk, Auth0, or Firebase Auth.
- ADDITIONAL affordance a project justifies, never the default door.
- project files the fabrik-lib request FIRST, never hand-rolls WebAuthn.
| `chrome-extension` | ✅ **use this** | ⚠️ only via `chrome.identity.launchWebAuthFlow` + the `https://<ext-id>.chromiumapp.org/` redirect the pack already mandates; a bare mailed link lands in a TAB that cannot reach `chrome.storage.session` |
| `desktop-app` | ✅ **use this** | ⚠️ needs a registered custom protocol handler; the token then goes to `safeStorage` (`desktop-app/72-desktop.md`) |
- service MUST be able to say which:
| **Another Fabrik service** (Docker-to-Docker on the `fabrik` network) | `X-Internal-Token` + `internal_auth.py`, `hmac.compare_digest`, 403 on reject | § Internal Service Auth (M2M) below — **never** an inline `APIKeyHeader`, never a per-service key name |
```

| Class | Status |
|---|---|
| core/10-python.md — the scaffold literal + the test file | CLEAN (ruff check clean on both; format clean on the test) |
| core/40-documentation.md — commands/docs are rules, not changelogs; trailers | CLEAN (present-tense adoption bullet; D-154/D-155 cited; trailers parse) |
| FLOOR 35/25/30 · 12-FACTOR | CLEAN (no surface) |
| Recurrence: Behavior Contract row without a killing test | FIXED r1 → r2 (native D-B: the template's 'tail sweep' assertion was vacuous — master never had the phrase) |
| Recurrence: Touches discipline / governance untouched | CLEAN (`--name-only` = the four Touches; the receipt is the orchestrator's) |
| Recurrence: fleet blast radius of a synced surface (the project contract) | CLEAN (native (4): one sentence inside the 'MORE THAN ONE AGENT' bullet, inert for a one-session project; `docs_updater.py` synced per the manifest) |
| Recurrence: proxy-as-evidence — the ticket gate RUN by the orchestrator | CLEAN (24 passed with `PYTHONPATH=<worktree>/src`; the first run without it graded the MAIN checkout's scaffold.py — 2 failed — the worktree-venv shadowing trap, now a LESSONS entry) |
| Recurrence: denominator on every count | CLEAN — 24 tests of 24 collected; 4 files of 4 Touches; the seed block compared byte-for-byte against `run_adopt`'s `ownership_block` |
| Recurrence: fail-open/fail-closed | CLEAN (the scaffold test asserts `endswith` the exact block; a fresh scaffold regenerates under `--sync`) |
| Recurrence: cost/quota accounting | CLEAN (pool r1 1/3/2 scored — a single-family deepseek draw; native opus×1 r1) |
| Recurrence: boundary/sentinel/prefix | CLEAN (native (2): the block ordering mutant — above the hand table — caught by the `endswith` assertion) |
| Recurrence: behavior-without-a-test | FIXED r1 → r2 (native D-B: the template's 'tail sweep' assertion was vacuous — master never had the phrase) |

## Round 1 — over `5cd2ff55..95d42942`
Finders: pool deepseek/deepseek-v4-flash ×2 + deepseek/deepseek-v3.2-exp (single-family draw) + native opus×1 — round 1.

### Adjudication (pool layer)
- deepseek-v4-flash (1/5): "the fire-rate proof is missing from the diff" and an invented `docs_updater.py:1357` requirement — REFUTED: the proof is the orchestrator's receipt half (T06 Scope (2)), run read-only over the 45 targets and pasted in the whole-plan receipt.
- deepseek-v3.2-exp (3/5): CLEAN, three rows mapped, the proof correctly read as the orchestrator's. Accepted.
- deepseek-v4-flash (2/5): "`sync_plans_index()` returns `changed=False` on a fresh scaffold so the test fails on new code" — REFUTED by execution (17 and 24 passed under the worktree's `src`); the "in place of the undeclared line" reading — a docstring nit, the fresh scaffold has no owner and the UNDECLARED header is the contract.

### Orchestrator execution
- `from fabrik import scaffold` with `PYTHONPATH=<worktree>/src` → the worktree's file; `pytest tests/test_scaffold_doc_seeding.py tests/test_scaffold_git_config.py -q` → **24 passed**; ruff clean.
- A fresh `_scaffold_shared` into scratch: `docs/development/PLANS.md` ends with `run_adopt`'s `ownership_block` byte-for-byte (`endswith` True; tail repr checked).
- `grep -c 'tail sweep'` → 0 in both docs; `grep -c -- '--adopt'` → 1 in each.

### Native finder (opus) — 6 hunt items executed; 3 raised + 1 residue
- (1) the seam literal: `ownership_block` from `docs_updater.py:1940-1943` vs the scaffold literal at `:1455-1460` — `repr()` EQUAL; a live scaffold ends with it and `sync_plans_index()` regenerates with `<!-- Merge owner: UNDECLARED` — CLEAN. (2) both mutants (heading dropped; block above the table) caught by `test_fresh_scaffold_plans_md_already_carries_the_ownership_markers:301` — CLEAN.
- **(3) DEFECT M** — the operating-model doc `:81-84` says NEITHER advisory fires inside a worktree; only the SessionStart hook has that suppression (`session_orient.py:163`), `validate_ownership_advisory()` guards the count and the hub sentinel (`docs_updater.py:1805-1822`). → FIX: split the sentence per advisory. Every other claim in the bullet verified against the merged code (refusal `:1929-1935`, `**Owner:**` `:1886`, the three shapes `:1022-1067`, last-row-wins `:1173-1195`, `--merge-owner REPO_DIR` `:202`, the header `:1205-1208`).
- (4) the template: numstat 1/1, one sentence inside bullet (d), gated by "MORE THAN ONE AGENT" — CLEAN.
- **(5) D-A H → adjudicated CLEAN-BY-DESIGN**: the ticket's gate is red INSIDE the worktree because the symlinked venv's editable `.pth` pins `/opt/fabrik/src` (master's scaffold.py); with `PYTHONPATH=<branch src>` 24 passed, with master's scaffold both new tests fail (red-on-revert proven). The merge tooling runs the gate in the MAIN checkout after applying the diff, which is where it grades the right file; the trap is a LESSONS entry at T06. **D-B L** → the vacuous 'tail sweep' assertion on the template → r2.
- **(6) D-C H → REFUTED**: CHANGELOG is an orchestrator-applied Delta at merge by the plan grammar (every ticket so far). **Residue (M)**: master's `docs/development/PLANS.md:11` still says "agent-1's tail sweep fills it" — T02a retired the phrase in `_PLANS_PHASE_NOTE` and `--check` reports the block stale; → the T06 merge fixup runs `docs_updater.py --sync` in the main checkout and stages the regenerated block.

Verdict r1: 2 text items → fixup dispatched to the same coder; the PLANS regen absorbed into the merge fixup. Board T06 stays 🔵.

## Round 2 — over `5cd2ff55..56b2d231` (the round-1 fixup 56b2d231: operating-model doc +2, tests +26)
Finders: pool deepseek/deepseek-v4-flash ×2 + deepseek/deepseek-v3.2-exp (single-family draw) + native: the orchestrator's own EXECUTED verification (two text fixes prescribed verbatim; the native Opus seat ran in round 1) — round 2.

### Adjudication (pool layer)
- deepseek-v4-flash ×2 (1/5 each): "the fire-rate proof row has no test in this diff" — REFUTED twice: the proof is the orchestrator's receipt half, RUN read-only over the 45 targets with its denominators in the whole-plan receipt; "`REPO_ROOT` undefined" — REFUTED, defined at the test file's head (the suite passes).
- deepseek-v3.2-exp (3/5): CLEAN. Accepted.

### Orchestrator execution
- In the coder's worktree with `PYTHONPATH=<worktree>/src`: `pytest tests/test_scaffold_doc_seeding.py tests/test_scaffold_git_config.py -q` → **25 passed**; ruff clean.
- The split sentence read at `docs/reference/multi-agent-operating-model.md:84-86`: "Both never fire in a single-session repo or in the hub. The SessionStart line additionally never fires inside a worktree … the `--check` advisory carries no such guard" — exactly the merged code's behaviour (`validate_ownership_advisory` guards the count + the hub sentinel; `_sessions_line` adds the worktree check). The new grep-test's red quoted by the coder.
- The template's drift-guard assertion now names itself as such.

Verdict r2: found: 0, fixed: 0 — **CONVERGED**. Merge target: `worktree-agent-ad6f11a764e3a69ac` @ 56b2d231, with the merge-time fixup (two STRATEGIC_BACKLOG rows, the hub PLANS-block regen via `docs_updater.py --sync`), the LESSONS delta and the CHANGELOG delta; `templates/governance/CLAUDE.md` is fleet-synced → `sync_enforcement_to_projects.py --force` after the merge.

## Pass Ledger
| Pass | Layer | Method | Findings → fixed |
|---|---|---|---|
| Pass 1 | pool ×3 (single-family) + native opus×1 | method: re-derivation (the seed block `repr()`-compared to `run_adopt`'s constant; two mutants; every doc claim checked against the merged code at path:line; the gate re-run with the right `PYTHONPATH`) | 2 text + 1 residue → 3 |
| Pass 2 | pool ×3 + orchestrator execution | method: re-derivation (25 tests re-run under the worktree's src; the split sentence re-read; the drift guard re-read) | 0 → 0 |
