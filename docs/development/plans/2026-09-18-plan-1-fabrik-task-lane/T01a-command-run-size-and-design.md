# T01a — `command_run.py`: SIZE at `start` (`--file`/`--declare`, the inventory) and `step --design`

## Scope
Implements spec § Chosen approach, Phase 0 (the SIZE inventory, the refusal enumeration, the persisted `declared`, the `RECORD:` print, the stdlib regex extraction with its fail-open arm) and Phase 2 (the `design` record field via `step --design`), restating nothing those sections already say. Every new behaviour is scoped to `--command fabrik-task`; every other command's `start` and `step` output is byte-identical to today's. DO-NOT: touch `_close`, the ledger row or any close verb (T01b); add any import outside the stdlib (`scripts/command_run.py:46-60`); read `CLAUDE.md` (T01b's exclusion parse); change `skill_router.py`.

Depends: —
Parallel: ⚡
Complexity: native
Gate: uv run pytest tests/test_command_run_fabrik_task.py -q
Docs: CHANGELOG (Deltas) · `docs/reference/command-run-protocol.md` § CLI gains the `--file`/`--declare`/`--design` lines — owned by T03, cited here

## Touches
- scripts/command_run.py — PRIMARY PATH
- tests/test_command_run_fabrik_task.py

## Behavior Contract
- **Given** `start --command fabrik-task` with no `--file`, **When** it runs, **Then** it exits 1 with `REFUSED — fabrik-task: missing --file` and no record opens — and an exception raised anywhere inside the lane block exits 1 with `REFUSED — fabrik-task: start could not complete (<exception class>)` too, never rc 0 with no record (`scripts/command_run.py:2557-2559`) (scripts/command_run.py:2360-2368; spec § Chosen approach, Phase 0)
- **Given** `start --command fabrik-review --file a.py`, **When** it runs, **Then** it exits 1 with `REFUSED — --file/--declare belong to --command fabrik-task` and every other command's start output is byte-identical to today's (scripts/command_run.py:2360-2368; spec § Chosen approach, Phase 0)
- **Given** a declared path that matches the governance-sync regex read from the hub's `.pre-commit-config.yaml` `- id: governance-sync` block, **When** `start` runs with `mechanism=no`, **Then** it exits 1 with `REFUSED — fabrik-task: sync → right-now + /fabrik-review`; with `mechanism=yes` also declared the lane named is `/fabrik-spec` (scripts/governance_sync_postcommit.sh:28-30; .pre-commit-config.yaml:149-155; spec § The decision rule)
- **Given** a declared path that is tracked, present and modified, or present and untracked, **When** `start` runs, **Then** it exits 1 with the dirty-at-start message; an absent declared path starts (scripts/command_run.py:2583, :2623-2663; spec § Chosen approach, Phase 0)
- **Given** a valid declaration, **When** `start` succeeds, **Then** the record carries `declared` with the seven keys and `sha`, and stdout carries `RECORD: <started_at>` after the pinned line (scripts/command_run.py:2636, :2682; spec § Chosen approach, Phase 0)
- **Given** the stdlib extraction of the `files:` scalar, **When** compared to PyYAML's read of the same file, **Then** they are equal on the live file, and an absent or empty scalar yields `sync_test: unavailable` plus the stderr line `⚠ fabrik-task: sync lane test SKIPPED (cannot read the governance-sync filter)`, never an empty regex and never a silent skip (scripts/governance_sync_postcommit.sh:44; spec § Chosen approach, Phase 0)
- **Given** `step --phase 2 --design <path>` on a `fabrik-task` record, **When** the file is ≤ 2,000 characters, **Then** `rec["design"]` holds its text and no later `step` overwrites it (a second `--design` is kept out with a stderr NOTE); over 2,000, or on a path that cannot be read, it is REFUSED with the phase NOT advanced (scripts/command_run.py:2370-2377, :1602; spec § Chosen approach, Phase 2)
- **Given** a `fabrik-task` record in a repo with no commits, **When** `start` runs, **Then** the record carries `sha: unavailable` and the dirty check is skipped (scripts/command_run.py:2636; spec § Chosen approach, Phase 0)

## Steps (the coder's order)
1. Toolchain preflight: `uv --version` and `.venv/bin/python -c "import yaml"` (PyYAML is present in the hub venv for the GRADER only — `command_run.py` never imports it).
2. Red first: write `tests/test_command_run_fabrik_task.py` with the `_cr`-shaped harness copied from `tests/test_command_run.py:43-82` (the span runs to the closing `)` — `:80-82` are `cwd=...`, `env=env`, `)`, and a copy cut at `:80` is both env-less and syntactically broken) (a `COMMAND_RUN_DIR` under `tmp_path`, `KAIZEN_EVENTS_DIR` beside it, `CLAUDE_SESSION_ID`, the auto-injected `--feedback`) and the git-repo fixture shape of `:495-510`; run it and watch the eight tests FAIL for the right reason (unknown `--file` flag; no `RECORD:` line; no `design` key) — one of the eight drives the sync fail-open through `FABRIK_HUB_ROOT` and asserts both `sync_test: unavailable` and the stderr SKIP line.
3. `start` parser (`scripts/command_run.py:2360-2368`): add `--file` (`action="append"`, `metavar="PATH"`) and `--declare` (`k=v,…`); inline in `_mutate` where `start` is handled (`:2583`), BEFORE the record literal at `:2623` is built. ⚠️ Bind `_cmd = (args.command or "").lstrip("/")` ONCE and test `_cmd`, never raw `args.command`: the record itself normalises at `:2625`, so a raw-args guard is bypassed entirely by `--command /fabrik-task` (executed 2026-09-18 — a record opened with `command: 'fabrik-task'` and NO `declared` key, the SIZE gate skipped, while `_close` keys on the normalised name and then demands `--commit`), and the mirror is a wrongly-refused `--command /fabrik-task --file a.py`. ⚠️ Wrap the whole lane block in `except Exception` (the BARE class — dropping `check=True` removes `CalledProcessError`, and the escapes INCLUDE `subprocess.TimeoutExpired` from `timeout=10`, `FileNotFoundError` when git is absent, `OSError`/`UnicodeDecodeError` from reading a file, `re.error` from the extracted scalar and `ValueError`/`KeyError` from the `--declare k=v` parse — the bare class is chosen because this list is not closed) that RETURNS 1 with `REFUSED — fabrik-task: start could not complete (<exception class>)` — a template of its own, so the arm is assertable by string and not only by rc — an escaping exception becomes rc 0 at `:2557-2559` and writes nothing. Then: if `_cmd != "fabrik-task"` and either flag is present → print the cross-command refusal, `return 1`; if `fabrik-task`: the guard order — required flags (each gap its own line) → path validity (normalise to repo-root-relative against `_repo_root()`; outside-repo and directory refusals) → dirty-at-start (`git diff --quiet HEAD -- <p>` non-zero with the file present, or present-and-untracked; skipped with `sha: unavailable` when `git rev-parse -q --verify HEAD` is rc 1) → the two lane tests (`len(files) > 3`; the sync regex) and the five declared answers evaluated together with the spec-chain precedence (`mechanism`/`oneway`/`tradeoffs`/fourth file → `/fabrik-spec` even when sync or `heavy` also tripped; sync or `heavy=yes` alone → `right-now + /fabrik-review`; `decision=no` alone → `right-now + /fabrik-review-scoped`), the parametrised lane message `REFUSED — fabrik-task: <test> → <lane>`.
4. The sync regex: a stdlib function that opens `Path(os.environ.get("FABRIK_HUB_ROOT") or "/opt/fabrik") / ".pre-commit-config.yaml"` — ⚠️ the `or` form, NEVER the two-arg `get(k, default)`: executed 2026-09-18, an EMPTY `FABRIK_HUB_ROOT` (a wrapper exporting an unset variable) makes the two-arg form yield the RELATIVE `.pre-commit-config.yaml`, i.e. the running repo's copy — and all 38 non-hub `/opt/*/.pre-commit-config.yaml` files carry a governance-sync scalar that DIFFERS from the hub's, so the lane's most consequential test would silently run on the wrong regex with no `unavailable` signal. The `or` form is also this file's own convention (`scripts/command_run.py:1798`, `:3097`; the two-arg form with a meaningful default appears at none of its ten `os.environ.get` sites). The DEFAULT is the hub's absolute path the spec mandates (never the running repo's copy); the override exists ONLY so a grader can reach the fail-open arm, as `COMMAND_RUN_DIR` (`:85`) and `COMMAND_RUN_TRANSCRIPT` (`:1998`) already do in this file. ⚠️ Without it that arm is UNREACHABLE by any test — a throwaway repo still reads the real, healthy hub file — and Coverage Checklist row 5 would claim a fail-open check no grader performs. The grader points `FABRIK_HUB_ROOT` at a tmpdir holding a config whose `files:` scalar is empty, locates the `- id: governance-sync` block and its single-quoted `files:` scalar (the SIXTH of six `files:` scalars in the file — anchored by the id, never the first match and never a positional index), unescapes `''`, compiles it with a BARE `re.compile` (never `re.X`: the canonical consumer is `grep -qE` in `scripts/governance_sync_postcommit.sh`, POSIX ERE with no verbose mode, and `re.X` would strip literal whitespace and treat `#` as a comment — harmless on today's 678-char scalar, which contains neither, and a silent divergence the day one is added); any failure (unreadable, absent, empty scalar) → `declared["sync_test"] = "unavailable"` and the test is skipped, never an empty regex — AND one stderr line at start so the fail-open is visible where it happens: `⚠ fabrik-task: sync lane test SKIPPED (cannot read the governance-sync filter)`; without it a project-repo agent gets a start byte-identical to a clean one and learns only at close, after the commit.
5. Persist `declared: {files, decision, heavy, mechanism, oneway, tradeoffs, sha, sync_test}` — ⚠️ set it CONDITIONALLY just after the record literal, NOT as a member of it: a literal member writes `declared: None` into every other command's record in ~46 repos, which is a record-shape change with no reader and breaks the byte-identical promise this ticket makes (found at T01a's acceptance review; the implementation was right and this wording was stale). `sync_test`'s positive value is `"ok"`, the spine's declared domain — after the only stdout print `print(pinned_line(new))` (`:2682`), print `RECORD: <started_at>` — only under `fabrik-task`; every refusal uses the close-time shape (`:3369-3371`: rc 1, `[command_run] ` on stderr, the bare message on stdout).
6. `step` parser (`:2370-2377`): add `--design PATH`; in the inline `step` handler (`:2708`), after `rec["phase_title"] = args.title` (`:2789`) and STRICTLY BEFORE the `phase` event is queued at `:2790` (the spine's Evidence says the same: "`design` is set between `:2789` and `:2790`") — never merely "before `save` (`:2792`)", because `_flush_events` runs in a `finally` (`:2544-2545`) and a refusal placed after `:2790` emits a `phase` event for a step that did not happen, the very thing `:3699` deletes the `run_close` event to prevent, on a `fabrik-task` record, read the file — an absent or unreadable path is its own REFUSED (`REFUSED — fabrik-task: --design <p> cannot be read`, rc 1, the phase NOT advanced), never an escaping exception (rc 0 at `:2557-2559`) — and a SECOND `--design` on an already-set record KEEPS the first value (as the Behavior Contract row says) and says so on stderr — `NOTE — fabrik-task: design already recorded; this --design was ignored` — never discarded silently and never a refusal, which would wedge the phase and with it the Stop hook; refuse over `_LEDGER_FIELD_CAP` (`:1602`) with `REFUSED — fabrik-task: --design is <n> chars, the cap is 2000`, else set `rec["design"]` once (a later `step` never overwrites a set value).
7. Run the gate green; `ruff format --check` and `ruff check` on the two files. ⚠️ The equality grader belongs in step 2's red-first batch, not here: it is a test this change ADDS, so `45-testing-strategy.md:21` requires it SEEN RED — write it in step 2 (it fails with no extractor to call), and here only re-run it. It reads the scalar both ways (stdlib vs `yaml.safe_load`) on the live hub file and asserts equality (678 chars today).
8. `python scripts/enforcement/check_doc_sync.py`; CHANGELOG line into the Deltas block (orchestrator-applied).
9. `/fabrik-review` on this ticket's changed surface to a coverage-adjudicated exit (partitioned by file: Opus on `scripts/command_run.py`, Sonnet on the test file — `dispatch_headroom.py --slices opus=1,sonnet=1`, stamped first); every finding FIXED or REFUTED; the fixing pass is never the last look.
10. Commit with explicit pathspecs + provenance trailers (`Agent-Role: subagent`, `Agent-Task: T01a`).

## Context Files
- .windsurf/rules/core/10-python.md
- .windsurf/rules/core/45-testing-strategy.md
- scripts/command_run.py
