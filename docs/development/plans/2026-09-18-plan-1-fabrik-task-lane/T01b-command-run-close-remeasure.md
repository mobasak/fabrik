# T01b — `command_run.py`: `--commit` on the close verbs, the own-commit re-measure, the two row fields

## Scope
Implements spec § Chosen approach, Phase 5 — the SIX INVARIANTS (i)–(vi), the four close-time refusal CONDITIONS — the spec's three plus the absent-`--commit` case this review added — carrying three distinct messages, the persisted-SHA close line — and UPGRADE's `upgrade` keying, restating nothing those paragraphs already say; this ticket's Behavior Contract IS the operator's Phase A acceptance criteria (D-293). Scoped to records whose `command` is `fabrik-task`; every other command's close and ledger row byte-identical to today's. DO-NOT: touch `start`/`step` (T01a); read the working tree's mtimes (the deleted leg, spec § Chosen approach, Phase 5); add a third row field (D-290); import PyYAML or read `.pre-commit-config.yaml` with anything but T01a's stdlib extractor.

Depends: T01a
Parallel: ⛓️
Complexity: native
Gate: uv run pytest tests/test_command_run_fabrik_task.py -q
Docs: CHANGELOG (Deltas) · `docs/reference/command-run-protocol.md` § CLI gains the `--commit` line and the two row fields — owned by T03, cited here

## Touches
- scripts/command_run.py — PRIMARY PATH
- tests/test_command_run_fabrik_task.py

## Behavior Contract
- **Given** a `fabrik-task` record with `declared.files = [a]` and a commit that changed `a` and added `b`, **When** `done --commit <that sha>` runs, **Then** the ledger row carries `oversized_mini: 1 · commit=<sha> · paths=b` (scripts/command_run.py:3121, :3655-3676; spec § Chosen approach, Phase 5 (iii)–(vi))
- **Given** the same record and a commit that changed only `a`, `CHANGELOG.md` and `docs/FEATURES.md`, **When** `done --commit` runs, **Then** the row carries `oversized_mini: 0` (the Doc Sync Matrix destinations parsed from `CLAUDE.md` § Doc Sync Matrix at close time plus `docs/CAPABILITIES.md` are excluded) (CLAUDE.md § Doc Sync Matrix; spec § Chosen approach, Phase 5 (iv))
- **Given** a commit that renames a declared file into `scripts/enforcement/` and also touches `docs/reference/technology-stack-decision-guide.md` — a Doc Sync Matrix EXCL prefix member that is ALSO a sync-regex hit, **When** `done --commit` runs, **Then** the row counts BOTH, because set B of invariant (v) takes every sync-regex hit excluded or not (scripts/command_run.py:3121; spec § Chosen approach, Phase 5 (v))
- **Given** `--commit` that is empty, ABSENT on a `done`, resolves to a merge, or is dated before `started_at`, **When** `done` runs, **Then** it is REFUSED — the four close-time CONDITIONS (empty · resolves to a merge · dated before `started_at` · ABSENT on a `done`) carry THREE distinct messages, the merge and the stale date sharing one — and the record stays `running` (scripts/command_run.py:3360-3371; spec § Chosen approach, Phase 5 (ii))
- **Given** `handoff --reason "UPGRADE: mechanism"` or `done --evidence "UPGRADE: sync — …"` on a `fabrik-task` record, **When** the close runs, **Then** the row carries `upgrade: mechanism` / `upgrade: sync`; a `/fabrik-review` close writes neither field (scripts/command_run.py:3074, :3655-3676; spec § Chosen approach, UPGRADE)
- **Given** a ROOT commit (no parent) whose only path is an undeclared file, **When** `done --commit <it>` runs, **Then** the diff is taken against the empty tree `4b825dc642cb6eb9a060e54bf8d69288fbee4904` and the row counts that path (spec § Chosen approach, Phase 5 (iii))
- **Given** `handoff --command fabrik-task --resume <seed> --reason "UPGRADE: mechanism"` with no `--commit`, **When** the close runs, **Then** the row carries `oversized_mini: unmeasurable=no-commit` and `upgrade: mechanism`, and the close is NOT refused; `no-commit` WINS unconditionally when `--commit` is absent — there is no diff to take, so the membership arm cannot run and `sync_test-unavailable` cannot apply; that reason is reachable only on a close that DID carry a commit (spec § Chosen approach, Phase 5 (ii), (vi))
- **Given** the parser of `CLAUDE.md` § Doc Sync Matrix, **When** run on the live file, **Then** it reads every table row (22 today) and yields 25 tokens — 23 concrete paths plus the two `<name>` prefixes (`docs/reference/`, `docs/workstation/`) (CLAUDE.md § Doc Sync Matrix; spec § Chosen approach, Phase 5 (iv))

## Steps (the coder's order)
1. Red first: extend `tests/test_command_run_fabrik_task.py` (T01a's harness) with the eight graders above (the Behavior Contract is eight rows — count them, do not take this number on trust); each builds a throwaway repo (`git init -b main`, commits with `-c user.email -c user.name`), runs the real `start --command fabrik-task --file a …` through the harness, commits the shapes the row asserts, and closes with `done --commit "$(git rev-parse HEAD)"` — plus one refusal test per close-time CONDITION — SIX in all: the four `--commit` conditions (empty · absent on a `done` · a merge · dated before `started_at`, carrying three messages since the merge and the stale date share one), the cross-command `--commit`, and the `UPGRADE: sync` claim with no sync hit; run them and watch them FAIL for the right reason (unknown `--commit`; no `oversized_mini` key).
2. Parsers: add `--commit SHA` to `done` (`scripts/command_run.py:2440-2456`), `handoff` (`:2458-2483`) and `blocked` (`:2485-2503`) — required on `done` only when the live record's command is `fabrik-task` (checked in `_close`, not argparse, because argparse cannot see the record); refused with `REFUSED — --commit belongs to --command fabrik-task` on any other record. ⚠️ That cross-command refusal sits OUTSIDE step 3's `rec["command"] == "fabrik-task"` block — placed inside it, it can never fire, and `done --command fabrik-review --commit <sha>` would be silently accepted, breaking the byte-identical promise this plan makes for every other command on a fleet-synced script.
3. In `_close` (`:3121`), immediately after the verb branch ends at `:3511` and STRICTLY BEFORE the `run_close` event is queued at `:3600` — never merely "before `_row` (`:3655`)": `_flush_events` runs in a `finally` (`:2544-2545`), so a refusal placed after `:3600` emits `run_close {verdict: done}` for a close that did not happen, which is exactly what `:3699` deletes that event to prevent, and step 5's mirroring into the event dict is only reachable from before `:3600` — the re-measure (git calls in the `:3215-3229` subprocess shape MINUS `check=True`, `cwd=rec["repo_root"]`, the whole block wrapped in `except Exception` → `unmeasurable=no-git`, never rc 0 at `:2557-2559` and never a refusal: a measurement failure must not block a close), ONLY when `rec["command"] == "fabrik-task"`: (ii) resolve `--commit` — `git cat-file -t` is `commit`, parent count ≤ 1 (`git log -1 --format=%p`), committer date (`%ct`) ≥ `rec["started_epoch"]`; else the refusal (`REFUSED — fabrik-task: --commit <v> is not this run's commit`; an empty value its own `… --commit is empty — re-read the capture file written beside the commit`, and an ABSENT `--commit` on a `done` its own `REFUSED — fabrik-task: done needs --commit; run phase 5's capture (`mkdir -p … && git rev-parse -q --verify HEAD > …/commit.sha`) and pass it` — the two are different mistakes and the empty-value text sends the second one to a file that does not exist); `blocked`/`handoff` without `--commit` → `oversized_mini: unmeasurable=no-commit`, never a refusal; (iii) `git -c core.quotePath=false diff --name-status -M -C -z <c>~1 <c>`, split on NUL (a root commit against the empty tree `4b825dc642cb6eb9a060e54bf8d69288fbee4904`). ⚠️ `-M -C` and `-z` are each load-bearing and `core.quotePath=false` is belt-and-braces (executed: `-z` ALONE already emits the raw path, so the override changes nothing in this command and is kept only so a future reader who drops `-z` is not silently re-quoted). Executed 2026-09-18: without `-z`/`quotePath=false` git emits `"caf\303\251.md"` for a non-ASCII path, so a declared `café.md` never matches its own diff line and is counted oversized; and without `-C`, a commit carrying BOTH a rename and a copy is MIS-ATTRIBUTED — `-M` alone printed `R100 orig.md copy.md` + `A renamed.md`, pairing the rename's source with the COPY, where `-M -C` printed the correct `C100 orig.md copy.md` + `R100 orig.md renamed.md`. Field structure, executed: `-z` TERMINATES every field (so the split yields a trailing empty field to discard), an add/modify/delete is TWO fields (status, path), and BOTH a rename and a copy are THREE (`R100`/`C100`, source, destination) — a splitter that takes three fields only for `R` reads a `C100` record's destination as the next status and every later field is off by one; (iv) EXCL = the Doc Sync Matrix's *Update* column parsed from `<repo_root>/CLAUDE.md` between the first line that STARTSWITH `## Doc Sync Matrix` and the next line that startswith `## ` — ⚠️ startswith, never equality: the live heading is `## Doc Sync Matrix (update matched docs in same change — gate-enforced)` in BOTH copies (hub `:278`, template `:250`, executed), so an equality read finds nothing and silently takes the fallback, collapsing EXCL from 26 paths to 6; and the scan's END must fall back to end-of-file when no later `## ` exists (an unguarded `next()` raises, and `:2557-2559` turns that into rc 0) (every backticked `.md`/`.example`/`.sql` token; `<name>.md` entries as prefixes) + the five ledger files — `CHANGELOG.md`, `docs/DECISIONS.md`, `docs/STRATEGIC_BACKLOG.md`, `INDEX.md`, `docs/LESSONS_LEARNT.md` (enumerated HERE because the spine's Global Constraints names only the four shared-append files and the fifth is defined in the spec, which is over the READ budget and in no ticket's Context Files) — + `docs/CAPABILITIES.md` (a named constant with its own grader asserting the file exists); the matrix is read from the record's `repo_root`, and a repo whose `CLAUDE.md` lacks the section falls back to those five ledger files + the constant alone — stated in the source's docstring and graded, never a fourth `unmeasurable` reason; (v) the count is the size of the UNION OF TWO SETS, deduplicated so a path in both is one — **A** = the commit's paths that are OUTSIDE `declared.files ∪ EXCL` (an `R`/`C` destination inheriting its source's membership; the `R`/`C` SOURCE path is judged on its own membership exactly like any other path, and both tokens may contribute), and **B** = every path in the commit, EXCLUDED OR NOT, source or destination, that matches T01a's sync regex. ⚠️ Not a three-way exclusion union: the two readings disagree, and the overlap is real — `docs/reference/technology-stack-decision-guide.md` is both a matrix EXCL prefix member and a sync-regex hit (executed), so under the union it COUNTS and under an exclusion it does not; a `--evidence` beginning `UPGRADE: sync` with zero sync hits in the commit → `REFUSED — fabrik-task: --evidence claims UPGRADE: sync but the commit has no sync-regex hit`; (vi) the field: `0` · `<n> · commit=<sha> · paths=<first three>` · `unmeasurable=<no-commit|no-git|sync_test-unavailable>` — ⚠️ **This field order DIVERGES from the CONVERGED spec**, which states `<n> · paths=<first three> · commit=<sha>` at its § Chosen approach Phase 5 (vi); the divergence is deliberate, decided by this review on executed evidence, minted in **D-294** (this plan set's CONVERGED row, written by `/fabrik-plan-review`, not by any ticket — T04a's `Docs: none new` is about the BUILD's rows) and routed to `docs/STRATEGIC_BACKLOG.md` in that same commit for the spec's next non-delta pass. The SAME declaration covers clause (iii)'s `-M -C -z -c core.quotePath=false`, which likewise extends the spec's bare `-M` — `commit=` comes BEFORE `paths=` so the `_cap_field` truncation costs only the tail: executed 2026-09-18, three deep paths make the value 2,441 chars and the old `paths=…·commit=` order loses `commit=` entirely at the 2,000 cap, while the reordered form keeps it (`no-git` when git itself is unavailable; `sync_test-unavailable` when `declared.sync_test == "unavailable"` and the close's re-run of T01a's extractor still fails — the membership arm still runs and a count still wins); and the source's docstring carries the measure's own COBRA beside the counter it introduces (D-253, FIX DIRECTIVE 5): the cheapest way to score `oversized_mini: 0` without producing the outcome is to park extra work under `docs/reference/` or `docs/workstation/`, whose matrix rows are whole-directory prefixes — the counter-measures are phase 4's seats, who are told the declared size and asked whether the change fits it, and V3's probe, capped by `_cap_field` (`:1605-1606`).
4. `upgrade`: on any `fabrik-task` close, ANCHORED — the value is written only when `args.reason` (`handoff`/`blocked`) or `args.evidence` (`done`), stripped, BEGINS `UPGRADE:` (the spec's word, three times), and is then the first whitespace-delimited token after the colon — `UPGRADE: sync` and `UPGRADE:sync` both yield `sync`; a bare `UPGRADE:` with no token writes NO field (never an IndexError, which `scripts/command_run.py:2557-2559` would turn into rc 0 with nothing written); the match is case-SENSITIVE, so `upgrade:` writes nothing. Absent otherwise. ⚠️ An unanchored substring search is a live corruption, executed: `--evidence "fix complete; no UPGRADE: was required"` writes `upgrade: was`, which both inflates T02's `upgrade` rate and, for a value of `sync`, silently removes the row from the `oversized_mini` denominator.
5. Write both keys into `_row` at `:3674-3675` (after `cost_usd`, before `**_tok`) under the `fabrik-task` branch only; mirror them into the `run_close` kaizen event dict (`:3604-3623`) so the event stream carries what the row carries; the append at `:3705-3712` needs no change.
6. Run the gate green; `ruff format --check` + `ruff check`; the negative grader: a `/fabrik-review` record's close produces a row without either key and byte-identical stdout.
7. `python scripts/enforcement/check_doc_sync.py`; CHANGELOG line into the Deltas block.
8. `/fabrik-review` on this ticket's changed surface to a coverage-adjudicated exit (partitioned by file: Opus on `scripts/command_run.py`, Sonnet on the test file — `dispatch_headroom.py --slices opus=1,sonnet=1`, stamped first); the shared-tree hazards (a sibling's commit in the window, the rebase) are the review's named classes; every finding FIXED or REFUTED.
9. Commit with explicit pathspecs + provenance trailers (`Agent-Role: subagent`, `Agent-Task: T01b`).

## Context Files
- .windsurf/rules/core/10-python.md
- .windsurf/rules/core/45-testing-strategy.md
- scripts/command_run.py
