# T04 — `dispatch_headroom.py --slices`: the floor stands down under a partition; the docstrings, printed sentences and the CLI reference doc

## Scope
In `scripts/sysadmin/dispatch_headroom.py` (hub-only, not synced) implement the spec's D3 (docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:141-145) and the D7 sub-bullet on its own wording (`:174`): (1) `budget()` (`:404-412`) gains a keyword-only `slices: dict[str, int] | None = None` appended AFTER `mechanical` — every caller is positional today (`:668`, `:688`, `:689`, `tests/test_quota_dashboard.py:3275`, 50 `dh.budget(` sites in `tests/sysadmin/test_dispatch_headroom.py`) and stays byte-identical when `slices` is None. When `slices` is given: `wanted = sum(slices.values())`, `full_mix()`'s padding is bypassed (`:368-369`), the `wanted < FLOOR` raise at `:550-557` stands down (its comment calls it unreachable BECAUSE `full_mix` pads — it becomes reachable and would silently re-raise a 2-seat partition to 3), the below-the-floor advisory at `:565` stands down, `floor_granted` (`:448-449`) is exported as 0 and the labels at `:451-455`, `:464`, `:520` are not printed; the cap raises at `:442-447`, `:471`, `:507`, `:537` stay (harmless for the count: `seats = min(caps.values())` at `:558` includes `wanted`, so a 2-slice partition prints 2); `trim()` (`:373-388`) is reused unchanged — Haiku first, then extra Opus, then Sonnet, never below one Opus. (2) CLI: `--slices opus=<n>,sonnet=<n>,haiku=<n>` (parse with the existing `parse_mix` grammar; any count accepted — the at-most-one-Haiku rule is the review loop's, D2, not the CLI's); `--units` becomes optional (`required=True` at `:648` → default None) and defaults to the slice count when `--slices` is given; `--units 0 --slices …` never reaches the nothing-to-partition branch at `:543-548`; `--slices` without `--units` runs; `--units` without `--slices` behaves exactly as today. `--json` gains `slices` and `mix_by_slice` beside every key it carries today (`:698-717`); `box_caps_floored` (`:694`) reads false under slices. (3) Wording: the module docstring (`:15`), `full_mix()`'s (`:353`) and `_mix_story()`'s (`:579` — it quotes the first draft's mistake; describe it without the retired wording) and the two PRINTED sentences (`:602` "Haiku mechanical seat(s) — one per unit", `:606` "one Sonnet breadth seat per unit") are rewritten to name their scope beside the mix — "for a units-sized grounding surface" — so neither "one Sonnet breadth seat AND one Haiku mechanical seat" nor "one Sonnet + one Haiku" survives a case-insensitive line-joined search, and the `--slices` story prints the partition ("Opus on the risky slices, Sonnet on the rest, at most one Haiku class seat; every file read once"). (4) `docs/workstation/claude-account-rotation.md` § 223-239 (the CLI reference the script's `# AFTER-EDIT:` header couples) documents `--slices`, the optional `--units`, the floor rule under partition (DD2: one seat per non-empty slice kind; one Opus seat over the most consequential slice when no risky unit exists; the three-angle floor retired for review loops, kept for grounding/adjudication and `/fabrik-review-scoped`), and drops the wrapped D-191 sentence at `:227-228`. Tests (`tests/sysadmin/test_dispatch_headroom.py`, 31 today): V4 red-first on the `:550-557` site (a 2-slice partition reads `SEATS: 3` with "raised to the floor" today), the trim order under a cap, `--slices` without `--units`, `--units 0 --slices`, the JSON keys, the absence of every floor label under slices, and the wording test on the printed sentences (the V7 corpus test is T07's; this ticket's own test asserts its two printed lines). DO-NOT: change `FLOOR`, `full_mix()`'s units path or `trim()`'s order; touch `quota_dashboard.py` (T05) or core/62 / the fragment (T06/T07) although the AFTER-EDIT header names them — say in the commit body that their sentences are rewritten by T05/T06/T07 of this plan; add a `--slices` shape the review commands will not emit.

Depends: —
Parallel: ⚡
Complexity: complex
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/sysadmin/test_dispatch_headroom.py tests/test_quota_dashboard.py -q -k "dispatch or budget or banner"
Gate: python3 scripts/sysadmin/dispatch_headroom.py --slices opus=1,sonnet=1 && python3 scripts/sysadmin/dispatch_headroom.py --units 3 --mechanical 0
Docs: docs/workstation/claude-account-rotation.md § dispatch_headroom.py (Touches); CHANGELOG entry (Deltas)

## Touches
- scripts/sysadmin/dispatch_headroom.py — PRIMARY PATH
- tests/sysadmin/test_dispatch_headroom.py
- docs/workstation/claude-account-rotation.md

## Behavior Contract
- **Given** `--slices opus=1,sonnet=5,haiku=1` on an idle box, **When** `dispatch_headroom.py` runs, **Then** it prints `SEATS: 7` and the mix `{opus 1, sonnet 5, haiku 1}`; under a cap of 5 it trims Haiku first, then the extra Opus seats, then Sonnet, never below one Opus (scripts/sysadmin/dispatch_headroom.py:373-388)
- **Given** `--slices opus=1,sonnet=1`, **When** it runs, **Then** it prints `SEATS: 2` with no "raised to the floor" reason, no below-the-floor advisory, `floor_granted` 0 and `box_caps_floored` false for both halves (scripts/sysadmin/dispatch_headroom.py:550-557, :565, :448-449, :694)
- **Given** `--slices opus=1,sonnet=2` without `--units`, **When** it runs, **Then** it runs (units defaults to the slice count 3); `--units 0 --slices opus=1` prints `SEATS: 1`, never `SEATS: 0` through the nothing-to-partition branch (scripts/sysadmin/dispatch_headroom.py:543-548, :648)
- **Given** every positional `budget(units, heavy, b, q, s, risky, mechanical)` call today (3 script sites, `tests/test_quota_dashboard.py:3275`, 50 sites in `tests/sysadmin/test_dispatch_headroom.py`), **When** `slices` is not given, **Then** the result is byte-identical to today and the 31 existing tests pass unchanged (scripts/sysadmin/dispatch_headroom.py:404-412)
- **Given** the module docstring, `full_mix()`'s, `_mix_story()`'s and the printed sentences at `:602`/`:606`, **When** grepped case-insensitively over line-joined text, **Then** "one Sonnet breadth seat AND one Haiku mechanical seat" and "one Sonnet + one Haiku" are absent and "units-sized grounding surface" is present in the printed sentences (scripts/sysadmin/dispatch_headroom.py:15, :353, :579, :602, :606)
- **Given** `docs/workstation/claude-account-rotation.md` § 223-239, **When** read line-joined, **Then** it documents `--slices`, the optional `--units` and the floor rule, and the wrapped D-191 sentence at `:227-228` is gone (docs/workstation/claude-account-rotation.md:223-239)
- **Given** `--json`, **When** it prints, **Then** the payload carries `slices` and `mix_by_slice` beside every key it carries today (scripts/sysadmin/dispatch_headroom.py:698-717)

## Context Files
- .windsurf/rules/core/10-python.md
- scripts/sysadmin/dispatch_headroom.py
- tests/sysadmin/test_dispatch_headroom.py
- docs/workstation/claude-account-rotation.md
- docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md

## Implementation notes
- The box probe fails soft; tests build `b`/`q`/`s` dicts as the existing tests do (`b` needs `ok`, `mem_available_gb`, `cores`, `load1` — a hand-built `ok: True` dict without them raises, `_mix_story`'s docstring at `:583-584`).
- `tests/sysadmin/test_dispatch_headroom.py:296` and `:399` carry the D-191 wording inside docstrings/comments as the historical record of a round finding — they are not the script's sentences; V7 scopes `scripts/**/*.py`, so they stay.
- Red-first: run `--slices opus=1,sonnet=1` on the unmodified script through `budget(…, slices=…)` — it raises `TypeError` (unknown keyword); after the signature lands but before the FLOOR sites stand down it prints 3 with the floor reason; the test is green only when both are done.
