# T07 — The three review commands, the rules-review D-048 cite, the subagents fragment; the V7/V8 corpus tests

## Scope
Rewrite the command corpus surfaces (rendered box-wide from the MAIN checkout: render → `--check` → commit; the `command-corpus-check` pre-commit hook at `.pre-commit-config.yaml:105-114` refuses a commit whose sources are ahead of the installed corpus): (1) `commands/_sources/fabrik-review.md` — Phase 1 (`:139`, the seat split at `:166-172`): the surface is PARTITIONED into disjoint slices by file (Opus risky, Sonnet ordinary, at most one Haiku class seat only when the brief names a non-scriptable inventory class), sized with `dispatch_headroom.py --slices opus=N,sonnet=N,haiku=N`, stamped, dispatched in ONE message, the hygiene script run on the surface FIRST and its hits fixed before the seats go out (D4); every finder brief carries the D8 lessons verbatim (pin dirs created once before the first dispatch and never touched while seats run; `git init` inside a `git archive` pin; every `command_run.py` probe sets `COMMAND_RUN_DIR`, `COMMAND_RUN_TRANSCRIPT`, `KAIZEN_EVENTS_DIR`; every mutation applied, tested and restored inside ONE Bash call with an asserted restore; never bare-grep a tracked path — `git show <sha>:<path>`; Python `time.sleep` in fixtures, never a foreground shell sleep; a transcript quote filters out the Stop-hook feedback, skill payloads and the re-invocation notice and prefilters raw lines by timestamp; the Read tool truncates without a marker — read in offset pages and confirm the last line; HARD TIME BOX 15 minutes; `MACHINERY:` last); Phase 2 (`:260-307`): the orchestrator EXECUTES every candidate with a concrete failure scenario (a probe on a pinned copy, a failing test, a mutation on a copy; a doc claim by a pinned read quoted beside the sentence) before CONFIRMED and executes the refutation before REFUTED, citing the command and output in the row; a one-seat candidate the orchestrator cannot reproduce is `RECORDED — unexecuted (<why>)` (parentheses, never a colon), a reproduced-and-kept one `RECORDED — by design (<owning row>, round N[; …])` or `(D-nnn)`, an executed measurement `RECORDED — measured (<why>)`, a hygiene hit adjudicated false `RECORDED — hygiene false positive (<why>)`; Phase 4 (`:360-364`): the exit rule becomes the delta round — round 1 is the only full pass; a round that CONFIRMED anything fixes it in that round and the next round is a DELTA over `git diff <last round's commit>..HEAD -- <surface>` plus one hop of callers/callees plus sibling commits on the surface, same partition, the class ledger persisting (the receipt cites the classes not touched as standing-clean from the last full pass), the hygiene script at the round's start and close, the closing delta round always carrying a fresh non-authoring seat (Opus if any hunk is risky, else Sonnet), recorded with `round --seats <n> --findings <raw> --confirmed <n> --classes-swept … --classes-new …`; a delta round that confirms zero closes; `## BLOCKED: NON-CONVERGENCE` after three non-decreasing nonzero delta rounds stays the only other exit; the completion claim at `:437` reads "only when the last row carries `confirmed: 0, fixed: 0` and `unexecuted: 0` or none, from a delta round with a fresh seat" and DROPS `new: 0` (`new:` stays prose, the stopped-learning signal, `:435`); the example rows at `:422-424` carry `found: F, new: N, confirmed: C, fixed: X, unexecuted: U`; the receipt gains the `## Residual` section T02's template writes. (2) `commands/_sources/fabrik-repo-review.md` — waves (`:64`, and the 5 "wave" mentions at `:2`, `:44`, `:61-62`, `:64`, `:105`) become slices sized by `--slices`; a wave's reservation close stays a `--seats/--findings` round with no classes (why `PER_UNIT_ROUND_COMMANDS` keeps the command, DD8); the certification pass (`:152-162`) is the class-ledger close with `--confirmed`; the exit (`:154`) reads quiet = zero CONFIRMED. (3) `commands/_sources/fabrik-review-scoped.md` (D10): `:47`'s round line gains `--confirmed <n>`; `:68-70` drops the D-191 sentence ("D-191: the Opus authoritative seat + one Sonnet + one Haiku over the whole diff") and keeps the measured 3-reader one-brief floor (core/62:65); its fixes follow the delta-round rule; quiet = zero CONFIRMED, refuted never counts. (4) `commands/_sources/fabrik-rules-review.md:138` — "CITED, not counted (D-048)" becomes "CITED, not counted (D-203)". (5) `commands/_fragments/subagents-core.md:3` (one ~10 KB line, included by 20 of 36 sources; the assembler's EXTRACT table names it at 11 command keys, PARAMS at 20 — `commands/assemble_commands.py:141-211`, `:319+`): the D-191 sentence and the units×angles sizing become the partition rule + the delta-round sentence for the review loops, with the units-sized floor kept for grounding/adjudication commands; the floor sentence scoped as in core/62 (T06). (6) Tests in `tests/test_assemble_dispatch_step.py` (5 today): V7 — over the 36 rendered commands (render to a temp dir via the assembler's `--check` path or `assemble_commands.render()`), `commands/_sources/*.md`, the fragment, `.windsurf/rules/core/62-using-subagents.md`, `CLAUDE.md`, `templates/governance/CLAUDE.md`, `scripts/**/*.py` and `docs/workstation/*.md`, join each file's text with `" ".join(text.split())` and assert case-insensitively that "one sonnet breadth seat and one haiku mechanical seat" and "one sonnet + one haiku" appear in 0 files (print the denominator), that `dispatch_headroom.py`'s printed sentences carry "units-sized grounding surface", and that `_budget_probe`'s rendered banner carries "Box budget (D-189" (positive control) and not the short literal; V8 — the fragment's delta-round sentence survives a render into `/fabrik-review`; the existing `_floor("review")` test (`test_judgement_floors_name_no_haiku_seat_and_review_floors_name_a_class_wide_one`) is updated to the partition wording. Seen red FIRST on `templates/governance/CLAUDE.md:356`, `scripts/sysadmin/dispatch_headroom.py:602`, the rendered banner and `docs/workstation/claude-account-rotation.md:227-228` — i.e. written and run BEFORE T04/T05/T06 are merged into your worktree, then green after rebasing on them (Depends). DO-NOT: hand-edit `~/.claude/commands/*` (rendered); render from a worktree (the renderer PRUNES); touch core/62, the CLAUDE.md files or the docs (T06); touch `dispatch_headroom.py` (T04); change any command other than the four named and the fragment.

Depends: T04, T05, T06
Parallel: ⛓️
Complexity: native
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/test_assemble_dispatch_step.py -q && python3 commands/assemble_commands.py --check
Gate: python3 scripts/enforcement/check_command_corpus.py
Docs: CHANGELOG entry (Deltas)

## Touches
- commands/_sources/fabrik-review.md — PRIMARY PATH
- commands/_sources/fabrik-repo-review.md
- commands/_sources/fabrik-review-scoped.md
- commands/_sources/fabrik-rules-review.md
- commands/_fragments/subagents-core.md
- tests/test_assemble_dispatch_step.py

## Behavior Contract
- **Given** `commands/_sources/fabrik-review.md` after this ticket, **When** read, **Then** Phase 1 partitions by slice (Opus risky, Sonnet ordinary, at most one Haiku class seat), Phase 2 says the orchestrator executes every candidate and every refutation, Phase 4 says a delta round over the fix diff plus one hop closes on `confirmed: 0`, the completion claim at `:437` reads the new row and drops `new: 0`, and the example rows carry the five counters (commands/_sources/fabrik-review.md:166-172, :260-307, :364, :422-424, :435-437)
- **Given** `fabrik-repo-review.md`, `fabrik-review-scoped.md` and `fabrik-rules-review.md`, **When** read, **Then** waves are slices and the certification pass is the class-ledger close with `--confirmed`, the scoped command's `:47` round line carries `--confirmed` and `:68-70` keeps the 3-reader floor without the D-191 sentence, and the rules-review's `:138` cites D-203 instead of D-048 (commands/_sources/fabrik-repo-review.md:64, :154, :162; commands/_sources/fabrik-review-scoped.md:47, :68-70; commands/_sources/fabrik-rules-review.md:138)
- **Given** `commands/_fragments/subagents-core.md:3`, **When** rendered into the 20 commands that include it, **Then** the partition rule replaces the D-191 sentence, the delta-round sentence and the D8 brief lessons survive a render, and no rendered command carries a D-191 literal (commands/_fragments/subagents-core.md:3)
- **Given** the rendered corpus (36 commands), the sources, the fragment, core/62, both CLAUDE.md files, `scripts/**/*.py` and `docs/workstation/`, **When** the V7 test joins each file's text with `" ".join(text.split())` and searches case-insensitively, **Then** 0 files carry either D-191 literal, the banner positive control holds, and the test was seen red on `templates/governance/CLAUDE.md:356`, `dispatch_headroom.py:602`, the rendered banner and `claude-account-rotation.md:227-228` before the fixes (tests/test_assemble_dispatch_step.py:1)
- **Given** the main checkout after T07's edits, **When** `python3 commands/assemble_commands.py` renders and `--check` runs, **Then** `--check` exits 0 and `check_command_corpus.py` is green (commands/assemble_commands.py:1095)

## Context Files
- .windsurf/rules/core/40-documentation.md
- commands/_sources/fabrik-review.md
- commands/_sources/fabrik-repo-review.md
- commands/_sources/fabrik-review-scoped.md
- commands/_sources/fabrik-rules-review.md
- commands/_fragments/subagents-core.md
- tests/test_assemble_dispatch_step.py
- docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md

## Implementation notes
- Commands are rules, not changelogs: present-tense rule, delete the old text; history lives in git. Every `{{include:…}}` directive and `{{FLOOR}}`/`{{EXTRA_LIVE}}` placeholder keeps its exact spelling — the existing tests grade unfilled placeholders.
- The V7 scan of `scripts/**/*.py` excludes `__pycache__`; `tests/sysadmin/test_dispatch_headroom.py`'s docstrings carry the wording as a historical record and are outside the scan (`tests/` is not a V7 surface).
- The rendered corpus is not tracked: render to a temp dir in the test (the assembler's `--check` mechanism) and count the 36.
- The orchestrator renders in the MAIN checkout at merge (render → `--check` → commit); the coder's worktree runs only `--check` against a temp render.
