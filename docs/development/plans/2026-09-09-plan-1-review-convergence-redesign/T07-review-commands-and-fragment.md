# T07 — The three review commands, the rules-review D-048 cite, the subagents fragment, the assembler's rendered floor; the V7/V8 corpus tests

## Scope
Rewrite the command corpus surfaces (rendered box-wide from the MAIN checkout at merge: render → `--check` → commit; the `command-corpus-check` pre-commit hook at `.pre-commit-config.yaml:105-114` runs only when `pwd` is `/opt/fabrik`, so a worktree coder never renders). The ledger rows the text cites: D-206 (the exit counter, supersedes D-048), D-207 (the partition, supersedes D-191's seat mix), D-208 (the floor's new scope); D-203 is the operator's ruling and is cited only as such. (1) `commands/_sources/fabrik-review.md` — Phase 1 (`:139`; the seat rule is stated TWICE inside it — the one-line paragraph at `:142` ("≥1 `fabrik-reviewer` on Opus … plus ONE Sonnet seat per independent failure-class group AND the Haiku mechanical seats … `--units <groups>`") and the block at `:166-172` including the `≥1 Opus authoritative seat … regardless of diff risk` floor sentence and `:172`'s "the GROUPS come from the failure-class list … never from the Coverage Checklist" — both are replaced by ONE statement, the failure-class GROUPS giving way to file slices: the 16 failure classes stay the seats' HUNT LIST, the partition is by file): the surface is PARTITIONED into disjoint slices by file (Opus risky, Sonnet ordinary, at most one Haiku class seat only when the brief names a non-scriptable inventory class; a surface with no risky unit still gets one Opus seat over its most consequential slice, carved out of Sonnet's allocation — DD2), sized with `dispatch_headroom.py --slices opus=N,sonnet=N,haiku=N`, stamped, dispatched in ONE message, the hygiene script run on the surface FIRST and its hits fixed before the seats go out (D4); every finder brief carries the D8 lessons verbatim (pin dirs created once before the first dispatch and never touched while seats run; `git init` inside a `git archive` pin; every `command_run.py` probe sets `COMMAND_RUN_DIR`, `COMMAND_RUN_TRANSCRIPT`, `KAIZEN_EVENTS_DIR`; every mutation applied, tested and restored inside ONE Bash call with an asserted restore; never bare-grep a tracked path — `git show <sha>:<path>`; Python `time.sleep` in fixtures, never a foreground shell sleep; a transcript quote filters out the Stop-hook feedback, skill payloads and the re-invocation notice and prefilters raw lines by timestamp; the Read tool truncates without a marker — read in offset pages and confirm the last line; a denominator printed beside every count, and the match count beside any grep piped through `cut`; HARD TIME BOX 15 minutes; `MACHINERY:` last); Phase 2 (`:260-307`): the orchestrator EXECUTES every candidate with a concrete failure scenario (a probe on a pinned copy, a failing test, a mutation on a copy; a doc claim by a pinned read quoted beside the sentence) before CONFIRMED and executes the refutation before REFUTED, citing the command and output in the row; a one-seat candidate the orchestrator cannot reproduce is `RECORDED — unexecuted (<why>)` (parentheses, never a colon), a reproduced-and-kept one `RECORDED — by design (<the owning row's first-cell id>, round N[; …])` or `(D-nnn)`, an executed measurement `RECORDED — measured (<why>)`, a hygiene hit adjudicated false `RECORDED — hygiene false positive (<why>)`; Phase 4 (`:360-364`): the exit rule becomes the delta round — round 1 is the only full pass; a round that CONFIRMED anything fixes it in that round and the next round is a DELTA over `git diff <last round's commit>..HEAD -- <surface>` plus one hop of callers/callees plus sibling commits on the surface, same partition, the class ledger persisting (the receipt cites the classes not touched as standing-clean from the last full pass; a round is never a re-scope), the hygiene script at the round's start and close, the closing delta round always carrying a fresh non-authoring seat (Opus if any hunk is risky, else Sonnet; a delta round with no finder seat may not close), recorded with `round --seats <n> --findings <raw> --confirmed <n> --classes-swept … --classes-new …`; a delta round that confirms zero closes; `## BLOCKED: NON-CONVERGENCE` after three non-decreasing nonzero delta rounds stays the only other exit; EVERY site that states the old exit or the old counting rule is rewritten — `:2` (the frontmatter description's `found: 0`), `:11` (the section heading "close it only at the no-op round" → "close it only at the quiet delta round"), `:13-15`, `:20` (the run record's `--terminal "found:0 no-op round"` becomes `--terminal "confirmed:0 delta round"` — loop-shaped for `command_run.py`'s zero-rounds nudge through its `round` token, `:1872`), `:28-29` (the same paragraph's own statement of the rule — "When a round sweeps every known class with `--findings 0`" becomes "… and confirms zero (`--confirmed 0`)"), `:30` and `:391` (the two `done --evidence` strings), `:362`, `:386-388` (Phase 4's own `round` example gains `--confirmed <n>` and `:388`'s "every known class clean, `--findings 0` — is the machine-readable form of the EXIT" reads `--confirmed 0`), `:422-424` (the example rows carry `found: F, new: N, confirmed: C, fixed: X, unexecuted: U`), the whole paragraph `:429-435` (`:430-431`'s "`found:` counts only candidates NEEDING adjudication" becomes `found:` = raw recall, `confirmed:` = the exit counter, `new:` stays the stopped-learning signal — D-206; `:432`'s QUIET_PASS cross-reference names the new pair; `:433`'s "demands a quiet FINAL row" names `confirmed: 0`), `:437` and `:440` (the completion claim reads "only when the last row carries `confirmed: 0, fixed: 0` and `unexecuted: 0` or none, from a delta round with a fresh seat" and DROPS `new: 0`); the receipt gains the `## Residual` section T02's template writes. (2) `commands/_sources/fabrik-repo-review.md` — waves (`:64`, and every "wave" mention: `:2`, `:44`, `:64`, `:105`, `:162` — 5 live lines, 13 occurrences, 9 of them on `:64`; the 14th, at `:61`, sits inside the `<!-- POOL OFF -->` block `:47-63` and stays verbatim, the sanctioned exception) become slices sized by `--slices`; a wave's reservation close stays a `--seats/--findings` round with no classes (why `PER_UNIT_ROUND_COMMANDS` keeps the command, DD8); the certification pass (`:152-162`) is the class-ledger close with `--confirmed`; the exit (`:154`) reads quiet = zero CONFIRMED. (3) `commands/_sources/fabrik-review-scoped.md` (D10, DD11): `:47`'s round line gains `--confirmed <n>`; `:68-70` drops the D-191 parenthetical and reads "3 seats on ONE brief — the measured duplicate-brief technique (core/62:65): three readers of the same one-unit diff on different angles, `dispatch_headroom.py --units 1`; a multi-file diff partitions by file and sizes by `--units <N>` — the partition rule of `/fabrik-review` never applies here"; its fixes follow the delta-round rule; quiet = zero CONFIRMED, refuted never counts. (4) `commands/_sources/fabrik-rules-review.md:138` — "CITED, not counted (D-048)" becomes "CITED, not counted (D-206)". (5) `commands/_fragments/subagents-core.md:3` (one 5.6 KB line, included by 20 of 36 sources; the assembler's EXTRACT table names it at 11 command keys, PARAMS at 20 — `commands/assemble_commands.py:141-211`, `:319+`): the D-191 seat-mix sentence becomes the partition rule + the delta-round sentence for the two partitioned review loops (`/fabrik-review`, `/fabrik-repo-review` — D-207), with the units-sized floor kept for every other command (D-208) and rephrased WITHOUT the retired literal ("per independent unit one Sonnet breadth seat, plus one Haiku mechanical seat per grep-able class the surface has — the units-sized mix for a review, docs-review, sweep or audit surface; none on a judgement surface — a grounding or adjudication unit has no grep-able angle, `--mechanical 0`" — the same kind list as the `_floor()` else branch in (6), never naming a kind the judgement branch serves); the `THE DISPATCH STEP (D-191, binding on every fan-out)` heading and the `dispatch_headroom.py --units <N> [--heavy] [--risky <R>] [--mechanical <M>]` banner STAY beside a new `--slices` sentence — `assemble_commands.py`'s `_DISPATCH_STEP_RE` keys on that heading literal and `tests/test_assemble_dispatch_step.py:51-70` asserts the banner reaches ≥25 commands (30 today). (6) `commands/assemble_commands.py` `_floor()` (`:222-244`): its non-judgement `else` branch (`:234-240`) RENDERS the D-191 sentence into 10 of the 36 commands at render time with the `{native}` interpolation splitting the literal ("one Sonnet `fabrik-reviewer` breadth seat AND one Haiku mechanical seat per INDEPENDENT unit") — `fabrik-doc-converge`, `fabrik-docs-review`, `fabrik-features`, `fabrik-flows-review`, `fabrik-repo-review`, `fabrik-review`, `fabrik-rules-review`, `fabrik-spec-review`, `fabrik-ui-design-review`, `fabrik-workflow-review` (kinds `review` ×6, `sweep` ×2, `docs review`, `audit`); D-208 retires the three-angle floor ONLY for `/fabrik-review` and `/fabrik-repo-review`, so `_floor()` gains a THIRD branch keyed on a new kind `"review loop"` that only those two commands' PARAMS entries use (`:615` and `:645` today, `_floor("review", "`fabrik-reviewer`")` — the other four `review`-kind entries at `:427`, `:564`, `:584`, `:604` keep the kind `review` and the `else` branch) — the partition wording, VERBATIM so the test can assert it: `**plus the partition — the surface cut into DISJOINT slices by file: Opus on the risky slices, Sonnet {native} seats on the rest, at most ONE Haiku class seat when the brief names a non-scriptable inventory class; every file read once; sized by `dispatch_headroom.py --slices opus=N,sonnet=N,haiku=N` (D-207)`; the `_JUDGEMENT_KINDS` branch (`:226-232`) is unchanged; the `else` branch keeps the UNITS-SIZED mix for the other 8 (they are sweep/audit/review-family commands, not partitioned loops) but is rephrased to name its scope without the retired literal — VERBATIM: `**plus one Sonnet {native} breadth seat per INDEPENDENT unit and one Haiku mechanical seat per grep-able class the surface has (trimmed, each Haiku seat sweeps ONE class across every unit) — the units-sized mix for a review, docs-review, sweep or audit surface (D-208)`; `test_judgement_floors_name_no_haiku_seat_and_review_floors_name_a_class_wide_one` (`tests/test_assemble_dispatch_step.py:77-88`, asserting "one Haiku mechanical seat per INDEPENDENT unit" and "sweeps ONE grep-able class across every unit") is updated to the two new wordings. (7) Tests in `tests/test_assemble_dispatch_step.py` (5 today): V7 — over the 36 rendered commands (render to a temp dir with the assembler's render function, never to `~/.claude/commands`), `commands/_sources/*.md`, the fragment, `.windsurf/rules/core/62-using-subagents.md`, `CLAUDE.md`, `templates/governance/CLAUDE.md`, the TRACKED `scripts/**/*.py` (`git ls-files scripts/**/*.py` — 180 files; a bare glob walks ~3,200 vendored files under the gitignored `scripts/kilo-benchmarks/.lcb-venv/`), `docs/workstation/*.md`, `docs/reference/convergence-prompts.md` and `docs/reference/MD/ai-prompt-templates.md`: join each file's text with `" ".join(text.split())`, assert case-insensitively that THREE literals appear in 0 files (print the denominator) — "one sonnet breadth seat and one haiku mechanical seat", "one sonnet + one haiku", and the interpolation-tolerant "breadth seat and one haiku mechanical seat per independent unit" — that `dispatch_headroom.py`'s printed sentences carry "units-sized grounding surface", that `_budget_probe`'s rendered banner carries "Box budget (D-189" (positive control) and not the short literal, and that the two `docs/reference` files carry `confirmed: 0` (T06's edits have a grader); V8 — the fragment's delta-round sentence survives a render into `/fabrik-review`. Seen red FIRST on the population that carries a literal today — 21 of the 36 rendered commands (20 through the fragment, `fabrik-review-scoped` through its own `:69`), of which 10 also through `_floor()`, plus `templates/governance/CLAUDE.md:356`, `CLAUDE.md:355`, `scripts/sysadmin/dispatch_headroom.py:15` (the module docstring — `:602`/`:606` carry the units wording T04 rescopes, not a literal), the rendered banner and `docs/workstation/claude-account-rotation.md:227-228` — i.e. written and run BEFORE T04/T05/T06 are merged into your worktree and before your own edits, then green after both. DO-NOT: hand-edit `~/.claude/commands/*` (rendered); render to the installed corpus from a worktree (the renderer PRUNES); touch core/62, the CLAUDE.md files or the docs (T06); touch `dispatch_headroom.py` (T04); change any command other than the four named, the fragment and `_floor()`; give the partition wording to any command other than `/fabrik-review` and `/fabrik-repo-review`.

Depends: T04, T05, T06
Parallel: ⛓️
Complexity: native
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/test_assemble_dispatch_step.py -q
Docs: CHANGELOG entry (Deltas)

## Touches
- commands/_sources/fabrik-review.md — PRIMARY PATH
- commands/_sources/fabrik-repo-review.md
- commands/_sources/fabrik-review-scoped.md
- commands/_sources/fabrik-rules-review.md
- commands/_fragments/subagents-core.md
- commands/assemble_commands.py
- tests/test_assemble_dispatch_step.py

## Behavior Contract
- **Given** `commands/_sources/fabrik-review.md` after this ticket, **When** read, **Then** Phase 1 partitions by slice (Opus risky, Sonnet ordinary, at most one Haiku class seat), Phase 2 says the orchestrator executes every candidate and every refutation, Phase 4 says a delta round over the fix diff plus one hop closes on `confirmed: 0`, the `--terminal` string at `:20` names `confirmed`, the paragraph at `:429-435` defines `found:` as raw recall and `confirmed:` as the exit counter, the seat rule is stated once (the `:142` paragraph and `:166-172` merged, the failure-class GROUPS retired for file slices), and none of the old-exit sites (`:2`, `:11`, `:13-15`, `:20`, `:28-29`, `:30`, `:362`, `:386-388`, `:391`, `:422-424`, `:429-435`, `:437`, `:440`) still states `found: 0 · new: 0 · fixed: 0` or `--findings 0` as the exit (commands/_sources/fabrik-review.md:142, :166-172, :260-307, :364, :386-388, :422-424, :429-440)
- **Given** `fabrik-repo-review.md`, `fabrik-review-scoped.md` and `fabrik-rules-review.md`, **When** read, **Then** the 5 live "wave" lines (13 occurrences) are slices, the 14th (`:61`, inside `<!-- POOL OFF -->`) staying verbatim and the certification pass is the class-ledger close with `--confirmed`, the scoped command's `:47` round line carries `--confirmed` and `:68-70` keeps the 3-reader one-brief floor and the multi-file `--units <N>` clause without the D-191 parenthetical, and the rules-review's `:138` cites D-206 instead of D-048 (commands/_sources/fabrik-repo-review.md:64, :154, :162; commands/_sources/fabrik-review-scoped.md:47, :68-70; commands/_sources/fabrik-rules-review.md:138)
- **Given** `commands/_fragments/subagents-core.md:3` and `_floor()`'s three branches, **When** rendered, **Then** the partition wording reaches exactly `/fabrik-review` and `/fabrik-repo-review`, the other 8 `else`-branch commands and the 6 judgement commands keep a units-sized floor without the retired literal, the `THE DISPATCH STEP (D-191` heading and the `--units` banner survive in ≥25 commands, and no rendered command carries any of the three literals (commands/_fragments/subagents-core.md:3, commands/assemble_commands.py:234-240, :615, :645)
- **Given** the rendered corpus (36 commands), the sources, the fragment, core/62, both CLAUDE.md files, `scripts/**/*.py`, `docs/workstation/` and the two `docs/reference` files, **When** the V7 test joins each file's text with `" ".join(text.split())` and searches case-insensitively, **Then** 0 files carry any of the three literals, the banner positive control holds, the two `docs/reference` files carry `confirmed: 0`, and the test was seen red on the 21-command rendered population and the five source sites before the fixes (tests/test_assemble_dispatch_step.py:1)
- **Given** the MAIN checkout after the orchestrator merges this ticket, **When** `python3 commands/assemble_commands.py` renders and `--check` runs, **Then** `--check` exits 0 and `check_command_corpus.py` is green — the orchestrator's merge step, never the coder's gate, because `--check` diffs against the box-wide installed corpus (commands/assemble_commands.py:1035-1050, :1095)

## Context Files
- .windsurf/rules/core/40-documentation.md
- commands/_sources/fabrik-review.md
- commands/_sources/fabrik-repo-review.md
- commands/_sources/fabrik-review-scoped.md
- commands/_sources/fabrik-rules-review.md
- commands/_fragments/subagents-core.md
- commands/assemble_commands.py
- tests/test_assemble_dispatch_step.py

## Implementation notes
- The spec's text rules are at docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:131-139 (D2), :152-159 (D5), :161-166 (D6), :176-178 (D8), :184-186 (D10), :215-221 (Contract deltas), :238-239 (V7/V8) — read those lines with `sed -n` (the spec is not in Context Files: the READ budget).
- Commands are rules, not changelogs: present-tense rule, delete the old text; history lives in git. Every `{{include:…}}` directive and `{{FLOOR}}`/`{{EXTRA_LIVE}}` placeholder keeps its exact spelling — the existing tests grade unfilled placeholders; `dispatch_step_gaps()` (`assemble_commands.py:252-275`) matches "dispatched … in ONE message", which the partition prose keeps.
- `tests/sysadmin/test_dispatch_headroom.py:296`/`:399` carry the wording as a historical record inside docstrings; `tests/` is outside V7's surface.
- The rendered corpus is not tracked: render to `tmp_path` in the test and count the 36; `--check` (`assemble_commands.py:1035`) is safe from a worktree (temp render, prunes only `dest`) but diffs against `~/.claude/commands`, so it exits 1 in any worktree with source edits — that is why it is the orchestrator's merge-step gate.
