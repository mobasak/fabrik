# T06 — core/62, both CLAUDE.md files, the convergence prompts and the prompt template carry the partition rule and the new exit

## Scope
Rewrite the governance text that states the review loop's seat rule and exit — replacing, never commenting out (core/40:246): (1) `.windsurf/rules/core/62-using-subagents.md` § Dispatch policy (heading `:63`, the ~10 KB line `:65`): the D-191 sentence ("every unit gets one Sonnet breadth seat AND one Haiku mechanical seat … plus the Opus authoritative seat(s)") AND the clause that precedes it in the same line — "≥1 Opus authoritative seat on every review, regardless of diff risk" (the always-Opus re-read D6 retires, spec `:166`: the Opus seat reviews only its risky slices) — become the partition rule — a `/fabrik-review` or `/fabrik-repo-review` cuts its surface into DISJOINT slices by file, Opus on the risky units only (concurrency and locks, record and file formats, fleet-synced paths — `scripts/enforcement/`, `scripts/command_run.py`, the hooks, `templates/governance/` — auth, schema, migrations, secrets; a surface with no risky unit still gets one Opus seat over its most consequential slice, carved out of Sonnet's allocation — DD2), Sonnet on every other code and doc unit, at most ONE Haiku seat for a judgement-shaped inventory class the hygiene script cannot express and only when the brief names it, Fable (Opus by name when Fable refuses) orchestrating and EXECUTING every refutation and every confirmed reproduction, never a finder; the union of slices is the full pass, no file's logic read by two seats; sized by `dispatch_headroom.py --slices opus=N,sonnet=N,haiku=N` (T04's flag — this ticket depends on it so the spelling is frozen before the rule text names it), stamped BEFORE dispatch with `command_run.py dispatch --seats <n>`; round 1 the only full pass, every later round a DELTA over `git diff <last round's commit>..HEAD -- <surface>` plus one hop of callers/callees (by grep or `find_referencing_symbols`) plus sibling commits on the surface, the class ledger persisting across rounds (a delta round sweeps the classes its diff touches and CITES the rest as standing-clean from the last full pass; a round is never a re-scope), the closing delta round always carrying a fresh non-authoring seat (a delta round that dispatches no finder seat may not close); quiet = zero CONFIRMED (executed) code or doc defects, RECORDED and REFUTED never count (D-206); the partition and delta rules cite D-207, the floor's scope D-208, and D-203 only as the operator's ruling; the floor sentence "NEVER solo, never two — the FLOOR is three seats" gains "for every command that is not one of the two partitioned review loops (`/fabrik-review`, `/fabrik-repo-review`): `/fabrik-review-scoped`, the grounding and adjudication commands (`/fabrik-plan-review`, the researcher floors) and the sweep and audit reviews (`/fabrik-spec-review` renders the units-sized review floor, not a grounding one)" and the `/fabrik-review-scoped` one-unit clause stays; § Role separation (`:184-196`) is untouched (it is the rule DD10 cites). (2) `CLAUDE.md:355` and `templates/governance/CLAUDE.md:356` — the fan-out bullet's D-191 sentence ("one Sonnet breadth seat AND one Haiku mechanical seat, all dispatched in a SINGLE message … a 3-unit surface is 7 seats") becomes the same partition rule in one sentence plus the units-sized floor for grounding/adjudication surfaces; `CLAUDE.md:38-39` and `templates/governance/CLAUDE.md:30-31` — § COMMAND RUN-RECORD's "A round that sweeps every known class with **0 findings** IS the no-op round" becomes "A round that sweeps every known class and CONFIRMS zero code or doc defects (`round --confirmed 0`) IS the quiet round — refuted and recorded candidates never reopen the loop (D-206); a record whose rounds never state `confirmed` keeps the `--findings 0` rule"; the `round --findings <N>` example in that paragraph gains `--confirmed <N>`; and § FINAL OUTPUT's `FEEDBACK:` line placeholder `rounds <n> (<findings trend>)` (hub `CLAUDE.md:378`, template `:379`) becomes `rounds <n> (<confirmed trend, or the findings trend when a round never stated confirmed>)` — the shape T10's `_feedback_line` prints. Every anchor phrase in `CLAUDE.md` § UNIVERSAL governance markers stays byte-identical (fabrik-lib's drift check keys on them). (3) `docs/reference/convergence-prompts.md` § CODE REVIEW CONVERGENCE (`:64`) gains the delta-round brief (the fix diff + one hop, the class ledger cited as standing-clean, the fresh non-authoring seat, the 15-minute box, the D8 lessons) and the executed-refutation row shape (a refutation cites the executed command and its output); `docs/reference/MD/ai-prompt-templates.md:270` "done only when the last row reads `found: 0, fixed: 0`" becomes "done only when the last row carries `confirmed: 0, fixed: 0` (and `unexecuted: 0` or none) from a delta round with a fresh seat — a ledger without `confirmed:` closes on `found: 0, fixed: 0`". Both CLAUDE.md files, core/62 and `docs/reference/**` are fleet-synced (`fabrik_synced_manifest.py:122`, `:284`); they are distributed by T10's single forced sync at Finish — never by this ticket, because the text names `--slices` (T04) and `round --confirmed` (T10), and a sync before T10 would document a flag ~46 repos cannot run. DO-NOT: touch the command sources or the fragment (T07); change the POOL-OFF comment blocks (the sanctioned exception); reword any anchor in § UNIVERSAL governance markers; edit `templates/governance/CLAUDE.md` anywhere outside the two paragraphs (the hub/template divergences outside this surface — including the template's missing `doc-script-coupling` anchor, `CLAUDE.md:353` — are the existing `[infra]` backlog row "hub/template CLAUDE.md divergences outside the D-191 surface", `docs/STRATEGIC_BACKLOG.md:52`).

Depends: T04
Parallel: ⛓️
Complexity: native
Gate: python3 -c "import re,sys; t=lambda p: ' '.join(open(p,encoding='utf-8').read().split()).lower(); bad=[p for p in ['.windsurf/rules/core/62-using-subagents.md','CLAUDE.md','templates/governance/CLAUDE.md','docs/reference/convergence-prompts.md','docs/reference/MD/ai-prompt-templates.md'] if 'one sonnet breadth seat and one haiku mechanical seat' in t(p) or 'one sonnet + one haiku' in t(p) or 'regardless of diff risk' in t(p)]; miss=[p for p in ['docs/reference/convergence-prompts.md','docs/reference/MD/ai-prompt-templates.md'] if 'confirmed: 0' not in t(p)]; print('D-191 literal in', bad or 'none', '| docs missing confirmed: 0:', miss or 'none'); sys.exit(1 if bad or miss else 0)"
Gate: python3 -c 'import sys; a=open("CLAUDE.md",encoding="utf-8").read(); b=open("templates/governance/CLAUDE.md",encoding="utf-8").read(); ks=["COMMIT your own work NOW","PUSH it","explicit pathspecs only","Agent Provenance Trailers","NEVER `--force`","EXECUTE the real check","A bounded search returns \"not found in N\"","its row in `docs/DECISIONS.md` in the SAME change","`NEXT: operator decision` HAS A BAR","The header is the only hand-written half","EVERY code-changing chunk of work gets a review-family pass","last 7 lines of every task-completing response","CLEAN your own scratch"]; miss=[k for k in ks if k not in a]; tm=[k for k in ks if k not in b and k!="The header is the only hand-written half"]; print("hub anchors missing:", miss or "none", "| template anchors missing:", tm or "none"); sys.exit(1 if miss or tm else 0)'
Docs: `docs/reference/convergence-prompts.md` and `docs/reference/MD/ai-prompt-templates.md` (Touches); CHANGELOG entry (Deltas)

## Touches
- .windsurf/rules/core/62-using-subagents.md — PRIMARY PATH
- CLAUDE.md
- templates/governance/CLAUDE.md
- docs/reference/convergence-prompts.md
- docs/reference/MD/ai-prompt-templates.md

## Behavior Contract
- **Given** `.windsurf/rules/core/62-using-subagents.md` after this ticket, **When** read line-joined, **Then** § Dispatch policy carries the partition rule, the delta-round rule and the executed-critic rule, the `:65` floor sentence reads "NEVER solo, never two — the FLOOR is three seats for `/fabrik-review-scoped` and the grounding commands", and neither D-191 literal remains (.windsurf/rules/core/62-using-subagents.md:63-65)
- **Given** `CLAUDE.md` and `templates/governance/CLAUDE.md`, **When** read line-joined, **Then** the fan-out bullet carries the partition rule with neither D-191 literal, the § COMMAND RUN-RECORD paragraph says quiet = zero CONFIRMED with `round --confirmed`, the `FEEDBACK:` placeholder names the confirmed trend, and every anchor phrase in § UNIVERSAL governance markers is byte-identical to today (CLAUDE.md:38-39, :355, :378; templates/governance/CLAUDE.md:30-31, :356, :379)
- **Given** `docs/reference/convergence-prompts.md` § CODE REVIEW CONVERGENCE and `docs/reference/MD/ai-prompt-templates.md:270`, **When** read, **Then** the first carries the delta-round brief and the executed-refutation row shape and the second's "done" row reads the five-counter row with `confirmed: 0` as the exit (docs/reference/convergence-prompts.md:64, docs/reference/MD/ai-prompt-templates.md:270)

## Context Files
- .windsurf/rules/core/40-documentation.md
- .windsurf/rules/core/62-using-subagents.md
- CLAUDE.md
- templates/governance/CLAUDE.md
- docs/reference/convergence-prompts.md
- docs/reference/MD/ai-prompt-templates.md

## Implementation notes
- The spec's text for the rule is at docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:131-139 (D2), :152-159 (D5), :161-166 (D6), :145 (the floor under partition) — the coder reads those lines only (`sed -n`), not the whole spec (READ budget).
- The hub and template CLAUDE.md files differ by one line above the fan-out bullet (`:355` vs `:356`); edit each by content, never by line number copied from the other.
- The V7 corpus test (T07) is the grader for the absence claims; this ticket's Gate is the same search over its five files. Both Gate lines are single-quoted for the shell so backticks reach Python untouched; Gate 2 exits 0 today (the template's one missing anchor, `The header is the only hand-written half`, is the pre-existing divergence the backlog row owns and is exempted for the template only) and exits 1 the moment any hub anchor is reworded.
