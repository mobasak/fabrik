# T08 — `check_review_hygiene.py`: the grep-shaped classes as an advisory script

## Scope
Create `scripts/enforcement/check_review_hygiene.py` (fleet-synced by directory — `fabrik_synced_manifest.py:125-126`; stdlib only; a real `# AFTER-EDIT:` comment on line 2 naming `tests/enforcement/test_check_review_hygiene.py docs/workflows/FINAL_GATE_WORKFLOW.md`): the grep-shaped classes every review re-sweeps (spec D4, docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:147-150; DD6 `:273`; DD13 `:280`), run by the orchestrator at the START of every round on that round's surface and again at the round's CLOSE, and by the flip. CLI: `--surface <path>…` (files or dirs; the review's surface) and/or `--receipt <path>` (a receipt under `docs/development/reviews/`), `--phrase <text>` (repeatable: stale phrases the brief names), `--symbol <name>` (repeatable: a symbol that must still be referenced — `grep -c` over the surface = 0 is a hit), `--json`. Classes, each a `path:line` hit with its class name: (a) `{{…}}` residue in rendered commands (`~/.claude/commands/*.md` when the surface names them, or any `.md` on the surface) — not covered by `check_command_corpus.py` today (its `{{` hits at `:1089-1094` test PRESENCE of `{{include:run-record}}`, never residue); (b) fence parity by the CommonMark same-char-run rule — a fence closes only on a same-char run at least as long — copied from the private tracker in `check_command_corpus.py:943-960` (it is not exported; copy the 12 lines with a comment naming the source, do not import a private helper); (c) receipt row shape: an unescaped `|` inside a table cell (F280's class — `docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md:636` at 741eebbf carried `… one of the three | RECORDED …` mid-cell; a cell count that differs from the header's is the detector, after `\|` and code-span pipes are masked) and a disposition cell carrying more than one verdict token (F314's class, `:670` — count `VERDICT` matches per cell, importing `VERDICT` and `_table_rows` from `check_review_coverage` after T02 widened `VERDICT` to the RECORDED forms); (d) CHANGELOG entry shape under `## [Unreleased]` via `check_changelog.py`'s `check_changelog_quality()` (`:132`) called, not copied; (e) dead symbols (`--symbol`) by `grep -c` = 0 over the surface; (f) stale phrases (`--phrase`) case-insensitively over line-joined text of every `.md`/`.py` on the surface (line-joined because two of the D-191 sentence's sites wrap). Contract: ALWAYS exit 0 (`check_retired_terms.py:1-14`'s contract — a non-zero exit from a `warn_only` check is a BLOCKING red across ~46 repos), print `[ADVISORY] <class> <path>:<line> — <what>` lines and a final `hygiene: N hit(s) over M file(s)` denominator line; `--json` emits the same as a list. Register it in `scripts/final_gate.py` with `run_optional_check("scripts/enforcement/check_review_hygiene.py", "Review hygiene (advisory)", warn_only=True)` beside the other advisory checks (`:1186`, `:1203` — the `warn_only` contract at `:347-374`), inert when no `docs/development/reviews/*.md` changed (the same guard `check_review_coverage.py`'s registration uses at `:1163-1166`); add its row to `docs/workflows/FINAL_GATE_WORKFLOW.md`'s per-check reference. Graduation (DD6): the script stays advisory until infra measures its false-positive rate below 5 % over 20 receipts from the receipts' own `RECORDED — hygiene false positive (…)` rows — the backlog row T09 writes; do NOT add a blocking mode now. Tests (`tests/enforcement/test_check_review_hygiene.py`, red-first): V6 — the receipt at 741eebbf (`git show 741eebbf:docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md` into a tmp file) yields the F280 raw-pipe hit at its row and the F314 dual-verdict hit, and the receipt at 69f01b92 yields 0 hits; a rendered command fixture with `{{X}}`; an unclosed fence run and a 4-backtick fence holding a 3-backtick one (no hit); a `--symbol` present/absent; a `--phrase` that only matches line-joined; exit code 0 on every fixture including a hit; `--json` shape. DO-NOT: re-implement the ledger grammars (`_MEGA_ROW`/`_pass_counters` are the coverage gate's); add a pre-commit hook (the orchestrator invokes it; the gate registration is advisory) — say so in the docstring; edit `check_command_corpus.py`, `check_changelog.py` or `check_review_coverage.py`; write to any file.

Depends: T02
Parallel: ⛓️
Complexity: never-route
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_check_review_hygiene.py -q
Gate: python3 scripts/enforcement/check_review_hygiene.py --receipt docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md && python3 scripts/render_doc_script_links.py --check
Docs: docs/workflows/FINAL_GATE_WORKFLOW.md (Touches); INDEX rows for the script and its test (Deltas); CHANGELOG entry (Deltas)

## Touches
- scripts/enforcement/check_review_hygiene.py — PRIMARY PATH
- tests/enforcement/test_check_review_hygiene.py
- scripts/final_gate.py
- docs/workflows/FINAL_GATE_WORKFLOW.md

## Behavior Contract
- **Given** the D-191 receipt at 741eebbf (`git show`), **When** `check_review_hygiene.py --receipt` runs on it, **Then** it reports F280's unescaped `|` inside a table cell and F314's disposition cell carrying two verdict tokens; at 69f01b92 it is silent (docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md:636, :670)
- **Given** a rendered command carrying `{{X}}` residue, a markdown file with an unclosed fence run, a symbol named with `--symbol` that `grep -c` finds 0 times, and a phrase named with `--phrase`, **When** the script runs with `--surface`, **Then** each is reported as its class with `path:line`, and the fence class follows the CommonMark same-char-run rule copied from `check_command_corpus.py:943-960` (scripts/enforcement/check_command_corpus.py:943)
- **Given** any input, **When** the script runs, **Then** it exits 0, prints `[ADVISORY]`-prefixed lines, emits `--json` for the orchestrator, and is registered `warn_only=True` in `final_gate.py` beside the other advisory checks (scripts/final_gate.py:347-374, :1186)
- **Given** a receipt whose ledger row was fixed at a round's start by a hygiene hit, **When** the row is written, **Then** the hit counts as a confirmed doc defect of that round and a hit adjudicated false is `RECORDED — hygiene false positive (<why>)`, never counted — the verdict shape T02's `VERDICT` accepts (scripts/enforcement/check_review_coverage.py:51)

## Context Files
- .windsurf/rules/core/10-python.md
- scripts/final_gate.py
- docs/workflows/FINAL_GATE_WORKFLOW.md
- scripts/enforcement/check_retired_terms.py

## Implementation notes
- Read `check_command_corpus.py:943-960` and `check_changelog.py:120-160` with `sed -n` (not in Context Files — the READ budget); `check_review_coverage.py`'s `VERDICT` (`:51`) and `_table_rows` (`:122-213`) are imported, read their docstrings only.
- The 741eebbf fixture is fetched by `git show` inside the test into `tmp_path` (never a tracked-path grep); mark the test `skipif` when the object is absent (a shallow clone), with the reason printed.
- `render_doc_script_links.py --check` renders `docs/workflows/FINAL_GATE_WORKFLOW.md`'s `## Related scripts` block from the header — never hand-edit that block; the per-check row you add is prose above it.
- The INDEX row copies `check_convergence.py`'s row shape at `INDEX.md:1158` (link, one-line purpose, tier/scope note) — `check_review_coverage.py` itself has no INDEX row (a pre-existing gap, out of scope).
