# T08 — `check_review_hygiene.py`: the grep-shaped classes as an advisory script

## Scope
Create `scripts/enforcement/check_review_hygiene.py` (fleet-synced by directory — `fabrik_synced_manifest.py:125-126`; stdlib only; a real `# AFTER-EDIT:` comment on line 2 naming `tests/enforcement/test_check_review_hygiene.py docs/workflows/FINAL_GATE_WORKFLOW.md`): the grep-shaped classes every review re-sweeps (spec D4, docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:147-150; DD6 `:273`; DD13 `:280`), run by the orchestrator at the START of every round on that round's surface and again at the round's CLOSE, and at the flip. CLI: `--surface <path>…` (files or dirs; the review's surface) and/or `--receipt <path>` (a receipt under `docs/development/reviews/`), `--phrase <text>` (repeatable: stale phrases the brief names), `--symbol <name>` (repeatable: a symbol that must still be referenced — `grep -c` over the surface = 0 is a hit), `--json`; with NO arguments (the gate registration) it self-selects the changed receipts from `git status --porcelain -- docs/development/reviews/` and is silent when there are none — the same selection `check_review_coverage.py`'s `main()` makes with `_changed_md` at `:1650` (the comment block at `final_gate.py:1163-1166` explains why that registration passes no arguments). Classes, each a `path:line` hit with its class name: (a) `{{…}}` residue in rendered commands (`~/.claude/commands/*.md` when the surface names them, or any `.md` on the surface) — not covered by `check_command_corpus.py` today (its `{{` hits at `:1089-1094` test PRESENCE of `{{include:run-record}}`, never residue); (b) fence parity by the CommonMark same-char-run rule — a fence closes only on a same-char run at least as long with no info string — copied from `_fence_step` at `check_command_corpus.py:832-851` (20 lines from the `def` at `:832` to its last `return` at `:851`, numbered at 8092e8a8 — the working tree carries a sibling's uncommitted edit that shifts this file; read it with `git show 8092e8a8:<path>`; it is not exported: copy them with a comment naming the source, do not import a private helper); (c) receipt row shape: an unescaped `|` inside a table cell (F280's class — at 741eebbf the row at `docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md:596` carried `… one of the three | RECORDED …` mid-cell, one pipe more than the header row at `:315`; the detector is a pipe count that differs from the header's after `\|` and code-span pipes are masked) and a disposition cell carrying more than one verdict WORD — count bare occurrences of `CLEAN|FIXED|REFUTED|ROUTED|RECORDED` per cell, NOT `VERDICT` matches, because F314's cell (`:670` at 69f01b92 and HEAD: `RECORDED — the F250 shape … counted here as RECORDED`) carries `RECORDED` twice and matches `VERDICT` zero times under both today's and T02's grammar; measured at 69f01b92 the class fires on 6 of the receipt's 366 disposition-bearing rows (`:419`, `:540`, `:649`, `:670`, `:683`, `:694` — the receipt's own F338 named the dual-verdict class as the defect its tally mis-bucketed), and the orchestrator adjudicates each hit (FIXED or `RECORDED — hygiene false positive (<why>)`); (d) CHANGELOG entry shape under `## [Unreleased]` via `check_changelog.py`'s `check_changelog_quality()` (`:132`) called, not copied; (e) dead symbols (`--symbol`) by `grep -c` = 0 over the surface; (f) stale phrases (`--phrase`) case-insensitively over line-joined text of every `.md`/`.py` on the surface (line-joined because two of the D-191 sentence's sites wrap). Contract: ALWAYS exit 0 (`check_retired_terms.py:1-14`'s contract — a non-zero exit from a `warn_only` check is a BLOCKING red across ~46 repos), print `[ADVISORY] <class> <path>:<line> — <what>` lines and a final `hygiene: N hit(s) over M file(s)` denominator line; `--json` emits the same as a list. Register it in `scripts/final_gate.py` with `run_optional_check("scripts/enforcement/check_review_hygiene.py", "Review hygiene (advisory)", warn_only=True)` beside the other advisory checks (`:1186`, `:1203` — the `warn_only` contract at `:347-374`); add its row to `docs/workflows/FINAL_GATE_WORKFLOW.md`'s per-check reference (prose; the `## Related scripts` block between the `BEGIN/END related-scripts` markers at `:1072`/`:1107` is RENDERED — run `python3 scripts/render_doc_script_links.py` (mutating; it writes only between the markers of the docs a header names) after adding the header, then `--check`). Graduation (DD6): the script stays advisory until infra measures its false-positive rate below 5 % over 20 receipts from the receipts' own `RECORDED — hygiene false positive (…)` rows — the backlog row T10 writes; do NOT add a blocking mode now. Tests (`tests/enforcement/test_check_review_hygiene.py`, red-first): V6 as the spec's fixture CORRECTED at plan time (the spec named 741eebbf for both F280 and F314 and 69f01b92 as silent; F314 did not exist at 741eebbf — it was found in round 13, one commit later — and its dual-verdict cell exists at 69f01b92, the receipt's current BLOCKED state, so no commit is silent): `git show 741eebbf:<receipt>` into `tmp_path` yields exactly the F280 raw-pipe hit at `:596` and no dual-verdict hit at that row; `git show 69f01b92:<receipt>` yields no raw-pipe hit (F280 is escaped at `:636`) and dual-verdict hits on exactly the 6 measured rows, F314 at `:670` among them (assert the set and print the count); a synthetic receipt with neither yields 0 hits; a rendered command fixture with `{{X}}`; an unclosed fence run and a 4-backtick fence holding a 3-backtick one (no hit); a `--symbol` present/absent; a `--phrase` that only matches line-joined; exit code 0 on every fixture including a hit; `--json` shape; the no-argument mode on a scratch repo with one changed receipt and with none (`git init` inside the fixture). DO-NOT: re-implement the ledger grammars (`_MEGA_ROW`/`_pass_counters` are the coverage gate's — import `_table_rows` from `check_review_coverage`, same package, and nothing else); add a pre-commit hook (the orchestrator invokes it; the gate registration is advisory — say so in the docstring); edit `check_command_corpus.py`, `check_changelog.py` or `check_review_coverage.py`; write to any file.

Depends: T02
Parallel: ⛓️
Complexity: never-route
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_check_review_hygiene.py -q
Gate: python3 scripts/enforcement/check_review_hygiene.py --receipt docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md && python3 scripts/render_doc_script_links.py && python3 scripts/render_doc_script_links.py --check
Docs: docs/workflows/FINAL_GATE_WORKFLOW.md (Touches); INDEX rows for the script and its test (Deltas); CHANGELOG entry (Deltas)

## Touches
- scripts/enforcement/check_review_hygiene.py — PRIMARY PATH
- tests/enforcement/test_check_review_hygiene.py
- scripts/final_gate.py
- docs/workflows/FINAL_GATE_WORKFLOW.md

## Behavior Contract
- **Given** the D-191 receipt at 741eebbf and at 69f01b92 (`git show` into `tmp_path`), **When** `check_review_hygiene.py --receipt` runs on each, **Then** the first yields exactly F280's unescaped `|` inside a table cell at `:596` and the second yields dual-verdict hits on exactly the 6 measured rows with F314's cell at `:670` among them and no raw-pipe hit; a synthetic receipt with neither yields 0 hits (docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md:596, :670)
- **Given** a rendered command carrying `{{X}}` residue, a markdown file with an unclosed fence run, a symbol named with `--symbol` that `grep -c` finds 0 times, and a phrase named with `--phrase`, **When** the script runs with `--surface`, **Then** each is reported as its class with `path:line`, and the fence class follows the CommonMark same-char-run rule copied from `_fence_step` (scripts/enforcement/check_command_corpus.py:832-851)
- **Given** any input, **When** the script runs, **Then** it exits 0, prints `[ADVISORY]`-prefixed lines, emits `--json` for the orchestrator, self-selects changed receipts from `git status` when given no arguments, and is registered `warn_only=True` in `final_gate.py` beside the other advisory checks (scripts/final_gate.py:347-374, :1186)
- **Given** a receipt whose ledger row was fixed at a round's start by a hygiene hit, **When** the row is written, **Then** the hit counts as a confirmed doc defect of that round and a hit adjudicated false is `RECORDED — hygiene false positive (<why>)`, never counted — the verdict shape T02's `VERDICT` accepts (scripts/enforcement/check_review_coverage.py:51)

## Context Files
- .windsurf/rules/core/10-python.md
- scripts/final_gate.py
- docs/workflows/FINAL_GATE_WORKFLOW.md
- scripts/enforcement/check_retired_terms.py

## Implementation notes
- The seam test named in the spine's § Interfaces is `test_the_dual_verdict_class_counts_bare_verdict_words` — write it under that name.
- Read `check_command_corpus.py:832-851` and `check_changelog.py:120-160` with `sed -n` (not in Context Files — the READ budget); `check_review_coverage.py`'s `_table_rows` (`:122-213`) is imported — read its docstring and `main()`'s `_changed_md` selection (`:1650`) only.
- The two receipt fixtures are fetched by `git show <sha>:<path>` inside the test into `tmp_path` (never a tracked-path grep); mark the test `skipif` when either object is absent (a shallow clone), with the reason printed.
- The INDEX row copies `check_convergence.py`'s row shape at `INDEX.md:1158` (link, one-line purpose, tier/scope note) — `check_review_coverage.py` itself has no INDEX row (a pre-existing gap, out of scope).
