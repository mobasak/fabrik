# T02 — Coverage gate: the token, counter and header rules, the RECORDED verdicts, the residual and finders-cell checks, the receipt template

## Scope
On top of T01's parse (never re-parse; consume the 5-tuple and `_MEGA_ROW`'s four groups), implement in `scripts/enforcement/check_review_coverage.py` the refusal half of D7 (docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:170-171) and the verdict contract (D1 `:124-129`, D6 `:161-166`, V5 `:236`, V11 `:242`): (1) the TOKEN RULE — over every `|`-leading line of a ledger block (a contiguous pipe block holding ≥1 row either grammar parsed OR carrying a new-grammar token; the disposition and checklist tables fall out of scope by this test — their verdicts are parenthetical), scan every cell case-INSENSITIVELY for `(?<![\w-])(confirmed|unexecuted)\s*:`; refuse the row by name unless the resolving grammar consumed EVERY occurrence as a numeric counter (cell-anchored: the span from the literal to the end of its `(\d+)` group; Pass path: only tokens inside the counter cell that `_pass_counters` read), with ONE carve-out per occurrence: a prose verdict LABEL — ALL-CAPS literal; outside the counter cell; what follows the colon is not a VALUE (not empty, not digit-leading, not `n/a`); the resolving grammar read NO numeric `confirmed:`/`unexecuted:` counter on that row; and the row is resolved by a grammar or carries no numeric `found:`/`fixed:` token at all — measured: 28 corpus method cells / 30 occurrences exempt, 0 lost, fire rate ONE row (`docs/development/reviews/2026-08-31-plan-1-manifesto-command-pass-T20-fabrik-release-review.md:15`, `(verifier confirmed :63; :47-53)`), which the ORCHESTRATOR repairs as a Delta at this ticket's merge (a receipt under `docs/development/reviews/` is orchestrator territory, never a coder's Touches) — escape the cell as `(verifier confirmed at :63; :47-53)`; the refusal message names the repair per path (cell-anchored: "write `| confirmed: C |` between `found:` and `fixed:`, one counter per cell, no `new:` cell, no wrapper"; Pass row: "write `confirmed:` between `new:` and `fixed:` inside the counter cell — inside the `found:` cell when `found:` and `fixed:` sit in separate cells"). (2) The COUNTER RULE, both grammars: `unexecuted:` captured with no `confirmed:` (cell-anchored group 4 without group 2; Pass path: the token in the counter cell without a `confirmed:` token) refuses the row — `| u | x | found: 0 | fixed: 0 | unexecuted: 2 |` and `| Pass 19 | opus×1 | found: 0, fixed: 0, unexecuted: 2 | m |` are never old-grammar quiet; a refusal always preempts the quiet computation. (3) The HEADER RULE by CONTENT: at most ONE header per contiguous pipe block — the block's FIRST non-separator row, and only if it carries no numeric `found:`/`fixed:`/`confirmed:`/`unexecuted:` token in any cell; it is exempt from the token rule and not counted when testing whether the block parses; a second header-shaped row inside a block is a data row and refused; a header-and-separator-only block (the `--init` shape, `scripts/review_receipt.py:165-166`) is not yet a ledger, never a refused block; do NOT reuse `_table_rows`'s separator-adjacency test (`:176-213`, the round-89 swallow at `:201-210`) — `_ledger_shapes` reads `founds[-1]` from `ordered`, so the two readers must agree on which row is data. (4) `VERDICT` (`:51`) accepts `RECORDED\s*—\s*(unexecuted|by design|measured|hygiene false positive)\s*\(` beside CLEAN/FIXED/REFUTED/ROUTED — the parenthesised form for all four (D6: parentheses, never a colon; this ticket pins `RECORDED — hygiene false positive (<why>)`), so a RECORDED checklist row is no longer a `noverdict` row at `:532-539`. (5) The RESIDUAL check (V5): every `RECORDED — by design (` occurrence outside fences, anywhere in the receipt, is parsed as `(<owner>, round N[; <owner>, round N]…)` or `(D-nnn)`; for each `F-nnn` owner the row `| F-nnn |` must exist in the receipt and every N must be smaller than the closing row's `Pass N` (the verdict's own N — never a scrape of the owning row's prose); a `D-nnn` owner passes with no round token; failure refuses by name. `unexecuted: U > 0` on the closing row is not quiet (already T01's exit rule — assert it here on the residual fixture). `RECORDED — measured (…)` and `RECORDED — unexecuted (…)` never enter any counter. (6) The FINDERS-CELL rule (V11): the closing Pass row's second cell must carry at least one model token `(opus|sonnet|haiku)×\d+` (the shape every receipt writes, e.g. `native opus×1 + sonnet×3`); a closing row whose finders cell names no model is refused by name — no run-record reader is added (the gate has none today; adding one would be a new mechanism). (7) `scripts/review_receipt.py --init` (`:159-173`): the Pass Ledger prose says `found: F, new: N, confirmed: C, fixed: X, unexecuted: U`, the fenced example rows carry the five counters (`| Pass 2 | … | found: 0, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | method: re-derivation |`), the verdict grammar sentence at `:154-157` gains the four RECORDED forms, and `render()` (`:126-187`) emits a `## Residual` section between `## Pass Ledger` and `## Per-phase verdicts` with one sentence naming the three residual verdicts; `tests/test_review_receipt.py:74-95` (the mechanically-completed skeleton) is extended with a new-grammar closing row and a RECORDED checklist row, seen red first. The AFTER-EDIT header of `review_receipt.py` already couples this gate and its test. DO-NOT: change T01's grammars, tuple or exit rule (extend, never re-parse); add a `new:` slot; touch `check_convergence.py` (T03); write a run-record reader; edit `tests/enforcement/test_mega_validation_reports.py` (must stay green unchanged — its bare-colon header rows `| round | found: | fixed: | … |` at `:87`, `:402-403` rely on "a bare colon is not a numeric token").

Depends: T01
Parallel: ⛓️
Complexity: never-route
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_review_refusals.py tests/enforcement/test_review_confirmed_grammar.py tests/enforcement/test_review_exit_contract.py tests/enforcement/test_mega_validation_reports.py tests/test_review_receipt.py tests/test_check_review_coverage_rederivation.py -q
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_review_refusals.py -q -k corpus
Docs: CHANGELOG entry (Deltas); INDEX row for the new test file (Deltas)

## Touches
- scripts/enforcement/check_review_coverage.py — PRIMARY PATH
- tests/enforcement/test_review_refusals.py
- scripts/review_receipt.py
- tests/test_review_receipt.py

## Behavior Contract
- **Given** the row `| 19 | found: 0 | fixed: 0 | confirmed: 3 |` under a real vocabulary header with one prior parsing data row, **When** the gate runs, **Then** the row is REFUSED by name with the cell-anchored repair message, and every REFUSED fixture in the spec's V2 list is refused (scripts/enforcement/check_review_coverage.py:996)
- **Given** `| Pass 19 | opus×1 | found: 0, fixed: 0 | delta (confirmed: 3) |`, **When** parsed, **Then** it is refused (a token outside the counter cell) while `| Pass 19 | opus×1 | **found: 0, confirmed: 0** | **fixed: 0** |` parses and reads quiet (scripts/enforcement/check_review_coverage.py:1054)
- **Given** `| u | x | found: 0 | fixed: 0 | unexecuted: 2 |` or `| Pass 19 | opus×1 | found: 0, fixed: 0, unexecuted: 2 | m |`, **When** the gate runs, **Then** each is refused by the counter rule (`unexecuted:` without `confirmed:`), never read as old-grammar quiet (scripts/enforcement/check_review_coverage.py:1117-1121)
- **Given** a method cell `HIGH/CONFIRMED: two MORE anti-cheat errors` on a resolved old-grammar row, **When** the case-insensitive token scan runs, **Then** it is exempt under the five-conjunct carve-out (the 28 corpus cells, 30 occurrences, 0 lost) and `| 19 | found: 0 | fixed: 0 | Confirmed: 3 defects stand |` is refused; the rule's fire rate over the 275 receipts is exactly 1 row, `docs/development/reviews/2026-08-31-plan-1-manifesto-command-pass-T20-fabrik-release-review.md:15`, repaired by the orchestrator's Delta at this ticket's merge (scripts/enforcement/check_review_coverage.py:1019-1020)
- **Given** a header row `| Pass | Finders | found: F, confirmed: C, fixed: X | Method |` with its separator and one parsing data row, **When** the block parses, **Then** the header is exempt from the token rule and not counted when testing whether the block parses; a second header-shaped row inside the same block is a refused data row; the `--init` header with no data row is not yet a ledger (scripts/enforcement/check_review_coverage.py:176-213)
- **Given** a closing Pass row carrying `unexecuted: 1`, **When** the gate runs, **Then** the receipt is not quiet; a residual row `RECORDED — by design (F342, round 15; F358, round 17)` under a closing `Pass 18` passes, `(F999, round 18)` is refused by name, `(D-203)` passes, and `RECORDED — measured (<why>)` never enters `confirmed:` or `unexecuted:` (scripts/enforcement/check_review_coverage.py:520-539)
- **Given** a closing Pass row whose finders cell names no model token (`opus×N`, `sonnet×N`, `haiku×N`), **When** the gate runs, **Then** it is refused; a cell reading `native sonnet×2` passes (scripts/enforcement/check_review_coverage.py:1064)
- **Given** `review_receipt.py --init`, **When** it writes a skeleton, **Then** its Pass Ledger prose and fenced examples carry the five-counter row shape and the four RECORDED verdicts, the skeleton carries a `## Residual` section, `VERDICT` accepts `RECORDED — unexecuted (…)`, `RECORDED — by design (…)`, `RECORDED — measured (…)` and `RECORDED — hygiene false positive (…)`, and the mechanically-completed skeleton test passes with a new-grammar closing row (scripts/review_receipt.py:161-172, scripts/enforcement/check_review_coverage.py:51)

## Context Files
- .windsurf/rules/core/10-python.md
- scripts/enforcement/check_review_coverage.py
- scripts/review_receipt.py
- tests/test_review_receipt.py
- docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md

## Implementation notes
- The V2 fixture list at the spec's `:233` is the test's parametrisation: every SINGLE-ROW fixture is embedded under a real vocabulary header with one prior parsing data row (so the run grades the row, not the header rule); the block fixtures (the `--init` shape, the counter-vocabulary header alone, with one data row, the headerless two-row block, the three-row all-token block, the stray-separator variants) are blocks by design. Group them: REFUSED-by-token, REFUSED-by-counter, NOT-refused-and-parsed, NOT-refused-and-out-of-threat-model.
- Measure the carve-out over the corpus in the test (`git ls-files docs/development/reviews/*.md`, 275): 28 exempt cells / 30 occurrences under the adopted rule; exactly 1 refused row (T20). The corpus test reads the committed T20 file at `git show HEAD~:…` and asserts exactly that one refusal; the orchestrator applies the repair (`(verifier confirmed at :63; :47-53)` — a colon no longer directly after the word) as a Delta at merge and re-runs the gate on the file in the main checkout.
- The by-design owner check compares the verdict's own N; the closing `Pass N` comes from the LAST ledger row's head (`_PASS_HEAD`, `:1011`), never from a scrape of prose.
- Red-first on every row: each REFUSED fixture must pass the gate silently BEFORE the rule lands (old grammar reads it inert or quiet) and be refused after.
