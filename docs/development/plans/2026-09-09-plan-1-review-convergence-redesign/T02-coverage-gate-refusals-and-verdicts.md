# T02 — Coverage gate: the token, counter and header rules, the RECORDED verdicts, the residual and finders-cell checks, the receipt template

## Scope
On top of T01's parse (never re-parse; consume the 5-tuple, `_ledger_shapes`'s fourth element `refusals`, `_pass_counters_ext`'s reason strings and `_MEGA_ROW`'s four groups — this ticket turns T01's generic "Pass row refused: <reason>" into the per-path repair wording below), implement in `scripts/enforcement/check_review_coverage.py` (line numbers at 8092e8a8) the refusal half of D7 (docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:170-171 — read with the Read tool at offset 170 limit 2) and the verdict contract (D1 `:124-129`, D6 `:161-166`, V5 `:236`, V11 `:242`): (1) the TOKEN RULE — over every `|`-leading line of a LEDGER BLOCK (a contiguous pipe block holding ≥1 row either grammar parsed OR carrying a new-grammar token; a token-bearing block in which nothing parses is refused by name too, never treated as "not a ledger"; the disposition and checklist tables fall out of scope only because their verdicts are parenthetical and carry no colon token — a disposition block that DOES carry a colon token is in scope and refused by name) AND over every Pass-headed PROSE line the prose path of `_ledger_shapes` reads (`_PASS_HEAD` is the scope test — never ordinary narrative; D7's own clause, spec `:171`: a token on such a line outside the counter run `_pass_counters_ext` read refuses the line exactly as a token in a method cell does — at 8092e8a8 one Pass-headed prose line in the 275 receipts carries an occurrence outside a run, `docs/development/reviews/2026-08-04-plan-1-spine-ticket-plans-review.md:37`, a `CONFIRMED:` label on a line with no numeric counter: exempt by the carve-out, so the prose path refuses 0 today), scan every cell and every such prose line case-INSENSITIVELY for `(?<![\w-])(confirmed|unexecuted)\s*:`; refuse the row by name unless the resolving grammar consumed EVERY occurrence as a numeric counter (cell-anchored: the span from the literal to the end of its `(\d+)` group; Pass path: only tokens inside the counter run that `_pass_counters_ext` read), with ONE carve-out per occurrence — a prose verdict LABEL: ALL-CAPS literal; outside the counter cell; what follows the colon is not a VALUE (not empty, not digit-leading, not `n/a`); the resolving grammar read NO numeric `confirmed:`/`unexecuted:` counter on that row; and the row is resolved by a grammar or carries no numeric `found:`/`fixed:` token at all — measured over the ledger-block path alone (the header exempt; the prose path's own figure is given above) at 8092e8a8: 28 in-scope rows / 30 occurrences, of which 27 cells / 29 occurrences are exempt and ONE row is refused (`docs/development/reviews/2026-08-31-plan-1-manifesto-command-pass-T20-fabrik-release-review.md:15`, `(verifier confirmed :63; :47-53)`) — the spec's 28/30-of-29/31 was measured over every `|`-leading line, and the one row that drops out is `docs/development/reviews/2026-08-10-plan-2-review-loop-overhaul-review.md:92`, a narrated `Pass 11` row that the content rule classifies as its block's header (it carries no numeric token); do not widen the scan to recover the spec's figure — that would refuse a narrated closing row as a data row — which the ORCHESTRATOR repairs as a Delta at this ticket's merge (a receipt under `docs/development/reviews/` is orchestrator territory, never a coder's Touches) — escape the cell as `(verifier confirmed at :63; :47-53)`; the refusal message names the repair per path (cell-anchored: "write `| confirmed: C |` between `found:` and `fixed:`, one counter per cell, no `new:` cell, no wrapper"; Pass row: "write `confirmed:` between `new:` and `fixed:` inside the counter cell — inside the `found:` cell when `found:` and `fixed:` sit in separate cells, where `unexecuted:` follows `confirmed:` in that same cell"). (2) The COUNTER RULE, both grammars: `unexecuted:` captured with no `confirmed:` (cell-anchored group 4 without group 2; Pass path: the token in the counter run without a `confirmed:` token) refuses the row — `| u | x | found: 0 | fixed: 0 | unexecuted: 2 |` and `| Pass 19 | opus×1 | found: 0, fixed: 0, unexecuted: 2 | m |` are never old-grammar quiet; a refusal always preempts the quiet computation. (3) The HEADER RULE by CONTENT: at most ONE header per contiguous pipe block — the block's FIRST non-separator row, and only if it carries no numeric `found:`/`fixed:`/`confirmed:`/`unexecuted:` token in any cell (a bare colon `found:` with no digit is not a numeric token — `tests/enforcement/test_mega_validation_reports.py:71`, `:402-403` at 8092e8a8 rely on it); it is exempt from the token rule and not counted when testing whether the block parses; a second header-shaped row inside a block is a data row and refused; a header-and-separator-only block (the `--init` shape, `scripts/review_receipt.py:165-166`) is not yet a ledger, never a refused block; do NOT reuse `_table_rows`'s separator-adjacency test (`:176-213`, the round-89 swallow at `:201-210`) — `_ledger_shapes` reads `founds[-1]` from `ordered`, so the two readers must agree on which row is data. (4) `VERDICT` (`:51`) accepts `RECORDED\s*—\s*(unexecuted|by design|measured|hygiene false positive)\s*\(` beside CLEAN/FIXED/REFUTED/ROUTED — the parenthesised form for all four (D6: parentheses, never a colon; this ticket pins `RECORDED — hygiene false positive (<why>)`), so a RECORDED checklist row is no longer a `noverdict` row at `:532-539`; `VERDICT` is ALSO imported by `check_convergence.py` (`:429`, `:439`) and applied at its `:477` — the widening reaches that gate's checklist grading on sync day (a RECORDED row stops being `noverdict` there too); T03 is told. (5) The RESIDUAL check (V5) — a NEW whole-document scan sited beside `check_file`'s checklist walk (`:520-539`), never inside `_checklist_section`'s scope, run over `text_s` (the `_strip_fences` result already computed at `:484` — never raw `text`, so fenced examples are invisible to it): every `RECORDED — by design (` occurrence anywhere in the fence-stripped receipt is parsed as `(<owner>, round N[; <owner>, round N]…)` or `(D-nnn)`; for each F-owner (the owner is the owning row's FIRST-CELL id, matched WHOLE — `re.fullmatch(r"[A-Za-z0-9]{0,3}-?F\d+[a-z]?", owner)`, never a search that would lift `F9` out of `AF9`; the spec's D6 `:165` names only `<owning row>` and its V5 `:236` names the two refusal causes, absent owner and N not below the closing `Pass N`, so the grammar is derived from the id shapes ledger rows actually carry: over the 275 receipts at 8092e8a8, of the 543 first cells that fullmatch `[A-Za-z0-9-]{1,12}` and contain `F<digit>`, 503 are `Fnnn[a-z]` — 500 bare `| Fnnn |` and 3 sub-findings `| F8a |`…`| F8c |` at `docs/development/reviews/2026-09-02-freshness-guard-round2-review.md:94-96` — 0 are hyphenated `F-nnn`, and 40 are prefixed (`AF1`–`AF15`, `BF1`, `CF1`, `DF1`, `EF1`, `FF1`–`FF9`, `A-F1`–`A-F4`, `U0-F1`…`U3-F2`), all 543 inside that grammar; 0 `RECORDED — by design (` verdicts exist at 8092e8a8, so no observed owner could have been measured) its row must exist in the receipt: the owning row is found by splitting on `,`/`;` the first cell of every `|`-leading, non-separator row ANYWHERE in `text_s` (not only the LEDGER BLOCKS item (1) defines — 514 of the 543 owner rows at 8092e8a8 sit outside one, in findings tables), expanding a numeric range `Fa–Fb`/`Fa-Fb` (one multi-owner cell exists, `| F9, F12–F16 |` at `docs/development/reviews/2026-08-10-hub-governance-gates-review.md:325`), and fullmatching each token against the owner — an owner that matches neither that grammar nor `D-nnn` is looked up as ABSENT (V5's first cause, not a new one) and refused by name with the repair "cite the owning row's first-cell id verbatim, or `(D-nnn)`" and every N must be smaller than the closing row's `Pass N` (the verdict's own N — never a scrape of the owning row's prose; the closing `Pass N` read with a NEW capturing token `re.compile(r'^\s*' + _LIST_MARK + r'\s*\|?\s*\**Pass\s*(\d+)', re.I)` from the LAST row of `_ledger_shapes`'s `ordered` whose head it matches — the last Pass-headed counter row anywhere in `text_s`, table or prose, NOT `ordered[-1]`, which on 1 of the 71 ledger-bearing receipts at 8092e8a8 is a `_MEGA_ROW` — that receipt (`docs/development/reviews/2026-08-27-mega-umbrella-sso-validation-review.md`) carries NO Pass-headed row at all and is therefore the fail-closed case below, not a fallback; the LAST-matching rule is defensive and its positive fixture is synthetic; a receipt with a by-design verdict and NO Pass-headed row in `ordered` is refused by name ("no closing `Pass N` row to bound the round against") — fail-closed, never a skipped bound; a round-less `(D-nnn)` verdict has no N to bound and is exempt from that refusal. The token is NEW because `_PASS_HEAD` at `:1011` identifies the row but has no capture group, and its `\d` matches a single digit — so reusing it, even with a group added, would read `Pass 19` as `1`; the sibling `row_shaped` at `:1042-1044` already uses `Pass\s*\d+[a-z]?`); a `D-nnn` owner passes with no round token and `(D-nnn, round N)` is refused (the grammar admits no round token for a D-row); failure refuses by name. `RECORDED — measured (…)` and `RECORDED — unexecuted (…)` never enter any counter. (6) The FINDERS-CELL rule (V11): the closing Pass row's second cell must carry at least one model token `(opus|sonnet|haiku)×\d+` (the shape every receipt writes, e.g. `native opus×1 + sonnet×3`); a closing row whose finders cell names no model is refused by name — enforced in `check_file` only (the row tuple carries no cell split; `_committed_nonquiet` is the advisory reader of COMMITTED files and stays on the counters — a deliberate, named asymmetry, not the founding-enemy divergence: V11 is a shape rule, not a second definition of quiet); no run-record reader is added (the gate has none today; adding one would be a new mechanism). (7) `scripts/review_receipt.py --init` (`:159-173`): the Pass Ledger prose says `found: F, new: N, confirmed: C, fixed: X, unexecuted: U` (`new:` is prose the graders do not parse), the fenced example rows carry the five counters (`| Pass 2 | … | found: 0, new: 0, confirmed: 0, fixed: 0, unexecuted: 0 | method: re-derivation |`), the verdict grammar sentence at `:154-157` gains the four RECORDED forms, and `render()` (`:126-187`) emits a `## Residual` section between `## Pass Ledger` and `## Per-phase verdicts` whose one sentence names the three residual verdicts — `RECORDED — unexecuted (<why>)`, `RECORDED — by design (<the owning row's first-cell id>, round N[; …])` / `(D-nnn)`, `RECORDED — measured (<why>)` — INSIDE a fenced block (so the residual scan of (5) never reads the template's own literal as a by-design row); `tests/test_review_receipt.py:74-95` (the mechanically-completed skeleton) is extended with a new-grammar closing row and a RECORDED checklist row, seen red first. The AFTER-EDIT header of `review_receipt.py` already couples this gate and its test. DO-NOT: change T01's grammars, tuple, `_pass_counters_ext` or exit rules (extend, never re-parse); add a `new:` slot; touch `check_convergence.py` (T03 — but its use of the imported `VERDICT` at `:477` is named in § Interfaces); write a run-record reader; edit `tests/enforcement/test_mega_validation_reports.py` (must stay green unchanged — not in Touches).

Depends: T01
Parallel: ⛓️
Complexity: never-route
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_review_refusals.py tests/enforcement/test_review_confirmed_grammar.py tests/enforcement/test_review_exit_contract.py tests/test_review_receipt.py tests/test_check_review_coverage_rederivation.py -q
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_review_refusals.py -q -k corpus
Docs: CHANGELOG entry (Deltas); INDEX row for the new test file (Deltas)

## Touches
- scripts/enforcement/check_review_coverage.py — PRIMARY PATH
- tests/enforcement/test_review_refusals.py
- scripts/review_receipt.py
- tests/test_review_receipt.py

## Behavior Contract
- **Given** the row `| 19 | found: 0 | fixed: 0 | confirmed: 3 |` under a real vocabulary header with one prior parsing data row, **When** the gate runs, **Then** the row is REFUSED by name with the cell-anchored repair message, and every REFUSED fixture in the spec's V2 list is refused (scripts/enforcement/check_review_coverage.py:996)
- **Given** `| Pass 19 | opus×1 | found: 0, fixed: 0 | delta (confirmed: 3) |`, its prose twin `Pass 19: found: 0, fixed: 0 — delta (confirmed: 3)` and the mis-ordered `| Pass 19 | opus×1 | found: 0, fixed: 0, confirmed: 0 | m |`, **When** parsed, **Then** each is refused by name (a token outside the counter run, on the table and the prose path alike; a displaced token inside it) while `| Pass 19 | opus×1 | **found: 0, confirmed: 0** | **fixed: 0** |` parses and reads quiet (scripts/enforcement/check_review_coverage.py:1054)
- **Given** `| u | x | found: 0 | fixed: 0 | unexecuted: 2 |` or `| Pass 19 | opus×1 | found: 0, fixed: 0, unexecuted: 2 | m |`, **When** the gate runs, **Then** each is refused by the counter rule (`unexecuted:` without `confirmed:`), never read as old-grammar quiet (scripts/enforcement/check_review_coverage.py:1117-1121)
- **Given** a method cell `HIGH/CONFIRMED: two MORE anti-cheat errors` on a resolved old-grammar row, **When** the case-insensitive token scan runs, **Then** it is exempt under the five-conjunct carve-out (27 exempt cells / 29 occurrences of the 28 in-scope rows / 30 occurrences at 8092e8a8, the counts printed beside the assertion) and `| 19 | found: 0 | fixed: 0 | Confirmed: 3 defects stand |` is refused; the rule's fire rate over the 275 receipts is exactly 1 row, `docs/development/reviews/2026-08-31-plan-1-manifesto-command-pass-T20-fabrik-release-review.md:15`, repaired by the orchestrator's Delta at this ticket's merge (scripts/enforcement/check_review_coverage.py:1019-1020)
- **Given** a header row `| Pass | Finders | found: F, confirmed: C, fixed: X | Method |` with its separator and one parsing data row, **When** the block parses, **Then** the header is exempt from the token rule and not counted when testing whether the block parses; a second header-shaped row inside the same block is a refused data row; the `--init` header with no data row is not yet a ledger; a three-row block written entirely in the new grammar in which nothing parses is refused by name (scripts/enforcement/check_review_coverage.py:176-213)
- **Given** a residual row `RECORDED — by design (F342, round 15; F358, round 17)` under a closing `Pass 18`, **When** the gate runs, **Then** it passes; `(F999, round 18)` is refused by name (absent owner, N not below 18), `(AF9, round 3)` whose row is `| AF9 |` passes, `(F12, round 3)` whose owning row is `| F9, F12–F16 |` passes (the cell split, the range expanded), `(the F250 shape, round 3)` is refused as absent with the repair, `(D-203)` passes, `(D-203, round 3)` is refused, a receipt whose `ordered` holds no Pass-headed row but whose `| F1 |` row exists is refused for `(F1, round 1)` ("no closing `Pass N` row to bound the round against") and passes when its only by-design verdict is the round-less `(D-203)`, and `RECORDED — measured (<why>)` never enters `confirmed:` or `unexecuted:` (scripts/enforcement/check_review_coverage.py:520-539)
- **Given** a closing Pass row whose finders cell names no model token (`opus×N`, `sonnet×N`, `haiku×N`), **When** the gate runs, **Then** it is refused; a cell reading `native sonnet×2` passes (scripts/enforcement/check_review_coverage.py:1064)
- **Given** `review_receipt.py --init`, **When** it writes a skeleton, **Then** its Pass Ledger prose and fenced examples carry the five-counter row shape and the four RECORDED verdicts, the skeleton carries a fenced `## Residual` section, `VERDICT` accepts `RECORDED — unexecuted (…)`, `RECORDED — by design (…)`, `RECORDED — measured (…)` and `RECORDED — hygiene false positive (…)`, the fresh skeleton still passes `check_file`, and the mechanically-completed skeleton test passes with a new-grammar closing row (scripts/review_receipt.py:161-172, scripts/enforcement/check_review_coverage.py:51)

## Context Files
- .windsurf/rules/core/10-python.md
- scripts/enforcement/check_review_coverage.py
- scripts/review_receipt.py
- tests/test_review_receipt.py
- docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md

## Implementation notes
- The seam test named in the spine's § Interfaces is `test_the_refusal_rules_read_t01s_tuple` — write it under that name: it parses the V1 row through `_ledger_shapes`, asserts the 5-tuple and the empty `refusals` list, before any refusal rule runs.
- The new test file's corpus test function names carry the substring `corpus` (the second Gate line selects `-k corpus`).
- `tests/enforcement/test_review_exit_contract.py` is run by the Gate but not read: T01 grows `check_review_coverage.py` first, and this ticket's READ set has ~18 KB of headroom (243,290 of 262,144 at 8092e8a8) against `READ_BUDGET_BYTES` before that growth — every byte of Context is spent on the gate itself.
- The V2 fixture list at the spec's `:233` is the test's parametrisation: every SINGLE-ROW fixture is embedded under a real vocabulary header with one prior parsing data row (so the run grades the row, not the header rule); the block fixtures (the `--init` shape, the counter-vocabulary header alone, with one data row, the headerless two-row block, the three-row all-token block, the stray-separator variants) are blocks by design. Group them: REFUSED-by-token, REFUSED-by-counter, NOT-refused-and-parsed, NOT-refused-and-out-of-threat-model.
- Measure the carve-out over the corpus in the test (`git ls-files docs/development/reviews/*.md`, 275; print `len(files)`): 27 exempt cells / 29 occurrences under this ticket's scope; exactly 1 refused row (T20) at 8092e8a8 — pin that SHA in the test (`git show 8092e8a8:<path>` into `tmp_path`), never `HEAD~`, so the orchestrator's later Delta repair cannot flip the count under the test.
- Red-first on every row: each REFUSED fixture must pass the gate silently BEFORE the rule lands (old grammar reads it inert or quiet) and be refused after.
