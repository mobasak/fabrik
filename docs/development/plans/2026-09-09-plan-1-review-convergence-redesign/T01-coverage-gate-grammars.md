# T01 — Coverage gate: both grammars learn `confirmed:`/`unexecuted:`, the 5-tuple and its readers, the exit rule, the line normalisation

## Scope
In `scripts/enforcement/check_review_coverage.py` (fleet-synced, `scripts/enforcement/` recursive — `fabrik_synced_manifest.py:125-126`), implement the spec's D7 grammar half (docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:170-171, the first two bullets — read them in pages, they are ~9,000-character paragraphs) with backward compatibility as the CONTRACT (DD4): (1) `_MEGA_ROW` (`:996`) becomes `^\s*\|.*?\|\s*found:\s*(\d+)\s*(?:\||$)(?:\s*confirmed:\s*(\d+)\s*(?:\||$))?\s*fixed:\s*(\d+)\s*(?:\||$)(?:\s*unexecuted:\s*(\d+)\s*(?:\||$))?` — every counter cell terminates on `\|` OR end-of-line (GFM lets the closing pipe go); NO `new:` slot (an optional `new:` cell matches 262 corpus rows instead of 2 and flips them from `_pass_counters` to MEGA-first — measure it, do not add it); every `.group()` reader is renumbered (`:1120`). (2) `_pass_counters` (`:1054-1061`) gains `confirmed:` and `unexecuted:` tokens with the same stand-alone rule as `_FOUND_TOK`/`_FIXED_TOK` (`:1019-1020`, case-sensitive, `(?<![\w-])` … `(?!\s*\w)`), scanned ONLY inside the cell that carries `found:` (never the whole line — `found:`/`fixed:` keep their whole-line scan at `:1057-1058`, on which 425 of the 535 corpus Pass rows depend: 267 separate-cell rows, 158 one-cell rows without a comma run); on a PROSE Pass line the new tokens are read only inside the COUNTER RUN — the contiguous comma-joined `key: value` chain that begins at `found:`, bounded by the cell's pipes on a table row and by end-of-line or the first non-item character on a prose line — and the canonical order `found: F[, new: N][, confirmed: C], fixed: X[, unexecuted: U]` is enforced INSIDE the run by NAME (a mis-ordered run refuses the row with a message, never returns `None`); a second `confirmed:`/`unexecuted:` token returns `None` exactly as a second `found:` does (`:1059-1060`); an ABSENT optional token leaves the counter `None` (535 of 535 corpus Pass rows must keep parsing). Also update the retyped literal at `:1048` inside `_unparsed_pass_lines` (hoist ONE shared token fragment rather than a fourth copy — the file's own round-69 doctrine at `:1005-1009`) and check the second Pass-head regex `row_shaped` at `:1042-1044` against every shape you add. (3) `_ledger_shapes` (`:1064`) returns `(found, confirmed_or_None, fixed, unexecuted_or_None, line)` from BOTH construction sites (`:1121` table, `:1134` prose) and every reader is widened: the unpacks at `:581`, `:1263` (`f_found, f_fixed, f_line = rows[-1]` — a 5-tuple into 3 names raises `ValueError` on the mega path only), `:1284` (`for _, _, line in rows` — index 2 becomes `fixed`, `_MEGA_HASH_PAIR.search(int)` raises `TypeError`), `:1562`; the call sites `:572`, `:1223`, `:1542`; the annotations `:1066-1067`, `:1085`, `:1093-1096`, `:1224`. (4) The exit rule at `:581-584`, evaluated on `founds[-1]` as today: if the final row carries `confirmed` under EITHER grammar, quiet = `confirmed == 0 and fixed == 0 and (unexecuted in (None, 0))`; otherwise the old `found == 0` rule stands; the mega exit at `:1275` (`f_found != 0 or f_fixed != 0`) takes the SAME branch rule (confirmed when present, the pair otherwise — one definition of quiet); the error text at `:584` drops "a FRESH candidate counts even when refuted … D-048" for "a candidate CONFIRMED by execution counts; RECORDED and REFUTED rows never reopen the loop — D-203" and the comments at `:1550`, `:1554`, `:1560` are re-keyed. (5) `_strip_fences` (`:766-768`) normalises BEFORE `_kept_lines` splits (`:754`): replace every character of `_LINE_BREAKS` (`:299`) except `\n` and `\r` with a space and delete `U+200B`–`U+200D`, `U+FEFF`, `U+00AD` — applied to `text` at the top of `_strip_fences` and ALSO to the header window built at `:366` (which splits before the strip); reuse `_LINE_BREAKS`, never retype the set (`tests/enforcement/test_mega_validation_reports.py:335` re-derives it from `str.splitlines()`). (6) `tests/test_check_review_coverage_blocked.py:58-65` asserts over a 900-character `inspect.getsource` window — rewrite it to assert on the whole message string, so the D-203 rewording cannot red it for the wrong reason. The AFTER-EDIT header at `:2` gains `tests/test_check_review_coverage_blocked.py` and `tests/test_check_review_coverage_precommit.py`. DO-NOT: add the token rule, the counter rule, the header rule, the carve-out or the refusal messages (T02, on this same file, after you); touch `scripts/review_receipt.py` or `VERDICT` (T02); touch `check_convergence.py` (T03); edit `tests/enforcement/test_mega_validation_reports.py` — it must stay green UNCHANGED (if the widening reds it, the widening is wrong); write a fixture without the leading cell every real row has (`_MEGA_ROW` never matches a row that starts `| found:`).

Depends: —
Parallel: ⚡
Complexity: never-route
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_review_confirmed_grammar.py tests/enforcement/test_review_exit_contract.py tests/enforcement/test_mega_validation_reports.py tests/test_check_review_coverage_blocked.py tests/test_check_review_coverage_precommit.py tests/test_check_review_coverage_rederivation.py -q
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_review_confirmed_grammar.py -q -k corpus
Docs: CHANGELOG entry (Deltas); INDEX row for the new test file (Deltas)

## Touches
- scripts/enforcement/check_review_coverage.py — PRIMARY PATH
- tests/enforcement/test_review_confirmed_grammar.py
- tests/test_check_review_coverage_blocked.py
- tests/enforcement/test_review_exit_contract.py

## Behavior Contract
- **Given** a receipt whose last Pass Ledger row is `| Pass 19 | opus×1 | found: 4, new: 2, confirmed: 0, fixed: 0, unexecuted: 0 | delta |`, **When** `check_review_coverage.py` grades it, **Then** the exit rule passes (quiet by `confirmed: 0` although `found:` is 4) and the same receipt with `confirmed: 1, fixed: 1` fails naming `confirmed` (scripts/enforcement/check_review_coverage.py:581-584)
- **Given** a receipt whose last row is old-grammar `found: 3, fixed: 0` with no `confirmed:` token anywhere, **When** the gate runs, **Then** it fails on `found: 3` (the old rule stands) and the same receipt ending `found: 0, fixed: 0` passes (scripts/enforcement/check_review_coverage.py:582)
- **Given** the cell-anchored row `| 19 | found: 0 | confirmed: 0 | fixed: 0 | unexecuted: 0 |` and the same row without its closing pipe, **When** `_ledger_shapes` parses them, **Then** both resolve MEGA-first to `(0, 0, 0, 0, line)` and the widened `_MEGA_ROW` matches exactly the 2 rows today's regex matches over the 275 committed receipts (scripts/enforcement/check_review_coverage.py:996, :1117-1121)
- **Given** the prose line `Pass 19: found: 0, confirmed: 3, fixed: 0`, **When** `_pass_counters` reads it, **Then** it returns `(0, 3, 0, None)`, and the mis-ordered `Pass 19: found: 0, fixed: 0, confirmed: 0` is refused by name, never `None` (scripts/enforcement/check_review_coverage.py:1054-1061)
- **Given** the 275 committed receipts at 2dec30c8, **When** every `|`-leading line and prose Pass line is parsed by the widened grammars, **Then** 535 of 535 Pass rows parse with `confirmed=None` and every receipt's quiet/non-quiet verdict is unchanged — 0 flips (scripts/enforcement/check_review_coverage.py:1064)
- **Given** a receipt carrying `confirmed<U+200B>: 3` (a literal zero-width space after the word) and one carrying a `U+2028` inside a Pass row, **When** `_strip_fences` runs, **Then** the zero-width character is deleted and the `U+2028` becomes a space BEFORE `_kept_lines` splits the text, reusing `_LINE_BREAKS`, and the header window at `:366` sees the normalised text (scripts/enforcement/check_review_coverage.py:299, :754, :766)
- **Given** a mega-validation receipt whose last row carries `confirmed: 0`, **When** `check_mega_validation` reads it, **Then** the last row unpacks as the 5-tuple, the hash-chain pairs still read the raw line, the mega exit reads `confirmed` when present and the pair otherwise, and the 3 existing tuple-reading mega tests stay green unchanged (scripts/enforcement/check_review_coverage.py:1263, :1275, :1284)
- **Given** the widened tuple, **When** `tests/enforcement/test_review_exit_contract.py` and `tests/enforcement/test_mega_validation_reports.py` run unchanged, **Then** all pass, and the exit error text no longer says "a FRESH candidate counts even when refuted" but names D-203 (scripts/enforcement/check_review_coverage.py:584)

## Context Files
- .windsurf/rules/core/10-python.md
- scripts/enforcement/check_review_coverage.py
- tests/enforcement/test_review_exit_contract.py
- tests/test_check_review_coverage_blocked.py
- tests/test_check_review_coverage_precommit.py
- docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md

## Implementation notes
- Red-first: write the V1 test first and watch it fail on today's `founds[-1] != 0` (the row's `found: 4` fails today); write the `:1263`/`:1284` guard as a mega fixture whose last row carries `confirmed: 0` and watch it raise on the widened tuple before the readers are fixed; the corpus test walks `git ls-files docs/development/reviews/*.md` (275) and asserts the parsed count (535 Pass rows) and the per-receipt verdict set against the committed gate at `HEAD` (`git show HEAD:scripts/enforcement/check_review_coverage.py` loaded by `spec_from_file_location` into a scratch dir — never bare-grep a tracked path).
- The corpus test is slow-ish (275 files × 2 parsers); keep it under 10 s by parsing each file once per gate version.
- `tests/enforcement/test_review_exit_contract.py:106` reds if a `new:` slot is added to `_MEGA_ROW` — that test is the measurement's grader; leave the slot out.
- Every mutation on a copy under the scratchpad, restored inside the same Bash call with an asserted restore; every `command_run.py` probe (if any) sets `COMMAND_RUN_DIR`, `COMMAND_RUN_TRANSCRIPT`, `KAIZEN_EVENTS_DIR`.
