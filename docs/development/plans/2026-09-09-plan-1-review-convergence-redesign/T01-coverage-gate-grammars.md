# T01 — Coverage gate: both grammars learn `confirmed:`/`unexecuted:`, the extended counters and every reader, the exit rule in all three readers, the line normalisation

## Scope
In `scripts/enforcement/check_review_coverage.py` (fleet-synced, `scripts/enforcement/` recursive — `fabrik_synced_manifest.py:125-126`; every line number below is at 8092e8a8 = the working tree for this file), implement the spec's D7 grammar half (docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md:170-171, the first two bullets — read them with the Read tool at offset 170 limit 2; they are ~9,000-character paragraphs) with backward compatibility as the CONTRACT (DD4): (1) `_MEGA_ROW` (`:996`) becomes `^\s*\|.*?\|\s*found:\s*(\d+)\s*(?:\||$)(?:\s*confirmed:\s*(\d+)\s*(?:\||$))?\s*fixed:\s*(\d+)\s*(?:\||$)(?:\s*unexecuted:\s*(\d+)\s*(?:\||$))?` — every counter cell terminates on `\|` OR end-of-line (GFM lets the closing pipe go); NO `new:` slot: an optional `(?:\s*new:\s*(\d+)\s*(?:\||$))?` cell after `found:` matches 269 corpus rows instead of 2 at 8092e8a8 under either terminator (267 Pass-headed rows carrying a real `new:` cell plus the 2 rows today's regex matches; the spec's 262 does not re-derive) and flips them from `_pass_counters` to MEGA-first — measure that with the optional-slot variant over `git ls-files docs/development/reviews/*.md` (print both counts beside the assertion; the test asserts "far more than 2", never a hard-coded figure), then leave the slot out; `pair = (int(m.group(1)), int(m.group(2)))` at `:1120` becomes the four groups. (2) `_pass_counters` (`:1054-1061`) stays BYTE-IDENTICAL — it is the legacy strict pair, and its six call sites (`:809` `_grammar_shaped`, `:912` the list-wrapped-row refusal, `:1046` inside `_unparsed_pass_lines`, `:1118`, `:1132`, and `tests/enforcement/test_mega_validation_reports.py:647` with its five 2-tuple expectations at `:640-651`) keep their contract untouched: the MIRROR of a widened return is the narrow consumer it breaks, and a wrapper that returned `None` for a mis-ordered run would silently open the `:912` refusal and drop the row from `ordered` (the exit falling to the previous row). A NEW `_pass_counters_ext(line) -> tuple[int, int | None, int, int | None] | str | None` is called ONLY by `_ledger_shapes` (`:1118`, `:1132`, beside `_pass_counters`): it returns the four counters, or `None` when `_pass_counters` returns `None`, or a `str` REFUSAL REASON when the counter run is mis-ordered or carries a second `confirmed:`/`unexecuted:` token — the row is then kept in `ordered` with `confirmed=None`, `unexecuted=None` AND the reason is appended to a fourth return element of `_ledger_shapes`, `refusals: list[str]` (built locally per call — never module state: `_committed_nonquiet` loops `_ledger_shapes` over every committed receipt before `check_file` runs), which `check_file` reports as an error line (generic in this ticket: "Pass row refused: <reason> — <line>"; T02 owns the per-path repair wording). The new tokens (`_CONFIRMED_TOK`, `_UNEXECUTED_TOK`, the same stand-alone rule as `_FOUND_TOK`/`_FIXED_TOK` at `:1019-1020` — case-sensitive, `(?<![\w-])` … `(?!\s*\w)`) are read ONLY inside the COUNTER RUN — the contiguous comma-joined `key: value` chain that begins at the first `found:` TOKEN (the first `found:` that `_FOUND_TOK` matches, never a `found:` literal such as `sample found: 0 clean`), bounded by the cell's pipes on a table row (on a separate-cell row the run is the `found:` cell alone) and, on a prose line, by end-of-line or the first character that is not part of a comma-joined `key: value` item — never the whole line (`found:`/`fixed:` keep their whole-line scan at `:1057-1058`; the spec measured 425 of 535 corpus Pass rows depending on it — 267 separate-cell rows, 158 one-cell rows without a comma run — the test asserts INVARIANCE, not those figures); a narrated `— delta (confirmed: 0)` after the run is outside it (T02 refuses it on the prose path too; this ticket must not read it as a counter); the canonical order `found: F[, new: N][, confirmed: C], fixed: X[, unexecuted: U]` is enforced INSIDE the run over the items PRESENT in it by NAME (the refusal reason names the displaced token) — on a separate-cell row, where `fixed:` sits in its own cell, the run is `found: F[, new: N][, confirmed: C][, unexecuted: U]` and `unexecuted:` legally follows `confirmed:` inside the `found:` cell (the only writable spelling on that shape; T02's Pass-path repair message names it). This ticket DELIBERATELY departs from D7's "a second token returns `None` exactly as a second `found:` does": a `None` from the extended reader would leave the row inert and hand the exit to the previous round, so the extended reader returns a refusal string and the row stays in `ordered` — strictly safer, same verdict (refused by name). `_unparsed_pass_lines` (`:1023`) recognises the two new tokens so a half-written Pass-headed triple is NAMED in its output, not silently inert — replace the retyped literal at `:1048` by ONE shared token fragment (the file's round-69 doctrine at `:1005-1009`) and check the second Pass-head regex `row_shaped` at `:1042-1044` against every shape you add. (3) `_ledger_shapes` (`:1064`) returns `(tables, prose_runs, ordered, refusals)` where every row is `(found, confirmed_or_None, fixed, unexecuted_or_None, line)`, from BOTH construction sites (`:1121` table, `:1134` prose), and every reader is widened: the three call sites `:572`, `:1223`, `:1542` (a fourth element to unpack), the row unpacks at `:581`, `:1263` (`f_found, f_fixed, f_line = rows[-1]` — five values into three names raises `ValueError`, on the mega path only), `:1284` (`for _, _, line in rows` — the same `ValueError`), `:1562`; the annotations `:1066-1067`, `:1085`, `:1093-1096`, `:1224`. (4) The exit rule in ALL THREE readers takes ONE branch rule — if the final row carries `confirmed` under either grammar, quiet = `confirmed == 0 and fixed == 0 and (unexecuted in (None, 0))`; otherwise the old `found == 0` rule stands: `check_file` at `:581-584` (`founds[-1]`), the mega exit at `:1275` (`f_found != 0 or f_fixed != 0`), and `_committed_nonquiet` at `:1562-1573` (`quiet = int(rows[-1]) == 0` at `:1565`, whose message at `:1568` names `found:` — it gains `confirmed:`/`unexecuted:` when present; its own comment at `:1555-1557` names the two-readers divergence as this function's founding enemy, and the alignment guard `tests/enforcement/test_review_exit_contract.py:82-92` gains a V1-row case so the two readers are proven to agree on the new grammar too); the error text at `:584` drops "a FRESH candidate counts even when refuted … D-048" for "a candidate CONFIRMED by execution counts; RECORDED and REFUTED rows never reopen the loop — D-206" and the comments at `:1550`, `:1554`, `:1560` are re-keyed. (5) `_strip_fences` (`:766-768`) normalises BEFORE `_kept_lines` splits (`:754`): replace every character of `_LINE_BREAKS` (`:299`) except `\n` and `\r` with a space and delete `U+200B`–`U+200D`, `U+FEFF`, `U+00AD` — applied to `text` at the top of `_strip_fences` and ALSO to the header window built at `:366` (which splits raw text before the strip); reuse `_LINE_BREAKS`, never retype the set (`tests/enforcement/test_mega_validation_reports.py:336-338` re-derives it from `str.splitlines()`); `_block_regions`' raw split at `:702` is correct as is (it finds the fences; its two callers are `_kept_lines` at `:753` — downstream of the normalisation — and `_fence_parity_error` at `:438`, which judges fence PARITY on raw text for four callers and is OUT of this ticket's scope by design: parity is a per-line structural count that a line-ish character cannot fake into a ledger row). The module imports only the standard library (6 module-level imports + one in-function `hashlib`) — a `git show <sha>:<path>` copy runs standalone, no worktree needed. The AFTER-EDIT header at `:2` gains `tests/test_check_review_coverage_blocked.py` and `tests/test_check_review_coverage_precommit.py`. Two coupled test files carry a SIBLING's uncommitted edits in the main tree today (`tests/enforcement/test_mega_validation_reports.py`, `tests/enforcement/test_review_exit_contract.py` — `git status` ` M`): the coder's worktree branches from HEAD and never sees them; the orchestrator's merge stages only this ticket's diff. DO-NOT: add the token rule, the counter rule, the header rule, the carve-out or the per-path repair messages (T02, on this same file, after you — T02 consumes `refusals`); touch `scripts/review_receipt.py` or `VERDICT` (T02); touch `check_convergence.py` (T03); change `_pass_counters` in any way (`tests/enforcement/test_mega_validation_reports.py` must stay green UNCHANGED and is not in Touches); edit `tests/test_check_review_coverage_blocked.py` (its 900-character `inspect.getsource` window at `:58-66` starts at the `_committed_nonquiet` message and cannot see the `:584` rewording — measured 483 characters of headroom); write a fixture without the leading cell every real row has (`_MEGA_ROW` never matches a row that starts `| found:`).

Depends: —
Parallel: ⚡
Complexity: never-route
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_review_confirmed_grammar.py tests/enforcement/test_review_exit_contract.py tests/enforcement/test_mega_validation_reports.py tests/test_check_review_coverage_blocked.py tests/test_check_review_coverage_precommit.py tests/test_check_review_coverage_rederivation.py -q
Gate: /opt/fabrik/.venv/bin/python -m pytest tests/enforcement/test_review_confirmed_grammar.py -q -k corpus
Docs: CHANGELOG entry (Deltas); INDEX row for the new test file (Deltas)

## Touches
- scripts/enforcement/check_review_coverage.py — PRIMARY PATH
- tests/enforcement/test_review_confirmed_grammar.py
- tests/enforcement/test_review_exit_contract.py

## Behavior Contract
- **Given** a receipt whose last Pass Ledger row is `| Pass 19 | opus×1 | found: 4, new: 2, confirmed: 0, fixed: 0, unexecuted: 0 | delta |`, **When** `check_review_coverage.py` grades it, **Then** the exit rule passes (quiet by `confirmed: 0` although `found:` is 4), the same receipt with `confirmed: 1, fixed: 1` fails naming `confirmed`, and the same receipt with `unexecuted: 1` fails naming `unexecuted` (scripts/enforcement/check_review_coverage.py:581-584)
- **Given** a receipt whose last row is old-grammar `found: 3, fixed: 0` with no `confirmed:` token anywhere, **When** the gate runs, **Then** it fails on `found: 3` (the old rule stands) and the same receipt ending `found: 0, fixed: 0` passes — an invariance guard, seen red by neutering the old-rule branch on a copy (scripts/enforcement/check_review_coverage.py:582)
- **Given** the cell-anchored row `| 19 | found: 0 | confirmed: 0 | fixed: 0 | unexecuted: 0 |` and the same row without its closing pipe, **When** `_ledger_shapes` parses them, **Then** both resolve MEGA-first to `(0, 0, 0, 0, line)` and the widened `_MEGA_ROW` matches exactly the 2 rows today's regex matches over the 275 committed receipts, while the optional-`new:`-slot variant matches far more (both counts printed) (scripts/enforcement/check_review_coverage.py:996, :1117-1121)
- **Given** the prose line `Pass 19: found: 0, confirmed: 3, fixed: 0`, **When** `_pass_counters_ext` reads it, **Then** it returns `(0, 3, 0, None)` while `_pass_counters` still returns `(0, 0)`; the mis-ordered `Pass 19: found: 0, fixed: 0, confirmed: 0` returns a refusal reason naming `confirmed`, the row stays in `ordered`, and `check_file` reports the reason as an error (asserted on `_ledger_shapes`'s `refusals` and on the gate's output); a narrated `Pass 19: found: 5, fixed: 0 — delta (confirmed: 0)` reads `confirmed=None` from the run (scripts/enforcement/check_review_coverage.py:1054-1061)
- **Given** the 275 committed receipts at 8092e8a8, **When** every `|`-leading line and prose Pass line is parsed by the widened grammars, **Then** the set of parsed Pass rows is identical to the committed grammar's (both counts printed beside the assertion — the spec's 535 was measured with a different filter), every row parses with `confirmed=None`, `refusals` is empty for every receipt, and every receipt's quiet/non-quiet verdict is unchanged under all three readers — 0 flips — an invariance guard, seen red by neutering the `None` default on a copy (scripts/enforcement/check_review_coverage.py:1064)
- **Given** a receipt carrying `confirmed<U+200B>: 3` (a literal zero-width space after the word) and one carrying a `U+2028` inside a Pass row, **When** `_strip_fences` runs, **Then** the zero-width character is deleted and the `U+2028` becomes a space BEFORE `_kept_lines` splits the text, reusing `_LINE_BREAKS`, and the header window at `:366` sees the normalised text (scripts/enforcement/check_review_coverage.py:299, :754, :766)
- **Given** a mega-validation receipt whose last row carries `confirmed: 0`, **When** `check_mega_validation` reads it, **Then** the last row unpacks as the 5-tuple, the hash-chain pairs still read the raw line, the mega exit reads `confirmed` when present and the pair otherwise, and `tests/enforcement/test_mega_validation_reports.py` stays green unchanged (scripts/enforcement/check_review_coverage.py:1263, :1275, :1284)
- **Given** the V1 row as the closing row of a COMMITTED receipt, **When** `_committed_nonquiet` grades it, **Then** it agrees with `check_file` (quiet), the old-grammar `found: 3 | new: 0 | fixed: 0` row is still flagged by both readers, and the exit error text at `:584` names D-206 instead of "a FRESH candidate counts even when refuted" (scripts/enforcement/check_review_coverage.py:584, :1562-1573)

## Context Files
- .windsurf/rules/core/10-python.md
- scripts/enforcement/check_review_coverage.py
- tests/enforcement/test_review_exit_contract.py
- tests/test_check_review_coverage_precommit.py
- docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md

## Implementation notes
- The new test file's corpus test function names carry the substring `corpus` (the second Gate line selects `-k corpus`).
- Red-first: rows 1, 3, 4, 6, 7, 8 fail on today's code as written (row 7 raises `ValueError` on the widened tuple before the readers are fixed); rows 2 and 5 are invariance guards whose red is a mutation on a copy, restored in the same Bash call. The corpus test walks `git ls-files docs/development/reviews/*.md` (275; print `len(files)` beside every count — a wrong cwd printed `0 of 0` as a clean negative once) and compares the per-receipt verdict under the three readers against the committed gate at the plan's base SHA: `git -C /opt/fabrik show 8092e8a8:scripts/enforcement/check_review_coverage.py > <tmp>/old_gate.py` loaded by `spec_from_file_location` — the module is stdlib-only, so a bare copy runs; never a worktree (a `git worktree add` writes the shared repo's registry) and never a bare grep of a tracked path.
- `tests/enforcement/test_review_exit_contract.py:56`, `:64`, `:86`, `:97` are the `new:`-bearing separate-cell fixtures; `:106` is a docstring. The alignment guard is `:82-92`.
- Keep the corpus test under 10 s: parse each file once per gate version.
- Every mutation on a copy under the scratchpad, restored inside the same Bash call with an asserted restore; every `command_run.py` probe (if any) sets `COMMAND_RUN_DIR`, `COMMAND_RUN_TRANSCRIPT`, `KAIZEN_EVENTS_DIR`.
