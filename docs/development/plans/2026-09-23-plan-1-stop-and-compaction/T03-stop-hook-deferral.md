# T03 — the Stop hook: DEFERRAL D1–D4, the DECISION parser, the reasons, the input, the event

## Scope
Implements spec § C1 (the DEFERRAL shape, its scope, inputs and reasons) and § C2 (the DECISION block's parse and checks) in `.claude/hooks/final_gate_stop.py` (the `decision_block` event type and the `deferral` premature cause are registered by T01b). Folds in the review's recorded inputs: A-O40 (D4 fires only when the agent hands its own work to a later session — the five factual sentences are green fixtures) and A-O43 (an `asked:` or `scope:` quote must occur verbatim in a real operator entry, and an `asked:` quote ends in `?`). Produces the interfaces T04 and T05 consume (spine § Interfaces). DO-NOT: change the four other pattern shapes' behaviour or exemptions (only `_PERMISSION_RE`'s loop is replaced, by D3 — spec § C1 "The one behaviour that moves"); change `CAP`, `decide_stall` or any other cause, including the run-record cause (a DECISION block inside a live run still meets it, spec § C2; the agent closes its record with `command_run.py handoff`); touch `scripts/thread_anchor.py` (T04).

Depends: T01a, T01b, T02b
Parallel: ⛓️
Complexity: native
Gate: uv run pytest tests/test_final_gate_stop_deferral.py tests/test_stop_hook_deferral_exemption.py tests/test_final_gate_stop_hook.py tests/test_kaizen_hook_emitters.py tests/test_kaizen_events.py -q
Docs: CHANGELOG (Deltas) — the three docs rows are T05's

## Touches
- .claude/hooks/final_gate_stop.py — PRIMARY PATH
- tests/test_final_gate_stop_deferral.py
- tests/test_stop_hook_deferral_exemption.py
- tests/test_final_gate_stop_hook.py
- tests/test_kaizen_hook_emitters.py

## Behavior Contract
- **Given** an interactive turn ending on a D1–D4 deferral with no DECISION block, **When** the Stop hook runs, **Then** it blocks with `cause=deferral` and the one-paragraph reason, and warns through after three blocks (spec § C1)
- **Given** a turn ending on a well-formed DECISION block, **When** the Stop hook runs, **Then** the stall lane allows the stop, a `decision_block` event carries its ground, and `owned`+`scope:` inside a live run and an `asked:`/`scope:` quote not found in an operator entry are refused (spec § C2; A-O43)
- **Given** factual prose about a new session and the judged false matches, **When** D4 runs, **Then** it does not fire, while it fires on at least 6 of the 8 judged real excuses (spec § C1 D4; A-O40)

## Steps (the coder's order)
1. Red first, `tests/test_final_gate_stop_deferral.py` (new; synthetic transcripts built with the `_turn`/`_user`/`_asst_text` helpers of `tests/test_final_gate_stop_hook.py:816-824`):
   - red per shape: the say-the-word and menu quotes of `docs/reference/research/2026-09-23-stop-compaction/verdict-opdec.json`; the STOP-EXCUSE quotes of `verdict-context.json` — the test LOADS these files at runtime (each record has `id`, `verdict`, `quote`, and for opdec `shape`), so the coder never reads them whole;
   - one well-formed block per ground (`gate`, `underivable`, `owned`+`asked:`, `owned`+`scope:` outside a run);
   - malformed, one per rule: a missing field; an unknown ground; `gate` without a `_DECISION_GATE_RE` class; `underivable` without `searched:` naming a path, command or ledger id; `owned` with neither `asked:` nor `scope:`; `owned`+`scope:` with a `running` record (a scratch `COMMAND_RUN_DIR` file carrying `state: running` and a numeric `updated_ts` — `_run_record`'s own acceptance rules, `final_gate_stop.py:519-553`; the record's fields are written at `scripts/command_run.py:3673-3693`); an `asked:` quote under 12 characters; an `asked:` quote not ending in `?`; an `asked:` or `scope:` quote absent from the operator entries; an `asked:` quote present ONLY in an `isMeta: true` user row (a command's expansion body — the same rows `scripts/render_chat_history.py:242` skips);
   - the headless case (`CLAUDE_MESH_HEADLESS=1`, and a last user entry carrying `"entrypoint": "sdk-cli"`): no DEFERRAL;
   - `last_assistant_message` in the payload taking precedence over a transcript whose last text entry is older.
   Watch every red fixture FAIL on the unmodified hook.
2. Green fixtures, each proven by mutation (widen the shape on a throwaway copy, watch the fixture fail, restore): `NEXT: awaiting your reply`; `NEXT: none — terminal`; a findings list; a quoted operator question; the nine FALSE-MATCH quotes of `verdict-context.json`; the seven ordinary sentences the test reads from the `PROSE` list in `docs/reference/research/2026-09-23-stop-compaction/d4_probe.py` ("SessionStart fires on a new session and on resume.", "Given the new context from the logs, the fix is in scrape.py.", "The hook now reads a new session-recall index.", "Context is never a reason to stop, and a fresh session is never the remedy.", "the reconstructor handles a new session id correctly", "DONE: fixed the clean context test fixture", "STATE: tests green; the new chat widget ships"); A-O40's five ("The fix takes effect in a new session; nothing to do.", "Hooks load when you start a new session.", "Verified in a fresh session: the hook fires.", "The new session will inherit the synced hooks.", "A new session runs SessionStart first.").
3. Vocabulary: `_DEFER_RE` — D1's closed list, verbatim from spec § C1: operator decision · your call · your go / on your yes·word·go·approval · say the word · if you want · you decide / yours to decide / yours to choose · await(ing) your or the operator's (decision · go · approval · call · word · direction) · human gate · operator-gated · yours to run; a `NEXT:` line carrying one of these is D1, and `awaiting your reply` is excluded by construction ("reply" is not in the list). D2 = an `(a) … (b)` / `option A … option B` / numbered-choice list handing the choice to the operator; D3 = "say the word / say X and I'll …", "want me to …?", "shall I …?", "should I …?", "let me know if/which/whether …", "which do you prefer …?", "do you want …?" outside a quote or code span and not followed in the same message by its own answer. `BLOCKED:` anywhere in the message exempts a DEFERRAL globally, as it exempts every stall shape (spec § C1). And `deferral_shape(text: str) -> str | None` returning `D1`…`D4`. D4 = `(open|use|needs?|wants?|deserves?|requires?) a (fresh|new|clean|separate) (session|window|context|chat)` not followed within 60 chars by MCP · roster · reload · quota · 5h · weekly · reset; or `(a|the) (fresh|new|clean) (session|window|context) (finishes|should|must|checks)`; or `context (is )?getting (long|full|low|tight)` · `compact first` · `this session (is|has been) (running )?long`. Executed in review round 1 over the committed quotes: 6 of 8 excuses, 0 of 9 false matches, 0 of 5 A-O40 sentences — step 2's fixtures pin it.
4. The block, as spec § C2 states it:
   ```
   DECISION NEEDED (ground: gate|underivable|owned)
   - Question: <one plain sentence>
   - Why it is yours: <gate → a class; underivable → what changes and `searched:` what came back silent; owned → `asked:` the operator's open question, or `scope:` the line of the operator's request this step goes past>
   - Options: <A — what changes> · <B — what changes>
   - Recommendation: <A or B, and the one-line reason>
   ```
   `_DECISION_GATE_RE` = deploy · destructive · irreversible · spend (real money, a `$` amount) · cross-repo · publish · credentials · design approval · plan approval · Gate 1/2 · production data; `extract_decision_block(text: str) -> str | None` (the LAST `DECISION NEEDED (ground:` heading outside a fenced block, with its four labelled lines); `parse_decision_block(text: str, *, run_live: bool, transcript_path: str) -> tuple[bool, str]` applying spec § C2's checks, stated here in full: all four labelled lines present and non-empty; the ground one of the three; `gate` → a `_DECISION_GATE_RE` token on the "Why it is yours" line; `underivable` → that line states what changes AND carries `searched:` naming at least one path, command or ledger id; `owned` → an `asked:` quote (≥ 12 characters, ending in `?`) or a `scope:` quote, each occurring verbatim in an operator entry, and `scope:` refused when `run_live`; any failure returns `(False, <the missing or failing item>)`, which the hook reports as a DEFERRAL whose reason names it. Operator entries are read from the transcript (user rows that are not tool results, not `isCompactSummary`, not `isMeta`, not hook-injected).
5. Scope: skip the DEFERRAL check when `CLAUDE_MESH_HEADLESS=1` or the transcript's last user entry has `entrypoint == "sdk-cli"`.
6. `_detect_stall` (`:1631`) gains `text: str | None = None` (keyword, default preserves every positional caller). When given, it replaces the text `_final_turn` reads; when `_final_turn` returns None (no transcript, or none readable — today the early return at `:1647-1649`), the check proceeds on `text` with an empty tool list rather than returning None. The DEFERRAL check runs after the existing shapes, shares the stall counter, never takes the dispatch-kept exemption. D3 replaces the `_PERMISSION_RE` loop (`:1744-1751`). The return becomes `(kind, snippet)` with `kind == "deferral:D1"`…`"deferral:D4"` for the new shape.
7. `main` (`:1814`): read `last_assistant_message` from the payload; call `_detect_stall` when a transcript OR that text exists (the guard at `:1932-1934`), passing the text; log the key's presence as a kaizen field on every Stop (spec U1); correct the header claim at `:17`.
8. Reasons and events at every site that assumes the old kinds: the block path's `kind, snippet = stall` (`:2112`) and its `what` text (`:2127-2132`) — a `deferral:*` kind gets the DEFERRAL paragraph, spec § C1: the shape matched with its snippet; "the next step is yours if the plan, the rules, the ledger or the code decide it — do it now"; "if a human is genuinely needed, end with a DECISION block (CLAUDE.md § FINAL OUTPUT)"; `cause="promise-stall"` at `:2144` and at the warn-through `:1992` becomes `cause="deferral"` with a `shape` field for that kind; the existing stall reason at `:2137-2139` names only the two exits, a `ground: gate` DECISION block or `BLOCKED:` (C-O7, C-O10); a `decision_block` event carries `ground` on every accepted block (the type is registered by T01b).
9. Harvest (`:1836-1847`): pipe the SAME text `parse_decision_block` judged, and pass `--decision-ok` only on `(True, …)`.
10. Session helper for T04: `session_unpushed(root: Path, authored: set[str], *, timeout: float = 2.0, limit: int = 10) -> list[str]` returning `<sha> <subject>` lines of this session's unpushed commits, using `_commit_is_mine` (`:645`) exactly as `_ahead_of_upstream` (`:677`) does; `_ahead_of_upstream` is unchanged.
11. Existing graders: `tests/test_final_gate_stop_hook.py` asserts today's behaviour that this ticket changes — `:965` (`NEXT: round 7 — operator decision: Gate 2 approval…` → None; D1 now fires), `:1084-1093`, `:1119` and `:1166` (each asserts `kind[0] == "permission"` or the no-midrun allow; D3 now returns `deferral:D3`), `:1198` (`say the word and I'll run it` → None; D3 now fires), `:1248` (`Shall I run the next pass now?` → None; D3 now fires). Also `tests/test_kaizen_hook_emitters.py:583`: its `fp_footer` case (`NEXT: operator decision: approve the deploy (Gate 2).`) is asserted to be no stall, and D1 now fires on it — rewrite that case as a well-formed `ground: gate` DECISION block, which keeps the grader's point (a legitimate gated stop is not an override). Invert each to the new behaviour, one line of docstring naming this spec, and leave every other grader untouched. In `tests/test_stop_hook_deferral_exemption.py`, keep its three graders (the pattern shapes keep the per-line exemption) and add one asserting that the same exemption does not clear a DEFERRAL.
12. V1 BEFORE the commit (the commit ships the hook fleet-wide): run D1–D4 over every interactive turn end since 2026-08-09, each final message read whole from its transcript, with a throwaway script in the coder's scratch that imports this worktree's `deferral_shape` and walks every top-level `~/.claude-fleet/active/projects/*/*.jsonl` (subagent transcripts excluded), taking as a turn end the last assistant text entry before the next user entry that is not a tool result, a compaction summary or an `isMeta` row. Report the fire rate per shape and per repo; draw a random 60 fires and judge them per record with three native Sonnet seats (non-deferral fires ≤ 5%); confirm ≥ 17 of the 19 premature FALSE records in `verdict-opdec.json` fire and D4 fires on ≥ 6 of the 8 STOP-EXCUSE and 0 of the 9 FALSE-MATCH records of `verdict-context.json`, all on full text. A miss is fixed here and V1 re-run. The summary and the verdicts go into the Deltas block for T06's receipt.
13. Gate green; `uv run ruff check` on the touched files; CHANGELOG line into the Deltas block.
14. `/fabrik-review` on this ticket's changed surface, partitioned by file (Opus on the hook), to a coverage-adjudicated exit; every finding FIXED or REFUTED.
15. Commit with explicit pathspecs + provenance trailers (`Agent-Role: subagent`, `Agent-Task: T03`). The commit auto-syncs the hook fleet-wide through the post-commit governance sync (spine § Execution Discipline).

## Context Files
- .windsurf/rules/core/10-python.md
- .claude/hooks/final_gate_stop.py
- tests/test_stop_hook_deferral_exemption.py
- tests/test_final_gate_stop_hook.py
