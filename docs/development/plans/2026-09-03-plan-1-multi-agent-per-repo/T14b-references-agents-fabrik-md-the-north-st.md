# T14b — References — agents-fabrik.md, the north-star, command-corpus-check.md: zero references to the retired chains outside archives and ledgers

## Scope
The link sweep, re-derived 2026-09-04 with THIS ticket's own gate exclusions: 28 files reference `epic-to-ticket-workflow`, 8 `_traycer-skills`, 8 `fab-mega-0`, 6 `fab-ettw-`, 10 `traycer_mirror`, 2 `traycer-command-wiring`. After T06a–T12b the live references left are in three docs this ticket owns: `agents-fabrik.md` (9 hits — § Front door / MANDATORY ORCHESTRATOR PRE-FLIGHT `:344` names the chains; rewrite to `/fabrik-vision` → `/fabrik-epics` → `/fabrik-epics-review` → per-window `/fabrik-spec <epic>`), `docs/orchestrator/00-autonomous-factory-north-star.md` (9 hits — the north-star's chain references become the assembled commands; its history paragraphs stay, marked as history), `docs/reference/command-corpus-check.md` (`:55,67,80` — the wrapper-audit paragraphs are removed; the three sources are audited like any source). Also `agents-fabrik-core.md` § Front door line (it names the two trigger docs) — verify with the grep; it is a sync trigger, so the edit must hold for every project (it does: the paths are gone box-wide). The gate line's `git grep` is the denominator: 0 files outside the excluded set. ⚠️ **Three of the survivors are FUNCTIONAL, not prose, and they are owned by T14d, T14e and T14f** — split out of this ticket purely by read budget (the combined set measured 594,710 B against 262,144) — the author-blind pass proved the sweep's gate could never reach 0 without them: `scripts/review_rubric.py:103` points the rubric at `docs/orchestrator/epic-to-ticket-workflow/EVALUATION_CHECKLIST_FOR_EPIC_COMMANDS.md`, which **T11 moves** (the rubric would resolve a dead path); `scripts/enforcement/check_review_coverage.py:581,1314` keys a live report parser on `fab-mega-04-validate` and tells the operator to "Re-run fab-mega-04-validate", a command **T07a deletes** (a silent fail-open on a running check); and `scripts/command_run.py:1236` hard-codes the same name in its report-owing list. Their tests move with them, in the same tickets. README.md's seven `~/.traycer` references (the spec's § Chain consolidation (b) install-step retirement, which had no ticket at all) are T14d's. **The command sources and `docs/reference/command-evaluation-checklist.md` are T14g's** — the author-blind pass showed this ticket's gate could not reach 0 without them, and one of them (`fabrik-conformance-review.md:11`) is a live routing instruction, not prose. **DECLARED DEFERRALS, excluded from the gate rather than silently failing it:** `docs/orchestrator/orchestrator-cockpit-*.md` (spine I13 — the never-built cockpit, a separate retirement decision), `docs/development/epics/2026-08-27-epic-*.md` (historical epic artifacts, not live routing), `docs/CAPABILITIES.md` + `capabilities.json` + `docs/workstation/kaizen-shrink-audit.md` + `docs/workstation/claude-configuration-inventory.md` (inventories that describe what EXISTED, and are re-generated on their own schedule). ⚠️ **The gate is an ALLOWLIST, not a zero-count.** The third author-blind pass proved a `| grep -x 0` form can NEVER pass: after T10/T11/T09 the tombstones live at `docs/orchestrator/_retired/epic-to-ticket-workflow/*.RETIRED.md` and `_retired/traycer/traycer-command-wiring.RETIRED.md`, so the banned tokens are inside the PATHS `INDEX.md` is required by the Doc Sync Matrix to list — the file is structurally unfixable and so is `docs/STRATEGIC_BACKLOG.md:49`, which is an orchestrator-applied governance surface no ticket may own. Both are named in the gate's allowlist, and anything NOT in it fails loudly by name. The pathspec exclusions below are the declared deferrals, so a reader sees what was deferred instead of inferring it from a passing check. DO-NOT: touch `CLAUDE.md`/template/packs (T14a) or `src/fabrik/cli.py` (T14c).

Depends: T09
Parallel: ⛓️
Complexity: complex
Gate: bash -c 'set -e; allow="INDEX.md docs/STRATEGIC_BACKLOG.md"; got=$(git grep -l "epic-to-ticket-workflow\|_traycer-skills\|fab-mega-0\|fab-ettw-\|traycer_mirror\|traycer-command-wiring" -- ":!docs/orchestrator/_retired/" ":!docs/orchestrator/orchestrator-cockpit-*" ":!docs/development/epics/" ":!docs/CAPABILITIES.md" ":!capabilities.json" ":!docs/workstation/kaizen-shrink-audit.md" ":!docs/workstation/claude-configuration-inventory.md" ":!docs/DECISIONS.md" ":!CHANGELOG.md" ":!docs/LESSONS_LEARNT.md" ":!docs/development/reviews/" ":!docs/superpowers/" ":!docs/archive/" ":!docs/development/plans/" ":!.fabrik/plan-locks/"); for f in $got; do case " $allow " in *" $f "*) ;; *) echo "UNOWNED: $f"; exit 1;; esac; done'   # ALLOWLIST, not `| grep -x 0`: see Scope
Gate: python3 commands/assemble_commands.py --check   # `commands/_fragments/grounding-rules.md` is INCLUDED by fabrik-spec.md and fabrik-plan-after-chat.md, so editing it makes both rendered commands stale and `--check` reports HAND-EDITED. Render from the MAIN master checkout, never a worktree (CLAUDE.md:155).
Gate: python3 scripts/enforcement/check_doc_links.py
Docs: agents-fabrik.md (the canonical map — front door names /fabrik-vision) · docs/reference/command-corpus-check.md · CHANGELOG.md — orchestrator-applied

⚠️ **This ticket edits `commands/_fragments/grounding-rules.md`, which is a RENDER INPUT, not a leaf doc.** Its `:1` marker names `docs/orchestrator/_traycer-skills/` and `fab-mega-00-trigger` — both banned by this ticket's own allowlist gate — and TWO sources inline it (`commands/_sources/fabrik-spec.md`, `commands/_sources/fabrik-plan-after-chat.md`), so editing it makes both rendered commands stale and `assemble_commands.py --check` reports HAND-EDITED for each. The render happens in the MAIN master checkout, never a worktree (CLAUDE.md:155), and is the merge owner's act.

## Touches
- agents-fabrik.md — PRIMARY PATH
- commands/_fragments/grounding-rules.md
- agents-fabrik-core.md
- docs/orchestrator/00-autonomous-factory-north-star.md
- docs/reference/command-corpus-check.md

## Behavior Contract
- **Given** the four docs, **When** the gate's `git grep` runs with the stated exclusions, **Then** it lists 0 files (agents-fabrik.md:344)
- **Given** `agents-fabrik-core.md` § Front door, **When** read, **Then** the epic tier names `/fabrik-vision` and the multi-epic tier `/fabrik-vision` → `/fabrik-epics` → `/fabrik-epics-review`, with no `docs/orchestrator/...00-trigger` path (agents-fabrik-core.md:1)
- **Given** `docs/reference/command-corpus-check.md`, **When** grepped for `_traycer-skills`, **Then** the count is 0 and the audit denominator paragraph counts sources only (docs/reference/command-corpus-check.md:55)
- **Given** the tree, **When** `check_doc_links.py` runs, **Then** it reports no broken link into `docs/orchestrator/` (docs/orchestrator/00-autonomous-factory-north-star.md:1)

## Context Files
- docs/superpowers/specs/2026-09-03-multi-agent-per-repo-design.md
- docs/development/reviews/2026-09-03-orchestrator-chains-corpus-review.md
