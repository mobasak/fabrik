# Hooks Index — every hook on this box, in one place

Freshness is GATE-ENFORCED in the ADD direction: `scripts/enforcement/check_hooks_index.py`
(hub-side, Tier 2) fails the gate when a hook script OR event registration exists in the live configs
but is missing from this page. Removals are NOT mechanically caught — retiring a hook obliges you to
delete its row here (honest limit, not a promise).

## 1. Claude Code — fleet-synced project hooks

Distributed to every `/opt` project + the hub via `AGENT_HOOK_FILES` (`scripts/fabrik_synced_manifest.py`);
wired in the synced `.claude/settings.json`.

| Event | Hook | What it does |
|---|---|---|
| SessionStart | `final_gate_stop.py --baseline` | Snapshots the failing gate-check set at session open (runs the LEAN gate `--lean --check` — the same Tier-1 check set the Stop verdict uses, so attribution compares like with like) |
| SessionStart | `session_orient.py` | The binding ORIENT block: governing CLAUDE.md (hub-contract vs synced-template text branches on content-based repo identity), MEMORY.md state (bounded 256KB read), session-recall tools + mandatory-use cases, the enforcement mesh (FIVE Stop causes). **Arms the self-watch**: the ORIENT block carries the operator-mandated `claude-selfwatch.sh $CLAUDE_SESSION_ID` arming line and points at THIS index as the authority for any hook/mesh claim (an unarmed session's mid-stream death is recorded and Telegrammed but never revived). Fail-open. Autonomous marker: with `CLAUDE_MESH_AUTONOMOUS=1` it drops the sweep-eligibility marker into the PERSISTENT `~/.claude/state/autonomous/` (`MESH_STATE_DIR`) — never the /tmp lock dir, which dies with the VM (the Modern Standby cut). **Shared-checkout advisory**: a self-contained `/proc` scan (comm == `claude` + a `cwd` symlink match — no `git worktree list`, which carries subagent-residue false positives, and no `/proc/<pid>/environ` read) counts sibling `claude` sessions at this exact cwd; at ≥2 it prints one bullet naming the count and the `--worktree` adoption path, suppressed for the hub and for any session whose own cwd is under `/.claude/worktrees/`. Test seam: `FABRIK_PROC_ROOT` (mirrors `CLAUDE_MESH_HEADLESS`'s env-driven pattern). **Unnamed-session advisory** (`_identity_line`, D-034, D-267, D-268): an unset `CLAUDE_AGENT` warns in the hub AND in a project repo that either DECLARES a merge owner in `docs/DECISIONS.md` (`MERGE OWNER:` row, the grammar single-sourced at `docs_updater.py:938`/`decisions.py:34` and drift-pinned in the tests) or is a WORKTREE session with ≥2 live `claude` sessions in this exact checkout (`_count_sessions_sharing`, computed ONCE in `main()` and passed to both advisories so they cannot drift or disagree). ⚠️ In a shared MAIN checkout the identity bullet deliberately stands DOWN and the shared-checkout advisory above owns the case — one measured fact, one remedy per block. ⚠️ It does NOT key on `docs/development/PLANS.md`: that comment is RENDERED from the ledger row, so keying on it would let anyone silence the advisory by deleting one HTML comment while `read_merge_owner()` still returned the owner; adoption is the wrong axis too — a repo can run several lanes with plan-locks and carry neither marker nor row. A live reading, not a static census. Ledger read bounded to TWO 64 KB windows (head and tail, up to 128 KB; a row in the middle of a larger ledger is not seen — routed), owner capped at the grammar's 32 chars, worktree sessions get the `--worktree` remediation form |
| SessionStart (user-level, `~/.claude/settings.json`) | `/opt/fabrik/scripts/sysadmin/claude_selfwatch_orient.sh` | The self-watch ARM order for every `/opt` session whose project has NO `session_orient.py` — sync-excluded repos (fabrik-lib) never get the hub hook, so their panes would never arm a watch. Same gates as the hub hook (real sid · not headless · not `source=compact` · watch script present) plus: `/opt` trees only (the VS Code one-shot helpers in `/tmp` have no pane) and no project `session_orient.py` (two orders breed two arms). Fail-open. Tests: `tests/test_claude_selfwatch_orient.py` |
| UserPromptSubmit | `mcp_watch.py` | D-041 per-message MCP forcing. STALENESS = three signals compared against the **HARNESS PROCESS start** (`/proc` ancestry walk to the claude process — when the tool universe actually loads, so a window reload truthfully clears it; transcript-head timestamp is the fallback; **never stat times** — ctime moves on append and races the transcript flush, D-070): the repo `.mcp.json` mtime · the account-pointer SYMLINK's lstat (a rotation re-points `active` without touching the file — lstat on the roster PATH is a no-op, only the final component escapes dereferencing) · the roster's **hashed MCP slice**. ⚠️ The roster FILE's mtime is NOT a signal (D-073) — it is Claude Code's global state file, rewritten every few seconds, so it would make the banner fire on every prompt. Undetermined start ⇒ OVER-WARN, and the banner says so rather than asserting a comparison it never made. Banner leads with CHECK-YOUR-MCPs/fix-first; reload demoted to the class it restores. + cached `mcp_health` dead-server banner (TTL 15m, detached refresh); fail-open (exit 0 AND banners still emitted under hostile payload/cache shapes), ~30ms |
| SessionStart | `agent_role.py` | Injects the named agent's role charter (`CLAUDE_AGENT=<any [a-z0-9-]{1,32} name>` → `docs/reference/agents/<name>.md`) — hub-agent-roles spec r2, relaxed from the fixed `infra\|fleet\|intel` enum by multi-agent-per-repo I2 (any project-local name; the charter is optional, present → injected only when the file's first line IS `# Agent charter` or begins with `# Agent charter` followed by whitespace (a bare-prefix look-alike such as `# Agent charter-obsolete` does NOT inject) — a non-charter document sharing the name, e.g. a kaizen log, is never injected); fleet-safe silent no-op when the env is unset, malformed, unmarked, or no charter exists (every project) |
| UserPromptSubmit | `skill_router.py` | Bare-prose EN/TR routing to the owning `/fabrik-*` skill ("invoke it, or say in one line why not"); regex tier always, Haiku tier opt-in (`FABRIK_ROUTER_HAIKU=1`); never blocks or rewrites |
| SessionStart + UserPromptSubmit | `mail_notify.py` | Surfaces the repo's unread fabrik-mail (`/opt/fabrik-mail/<repo>/inbox`, override `FABRIK_MAIL_ROOT`) as a bounded, sanitized, untrusted-data-delimited summary (≤10 msgs, subject capped 120); repo identity from the git main checkout; whole body catch-all fail-open (a broken mailbox must never block a prompt) |
| Stop | `final_gate_stop.py` | Definition-of-done enforcer, SIX blocking causes: gate red on session-authored files (path-token attribution) · session's own work uncommitted · committed-but-UNPUSHED (branch ahead of upstream; the task-end push law — indeterminate/no-upstream never blocks) · checkpoint-stall (promises, plan-answered permission questions, passive obligations, assertive continuation claims, numbered `NEXT: round/pass N` footers naming undispatched own-loop work, and DEFERRAL D1-D4, spec 2026-09-23-stop-and-compaction-enforcement-design § C1 — a `NEXT:` line, a menu, an offer to act, or an own-work-to-a-later-session excuse that hands the operator a decision the plan, the rules, the ledger or the code already make; `_DEFER_RE` + `deferral_shape` (`.claude/hooks/final_gate_stop.py:1518`, `:2107`) are the ONE vocabulary this cause and `scripts/sysadmin/stop_mine.py`'s V1 backtest both count with, so a reworded deferral shows up as a falling fire rate rather than a silent miss; TWO things exempt it: a well-formed `DECISION NEEDED (ground: gate|underivable|owned)` block (`extract_decision_block`, `parse_decision_block`, `:2131`, `:2294`), and a line-start `BLOCKED:` escalation header outside any quoting fence (`_blocked_header`, `:1970`) — its `asked:`/`scope:` quotes are verified verbatim against the transcript's own operator entries, and an accepted block also emits a `decision_block` kaizen event carrying its `ground`) · a command RUN RECORD still `running` (close it by name via `command_run.py done/blocked`) · UNREVIEWED SPONTANEOUS WORK (PER-CHANGE: code files authored AFTER the last CLOSED command's record — a closed command covers the edits before its close and nothing after; a running record is the fifth cause's business; a per-session exemption would let one early review license a day of unreviewed plain-chat commits. Remedy `/fabrik-review-scoped`. The coverage is EVERY window a command of the session closed — the record's `covered` ledger (`command_run.py` appends `[started_epoch, close]` at each agent close, `start` carries it across its overwrite and seeds a pre-ledger closed record's own window, a nested run's windows join its caller's, deduped) plus the live record's `[started_epoch, ∞)`; edits before the SessionStart baseline are a resumed transcript's and never count, and the floor is the LATEST of THREE bounds (`_sixth_cause_floor`): that baseline, an EDIT-AGE window (`_SIXTH_CAUSE_MAX_EDIT_AGE_S`, 24 h), and the ledger's own birth (`_LEDGER_EPOCH`). The AGE bound is the one that usually binds — a RESUMED transcript HAS a baseline and is merely old. Work older than the window drops out of this cause entirely, the fail-open direction it takes by charter: edits older than the ledger were adjudicated by the per-session rule of their day and no ledger holds their closes; closed states = `command_run.py::AGENT_CLOSED_STATES` = {done, blocked, handoff} (the coroner's `died`/`expired` cover NOTHING — a reaped run had no review); a `started_epoch` ≤ 0 covers nothing; a `running` record covers only while the fifth cause still acts on it, and the operator's `COMMAND_RUN_STALE_H<=0` hatch disarms this cause too; NaN/Infinity/bool timestamps read as no record; warn-through re-arms; `step/round` refuse a closed record). The windows include every frame PARKED on the live record's `stack` — a nested `start` parks the running parent there and gives the child an EMPTY `covered` ledger on purpose, so for the whole life of a nested `/fabrik-review` both the parent's live window and every window the session had already closed were invisible and work authored under the parent's own contract would read as unreviewed (`stack` is flat, so no recursion is owed, and a frame's RUNNING window rides the live record's freshness while both ledgers of closed windows survive staleness) · a RUNNING review-family record EXEMPTS BY NAME the files its `--surface` spells (`_surface_reviewed`, `_REVIEW_FAMILY` bound to `command_run.py::REVIEW_FAMILY` by a parity grader like `_CLOSED_STATES`) — otherwise the sixth cause fires on exactly the files the running review is reviewing; the reach-back cannot move earlier because a `blocked` or `handoff` close must never certify the gap. Equality on a whole token only — `scripts/` exempts nothing under it; a colon is not a path character in the splitter, so `tests/b.py:44` and `a.py::test_x` end their token at it for free , while a leading `./` IS stripped because both its characters survive the split. A frame's running window counts only while the LIVE record says `running`: `kaizen_coroner.py` writes `died`/`expired` on the top-level record alone, so a reaped session's parked frames would otherwise cover `[started, ∞)` · a review-family `done` close whose ledger held NO earlier run covers back to the SESSION FLOOR — `command_run.py` records how far its contract reached in a DURABLE `first_review_reach` field (carried across every later `start` exactly as `covered` is, and joined at a nested pop), and `_first_review_base_case` floors that reach with the SessionStart baseline it alone knows. The three "must not launder" guarantees (a `blocked`/`handoff` close certifies nothing, a non-review command records nothing, a ledger with an earlier run stands the field down) live in the WRITER and are graded there — derived per stop from the live record they would evaporate at the session's next `start`. A corrupt ledger pair also stands it down: an unreadable pair is still evidence of an earlier run. Why it is owed at all: the writer's reach-back has nothing to reach back to with an empty ledger and falls back to the review's own `started_epoch`, so a session interrupted before any command closed would be PERMANENTLY unclearable. Narrow by construction: a window ADDED not a pair rewritten, `done` only, review-family only, and it stands down the moment any ledger window starts before the record's own start. It reads the LIVE record only — a nested review pops into its caller before the next stop, so its identity is gone by then (RECORDED, destination `docs/STRATEGIC_BACKLOG.md`). The three compose in one gradeable reader, `_unreviewed_spontaneous` — the function NAME is the durable address; a line cite drifts. Quote-span/negation/deadline exemptions; `BLOCKED:` exempts globally; human-gate wording is line-scoped AND a bare deferral phrase — "operator decision", "your call", "say the word" — no longer disarms the stall guard by itself: it must share its line with a HARD-STOP class (cross-repo · gate 1/2 · deploy/publish · spend/quota · irreversible/destructive · policy · rule-conflict citing `path:line`); 3-attempt warn-through per cause, independent counter slots (6). **Yields to the quota hold (D-158):** while `quota_stop.py`'s `fleet-exhausted` stamp stands the stop is ALLOWED outright — the hold denies the very tools that clear these causes (`final_gate.py` is not in its allow-list), so blocking only forced more assistant turns, and turns burn quota with every tool denied. Emits `stop_allowed_quota_hold`; fail-open. The yield requires the stamp AND a tick log fresher than `QUOTA_STOP_TICK_STALE_S` (900 s) — the same seam `quota_stop.py` fails OPEN on, so a stale stamp with a dead cron cannot disable all six causes while nothing is held |

### 1a. The CAPABILITY half — `permissions.allow` in the same synced file

The hub distributes the ENFORCEMENT half (the hooks above) **and the CAPABILITY half**: a `permissions.allow` array in the same synced `.claude/settings.json`, so a dispatched
`claude -p` coder can RUN the proof floor its brief mandates.

**Why it is needed:** `--permission-mode acceptEdits` auto-approves **file edits only**. Bash still
requires interactive approval, and `claude -p` is non-interactive — nobody is there to approve, so
every command is refused. A dispatched coder could WRITE code but never VERIFY it.

⚠️ **THE ALLOWLIST IS NOT SUFFICIENT — AND ON THIS BOX IT IS NOT THE OPERATIVE MECHANISM EITHER.**

**1. Trust is the real gate.** Claude Code ignores `permissions.allow` outright in an untrusted
workspace:

```
$ cd /opt/transdoc && claude -p '…python -m pytest --version' --model haiku
Ignoring 34 permissions.allow entries from .claude/settings.json:
this workspace has not been trusted.
The command `python -m pytest --version` requires your approval to run.
```

Trust lives in `<account-dir>/.claude.json` → `projects["<path>"].hasTrustDialogAccepted`, is **per
account dir**, and is NOT propagated by `--sync-shared` (which deliberately preserves it), and **`/opt` does NOT inherit to its subdirectories** — every repo is trusted in every account dir.

**2. The allowlist does no observable work on THIS box.** With user-level
`permissions.defaultMode: auto` + `skipDangerousModePermissionPrompt: true`, a command that is NOT in
the allowlist runs anyway — verified under BOTH default mode and `--permission-mode acceptEdits`:

```
$ cd /opt/transdoc && claude -p '…python -c "print(6*7)"' --model haiku --permission-mode acceptEdits
42                          <- NOT in the allowlist, executes regardless
```

So the array is **defensive depth**, not the fix: it matters on a box or account dir where prompting
is active, and it is the correct thing to ship fleet-wide, but it is not what unblocks a refused coder.

⚠️ **A refusal sentence is not a permission verdict; only a differential probe in the reporter's own environment is.** A model that has read session-recall context can decline with *"it's deliberately blocked in your allowlist"* while the permission system denied nothing, and a probe in the one trusted repo never lets trust bite — so the control probe uses `print(6*7)`, a string recall cannot supply, and runs in the reporter's tree.

Scope (34 rules): test/lint/type runners (`pytest`, `ruff`, `mypy`, `final_gate.py`), JS build+test
(`npm`/`pnpm`/`npx tsc`/`eslint`), `alembic`, read-only git (`status`/`diff`/`log`/`show`), and file
inspection (`ls`/`cat`/`head`/`tail`/`grep`/`rg`/`find`/`wc`). **Not granted:** arbitrary interpreters,
writes via git, network, docker, package installs, `rm`.

⚠️ **An agent cannot add this itself** — the auto-mode classifier blocks an agent editing its own
permission config, and that block does not lift with in-conversation authorization. It is a
human-applied change by design. Budget for that when it next needs widening.

## 2. Claude Code — user-level hooks (box-wide)

⚠️ Under fleet mode the live user config is **`$CLAUDE_CONFIG_DIR`
= `~/.claude-fleet/active` → the current account dir** (`can`/`mob`/`ob`/`sarp`), exported from
`.bashrc:243` and `.vscode-server/server-env-setup:10`. **`~/.claude/settings.json` is the LEGACY
installer copy and is NOT read by the CLI** — editing it changes nothing for a running session while appearing to work. ⚠️ **But it IS the `--sync-shared` copy SOURCE** (`claude_rotate.py` reads `CLAUDE_DIR/settings.json` as "the canonical" and overwrites every account dir's copy with it; `--from` selects only the ROSTER source). So a user-level hook is registered THERE and then synced — an entry added to an account dir's copy is reverted by the next sync . The four
account copies are independent files (`settings.json` is in `_SHARED_FILE_COPIES`, deliberately
COPIED not symlinked, because the CLI writes config back and would replace a symlink); they DRIFT,
and the `*/5` rotation flips which one is live. Re-push one to all four with
`claude_rotate.py --sync-shared --from <slug>`. DR-protected by `dr_claude_backup.sh`.

The hooks below are identical across the legacy copy and the account dirs.

| Event | Hook | What it does |
|---|---|---|
| SessionStart | `session-start-tap.js` | claude-manager session tap (account/quota rotation layer) |
| UserPromptSubmit (user-level, every window) | `/opt/fabrik/scripts/sysadmin/selfwatch_check.py` | **The mechanical half of the self-watch** (D-163): is this session's `selfwatch.lock` HELD? The watcher holds a `flock` on it for its whole life (`claude-selfwatch.sh:30-37`), so held IS armed — no registry, no pgrep; the rotation tick carries a VERBATIM vendored copy of this decider (`claude_rotate.py::_sid_is_armed`, lockstep-graded against `_armed` in `tests/test_claude_rotate_v2.py`) to enumerate the armed watches it wakes at a hold lift — the aro-wake twin ships as one file and cannot import this module. Unarmed → prints the arm order with the LITERAL sid on every prompt until armed; armed → silent. The arm is a background Bash task through `scripts/sysadmin/selfwatch_arm.sh` (D-356): a Monitor ends within 30 minutes and has no persistent mode at 2.1.280, so the wrapper runs the watcher unchanged in its own task, exits on the watcher's first line — stopping the watcher and killing its children first, so the lock is free for the re-arm — cuts the watcher's "STAYS ARMED" clause and prints the re-arm order; one wake per arm, and this check re-orders the arm after every wake (live-tested 2026-09-23: a probe death record woke the session in 24 s and the notice returned). The probe is READ-ONLY — `/proc/locks` by inode first, an `O_RDONLY` flock probe as fallback (taking `LOCK_EX` to ask could make the watcher's own `flock -n` see "already armed"); a read-only lock file no longer crashes into silence and any exception prints one stderr line; an unwritable lock DIR prints the fix-the-dir diagnosis instead of an arm order that would die at once; `CLAUDE_MESH_AUTONOMOUS=1` workers (`claude -p`, no Monitor tool) are silent; the sid transform is byte-wise like the watcher's `tr`. Same gates as the SessionStart ORDER except `source=compact`, which is the point: a compact-resumed session never saw the order. Fail-open. Tests: `tests/test_selfwatch_check.py` |
| UserPromptSubmit + PreToolUse (user-level, every window) | `/opt/fabrik/scripts/sysadmin/user_hook_gate.py <hub hook>` | Runs the hub's `mcp_watch.py` (UserPromptSubmit) and `quota_stop.py` (PreToolUse `.*`) for any window whose PROJECT does not already wire that hook — closes the sync-excluded gap (fabrik-lib had no MCP banner and no quota hold) without editing a synced hook or the exclusion ruling. Detection: the project ROOT is `CLAUDE_PROJECT_DIR` or the nearest ancestor of cwd holding `.claude/settings.json`; that file (+ `settings.local.json`) is PARSED and the gate defers only on a hook entry under the SAME `hook_event_name` naming this basename (a substring match anywhere would silently strip a window of its hold); `disableAllHooks` never defers. The wrapped `quota_stop.py` denies via stdout JSON with exit 0 — forwarded byte for byte; a hang or a missing hook is fail-open with one stderr line, inner timeout 8 s under a 10 s registration; in the 42 synced repos the project copy fires and the gate exits silently (no double banner, no double deny). Exit code and stdout pass through untouched. Fail-open toward COVERAGE (unreadable settings → the hook runs). D-163. Tests: `tests/test_user_hook_gate.py` |
| SessionStart (user-level, every window) | `/opt/fabrik/scripts/scratch_sweep.py --hook` | **The scratch advisory** (D-184/D-187): one line naming how many stale entries sit in THIS session's scratchpad, the dry-run command and the apply command — and what the sweeper never touches. Fires at SessionStart only: a `UserPromptSubmit` trigger is wallpaper, because a working session never becomes clean. A count-recording stamp inside the session's own dir (`<root>/<slug>/<sid>/.scratch-nagged`, never `~/.claude*/state`, which the sweeper's refusal set forbids it to write) holds the count seen at every COMPLETE evaluation — a silent zero included, a budget-truncated walk never, so an agent that complied and swept can be told again — and the line reprints only when that count has GROWN. A budget-truncated count is a lower bound and never reprints. Gates mirror `selfwatch_check.py`: real sid · not headless · not autonomous · an `/opt` cwd. Its freshness walk has a 4 s deadline (a large scratchpad's holder probe alone costs ~2 s); `SCRATCH_SWEEP_HOOK_BUDGET` is its stat-cap test seam. Fail-open, exit 0 on every path, one stderr line on an exception. Fire rate is bounded by the stamp, not by a census; each of those three behaviours is graded (`test_the_hook_line_reprints_only_when_the_candidate_count_grows`, `test_hook_line_fires_only_with_candidates_and_never_blocks`, `test_a_truncated_walk_never_makes_the_hook_line_reprint`). Tests: `tests/test_scratch_sweep.py` |
| UserPromptSubmit (user-level, every window) | `/opt/fabrik/scripts/sysadmin/quota_posture_hook.py` | **The `QUOTA:` line** (D-269): one injected line per prompt — `QUOTA: <slug> · 5h <n>% (<forecast>) · weekly <n>% (<forecast>) · Fable <n>% · band <GREEN\|AMBER\|RED\|WALL> · successor <slug or none>` — read from `~/.claude/state/quota-posture.json`, which the rotation tick writes each cycle. `<forecast>` is `reset in <h:mm>` or `wall in ~<m>m at <n>%/m`, whichever comes FIRST, or `no burn`; a figure with no reading prints `—` and a band it cannot compute prints `?`. A missing, unreadable or >`QUOTA_POSTURE_STALE_S` (900 s) file is ABSENT and SAYS so — `posture unavailable (<reason>)` — never silence, because silence reads as 'quota is fine'. ⚠️ Box-local, NOT fleet-synced: `scripts/sysadmin/` is absent from `fabrik_synced_manifest.py`, so the ~46 project repos never receive it; `--install` wires it into the six user-level settings files instead (the fleet root's `active` entry is a SYMLINK and is skipped, or one account would be wired twice). The rotation tick prints one advisory line naming how many settings files are unwired. Fail-open on every path, exit 0 always. Tests: `tests/test_quota_posture.py` |
| PreToolUse (user-level, every window) | `/opt/fabrik/scripts/sysadmin/quota_posture_hook.py` | **The RED hold** (same file, same D-row): at band RED it denies exactly the two things that START new work — the `Agent` tool when the session has no live run record, and a fresh `command_run.py start` outside the review family (TOKENISED with `shlex` and judged on argv, so `uv run`, a `cd … &&` chain, an absolute or QUOTED path, a quoted name and reordered flags all land, and the LAST `--command` wins as argparse resolves it — a regex is defeated by a quoted path, a quoted review-family name and a decoy first `--command`). Edits, tests, git, `done`, `round`, mail, Monitor and TaskStop stay allowed, so RED is finish-and-checkpoint, never freeze — and the review of the change you are checkpointing is explicitly permitted. At AMBER it emits the line once per (session, band) as `additionalContext` with NO permission decision; the marker is created by the asking (`O_EXCL`), so two concurrent calls cannot both decide they are first. ⚠️ **The WALL is not this hook's — but only the WALL** (D-306): the `fleet-exhausted` stamp's TIER is read BEFORE the band, and at `walled` the hook goes silent because `quota_stop.py` (row above) owns that deny and two differently worded denials for one call is worse than one. At the stamp's other tier, `urgent-90`, `quota_stop.py` ALLOWS — so this hook keeps enforcing its own band there, or a genuine fleet RED would pass BOTH hooks unheld; it also appends a CHECKPOINT clause to the prompt line at that tier, the turn boundary being the last moment an agent can checkpoint by choice instead of being denied mid-edit. The reader is a third copy of `claude_rotate.py::_stamp_tier` (no import: one source is a 6,000-line CLI, the other fleet-synced), graded three-way against the same bytes. A session on a Fable model is banded on its Fable window, read from the transcript's last 64 KiB — the payload carries no model — and an unknown model never bands on Fable. Fire rate measured over 2,361 tick rows before it shipped: AMBER 6.0%, RED 4.7%. Tests: `tests/test_quota_posture.py` |
| (installer, not a hook) | `/opt/fabrik/scripts/sysadmin/install_user_hooks.py [--check [--warn]]` | Creates / verifies the four user-level registrations above in the canonical `~/.claude/settings.json` and every account dir : they are pushed by `--sync-shared`, and `check_hooks_index.py` derives its required set FROM the file so it cannot see a MISSING entry. `--check --warn` runs as the warn-only `User-Level Hooks Registered` row of `final_gate.py` (a bare `--check` exits 1 on drift, and a warn-only row that exits non-zero still FAILS the gate) (hub-only — the script is not synced). Tests: `tests/test_install_user_hooks.py` |
| SessionStart | `session_context.py` (/opt/session-recall) | Injects recent-context: last sessions for this project, closing context, recently-active sibling projects |
| Stop | `claude-sound.sh done` | Task-finished sound (state-based park decider — rings only at true final rest). ⚠️ Its 2-hour lock-dir prune SKIPS flock-held files (`_flock_held` — a self-watch's lock file is never touched, so an mtime prune would orphan every watcher) and is PER-ENTRY: one entry vanishing between `iterdir()` and `stat()` (a concurrent session releasing its own lock) must not abort the prune and leave every stale lock behind it. This file is box-local, tracked by no repo, and the mesh scripts are otherwise diagnosed READ-ONLY — a change needs the operator's explicit approval and a backup in `~/.claude/bin/backups/`. Mid-stream death family: a tail whose last assistant record carries `isApiErrorMessage` + any of the CLI's five mid-stream death texts (`Response stalled mid-stream` · `Server error mid-response` · `Connection closed mid-response` · `Response stalled while thinking` · `Connection closed while thinking` — the whole family at one construction site in the 2.1.219 string table) is a DEATH, never busy-input — while a `<sid>.holdlifted` file is NOT a death record: nothing rings, nothing Telegrams, the decider/coroner/reboot-sweep never read it; only the self-watch consumes it — checked BEFORE the pending-waiter probes; waiters get one 120s `busy-stalled-wait` recheck (a pending wakeup defers — it IS the revival), then `api_error_stalled` `.errparked` + error voice wake the armed self-watch. The loud variants also fire StopFailure (Layer 1 heal-at-death, below) — tail-detection doubles them harmlessly (dup-park guarded) and is the only net for the silent ones. Connection-failure class: a no-role `type=system`/`subtype=api_error` record whose `error.connection.code` is present AND `retryAttempt == maxRetries` (the CLI's own retries exhausted — the STRUCTURAL key, covering ENOTIMP/ECONNRESET/ECONNREFUSED/ETIMEDOUT/ENOTFOUND, not a string allowlist) is the same `stalled-api-error` death routed into the same `.errparked` revival; a still-retrying `api_error` (attempt < max) is NOT a death; recovery-discrimination (real operator input after the record) suppresses it |
| Notification | `claude-sound.sh attention` | Attention/input-needed sound (matcher `permission_prompt`) |
| PreToolUse | `quota_stop.py` (project, synced) | **Fleet-wide graceful stop** on the rotation tick's `~/.claude/state/fleet-exhausted` stamp — set by `claude_rotate.py::_fleet_active_wall_advisory` when the ACTIVE account is walled — or its SESSION window is at `ROTATE_URGENT_DRAIN_PCT` (90) — and no successor exists (or rotation is paused), cleared by the tick on relief. ⚠️ **TWO TIERS, ONE STAMP, AND ONLY ONE OF THEM HOLDS (D-306).** Line 2 of the stamp names the arm: `walled` (the wall — a window at/over `ROTATE_THRESHOLD`, its `caps.json` cap, or 100) or `urgent-90` (the 90-line warning, which fires EIGHT points earlier on the default `ROTATE_THRESHOLD` of 98 because that runway is exactly what a graceful stop needs, D-111; that gap is on the SESSION window alone, since `urgent` tests `five_hour` while a `caps.json` cap is applied to `seven_day`, so a cap opens an episode at `walled` on its own axis rather than shortening this runway). At `urgent-90` the hook DENIES NOTHING and writes one stderr CHECKPOINT nudge; a stamp not plainly naming a known tier (every stamp written before the field, or an unreadable one) reads as `walled`, so the migration fails CLOSED. Matcher `.*`; at the `walled` tier the rule is DEFAULT-DENY — Edit/Write/MultiEdit/NotebookEdit, MCP editors (serena replace/insert/rename, browser clicks), Agent/Workflow dispatch and any Bash that is not ONE simple checkpoint/read command (no `&&`/`;`/`\|`/`$( )`/file redirection; `git reset` only in its `HEAD --` realign form; no `sed`, `find`, `rg`, `date` or `git branch` at all, no `$'…'`/`$"…"` quoting, no `$` that OPENS a word — unquoted in any form (`git push $PV` word-split into `--force`) or as the whole of a double-quoted span (`git push origin "$F"` handed git `--force` as one word); a `$` inside quoted MESSAGE text (`"fix $X"`) is data, but a `$` or backtick anywhere in a pathspec/refspec is held (`git add "./$FILES"` swept — the checker cannot expand it, bash will), and on a git line only the verb's own CHECKPOINT/READ flags — a POSITIVE per-verb set in `_GIT_VERB_FLAGS`, read as argv; `--force`/`+ref`/`--delete`/`:ref`, `--amend`/`-a`, `-A`, `--output`, `--upload-pack`, `--ext-diff`, `--textconv` and every unlisted flag (git's abbreviations included) are held, fail-closed; a pathspec to add/commit/reset may not normalise to `.`/`..` or escape upward (`../x`), contain a glob (`[` included — git reads it as one) or be `:(magic)`; a push refspec may not start with `+`/`:` and a fetch refspec may not start with `+` or contain `:` (before or after `--`); `git commit` only in the template shape `-m\|-F … -- <paths>` (a bare commit takes the whole index — a sibling's staged hunks included); `git reset` only as `[-q] HEAD -- <paths>`; `command_run.py`/`mail.py`/`thread_anchor.py` only as bare `scripts/…` or `/opt/<repo>/scripts/…`) are DENIED with one instruction (commit + push with explicit pathspecs, close the run record, end the turn); reads, git, `command_run.py`, `mail.py`, `thread_anchor.py`, `Monitor`, `TaskStop` and the exact self-watch arm (`bash /opt/fabrik/scripts/sysadmin/selfwatch_arm.sh <sid>`, D-356 — the hold's lift wakes only an armed session) stay allowed so the Stop hook's commit-and-push law can be met and a held session can stop its own native subagent; the denial text ORDERS the self-watch arm with the session's own sid interpolated from the hook payload (`<your sid>` when the payload carries none — or a malformed one: non-string, empty, over 80 chars, or any char outside `[A-Za-z0-9._-]`); a quoted newline inside a `git commit ` line's -m body is DATA to the masker (either quote) so the ordered exit can carry the provenance trailers — a newline anywhere else is still the multi-line veto — because the hold's lift — the tick's stamp unlink — writes a `.holdlifted` file for every ARMED watch (rows 106/112) and an unarmed session stays idle until the operator restarts it (D-177/D-178). ⚠️ Those operator vetoes read the line with **shell-QUOTED spans MASKED** (`_mask_quoted`) — punctuation inside a quoted ARGUMENT is data, not an operator. Without masking a `;` in a `--reason` string refuses the very `command_run.py blocked` call this hold orders (CLAUDE.md's BLOCKED format puts one there whenever two sources are listed), deadlocking the hold against the Stop hook's open-record block. Masking keeps `$(`/backticks live inside double quotes and leaves an unbalanced-quote line untouched, so every chained-command tooth still bites. FAIL-OPEN: no stamp / unreadable → allow; tick log older than `QUOTA_STOP_TICK_STALE_S` (900s) → allow + one warning (a dead cron never freezes the fleet). Hooks load at session START: a session already running when `settings.json` synced gets the hook on its next start; the stamp can outlive relief by one tick (≤5 min). The mail broadcast still goes out; this is what reaches a session MID-TURN. |
| PreToolUse | `claude-sound.sh attention` | Question-popup ring (matcher `AskUserQuestion` — that popup emits no other hook event) |
| PreCompact | `claude-sound.sh compact-start` | Writes the `compacting` marker so the decider reads compaction as busy (transcript shows nothing mid-compact) |
| PostCompact | `claude-sound.sh compact-end` | Clears the `compacting` marker |
| StopFailure | `claude-sound.sh failure` | Failure pipeline + the **resume mesh**: writes the `errparked` death record (skipped for sid-less payloads; the decider CLEARS it on a busy turn-death — a live waker makes the death non-terminal), triggers HEALTH-AWARE account rotation for auth/rate classes (**default ON** — a switch requires a VERIFIED unwalled sibling and targets it by name via `--switch`, never blind churn; `CLAUDE_SOUND_AUTOROTATE=0` is the wait-only escape hatch; 10-min limiter), spawns the opt-in headless reviver (`claude-autoresume.sh`, `CLAUDE_SOUND_AUTORESUME=1`; its `claude -p` child carries `NO_REVIVE` + `CLAUDE_MESH_HEADLESS` so it never forks a second writer or arms a pane watch), and on a truly-dead `/opt` ring escalates to Telegram (`mesh-notify`, 30-min suppress, every outcome logged; its cwd gate matches `/opt` **and** `/opt/*` — an `/opt/*`-only glob drops a session whose cwd is `/opt` exactly, logged as `cut-unnotifiable`). Pane auto-continue: **EVERY interactive session arms `claude-selfwatch.sh` via the ORIENT-ordered persistent Monitor** (operator-mandated; skipped for headless runs and compact-resume — the armed Monitor survives compaction). The self-watch consumes a pre-arm marker silently and consumes on fire — one wake per death record, network-gated for all classes; **and it polls `<sid>.holdlifted` beside the death marker — the RELIEF WAKE (D-177/D-178): the rotation tick writes the lift epoch there for every ARMED watch when the fleet-quota hold lifts; the watch prints ONE `RESUME: the fleet-quota hold LIFTED at <lift HH:MM> …` line naming `command_run.py line` and `thread_anchor.py line`, consumes the file and stays armed; any lift already present at arm is deleted unconditionally (pre-arm history — no age test); an offline lift waits on the same connectivity gate and fires when the network returns; a death and a lift together wake death-first, lift on the next iteration (harness fixtures W10-W10d); a watch whose lock FILE vanishes exits with one line instead of polling as an invisible orphan (W11)**; **STANDING**: it keeps watching after a wake (a one-wake-then-exit shape leaves every second death un-woken whenever the agent has not re-armed yet), a duplicate arm for the same sid exits at once under a per-sid flock (`<sid>.selfwatch.lock`), the offline ceiling restarts the cycle instead of ending the watch, and a `rate_limit` wait RE-ASKS `claude-quota.py` every slice so a manual account switch ends the wait within one slice instead of sleeping to the DEAD account's reset clock. Fixtures W6/W7/W9/BQ10 in `claude-mesh-test.sh`. Quota-health: a `rate_limit` death is parsed by `claude-quota.py` into a WALL (`rateLimitType` + `resetsAt`, from the manager tap's exhausted window or the payload's `error_details`); both revival layers then wait to that CLOCK in ≤60s slices instead of a blind 90s, and the operator gets a "revival scheduled in Nm" Telegram. Fixture harness: `claude-mesh-test.sh` (A0a is the recorded baseline red). The decider also bridges WAKER LOSS (a death stranding a pending task/subagent → permanent busy-silence): every busy-waker verdict arms a detached zero-API sleeper that re-evaluates after the staleness bound — a **provably** lost waker (dispatched, never completed; persistent Monitors and the background-Bash self-watch arm — a command ending in a shell running `selfwatch_arm.sh <sid>`, D-359 — are standing watches, never wakers) rings "(waker lost)" in the error voice, writes a `waker_lost` death record (armed self-watches wake the pane), and Telegrams for `/opt` sessions with the true class |

## 2c. Boot network guard — `scripts/wait_for_network.sh` (not a hook; the startup pipeline's first step)

WSL brings its network up **after** the first interactive shell, and `~/.bashrc` sources
`wsl_startup_hook.sh` the moment one opens. That race loses real alerts (`Temporary failure in name resolution`, `No route to host`), strands the pipeline's own auto-commit push and returns empty classification rounds — all of which succeed ninety seconds later. The guard blocks until `getent hosts` resolves
`WAIT_NET_HOST` (default `api.telegram.org`), bounded by `WAIT_NET_TIMEOUT_S` (default 90s), polling
every `WAIT_NET_INTERVAL_S` (default 3s).

**It ALWAYS exits 0.** A boot guard that can hang is worse than the race it fixes — it would freeze
every login shell on a box that is offline by choice. A timeout prints a warning naming the steps
that may fail, and stands aside. A non-numeric override falls back to the default rather than
looping forever. Tests: `tests/test_wait_for_network.py`.

## 2b. Cron @reboot — the resume sweep (not a hook; documented here as mesh Layer 4)

`claude-reboot-sweep.sh` (crontab `@reboot`, box-side, DR-versioned). Standby-survivable: reads persistent `.autonomous` markers (`~/.claude/state/autonomous/` first, legacy
/tmp lock dir second, gather-list union), self-flocks (lock fd closed in resume children),
claims each marker atomically via `mv` before spawning (a failed claim skips, never boot-loops),
widens eligibility to the `stalled-api-error*` death classes, and classifies `vm-cut`. Leg B —
ONLY on a fresh boot (lock dir absent; cron-restart re-fires skip it) — scans pre-boot
transcripts (≤20, 48h window, subagent sidecars excluded) for cut-mid-work sessions
(interactive panes included) and Telegrams one `claude --resume <sid>` per cut session via
`claude-sound.sh mesh-notify` (24h persistent per-sid suppress; sids Leg A just resumed are
excluded; panes are notified, never auto-resumed). A missing `claude` binary skips only the
marker leg — the notify leg still runs. Fixtures: mesh harness §RS (135 total).

### 2c. Cron */5 — the quota-rotation tick (mesh sibling of §2b)

`claude_rotate.py --tick` (crontab `*/5`, flock'd via `~/.claude/state/rotate.lock`; repo-side
script `scripts/sysadmin/claude_rotate.py` + aro-wake twin). FEATURE-DETECTED, two modes. Fleet
mode (≥1 scaffolded dir under `~/.claude-fleet/` — the login-once architecture): per-ACCOUNT
telemetry + advisories ONLY — pinned-identity grouping, freshest-token quota with a
cached-with-age fallback, ≥`ROTATE_DRAIN_THRESHOLD` (85) fires one advisory Telegram per
account per 24h + drain fabrik-mail to that account's mapped repos; the FLIP LEG re-points the
`active` pointer when the active account trips — the 5h leg at `ROTATE_THRESHOLD` (98, D-201), the weekly
leg at its `caps.json` cap — on the PROJECTED reading (reading + the burn since the previous tick, remembered in `state/tick-last-reading.json` — a line checked every 5 minutes must be crossed BEFORE the wall, D-103), to the perishable-first sibling with proven headroom (walled / cap-walled / ≥threshold /
no-5h-budget siblings excluded), DWELL-EXEMPT — a trip is a wall, never churn (D-104); and at session ≥ 90 with NO
eligible successor the wall advisory fires early as the operator's URGENT mail — stop gracefully, hook
to 1 min after the next account's session reset, with the instant stated (`_next_session_relief`). Legacy mode (until the fleet root is
populated): polls the LIVE account's `oauth/usage` both windows; at `ROTATE_THRESHOLD`
switches to the PERISHABLE-FIRST successor (soonest weekly reset; picked under the shared
switch flock — TOCTOU-free vs manual `--switch`), Telegrams one line; with no eligible sibling
at 85 broadcasts the graceful-drain fabrik-mail (commit-and-push checkpoint + revival time;
24h stamp suppress) + one Telegram; keeps parked snapshots warm (expiry-keyed refresh,
identity-gated filing). `--status [--json]` = the operator's live quota table (same feature
detection). Cron sibling: weekly `claude_rotate.py --keepalive` (Mon 06:20) is RETIRED (a ping never extends a refresh chain; the tick warns inside 5 d with the re-login
block and pushes once per chain inside 3 d), a no-op that prints why until the line is deleted. Ledger:
`~/.claude/state/rotate-ledger.jsonl` (VM-cut-survivable). Reference:
`docs/workstation/claude-account-rotation.md`.

## 3. Cascade hooks — DORMANT

`.windsurf/hooks.json` (fleet-synced): `post_write_code` → `validate_conventions` + `check_secrets`;
`post_cascade_response` → `final_gate --lean --check`. **No live runtime consumes this file** — Windsurf
Cascade is retired; the file stays synced as a template for a future non-Claude tool. Do not count it as
active enforcement.

## 4. Git pre-commit hooks

`.pre-commit-config.yaml` (hub; projects carry their own stack-specific configs — not synced).

| Hook | What it does |
|---|---|
| pre-commit-hooks (large files · merge conflicts · private keys · forbidden `.env`/keys/certs) | Standard commit safety |
| `command-corpus-check` | Installed `~/.claude/commands` + skills must match the rendered `_sources/` (hand-edits die on re-render) |
| `decisions-ledger-check` | `docs/DECISIONS.md` has unique ids and resolving `supersedes` pointers (`scripts/decisions.py --root . --check`, this repo only) — three sessions mint ids from a stale max-id read, so collisions happen |
| `research-ledger-check` | Every `docs/reference/research/*-ledger.md` row carries a disposition — `USED → · DUPLICATE of · REJECTED — read whole: · UNREACHABLE — …; tried …` (`scripts/check_research_ledger.py`, this repo only; D-352) — a research fan-out's facts are filed whole before synthesis |
| `governance-sync` (`stages: [manual]`) | NOT RUN BY PRE-COMMIT — the entry only holds the trigger regex (its `files:` filter is the truth, not memory). The sync itself runs from the plain post-commit hook in § 5 (D-369) |
| `review-coverage-staged` | A staged `docs/development/reviews/*.md` review artifact must pass the full `check_review_coverage.py` coverage grammar AT COMMIT (Surface hash · rubric invocation · Pass-2 + re-derivation ledger rows · standing recurrence classes · found: 0) — closes the gap where a review committed while the tree was dirty escaped the advisory sweep. `archived/` excluded (a move, not an endorsement). |
| `refresh-before-ranker` | Both pipeline entry points — `daily_refresh.sh` (cron 06:00) and `wsl_startup_hook.sh` (boot), which both run the ranker (the daily lock they share only makes them exclusive within one uptime — `/tmp` is cleared at boot) — must each invoke `claude_p_cost.py --refresh` BEFORE `rank_task_subagents.py`, which renders that rate into `TASK_SUBAGENT_SELECTION.md`. Wired the other way round the selection doc publishes yesterday's figure for a full cycle; wired into only ONE entry point (as Phase C first shipped) the other silently becomes the whole day's pipeline whenever it wins the lock. Scoped to those two files, so it costs nothing on any other commit. |
| `thread_anchor.py` (via `final_gate_stop.py` at Stop + `SessionStart`/`UserPromptSubmit` in settings.json) | Thread anchors — durable NEXT:-line memory. Stop harvests the final message's `NEXT:` to `~/.claude/state/threads/<session>.json` (best-effort, 5s cap, skips unsynced projects; runs BEFORE the eligibility early-return with a `__file__` fallback, emitting an `anchor_harvest` kaizen event per attempt) AND the prompt-side `line --hook` harvests again from the transcript — the harness can fire Stop before the final text entry is flushed , so the race-free prompt-time pass is the one that always lands; the prompt hooks inject ≤4 open anchors, silent when empty. **On `SessionStart` with `source=compact`, `line --hook` prints `## ⏮ WHERE YOU ARE` INSTEAD of the usual block** (spec 2026-09-23-stop-and-compaction-enforcement-design § C3, `scripts/thread_anchor.py:686` `cmd_where`) — the live command run, the last `NEXT:`, an open DECISION block (harvested only by a Stop-side `harvest --decision-ok`, which the hook runs only at an exit that ALLOWS the Stop — a blocked Stop gets no prompt to clear it, cleared by the next `UserPromptSubmit` whose whole first token is not a built-in slash command, `:516` `cmd_clear_decision`), and this session's own unpushed commits and dirty files, all rebuilt from records — never from agent-written notes. Detail: `docs/reference/thread-anchors.md`. |
| `push-gate` (**pre-push**) | The local replacement for GitHub Actions. Runs `check_duplicates.py` before every push — parity with what `ci.yml` actually enforced, nothing more. Hub Actions are `disabled_manually` and both workflow files are deleted. |

⚠️ **Two facts about the pre-push stage, both learned by running the real hook:**

1. **pre-push DOES stash.** It prints "Unstaged files detected / Stashing unstaged files" on every
   push with a dirty tree, which on this shared tree is always. A hook whose auto-fix conflicts with that stash ("Stashed changes conflicted with hook auto-fixes... Rolling back fixes") is the WIP-destruction shape that got the trailer guard removed from pre-commit (§5). It is survivable only because `push-gate` writes nothing into the tree (`--report /tmp/…`; the default writes
   `duplicate-report.json` into the repo, and pre-commit fails any hook that modifies a tracked file even when the check itself printed PASS).
   **Any hook added to this stage MUST stay non-mutating.**
2. **A hook with no explicit `stages:` runs at EVERY stage, including pre-push.** Without `default_stages: [pre-commit]` a push fires the whole commit-blocker set *and* `governance-sync`, which writes into every project tree. Verified by executing
   `.git/hooks/pre-push` directly; `pre-commit run --hook-stage pre-push` does NOT reproduce it.

**Install:** `.venv/bin/pre-commit install -t pre-push` (or a bare `pre-commit install`, now that
`default_install_hook_types` covers pre-commit and pre-push — deliberately NOT post-commit, D-369). **A config stanza that is installed nowhere is not
enforcement** — this is wired in the hub only; the project repos have no local push gate.

## 5. Plain git hooks (NOT managed by pre-commit)

| Hook | What it does |
|---|---|
| `.git/hooks/post-commit` → `scripts/governance_sync_postcommit.sh` | The governance sync: a commit touching the trigger regex (the `governance-sync` entry's `files:` in `.pre-commit-config.yaml`) distributes to every `/opt` project; runs only from `/opt/fabrik`, never from a worktree. **Install: `bash scripts/install_post_commit_hook.sh`** (idempotent; replaces pre-commit's generated post-commit shim, refuses any other foreign hook; resolves the shared hooks dir via `--git-common-dir`; re-run by `wsl_startup_hook.sh`, cwd-pinned). ⚠️ Deliberately NOT a pre-commit stage (D-369): pre-commit stashes every unstaged change with a tree-wide `git checkout -- .` around each run and, when the restore patch conflicts, rolls back with another tree-wide checkout — around a ~60 s sync that hid every session's WIP and destroyed any edit made mid-window, clean files included (reproduced in a scratch repo; mail 01M3492KY09K6DP47Z21XVM2SH). The commit stage still runs under pre-commit with the same mechanism for its ~0.5 s. Don't run `pre-commit install -t post-commit`: it takes the hook over, keeping this one as `post-commit.legacy` and running it BEFORE its own run — so the sync still runs outside the stash, but every commit then pays a needless stash cycle; the next shell's installer run restores the plain hook and removes the legacy copy (both pinned by `tests/enforcement/test_governance_sync_postcommit.py`, driving the real binary). The installer refuses when `core.hooksPath` is set (git would never run `.git/hooks/post-commit`). |
| `.git/hooks/commit-msg` → `scripts/check_commit_trailers.py` | Rejects a commit whose `Agent-Role:` trailer git cannot parse — a blank line inside the block, or prose glued to its top. Runs at commit-msg, the last moment the message is editable: once pushed, an unparseable block needs a force-push (HARD STOP). Delegates to `git interpret-trailers --parse`; passes through commits with no authored `Agent-Role:`, cherry-pick replays (⚠️ **only** `CHERRY_PICK_HEAD` — `MERGE_HEAD` was a real bypass: a conflict-resolution commit is newly AUTHORED), and any state where git is unavailable. **Install: `python3 scripts/check_commit_trailers.py --install`** (idempotent; refuses to clobber a foreign hook — add `--force` to reclaim it from pre-commit; resolves the shared hooks dir via `--git-common-dir` so it works from a worktree; re-run automatically by `wsl_startup_hook.sh` on every interactive shell, **cwd-pinned to $FABRIK_ROOT** — unpinned it installed nothing from `$HOME` and would have written into whatever other repo the shell was in). The shim FAILS OPEN — a missing/unreadable script, no runnable interpreter, or a guard that CRASHES all exit 0. Only the dedicated reject status (9) blocks, so a sibling's half-written edit to the guard cannot make every commit in the repo fail. ⚠️ Never run `pre-commit install --hook-type commit-msg`: it takes this hook over and restores the doubled stash cycle (pinned by a test). ⚠️ Deliberately NOT a pre-commit stage: pre-commit's commit-msg stage runs a SECOND `staged_files_only()` stash/restore per commit on a tree three agents share — where a pre-commit stash has already reverted uncommitted work once — and adds a second site for the "Your pre-commit configuration is unstaged" abort. Both were observed live. Exists because prose provably failed: after CLAUDE.md's malformed example was fixed fleet-wide, the next 50 commits still parsed 0/50. Tests: `/opt/ai-model-catalog/engine/tests/test_commit_trailer_guard.py`. |

## Deeper documentation

- Behavioral view (what agents experience): `agent-command-routing-and-gates.md` §1, §5
- Config-surface view: `claude-configuration-inventory.md` §5
- Distribution mechanics: `docs/workflows/SYNC_ENFORCEMENT_WORKFLOW.md`
- Fullest single reference per hook: the hook file's own module docstring
- Sound hooks: `~/.claude/bin/claude-sound.sh` + its review receipt in `docs/development/reviews/`

<!-- BEGIN related-scripts: generated by scripts/render_doc_script_links.py — do not hand-edit -->
## Related scripts

Scripts that declare this document in their `# AFTER-EDIT:` header — editing one of them
means updating this page in the same change. This list is generated from those headers
(`python3 scripts/render_doc_script_links.py`); add the doc to a script's header, not here.

- `scripts/check_commit_trailers.py`
- `scripts/enforcement/check_citations_resolve.py`
- `scripts/enforcement/check_hooks_index.py`
- `scripts/scratch_sweep.py`
- `scripts/sysadmin/install_user_hooks.py`
- `scripts/sysadmin/quota_posture_hook.py`
- `scripts/sysadmin/selfwatch_check.py`
- `scripts/sysadmin/stop_mine.py`
- `scripts/sysadmin/user_hook_gate.py`
<!-- END related-scripts -->
