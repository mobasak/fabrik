# Review-family adoption of D-203 — the spec and plan reviews close on CONFIRMED, partition by section, and run delta rounds

Status: CONVERGED (2026-09-10 — 9 passes under D-212, closing pass confirmed 0; flip row D-215)
Profile: delta
Owner: infra (the unnamed hub window)
Date: 2026-09-10
Supersedes / extends: D-205's deliberate deferral (the review-family widening was "a backlog row, not this plan") — this is that row, `docs/STRATEGIC_BACKLOG.md:60`, specced.

## In one line

`/fabrik-spec-review` and `/fabrik-plan-review` (and every other consumer of the shared termination fragment `commands/_fragments/term-edit.md`) exit on a delta round that CONFIRMS zero defects by execution, read each section once per round under a model partition, re-review only the edited sections plus their cross-references after round 1, run the grep-shaped classes as a script instead of a seat, and escalate NON-CONVERGENCE as one operator question — the D-203 rules (D-206/D-207/D-208) the code-review loops already run, applied to the document loops, on the operator's bootstrap ruling of 2026-09-10 (D-212).

## Intake Inventory

The conversation is this run's denominator (post-compact, recovered from the transcript by `session-recall` and the jsonl: seq 285508 the post-mortem list, 285543 the go-line + the 6–8 h question, 285700 "go"). Every item is anchored to the operator's own words.

| I# | Item (anchored) | Disposition | Where |
|---|---|---|---|
| I1 | "Before pinning any delta: re-read every edited paragraph whole, grep the set for the old wording, count table pipes." | IN | § The delta D7 (pre-pin duty) + D6 (`stale-phrase`, `table-parity` at pin time) |
| I2 | "Run every flip gate at pass 1 on a flipped scratch copy. Three rounds." | IN | § The delta D7 (the flip gates at pass 1 and at close, on a flipped scratch copy) |
| I3 | "Tickets cite spec sections instead of re-narrating them. Halves the review surface and kills the drift class." | IN | § The delta D8 (`/fabrik-plan-after-chat` emit rule) |
| I4 | "A one-sentence delta gets one Opus seat, not three. That is the D-203 floor rule the plan itself ships." | IN | § The delta D3 (delta rounds sized by the fix) + D2 (the floor stands down under a partition, D-208) |
| I5 | "A spec gap surfaced in plan review is escalated as one question, not iterated." | IN | § The delta D5 (NON-CONVERGENCE re-keyed; a spec gap is ONE question) |
| I6 | "Same exit rule. CONVERGED = a delta round with zero CONFIRMED defects, where confirmed means the seat executed it (anchor read at the pinned SHA, count re-derived, gate line run). Refuted, plausible and wording-nit candidates are RECORDED, never counted, and never re-open the loop. … Same confirmed: counter on the plan's Pass Ledger row" | IN | § The delta D1 (the exit) + D4 (the verdicts) + the ledger row grammar |
| I7 | "Same partition. … Opus only on the tickets that touch fleet-synced grammars and gates (T01, T02, T10 here), Sonnet on the rest, every ticket read once per round." | IN | § The delta D2 |
| I8 | "The grep-shaped classes become the hygiene script, not a seat. … check_review_hygiene.py --surface <plan dir> with plan classes: old-wording survivors, table pipe parity, Behavior Contract roll-up equals the union of tickets, File Scope equals the union of Touches, Depends acyclic, ticket emit shape, every path:line resolving at the pinned SHA, the digest column shape." | IN — CHANGED (derived) | § The delta D6: 3 of the 8 named classes are NEW hygiene classes; 5 are ALREADY graded by `check_plan_tickets.py` / `check_plan_quality.py` / `check_rule_grounding.py` (cited there) and become "run the existing gates at pass 1 and at the close" (D7), never a second grader |
| I9 | "Delta rounds sized by the delta. A one-paragraph fix gets one seat, Opus if the paragraph is a rule or grammar, Sonnet if it is wording, plus the script. No three-seat floor. The delta is the fix diff plus one hop, meaning the tickets and spine sections that cite the edited sentence" | IN | § The delta D3 |
| I10 | "The executed critic as the rule, not my habit. A finder's candidate without an executed probe is PLAUSIBLE and RECORDED." | IN | § The delta D4 |
| I11 | "The NON-CONVERGENCE escalation, applied. Three non-decreasing delta rounds name the suspected foundation error and stop. … it becomes one question to you instead of five rounds of grammar design by review." | IN | § The delta D5 |
| I12 | "`/fabrik-spec` for the review-family adoption (spec-review and plan-review under D-203: the confirmed exit, section-scoped delta rounds, the hygiene script for spec and plan classes, executed-critic verdicts, NON-CONVERGENCE escalation)" | IN — CHANGED (derived): the two commands share ONE termination fragment with 11 other commands | § Personas (the other consumers), § What exists today row 1, § The delta D1 note on blast radius, § Decisions taken (approval decides) |
| I13 | "but at the end you will run this problematic /fabrik-spec-review command, ,and it will take 6-8 hours how can we avoid this?" (verbatim, the doubled comma kept) | IN | § Decisions taken D-212 (the bootstrap ruling) + § Validation V6 (this spec's own review is the first application) |
| I14 | "go" (2026-09-10) on the proposal "rule that D-203's exit, partition and delta rules bind the review family now (I mint the D-row in the spec commit)" | IN | § Decisions taken — D-212 minted in the DRAFT commit, before the in-turn review |
| I15 | (the same approved proposal) "the spec is a delta on two commands, one script and the fragment, so it runs as Profile: delta; and the plan that follows is small enough (roughly four tickets) for the inline execution profile" | IN | `Profile: delta` (line 4); § Cost (the plan shape) |
| I16 | Backlog rule (4), the filed destination of D-205's deferral: "executed evidence pinned once — a corpus leg's command and output stored beside the spec, re-executed only when the CLAIM changed" | IN | § The delta D3 (unchanged claims are never re-executed or re-fetched) |
| I17 | Backlog rule (5): "review edits CORRECT, never EXTEND — a finding that asks for new content is a plan-time item, not a spec edit" | IN | § The delta D5 |
| I18 | "i never open new window, i compact, you use session recall if needed we continue." | OUT-OF-SCOPE — a working preference, not a review rule | memory `feedback_never_new_window_compact_and_recall.md` (written 2026-09-10) |
| I19 | "please use session-recall to write them down" (the hand-off memory) | OUT-OF-SCOPE — done before this run | memory `project_review_family_adoption_handoff.md` (written and corrected 2026-09-10) |

Intake: 19 items — 17 IN (two CHANGED by derivation, I8 and I12), 2 OUT-OF-SCOPE (each named above), 0 ASK.

## Personas

- **PRIMARY — the operator**, in their own words: *"currently our reviews are taking too much time, even days, and we cant work proper work efficiently"* (2026-09-08, the D-203 turn) and *"it will take 6-8 hours how can we avoid this?"* (2026-09-10). Minimal loop, counted — the STEP BUDGET is **5** and this delta does not change it, it changes the wait inside steps 1 and 4: (1) invoke `/fabrik-spec <brief>` · (2) answer at most one batched question · (3) read the ask ↔ spec table and approve · (4) invoke `/fabrik-plan-after-chat <spec>` (its `/fabrik-plan-review` runs itself) · (5) give the go-word to `/fabrik-execute-plan`.
- **The reviewing session** (Fable, or Opus by name when Fable refuses): pins the artifact once, runs the hygiene script at every pin and the flip gates at pass 1 and at the close, dispatches the partition, EXECUTES every candidate and every refutation, sizes each delta round to the fix, writes the Pass Ledger row with `confirmed:`, records `round --confirmed` on the run record, and holds the pen for every D-row (subagents never mint).
- **The seats**: one Opus `fabrik-reviewer` on the rule/grammar sections (a spec's delta, decisions, contract and validation sections; a plan set's tickets touching fleet-synced grammars and gates plus the spine's contract sections), one Sonnet `fabrik-reviewer` on every other section, `fabrik-researcher` seats only for external facts the artifact cites (Opus for the authoritative highest-risk fact, Sonnet breadth — the grounding floor of D-207 stays for grounding surfaces). Read-only; they propose, never adjudicate.
- **The graders** (automated consumers): `scripts/enforcement/check_spec_convergence.py` (heading floor + the ≥2-URL approach floor, unchanged), `check_convergence.py` (the plan flip — gains the closing-row `confirmed: 0` rule, D1), `check_plan_tickets.py` / `check_plan_quality.py` / `check_rule_grounding.py` (the plan-set contracts, unchanged, run at pass 1 and at the close), `check_citations_resolve.py` (the anchor check, unchanged, run at every pin — not a flip gate), `check_review_hygiene.py` (gains the spec/plan classes, D6), `scripts/command_run.py` (the TERMINAL verdict on `round --confirmed 0` after ≥2 rounds — landed by D-211, unchanged) and the Stop hook that reads the record.
- **The renderer and the corpus**: `commands/assemble_commands.py` substitutes `term-edit` into 13 commands and `subagents-core` into every command; a fragment edit re-renders all of them box-wide (render → `--check` → commit from the main checkout). The 11 other `term-edit` consumers (`/fabrik-data-contract`, `-deploy-checklist`, `-deploy-plan-review`, `-doc-converge`, `-features`, `-flows`, `-flows-review`, `-rivals`, `-ui-design`, `-ui-design-review`, `-workflow-review`) receive the exit, verdict, breaker and delta rules through the fragment; their seat shape stays units-sized unless they partition (D-208's rule, restated in the fragment).
- **The ~46 project sessions**: run these commands from the rendered corpus and the synced `scripts/enforcement/`; their in-flight plan spines with `edits: 0` ledgers keep grading (D1's rule fires only when a ledger row carries `confirmed:`). One project already adopted the rules ahead of the sync by its own ruling (trade-intelligence D-007, 2026-09-08, "supersedes" the hub's D-048 from their ledger) — the bootstrap shape D-212 sanctions, and a cross-repo pointer `scripts/decisions.py --check` reads as DANGLING (a checker limitation, filed in the backlog at the flip).
- **The plan emitter and its consumer**: `/fabrik-plan-after-chat` writes tickets that CITE the spec section they implement (D8); `/fabrik-execute-plan` coders read the cited section from the spec, not a re-narration.
- **infra**: owns the corpus, the fragment, the hygiene graduation measurement (advisory until <5 % false positives over 20 receipts, DD6 of the redesign spec) and the first-three-reviews measurement (§ Lifecycle).

## Goal

The document review loops close when nothing CONFIRMED remains, not when nobody edited; they read each section once per round; after round 1 they re-read only what changed and what cites it; the grep-shaped checks run as a script before any seat reads the artifact; and a stuck loop names its foundation error as one question. Measured target: a spec review of a delta-profile spec in 3–5 rounds and about an hour, against 44 rounds / 451 minutes on 2026-09-08 (§ Why this exists).

## Why this exists

- The redesign spec's own review (`/fabrik-spec-review`, 2026-09-08/09) ran **44 rounds over 451 minutes** (`run_open` → `run_close` on the record; D-204, D-205), three seats each; the run record's series (`~/.claude/state/events/dd3c06d1-…jsonl`, `round` events, command `fabrik-spec-review`): 26 · 14 · 23 · 19 · 13 · 18 · 11 · 9 · 12 · 8 · 11 · 14 · 7 · 5 · 4 · 5 · 5 · 4 · 4 · 4 · 1 · 2 · 3 · 3 · 5 · 4 · 3 · 2 · 4 · 3 · 2 · 3 · 2 · 3 | 4 · 3 · 5 · 5 · 4 · 2 · 3 · 2 · 4 · 0. Rounds 9–43 changed 1–6 lines each, almost all in one paragraph, and every one of them paid three seats re-reading 320–348 lines (D-204; `docs/STRATEGIC_BACKLOG.md:60`).
- The plan review of the same work (`/fabrik-plan-review`, 2026-09-09) closed in **13 rounds** (40 · 34 · 27 · 12 · 5 · 2 · 6 · 2 · 2 · 3 · 6 · 4 · 0) only because the reviewer chose to run passes 5–13 as delta rounds against the command's text (D-209); four findings arrived twice from overlapping seats and were adjudicated twice; the "coherence + gates" seat dispatched every round confirmed three defects in nine rounds, all grep-shaped (a table row in the wrong shape, unescaped pipes, a stale count); eight of the thirteen rounds were the orchestrator's own fix residue in one clause.
- Three sentences cause it, all in text the two commands share or restate: the exit "a full, demonstrably-thorough round makes ZERO edits" (`commands/_fragments/term-edit.md:3`, restated at `fabrik-spec-review.md:223-224`, `:254` and `fabrik-plan-review.md:240-241`, `:306`); the seat rule "THREE seats minimum on different angles even when the artifact has fewer than three units" on every round including a scoped one (`term-edit.md:3`, `fabrik-spec-review.md:214`, `fabrik-plan-review.md:232`) plus "≥1 native `fabrik-researcher` on Opus" every round (`fabrik-spec-review.md:215`); and the quiet rule "the final pass must also have raised zero candidates" where a refuted FRESH candidate still counts (`term-edit.md:20`, citing D-048 — superseded for the code loops by D-206 and still live here).
- What the code loops already got (D-206/D-207/D-208, built and validated by D-211) is exactly the missing half: `confirmed:` as the exit counter, RECORDED/REFUTED never reopening, a partition instead of overlapping seats, delta rounds, the hygiene script, the run record's TERMINAL on `--confirmed 0`. D-205 kept those landing sites to the code loops on purpose; the operator's 2026-09-10 "go" on the bootstrap proposal is the ruling that the family follows now (D-212).

## What exists today (grounded — `path:line`, re-read 2026-09-10 at 5721b699)

| # | Surface | What it says today | Line(s) |
|---|---|---|---|
| 1 | `commands/_fragments/term-edit.md` — the SHARED termination contract, substituted by `commands/assemble_commands.py:143-183` into 13 commands (`grep -l 'include:term-edit' commands/_sources/*.md` → 13) | exit = "a full, demonstrably-thorough round makes ZERO edits"; pass SHAPE "1 wide + k scoped + 1 wide", middle passes "SCOPED to the EDITS the prior pass made plus the sections that cross-reference them", the CLOSING pass "a full fresh read"; "THREE seats minimum on different angles"; closing pass = `method: re-derivation`; "Minimum two passes, ALWAYS"; md5 anti-cheat; quiet = "raised zero candidates" with D-048's `Standing:` citation for re-raises; stall breaker on 3 non-decreasing nonzero `new:` → `## BLOCKED: NON-CONVERGENCE`; probe duty; the ledger `| Pass | axes | method | raised | new: | edits made | md5 |` | `:3`, `:16`, `:18`, `:20`, `:22-25` |
| 2 | `commands/_sources/fabrik-spec-review.md` | restates the zero-edits exit (`:6`, `:223-224`, `:254`); one seat per axis every round + "Never solo, never two" (`:213-214`); "**≥1 native `fabrik-researcher` on Opus** … mandatory floor" every round (`:215`); REFUTE before editing, no RECORDED form (`:216-220`); the only non-quiet stop is a BLOCKING unknown (`:258-260`); the approval gate and the ask ↔ spec table (`:262-295`, unchanged by this spec); the 1c floor audit at the flip (`:116-125`, unchanged) | 297 lines |
| 3 | `commands/_sources/fabrik-plan-review.md` | restates the zero-edits exit (`:8`, `:240-241`, `:306`); "Never solo, never two" (`:232`); the class sweep + `check_convergence.py` at the flip (`:316-318`); the plan-set md5 and `check_plan_tickets` precondition (`:32`, `:39`) | 358 lines |
| 4 | `commands/_fragments/subagents-core.md:3` | "The two PARTITIONED review loops — `/fabrik-review` and `/fabrik-repo-review` (D-207 …) — cut the surface into DISJOINT slices by FILE … Under a partition the three-seat floor stands down"; "Every OTHER command is UNITS-sized" with the three-seat floor; `dispatch_headroom.py --slices` for a partition | one paragraph |
| 5 | `commands/assemble_commands.py:429-465` | the per-command parameters of `term-edit` (`ARTIFACT`, `DONE_ACT`, `AXES`, `EXEMPT_NOTE`; spec-review `:429-435`, plan-review `:446-452`) and of `subagents-core` (`FLOOR` = `_floor("review", …)` for spec-review at `:441`; `_floor("grounding", …)` for plan-review at `:461`, guarded by the comment at `:459-460` "a grounder is a judgement") | — |
| 6 | `.windsurf/rules/core/62-using-subagents.md:184-190`, `:207` | § Role separation: "author's own quiet round never closes a review loop"; the RO grounders row names `/fabrik-spec`, `-spec-review`, `-plan-review` | fleet-synced |
| 7 | `CLAUDE.md:358` and `templates/governance/CLAUDE.md:359` | the fan-out bullet: "The two partitioned review loops (`/fabrik-review`, `/fabrik-repo-review`; D-207 …)" and "Every OTHER command is UNITS-sized" | fleet-synced |
| 8 | `scripts/enforcement/check_review_hygiene.py` | classes `template-residue`, `fence-parity`, `raw-pipe` (receipt tables only, `_receipt_hits` `:309`), `dual-verdict`, `changelog-entry`, `dead-symbol`, `stale-phrase` (`:1-45`); `--surface` md gets residue + fence + phrase (`_surface_hits` `:410-438`); advisory, always exits 0 (`:619-679`); 36 tests in `tests/enforcement/test_check_review_hygiene.py` (664 lines) | 679 lines |
| 9 | `scripts/enforcement/check_convergence.py` | the plan flip refuses a CONVERGED spine without a `method: re-derivation` Pass row (`_REDERIVATION_ROW` `:118-126`, `:561-563`); `QUIET_PASS` is the receipt pair, both grammars (`:187-230`); it does not read a spine's `edits:`/`raised:`/`confirmed:` counters | — |
| 10 | `scripts/enforcement/check_spec_convergence.py` | the three heading regexes (`:87-89`) and `FLOOR_MIN_URLS = 2` (`:79`); it does not read a Pass Ledger (0 hits for `Pass Ledger`) | — |
| 11 | `scripts/enforcement/check_plan_tickets.py`, `check_plan_quality.py`, `check_rule_grounding.py` | already graded plan classes: Depends acyclic (`_find_cycle` `:878`, `:1654`), Behavior Contract roll-up = union of ticket rows (`:19`, `:2303`), Touches ⊆ File Scope (`:25`, `:1918`), the ticket field contract and section pillars (`check_plan_quality.py:1-25`), digest quote integrity + MATCHED-pack completeness (`check_rule_grounding.py:1-30`); `path:line` anchors that LAND — `check_citations_resolve.py:1-25` grades BEYOND-EOF and BLANK-TARGET over specs, plans, reviews and reference docs, advisory, and by measurement does NOT grade a missing path | — |
| 12 | `scripts/command_run.py` + `docs/reference/command-run-protocol.md:55`, `:119-124` | `round --confirmed <N>`; TERMINAL fires on `confirmed == 0` after ≥2 rounds, adoption sticky per record (D-211) — already usable by every command | landed |
| 13 | `commands/_sources/fabrik-plan-after-chat.md:14-21`, `:36-45`, `:418-421` | the spec "IS the grounded source of truth"; "What we already agreed" is EXTRACTED from the spec; the ticket skeleton's `## Scope` is "one paragraph of the WHAT" — nothing says CITE the spec section rather than restate it | — |
| 14 | Fleet denominators — re-derive at run time, never cite: `ls /opt/*/docs/development/plans/*/*.md /opt/*/docs/development/plans/*.md \| wc -l`; `grep -l '^## Pass Ledger' <those>`; a COUNTER row = a Pass-headed table row (`^\| *Pass`) carrying `confirmed: <digits>` OUTSIDE code spans and fences (prose and quoted fixtures do not count — the redesign spine's Pass 4 cell quotes ``a fifth `confirmed: 3` row`` and its closing row reads `confirmed 0` without a colon: neither is a counter); the same over `/opt/*/docs/superpowers/specs/*.md` | plan md files 693, of which 56 carry `## Pass Ledger`; spec files 214 (this spec is the 214th), of which 19 carry a Pass Ledger heading (`-i`; 18 case-sensitive); counter rows by the masked definition: 0 of 907 files (executed 2026-09-10 12:50) — the three sessions and the daily pipeline move the first two numbers within a day, which is why the row states its commands | measured 2026-09-10 |

Profile trigger check: every intake item maps to code or text that exists today (rows 1–13); the delta is a change to an existing engine — `Profile: delta` holds.

## The delta

**D1 — The exit (the fragment, once).** `term-edit.md:3` and `:20` change from "ZERO edits / raised zero candidates" to the D-206 rule: a round is QUIET when its partitioned pass CONFIRMED zero defects — confirmed by EXECUTION (for a document claim: a pinned read quoted beside the sentence, a count re-derived from its primary source, a `path:line` resolved at the pinned SHA, a gate line run, a URL status probed by the orchestrator) — and the md5 anti-cheat stays as the second half: a round that confirmed zero and still edited the artifact is a relabelling, not quiet. The ledger row becomes `| Pass N | <seats by model> | method: re-derivation — <scope> | raised R | new: N | confirmed: C | fixed: X | edits: E | md5 start → end |` (a non-closing pass writes `method: citation — <scope>` in the same single cell) — the D-206 counter names; the Method cell LEADS with the literal token `method: re-derivation` and puts the scope after it (`method: re-derivation — delta over D1, D3 + one hop`), because `check_convergence.py`'s `_REDERIVATION_ROW` (`:125-126`) matches `method\W{0,4}…re-?deriv` and `method: delta re-derivation` never matches it (executed: True / False / True for the three spellings); `unexecuted:` is prose in the Method cell for the document loops, because `check_review_coverage.py`'s token rule binds only `docs/development/reviews/`. The three restatements (`fabrik-spec-review.md:6`, `:223-224`, `:254`; `fabrik-plan-review.md:8`, `:240-241`, `:306`) are deleted in favour of the fragment's sentence — one source of truth (CLAUDE.md § READ BEFORE YOU EDIT). Grader: the run record's TERMINAL verdict is the machine check for every consumer (`round --confirmed` per pass, `done` only at TERMINAL — D-211 (1)–(3)); for PLAN spines, whose ledger persists, `check_convergence.py` gains one rule at the CONVERGED flip: when any Pass-headed row carries a `confirmed: <digits>` COUNTER — matched after the gate's own fence strip and with code spans masked, so a quoted fixture in a cell (the redesign spine's Pass 4: ``a fifth `confirmed: 3` row``) and a colon-less `confirmed 0` are NOT counters — the LAST Pass-headed row must read `confirmed: 0` (adoption is a property of the whole ledger, mirroring D-211 (1)); a ledger without a counter row keeps today's checks — by that definition 0 of 907 plan and spec files fleet-wide carry a counter row today (§ What exists today row 14), so no existing spine's flip changes (§ Validation V2). No spec-ledger grader now: 0 of 213 specs persist the counter, and a detector with nothing to fire on is wallpaper (THE FIX DIRECTIVE 5) — backlog after the first 20 spec reviews under the new text. Blast radius, stated: the fragment is one declaration for 13 commands; a parameterised second exit rule for the other 11 is the two-truths defect (§ Rejected alternatives 1) — the approval gate decides the widening (§ Decisions taken).

**D2 — The partition (spec-review, plan-review; the fragment's rule for the rest).** Round 1 is one combined pass with DISJOINT slices: one Opus `fabrik-reviewer` on the rule/grammar sections — a spec's delta, decisions, contracts, validation and grounding tables; a plan set's tickets that touch fleet-synced grammars or gates plus the spine's Interfaces, Behavior Contract, Global Constraints and Execution Discipline — one Sonnet `fabrik-reviewer` on every other section or ticket, and `fabrik-researcher` seats only for the external facts the artifact actually cites (the grounding floor of D-207 stays for that grounding surface: Opus authoritative on the highest-risk fact, Sonnet breadth, `--mechanical 0`). No section's text is read by two seats; the union IS the pass. `fabrik-spec-review.md:213-220` and `fabrik-plan-review.md:232` are rewritten to that shape; the mandatory every-round Opus grounding seat (`:215`) becomes "when a cited fact changed". The budget is `dispatch_headroom.py --slices opus=N,sonnet=N` (+ the researcher seats as their own slices) → `command_run.py dispatch --seats` → the seats in ONE message; under a partition the three-seat floor stands down (D-208, extended to the `term-edit` consumers by the approval rows) — the fragment says so in one sentence, and a consumer that still dispatches units-sized grounding seats (rivals, flows-review, data-contract …) keeps its own floor. `subagents-core.md:3`'s "The two PARTITIONED review loops" widens to "the partitioned review loops — `/fabrik-review`, `/fabrik-repo-review`, and the `term-edit` family's `/fabrik-spec-review` and `/fabrik-plan-review` (D-212)"; the `FLOOR` strings for the two commands in `assemble_commands.py` — `:441` (`_floor("review", …)` for spec-review) and `:461` (`_floor("grounding", …)` for plan-review, outside the `:429-451` block a reader would stop at) — change to the partition sentence; `CLAUDE.md:358` / `templates/governance/CLAUDE.md:359` and `core/62:184-207` carry the same one-line widening (fleet-synced → the next forced sync).

**D3 — Delta rounds sized by the fix.** `term-edit.md:3`'s pass SHAPE becomes: round 1 is the only full pass; every later round is a DELTA round over the previous round's fix diff plus one hop — the sections whose cross-reference tokens cite the edited section (`I#`, `D#`, `G#`, `T##`, a `§` heading name, a `path:line`) — under the same partition rule (a rule/grammar paragraph → one Opus seat, wording → one Sonnet seat) plus the hygiene script; the CLOSING round is the delta round that carried a fresh non-authoring seat, re-derived (`method: re-derivation — delta over …`) every claim inside the delta and confirmed zero — never a full fresh read (D-203 R5, D-207; measured on the plan review: passes 5–13 closed as delta rounds). Its `round` call RE-DECLARES every class of the record's ledger as swept (`--classes-swept <all known>`), citing the last full pass for the classes its delta did not touch. That re-declaration is the REVIEW's discipline, not the tool's check: `command_run.py`'s TERMINAL verdict needs the record's persistent class ledger to hold no open class and the closing round to have swept at least one (`open_c` at `:367`, `swept_all` at `:383`, `quiet` at `:390`, `terminal` at `:391`; the banners at `:394-420`, whose `:416-418` text already asks that "its receipt cites the standing-clean classes"), so a delta round that sweeps one class while the rest stand clean from round 1 also reads TERMINAL (probed: a delta round that declares NO class prints no verdict) — the receipt's Method cell is where the standing-clean classes are named, so a reader can see what the closing round did not re-read. An unchanged claim is never re-executed and an unchanged URL never re-fetched: a corpus leg's command and output are pinned once beside the claim and re-run only when the claim's text changed (backlog rule 4); the first full pass still re-verifies every cited fact live (D-153 (3)'s "full re-verification" survives; its "full closing re-derivation" is narrowed to the delta — § Decisions taken). A sibling commit landing on a plan set's surface mid-review joins the next delta.

**D4 — Executed-critic verdicts.** The document loops adopt the D-206 verdict family: a seat's candidate is CONFIRMED only when the orchestrator executed it; otherwise `RECORDED — unexecuted (<why>)`, `RECORDED — by design (D-nnn)` / `(In, round k)`, `RECORDED — measured (<why>)`, `RECORDED — hygiene false positive (<why>)`; REFUTED carries the executed disproof (the quoted line, the re-derived count, the status code); wording nits are RECORDED, never edited in a closing round. D-048's `Standing:` line under the ledger (`term-edit.md:20`) is retired in favour of RECORDED rows in the artifact's own ledger (D-206 retired it for the receipts; the approval row retires it here). Seats PROPOSE with a concrete failure scenario and the command that would prove it; the orchestrator runs it.

**D5 — NON-CONVERGENCE re-keyed, and edits correct, never extend.** The breaker at `term-edit.md:20` is keyed on CONFIRMED instead of `new:`: three consecutive delta rounds with non-decreasing, nonzero `confirmed:` → `## BLOCKED: NON-CONVERGENCE` naming the suspected foundation error, and the foundation error is delivered as ONE operator question with a recommended answer (the question bar), never as grammar designed by review (measured: plan-review rounds 10–12 ran 3 → 6 → 4 on a spec gap, V5's owner grammar, that one question would have closed). A finding that asks for NEW content — a mechanism the spec never chose, a section the plan never had — is a plan-time or spec-time item routed as that question or a backlog row; a review edit CORRECTS (backlog rule 5; `/fabrik-spec-review`'s D7 grew 10.6k → 23.3k characters under review). `check_convergence.py`'s NON-CONVERGENCE acceptance is unchanged (a spine may stop `Status: BLOCKED`).

**D6 — The hygiene script learns the spec and plan classes (extend, never a sibling).** `scripts/enforcement/check_review_hygiene.py` gains, on `--surface`: `table-parity` — a markdown table row whose cell count differs from its header's, on ANY `.md` surface: the cell-count logic at `_receipt_hits` `:334` becomes a helper CALLED by both `_receipt_hits` (class `raw-pipe`, receipts, unchanged — the 36 existing tests keep passing) and `_surface_hits` (`:410`, class `table-parity`) — a shared helper, never a move; a precision fix to `template-residue`: `TEMPLATE_RESIDUE` (`:57`) narrows to the fragment-parameter shape (`include:<name>` or an `UPPER_CASE` parameter between double braces) so a Docker Go template in a command's example (`.Image` between double braces, `fabrik-deploy-verify.md:227`, the one standing false positive over the 36 rendered commands, executed 2026-09-10) stops firing; and the already-applicable `fence-parity` and `stale-phrase` (`--phrase` from the round's brief: the old wording of every edited sentence). NOT a hygiene class: `path:line` anchors — `scripts/enforcement/check_citations_resolve.py` already grades BEYOND-EOF and BLANK-TARGET over specs, plans and reviews and measured-and-rejected the path-does-not-exist half as wallpaper (its docstring `:17-19`; the bare run today: 143 citations do not land of 1,457 examined across 561 docs — a number that moves with the 30-day window, stated with its invocation), so the anchor check at pin time IS that script (D7), and the two things it lacks — a `--pin <dir>` root and bare-basename resolution against `git ls-files` (this DRAFT: 79 anchors, 38 bare basenames, 0 unresolved once resolved) — are an EXTEND filed as a backlog row, never a second grader. The intake-count check (I# rows vs the operator's turns), the heading floor, the URL status codes and the digest column shape are not classes either: the first is a judgement (the A0 seat), the second and fourth are graded already (`check_spec_convergence.py:87-89`, `check_rule_grounding.py`, `check_plan_quality.py`), the third needs the network and stays the orchestrator's `curl -sSI` duty (an enforcement script that reaches the network reds a gate offline). The five plan classes I8 names that the plan gates already own are not re-implemented. Fire rates are measured before the plan lands (§ Validation V5); the script stays advisory (`warn_only=True`, exits 0) and graduates under the redesign spec's DD6 rule — no blocking mode.

**D7 — Before Pass 1 and before every pin (the fragment).** Mirroring `term-coverage.md:5-8`'s three obligations, `term-edit` gains a "Before Pass 1" block: (1) PIN the artifact once (`cp` to scratch, md5 in every brief), and never touch the pin while seats run; (2) run `check_review_hygiene.py --surface <pin> --phrase <old wording…>` on the pin and `check_citations_resolve.py --changed` in the REAL tree (the working-tree artifact is dirty during a review, so `--changed` grades it against the real repo paths; at a scratch root every repo path is missing and ungraded and the script prints a green tick over 0 examined anchors, executed 2026-09-10 — the `--pin <dir>` root is the backlog EXTEND), then the artifact's flip gates at pass 1 and again at the close, so a flip never discovers a gate red. HOW each flip gate is invoked so that the artifact is provably in its examined set — which flag, which tree, tracked-and-uncommitted or not, and for a gate that prints nothing on success the target list derived from its own discovery code — is NOT written here: it is the adoption plan's per-gate invocation matrix, one row per flip gate with a red-first check that a pinned artifact appears in the gate's examined set (§ Cost, the Plan-shape bullet). This review found by execution four invocation shapes that grade nothing and return green (named in § Machinery report), and every attempt to carry the recipe in the spec re-opened the loop — the D5 shape, resolved by moving the mechanics to where they are executed and tested; the working-tree artifact is never flipped for a gate; (3) before pinning any delta round: re-read every edited paragraph WHOLE, grep the artifact for the old wording, count the edited tables' pipes — the orchestrator's residue is caught by the script, never by a seat (measured: 8 of 13 plan-review rounds and 35 of 44 spec-review rounds were fix residue).

**D8 — Tickets cite the spec, never re-narrate it.** `fabrik-plan-after-chat.md:14-21` and the ticket skeleton at `:418-421` gain one rule: when a `/fabrik-spec` design doc feeds the plan, a ticket's `## Scope` and `## Behavior Contract` rows CITE the spec section they implement (`spec § <heading>` or `<spec path>:<line>`) and restate nothing the section already says; "What we already agreed" (`:36-45`) is a list of citations; the plan review grades the cite → section correspondence as an anchor (`check_citations_resolve.py` covers the `path:line` form at pin time, D7). No new grader: measured after the first five spec-fed plans under the rule (backlog row), because a WARN on "Scope longer than N characters without a `§`" has no measured fire rate yet (THE FIX DIRECTIVE 5).

**D9 — The bootstrap and the honest receipt.** D-212 (§ Decisions taken) binds the family from 2026-09-10; until the corpus text lands, a review run under it says so in its Pass Ledger's Method cells ("under D-212; the fragment text lags") — the same gap the D-191 receipt lived in for a day (D-211). This spec's own in-turn review is the first application (§ Validation V6, § Machinery report).

## Chosen approach (and why it is the lean one)

Change the ONE fragment and delete the restatements, extend the ONE hygiene script (one class and one precision fix), add ONE grader rule where a ledger persists (plan spines) and none where it does not (specs — the run record already grades those), widen ONE sentence in `subagents-core.md` / the two `CLAUDE.md` files / `core/62`, and add ONE emit rule to `/fabrik-plan-after-chat`. Every rule cites D-203/D-206/D-207/D-208/D-211 and the landed mechanics instead of re-narrating them, which is what keeps the corpus diff to roughly 60 lines of prose. The external practice (§ Grounding): incremental re-review of documents is the norm in code tooling (G1, G2) and in older document disciplines (G3 RFC 7735, G4 Wiley); an unreproduced finding is dropped or channelled, never counted (G5–G8), and skipping execution is where false positives live (G9: 50 % vs 8.6 %); overlapping unconstrained reviewers score below a single one while a partition by domain/risk is the production shape (G10–G13); a progress-based stall detector is the named replacement for a hard cap (G16–G18), which is D-203 R6's exact choice.

## Rejected alternatives

1. **Parameterise `term-edit` so only spec-review and plan-review get the new exit** — two exit rules in one fragment is the two-sources-of-truth defect (CLAUDE.md § READ BEFORE YOU EDIT); the other 11 consumers already share the rule and the breaker, so they share the fix.
2. **A hand-inlined copy of the new contract in the two sources** — the fragment exists because `assemble_commands.py:340` records a copy that "had drifted behind the fragment".
3. **A round cap (1–2 CI / 3–5 interactive, G15)** — rejected by D-203 R6; the stall breaker on CONFIRMED replaces it (G16–G18).
4. **Majority vote across passes ("flagged in only one pass is noise", G15)** — rejected in the redesign spec; the executed critic decides, not a count of seats.
5. **Keep the closing full fresh read as the recall mechanism** — measured: 35 of 44 spec-review passes changed ≤6 lines and re-read 348; the closing delta round with a fresh non-authoring seat plus the script is D-207's rule; the loss (a never-edited section read once by one seat) is the trade D-203 R3/R5 made.
6. **A spec-ledger grader now** — 0 of 213 specs persist a `confirmed:` row; the run record's TERMINAL verdict grades the series; revisit after 20 reviews.
7. **A separate `check_spec_shape.py`** (the backlog row's name) — duplicates the hygiene script's expand/scan/notes machinery and the existing plan gates; extend, don't duplicate (CLAUDE.md § Before new scripts).
8. **URL status probes inside the hygiene script** — an enforcement script that needs the network fails offline gates across ~46 repos; the probe stays the orchestrator's curl duty (`fabrik-spec-review.md:99-106`).
9. **Restore the three-seat floor for delta rounds** — D-208 retired it under a partition; a one-paragraph delta with three seats is the 2.5× overspend D-190 prices.
10. **A `Scope`-length WARN in `check_plan_tickets.py` for D8** — no measured fire rate; corpus text first.
11. **Drop an unreproduced candidate without a trace (G5's rule)** — a RECORDED row with its reason is what the next delta round, the plan and the hygiene-graduation count read; dropping loses the recall-cost signal the source itself says to route to a "suspected-but-not-reproduced" channel.
12. **A second anchor grader (`anchor-unresolved` in the hygiene script)** — `check_citations_resolve.py` already grades anchors that land and measured the missing-path half as wallpaper (500+ of 1,600 hub citations point at another repo's files); extend it with `--pin` and basename resolution (backlog), never duplicate it.

## Contract deltas

None in `docs/data-contract.md` / `docs/ui-design.md` (no data or GUI surface). Corpus contracts that change: the `term-edit` ledger row grammar (additive counters; the Method cell keeps the leading `method: re-derivation` token so `check_convergence.py`'s `_REDERIVATION_ROW` still matches — D1); `check_review_hygiene.py`'s JSON `hits[].cls` gains one class name (`table-parity`, additive; `raw-pipe` on receipts unchanged) and `template-residue` fires on fewer strings (the narrowing — a receipt that was RECORDED as a hygiene false positive for the Go-template form stops hitting); `check_convergence.py`'s plan flip gains one refusal (only when a counter row is present).

## Cost

- Corpus: `term-edit.md` ~25 → ~32 lines; `fabrik-spec-review.md` −~12 / +~10 lines at `:6`, `:213-224`, `:254`; `fabrik-plan-review.md` −~8 / +~6 at `:8`, `:232`, `:240-241`, `:306`; `subagents-core.md:3` one clause; `assemble_commands.py:441` and `:461` (the two `FLOOR` strings); `fabrik-plan-after-chat.md` +~6 lines; rendered box-wide from the main checkout (render → `--check` → `check_command_corpus.py` → commit).
- Code: `check_review_hygiene.py` +~40 lines (the shared cell-count helper + `table-parity`, the `TEMPLATE_RESIDUE` narrowing) with +~60 test lines (red-first fixtures: a 4-cell row under a 3-cell header on a `.md` surface; a receipt row still firing `raw-pipe`; the Go-template form no longer a hit, the `include:` and `UPPER_CASE` forms still hits); `check_convergence.py` +~15 lines with +~40 test lines (a spine whose last Pass row reads `confirmed: 2` is refused; `edits: 0` ledgers pass); one new test file, `tests/enforcement/test_flip_gate_matrix.py` (+~40 lines: one parametrised test over the five matrix rows), holding the per-gate invocation matrix's red-first rows (§ Cost, the Plan-shape bullet).
- Fleet-synced text: `core/62`, both `CLAUDE.md` files — ride the next forced sync (the review-convergence plan's held sync is a SEPARATE thread and is not folded in).
- Docs: `docs/workflows/FINAL_GATE_WORKFLOW.md` (`table-parity`, the residue narrowing, and the flip-gate invocation matrix as its documented form), `CHANGELOG.md`, `INDEX.md` (this spec), `docs/DECISIONS.md` (D-212 now; the approval rows later), `docs/STRATEGIC_BACKLOG.md:60` (closed by this spec; two follow-up rows: the spec-ledger grader after 20 reviews, the D8 WARN after 5 plans).
- Plan shape: `Profile: small` (D-169) — the CODE diff, tests excluded as D-169 and `fabrik-plan-after-chat.md:199` count it, is 3 files / ~55 lines (`check_review_hygiene.py` +~40, `check_convergence.py` +~15, two `FLOOR` strings in `assemble_commands.py`), inside the ≤ ~400 lines / ≤ 5 files bar; the tests are ~140 lines over three files (+~60, +~40, +~40), about 2.5× the code diff against D-169's ~1.5× guide — the plan states that overage and its reason (every changed rule is a grader, and graders are proven red-first), and if the plan gate refuses the profile on that ratio the plan runs as the same ≤3-phase monolith without the `Profile:` line; a ≤3-phase monolith (1 corpus + assembler + the flip-gate invocation matrix · 2 hygiene classes · 3 grader rule + docs + render + sync), `/fabrik-review-scoped` per phase, one heavy round at Finish. The flip-gate invocation matrix lives in phase 1 with the fragment it serves: one row per flip gate (`check_spec_convergence`, `check_rule_grounding`, `check_plan_tickets`, `check_convergence`, `check_plan_quality` through `validate_conventions`), each row written from the script's own target-selection code and proven by a red-first test in `tests/enforcement/test_flip_gate_matrix.py` that a pinned artifact appears in the gate's examined set — measured before writing: four of those invocations returned green over nothing in this review (§ Machinery report). The anchor check is not a flip gate: `check_citations_resolve.py --changed` runs at every pin (D7) and has no matrix row.
- Expected saving, to be measured (§ Lifecycle): a delta-profile spec review from 44 rounds / 451 min to 3–5 rounds / ~60 min; a plan review from 13 to ~6 rounds; seats per round from 3 (spec) / 9→3 (plan) to 2–3 in round 1 and 1–2 in a delta round.

## Validation

- **V1** — red-first tests for `table-parity` and the `template-residue` narrowing (fixtures above); a mutant that deletes each class's branch turns its test red (asserted on disk, per THE FIX DIRECTIVE 4).
- **V2** — `check_convergence.py`: the new refusal red-first on a synthetic CONVERGED spine whose last Pass row reads `confirmed: 2`; a spine with `edits: 0` rows and no counter passes; every spine with a `## Pass Ledger` under `/opt/*/docs/development/plans` (re-derived at run time; the receipt states the count) re-graded with 0 new refusals.
- **V3** — `python3 commands/assemble_commands.py --check` clean after the render; `check_command_corpus.py` green; `grep -c 'ZERO edits' ~/.claude/commands/fabrik-spec-review.md` → 0 and the same for plan-review; `check_review_hygiene.py --surface ~/.claude/commands/` reports 0 `template-residue` hits after the narrowing (today: 1, the Go template at `fabrik-deploy-verify.md:227` — the pre-change count is the red half).
- **V4** — `check_rule_grounding.py`'s MATCHED set for the plan's File Scope (core/10, 40, 45, 62) is quoted in the plan's digest; `check_spec_convergence.py --root .` green on this spec at the flip.
- **V5** — fire rates stated before the plan lands: `check_review_hygiene.py --surface` over the 213 specs and the 693 plan md files fleet-wide, hits per class, with the false-positive count from a hand-read sample of 20 hits per class (the redesign spec's DD6 shape).
- **V6** — this spec's own `/fabrik-spec-review`, run under D-212 (a pin per pass, never touched while its seats run; the hygiene script and the anchor check at every pin; `check_spec_convergence` on a flipped scratch copy at pass 1 and at the close; 1 Opus + 1 Sonnet by section + researcher seats only for cited facts; delta rounds; close on `round --confirmed 0 --classes-swept <every ledger class>` on the record — the re-declaration D3 mandates as discipline), is the first application. Command: a python filter over `~/.claude/state/events/<sid>.jsonl` for `event == "round"` and `command == "fabrik-spec-review"` dated 2026-09-10 → the `findings`/`confirmed` series, seats per round, and `run_open` → `run_close` wall-clock. Pass bar: rounds ≤ 5 and wall-clock ≤ 90 min against the 44 / 451 baseline; the series goes in § Machinery report.
- **V7** — the first three reviews under the landed text (one spec, two plans): the same events-log filter per record (rounds, seats per round via `dispatch`/`round --seats`, wall-clock) plus `grep -c 'RECORDED — hygiene false positive'` over their ledgers against `check_review_hygiene.py --json` hit counts. Pass bar: a spec review ≤ 8 rounds and ≤ 120 min; a plan review ≤ 8 rounds; the hygiene false-positive share < 5 % (DD6) — misses are findings against the fragment text, filed as backlog rows with the numbers.

## Decisions taken

- **D-212 (minted in this DRAFT's commit, 2026-09-10, operator — "go" on the bootstrap proposal):** D-203's exit, partition and delta rules bind the whole review family (every `term-edit` consumer) effective now; the corpus text follows in the adoption plan; a review run before the text lands says so in its ledger's Method cells. REVERSIBLE (command text). Supersedes, in part, D-153 (3): `/fabrik-spec-review` keeps its full FIRST-pass re-verification and the ≥2-URL approach floor; its "full closing re-derivation" becomes the closing delta round's re-derivation of the delta.
- **At approval (minted by the approving turn, per `/fabrik-spec-review` § After CONVERGED):** the spec's approval row; a row narrowing D-186/D-188 for the `term-edit` consumers under a partition (the D-208 shape); a row retiring D-048's `Standing:` citation in `term-edit` in favour of the RECORDED family (D-206's shape); and — if the operator does NOT accept the widening to all 13 consumers — a row recording the narrower landing and the parameterisation it forces (§ Rejected alternatives 1 becomes the design; this spec is edited before planning).
- **Not decided here:** the hygiene script's graduation (DD6 of the redesign spec stands); the spec-ledger grader and the D8 WARN (backlog rows with their measurement triggers).

## Lifecycle

Lands via the small plan; the first three reviews (V7) measure the saving and the hygiene false-positive rate; the two backlog rows fire on their counts (20 spec reviews; 5 spec-fed plans); degradation: a `TRIMMED partition` line from `dispatch_headroom.py` means the union is not the full pass and the ledger names the unread sections for the next delta (the code loops' rule, `subagents-core.md:3`); superseded only by a new D-row.

## Grounding (external facts and the approach floor — fetched 2026-09-10 by four native `fabrik-researcher` seats, one Opus; raw-arm fetches; every quote re-matched by the orchestrator on a `curl` raw fetch with whitespace normalised; every URL status-probed by the orchestrator: 18 × HTTP 200, the Medium page 403 and dropped)

| G# | Source | Fetched | Tool(s) | What it grounds |
|---|---|---|---|---|
| G1 | https://agentpatterns.ai/code-review/diff-based-review/ | 2026-09-10 | WebSearch → exa raw → curl | "Review what changed, not the full output — mistakes live in the delta, and diffs compress review effort to the right scope." → D3; the same page carries Graphite's caveat that diff-only review "may miss violations of global invariants" → why D3's delta includes the cross-referencing sections |
| G2 | https://mastra.ai/articles/ai-code-review-tools | 2026-09-10 | exa raw | "then reviews each later commit with an incremental pass focused on the update." → D3 (a shipping tool's per-update pass) |
| G3 | https://www.rfc-editor.org/rfc/rfc7735.html (IETF, Tracking Reviews of Documents) | 2026-09-10 | WebSearch → exa raw → curl | "SecDir usually performs a follow-up review, but the SecDir secretary may choose not to request that follow-up if any issues identified at Last Call are addressed and there are otherwise no major changes to the document." → D3 sized by the fix, in a document (not code) discipline |
| G4 | https://authors.wiley.com/Reviewers/journal-reviewers/how-to-perform-a-peer-review/reviewing-revised-manuscripts.html | 2026-09-10 | exa raw → curl | "The primary aim of reviewing a revised manuscript is to confirm that the changes requested during the initial review have been adequately addressed." → D3's delta round is the revised-manuscript review |
| G5 | https://agentpatterns.ai/code-review/reproduce-before-report-verification-gate/ | 2026-09-10 | WebSearch → exa raw → curl | "A reproduce-before-report verification gate drops any reviewer finding the verifier cannot reproduce against actual code behavior." · "Non-reproducing findings are dropped, not appended with a low-confidence flag." · "A pipeline that fans out reviewers but reports every candidate is fan-out: it amplifies false positives instead of filtering them." → D4's never-counted half; the source DROPS an unreproduced finding where D4 RECORDS it with its reason — a deliberate divergence, because a recorded row is what lets the next delta round or the plan pick it up (§ Rejected alternatives 11) |
| G6 | https://arxiv.org/abs/2604.19049 (Refute-or-Promote) | 2026-09-10 | WebSearch → exa raw → curl | "ten dedicated reviewers unanimously endorsed a non-existent Bleichenbacher padding oracle in OpenSSL's CMS module; it was killed only by a single empirical test, motivating the mandatory empirical gate." → D4: argument does not confirm, execution does |
| G7 | https://arxiv.org/abs/2608.18167 (Adversarial Review) | 2026-09-10 | exa raw | "outperforming a five-agent baseline while using only three agents"; "it requires that disagreement be minimal, structured, and evidence-grounded" → D2's seat economy, D4's evidence bar |
| G8 | https://cloud.google.com/blog/topics/threat-intelligence/staying-ahead-of-adversarial-ai-through-agentic-source-code-review (Mandiant, 2026-08-18) | 2026-09-10 | WebSearch → exa raw | "any findings that fail to pass this dynamic testing phase are discarded." → D4 in a production pipeline |
| G9 | https://arxiv.org/html/2511.16708 (CodeX-Verify; body) | 2026-09-10 | exa raw → curl | "Our 50% false positive rate is higher than test-based methods (8.6%)" — an execution-free verifier measures 50 % false positives against 8.6 % for a test-based method, a gap the paper itself attributes to its wider scope ("we flag security holes and quality issues that don't affect test outputs"), so it grounds D4's COST side, not a causal claim; abstract: "diminishing returns of +14.9pp, +13.5pp, and +11.2pp for agents 2, 3, and 4" → D2 |
| G10 | https://agentpatterns.ai/code-review/evidence-grounded-disagreement/ | 2026-09-10 | exa raw | "Two review agents told to converge tend to agree with each other; only requiring every objection to cite code makes the second reviewer worth it." (naive pair F1 0.457 < single 0.495; grounded pair 0.533) → D2: overlapping unconstrained seats are worse than one |
| G11 | https://www.codeant.ai/blogs/ai-code-review-false-positives | 2026-09-10 | exa raw | "For most teams, <10% should be your baseline 'acceptable' threshold. For high-throughput organizations, <5% is the optimal target." → DD6's 5 % graduation bar (carried) |
| G12 | https://blog.cloudflare.com/ai-code-review/ | 2026-09-10 | exa raw → curl (the orchestrator's raw fetch found BOTH sentences on the page after a seat reported the first absent) | "Instead of asking one model to review everything, we split the review into domain-specific agents." · "Rather than relying on one model with a massive, generic prompt, we launch up to seven specialised reviewers …" → D2's partition by domain |
| G13 | https://www.cortex.io/post/risk-based-code-review | 2026-09-10 | WebSearch → exa raw → curl | "ties review rigor to blast radius and reversibility" (Grabowski's 2026 framework, as quoted there) → D2: Opus on the rule/grammar sections, Sonnet on wording |
| G14 | https://agentpatterns.ai/code-review/review-then-implement-loop/ | 2026-09-10 | exa raw | "Cap automated fix attempts at one pass. If the coding agent's fix does not resolve the finding cleanly (tests fail, or new issues appear), escalate to human review rather than looping." → D5's escalation (adopted as the escalation, not as the cap — R6) |
| G15 | https://zylos.ai/research/2026-03-01-multi-model-ai-code-review-convergence/ | 2026-09-10 | exa raw | "Most practitioners recommend hard caps of 1-2 loops for automated CI/CD, with 3-5 rounds for interactive review sessions." · "issues flagged in only one pass are discarded as noise" → CONTRADICTS the no-cap rule; rejected by D-203 R6 (§ Rejected alternatives 3, 4) |
| G16 | https://arxiv.org/html/2607.24604v1 (Looping Is Not Reliability) | 2026-09-10 | exa raw | "Generate–test–revise loops are common in coding agents, but repetition alone provides no reliability guarantee." → D5: a round count is not a signal; confirmed progress is |
| G17 | https://praison.ai/docs/features/escalation-doom-loop | 2026-09-10 | WebSearch → exa raw → curl | a detector that "watches a long-running agent session for stuck states — repeated actions, repeated failures, no progress …" with human escalation as a rung → D5's progress-keyed breaker |
| G18 | https://github.com/langchain-ai/langchain/issues/36139 | 2026-09-10 | exa search highlights | "Add an optional progress-aware termination guard that detects when an agent is not making progress … and stops execution early." / "Iteration limits: stop late, not early" → D5: a stall detector is a distinct mechanism from a cap |

Search legs (real searches, 2026-09-10): 7 of the 18 rows (G1, G3, G5, G6, G8, G13, G17) were reached through a `WebSearch` leg, the Tool(s) column says which; the rest were re-verified fetches of sources cited on 2026-09-08 or follow-ups from those searches; the one Brave search (U3) only triaged the dropped Medium page and reaches no row; ≥2 distinct URLs from ≥2 tools (WebSearch, exa raw, curl) with search legs — the 1c floor is met. Behaviours R1–R11 are ruled (D-203) and grounded by their rows, not by literature (D-153 (1)). Dropped: the Medium termination-criteria page (HTTP 403 on every arm; not needed). Corrections to the redesign spec's citations reported by two seats were EXECUTED by the orchestrator and did not hold: the Cloudflare sentence is on the page and the "4.2%" figure is in the paper body — no edit to `2026-09-08-review-convergence-redesign-design.md`.

## fabrik-lib verdict

No module applies (`/opt/fabrik-lib/README.md` module table: `doc-crawl`, `docx-io`, `pdf-extract` are content extractors, not markdown-structure checks); BUILD is `table-parity` and the residue narrowing inside the hub's own synced enforcement script — not a fabrik-lib candidate (enforcement is hub-owned and synced, fails (d) "no existing module" in the sense that the hub script IS the home).

## Constraints digest

Computed MUST-READ set: FLOOR (core/35, core/25, core/30) + MATCHED by `review_rubric.py --changed` over the surfaces (core/10, core/40, core/45, core/62) — read in full 2026-09-10. The table's FIRST cell is the verbatim quote and the SECOND the `file:line`, because `check_rule_grounding.py`'s `_digest_rows` is column-positional (executed on the DRAFT's earlier `#`-first shape: it read `quote='C1' path='No'`; the 2026-09-09 plan review burned a pass on the same swap).

| Verbatim quote | Source | Applies to |
|---|---|---|
| "- **No skipped heading levels** — `##` to `###`, never `##` to `####`" | `.windsurf/rules/core/40-documentation.md:240` | the fragment and the two sources keep their heading depth; the hygiene classes do not police it (the doc gates do) |
| "- **Fenced code blocks only** — never indented code (AI treats it inconsistently)" | `.windsurf/rules/core/40-documentation.md:242` | `fence-parity` is the only fence rule; probe fences begin `$ ` per `term-edit.md:20` |
| "author's own quiet round never closes a review loop: the context that shaped the artifact is the one" | `.windsurf/rules/core/62-using-subagents.md:188` | D3's closing delta round carries a fresh non-authoring seat |
| "the solo self-convergence loop:** a command whose loop dispatches NO finders anywhere (an author" | `.windsurf/rules/core/62-using-subagents.md:190` | unchanged: the author-side commands (`/fabrik-ui-design`, `/fabrik-features`) keep their sanctioned solo loop under the fragment |
| "**`uv`** is the mandated Python package manager. Never use raw `pip`, `pip install`, `poetry`, or `pipenv`." | `.windsurf/rules/core/10-python.md:21` | the plan's test commands (`uv run pytest tests/enforcement/…`) |
| "**`datetime.now(UTC)`, never `datetime.utcnow()`** — deprecated and naive; naive datetimes" | `.windsurf/rules/core/10-python.md:219` | unconstrained — the hygiene classes carry no timestamps |
| "Ruff's selected rule-sets MUST include `ASYNC` (blocking IO in async code — machine-enforces" | `.windsurf/rules/core/10-python.md:245` | `ruff` clean on the two scripts at the gate |
| "**Behavior Contract**: every ticket enumerates its distinct **user-observable behaviors / acceptance criteria** and tests **each one**" | `.windsurf/rules/core/45-testing-strategy.md:19` | V1, V2: one test per class / per refusal |
| "**Watched-fail-first** (for tests this change adds or modifies; trivia stays skipped per the Behavior Contract)" | `.windsurf/rules/core/45-testing-strategy.md:21` | V1, V2 red-first, mutant asserted on disk |
| "All services deploy via `fabrik apply` (SSH + Docker Compose) on the `fabrik` network." | `.windsurf/rules/core/30-ops.md:107` | unconstrained — no service |
| "=> Mandate: config via env vars only (`os.getenv("KEY", "default")`); **ZERO secrets/constants in code**." | `.windsurf/rules/core/35-security-auth.md:266` | the hygiene script reads `PROJECT_ROOT` from the env as today (`:574-590`); no secret surface |
| "**"Own database" means a DATABASE on `postgres-main`, never a database SERVER.**" | `.windsurf/rules/core/25-data-postgres.md:25` | unconstrained — no database |

## Shape / infra implications

None: hub scripts, the command corpus and fleet-synced rule text; no `specs/services/*.yaml`, no `shape:` flag.

## Documentation landing sites

`commands/_fragments/term-edit.md` (the contract) · `commands/_sources/fabrik-spec-review.md`, `fabrik-plan-review.md`, `fabrik-plan-after-chat.md` · `commands/_fragments/subagents-core.md` + `commands/assemble_commands.py` (the FLOOR strings) · `.windsurf/rules/core/62-using-subagents.md` § Role separation (one sentence) · `CLAUDE.md` + `templates/governance/CLAUDE.md` fan-out bullet (one clause) · `docs/workflows/FINAL_GATE_WORKFLOW.md` (`table-parity`, the residue narrowing, the flip-gate invocation matrix — § Cost, the Plan-shape bullet) · `docs/reference/command-run-protocol.md` (no change; `round --confirmed` is generic) · `CHANGELOG.md`, `INDEX.md`, `docs/DECISIONS.md`, `docs/STRATEGIC_BACKLOG.md:60`.

## Open / blocking unknowns

- Resolved: the exit, partition, delta and verdict rules (D-203 → D-206/D-207/D-208; D-212 for the family); the grader placement (run record for every consumer, `check_convergence.py` for spines).
- Open, with a resolution step: the widening to all 13 `term-edit` consumers — recommended by derivation (§ Rejected alternatives 1), decided at the approval gate; the hygiene classes' fire rates (V5, at plan time); the expected saving (V6 now, V7 after landing).

## Machinery report

DRAFT hand-over: written 2026-09-10 from the run record's open at 12:22 local; four grounding seats (1 Opus, 3 Sonnet), 18 sources kept, 1 dropped (403); two seat-reported citation defects in the approved redesign spec refuted by the orchestrator's own raw fetch; the nine-problem validation of 2026-09-09 re-graded against the RENDERED command — two of its rows were over-stated (the fragment already scopes middle passes and carries a `new:`-keyed breaker), recorded in the hand-off memory before this DRAFT.

`/fabrik-spec-review` (nested record, opened 09:34:11 UTC 2026-09-10, under D-212): 69 min wall-clock, 9 passes — seats per pass 4/2/1/1/1/1/1/1/1 (pass 1: 1 Opus + 1 Sonnet `fabrik-reviewer` partitioned by section + 1 Opus + 1 Sonnet `fabrik-researcher` partitioned by source; later passes one or two fresh seats sized to the fix), every pass md5-pinned, the hygiene script and the flip gate run on every pin BEFORE dispatch, every candidate executed by the orchestrator; raised/confirmed series 18/14 → 14/3 → 10/1 → 8/2 → 6/2 → 9/5 → 10/5 → 5/2 → 6/0; round 1 confirmed 14 (four researcher-side cell over-reads, three reviewer-side count/quote drifts, eight rule-level defects incl. a counter rule a live spine would have tripped, a Method-cell spelling the flip gate cannot parse, a duplicate anchor grader, a column-swapped digest, a missing FLOOR anchor, an unachievable V3 and the class re-declaration TERMINAL needs), rounds 2–8 the orchestrator's own fix residue, almost all in ONE clause — D7's per-gate invocation recipe — where each pass found by execution another flip-gate invocation that grades nothing and returns green: (i) `check_citations_resolve.py --root <scratch>` — every repo path missing, 0 anchors examined, green tick (pass 2); (ii) `check_convergence.py --project-root <scratch>` — `_converged_targets` reads `git status` and skips `??`, so a committed-clean or untracked copy is 0 targets, silent rc 0 (pass 5); (iii) `check_plan_quality` — no CLI, cwd-bound `PLAN_DIR`, reached only through `validate_conventions --strict --git-diff` where its findings are strict-exempt (passes 4–5); (iv) `check_plan_tickets --plan-dir <scratch>` prints `✗ … not a dated plan directory` and exits 0, and `check_convergence` prints nothing at all on success (pass 6) — the spec's own D5 breaker fired at pass 5 (`command_run.py` printed its oscillation advisory), a first 'principle' rewrite at pass 6 was itself unsatisfiable for gates that print nothing, and the mechanics finally left the spec at pass 7 for the plan's per-gate matrix with a red-first examined-set test; pass 9 quiet; the spec grew 217 → 219 lines; final md5 4a95ce296dc13167767d46640a92d680. RECORDED at the closing pass (plausible, unexecuted wording — never edited in a quiet round, D-206; the approving turn or the plan applies them): (r1) § Personas line 44 and D7 (1) say 'pins the artifact once' where V6 says 'a pin per pass' — read as once PER PASS, never re-pinned mid-pass; fix: add 'per pass' at both; (r2) § Personas line 44 lists the hygiene script at every pin but not the anchor check (`check_citations_resolve.py --changed`), which line 46, D7 (2) and V6 do name; (r3) the matrix test estimate (+~40 lines) is a floor — five gates may need their own fixtures. Against the 44-pass / 451-minute baseline: V6's bar (≤ 5 passes, ≤ 90 min) MISSED.
