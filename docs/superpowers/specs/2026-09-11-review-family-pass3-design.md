# Review-family adoption, pass 3 — the last five loops, the bounded hop, and the six cost fixes

Status: CONVERGED (2026-09-11 — 11 passes under D-212 and the spec's own D4/D5, closing pass confirmed 0; flip row D-223)
Profile: delta
Owner: — (infra; the unnamed hub window fabrik-06, session dd3c06d1)
Date: 2026-09-11
Predecessors: `docs/superpowers/specs/2026-09-08-review-convergence-redesign-design.md` (D-203 → D-206/D-207/D-208) · `docs/superpowers/specs/2026-09-10-review-family-adoption-design.md` (D-212/D-215/D-217/D-218/D-219; its plan EXECUTED and archived at f31990d1) · `docs/STRATEGIC_BACKLOG.md` row 61 (this spec's origin) · `docs/LESSONS_LEARNT.md` 2026-09-11 "A delta-review loop converges only when the kill and the hop are both pinned"

## In one line

Finish the review family's move under D-203 — the five loops that include neither termination fragment, the five `term-coverage` consumers, and 66 restatements of the retired exit in 11 commands — and cut the measured cost of every loop with six text-only fixes: a bounded delta hop, delta rounds sized by the fix, a route-up that fires before the scoped record opens or ends it one round early, a mandatory round zero, the git-verb prohibition in every seat brief, and no second docs loop over docs the heavy review already graded.

## Intake Inventory

The conversation is the denominator (operator words quoted; the 2026-09-10 rows carried from the archived plan's backlog row; the 2026-09-11 rows from this session's turns after the plan closed).

| I# | Item (anchored) | Disposition | Where |
|---|---|---|---|
| I1 | 2026-09-10: "ok note them, do not forget, strategic_backlog.md assign yourself" — the pass-3 row: the five fragment-less review loops | IN | § The delta D1 |
| I2 | same row: verify the `term-coverage` inheritors (`/fabrik-conformance-review`, `/fabrik-service-test`, `/fabrik-user-test`) | IN — CHANGED: the population is FIVE consumers, not three (re-derived § What exists today, row 3) | § The delta D2 |
| I3 | the row's third class (added at the archive): the other 11 `term-edit` consumers' 66 prose restatements of the retired exit; `fabrik-review.md:11`; `fabrik-ui-design-review.md:46,49` | IN | § The delta D3 |
| I4 | 2026-09-11: "all agents are waiting for your work here. our review commands was taking too much time." | IN — the motivating failure | § Why this exists |
| I5 | "does it need any modification so that it can be more accurate, fast, lean, effective, also takes shorter time?" → the six fixes named in reply; "so can you include all these fixes into fabrik-spec pass-3 now?" | IN | § The delta D4–D9 |
| I6 | "how many subagents did you use while executing this plan?" — 116 review seats, 0 coder seats, 95 % of 882 min in review loops | IN — the measurement that sizes the fixes | § Why this exists |
| I7 | "answer my question before invoking a fabrik-spec command" — the operator wants the state stated before machinery runs | IN — the close-out of this spec states the ask ↔ spec table FIRST (this command's Phase 6) | § Machinery report |
| I8 | 2026-09-10: "/opt/fabrik-lib-account and /opt/fabrik-lib-review are not repos which agents run in" | OUT-OF-SCOPE — a census convention, already encoded in `check_convergence.py`'s comment and the workflow doc (worktrees excluded via `git worktree list`) | archived plan f31990d1 |
| I9 | the trade-intelligence held sync of the previous plan (mail 01M23G21EBSS4BC187HQ7PS02X) | OUT-OF-SCOPE — a separate thread, never folded in (operator ruling carried from the previous spec) | memory `project_review_convergence_plan_finish_state` |
| I10 | the seat that ran `git stash --keep-index` in the shared tree during the docs review (this session, 2026-09-11) | IN — the failure D8 encodes | § The delta D8 |
| I11 | rounds 8–11 of the Finish review were residue of the previous round's fixtures (a filesystem-order mutant kill, an ungraded helper, a cleanup that raised on the red path) | IN — the failure D7 encodes | § The delta D7 |
| I12 | the Finish docs review took 12 passes over one 1,118-line reference — one neighbouring section per pass | IN — the failure D4 and D9 encode | § The delta D4, D9 |

Intake: 12 items — 10 IN, 2 OUT-OF-SCOPE (each named above), 0 ASK.

## Personas

One line each (delta profile; no new persona): **the review orchestrator** (a Fable/Opus session running any review-family command — the primary; the operator's words: *"our review commands was taking too much time"*) · **the operator** who waits on the loop and reads its close-out · **the finder/reconciler seats** (`fabrik-reviewer`, `fabrik-researcher`, `design-review`, `general-purpose` reconcilers — automated consumers of every brief this spec changes; the duty holder for D8 is the seat, the duty holder for D7 is the orchestrator) · **the assembler and the graders** (`assemble_commands.py`, `check_convergence.py`, `check_review_coverage.py`, `check_review_hygiene.py`, `command_run.py` — the automated consumers of the fragment grammar) · **the ~46 project repos** that receive the rendered commands box-wide. The primary's minimal loop is unchanged from the predecessor spec (pin → dispatch → execute → fix → record → close); this spec removes rounds from it, it adds no step.

## Goal

Every review-family command closes on the CONFIRMED exit with a delta hop that is bounded in the fragment text, sizes its delta rounds by the fix, never runs two loops over one diff, and briefs its seats with the git-verb prohibition — so a `Profile: small` plan's review cost falls from the measured 838 of 882 minutes to a budgeted fraction, without lowering recall (round 1 keeps its partition and floor).

## Why this exists

Measured on plan `2026-09-10-plan-1-review-family-adoption` (the fleet feedback ledger, `~/.claude/state/command-feedback.jsonl`, session dd3c06d1, re-read 2026-09-11):

| Record | Wall-clock | Rounds | Seats | What the rounds did |
|---|---|---|---|---|
| Phase A `/fabrik-review-scoped` | 67 min | 3 | 9 | three rounds, then routed up — the heavy loop redid the same diff |
| Phase A `/fabrik-review` (routed up) | 266 min | 15 | 31 | rounds 11–14 confirmed graders for behaviour the change never touched (the breaker fired 3·3·3·4) |
| Phase B `/fabrik-review-scoped` | 48 min | 3 | 5 | — |
| Phase C `/fabrik-review-scoped` | not in the ledger | | | its record left no ledger row; the 838-minute total therefore UNDER-counts one phase's seats |
| Finish `/fabrik-review` | 298 min | 12 | 34 | series 10 7 4 6 4 3 3 5 2 3 1 0; rounds 8–11 were residue of the previous round's own fixtures |
| Finish `/fabrik-docs-review` | 159 min | 12 | 37 | 12 passes (the ledger row's `rounds`); the per-pass series 12 9 8 13 6 4 5 8 3 2 2 0 is in this session's docs-review summary (scratch, not a committed artifact); one neighbouring section of a 1,118-line reference per pass |
| `/fabrik-execute-plan` (the whole run) | 882 min | — | 0 coder seats | 838 min (95 %) inside review loops; the code and docs the plan asked for took under an hour |

Three properties of the loops caused it, none of them a defect of `/fabrik-execute-plan` itself: (a) the hop is bounded in EXTENT — `term-edit.md:3` scopes it to the sections whose cross-reference tokens cite the edited section, and `fabrik-review.md:422-424` computes it from the diff plus symbol references — but nothing bounds what a delta round may COUNT, so a real pre-existing defect found one hop out confirms the round and creates the next one; (b) every round costs the floor — the full floor's seats on a four-line delta; (c) the scoped loop runs its three rounds and THEN routes up in a separate turn. The predecessor plan landed the exit, the partition and the delta rounds; it did not bound the hop, and the two Finish loops are the proof. The operator's words: *"all agents are waiting for your work here. our review commands was taking too much time."*

## What exists today (grounded — `path:line`, re-read 2026-09-11 at f31990d1, the SHA every count was taken at)

1. **The fragment population.** `commands/_fragments/term-edit.md` (27 lines) is included by 13 sources (`grep -l 'include:term-edit' commands/_sources/*.md`: data-contract, deploy-checklist, deploy-plan-review, doc-converge, features, flows, flows-review, plan-review, rivals, spec-review, ui-design, ui-design-review, workflow-review). `commands/_fragments/term-coverage.md` (35 lines) is included by 5 (conformance-review, repo-review, review, service-test, user-test). `commands/_fragments/subagents-core.md` (15 lines) is included by 20.
2. **The five fragment-less loops** (`grep -c -i confirmed` per source): `fabrik-review-scoped.md` 2 mentions, includes only `run-record`, 103 lines; `fabrik-docs-review.md` 0, includes `grounding-artifact` + `subagents-core`, 211 lines — its exit is "zero new discrepancies AND makes zero doc edits — a no-op pass" (`:141`) with "Never solo, never two" (`:128`); `fabrik-rules-review.md` 0, 163 lines — "a pass whose gap list is identical to the prior pass (a no-op …)" (`:10`); `fabrik-epics-review.md` 1, 541 lines — "A doer produces, a separate review forces the no-op" (`:19`); `design-review.md` 0, 48 lines — "done ONLY when a fresh, demonstrably-thorough design-review pass finds ZERO issues" (`:39`).
3. **The `term-coverage` consumers**: `fabrik-conformance-review.md` 0 `confirmed` mentions, `fabrik-service-test.md` 3, `fabrik-user-test.md` 4, plus `/fabrik-review` and `/fabrik-repo-review` (the code loops D-203 re-cut first). The fragment's exit row (`term-coverage.md:15`) still reads `found: 0 · new: 0 · fixed: 0`, the literal recurs at `:33` in the stall-breaker sentence, and the D-048 history clause sits at `:24` — it carries no `confirmed:` counter, so its five consumers inherit the OLD exit grammar; the graders accept it ("a ledger without `confirmed:` keeps the `found: 0` rule", D-206). Two more sentences predate D-207 and mandate an unbounded closing sweep for DIFF-surface reviews only: `:12` "The round SHAPE — for reviews whose surface is a DIFF … the closing pass is a FULL fresh sweep — non-negotiable … net: 1 wide + k scoped + 1 wide", whose own closing sentence EXEMPTS `/fabrik-user-test`, `/fabrik-service-test`, `/fabrik-conformance-review` and `/fabrik-repo-review` ("keep their own loop shapes unchanged; nothing in this paragraph rewrites a certification gauntlet"), and `:31` "a fresh, independent round (on a diff surface: the closing FULL sweep …)" — so the sweep binds exactly one consumer, `/fabrik-review`, where it contradicts D-207's "round 1 is the only full pass" today and would contradict D4's counting rule head-on.
4. **The 66 lines** (81 occurrences by `grep -o`) carrying one of the five phrases `ZERO edits|no-op|edit-free|changes nothing|needs no edits` (case-insensitive, re-derived 2026-09-11 on the sources): ui-design 14, ui-design-review 11, flows-review 10, data-contract 7, workflow-review 5, doc-converge 5, deploy-plan-review 4, flows 3, features 3, deploy-checklist 3, rivals 1 = 66 lines. Not all are restatements of the retired exit: the population also holds section headings (`fabrik-features.md:88` "Converge (the self-audit LOOP — iterate to a no-op)", `fabrik-deploy-checklist.md:162` the same heading with an `[anywhere]` qualifier), an intra-command forward reference to the included fragment's own heading (`fabrik-rivals.md:38`) and one live carve-out (`fabrik-deploy-checklist.md:193` "a post-convergence action, exempt from the no-op rule") that SURVIVE — the plan enumerates the survivors line by line before editing. Six fragments use "no-op" in its ordinary sense — `run-record.md:36` (33 includers), `subagents-core.md:6` (20), `cert-board-contract.md:14`, `repo-identity.md:4`, `test-generation-loop.md:19` (2 each; `subagents-core.md:6` and `test-generation-loop.md:19` sit inside `<!-- POOL OFF -->` blocks, which still render) and `close-feedback.md:86`, which the assembler auto-appends to all 36 commands — so a corpus-wide phrase gate on "no-op" is a permanent false positive. `fabrik-review.md:11` "close it only at the quiet delta round"; `fabrik-ui-design-review.md:46` "(parallel grounders per axis)", `:49` "one INDEPENDENT grounder each".
5. **The hop bounds EXTENT, never COUNTING**: `term-edit.md:3` "every later round is a DELTA over the previous round's fix diff plus one hop — the sections whose cross-reference tokens (`I#`, `D#`, `G#`, `T##`, a `§` heading, a `path:line`) cite the edited section"; `fabrik-review.md:422-424` "Its surface is computed, not judged: `git diff <the last round's commit>..HEAD -- <the review's surface>`, PLUS one hop of callers and callees"; `fabrik-repo-review.md:156`, `core/62-using-subagents.md:65`, `CLAUDE.md:358`, `templates/governance/CLAUDE.md:359`, `subagents-core.md:3` and `docs/reference/convergence-prompts.md` carry the same "plus one hop of callers and callees" sentence (7 RULE-TEXT sites; 14 occurrences repo-wide at f31990d1 by a whitespace-tolerant scan — the other 7 are 6 review receipts under `docs/development/reviews/` and the archived plan, which are frozen records and are not edited; two of the seven wrap across lines and one reads `PLUS`, so a plain grep undercounts). None of the seven says what a delta round may COUNT: a defect found inside the hop but outside the previous round's hunks is CONFIRMED by every one of them.
6. **Seat sizing today**: `fabrik-review.md:438` "The closing delta round ALWAYS carries a fresh, non-authoring finder seat"; `fabrik-review-scoped.md:60-75` "The floor is 3 readers … three readers of the same one-unit diff on different angles, `dispatch_headroom.py --units 1`"; D-208 keeps the floor for `/fabrik-review-scoped` and grounding surfaces; D-218 sizes a `term-edit` delta round "by the fix (one seat for one paragraph plus the hygiene script)" — the code loops and the coverage loops have no such sentence.
7. **The route-up**: `fabrik-review-scoped.md:27-33` — ">5 files → STOP and run the full `/fabrik-review` instead … Invoked per phase/ticket by a `Profile: small` plan: step 1's trigger is SATISFIED by the plan's Finish `/fabrik-review` … record `ROUTED-TO-FINISH: <trigger>` and continue the light pass here"; `:32-33` "Step 5's three-rounds trigger still escalates". Under `Profile: small` the scoped loop therefore runs to its own close and the heavy loop repeats the diff (Phase A: 67 + 266 min).
8. **Round zero**: the orchestrator's pre-pin probe exists as prose in `term-edit.md:20` ("Before Pass 1 and before every pin — three mechanical obligations": RESIDUE before every pin: re-read edited paragraphs, grep old wording, hygiene script) for DOCUMENT loops; no sentence in `term-coverage.md`, `fabrik-review.md` or `subagents-core.md` names the CODE-side probe (mutants asserted dead by test name on a fresh copy, environment-bound tests deselected, `__pycache__` purged).
9. **The git-verb prohibition**: `commands/_agents/fabrik-reviewer.md:33` already says "Read-only — and that includes git. Never edit, write, or commit, and never `git checkout --`, `git restore`, `git stash`, `git reset`"; `commands/_agents/fabrik-researcher.md`, `commands/_agents/design-review.md` (the 107-line agent definition; `commands/_sources/design-review.md` is the 48-line command) and `commands/_agents/fabrik-gui.md` carry no git sentence; no seat BRIEF template (`subagents-core.md`, the `/fabrik-review` and `/fabrik-docs-review` brief paragraphs) carries it — and the pass-9 docs-review seat that ran `git stash --keep-index` was a `fabrik-reviewer` briefed by a prose brief that said only "never write into /opt/fabrik". `CLAUDE.md` § Shared repo names the stash hazard for the ORCHESTRATOR and carries no recovery recipe for a seat's stash.
10. **The Finish docs loop**: `fabrik-execute-plan.md:272` runs `/fabrik-docs-review` at Finish over docs the plan changed; `:400-406` "whole-plan doc-coverage RECEIPT … run /fabrik-docs-review → converge docs to a truthful fixed point"; nothing excludes docs the whole-plan `/fabrik-review` already graded as review surface (the archived plan's `FINAL_GATE_WORKFLOW.md` rows were graded by both loops).
11. **The graders**: `check_convergence.py::_closing_row_fail` refuses a CONVERGED/EXECUTED flip whose last Pass row does not read `confirmed: 0`; `check_review_coverage.py` demands a quiet final ledger row and reads `confirmed:` where present; `check_review_hygiene.py --surface` grades the rendered corpus for retired phrases (`--phrase`), residue and table shape; `tests/enforcement/test_flip_gate_matrix.py` pins the flip-gate invocation matrix; `tests/test_assemble_dispatch_step.py` pins the assembler's FLOOR strings and kinds.
12. **The decision rows**: D-206 (the counter), D-207 (partition by file), D-208 (the floor retired for the two partitioned code loops, KEPT for grounding surfaces and `/fabrik-review-scoped`), D-212 (the family moves under D-203 now), D-218 (the floor stands down for a `term-edit` loop that partitions by section; a delta round sized by the fix).

Profile trigger check: every intake item maps to text that exists today (rows 1–12); the delta is a change to existing fragments, sources, agent definitions and one pack sentence — no new component, no new script. `Profile: delta` holds.

## The delta

Every item CITES the rulings and the landed mechanics; nothing re-narrates them.

- **D1 — four of the five loops move under D-203 by INCLUSION; the fifth by CITATION; no third fragment.** `/fabrik-docs-review`, `/fabrik-rules-review`, `/fabrik-epics-review` and `/design-review` (`commands/_sources/design-review.md`, the 48-line command) include `term-edit` (their artifact is a document set, a rule set, an epic set, a screen set — the edit-shaped contract) with ALL FIVE of the fragment's slots filled per command — `ARTIFACT`, `DONE_ACT`, `DONE_WORD`, `AXES`, `EXEMPT_NOTE` (an unfilled slot is a hard render refusal — `assemble_commands.py:1113,1139-1143` prints `RENDER ERRORS: <cmd>: unresolved ['{{SLOT}}']` and exits 2 before writing either tree, which V3's `--check` already runs) — the Pass Ledger arriving WITH the include (`term-edit.md:22,24-27` render it into the REPORT; no `term-edit` consumer carries a host-side `## Pass Ledger` heading and none is added — a host-side ledger would be a fourth copy of the contract), each host's existing ledger prose rewritten to a citation of it (`design-review.md:39`'s one-line `Pass k — found: N, fixed: M` form, its `Standing:` line — retired by D-219 — and its D-048 citation — superseded by D-206 — all go in the same edit), `commands/_sources/design-review.md` also gaining `include:subagents-core` (the fragment cross-references a `## Subagents` Floor line the flat command has no target for), and each command's exit vocabulary rewritten to a citation of the fragment's closing round (the "no-op pass" of `fabrik-docs-review.md:87,141-165`, the "identical gap list" of `fabrik-rules-review.md:10`, the "forces the no-op" of `fabrik-epics-review.md:19`, the "ZERO issues" of `design-review.md:39`). `/fabrik-review-scoped` does NOT include `term-coverage`: it carries none of the three artifacts `term-coverage` imposes — no persisted receipt, no Surface hash, no Coverage Checklist (0 hits for `receipt|surface hash|coverage checklist` over the 103-line command; `term-coverage.md:6,8,10` mandate all three before pass 1) — and that lightness is deliberate (`fabrik-review-scoped.md:51-53` — "deliberately no review file … that asymmetry is the lightness"; `:63-65` records the independent-reader asymmetry, a different axis, and calls only the ABSENCE OF RECALL accidental); it gains ONE paragraph that CITES the fragment's exit vocabulary — the CONFIRMED counter (D-206), the counting rule of D4, `round --confirmed` every pass — and keeps D-208's three-reader floor at ROUND 1 only (D5). A third fragment is rejected (§ Rejected alternatives 1).
- **D2 — `term-coverage.md` gains the `confirmed:` counter and the same closing-row grammar as `term-edit`**: its exit row (`:15`) and the stall-breaker literal (`:33`) read `confirmed: 0 · fixed: 0 · unexecuted: 0` (D-206), `found:`/`new:` stay as recall and the stall-breaker keys on `confirmed:` (three consecutive non-decreasing nonzero delta rounds); the D-048 history clause (`:24`) is deleted (present-tense rule, `core/40`); the DIFF-surface round SHAPE at `:12` and the closing-sweep clause at `:31` are replaced by D-207's shape — round 1 the only full pass, every later round a delta under D4's counting rule and D5's sizing (the two-round minimum stays: a quiet round 1 is confirmed by a closing delta round) — while `:12`'s exemption sentence for the non-diff consumers (`/fabrik-user-test`, `/fabrik-service-test`, `/fabrik-conformance-review`, `/fabrik-repo-review` keep their own loop shapes) is KEPT verbatim. Its five consumers are verified by execution to render the counter — the bullet at `commands/_sources/fabrik-conformance-review.md:169-178` restates the exit ROW, the `found:`/`new:` semantics and the D-048 clause in the HOST's own text (the only host-side restatement; the source grep for the literal → 3 hits, 2 of them the fragment's own) — those three clauses and its Pass-Ledger clause (`:176-178`, the fragment's counters restated) become a citation of the fragment in the same edit, and its Checklist-adjudication clause goes too (the fragment carries the same rule at `term-coverage.md:25`, in more words), while the bullet's one HOST-ONLY requirement — "every Inventory row terminal (no `PENDING`)" — stays, since the fragment carries no Inventory clause and no grader enforces it (1 of 77 `scripts/enforcement/` files mentions `PENDING`: `check_certification_coverage.py`) — (ACCEPTANCE after the plan, not a measurement today: the literal `found: 0 · new: 0 · fixed: 0` grepped on the five RENDERED files → 0 hits, from 11 today — 3 · 2 · 2 · 2 · 2; `/fabrik-conformance-review`'s 0 `confirmed` mentions become ≥1 by inclusion alone).
- **D3 — the restatements of the retired exit among the 66 lines are deleted or rewritten as citations** in the 11 `term-edit` consumers, plus `fabrik-review.md:11` and `fabrik-ui-design-review.md:46,49`; the plan lists every surviving line (the two headings, the intra-command forward reference at `fabrik-rivals.md:38`, the deploy-checklist carve-out) by `path:line` first. Acceptance: the five-phrase grep on the 11 SOURCE files → exactly the enumerated survivors (`check_review_hygiene.py --phrase` scans raw text with no fence or own-sentence exemption, so the grader is the source-file grep with the survivor list, never a rendered-corpus phrase gate).
- **D4 — THE BOUNDED HOP, in the fragment text (F1).** `term-edit.md` and `term-coverage.md` each gain ONE sentence defining the delta round's surface: *a CONFIRMED defect of a delta round is a defect of a line INSIDE the previous round's fix hunks, or a live contradiction those hunks created with a sentence of the same section; a defect one hop out — a caller, a callee, a neighbouring section, a pre-existing claim — is RECORDED with a named destination (a backlog row, a mail, a sibling ticket) and never counted, even when real and executed.* The ledger's `confirmed:` and the record's `--confirmed` count the SAME thing under this rule (the two-number convention of the archived plan's receipt is retired). The seven sites of "plus one hop of callers and callees" — `subagents-core.md:3`, `fabrik-review.md:423`, `fabrik-repo-review.md:156`, `core/62-using-subagents.md:65`, `CLAUDE.md:358`, `templates/governance/CLAUDE.md:359`, `docs/reference/convergence-prompts.md` — keep the extent clause and gain the counting clause by citation of the fragment sentence (`/fabrik-docs-review` inherits it through `subagents-core` and needs no edit). Grader: `check_review_hygiene.py --surface commands/_sources --surface commands/_fragments --surface .windsurf/rules/core/62-using-subagents.md --surface CLAUDE.md --surface templates/governance/CLAUDE.md --surface docs/reference/convergence-prompts.md --phrase 'plus one hop of callers and callees'` (one `--surface` per path; the phrase matcher is whitespace-tolerant and case-insensitive, so it finds the two wrapped sites and the `PLUS` spelling a plain grep misses) → exactly 7 hits after the plan, every one on a line that ALSO carries the counting citation (the extent clause stays; the grader proves presence of the sites, the citation is read); the flip-gate matrix unchanged.
- **D5 — DELTA ROUNDS SIZED BY THE FIX (F2).** Round 1 is the only full pass and keeps its partition (D-207) or its floor (D-208 for `/fabrik-review-scoped` and grounding surfaces). Every later round dispatches by the size of the previous round's fix diff — changed lines of code, tests and graded docs, EXCLUDING the review receipt, which every round rewrites by construction — at or under a stated budget — **20 changed lines** (the value is a parameter of the fragment, one place) — ONE fresh non-authoring seat plus the hygiene script; above it, the round-1 partition over the touched slices. `dispatch_headroom.py` prints the size: `--slices` or `--units` as today, plus `--delta <changed lines>` alongside the round-1 `--slices`, which prints `SEATS: 1` at or under the budget and the slices' sum above it (a stamp is still owed). The 20 is a JUDGEMENT, not a measurement — the archived plan's deltas never went below 15 changed lines under this metric (rounds 9–12: 88, 75, 53, 15 excluding the receipt; 136, 96, 61, 25 with it; seats 3, 2, 2, 1; FIXED rows 9, 7, 5, 0), so the only evidence is that the one one-seat round (12), over a 15-line code-and-test delta, confirmed 0 — 20 sits in the unmeasured gap between 15 and 53; V6 calibrates the constant from the first three reviews under the landed text, and the § Cost targets assume most delta rounds fall under it once D4 stops the residue chain. Needs a D-row narrowing D-208's "KEPT for … `/fabrik-review-scoped`" to round 1 (§ Decisions taken). Grader: `tests/test_assemble_dispatch_step.py` pins the two FLOOR strings' new clause; `dispatch_headroom.py`'s test pins `--delta 4 --slices opus=1,sonnet=1 → SEATS: 1`, `--delta 20 … → SEATS: 1` and `--delta 21 … → SEATS: 2`.
- **D6 — THE ROUTE-UP PRECEDES THE SCOPED RECORD, OR ENDS IT EARLY (F3).** `fabrik-review-scoped.md:27-33`: the file classification of step 1 — the command's own trigger list, "any gate/hook/enforcement path, auth/schema/migration/concurrency surface, or >5 files" (`:27-28`) plus the frontmatter's "operator-named work" (`:2`) — moves BEFORE the command's `start`: when the trigger fires, the scoped command opens NO record and invokes `/fabrik-review` in the same turn, whose `start` names the trigger in its surface (`--surface "ROUTED-UP: step 1 — <the trigger> · <the diff range>"`) — the ledger's only positive witness that the route-up fired (`command_run.py:1863` → `:2702`, executed). Two paths change, one does not: (i) the spontaneous route-up above; (ii) step 5's escalation (`:57-59`, today "three rounds each confirming something = the surface outgrew this command: STOP and run the full `/fabrik-review`") fires one round earlier and in the SAME turn: after the SECOND CONSECUTIVE round that confirms defects the loop escalates instead of running another — it closes `done --command fabrik-review-scoped --evidence "ROUTED-UP after 2 confirming rounds — continued in <the heavy review's receipt path>; <x> fixed / <y> refuted" --feedback "<the four fields>"` (a second documented evidence form beside `:94-95`'s `new 0` string, which an escalating loop cannot write truthfully; `--command` and `--evidence` are `required=True` (`command_run.py:1662`, `:1667`) and the four-field `--feedback` is refused if absent, short of four fields or bare (`:2492`, `:2506`, `:2544`), executed; `/fabrik-review-scoped` is not in the persisted-report tuple at `:2288`, so its `done` needs no receipt) and the heavy review runs at once — the verb is `done`, never `handoff`, because only `done` reaches back (`:2585`): the scoped record's `done` window reaches back to the previous AGENT-closed window's close, so with a prior closed record in the session it covers the pre-`start` edits the command exists to review — with none they stay in the pre-existing gap RECORDED below — and the heavy review's window covers the rest (executed this review with both close verbs on a scratch repo, a prior closed record present: `done` lands a pre-`start` edit INSIDE the covered union, `handoff` leaves it OUTSIDE); (iii) under `Profile: small` the per-phase invocation is unchanged — the `:29-33` carve-out stays as written (the plan's Finish `/fabrik-review` satisfies the trigger and the light pass runs, because a phase commit needs its own covered window; its cost falls under D5's sizing). Coverage, stated with its precondition (executed by the round-4, round-5, round-7, round-9 and round-10 seats on scratch repos): a heavy review's `done` reaches back to the previous AGENT-closed window's close (`scripts/command_run.py:2585-2609`, `:579`, `:1807-1833`), so with a prior closed record in the session the route-up before `start` covers the routed-up edits; a `handoff`-closed scoped record placed BETWEEN the edits and the heavy review caps that reach-back at its own start (why path (i) precedes `start` and path (ii) closes with `done`); with NO prior record in the session the edits before the chain's first `start` (the scoped `start` on path (ii), the heavy `start` on path (i)) stay outside every window — the record protocol's pre-existing gap for spontaneous edits, unchanged by this spec and RECORDED to the backlog; under `/fabrik-execute-plan` a nested review starts with an empty ledger and the parent's open window covers (`command_run.py:1878`, `final_gate_stop.py:677`). No `command_run.py` change, no new verb, no `handoff` on any path. Grader: a `command_run.py` probe pair under scratch env (`--session probe-<n>`): a closed record → edits → a `fabrik-review` `start`, its receipt written under `docs/development/reviews/` (since `done` REFUSES a review record with no persisted report — executed this review) → `done` with a covered window whose `lo` precedes the edits (green); the same with an intervening scoped record closed `handoff` → the capped window (red — the arm that forbids the verb).
- **D7 — ROUND ZERO IS THE ORCHESTRATOR'S PRE-PIN PROBE (F4), stated in both fragments.** Before any seat sees a delta: every mutant a fix names is asserted dead by the NAME of the failing test on a fresh copy of the pin, with environment-bound tests deselected and `__pycache__` purged; every added line is re-read whole (length, citations, claims); the class of the fix is swept (the siblings of the shape). A round whose seats confirm only residue of the previous round's fix is the signal to sweep the class, not to run another round. This extends `term-edit.md:20`'s RESIDUE obligation to code loops (`term-coverage.md`) and names the code-side method; the Pass Ledger's `method:` cell may read `method: round-zero probe — <what>` on the row that records it. Grader: none mechanical — the receipt's Pass row names it (the coverage gate already reads the method cell); the lesson entry cites the measurement.
- **D8 — THE GIT-VERB PROHIBITION in every seat brief and every agent definition (F5).** `subagents-core.md` gains the sentence *a seat runs NO git command that mutates state in the shared tree — no stash, checkout, reset, restore, apply, commit, clean; read-only git only (`show`, `diff`, `log`, `status`, `ls-files`); every probe on a copy*; the four agent definitions under `commands/_agents/` (`fabrik-reviewer.md` already carries it at `:33`; `fabrik-researcher.md`, `design-review.md` — the 107-line AGENT definition, not the 48-line command of the same basename — and `fabrik-gui.md` gain it) carry the same sentence; `CLAUDE.md` § Shared repo gains the recovery recipe for a seat's stash (`git stash show --name-only 'stash@{0}'`, then `git show 'stash@{0}':<path> > <path>` per file, md5-verified against the pre-incident values, the entry left for a human — never a pop, which the classifier blocks). Grader: a red-first case in `tests/test_agent_definitions.py` asserting the sentence in EACH of the four `commands/_agents/*.md` (the hygiene script's `--phrase` is a negative check and `--symbol` pools the whole surface, so neither can count per file).
- **D9 — NO SECOND DOCS LOOP OVER DOCS THE HEAVY REVIEW GRADED (F6).** `fabrik-execute-plan.md:272` and `:400-406`: the Finish `/fabrik-docs-review` runs over the plan's docs MINUS the docs the whole-plan `/fabrik-review` already graded (its receipt's Coverage Checklist names them); the Finish brief lists the excluded docs with the receipt row that graded each. When the set is empty the step records `SKIPPED — every changed doc was review surface` and the plan's Execution note says so. Grader: the plan-quality check already reads Execution notes; a red-first test in `tests/enforcement/test_check_plan_quality*.py` accepts the `SKIPPED —` shape (extend the existing one).

## Chosen approach (and why it is the lean one)

Inclusion over a third fragment; one sentence for the hop and one number for the delta budget, each in exactly one place (the two fragments), cited everywhere else; every other change deletes text (66 restatements, the D-048 clause) or moves a duty to where its holder reads it (the agent definitions, the seat brief). One script gains one flag (`dispatch_headroom.py --delta`), red-first on its existing suite; the route-up opens no record on path (i), and path (ii) closes its record with `done`, so a `done` reach-back leaves no uncovered edit under D6's stated precondition (a prior closed record in the session). No new script, no new dependency.

## Rejected alternatives

1. **A third fragment (`term-loop`) for the five loops** — rejected: it would restate `term-edit`'s exit in different words, the defect `core/40`'s single-source rule and the predecessor's rounds 8–11 (a fragment that restated the pack re-opened four times) exist to prevent; the five loops are edit-shaped or coverage-shaped, both fragments already exist.
2. **A round cap** — rejected by D-203 R6 and the carried rulings; the bounded hop ends loops by construction, not by count.
3. **Counting one-hop findings as confirmed but not requiring a fix** — rejected: a counted-but-unfixed row is exactly the non-quiet ledger `check_review_coverage.py` refuses; RECORDED with a destination is the family's existing disposition (D-206).
4. **Sizing delta rounds by the floor with cheaper models (Haiku)** — rejected by D-208's evidence (Haiku seats confirmed nothing in the D-191 rounds 15–18) and D-190's pricing; the saving is in seats per round, not price per seat.
5. **Keeping the scoped loop's third round before a route-up "for the light pass's own value"** — rejected by the Phase A measurement: the third light round bought nothing the heavy loop did not redo; the first round is the floor's recall (D-208) and a second round, sized by the fix under D5, is the cheapest proof that the surface outgrew the light layer.
6. **A hard gate that refuses a `git stash` from a seat** — rejected: the classifier already blocks the recovery and not the act; a tool-level guard on stash is outside this repo's reach (the harness), so the control is the brief text plus the recovery recipe; measured fire rate before any check: 1 incident in ~120 seats.
7. **Running the Finish docs review over everything and de-duplicating findings afterwards** — rejected: the duplicate cost is the seats and the passes, not the findings.
8. **Merging `term-edit` and `term-coverage` into ONE fragment once their closing-row grammar converges (D2)** — rejected for this pass: the two contracts still differ in what they grade (a document set with an md5 anti-cheat and a Pass Ledger vs a diff with a rubric-derived Coverage Checklist, a persisted receipt and a Surface hash), so a merge is a rewrite of both contracts across all 18 consumers, not a counter change; D2 makes the EXIT ROW identical, which is what the graders read, and leaves the merge as a backlog row with a trigger (three reviews under the landed text showing no consumer needs the coverage-only obligations).

## Contract deltas

None to `docs/data-contract.md` or `docs/ui-design.md` (no data, no screens). Command-corpus contract: `term-edit.md` and `term-coverage.md` gain one hop sentence, one delta-budget number and the round-zero paragraph; `term-coverage.md` gains the `confirmed:` counter; `subagents-core.md` gains the git-verb sentence; `dispatch_headroom.py --delta` is an additive flag (a caller passing none behaves as today — the MIRROR: no existing invocation breaks; the two FLOOR strings the assembler renders change wording, pinned by their test); the route-up opens no scoped record on path (i) and path (ii) closes its record with `done`, so the record protocol gains no verb and the covered-window reach-back keeps its `done`-only guarantee — the cost, stated: on BOTH paths the routed-up / pre-`start` edits are covered when the session already carries a closed record (D6's precondition) or a running parent whose open window covers them (the nested `/fabrik-execute-plan` case in D6) — with neither they stay in the record protocol's pre-existing gap, which running the heavy review in the same turn does not close; under the precondition the heavy review runs in the same turn or the Stop hook blocks; `term-coverage`'s DIFF-surface round-shape sentences (`:12`, `:31`) change meaning for `/fabrik-review` only — the non-diff exemption at `:12` is kept, so the two certification gauntlets and the inventory reviews keep their loop shapes; the plan re-reads `/fabrik-review`'s own round text for a restatement of the closing full sweep.

## Cost

- Corpus: `term-edit.md` +~6 lines; `term-coverage.md` +~8 / −~6 (the counter, the hop, the D-048 history clause deleted); `subagents-core.md` +2; `fabrik-review-scoped.md` −~8 / +~4 and `fabrik-review.md` +~2 (the `ROUTED-UP: step 1` surface on a routed-up `start`); `fabrik-docs-review.md` −~20 / +~6 (the include replaces its loop paragraphs); `fabrik-rules-review.md`, `fabrik-epics-review.md`, `commands/_sources/design-review.md` −~6 / +~4 each (the exit vocabulary and the ledger prose rewritten to citations; the ledger itself renders from the fragment); `docs/reference/convergence-prompts.md` +1 (the counting clause); `fabrik-conformance-review.md` −~8 / +~2 (the exit-row, counter, D-048, Pass-Ledger and Checklist clauses of its DONE bullet; the Inventory clause stays); the 11 consumers −66 restatements (+ ~11 citation lines); `fabrik-review.md` and `fabrik-repo-review.md` −2 / +2 (the hop citation); `fabrik-execute-plan.md` +~6; four agent definitions +1 each; `CLAUDE.md` § Shared repo +~4 (the recipe) and `templates/governance/CLAUDE.md` the same bullet; `core/62` § Role separation +1 sentence (the hop, cited by the fragments).
- Code: `scripts/sysadmin/dispatch_headroom.py` +~25 (`--delta`); tests +~50 across the existing suites (`dispatch_headroom`, `test_agent_definitions.py`, `test_assemble_dispatch_step.py`); `command_run.py` unchanged. Fleet-synced text: `core/62` and `templates/governance/CLAUDE.md` are governance-sync TRIGGERS (`.pre-commit-config.yaml`'s `governance-sync` regex) — each commit touching them distributes on the POST-commit sync by itself (`scripts/command_run.py` is a trigger too, but this spec does not touch it); D-169's ONE forced sync at Finish is the profile's discipline, not the distribution mechanism. The hub's own `CLAUDE.md` is hub-only (`scripts/fabrik_synced_manifest.py:100-105`).
- Plan shape: `Profile: small` (D-169) — code diff 1 file / ~25 lines, tests excluded; the corpus edits are text. Three phases: A the two fragments + `subagents-core` + `dispatch_headroom.py --delta` with their tests; B the five loops' inclusion + the 66 restatements + the route-up + the Finish docs-loop rule; C the agent definitions, `CLAUDE.md` × 2, `core/62`, the docs, the sync, the heavy round.
- Targets, not projections — V6 measures them (§ Lifecycle): a Finish `/fabrik-review` of a small plan from 12 rounds / 34 seats / 298 min to ≤5 rounds / ≤10 seats / ≤90 min; a Finish docs review from 12 passes / 37 seats to ≤3 passes / ≤5 seats or SKIPPED; a spontaneous scoped review whose diff trips step 1 to the heavy loop alone (path (i): 0 light rounds, 0 light seats — witnessed by the heavy review's ledger row opened with `ROUTED-UP: step 1` in its surface and no scoped row beside it); a `Profile: small` phase that escalates at step 5 from a 67 min / 3 rounds / 9 seats light loop to a two-round light loop (round 1 at the D-208 floor, round 2 one seat under D5's budget, three if its fix diff exceeds it: 4 seats, 6 above the budget) plus the heavy loop in one turn.

## Validation

- **V1** — red-first: `test_assemble_dispatch_step.py` pins the new FLOOR strings (a mutant restoring the old wording turns its test red); D4's counting citation is pinned by a red-first case in `tests/test_assemble_dispatch_step.py` (which already reads the corpus sources) asserting the citation in EACH of the seven hop-site files by path — the D8 reasoning, per file — and a mutant deleting the extent clause drops the seven-surface hygiene run below 7 hits; `dispatch_headroom.py --delta 4 --slices opus=1,sonnet=1 → SEATS: 1`, `--delta 20 … → SEATS: 1`, `--delta 21 … → SEATS: 2`; D6's probe pair on a scratch env — green: a closed record → edits → `fabrik-review` `start`/receipt/`done` with the window's `lo` before the edits; red: an intervening scoped `start`/`handoff` caps the window so its `lo` lands AFTER the pre-`start` edits, which then sit outside every window.
- **V2** — the five-phrase grep on the 11 sources → exactly the enumerated survivors; the seven-surface `--phrase 'plus one hop of callers and callees'` run of D4 → 7 hits, each line carrying the counting citation; the literal `found: 0 · new: 0 · fixed: 0` grepped on the five rendered `term-coverage` consumers → 0.
- **V3** — `python3 commands/assemble_commands.py --check` clean; `check_command_corpus.py` green; every render from the main checkout.
- **V4** — this spec's own `/fabrik-spec-review` under D-212 with D4/D5 applied to itself as the review's BRIEF (the operator-approved intent for this review, the D-212 precedent — the rules are not yet landed text; the row that makes them a ruling is minted at approval): the delta after pass 1 is one seat per small delta; the Pass Ledger states where the hop stopped.
- **V5** — `tests/test_agent_definitions.py` asserts the git-verb sentence in each of the four `commands/_agents/*.md`; the recovery-recipe bullet extracted from `CLAUDE.md` and `templates/governance/CLAUDE.md` by its anchor line compares byte-for-byte (the two files are different documents by design).
- **V6** — the first three reviews under the landed text (one `/fabrik-review` Finish, one `/fabrik-docs-review`, one `/fabrik-review-scoped` that escalates on path (ii)) plus the first path-(i) route-up (the heavy review's own row, opened with `start --surface "ROUTED-UP: step 1 …"` — the surface word D6 prescribes — and no scoped row beside it), measured from the feedback ledger: rounds, seats, wall-clock, and the count of RECORDED-one-hop rows per receipt.

## Decisions taken

- **At approval (minted by the approving turn):** the spec's approval row; a row narrowing D-208 — the three-reader floor of `/fabrik-review-scoped` and the grounding surfaces applies to ROUND 1; every later round is sized by the fix under the fragment's delta budget (one fresh seat at or under 20 changed lines); a row stating the bounded hop as the family's delta rule (the fragment sentence is the text; the row is the ruling).
- **Not decided here:** the delta budget's exact value beyond 20 (V6 measures it); a harness-level guard on `git stash` (outside this repo).

## Lifecycle

Lands via one small plan; V6 measures the saving on the first three reviews and re-pins the budget if the RECORDED-one-hop rows show real defects escaping (a RECORDED row with a destination is never lost — it is a backlog row, so the fix is a ticket, not a re-opened loop); degradation: a loop that trips the breaker under the bounded hop names its foundation error as today; retired when the family is folded into one termination fragment, if ever.

## Grounding (external facts — the approach floor; fetched 2026-09-11 by one native `fabrik-researcher` seat, quotes verbatim after whitespace normalisation; the orchestrator probes each URL's status at the flip)

| G# | Claim the approach rests on | Source | Verdict |
|---|---|---|---|
| G1 | review effectiveness falls with the size of the reviewed change and the time spent (why a delta round bounds its surface) | SmartBear, "Best practices for peer code review" (the Cisco study): "developers should review no more than 200 to 400 lines of code (LOC) at a time… a review of 200-400 LOC over 60 to 90 minutes should yield 70-90% defect discovery" — https://smartbear.com/learn/code-review/best-practices-for-peer-code-review/ (fetched 2026-09-11 on two arms; HTTP 200 at the flip probe) | VERIFIED |
| G2 | a mutant's kill can be nondeterministic — timeouts and external load (why round zero asserts a kill by test name on a fresh copy, and why the filesystem-order kill of the archived plan's round 8 was a real class) | PIT FAQ: "a mutant may be detected as timed out on one run, but killed or surviving on another" — https://pitest.org/faq/ (fetched 2026-09-11; HTTP 200 at the flip probe) | VERIFIED |
| G3 | `git stash` saves the working directory and index state and reverts the working directory to HEAD (why a seat's stash sweeps every session's files) | git-scm.com, `git stash` DESCRIPTION: "The command saves your local modifications away and reverts the working directory to match the HEAD commit." — https://git-scm.com/docs/git-stash (fetched 2026-09-11; HTTP 200 at the flip probe) | VERIFIED |

Internal measurements (the primary evidence): the feedback ledger rows in § Why this exists; the archived plan's receipt (`docs/development/reviews/2026-09-10-plan-1-review-family-adoption-review.md`, rows M1–S2 and Pass 8's non-convergence note); the docs-review summary in this session's scratch (12 passes, the BAR imposed at pass 9); `docs/LESSONS_LEARNT.md` 2026-09-11.

## fabrik-lib verdict

One line: no capability is vendored, enhanced or built — the delta is command text, two additive flags on hub RUN_SCRIPTS, and one pack sentence; `fabrik-lib/README.md` offers no review-loop module.

## Constraints digest

| # | Rule (verbatim) | Pack `file:line` | How this spec obeys it |
|---|---|---|---|
| C1 | "Role separation (review loops) — who hunts LAST is never the author" | `.windsurf/rules/core/62-using-subagents.md:184` | the closing round keeps a fresh non-authoring seat under D5; round zero is the author's probe, never the closing read |
| C2 | "loop is a delta round with a fresh seat (D-212). **One sanctioned exception — the solo" | `core/62-using-subagents.md:190` | D1 cites the exception's home; the five loops include the fragment that cites it |
| C3 | "**⚠️ THE OPENROUTER POOL IS OFF — operator ruling D-181 (2026-09-07), OFF BY POLICY" | `core/62-using-subagents.md:65` | every seat native; D5's one-seat delta is a native seat |
| C4 | "Source, config, or Docker file changed \| `CHANGELOG.md` entry under `## [Unreleased]`; `INDEX.md` reflects change" | `core/40-documentation.md:66` | the plan's per-phase CHANGELOG entries; INDEX rows for this spec and the plan |
| C5 | "Internal plans, changelogs, and developer notes are exempt from brand voice — clarity and speed matter more than tone." | `core/40-documentation.md:205` | the fragment sentences are rules, present tense, one place each |
| C6 (FLOOR) | "Scaffolded doc templates, Documentation Sync Matrix, changelog, INDEX.md, writing style" | `core/40-documentation.md:7` | § Documentation landing sites |

FLOOR packs core/25, core/30, core/35 and the 12-factor axes: read; unconstrained here (no data, no ops, no auth surface) — stated, not skipped.

## Shape / infra implications

None: the hub has no `project.yaml`; nothing deploys. Box-wide render of the corpus; fleet sync of `core/62` and `templates/governance/CLAUDE.md` (the hub's own `CLAUDE.md` is hub-only).

## Documentation landing sites

`docs/workflows/FINAL_GATE_WORKFLOW.md` (the `term-coverage` counter and the hop sentence as the graders read them); `docs/reference/command-run-protocol.md` (one paragraph: the covered-window reach-back, the before-`start` route-up and the `done`-closed escalation — no code change; and its `:190` receipt-refusal tuple corrected to the code's six commands, which omit `fabrik-review-scoped`, found by this review); `docs/workstation/` nothing; `CHANGELOG.md` per phase; `INDEX.md` rows for this spec and its plan; `docs/DECISIONS.md` the rows above; `docs/STRATEGIC_BACKLOG.md` row 61 → SHIPPED at the plan's archive and a new row for the record protocol's spontaneous-edit coverage gap that D6 RECORDS.

## Open / blocking unknowns

- Resolved: the `term-coverage` population (five, not three); the reviewer definition already carries the git sentence (D8 is the briefs and the other three definitions).
- Resolved this review: the route-up happens BEFORE a scoped record opens (path (i)) or closes the open record with `done` (path (ii)), and on both paths the routed-up edits are covered only under D6's precondition (a prior closed record in the session) or a running parent's open window — a `handoff`-closed scoped record caps the successor's reach-back (executed by the round-4, round-5, round-7, round-9 and round-10 seats on scratch repos); the reach-back at `scripts/command_run.py:2585` stays on `done`, and D6 changes no code.

## Machinery report

DRAFT written 2026-09-11 from the run record's open; the intake enumerated from the conversation (12 items); the counts re-derived on the sources this turn; one researcher seat dispatched for G1–G3 (the approach floor); no external vendor fact asserted. The operator asked for the answer before the machinery ("answer my question before invoking a fabrik-spec command") — the close-out presents the ask ↔ spec table first.

CONVERGED 2026-09-11 after the in-turn `/fabrik-spec-review` under D-212 and the spec's own D4/D5: 11 passes in ~165 min wall-clock (nested record open 11:01 → close 13:47 local); seats per pass 3 · 2 · 1 · 1 · 1 · 1 · 1 · 1 · 1 · 1 · 1 = 14 (pass 1 the section partition — Opus on the rule sections, Sonnet on the rest, one researcher seat on G1–G3; every later pass one fresh Opus seat over a delta at or under 20 changed lines plus the hygiene and citation scripts); confirmed series 20 · 12 · 6 · 5 · 8 · 3 · 5 · 4 · 2 · 2 · 0 — passes 7–11 were one paragraph's residue chain (D6's coverage claim: the close verb, the parser line, the precondition, the running-parent source, the surface witness), the D7 signal this spec names; 175 lines before this paragraph. RECORDED residues to apply as plan-time text, never spec edits: r1 § Contract deltas says "a closed record" where the mechanism needs an AGENT-closed one (`done`/`blocked`/`handoff`; a coroner-reaped `died`/`expired` record reads as no record — `scripts/command_run.py:579`); r2 the `ROUTED-UP: step 1` surface convention is decided in D6 and budgeted at § Cost but not enumerated in § Contract deltas or § Documentation landing sites; r3 § Cost lists `fabrik-review.md` twice (the surface convention and the hop citation — two edits, one file, one ticket); r4 D4's seven hop-site citations are assigned to no phase in § Cost's split (Phase A with the fragments); r5 § Rejected 5's per-round claim is an entailment of the loop-level Phase A figures; r6 the D6 heading does not name path (iii); r7 § Blast radius's deletion list is illustrative, not the count.
