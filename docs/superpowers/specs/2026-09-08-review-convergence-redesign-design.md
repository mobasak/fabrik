# Review convergence redesign — partitioned single pass, delta rounds, zero-CONFIRMED quiet round

Status: CONVERGED
Profile: delta
Owner: infra
Date: 2026-09-08
Ruling: D-203 (the operator's five rules, minted by the sibling hub session from ITS OWN twelve-round review — its `why` cell's series is that review's, not the D-191 review's — the cell says "twelve rounds" and lists fourteen values, the sibling's row to correct; this spec grounds the same rules on the D-191 review, the longer of the two). D-203 records the ruling and names what it supersedes; the ledger's one-row-per-superseded-decision convention still owes three mechanism rows — the D-048 exit rule, the D-191 seat mix, the D-186/D-188 review floor — minted at approval.

## In one line

A `/fabrik-review` or `/fabrik-repo-review` closes on a QUIET round, and quiet now means: the partitioned pass confirmed zero code or doc defects by execution. Refuted candidates never count. Every file is read once per round by exactly one model, chosen by what it is good at, and every round after the first reviews only the previous round's fix diff plus its callers.

## Intake Inventory

The conversation that invoked this spec (2026-09-08, this session) stated the items below. I1–I12 are the operator's own words (verbatim from the transcript; `[sic]` marks misspellings only, not missing apostrophes); I13–I19 are the agreed restatement of the operator's rulings that the invoking brief carried — R1–R5 were ruled in the 17:49 UTC turn and I10 in the 17:51 turn (20:49 and 20:51 local); I13 and I15–I19 are the brief's restatements of mechanism, grounded on those rulings and the frozen ledger, and the technical wording is the session's, recorded as D-203 — and I20 is the operator's own words again, from the 17:33 UTC turn. IN = covered at the named section.

| I# | Item (anchored) | Disposition | Where |
|---|---|---|---|
| I1 | "currently our reviews are taking too much time, even days, and we cant work proper work efficiently. we need to fix this." | IN | § Why this exists · § Goal |
| I2 | "what does this say? https://agentpatterns.ai/code-review/review-then-implement-loop/" | IN | § Grounding (G1) — the page's one-pass-then-escalate rule, and why this delta adopts its verification gate but not its single-pass cap |
| I3 | "what are the current best practises to use?" | IN | § Grounding (G1–G9) · § Chosen approach |
| I4 | "exit only quite [sic] round should stay." (R1) | IN | § The delta D1 · § Decisions taken R1 |
| I5 | "A refuted candidate counts as a fresh finding: this is wrong, it is definetly [sic] not." (R2) | IN | § The delta D1, D6 · § Decisions taken R2 |
| I6 | "… i keep saying you use more agents, i was thinking you are diving [sic] tasks amongst the agents according to their type, and in a single pass, they make a combined full pass. the way we do currently is wasteful." (R3) | IN | § The delta D2, D3 · § Decisions taken R3 |
| I7 | "… we have fable, opus, sonnet, haiku, all agents must be used efficiently and wisely not wastefully." (R4) | IN | § The delta D2, D4 · § Decisions taken R4 |
| I8 | "… while fixing, if you create new errors, they must be definetely [sic] fixed, in this case, we can review only the fix without making ful [sic] pass …" (R5) | IN | § The delta D5 · § Decisions taken R5 |
| I9 | "i want to switch to \"A review closes on a round with zero confirmed code or doc defects\" …" | IN | § The delta D1 · § Decisions taken R1/R2 |
| I10 | "… i want to see all these decisions in the created spec, do not skip a single thing/ good decisions." | IN | § Decisions taken — every ruling R1–R11 and every design decision DD1–DD13 has its own row |
| I11 | "i am planning to accept it and update /fabrik-review and /fabrik-repo-review" | IN | § The delta — both commands are the primary surfaces; § Documentation landing sites |
| I12 | "but first what will we lose/drop tell me" | IN | § Rejected alternatives (the redundancy trade, stated) · § Lifecycle (degradation) |
| I13 | The BLOCKED D-191 receipt flips to CONVERGED under the new rule once this ships (R11; the invoking brief's wording, not an operator turn) | IN (at round 19, not the announced 18 — D9 says why) | § The delta D9 (first application) |
| I14 | The seat-brief lessons of the D-191 review (R10: pin once, `git init` the pin, `KAIZEN_EVENTS_DIR` on every probe, restore inside one Bash call, never bare-grep, Python `time.sleep`, 15-min box) | IN | § The delta D8 |
| I15 | The NON-CONVERGENCE escalation stays as the only other exit (R6) | IN | § The delta D1 · § Decisions taken R6 |
| I16 | The seat budget still runs through `dispatch_headroom.py`, sized by slices (R7) | IN | § The delta D3 · § Decisions taken R7 |
| I17 | The consensus/critic rule: a one-seat, unreproduced candidate is dropped (R8) | IN | § The delta D6 · § Decisions taken R8 |
| I18 | `confirmed:` on the ledger row, `--confirmed` on `round`, backward compatibility for in-flight receipts (R9) | IN | § The delta D6, D7 · § Contract deltas |
| I19 | The pool stays OFF (D-181/D-182); D-190 pricing stays; no new dependency; red-first graders | IN | § Constraints digest (the standing rulings row) · § Cost |
| I20 | "what does this mean ? operator decision: adopt the zero-CONFIRMED convergence bar as a D-row (receipt flips to CONVERGED at round 18," (the operator's turn quotes the previous NEXT line and ends there) | IN | § Why this exists (the explanation that preceded the ruling) · § The delta D9 |

Intake: 20 items — 20 IN, 0 OUT-OF-SCOPE, 0 ASK. Every item derives from the operator's rulings this turn (D-203) or from the frozen ledger; no question cleared the bar. Denominator: 5 operator turns on 2026-09-08 between 17:33 and 17:51 UTC (20:33–20:51 local), re-derived author-blind by the review's intake seat; every turn has at least one row.

## Personas

**Primary persona, in the operator's own words:** *"currently our reviews are taking too much time, even days, and we cant work proper work efficiently."* — the OPERATOR who waits on a review to close before the next piece of work can start.

The primary's minimal loop, counted (the frozen STEP BUDGET downstream contracts meet or bump):

1. Invoke `/fabrik-review <surface>` (or `/fabrik-repo-review`).
2. Run the hygiene script on the surface and fix its hits; partition the surface; budget → `dispatch --seats` → the seats, in one breath; round 1 is one partitioned full pass, all seats in one message, 15-minute box.
3. The orchestrator adjudicates by execution and fixes what is CONFIRMED in this same round (fixes never carry over).
4. The hygiene script runs again at the round's close; the round is recorded (`round --confirmed <n>`).
5. The delta round: the hygiene script on the delta surface, then budget → stamp → the seats over the fix diff plus callers only.
6. A delta round confirms zero → the receipt flips to CONVERGED, the run closes.

Six steps; steps 3–5 repeat only while a delta round confirms something. Today the same loop is steps 2–4 repeated as full passes without a bound (18 times on the D-191 review).

Every duty the delta creates names its holder:

| Persona | Role in this delta | Duty |
|---|---|---|
| Operator | rules on the closing bar; reads the receipt; owns the D-rows a `by design` trade may cite | approves this spec; owns the D-rows |
| Orchestrator session (Fable, or Opus by name when Fable refuses) | partitions the surface, dispatches, adjudicates | EXECUTES every refutation and every confirmed reproduction (D6); never a finder |
| Opus seat | reviews the risky slices only | returns candidates with a concrete failure scenario |
| Sonnet seats | review the ordinary code and doc slices | same |
| Haiku seat (at most one) | one grep-shaped class the hygiene script cannot express, surface-wide, only when the brief names it | returns `file:line` hits; every scriptable class is the close-out script's (D4) |
| `check_review_coverage.py` (gate, fleet-synced) | grades the receipt | reads `confirmed:` as the exit counter; keeps the old rule for ledgers without it (D7) |
| `check_convergence.py` QUIET_PASS (gate) | grades plans/receipts for convergence claims | its quiet test follows D7's grammar |
| `command_run.py` (fleet-synced) | the run record | `round --confirmed <n>`; TERMINAL fires on confirmed == 0 with a class-ledger close (D7) |
| `dispatch_headroom.py` | the seat budget | sizes by SLICES, prints the partition mix (D3) |
| `check_review_hygiene.py` (new, gate-adjacent, fleet-synced) | the grep-shaped classes at every round's start and close and at the flip | its false-positive rate is measured from the receipts' disposition rows by infra (DD6); it errors only after graduation |
| `/fabrik-review-scoped` | the per-change review the Stop hook enforces | inherits the exit rule and the row grammar (D10); keeps its measured one-unit duplicate-brief floor |
| `quota_dashboard.py` board banner | prints the budget sentence | its D-191 sentence is replaced with the partition sentence (D7) |
| Sibling sessions | subtract the stamp | unchanged: budget → stamp → dispatch in one breath |
| The ~46 synced projects | run the same commands and gates | receive the change by the post-commit governance sync; in-flight receipts keep parsing (D7) |
| `/fabrik-execute-plan` | invokes `/fabrik-review` at phase boundaries | inherits the new exit; nothing else changes for it |

## Goal

Cut a review's wall-clock from days to about one full pass plus a few short delta rounds, without lowering what "reviewed" means: every confirmed defect is still fixed with a red-first guard, every class is still swept, and the exit is still a quiet round.

## Why this exists

Measured on this box, 2026-09-08, the `/fabrik-review` over D-191..D-194 (`docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md`):

| Measure | Value |
|---|---|
| Rounds | 18 (closed BLOCKED on the bar contradiction, not on a defect) |
| Wall-clock | 8.4 h from the record's open to its close (07:38 → 16:03 UTC, the run-record method); 7.8 h from the first round event (08:06) to the last (15:56); the span is TWO run records — 07:38:43→14:23:57 (rounds 1–14) and 14:30:25→16:03:19 (rounds 15–18, the record's own counter reset to 1) — with one `stop_allowed_quota_hold` event at 14:24:33 UTC in the 6.5-minute gap between them; the longest gap between consecutive rounds is 43 min, a round's own length |
| Seats per round | 11 in round 1, 10 in every round from 3 to 13, then 8, 7, 6, 6, 6 (round 2 reads 0 stamped while its finders cell lists 11 seats — the unstamped class that F292 named at round 12); ~3.3M input tokens per seat (F69/F104, 69.5M over 21 seats) — 20M–36M input tokens per round, mostly cached |
| Raw-candidate series | 24→16→27→15→17→18→23→38→28→16→14→15→13→16→8→14→12→11 — never quiet |
| Confirmed defects, rounds 15–18 | from the Opus seat and one Sonnet seat; the Haiku seats produced zero confirmed defects in rounds 15–18 |
| Fix residue | the confirmed defects of rounds 14–17 were each in the previous round's fix |
| Round 18 | 0 confirmed defects in the code under review (the Opus seat raised none there; F361, a grader defect, it co-raised), 11 raw candidates, and three FIXED rows — F361 (a grader whose red path leaked a file), F362 (a missing regression guard, mutation-proven), F363 (a doc defect) — which under D1 are confirmed grader and doc defects; not quiet under D-048, and not quiet under D1 either |

Three causes, each a rule rather than a defect in the code under review: (a) D-048 counts a refuted candidate as fresh, so a surface this size never produces a quiet round; (b) D-191 gives every unit a Sonnet seat AND a Haiku seat with an Opus seat over everything, so each file is read two or three times per round; (c) every round is a full pass, so a fix that touched four files costs a forty-minute sweep of ninety. The same shape elsewhere: brand-identity-creator's primary-text review ran 35 passes (their D-021/D-022, 2026-09-06); web-ecommerce-factory's T01 review ran 42 rounds without a quiet round (their D-105, 2026-09-04).

How the delta resolves exactly that: (a) becomes "quiet = zero CONFIRMED, executed"; (b) becomes a partition, one model per slice; (c) becomes delta rounds. Under the new rules the D-191 review would have closed around round 16 at roughly two fifths of the seat spend (a model, not a measurement): the last confirmed defects outside a fix chain landed in rounds 13–14 (F312, F317/F318 — pre-existing corpus and template defects — and F329–F331); rounds 15–18's 19 FIXED rows belonged to four fix chains — the seat dater and its graders (F336, F340, F344/F345, F347, F348, F352/F353, F354, F362), the wave/terminal text (F337, F346, F350), the caveat matrix (F339) and the events-pin grader (F349, F355, F361) — plus the receipt-tally hygiene row F338 and one fresh doc finding (F363); the rest were RECORDED or REFUTED. Under D5 each chain's residue is a delta round over a few files, so the model is round 1 (7 seats) plus ~15 delta rounds of 2–4 seats. "Residue" is this spec's classification, read off each F-row's cause cell, not a receipt tag.

## What exists today (grounded)

| Surface | Today | Anchor |
|---|---|---|
| `/fabrik-review` exit | "This pass found or fixed anything → you are structurally not done. Go back to Phase 1 and run a fresh, fully-independent finder round" | `commands/_sources/fabrik-review.md:364` |
| `/fabrik-review` completion claim | "only when the last row is `found: 0 · new: 0 · fixed: 0`" | `commands/_sources/fabrik-review.md:437` |
| `/fabrik-repo-review` exit | "You EXIT when, after that pass, every checklist row is adjudicated" — and its waves (round 14–16 of the D-191 review) | `commands/_sources/fabrik-repo-review.md:154`, `:64` |
| The seat rule (D-191) | "every unit gets one Sonnet breadth seat AND one Haiku mechanical seat … plus the Opus authoritative seat(s)" | `commands/_fragments/subagents-core.md:3` · `CLAUDE.md:355` · `.windsurf/rules/core/62-using-subagents.md:65` · the board banner's sentence "one Sonnet + one Haiku seat per unit plus the Opus authoritative seat(s)" at `scripts/sysadmin/quota_dashboard.py:1911-1912` |
| `/fabrik-review-scoped` | "3 seats (D-191: the Opus authoritative seat + one Sonnet + one Haiku over the whole diff …)" and the old `round --seats <n> --findings <n>` line | `commands/_sources/fabrik-review-scoped.md:68-70`, `:47` |
| The `new:` counter | "`new:` stays the stopped-learning signal"; the completion claim reads all three counters on the row | `commands/_sources/fabrik-review.md:435-437` |
| The three-seat floor (D-186/D-188) | "NEVER solo, never two — the FLOOR is three seats" | `commands/_fragments/subagents-core.md:3` |
| The seat budget | `full_mix()` = one Sonnet per unit + Haiku per unit (or `--mechanical`) + Opus per risky unit; `FLOOR = 3`; `trim()` | `scripts/sysadmin/dispatch_headroom.py:352`, `:42`, `:373`, `:404` |
| The gate's exit rule | "final ledger round raised N (a FRESH candidate counts even when refuted … D-048) — the exit round must be quiet" | `scripts/enforcement/check_review_coverage.py:584` |
| The ledger row grammar | `\| found: N \| fixed: M \|` cell-anchored; Pass-labelled lines parsed by `_unparsed_pass_lines` / `_ledger_shapes` | `scripts/enforcement/check_review_coverage.py:996`, `:1023`, `:1064` |
| The run record's round | `round --seats --findings --classes-swept --classes-new`; the round dict is appended at `rounds.append` and TERMINAL requires `bool(classes) and not open_c and findings == 0 and bool(last.get("swept"))`, read off `last = rounds[-1]`; the banner prints "found 0 new findings" and the round line prints `findings:` | `scripts/command_run.py:1546`, `:2006`, `:322` (comment at `:320`), `:312`, `:316` (the round line), `:329-331` (the banner) and `:331-334` (its "a scoped round never closes the loop" clause, which opens mid-line at `:331`) |
| The oscillation advisory | `PER_UNIT_ROUND_COMMANDS = {fabrik-execute-plan, fabrik-repo-review}` | `scripts/command_run.py:274` |
| The unstamped-seat nudge | counts seat transcripts newer than the round | `scripts/command_run.py:1976` |
| The run-record paragraph | "A round that sweeps every known class with 0 findings IS the no-op round" | `CLAUDE.md` § COMMAND RUN-RECORD |
| The prior exit ruling | D-048: `found: 0 · fixed: 0` canonical; a re-raise of a standing row is cited, never counted; a fresh candidate counts even when refuted | `docs/DECISIONS.md` D-048 |
| The receipt of the first application | BLOCKED at round 18 with a `## BLOCKED: spec contradiction` section | `docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md` § BLOCKED |

Every mechanism the Intake Inventory names maps to code or contract text that exists today (the 16 rows above); I1, I3, I12 and I14 are the operator's problem statement and lessons, which the ruling row D-203 carries. The profile trigger holds — this is a delta on existing engines.

## The delta

### D1 — Quiet is redefined; the exit stays a quiet round (R1, R2, R6)

- A round is QUIET when its pass (full or delta) CONFIRMED zero code or doc defects. `found:` keeps counting raw candidates (the seats' recall); `confirmed:` is the exit counter.
- CONFIRMED means EXECUTED: a probe on a pinned copy, a failing test, a mutation on a copy that the grader turns red, or — for a doc claim — a pinned read of the artifact the doc describes, quoted beside the sentence. A candidate the orchestrator cannot execute is RECORDED with the reason and does not count; a candidate executed and shown false is REFUTED and does not count; a candidate executed and shown TRUE that makes no code or doc claim (a measured prevalence figure, F365) is `RECORDED — measured (<why>)` and does not count; a candidate executed, reproduced and kept on purpose is `RECORDED — by design (<owning row>, round N)` — a deliberate trade whose owning row is a D-row or a receipt row adjudicated in an EARLIER round N (never the round being closed — otherwise the closing round could mint its own licence; the verdict carries N itself because the receipt's disposition table has no round column and 42 of the D-191 receipt's 366 F-rows carry no `round N` token in their prose (detector `round \d`; 35 carry no "round" substring at all)), listed in the receipt's residual section; it never enters U, and a `by design` row whose owning row is absent, or any of whose N is not smaller than the closing row's `Pass N`, is refused by name (V5); the verdict's grammar is `(<owning row>, round N[; <owning row>, round N]…)` for receipt rows — every N compared — and `(D-nnn)` with no round token for a D-row, which is earlier than any closing round by construction. Neither disposition reopens the loop. This supersedes D-048's "a fresh candidate counts even when refuted".
- The only other exit stays: `## BLOCKED: NON-CONVERGENCE` after three non-decreasing nonzero delta rounds, naming the suspected foundation error; and the three sanctioned BLOCKED cases. No round cap.
- "Quiet cannot be reached by relabelling": a candidate with a concrete failure scenario that the orchestrator did not execute may not be REFUTED — it is RECORDED as `unexecuted`, listed in the receipt's residual section, and the Pass Ledger row carries `unexecuted: U` as a fifth counter — U counts the CODE candidates recorded unexecuted; a doc candidate recorded unexecuted is named in the residual section and not counted (parsed by both grammars with the same stand-alone-token rule as `found:`; absent = 0). A closing round with `unexecuted` > 0 is not quiet (V5).

### D2 — One combined pass, partitioned by model (R3, R4)

- The surface is cut into DISJOINT slices. Each slice goes to exactly one seat, by kind:
  - **Opus** — the risky units only: concurrency and locks, record and file formats, fleet-synced surfaces (`scripts/enforcement/`, `scripts/command_run.py`, the hooks, `templates/governance/`), auth, schema, migrations, secrets handling.
  - **Sonnet** — every other code and doc unit.
  - **Haiku** — only a grep-shaped class the hygiene script (D4) cannot express, ONE seat for it across the whole surface, and only when the brief names it (a judgement-shaped inventory count); the scriptable classes — stale phrases, table and fence shape, dead symbols, `{{` residue — are the script's from round 1's start, never a seat.
  - **Fable** (the orchestrator; Opus by name when Fable refuses) — partitions, dispatches, adjudicates, executes every refutation and every confirmed reproduction. Never a finder.
- The union of the Opus and Sonnet slices IS the full pass, partitioned by FILE; a Haiku class seat, when the brief names one, is orthogonal to that partition — it sweeps ONE grep-shaped class across the whole surface, a mechanical read that judges no file's logic, exactly as the hygiene script does — and is the only seat that touches a file another seat owns. No file's LOGIC is read by two seats in one round of `/fabrik-review` or `/fabrik-repo-review`: a slice the Opus seat takes (a risky unit, or DD2's most-consequential slice on a surface with no risky unit) is carved OUT of the Sonnet allocation, never added to it. The authoritative second read that D-191 put over everything is replaced by the orchestrator's execution of every candidate (D6). (`/fabrik-review-scoped`'s one-unit duplicate-brief floor is a different, measured technique and stays — D10.)
- Sizing on the D-191 review's surface (a model, not a measurement): 3 risky units (Opus ×1 with all three, or ×3 when independent), 5 ordinary units (Sonnet ×5), the grep classes covered by the script from round 1's start (D4) with ONE Haiku seat for the judgement-shaped inventory class the script cannot express → 7 seats in round 1 (9 with three independent Opus slices), 2–4 in a delta round, against 10 in every round from 3 to 13 today (11 in round 1).

### D3 — The seat budget is sized by slices (R7, and the mechanism R3's partition runs on)

- `dispatch_headroom.py` gains `--slices opus=<n>,sonnet=<n>,haiku=<n>` (the partition the orchestrator computed) and prints `SEATS:` = min(Σ slices, the CLI cap, the box cap minus sibling seats, quota headroom), trimming Haiku first (its classes fold into the script), then extra Opus, then Sonnet, never below one Opus when a risky slice exists. `--units/--risky/--mechanical` stay for the commands that size by units (spec/plan grounding); `full_mix()` keeps serving them. The discriminator is explicit: `budget()` gains a keyword-only `slices=None` parameter appended AFTER `mechanical` (every caller is positional today — `dispatch_headroom.py:668/688/689`, `tests/test_quota_dashboard.py:3275`, 50 `dh.budget(` sites in 19 of the 31 tests of `tests/sysadmin/test_dispatch_headroom.py`); when `slices` is given, `FLOOR` stands down at every site that can RAISE the seat count or label it: `full_mix()`'s padding (`:368-369`), the `wanted < FLOOR` raise inside `budget()` (`:550-557`, whose own comment calls it unreachable BECAUSE `full_mix` pads — it becomes reachable and would silently re-raise a 2-seat partition to 3), the below-the-floor advisory (`:565`), and the `floor_granted` export (`:448-449`, which would label a slice-sized dispatch as floor-granted). `grep -n FLOOR` inside `budget()` (`:404-575`) at 2dec30c8 returns 17 lines: code reads at `:442, :446, :447, :471, :507, :537, :551, :557, :565`; label reads at `:454, :464, :554` (f-strings) and `:520` (a plain string naming the floor); comments at `:426, :439, :444, :450`. Under slices EVERY raise and EVERY label stands down — the raises `:551/:557` (with their reason at `:554`), `:565`, `full_mix()`'s `:368-369`, and the labels `:449` (`floor_granted`, exported as 0), `:451-455` ("floor granted … bounded by the floor"), `:464` (", never below the floor"), `:520` ("run the FLOOR"); the cap raises at `:442-447` (box), `:471` (unknown box), `:507` and `:537` (quota) are harmless for the count because `seats = min(caps.values())` (`:558`) includes `wanted`, so a 2-slice partition still prints 2 — V4 asserts the count AND the absence of every floor label. The `--json` payload's `floor` (`:716`) stays exported for the board and `box_caps_floored` (`:694`) reads false under slices, so the board never labels a slice-sized dispatch as floor-granted. The floor is DD2's; when `slices` is None the D-186/D-188 floor applies unchanged. CLI precedence: `--slices` wins; `--units` becomes optional and defaults to the slice count when `--slices` is given (it is `required=True` today, `:648`), so `--slices … --units 0` cannot print `SEATS: 0` through the "nothing to partition" branch (`:543-548`). The board's `_budget_probe` runs `--units 1` (`quota_dashboard.py:1860`) and keeps working; its banner sentence changes to the partition wording.
- The 15-minute seat time box stays. Budget → `dispatch --seats <n>` → the seats, in one breath, stays. The 25-minute sibling reservation stays.
- The floor under partition (re-deciding D-186/D-188 for tiny surfaces): every non-empty slice kind present in the surface gets its seat; a surface with no risky unit still dispatches one Opus seat over its most consequential slice; a one-file surface may be one Opus seat plus the close-out script. The measured "three seats on different angles" rule is retired for review loops — the third angle is the orchestrator's execution, not a third reader. (D-186/D-188 stay in force for grounding and adjudication surfaces: spec and plan grounding still run the researcher floor.)

### D4 — Most of Haiku's work becomes a script at close (R4)

- The grep-shaped classes that every review re-sweeps (stale phrases named in the brief, `{{` residue in rendered commands, receipt row shape and verdict words, fence parity, CHANGELOG entry shape, dead symbols by `grep -c`) move into `scripts/enforcement/check_review_hygiene.py`, run by the orchestrator at the START of every round on that round's surface (its hits are fixed before the seats go out, so the seats review those fixes as part of the delta) and again at the round's close, and by `check_review_coverage.py` at the flip. A hit fixed at a round's start is a confirmed doc defect of THAT round (counted); a round is quiet only if its closing run is silent — so the loop terminates on a silent script, never on an exempted one (DD13). A Haiku seat is dispatched — in round 1, or in a delta round whose fix diff raises one (D5) — only for a class the script cannot express (a judgement-shaped inventory), and only if that class is in the brief.
- Fire rate before it blocks: the script runs advisory for its first 20 receipts fleet-wide (the D-033 tier). The instrument is the receipts themselves: every hygiene hit the orchestrator adjudicates gets a disposition row (`FIXED` or `RECORDED — hygiene false positive: <why>`), and infra counts the false-positive rows against the hits over those 20 receipts; it graduates to a gate error only when the rate is below 5 % (the codeant.ai optimal target, G7). The feedback ledger (`~/.claude/state/command-feedback.jsonl`) carries only the four free-text fields and cannot measure this.

### D5 — Delta rounds (R5)

- Round 1 is the ONLY full partitioned pass. Every round ≥ 2 is a DELTA round: its surface is the previous round's fix diff plus its callers and callees (one hop, by symbol), plus any sibling commit that landed on the original surface since the last round.
- The same partition rule applies to the delta: risky hunks → Opus, the rest → Sonnet, grep classes → the script (or one Haiku seat if a brief-named class is not scriptable).
- A fix that creates a new defect is CONFIRMED in the next delta round and fixed; the loop continues until a delta round confirms zero. The class ledger persists across rounds; a delta round sweeps the classes its diff touches and CITES the classes it did not touch as standing-clean from the last full pass (the receipt says which).
- A round is never a re-scope: the brief for a delta round is the fixed class ledger applied to the delta surface.
- A delta round that dispatches NO finder seat may not be the closing round: the fix diff is the orchestrator's own work, and core/62 § Role separation binds — "The loop-closing round's FINDER pass runs in a context that did not author the artifact" (`.windsurf/rules/core/62-using-subagents.md:186`). The closing delta round always carries at least one fresh, non-authoring seat over the fix diff (Opus if any hunk is risky, else Sonnet); the hygiene script and the orchestrator's own execution never close a review alone.
- The delta surface is computed, not judged: the fix diff is `git diff <last round's commit>..HEAD -- <the review's surface>`; one hop of callers/callees is the set of files that reference a symbol the diff changed (`grep -rn '<symbol>' --include='*.py'`, or `serena` `find_referencing_symbols` where the language server is up), plus the tests that import the changed module; the brief lists that file set.

### D6 — The critic step and the consensus rule (R2, R8)

- The orchestrator holds the critic role: for every candidate a seat raises with a concrete failure scenario, the orchestrator EXECUTES it (probe, failing test, or mutation on a pinned copy) before it becomes CONFIRMED, and executes the refutation before it becomes REFUTED. Disagreement is evidence-grounded (G3, G3b): a refutation cites the executed command and its output in the receipt row. This is adjudication, which core/62 already places with the orchestrator — "Adjudication — decide/refute/merge — stays with the orchestrator … this rule governs who HUNTS last, never who adjudicates" (`.windsurf/rules/core/62-using-subagents.md:193-195`).
- A candidate raised by one seat and not reproduced by the orchestrator is RECORDED, never counted (R8). It is not silently dropped: the receipt row carries `RECORDED — unexecuted (<why>)` — parentheses, never a colon, so a reason that starts with a number ("3 attempts timed out") can never spell the `unexecuted: 3` token the ledger gate refuses; the "suspected-but-not-reproduced" channel the reproduce-before-report literature names (G2) is this row.
- A candidate the orchestrator reproduced and keeps on purpose is `RECORDED — by design (<owning row>, round N[; <owning row>, round N]…)` — or `(D-nnn)` with no round token when the owner is a D-row — (D1): every owning receipt row is from an earlier round N than the closing round's, and the gate refuses the row otherwise.
- The Opus authoritative re-read of every file is retired; the Opus seat reviews only its risky slices. The safety net for a Sonnet slice's blind spot is the executed adjudication, not a second reader.

### D7 — Contract and gate changes (R9)

- Receipt Pass Ledger row: `| Pass N | <finders> | found: F, new: N, confirmed: C, fixed: X, unexecuted: U | <method> |` — a PASS-HEADED row (the template's shape; it is parsed by the Pass grammar, never by the cell-anchored one), `confirmed:` between `new:` and `fixed:`; `new:` (candidates first raised that pass, the stopped-learning signal of `fabrik-review.md:435`) stays on the row as prose the graders do not parse. `fixed:` counts CONFIRMED defects fixed in that round, and a fix is applied in the round that confirmed it (never carried over), so `confirmed: 0` implies `fixed: 0`. BOTH row grammars in `check_review_coverage.py` accept the old pair and the new form: `_MEGA_ROW` (`:996`, the cell-anchored `| found: N | fixed: M |` row) gains an optional `| confirmed: C |` cell between the two and an optional `| unexecuted: U |` cell after `fixed:` — `^\s*\|.*?\|\s*found:\s*(\d+)\s*(?:\||$)(?:\s*confirmed:\s*(\d+)\s*(?:\||$))?\s*fixed:\s*(\d+)\s*(?:\||$)(?:\s*unexecuted:\s*(\d+)\s*(?:\||$))?` — every counter cell terminates on `\|` OR end-of-line, because GFM lets the closing pipe be omitted and a `\|`-only terminator makes a correct closing row without it either refused (its trailing counter uncaptured) or inert (matching neither grammar, so `founds[-1]` grades the PREVIOUS round — fail-open); over the 275 receipts the terminator change matches the same 2 rows as today's regex — and every `.group()` reader in `_ledger_shapes` (`:1064`) is renumbered; the Pass-labelled grammar (`_pass_counters`, `:1054`) gains `confirmed:` and `unexecuted:` tokens with the same stand-alone rule as `found:`/`fixed:` — case-sensitive like them, so a mis-cased literal never parses as a counter and falls to the token rule — the two NEW tokens only are scanned in the cell that carries `found:`, never the whole line (`found:`/`fixed:` keep today's whole-line scan at `:1057-1058`, on which 425 of the 535 corpus Pass rows depend — 267 carry their two counters in separate cells, `| **found: 0** | **fixed: 0** |` (detector: after `_strip_fences`, a `_pass_counters`-parsed PIPED row — one whose stripped line starts with `|` — whose `found:` and `fixed:` tokens sit in different `|`-delimited cells; a prose Pass line that uses `|` as a decorative delimiter is one cell, and a naive `split('|')` over every line reports 283 instead), and a further 158 hold both in one cell without a comma-joined run — 142 with a mid-dot as the separator between `found:` and `fixed:` (`found: 24 · new: 24 · fixed: 0`) and 16 prose lines with other separators (one of them carries mid-dots only in its finders list and a pipe between its counters) — so `fixed:` lies outside the comma-joined run either way; the remaining 110 are comma-joined in one cell, bold wrappers stripped before the run is read), so a NEW token anywhere else on the row is not a counter and refuses the row through the token rule; on a Pass row whose `found:` and `fixed:` sit in separate cells the new tokens go INSIDE the `found:` cell (`| **found: 0, confirmed: 0** | **fixed: 0** |`) and the refusal message says so for that shape (an un-bolded per-cell row `| found: 0 | confirmed: 0 | fixed: 0 |` resolves MEGA-first and needs no such rule); on a PROSE Pass line (`Pass 19: found: 0, confirmed: 3, fixed: 0`, parsed into `ordered` by `_ledger_shapes` like a table row — 19 of the 275 receipts end on one, and 25 of the corpus's 43 prose Pass rows carry a list marker — 0 in a `- | Pass … |` table-in-list shape, 8 of those 25 with a pipe elsewhere on the line (16 of the 43) — and the gate routes all 43 to the prose branch) the new tokens are read only inside the COUNTER RUN — the contiguous comma-joined `key: value` chain that BEGINS at `found:` (never extended backwards: a `confirmed:` written before `found:` is outside the run and refuses the line, as the cell-anchored path refuses `| confirmed: 3 | found: 0 |`), bounded by the cell's pipes on a table row and, on a prose line, by end-of-line or the first character that is not part of a comma-joined `key: value` item (a dash, an opening parenthesis, a bare word); on a separate-cell row the run is the `found:` cell alone, so `, fixed: X` is inside the run only when it sits in the same cell — and the canonical ORDER `found: F[, new: N][, confirmed: C], fixed: X[, unexecuted: U]` is enforced INSIDE the run in full (a run whose `found:`/`new:`/`confirmed:`/`fixed:`/`unexecuted:` items are not in that order refuses the row BY NAME on both grammars, never by returning `None` — on a prose line the displaced token lies inside the run where the token rule cannot reach it, so a `None` would leave the row inert and hand the exit to the previous round — measured at 0 refusals over the 535 corpus runs, 110 of which carry two or more items — symmetric with the cell-anchored path, which refuses the same displacement as a cell) — and a token on a Pass-headed prose line outside that run refuses the line exactly as a token in a method cell does (never a token in ordinary narrative — an unscoped reading would refuse 2 prose lines in the 275 receipts), so a narrated `— delta (confirmed: 0)` after `fixed: 0` can never become the exit counter (the whole-line-is-one-cell reading would flip `Pass 19: found: 5, fixed: 0 — delta (confirmed: 0)` from not-quiet to quiet); and a HEADER row — defined by CONTENT, at most ONE per contiguous pipe block: the block's FIRST pipe row that carries no numeric `found:`/`fixed:`/`confirmed:`/`unexecuted:` TOKEN in any cell (the block's first non-separator row and ONLY that row is the candidate — never a forward scan for the first token-free row, which would let a narrated `confirmed: 3 stand` below a quiet row hide as a header; a test on tokens, never on whether a grammar resolved the row — `| 19 | found: 0 | Confirmed: three | fixed: 0 |` carries numeric `found:`/`fixed:` tokens and is a data row however unresolved; a header names the vocabulary, `confirmed: C`, never a value; a counter-free row BELOW the header is a data row, so a token in it is refused and the T20 checklist cell keeps its refusal) (`| Pass | Finders | found: F, confirmed: C, fixed: X | Method |` names the vocabulary, never a value; a ledger whose table restarts as a SEPARATE contiguous pipe block gets its own header, but a second header-shaped row INSIDE one block is a data row and is refused — measured: 0 committed exemplars in the 51 blocks of the 275 receipts that hold a parsed Pass-headed ledger row — 52 counting the one cell-anchored ledger, 56 under the parsed-or-tokened in-scope test, 0 exemplars under every denominator — and the D-191 receipt's ledger is one block with one header at `:299` across both of its run records), so that no structural test can immunise a counter-bearing closing row: `_table_rows`'s separator-adjacency test (`:176-200`, its `run_initial` conjunct at `:182`/`:184`) is NOT reused here, because `_ledger_shapes` parses every `|`-leading line into `ordered` and reads `founds[-1]` from it (`:1117-1121`, `:581-584`), so a first data row followed by a stray separator would be "header" to one reader and the exit row to the other (the round-89 swallow the checker's own comment names at `:200-207`) — is exempt from the token rule, is not counted when testing whether the block parses, and never makes a block token-bearing (a header-and-separator-only ledger — the shape `review_receipt.py --init` writes before round 1 — is simply not yet a ledger, never a refused block), because an honest header naming the counter vocabulary (`| Pass | Finders | found: F, confirmed: C, fixed: X | Method |`) is not a ledger row and refusing it would re-commit the round-91 false positive the checker's own doctrine names (`:204-207`); and a SECOND token of either kind (`confirmed: 0, …, confirmed: 3`) returns `None` exactly as a second `found:` does (`:1057`), so the row falls to the token rule and is refused rather than read — an ABSENT optional token leaves the row parsed with that counter `None`, otherwise every old-grammar Pass row (535 of 535 in the 275 receipts) would go inert; `_ledger_shapes` returns a `(found, confirmed_or_None, fixed, unexecuted_or_None, line)` tuple to every reader (the ONE extraction contract) — and every reader is enumerated in the ticket, because the widening breaks positional unpacks: `founds = [str(f) for f, _x, _ln in ordered_rows]` at `:581` and `rows = [str(f) for f, _x, _ln in ordered_rows]` at `:1562`, the annotations at `:1066-1067`, `:1085`, `:1093-1096`, `rows = tables[0]` at `:1242`, and the two grammar consumers whose behaviour shifts when `_pass_counters` learns `confirmed:` — `_grammar_shaped` (`:804`) and the list-wrapped-row refusal (`:912-913`); `_unparsed_pass_lines` (`:1023`) recognises `confirmed:` so a half-written Pass-headed triple is named, not silently inert, and the token rule above covers every ledger row not resolved by `_pass_counters` that carries a new-grammar token; an OLD-grammar tail of a counter-bearing ledger that parses under neither grammar hands the exit to the LAST ROW THAT PARSES today — 1 of the 275 receipts, `docs/development/reviews/2026-09-02-external-services-chain-review.md` (ten inert rows, `Pass 62`–`Pass 71` at `:304-313`, prose counter cells), so `founds[-1]` is `Pass 61`'s `found: 66` and that receipt fails the exit today, fail-CLOSED; the fail-OPEN direction of the same class (an inert tail after a quiet row) has 0 exemplars in the 275 and is booked to the backlog on that basis beside the comma-cell shape; a ledger whose table parses no counter row at all (`docs/development/reviews/2026-08-26-plan-1-plan-lock-release-check-phase-A-review.md:21`, `| **exit** | — | **found: 0** | | |`, a receipt from before the Pass grammar) is a different pre-existing class — the exit check never runs — and is the gate's own denominator, not this spec's; and because `_ledger_shapes` resolves a `|`-leading line MEGA-first (`:1117-1119`) and already carries the raw line in every row tuple (`:1121`), a `|`-leading line of the Pass Ledger table — a contiguous table block that holds at least one row either grammar parsed OR carries a new-grammar token (a token-bearing block in which nothing parses is refused by name too, never treated as "not a ledger"; the disposition and checklist tables fall out of scope by this test alone — their verdicts are parenthetical per D6, so they carry no token and parse no counter row — and a disposition block that DOES carry a colon token is in scope and refused by name) — that carries a NEW-GRAMMAR TOKEN — `(?<![\w-])(confirmed|unexecuted)\s*:`, scanned case-INSENSITIVELY over every cell of a ledger row with ONE carve-out: an occurrence is a prose verdict label, not a token, when ALL of these hold — its literal is ALL-CAPS (`CONFIRMED:`/`UNEXECUTED:`, the corpus's label shape: all 30 exempt occurrences are `CONFIRMED`, 0 title-case); it sits outside the COUNTER CELL (the cell whose `found:` the resolving grammar read as a numeric counter — a method cell that merely narrates `found:` is not it); what follows the colon is not a VALUE — not empty, not text beginning with a digit (`3 defects`, `3rd time`), not `n/a` (a value is a counter, never a label; no number-word list, because an enumeration is never closed and an old-grammar row cannot hide a counter behind a word, see below); the resolving grammar read NO numeric `confirmed:`/`unexecuted:` counter on that row (a new-grammar row already has one, so a second literal of that kind is never a label; on such a row even `delta — CONFIRMED: see residual` is refused); and the row is either RESOLVED by a grammar or carries no numeric `found:`/`fixed:` token at all (a checklist or disposition row, or a Pass-headed ledger row whose counters are narrated so no numeric token exists — 7 corpus occurrences sit on such rows, 4 of them on narrated Pass rows; an UNRESOLVED row that does carry `found:`/`fixed:` tokens is a broken data row and every occurrence on it refuses by name) — 28 corpus method cells (30 occurrences, `CONFIRMED: the unparsed-row check …`, `HIGH/CONFIRMED: two MORE anti-cheat errors …`) carry that shape and stay exempt, 0 lost, 0 in a counter cell — while any other non-lowercase literal on the row (`Confirmed: 3` in its own cell, `Unexecuted: 2` after `fixed:`, `CONFIRMED: 3` narrated inside the counter run, the title-case `Confirmed: three`, `Unexecuted: n/a`, the half-typed `CONFIRMED: |`) is refused by name; the one shape this leaves standing — `| 19 | found: 0 | fixed: 0 | CONFIRMED: thirteen defects stand |` on an OLD-grammar row, exempt and read quiet through `found: 0` — is a prose lie in the same class as a false `found: 0` with no token at all, which no grammar can catch and the checker's own doctrine leaves to the operator's eye (`:205-210`, forgery out of the threat model); the token rule closes RELABELLING of a new-grammar counter, not lying in prose ("write the counter lowercase"), because a case-sensitive grammar cannot see it and the row would go inert or read quiet; measured at 0 committed rows; the optional whitespace before the colon closes `unexecuted : 2` reading quiet at the cost of ONE committed hub row, named below), any value or none, so `confirmed: three`, `unexecuted: n/a` and the half-typed `confirmed: |` are all tokens, refused, never inert — anywhere in the line, of which `_MEGA_ROW` did not consume EVERY occurrence as a numeric counter (no match at all, or any occurrence whose span — from the token literal to the end of its `(\d+)` group — the regex did not consume) is REFUSED by name, with ONE exemption, per OCCURRENCE not per row — on a row RESOLVED by `_pass_counters` (i.e. one `_MEGA_ROW` did not match, `:1117-1119`; the literal "parseable by the Pass grammar" is NOT the test, because a Pass-headed cell-anchored row is parseable by both and resolves MEGA-first) only the tokens INSIDE the counter cell that `_pass_counters` read as numeric counters are exempt; a bare-colon or non-numeric token inside that cell, a token standing as a counter in any other cell (`| confirmed: 3 |`, `| unexecuted: 2 |`, bold- or period-wrapped) and a token narrated in a method cell (`delta (confirmed: 3)`) all refuse the row — symmetric with the cell-anchored path, so the rule is ONE sentence for both grammars: every occurrence of the token must be a numeric counter the resolving grammar read, or the row is refused; and the checker's notion of a line is made GFM's before any of this runs — `_strip_fences` replaces the Unicode line-ish characters Python's `splitlines()` breaks on and GFM does not (`U+2028`, `U+2029`, `\x0b`, `\x0c`, `\x1c`–`\x1e`, `\x85`) with a space and deletes the zero-width characters `U+200B`–`U+200D`, `U+FEFF` and `U+00AD` (a `confirmed\u200b: 3` renders as `confirmed: 3` and would parse as no counter at all — 0 corpus rows carry one; the single `U+FEFF` in the corpus is prose about a BOM bug), because a row split at a `U+2028` leaves its `confirmed: 3` fragment on a non-`|`-leading line the token rule never sees while the `|`-leading half reads old-grammar quiet (one such character exists in the corpus today, `docs/development/reviews/2026-09-01-mail-handling-enforcement-review.md`); a Pass-headed row that resolved MEGA-first (`:1117-1119`) is therefore covered, so `| Pass 19 | opus×1 | found: 0 | fixed: 0 | confirmed: 3 |` is refused, not read quiet through the old rule: the test is on the TOKEN, not on a cell's shape, so a displaced counter (before `found:`, between `confirmed:` and `fixed:`, after a method cell, in the trailing segment GFM lets stand without a closing pipe), a bold-wrapped `**confirmed: 3**`, a punctuated `unexecuted: 2.` and a comma-joined triple without its `Pass` head are ONE case, and no row carrying the token can go inert and hand the exit to the previous round (`founds[-1]`); a method cell narrating "delta (confirmed: 0)" is refused too, and that is affordable because the token rule's fire rate over the 275 committed receipts at 2dec30c8 is ONE row — `docs/development/reviews/2026-08-31-plan-1-manifesto-command-pass-T20-fabrik-release-review.md:15`, a checklist cell reading `(verifier confirmed :63; :47-53)` inside a table no grammar parses, refused by name, which the ticket repairs in the same commit (measured over every `|`-leading line, Pass-headed rows and every table included, and 0 of the 43 prose Pass rows: 0 rows for the token without the whitespace tolerance, 1 for the adopted token, 28 and 29 ROWS respectively under `re.I` — 30 occurrences on the 28, 31 on the 29, the 31st being the refused T20 cell), so sync day breaks one hub row, fixed in-commit; the refusal message names the repair per path ("cell-anchored: write `| confirmed: C |` between `found:` and `fixed:`, one counter per cell, no `new:` cell, no wrapper; Pass row: write `confirmed:` between `new:` and `fixed:` inside the counter cell — inside the `found:` cell when `found:` and `fixed:` sit in separate cells") — the cell-anchored regex deliberately has NO `new:` slot, because an optional `new:` cell would make it match the 260 Pass-headed rows that carry `| found: F | new: N | fixed: X |` cells today and flip them from `_pass_counters` to MEGA-first resolution (measured: 262 matches against 2); V2 carries the fixtures — on BOTH resolution paths, inside the MEGA branch and as a scan of the ledger table's `|`-leading lines `_MEGA_ROW` did not match; rows that only the Pass grammar parses (the comma-cell shape `_MEGA_ROW` never matches) carry the token inside their counter cell by design and are never refused for it, while a Pass-headed row written in the cell-anchored shape resolves MEGA-first (`:1117-1119`) and IS refused when mis-ordered, which is the desired behaviour. A non-Pass-headed row whose counters sit in ONE comma-separated cell matches neither grammar today and stays inert — that is a pre-existing shape this delta does NOT change: 22 of the 275 committed receipts at 2dec30c8 carry such rows — all 22 one identical row family (`| 2 — closing re-derivation sweep | found: 0, fixed: 0 … |` at `:26`) from the 2026-08-31 manifesto-command-pass tickets T12–T33 (detector: a `|`-leading line outside a Pass-headed table whose cell begins `found: N, [new: N, ]fixed: M`; 21 of the rows sit at `:26`, T22's at `:32`; a bold-wrapped variant `**found: N, fixed: M**` is a separate shape (detector `\*\*found:\s*\d+\s*,\s*(?:new:\s*\d+\s*,\s*)?fixed:\s*\d+.*?\*\*`) measured at 15 lines in 14 of the 275 receipts, of which only 2 are non-Pass-headed table rows — `docs/development/reviews/2026-08-11-plan-2-stalled-midstream-resume-review.md:35` and `docs/development/reviews/2026-08-12-plan-2-agent-roles-wiring-review.md:32` — the other 13 being prose) — and a naming rule would fail them on sync day against DD4; it goes to the backlog with that fire rate (24 non-Pass-headed table rows across both shapes, 22 + 2 — the bold family's 13 prose lines are not ledger rows), to be named only after the hygiene script (D4) measures it. The cell-anchored shape D7 widens is itself rare: 2 `_MEGA_ROW`-matching rows in 36,298 lines (`wc -l`; 36,299 by `splitlines()`, the one `U+2028`) across the 275 receipts (`docs/development/reviews/2026-08-27-mega-umbrella-sso-validation-review.md:7-8`), 0 refused under the new rule — DD4's sync-day claim is measured hub-side (0 of 275); the ~46 projects' receipts are not in this tree and are measured by the ticket's dry run before the sync. Executed at review time: a non-Pass-headed cell-anchored triple parses under NEITHER grammar today and would go inert (the fail-open direction the file's round-13 comment names), a mis-ordered `confirmed:` cell would read quiet through the pair grammar, and D9's Pass-headed comma row parses under the Pass grammar only — which is why both grammars and the refusal change together.
- `check_review_coverage.py` exit rule, evaluated on the FINAL ledger row only (`founds[-1]`, `:579-584`, as today): if that row carries `confirmed:` under EITHER grammar — the cell-anchored group 2 or the Pass grammar's token — (the ONLY selector of the new rule — an `unexecuted:` cell on a row with no `confirmed:` is a malformed row, refused by name through a SEPARATE counter rule that binds BOTH grammars — on the cell-anchored path group 4 captured that token, so the token rule cannot see it, and the rule is "group 4 captured while group 2 did not ⇒ refuse"; on the Pass path an `unexecuted:` token with no `confirmed:` token in the same counter cell refuses the row (`| Pass 19 | opus×1 | found: 0, fixed: 0, unexecuted: 2 | m |` is refused, never old-grammar quiet) — because `| u | x | found: 0 | fixed: 0 | unexecuted: 2 |` would otherwise read as old-grammar quiet with two unexecuted code candidates standing; a refusal always preempts the quiet computation), quiet = `confirmed: 0` and `fixed: 0` and `unexecuted:` absent or 0 — `fixed:` counts CONFIRMED defects fixed in that round, so a quiet round's `fixed:` is 0 by definition, and `unexecuted:` counts the code candidates recorded unexecuted (D1); hygiene edits adjudicated `RECORDED — hygiene false positive` (DD13) are noted in the method cell, never counted — a hygiene hit FIXED at the round's start is a confirmed doc defect of that round and IS counted (D4); if it carries no `confirmed:` (an in-flight receipt written under the old grammar, or a review whose early rows are old-grammar and whose last row is new — only the last row decides), the old rule stands — `found: 0`. The D-048 clause in the error text is replaced by the D-203 clause. `check_convergence.py`'s `QUIET_PASS` (`:200`, today `found:\s*0\b[^\n]*?fixed:\s*0\b`) becomes `found:\s*0\b(?![^\n]*(?<![\w-])(?:confirmed\s*:\s*\d|unexecuted\s*:\s*\d*[1-9]))[^\n]*?fixed:\s*0\b|confirmed\s*:\s*0+\b(?![^\n]*(?<![\w-])unexecuted\s*:\s*\d*[1-9])[^\n]*?fixed:\s*0+\b` (`0+` so a zero-padded `confirmed: 00` reads the same at both gates) — the old pair applies only to a row that carries no NUMERIC `confirmed:` counter and no `unexecuted:` above zero (otherwise `found: 0, confirmed: 3` or `found: 0, fixed: 0, unexecuted: 2` would read quiet at this gate and not at the coverage gate; the lookaheads are anchored to a digit so a prose `CONFIRMED: see residual` label on an honest old-grammar row keeps its quiet match — this gate runs `re.I` and a label-killed match would fail a genuinely converged receipt, the zero-false-positive promise the file makes at `:186-189` — and they carry the same `\s*` before the colon as the coverage token, so `unexecuted : 2` cannot read quiet here while refused there), and a stated `unexecuted:` above zero, zero-padded or not (`01`), keeps the two gates in step — DD9's one definition of quiet; measured: the set of receipts quiet under today's regex and under this one is identical over the 275 (166 and 166, 0 flips); this gate stays a PRESENCE test (`:190-199`, "evidence presence, not truth"), so a narrated `confirmed: 0` in a method cell can satisfy it while the coverage gate refuses the row — the coverage gate is the depth, so D9's row reads quiet there too (V13).
- `command_run.py round` gains `--confirmed <n>` (default: None = not stated), stored on the round dict (`rounds.append`, `:2006`) and on the round event. The TERMINAL verdict, evaluated on `last = rounds[-1]` (`:312`) as today, fires when the round swept classes (`swept` non-empty), no class is open, and — when the LAST round states `confirmed` — `confirmed == 0`; when the last round does not state it, the old `findings == 0` test stands, and `round` WARNS when `--confirmed` is absent while `--findings > 0` under a command whose earlier rounds stated it. The round line (`:316`, `findings:`) and the TERMINAL banner (`:329-331`, "found 0 new findings") print `confirmed:` beside `findings:` so the tool never states a false count; the banner's continuation at `:331-334` ("IF this round was a FULL fresh sweep (never a round scoped to the last fixes) … a scoped round never closes the loop; run the closing full sweep first") is replaced by the delta-round rule — a delta round closes when it carried a fresh non-authoring finder seat and its receipt cites the standing-clean classes from the last full pass (D5, DD10). The oscillation advisory reads the `confirmed` series when every round states it, the `findings` series otherwise; the close-out `FEEDBACK:` trend (`rounds <n> (<trend>)`) shows the `confirmed` series when present.
- The seats-declared grader and the unstamped-seat nudge are unchanged.
- Both commands' text, `/fabrik-review-scoped` (D10), the subagents fragment, core/62 § Dispatch policy, the CLAUDE.md fan-out bullet and § COMMAND RUN-RECORD paragraph, their `templates/governance/CLAUDE.md` mirrors, and the board banner sentence at `scripts/sysadmin/quota_dashboard.py:1911-1912` ("one Sonnet + one Haiku seat per unit plus the Opus authoritative seat(s)"; + `docs/workstation/quota-dashboard.md`) carry the partition rule and the new exit; the D-191 sentence is replaced everywhere it renders or prints, in ALL THREE of its wordings — the fragment's "one Sonnet breadth seat AND one Haiku mechanical seat", the script's "one Sonnet + one Haiku seat per unit" (two adjacent string literals at `:1911-1912`, invisible to a line grep) and the rotation doc's "one Sonnet breadth seat and one Haiku mechanical seat, plus one Opus authoritative seat per risky unit" (`docs/workstation/claude-account-rotation.md:227-228`, wrapped across a line break) — and in `dispatch_headroom.py` itself, the file D3 edits: three docstrings (`:15` the module's, `:353` `full_mix()`'s, `:579` `_mix_story()`'s — the last quotes the first draft's mistake and is rewritten to describe it without the retired wording) and two PRINTED sentences (`:602`, "Haiku mechanical seat(s) — one per unit"; `:606`, "one Sonnet breadth seat per unit"). The units-sized mix stays live for grounding and adjudication commands (D3), so those five are not deleted: each is rewritten to name its scope ("for a units-sized grounding surface") beside the mix, and V7 asserts the scope words are present and the review-loop wording absent — verified case-insensitively over LINE-JOINED text of the rendered corpus, `scripts/**/*.py` and `docs/workstation/`, and on the rendered banner with a positive control (V7), never a bare line grep and never a count (at 2dec30c8: 1 fragment reaching the 20 of 36 sources that carry the `{{include:subagents-core}}` directive — the assembler's EXTRACT table names the fragment at 11 command keys, a subset of those 20, and its PARAMS table at 20 (set-equal to the include sites), so the rendered reach is exactly 20 — 0 sources hard-coding it, 6 script sites across two scripts, 1 doc passage). `fabrik-review.md:437`'s completion claim reads the new row and DROPS its `new: 0` conjunct — `new:` is prose (the stopped-learning signal), and a delta round that confirms zero closes whatever it raised fresh and refuted.

### D8 — The seat-brief template carries the D-191 review's lessons (R10)

Every finder brief the two commands emit states: pin dirs are created ONCE before the first dispatch and never touched while seats run; `git init` inside a `git archive` pin (a bare pin fails one dispatch_headroom test through `_repo_root()`); every `command_run.py` probe sets `COMMAND_RUN_DIR`, `COMMAND_RUN_TRANSCRIPT` and `KAIZEN_EVENTS_DIR` (F349: tests wrote fabricated rounds under the live sid until the conftest pin); every mutation is applied, tested and restored inside ONE Bash call with an asserted restore (a `trap` across calls is unreliable); never bare-grep a tracked path (`git show <sha>:<path>`); Python `time.sleep` in fixture scripts, never a foreground shell sleep; a quote verified against a session transcript filters out the Stop-hook feedback, the skill-invocation payloads and the re-invocation notice ("the skill instructions were previously loaded") that also arrive as `type: user` (a brief's ARGUMENTS text is not the operator's words) and prefilters the raw line by timestamp before parsing (a 793 MB session file parses in under a second that way, ~40 s otherwise); the Read tool truncates a long file on a token cap with no marker — read in offset pages and confirm the last line; HARD TIME BOX 15 minutes; report `MACHINERY:` last.

### D9 — First application (R11)

The D-191 receipt (`docs/development/reviews/2026-09-08-box-bound-seats-d191-review.md`, BLOCKED at round 18) is the first review run under the new rules: round 18 confirmed three defects by execution (F361–F363, a grader, a missing guard and a doc), so under D1 it is NOT the quiet round — one DELTA round over that round's fix diff (the two test files and the protocol doc, plus callers) runs in the commit that ships D7, with the partition of D2 and a fresh non-authoring seat (DD10); if it confirms zero the receipt flips to CONVERGED at round 19. R11 asked for the flip "under the new rule once this ships"; the new rule applied honestly needs that one round, because reaching quiet by reclassifying the three fixes as hygiene is the relabelling D1 forbids. Round 18's Pass Ledger row is re-adjudicated, not merely re-grammared: its method cell ("0 CONFIRMED code/doc defects … the 11 are grader-hardening, doc and refuted mechanical candidates") is rewritten to "3 CONFIRMED — F361 a grader whose red path leaked a file (observed), F362 a missing guard (mutation-proven), F363 a doc row read against the code — 9 fresh candidates: 3 confirmed and fixed, 4 refuted, 2 recorded — one by design, one a measured prevalence figure", and its counters to the new grammar (`| Pass 18 | <finders> | found: 11, new: 9, confirmed: 3, fixed: 3, unexecuted: 0 | <method> |` — re-derived from the receipt's rows F361–F366 at 69f01b92: three confirmed and fixed (F361–F363), one `RECORDED — by design (F342, round 15; F358, round 17)` (F364 — reproduced, the fail-toward-counting trade; its cell's word "accepted" is retired in the same rewrite), one `RECORDED — measured` (F365 — a prevalence figure, 0 two-stamp lines in 932,029, that makes no code claim; it never enters U, which counts only unexecuted code candidates), and four refuted claims bundled in F366 — the fourth, the `## Gate` block filled at the flip, is a fresh claim about a block no standing row adjudicated (F319 covers the Coverage Checklist and the `IN ROUND 1` heading and supplied the refutation's REASON, not the row), so `unexecuted: 0`; `new: 9` because two of the eleven raw candidates were re-raises folded into those rows' cells rather than rowed — F342/F358 in F364's cause cell and F356's subject (the giant-line chunk stamp) inside F365, whose cell gains "(F356 re-raised)" in the same rewrite so the fold is checkable — so the receipt rows nine while `--findings` counted eleven, the divergence F363 documents — a Pass-headed row, which parses under the Pass grammar today and after D7 ONLY with its literal `Pass 18` head; the cell-anchored grammar never applies to a Pass-headed comma row, and the same counters without the head match neither grammar and would make the row inert), round 19's row is written by the delta round, and the `## BLOCKED: spec contradiction` section is closed by a one-line pointer to D-203 and this spec.

### D10 — `/fabrik-review-scoped` inherits the exit and the grammar, keeps its floor (a consequence of R9 and I11: one gate, one grammar, so the third review command cannot keep the old exit)

The Stop-hook-enforced per-change review (`commands/_sources/fabrik-review-scoped.md`) writes receipts the same gate grades, so D1, D6 and D7 bind it: quiet = zero CONFIRMED, executed; refuted never counts; the row carries `confirmed:`. Its seat rule is different by design and stays: a one-unit surface gets three seats on ONE brief (the measured duplicate-brief technique of core/62:65 — 1 seat found 0, 3 found 0/5/0), because there is nothing to partition; its text drops the D-191 sentence at `:68-70` and adopts the delta-round rule for its fixes.

## Chosen approach (and why it is the lean one)

One model per slice, execution as the critic, delta rounds — the "reviewer + critic, three agents beat five" shape (G3) with the reproduce-before-report gate (G2) held by the orchestrator, diff-based re-review after a fix (G6), and hard resource ceilings per seat (G8). It is the leanest of the alternatives in § Rejected alternatives because it changes text in two commands, one fragment, one rule pack, one contract paragraph, two gate constants and two script arguments, adds one script, and retires two overlapping seat kinds; it adds no dependency and no new mechanism beyond a counter.

Walked against the owner's five criteria (a narrative per criterion, the same shape every delta spec here uses): quality first — the executed-critic rule is stricter than today's argued refutations; total cost of ownership — seat spend drops to about two fifths and rounds after the first to minutes; speed to ship — corpus render plus the post-commit sync; easy to maintain — one grammar, one counter; set and forget — the hygiene script replaces recurring Haiku seats.

The six reality-challenges: nothing paid where free exists (native seats only); nothing complex where simple exists (a counter and a partition, no new orchestration layer); consume before build (the existing `dispatch_headroom.py`, `command_run.py` and gate grammars are extended, not replaced); low maintenance (fewer seats, one script); compatible with Fabrik infra (fleet-synced surfaces change together, render → `--check` → commit); no duplicate — no existing project or service is a review engine.

## Rejected alternatives

Written in full, as are § Constraints digest and § Lifecycle (the delta profile's one-line collapse is waived because this delta rewrites a pack-relevant surface, core/62, and the operator asked to see every decision — I10); § fabrik-lib verdict and § Shape / infra implications stay one line.

| Alternative | Why rejected |
|---|---|
| Keep D-048's rule and add a round cap (3 interactive, per G5) | The operator ruled the quiet-round exit stays and refuted candidates do not count (R1, R2, R6); a cap alone leaves the counting defect and ends reviews by fiat |
| Keep the overlapping seats (Sonnet + Haiku per unit + Opus over all) and only change the counter | The operator called the overlap wasteful (R3); the measured marginal gain of an extra reader falls monotonically (G4) and the D-191 Haiku seats confirmed nothing in rounds 15–18 |
| A one-pass automated fix then escalate to a human (the agentpatterns loop, G1) | That pattern's fixer is a coding agent editing a human's PR; ours is the orchestrator fixing confirmed defects with red-first graders, and the operator ruled a fix's residue must be fixed (R5). Adopted from it: the verification gate and "escalate rather than loop" as the NON-CONVERGENCE exit |
| Majority vote across seats (a finding counts only when two seats raise it) | Under partition no two seats read the same file's logic, so a vote is impossible by construction; execution by the orchestrator (R8, D6) is the stronger filter and needs no redundancy |
| Full re-sweep after every fix (today) | Fix residue is real but local; the delta round covers the fix plus callers (G6) at a fraction of the cost — 2–4 seats over a few files against 7–9 over the whole surface, minutes against forty; the class ledger persists so nothing is re-scoped |
| A new "review orchestrator" service or a second script driving the loop | Over-engineering: the loop already lives in the command text and the run record; the delta is text, a counter and a partition |
| Drop the Haiku tier entirely | The operator ruled all four models are used where they pay (R4); the judgement-shaped inventory classes are Haiku's lane, in round 1 or in a delta round that raises one, and every scriptable class is the script's from round 1's start |
| Keep argued refutations (a seat's reasoning suffices) | Ten reviewers unanimously endorsed a non-existent bug until one empirical test killed it (G2b); refutation must be executed |
| A severity-weighted exit (only CRITICAL/HIGH confirmed defects block; the literature classifies findings by severity, G5) | The operator ruled zero CONFIRMED code or doc defects of ANY severity (R1); the FIX DIRECTIVE fixes a confirmed defect at its root whatever its size, and a severity filter would ship a known defect — severity stays a triage label in the receipt, never an exit knob |
| Majority vote across passes ("issues flagged in only one pass are discarded as noise", G5) | The same reason as the vote across seats: under partition and delta rounds no two passes read the same file's logic, so the noise filter is the executed adjudication (D6), not a second pass |

What is DROPPED, stated (I12): the automatic second look at a file by a second reader. In 18 rounds the second reader added no confirmed defect the executed adjudication would have missed; the delta accepts that loss in exchange for executed refutations and delta rounds. The operator was told this before ruling.

## Contract deltas

- **Receipt grammar** (`docs/development/reviews/*.md`, fleet-wide): Pass Ledger rows carry `found: F, new: N, confirmed: C, fixed: X, unexecuted: U`; the Coverage Checklist and disposition rows gain the verdicts `RECORDED — unexecuted (<why>)` for unreproduced candidates `RECORDED — by design (<owning row>, round N[; <owning row>, round N]…)` — or `(D-nnn)` for a D-row owner — for reproduced-and-kept trades, and `RECORDED — measured (<why>)` for an executed measurement that makes no claim; the receipt gains a residual section listing all three. Old rows keep parsing.
- **Run record** (`~/.claude/state/command-runs/*.json`): a round dict gains `confirmed` (int or absent); the round event gains `confirmed`.
- **`dispatch_headroom.py` CLI**: `--slices opus=N,sonnet=N,haiku=N`; `--json` gains `slices` and `mix_by_slice`.
- **Command text**: `/fabrik-review` Phase 1 (partition), Phase 2 (execute every candidate), Phase 4 (delta round; quiet = confirmed 0; the completion row at `:437`); `/fabrik-repo-review` Phase 1 (waves become slices; a wave's reservation close stays — its round is still a `--seats/--findings` close with no classes, which is why `PER_UNIT_ROUND_COMMANDS` keeps the command: the oscillation advisory would read the per-slice reservation rounds as a series), Phase 4 (delta rounds; the certification pass is the class-ledger close); `/fabrik-review-scoped` (D10); the subagents fragment; core/62 § Dispatch policy (and the `:65` floor sentence, which gains "for `/fabrik-review-scoped` and the grounding commands"); the CLAUDE.md fan-out bullet and § COMMAND RUN-RECORD paragraph; `templates/governance/CLAUDE.md` mirror of both; the board banner (`quota_dashboard.py:1911-1912`, `docs/workstation/quota-dashboard.md`); `docs/workstation/claude-account-rotation.md` § 223-239 (the `dispatch_headroom.py` CLI reference that the script's `# AFTER-EDIT:` header couples — `--slices`, the optional `--units`, the floor rule).
- No data contract, UI contract or `shape:` change — internal machinery.

## Cost

- Build: ~10 files of text, two scripts (`command_run.py`, `dispatch_headroom.py`) with small argument additions, two gates (`check_review_coverage.py` — both row grammars, the `_ledger_shapes` tuple shape and its readers, the exit rule; `check_convergence.py` QUIET_PASS) with a grammar extension, one new script (`check_review_hygiene.py`), one script string (`quota_dashboard.py`), ~14 red-first tests. D-190 pricing (haiku 1× · sonnet 2× · opus 5× · fable 10×) stays the cost unit `dispatch_headroom.py` prints. Roughly one plan of five tickets; `Profile: small` per D-169 is plausible for the plan and the plan decides.
- Run: per review, seats per round fall from 10 (11 in round 1) to 7 in round 1 (6 when no non-scriptable class is briefed, 9 with three independent Opus slices) and 2–4 in delta rounds; on the D-191 surface the modelled total is round 1 at 7 seats plus ~15 delta rounds at ~4 — 67 seats against the 165 the receipt's Pass Ledger records (11 + 11 real in round 2 + 10 × 11 + 8 + 7 + 6 + 6 + 6), about two fifths of the seat spend (67/165 = 0.41), and delta rounds of minutes instead of forty. Measured after the first three reviews under the new rules (Lifecycle).

## Validation

| V# | Behaviour | Proof |
|---|---|---|
| V1 | A closing round with `confirmed: 0` and a swept class ledger reads quiet; a non-zero `found:` on that row does not fail the gate | `check_review_coverage.py` test: red-first on a fixture shaped like the D-191 receipt's round-19 row (`found: N, new: N, confirmed: 0, fixed: 0`) |
| V2 | A ledger without `confirmed:` keeps the old `found: 0` rule; a ledger row not resolved by `_pass_counters` that carries a `confirmed:`/`unexecuted:` token `_MEGA_ROW` did not capture as a numeric counter is refused by name, never read as quiet or left inert | test, red-first on each row (every row carries the leading cell every real row has — without it `_MEGA_ROW` never matches): an old-grammar receipt with a non-quiet last row still fails, a quiet one passes. REFUSED by the token rule: `\| 1 \| found: 0 \| fixed: 0 \| confirmed: 3 \|` (trailing counter), `\| 1 \| found: 0 \| fixed: 0 \| confirmed: 0 \|` (terse trailing counter — the message names the repair), `\| 1 \| found: 3 \| fixed: 3 \| delta (confirmed: 0) \|` (a prose method cell — 0 such rows in the 275 committed receipts), `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| method: delta \| unexecuted: 2 \|` (displaced after a method cell), `\| 1 \| confirmed: 3 \| found: 0 \| fixed: 0 \|` and `\| 1 \| unexecuted: 2 \| found: 0 \| fixed: 0 \|` (before `found:`), `\| 19 \| found: 0 \| confirmed: 0 \| unexecuted: 0 \| fixed: 0 \|` (displaced between), `\| 19 \| found: 0 \| fixed: 0 \| confirmed: 3` (no closing pipe, trailing segment), `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| **unexecuted: 2** \|` (bold wrapper), `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| unexecuted: 2. \|` (trailing punctuation), `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| unexecuted : 2 \|` (whitespace before the colon), `\| Pass 19 \| opus×1 \| found: 0, Confirmed: 3, fixed: 0 \| m \|`, `\| Pass 19 \| opus×1 \| found: 0, fixed: 0 — narrated CONFIRMED: 3 still stand \| m \|`, `\| 19 \| found: 0 \| Confirmed: 3 \| fixed: 0 \|` and `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| Unexecuted: 2 \|` (a non-lowercase literal followed by a digit, anywhere on the row — refused by name; `\| … \| delta — CONFIRMED: see residual \|` is a prose label on an OLD-grammar row, not refused — the same cell on a row carrying a numeric `confirmed:` counter is refused by name), a closing row `\| Pass 19 \| opus×1 \| found: 0, fixed: 0 \| delta (confirmed: 3) \|` followed by a stray separator line, standalone as the ledger's first row or embedded under a real header (refused either way — a counter-bearing row is never a header), `\| 19 \| found: 0 \| Confirmed: three \| fixed: 0 \|` and `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| Unexecuted: n/a \|` (mis-cased and non-numeric — refused), `\| 19 \| found: 0 \| fixed: 0 \| Confirmed: 3 defects stand \|`, `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| Unexecuted: 2 standing \|`, `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| Unexecuted: two standing \|` and `\| 19 \| found: 0 \| fixed: 0 \| CONFIRMED: \|` (a title-case literal, a literal on a row that carries a numeric new-grammar counter, or a value after the colon — refused, whatever follows); NOT refused, and named as out of the threat model: `\| 19 \| found: 0 \| fixed: 0 \| CONFIRMED: thirteen defects stand \|` and its Pass-path twin `\| Pass 19 \| opus×1 \| found: 0, fixed: 0 \| delta — CONFIRMED: thirteen defects stand \|` on an old-grammar row (a prose lie beside an honest-looking `found: 0`, the operator's eye's job — the checker's `:205-210`), a headerless two-row block `\| 19 \| found: 0 \| Confirmed: 3 \| fixed: 0 \|` over `\| 20 \| found: 0 \| fixed: 0 \|` (the first row is a data row, not a header — refused); every SINGLE-ROW fixture in this cell is written EMBEDDED under a real vocabulary header with one prior parsing data row, so its red-first run grades the row and not the header rule (the block fixtures — the `--init` shape, the counter-vocabulary header alone, the counter-vocabulary header with one parsing data row, the headerless two-row block, the three-row all-token block, the stray-separator variants — are blocks by design), `\| 19 \| found: 0 \| confirmed: three \| fixed: 0 \|` (non-numeric value), `\| 19 \| found: 5 \| confirmed: \| fixed: 0 \|` and `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| unexecuted: \|` (empty value — the bare-colon token), `\| Pass 19 \| opus×1 \| found: 0, confirmed: 0, fixed: 0, confirmed: 3 \| m \|` (a second `confirmed:` voids `_pass_counters`, the token rule refuses), `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| unexecuted: 0 \| unexecuted: 2 \|` (a second occurrence), `\| 18 \| found: 11, new: 9, confirmed: 3, fixed: 3, unexecuted: 0 \|` (a comma triple without its `Pass` head), `\| Pass 19 \| opus×1 \| found: 0 \| fixed: 0 \| confirmed: 3 \|` (Pass-headed, resolved MEGA-first, displaced), and a three-row ledger written entirely `\| N \| found: 0 \| confirmed: C \| unexecuted: U \| fixed: 0 \|` (a token-bearing block in which nothing parses). REFUSED by the counter rule: `\| u \| x \| found: 0 \| fixed: 0 \| unexecuted: 2 \|` and `\| x \| found: 9 \| fixed: 0 \| unexecuted: 0 \|` (`unexecuted:` without `confirmed:`, cell-anchored) and `\| Pass 19 \| opus×1 \| found: 0, fixed: 0, unexecuted: 2 \| m \|` (the same on the Pass path). Also REFUSED by the token rule: `\| 19 \| found: 0 \| new: 2 \| confirmed: 0 \| fixed: 0 \|` (a cell-anchored row has no `new:` cell — the message names the repair), and on the Pass path `\| Pass 19 \| opus×1 \| found: 0, fixed: 0 \| delta (confirmed: 3) \|` (a token outside the counter cell), `\| Pass 19 \| opus×1 \| found: 0, confirmed: , fixed: 0 \| m \|` (half-typed), `\| Pass 19 \| opus×1 \| found: 0, confirmed: three, fixed: 0 \| m \|` (non-numeric), `\| Pass 19 \| opus×1 \| found: 0, confirmed: 0, fixed: 0 \| unexecuted: 2 \|` and `\| Pass 19 \| opus×1 \| found: 0, confirmed: 0, fixed: 0 \| m \| confirmed: 3 \|` (displaced out of the counter cell), and `\| Pass 19 \| opus×1 \| found: 0, fixed: 0 \| delta<U+2028>confirmed: 3 \|` written with a literal U+2028 (one GFM row, refused after the line-ish normalisation — without it the fragment escapes the scan), `\| Pass 19 \| opus×1 \| **found: 0** \| **confirmed: 0** \| **fixed: 0** \|` (separate cells — the new token outside the `found:` cell; the message names the in-cell placement), and the prose line `Pass 19: found: 0, fixed: 0 — confirmed: three defects still stand` (a token outside the counter run, refused), `Pass 19: found: 5, fixed: 0 — delta (confirmed: 0)`, `- Pass 2 (SCOPED) — F1's hunk: found: 0, new: 0, fixed: 0 (confirmed: 0)` and the list-marked `- \| Pass 19 \| sonnet×2 \| found: 5, fixed: 0 \| delta (confirmed: 0) \|` (all refused — a narrated token outside the run, never the exit counter), and the mis-ordered runs `Pass 19: confirmed: 0, found: 5, fixed: 0` (a token before `found:`, outside the run), `Pass 19: found: 0, fixed: 0, confirmed: 0`, `\| Pass 19 \| opus×1 \| found: 0, fixed: 0, confirmed: 0 \| m \|` and `Pass 19: found: 0, unexecuted: 0, confirmed: 0, fixed: 0` (refused — order enforced inside the run, as the cell-anchored path refuses a displaced cell). NOT refused and parsed: `\| Pass 19 \| opus×1 \| **found: 0, confirmed: 0** \| **fixed: 0** \|` (quiet), the prose line `Pass 19: found: 0, confirmed: 3, fixed: 0` (not quiet), and the header row `\| Pass \| Finders \| found: F, confirmed: C, fixed: X \| Method \|` followed by its separator and one parsing data row (the header is exempt, and it is not counted when testing whether the block parses), and the header `\| Pass \| Finders \| Counters \| Method \|` with its separator and NO data row — the shape `review_receipt.py --init` writes (`:165-166` at 2dec30c8; it carries no counter token at all) — and the counter-vocabulary header with its separator and no data row (both not refused — not yet a ledger). Also NOT refused: `\| Pass 9 (C round 4) \| native verifier \| **found: 0** \| **fixed: 0** \| ALL CLOSED \|` (a corpus shape — `found:`/`fixed:` in separate cells stay parsed and quiet; 267 of 535 Pass rows), `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| unexecuted: 0 \| delta re-derivation \|` (quiet), `\| 19 \| found: 4 \| confirmed: 0 \| fixed: 0 \|` (quiet — `found` is prose), `\| 19 \| found: 4 \| confirmed: 1 \| fixed: 1 \|` (not quiet), `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| unexecuted: 0` (quiet) and `\| 19 \| found: 0 \| confirmed: 0 \| fixed: 0 \| unexecuted: 2` (not quiet) — the end-of-line terminator, `\| Pass 18 \| opus×1 \| found: 11, new: 9, confirmed: 3, fixed: 3, unexecuted: 0 \| m \|` (resolved by `_pass_counters`, exempt, not quiet), and a disposition-table row `\| F212 \| grader \| RECORDED — unexecuted (3 attempts timed out) \|` outside the ledger table (out of scope, and no colon token; the same row INSIDE the ledger block is not refused by the token rule but splits the table and trips the existing multi-group error at `:576-581`) |
| V3 | `round --confirmed 0 --classes-swept <all>` prints TERMINAL; `round --confirmed 0` with no classes does not; `round --findings 0` with no `confirmed` on any round keeps the old test | `tests/test_command_run.py`, red on revert of each clause |
| V4 | `dispatch_headroom.py --slices opus=1,sonnet=5,haiku=1` prints SEATS 7 on an idle box, trims Haiku first under a cap, never below one Opus (`--slices` accepts any count — the at-most-one-Haiku rule is the review loop's, D2, not the CLI's); `--slices opus=1,sonnet=1` prints SEATS 2 with no "raised to the floor" reason and no below-the-floor advisory; `--slices` without `--units` runs | `tests/sysadmin/test_dispatch_headroom.py`, red-first on the `:550-557` site |
| V5 | A RECORDED — unexecuted code candidate in the closing round makes the receipt not quiet: the Pass row's `unexecuted: 1` fails the exit; `unexecuted: 0` or absent passes | gate test on the `unexecuted:` token, red-first ; a residual row `RECORDED — by design (F-nnn, round N[; F-nnn, round N]…)` whose F-nnn is absent, or any of whose N is not smaller than the closing row's `Pass N` (the gate compares the verdict's own N — never a scrape of the owning row's prose), is refused by name; `(D-nnn)` with no round token passes the N comparison; `(F342, round 15; F358, round 17)` against `Pass 18` passes (red-first on each row); a residual `RECORDED — measured (<why>)` row never enters `confirmed:` or `unexecuted:` and does not fail the exit (red-first) |
| V6 | The hygiene script reproduces the D-191 review's Haiku findings on the receipt at 741eebbf (F280's raw pipe, F314's dual verdict) and is silent on the receipt at 69f01b92 | `tests/enforcement/test_check_review_hygiene.py` |
| V7 | The rendered corpus, `scripts/` and `docs/workstation/` carry none of the three wordings of the D-191 sentence, and every fan-out command carries the partition step | `tests/test_assemble_dispatch_step.py` extension: a case-insensitive match over text joined and whitespace-normalised (`" ".join(text.split())` — the rotation doc wraps between "one Haiku" and "mechanical seat", so a bare concatenation would pass vacuously) of the 36 rendered commands (the denominator is `commands/_sources/*.md` = 36 plus the assembler — the rendered corpus is not tracked), `commands/_sources/*.md` and the fragment, `.windsurf/rules/core/62-using-subagents.md`, `CLAUDE.md`, `templates/governance/CLAUDE.md` (the fleet-synced one — seen red first at `:356`), `scripts/**/*.py` (seen red first on `dispatch_headroom.py:602/:606`, whose printed sentences gain the scope words "units-sized grounding surface" — asserted present — while the review-loop wording is asserted absent), `docs/workstation/claude-account-rotation.md` (wraps across `:227-228`) and `docs/workstation/quota-dashboard.md` (0 today — asserted, not a red-first site) = 0 for the two distinct literals — "one Sonnet breadth seat AND one Haiku mechanical seat" (the case-insensitive match is what covers the rotation doc's lowercase "and") and the SHORT "one Sonnet + one Haiku", which subsumes the script's "one Sonnet + one Haiku seat per unit" and `/fabrik-review-scoped`'s "one Sonnet + one Haiku over the whole diff" (`:69`); the script's phrase asserted on the RENDERED banner with a positive control — `assert "Box budget (D-189" in html` first, then the absence — because `_budget_probe()` wraps the probe in `try/except` (`:1859`, `:1914`) and an unavailable banner would pass an absence check vacuously; seen red on the rendered banner first (the phrase is in it today) |
| V8 | A delta round's brief names the fix diff + callers and cites the standing-clean classes | command text test (the fragment's delta-round sentence survives a render) |
| V9 | First application: one delta round over round 18's fix diff confirms zero, the D-191 receipt flips to CONVERGED at round 19 and `check_review_coverage.py` is OK on it | the commit that ships D7 |
| V10 | Fire rate: the hygiene script advisory on the first 20 receipts, false-positive rate measured before it errors | the feedback ledger + a backlog row until measured |
| V11 | A delta round with no finder seat cannot close: the closing row's finders cell must name at least one Opus or Sonnet seat by its model token (`opus×1`, `sonnet×2` — the shape every receipt already writes); a closing row whose finders cell names no model is refused by the gate. The run record's stamp is the orchestrator's own duty (DD10) and is not joined by the gate — the gate has no run-record reader today and adding one would be a new mechanism | gate test, red-first, on the finders cell |
| V12 | The Fable→Opus adjudication fallback is exercised once, DELIBERATELY: the first delta round under the new rules is adjudicated on Opus by name whether or not Fable is available, and the receipt records it as the rung's exercise | the first application's receipt (D9) carries the recorded rung |
| V13 | `check_convergence.py` QUIET_PASS reads D9's row as quiet and an old-grammar `found: 0 · fixed: 0` row as quiet; `found: 11, fixed: 0` without `confirmed:` is not quiet | red-first on `:200` |

## Decisions taken

Operator rulings received 2026-09-08 (recorded as D-203 by the sibling session; each restated here so the spec carries them all — I10):

| R# | Ruling (operator's words or the agreed restatement) | Applied at |
|---|---|---|
| R1 | "exit only quite [sic] round should stay" — a quiet round is REDEFINED as a round whose partitioned pass confirms zero code or doc defects | D1 |
| R2 | "A refuted candidate counts as a fresh finding: this is wrong, it is definetly [sic] not" — RECORDED and REFUTED never reopen the loop; CONFIRMED means executed; D-048 superseded | D1, D6 |
| R3 | "diving [sic] tasks amongst the agents according to their type, and in a single pass, they make a combined full pass. the way we do currently is wasteful" — one partitioned pass, disjoint slices, one model per slice; D-191's seat mix superseded | D2, D3 |
| R4 | "we have fable, opus, sonnet, haiku, all agents must be used efficiently and wisely not wastefully" — each model where it pays; after round 1 a model sweeps only its changed slice; Haiku's classes become a script | D2, D4 |
| R5 | "if you create new errors, they must be definetely [sic] fixed … we can review only the fix without making ful [sic] pass" — delta rounds over the fix diff plus callers | D5 |
| R6 | No round cap; the NON-CONVERGENCE escalation stays as the only other exit | D1 |
| R7 | The seat budget still runs through `dispatch_headroom.py`, sized by slices; the 15-minute box and budget → stamp → dispatch stay | D3 |
| R8 | A candidate raised by one seat and not reproduced by the orchestrator is RECORDED, never counted | D6 |
| R9 | `confirmed:` on the ledger row, `--confirmed` on `round`, TERMINAL on confirmed == 0 with a class-ledger close, backward compatibility for in-flight receipts | D7 |
| R10 | The seat-brief template carries the D-191 review's lessons | D8 |
| R11 | The BLOCKED D-191 receipt flips to CONVERGED under the new rule once this ships | D9 — applied honestly: one delta round over round 18's three confirmed fixes, then the flip at round 19 |

Design decisions this spec makes under those rulings (each reversible; override by re-freeze):

| DD# | Decision | Why |
|---|---|---|
| DD1 | "Executed" = a probe on a pinned copy, a failing test, or a mutation on a copy — and for a DOC claim, a pinned read of the artifact the doc describes, quoted beside the doc's sentence (F363: the protocol row read against `command_run.py`'s counting); an unexecutable candidate is `RECORDED — unexecuted (<why>)` and a closing round carrying one for a code claim is not quiet; a reproduced-and-kept one is `RECORDED — by design (<owning row>, round N[; …])` (or `(D-nnn)` for a D-row) with every owning receipt row from an earlier round, refused otherwise | closes the relabelling loophole the operator was warned of |
| DD2 | The floor under partition: one seat per non-empty slice kind; a surface without a risky unit still gets one Opus seat over its most consequential slice (carved out of Sonnet's allocation); D-186/D-188's three-different-angles rule stays for grounding and adjudication surfaces and for `/fabrik-review-scoped`'s one-unit duplicate-brief floor; the discriminator is `budget(slices=…)`, which stands every FLOOR site down (D3) | the third angle in a review is the orchestrator's execution |
| DD3 | The delta surface = fix diff + one hop of callers/callees by symbol (the files that reference a changed symbol, by grep or the language server, plus the tests importing the changed module) + sibling commits on the original surface since the last round | one hop is where fix residue lived in rounds 14–17; deeper hops re-create the full pass |
| DD4 | `found:` stays (recall), `confirmed:` is added (the exit), `fixed:` stays; old rows parse; the gate branches on the presence of `confirmed:` | in-flight receipts in ~46 projects must not break on sync day |
| DD5 | `dispatch_headroom.py` gains `--slices` and keeps `--units` for the commands that size by units | spec and plan grounding are judgement surfaces; their D-186/D-188 floor stays |
| DD6 | The hygiene script starts advisory and graduates on a measured false-positive rate below 5 % over 20 receipts | a detector that fires on legitimate patterns is wallpaper (CLAUDE.md § FIX DIRECTIVE 5) |
| DD7 | The Opus seat's scope is a fixed list (concurrency, locks, record/file formats, fleet-synced paths, auth, schema, migrations, secrets); the orchestrator may add a unit to it with the reason in the brief | a list beats a judgement call under time pressure |
| DD8 | The oscillation advisory reads the `confirmed` series when every round states it; `PER_UNIT_ROUND_COMMANDS` keeps `fabrik-repo-review` because its per-slice reservation rounds (no classes) would read as a series | a reservation close is not a convergence pass |
| DD9 | `check_convergence.py` QUIET_PASS follows the same grammar (D-203 names it) | one definition of quiet across the gates |
| DD10 | The closing delta round always carries ≥1 fresh non-authoring finder seat; the hygiene script and the orchestrator never close a review alone | core/62:186 — who hunts last is never the author |
| DD11 | `/fabrik-review-scoped` inherits the exit rule and row grammar and keeps its measured one-unit floor; the board banner and its doc follow the partition wording | the Stop-hook review is the highest-frequency review surface; a floor measured on one brief is not an overlap |
| DD12 | A confirmed defect is fixed in the round that confirmed it — fixes never carry over — so `confirmed: 0` implies `fixed: 0` and D1's quiet and D7's gate are one definition | a deferred fix is the residue the delta round exists to catch, not a way to reach quiet |
| DD13 | The hygiene script runs at a round's start (hits fixed, reviewed by that round's seats) and at its close; a closing round is quiet only if the closing run is silent; a hygiene hit adjudicated a false positive is `RECORDED — hygiene false positive` and never counts | a script exempted at the close would be the relabelling loophole in mechanical form |

## Lifecycle

- **Adoption:** ships as one plan; the corpus renders from the main checkout; the post-commit governance sync distributes the gates and `command_run.py`; the D-191 receipt is the first flip (D9). Reviews in flight in project repos keep their old grammar until their next round writes a `confirmed:` row.
- **Growth:** the hygiene script's class list grows by brief-named classes that repeat in two reviews (the feedback ledger's `change:` field is the source); the partition's risky list (DD7) grows by D-row. Trigger to revisit: three reviews in a row where a delta round confirms a defect outside the one-hop surface (read from the receipts' F-rows, which name the round and the file) — then DD3 widens to two hops.
- **Degradation:** if Fable refuses, Opus adjudicates by name (recorded); independently of any refusal, the first delta round of the first review under the new rules is adjudicated on Opus by name on purpose (V12), so the ladder's bottom has run before it is needed; if the box cap trims below one seat per slice kind, the orchestrator serialises slices across messages under one round (the stamp accumulates, the reservation clock is per message); if execution of a candidate is impossible on a pinned copy (a race, a live-only path), it is `RECORDED — unexecuted (<why>)` and named in the residual section.
- **Retirement:** superseded by a new D-row; the old grammar keeps parsing for the receipts written under it.

## Grounding (external facts and the approach floor — fetched 2026-09-08 by four native `fabrik-researcher` seats, one Opus; raw-arm fetches, verbatim quotes)

| G# | Source | Fetched | What it grounds |
|---|---|---|---|
| G1 | https://agentpatterns.ai/code-review/review-then-implement-loop/ | 2026-09-08 | "Cap automated fix attempts at one pass. If the coding agent's fix does not resolve the finding cleanly (tests fail, or new issues appear), escalate to human review rather than looping." — adopted as the NON-CONVERGENCE exit, not as a cap (R5/R6); acceptance floor "adopted at 16.6% versus 56.5% for humans" |
| G2 | https://agentpatterns.ai/code-review/reproduce-before-report-verification-gate/ | 2026-09-08 | "A reproduce-before-report verification gate drops any reviewer finding the verifier cannot reproduce against actual code behavior." · "A pipeline that fans out reviewers but reports every candidate is fan-out: it amplifies false positives instead of filtering them." · the suspected-but-not-reproduced channel → `RECORDED — unexecuted (<why>)` |
| G2b | https://arxiv.org/abs/2604.19049 (Refute-or-Promote) | 2026-09-08 | "the pipeline killed ∼79% of ∼171 candidates before advancing to disclosure"; "ten dedicated reviewers unanimously endorsed a non-existent Bleichenbacher padding oracle in OpenSSL's CMS module; it was killed only by a single empirical test, motivating the mandatory empirical gate." (the abstract's figure; the paper's § 4.2 says "80+ agents — including dedicated adversarial reviewers" for the same event) → DD1 |
| G3 | https://arxiv.org/abs/2608.18167 (Adversarial Review) | 2026-09-08 | "outperforming a five-agent baseline while using only three agents"; "it requires that disagreement be minimal, structured, and evidence-grounded" → D6 |
| G3b | https://agentpatterns.ai/code-review/evidence-grounded-disagreement/ | 2026-09-08 | naive reviewer pair F1 0.457 below a single reviewer 0.495; grounded pair 0.533 — "only requiring every objection to cite code makes the second reviewer worth it" → refutations cite the executed command |
| G4 | https://arxiv.org/abs/2511.16708 (+ html body) | 2026-09-08 | "diminishing returns of +14.9pp, +13.5pp, and +11.2pp for agents 2, 3, and 4"; "Security detects 87.5% of security bugs but only 4.2% overall" (overall TPR) → retire the overlapping reader |
| G5 | https://zylos.ai/research/2026-03-01-multi-model-ai-code-review-convergence/ | 2026-09-08 | "hard caps of 1-2 loops for automated CI/CD, with 3-5 rounds for interactive review sessions" → not adopted (R6, § Rejected alternatives row 1); "issues flagged in only one pass are discarded as noise" → rejected as a mechanism (§ Rejected alternatives, majority vote across passes) in favour of D6; "A good tool achieves >60% signal ratio; a great tool >80%." → the signal ratio the hygiene script must clear before it errors (DD6 states it as a false-positive rate below 5 %); "Dual-threshold circuit breakers: Warning threshold triggers 'wrap up'; hard threshold forces output and terminates." → D1's two-tier stop (the NON-CONVERGENCE escalation after three non-decreasing delta rounds; the 15-minute seat box as the hard threshold) |
| G6 | https://mastra.ai/articles/ai-code-review-tools · https://agentpatterns.ai/code-review/diff-based-review/ | 2026-09-08 | "then reviews each later commit with an incremental pass focused on the update." → D5; "Review what changed, not the full output — mistakes live in the delta" → D5's delta surface |
| G7 | https://www.codeant.ai/blogs/ai-code-review-false-positives | 2026-09-08 | "For most teams, <10% should be your baseline 'acceptable' threshold. For high-throughput organizations, <5% is the optimal target." → DD6's 5 % bar. (An earlier draft of this session cited 10–30 % bands and a SonarSource 3.2 % figure from this page; the live page carries neither — corrected by the seat.) |
| G8 | https://blog.cloudflare.com/ai-code-review/ | 2026-09-08 | "Instead of asking one model to review everything, we split the review into domain-specific agents." · "Top-tier: … Reserved exclusively for the Review Coordinator", "Standard-tier: … The workhorse for our heavy-lifting sub-reviewers", and a third model (Kimi K2.5, no tier label on the page) "Used for lightweight, text-heavy tasks" · "If the coordinator isn't sure, it uses its tools to read the source code and verify." → D2's model-by-slice partition and D6 |
| G9 | https://javascript.plainenglish.io/dont-let-your-ai-agents-loop-forever-an-engineering-guide-to-termination-criteria-c09d8d68f871 | 2026-09-08 | "Enforce hard ceilings across three independent dimensions: turns, tokens, and wall-clock time" → the 15-minute seat box and the reservation clock stay per seat, not per review. Liveness: the orchestrator's `curl -sSI` status probe returns HTTP 403 (a bot block, with and without a browser user agent); the page and the quote were fetched verbatim through the raw-arm fetcher this session, so the citation stands with that caveat, and G5's dual-threshold sentence is the verified fallback |

Search leg (real search, `WebSearch`, 2026-09-08): the Opus seat's query surfaced https://arxiv.org/html/2607.06065 (SWE-Review), https://arxiv.org/html/2607.24604v1 ("Looping Is Not Reliability"), and the Cloudflare/mastra/projectdiscovery pages the fourth seat opened; ≥2 distinct URLs from ≥2 tools with at least one via search — the 1c floor is met. Behaviours R1–R11 are ruled (D-203) and grounded by their row, per D-153; the literature above grounds HOW they are built.

## fabrik-lib verdict

One line: nothing to vendor — the surfaces are hub scripts and the command corpus; of the 71 modules in `/opt/fabrik-lib/README.md` (listed twice, 142 rows) two mention review and neither is a review engine (`subagents/` is Retired — fabrik-lib's own ledger row D-132, not the hub's); this delta is not a fabrik-lib candidate (hub-specific machinery, not generic: it fails the ≥2-project-types and small-interface conditions).

## Constraints digest

| Pack | Rule (verbatim) | Anchor | Applies to |
|---|---|---|---|
| core/62 | "The loop-closing round's **FINDER pass** runs in a context that did **not author the artifact**" | `.windsurf/rules/core/62-using-subagents.md:186` | D5/DD10: the closing delta round carries a fresh non-authoring seat; the orchestrator adjudicates, never hunts last |
| core/62 | "**NEVER solo, never two — the FLOOR is three seats.** A surface with fewer than three units still dispatches THREE seats on DIFFERENT angles over it" | `.windsurf/rules/core/62-using-subagents.md:65` | the MUST this delta narrows (DD2): retired for the partitioned review loops, kept for grounding/adjudication surfaces and `/fabrik-review-scoped`; the supersede row is minted at approval |
| core/62 | "the `/fabrik-review-scoped` floor is one unit, i.e. the script's 3-seat answer" | `.windsurf/rules/core/62-using-subagents.md:65` | D10: stays |
| core/62 | "**Adjudication — decide/refute/merge — stays with the orchestrator** … this rule governs who HUNTS last, never who adjudicates" | `:193-195` | D6: executing a refutation is adjudication |
| core/62 | "Gating a review's trust on a specific pool model name instead of the methodology (native-Opus authority + refutation)." (Banned) | `:262` | D2 names roles by kind; trust rests on executed refutation |
| core/62 | "every fan-out a command names runs NATIVE (Runtime A)" | `:65` | all seats native; the pool stays OFF (D-181/D-182) |
| core/40 | "**No commented-out content blocks** — dead text confuses AI and pollutes diffs" | `.windsurf/rules/core/40-documentation.md:246` | the retired D-191 sentence is replaced, not commented out (the POOL-OFF comments are the sanctioned exception) |
| core/40 | "**Keep tables simple and atomic** — no multiline cells" | `:245` | the new ledger row grammar stays one line |
| core/10 | "**`uv`** is the mandated Python package manager." | `.windsurf/rules/core/10-python.md:21` | no new dependency; nothing to install |
| core/58 | "every external call has timeout + retry with backoff" | `.windsurf/rules/core/58-resilience.md:87` | seats keep the 15-minute box; the reservation clock bounds a hung seat |
| FLOOR 35 / 25 / 30 | glob-scoped to auth, DB and Docker paths | `.windsurf/rules/core/35-security-auth.md:1-8` · `25-data-postgres.md:1-8` · `30-ops.md:1-8` (the activation globs and goals) | no row applies: the delta touches no auth, schema or compose surface — read, none applicable |
| standing rulings | the pool is OFF (D-181/D-182); D-190 pricing (haiku 1× · sonnet 2× · opus 5× · fable 10×); no new dependency | `docs/DECISIONS.md` | I19: every seat native; the cost unit unchanged; nothing to install |

## Shape / infra implications

One line: none — hub machinery; of the 72 `specs/services/*.yaml` two mention "review" and neither is a review engine; no `shape:` flag, no deploy. The 12-Factor and hard-constraint audit (`/fabrik-spec` § 1b-bis) was run: every hard-constraint row and every factor is inapplicable — no deployed service, no config surface, no process model, no payment, storage, vector or LLM-gateway choice, the seats are native Claude subagents; of the architectural mandates only resilience applies, satisfied by the 15-minute seat box and the reservation clock (§ Constraints digest, core/58) and by the Fable→Opus fallback of § Lifecycle — exercised once deliberately per V12, and automatically on any future refusal.

## Documentation landing sites

- `docs/reference/command-run-protocol.md` — the `round --confirmed` row and the TERMINAL rule.
- `docs/workstation/quota-dashboard.md` § The box-budget banner — the slice mix `dispatch_headroom.py` prints; `docs/workstation/claude-account-rotation.md` § 223-239 — its CLI reference (coupled by the script's `# AFTER-EDIT:` header).
- `docs/reference/convergence-prompts.md` — the delta-round brief and the executed-refutation row shape; `docs/reference/MD/ai-prompt-templates.md` § the review "done" row (`:270`, today `found: 0, fixed: 0`) — the new row shape and exit counter.
- `docs/DECISIONS.md` — D-203 (ruling), the supersede rows for D-048, D-191 and the D-186/D-188 review floor at approval.
- `CHANGELOG.md` — one entry per ticket; `INDEX.md` — the new script and its test; `docs/STRATEGIC_BACKLOG.md` — the hygiene script's fire-rate row until measured.
- The three commands (`/fabrik-review`, `/fabrik-repo-review`, `/fabrik-review-scoped`), the fragment, core/62 (§ Dispatch policy and the `:65` floor sentence), both CLAUDE.md files, the board banner and `dispatch_headroom.py`'s own docstrings and printed sentences self-document the rule.

## Open / blocking unknowns

- Resolved: whether a refuted candidate counts (R2, D-203); whether the exit stays a quiet round (R1); the partition and the delta rule (R3–R5).
- Open, with a resolution step: the hygiene script's false-positive rate on real receipts — measured advisory over the first 20 receipts (DD6, V10) before it may error. The modelled "closes around round 16 at two fifths of the spend" is a projection from the D-191 series; the first three reviews under the new rules measure it (Lifecycle).

## Machinery report

DRAFT hand-over at 2dec30c8: 320 lines / 21 H2, ~18 minutes from the run record's open (the `/fabrik-spec` record, 20:53 local); four grounding seats (1 Opus, 3 Sonnet); one quoted fact corrected against the live page by the grounding seats (G7's thresholds); the review's passes re-matched G1–G9 on the raw pages, widened four quoted spans to the verbatim page text (G2b, G5, G6, G8) and added G9's 403 liveness caveat and G6's second source URL, all in pass 2; no other fact was corrected (G2's `(<why>)` at pass 18 is spec notation, not a page quote). Friction with the D-153 text: none; the delta profile's collapsed sections held. The review's own wall-clock, pass count and final line count are appended below by `/fabrik-spec-review`.

`/fabrik-spec-review` (nested record, opened 21:12:58 local 2026-09-08): 7 h 29 min wall-clock, 45 passes (three seats each — one Opus authoritative, two Sonnet breadth — pinned by md5; findings 26 → 14 → … → 4 → 0), the spec grew 320 → 348 lines; pass 44 raised zero confirmed defects and three prose residues (the intake provenance at `:15`, the Haiku lane row at `:208`, the zero-padding symmetry in `QUIET_PASS`), pass 45 re-read the corrected text with zero confirmed defects and zero edits — the md5-verified no-op. RECORDED, not counted, from the closing passes: the first `QUIET_PASS` alternative still requires a bare `found: 0` while the second tolerates `0+` (fail-closed, 0 zero-padded counter lines in the 275 receipts); colon homoglyphs (U+FF1A and kin) are outside `_strip_fences`'s normalisation and inside the forgery exclusion (0 exemplars); the `RECORDED — hygiene false positive` verdict is written with a `: <why>` suffix at D4 and bare at D7/DD13 (not a gate token; the plan pins one shape); I14's provenance is named only by the umbrella range at `:15` and by `:120`.
