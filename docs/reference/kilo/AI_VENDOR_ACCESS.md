Last verified: 2026-07-12

# AI Vendor Access — Özgür's Reachable Set

Single source of truth for which vendors the operator can call today. Hand-edited (not auto-generated). The specialty-catalog seeder (`scripts/kilo-benchmarks/seed_specialty_catalog.py`) parses this file to set `agents.reachable_with_existing_keys` (1 = accessible, 0 = new-vendor / needs signup). Rows with Status ✅ or ⚠️ are accessible (⚠️ = accessible but low balance — pick a ✅ peer if one is on the Pareto frontier).

## Column contract

- **Vendor** — human name.
- **Category** — LLM / Image / Video / TTS / STT / Translation / Embed / Multi / Image-utility (mixed types).
- **DB provider(s)** — comma-separated list of `agents.provider` values (EXACT match — the seeder splits on `,` and strips whitespace).
- **Auth mechanism** — how the key is stored / injected.
- **Status** — one of `✅` (accessible), `⚠️` (accessible, low balance / subscription-only), `❌` (not accessible, needs signup or deprecated).
- **Credits/quota** — current balance or tier.
- **Notes** — non-load-bearing detail (e.g. deprecation notice).

## LLM gateways

| Vendor | Category | DB provider(s) | Auth mechanism | Status | Credits/quota | Notes |
|---|---|---|---|---|---|---|
| OpenRouter | LLM (aggregator) | openai, anthropic, google, x-ai, meta-llama, qwen, deepseek, mistralai, moonshotai, z-ai, minimax, bytedance-seed, microsoft, nvidia, hexgrad, canopylabs, zyphra, sesame | env `OPENROUTER_API_KEY` | ✅ | credits pool | Primary LLM gateway. 5.5% fee + $0.80 min credit-purchase; no per-token markup. Dual-routed with Kilo for most models. |
| Kilo CLI | LLM (aggregator) | (peer gateway — same `provider` values as OpenRouter) | subscription (Kilo CLI login) | ✅ | subscription-billed | Peer gateway to OpenRouter. Pick per-model by whichever bake-off row shows the cheaper rate. |
| Claude Max (direct via Claude Code) | LLM | anthropic | Claude Code OAuth (`~/.claude/` credentials) | ✅ | Max subscription | Operational path only (sysadmin, watchdog, plan execution). NOT for `fabrik ai generate` content utilities. |

## Specialty vendors (image / TTS / STT / translation)

| Vendor | Category | DB provider(s) | Auth mechanism | Status | Credits/quota | Notes |
|---|---|---|---|---|---|---|
| Replicate | Image | stability, bfl-via-replicate, recraft-ai | env `REPLICATE_API_TOKEN` | ✅ | credits pool | Official-model route for BFL (FLUX) via Replicate; also stability + Recraft. |
| Fal.ai | Image | bfl | env `FAL_KEY` | ✅ | credits pool | BFL FLUX via Fal — often cheaper than direct BFL. |
| BFL direct | Image | (none — deprecated) | env `BFL_API_KEY` (unset) | ❌ | none | Deprecated per AFCL: Replicate + Fal cover FLUX cheaper. Do NOT recommend a `bfl-direct` route. |
| Recraft direct | Image | recraft | env `RECRAFT_API_KEY` | ✅ | small credits | Direct route for Recraft models when Replicate charges more. |
| Soniox | STT | soniox | env `SONIOX_API_KEY` (3 keys rotated) | ✅ | 3-key rotation | Primary STT vendor — Universal-2 quality tier. |
| ElevenLabs | TTS | elevenlabs | env `ELEVENLABS_API_KEY` | ✅ | free tier (10K chars/mo) | Primary TTS vendor for expressive voices. Free tier is enough for dev; watch quota. |
| Alibaba DashScope | LLM + Translation | qwen, qwen-mt-turbo | env `DASHSCOPE_API_KEY` | ✅ | credits pool | Qwen inference + qwen-mt-turbo translation specialist route. |
| SiliconFlow | Multi (LLM + Image + TTS + Embed) | qwen, deepseek, z-ai, moonshotai, minimax, tencent, google, bfl, black-forest-labs, meituan, stepfun, nex-agi, wan-ai, inclusionai, bytedance-seed, indexteam, fishaudio, funaudiollm, tongyi-mai, openai | env `SILICONFLOW_API_KEY` | ✅ | credits pool | 72-model gateway on international endpoint `api.siliconflow.com` (NOT `.cn` — different key domain). Covers LLMs (Qwen 2.5→3.6 line, DeepSeek V3/V4, GLM 4.5→5.2, Kimi K2.5-2.7, MiniMax M2.5/M3, Tencent Hunyuan+Hy3, gemma-4, gpt-oss-20b/120b), image gen (all FLUX + Qwen-Image + Wan2.2 + Z-Image-Turbo), TTS (fish-speech-1.5, IndexTTS-2, CosyVoice2), embeddings (Qwen3-Embedding 0.6B/4B/8B), rerankers (Qwen3-Reranker 0.6B/8B). Full list: `python scripts/kilo-benchmarks/scrape_siliconflow_catalog.py` (also flips `via_siliconflow=1` on affected agents rows). |
| ModelScope | LLM (aggregator) | qwen, deepseek, z-ai, moonshotai, minimax, mistralai, stepfun, nex-agi, meituan, baidu, xiaomi, tencent, shanghai-ai-lab, alibaba-iic, llm-research, medaibase, musepublic, opencompass, opengvlab, xgenerationlab | env `MODELSCOPE_API_KEY` | ✅ | credits pool | 55-model Alibaba model-hub gateway on `https://api-inference.modelscope.cn/v1` (OpenAI-compatible). **Delivered** (via_modelscope=1 in DB, active-status set): ZhipuAI GLM direct (4 rows: GLM-4.7-flash / GLM-5 / 5.1 / 5.2), Tencent Hunyuan Hy3 (1 row), plus 27 Qwen/DeepSeek/MiniMax/Kimi/stepfun/moonshotai/nex-agi active overlap rows = 32 active total (31 active+unblocked visible in the GUI). Also flipped: `xiaomi/mimo-v2-flash` — but that row is `status='deprecated'` (superseded by mimo-v2.5), not counted in the active set. **Fetched but unmatched** (awaits new-row ingestion — plan-2 scraper is flip-only, not INSERT): Shanghai AI Lab Intern-S1/S1-mini/S2-Preview, PaddlePaddle ERNIE-4.5-PT (0.3B / 21B-A3B / 300B-A47B / VL-28B — PT = post-training checkpoints, not the DB's Instruct routes), XiYanSQL, IIC GUI-Owl, LLM-Research Llama-4-Maverick, MedAIBase, MusePublic Qwen-Image-Edit, OpenGVLab InternVL3.5-241B, OpenCompass. Format: `ms-*`. Full list: `python scripts/kilo-benchmarks/scrape_modelscope_catalog.py` (also flips `via_modelscope=1` on affected agents rows). |
| Anthropic direct API | LLM | anthropic | env `ANTHROPIC_API_KEY` | ⚠️ | subscription-billed via Claude Code | Kept for `fabrik ai generate` content utilities only — never for operational paths (those use Claude Max via Claude Code OAuth). |

## Direct-API vendors — need signup + payment method (not currently reachable)

| Vendor | Category | DB provider(s) | Auth mechanism | Status | Credits/quota | Notes |
|---|---|---|---|---|---|---|
| OpenAI direct | Multi (LLM + Image + TTS + STT + Embed) | (none — routed via OpenRouter today) | env `OPENAI_API_KEY` (unset) | ❌ | needs signup | OpenRouter route is cheaper + no separate signup; direct only if a route is OR-blocked. |
| Google Cloud (Gemini API) | Multi (LLM + Image) | (none — routed via OpenRouter today) | GCP service account (unset) | ❌ | needs signup | Gemini API via OpenRouter today; direct only for GCP-only features (grounding, live). |
| Azure OpenAI | Multi (LLM + Image + TTS + STT + Embed) | (none) | Azure AAD (unset) | ❌ | needs signup | No compelling reason today — OpenRouter covers the model set. |
| Azure AI Translator | Translation | (none — no `azure` rows in DB yet) | env `AZURE_TRANSLATOR_KEY` + `AZURE_TRANSLATOR_REGION` + `AZURE_TEXT_ENDPOINT` (SET in `/opt/fabrik/.env` — migrated from `/opt/youtube/.env`) | ✅ | Azure credits | Distinct from Azure OpenAI (this is `Cognitive Services / Translator`). Peers with DeepL + Alibaba `qwen-mt-turbo` in the MT specialist ladder. Needs a `discover_kilo_agents.py` pass or `scrape_azure_translator_catalog.py` seed to surface `azure/translator-*` rows. |
| Deepgram | STT | (none — no `deepgram` rows in DB yet) | env `DEEPGRAM_API_KEY` (unset) | ❌ | needs signup | Would compete with Soniox for STT; only signup if Soniox 3-key rotation exhausts. |
| AssemblyAI | STT | (none) | env `ASSEMBLYAI_API_KEY` (unset) | ❌ | needs signup | Universal-2 is comparable-quality; not needed while Soniox is up. |
| DeepL | Translation | (none — no `deepl` rows in DB yet) | env `DEEPL_API_KEY` + `DEEPL_API_URL` (SET in `/opt/fabrik/.env` — migrated from `/opt/youtube/.env`) | ✅ | Free tier (500K chars/mo) | Excellent translation quality. Key present but no `deepl` rows in `agents` yet — needs a `scrape_deepl_catalog.py` seed or a `discover_kilo_agents.py` pass to surface `deepl/*` models with the specialty-vendor `translation` service_type. |
| HuggingFace | Multi (LLM + Image + TTS + STT + Embed) | (none — no `huggingface` rows in DB yet) | env `HF_TOKEN` (SET in `/opt/fabrik/.env` — verified `hf_...` format 2026-07-12) | ✅ | free tier + PAYG | Inference Endpoints + Serverless Inference over 100k+ OSS models. Key present but no `huggingface` rows in `agents` yet — needs `discover_kilo_agents.py` seeding pass or a `scrape_huggingface_catalog.py` write to surface the reachable model set. Value: OSS-only fine-tuned variants not on OR / Novita / SiliconFlow / ModelScope. |
| Fireworks | LLM | (none — no `fireworks` rows in DB yet) | env `FIREWORKS_API_KEY` (unset) | ❌ | needs signup | Fast LLM inference (llama, DeepSeek, Mixtral, Qwen). Competes with OR:DeepInfra + Novita on speed/price. Signup candidate if OR routing to Fireworks proves cheaper direct. |
| Vertex AI (Google Cloud) | Multi (LLM + Image + Agent) | (none — routed via OpenRouter today) | GCP service account with `aiplatform.user` role — SA present at `~/.gcp/youtubeprojects-ocoron-401d704156ba.json` (project `youtubeprojects-ocoron`, SA `agent-platform-access@`) | ⚠️ | billing not enabled on project | SA valid and reaches Vertex endpoint (`us-central1-aiplatform.googleapis.com` returned HTTP 403 with billing-required message on 2026-07-12 probe). **One-step unblock**: enable billing on project `youtubeprojects-ocoron` at `https://console.developers.google.com/billing/enable?project=youtubeprojects-ocoron`. Then verify Vertex AI API is enabled + SA has `roles/aiplatform.user`. Once green, flip Status to ✅ and set `GOOGLE_APPLICATION_CREDENTIALS=~/.gcp/youtubeprojects-ocoron-401d704156ba.json` in `.env`. Sibling note: `/opt/site-provisioner/.env` has a different `GOOGLE_APPLICATION_CREDENTIALS` pointing at `dns-manager-gsc-*.json` (Cloud DNS SA, unrelated). |
| Leonardo AI | Image | (none — no `leonardo` rows in DB yet) | env `LEONARDO_API_KEY` (unset) | ❌ | needs signup | Stylized image gen (game/comic art, character-consistent portraits). Product-specific — not competitive on realism vs FLUX/Imagen; only signup if a project needs Leonardo's specific styles. |
| Clipdrop (Stability) | Image-utility | (none — no `clipdrop` rows in DB yet) | env `CLIPDROP_API_KEY` (unset) | ❌ | needs signup | Stability's utility-image APIs: bg-remove, uncrop, sketch-to-image, cleanup. Signup candidate for a product pipeline (e.g., product-photo enhancement) — NOT for general image gen (SDXL is on Replicate + Fal cheaper). |
| Segmind | Image | (none — no `segmind` rows in DB yet) | env `SEGMIND_API_KEY` (unset) | ❌ | needs signup | Stable Diffusion pipelines + LoRA hosting. Overlaps heavily with Replicate + Fal for FLUX/SDXL; signup only if a specific LoRA or ControlNet route is Segmind-only. |
| GetImg | Image | (none — no `getimg` rows in DB yet) | env `GETIMG_API_KEY` (unset) | ❌ | needs signup | Multi-model SD/Flux aggregator + face-fix + upscaler. Same category as Segmind; signup only if a specific SKU is Getimg-only cheaper than Replicate/Fal. |
| WaveSpeed | Multi (Image + Video + Audio + LLM + 3D) | (none — no `wavespeed` rows in DB yet) | env `WAVESPEED_API_KEY` (SET in `/opt/fabrik/.env`, `wsk_live_...` format) | ✅ | live account | **941 models across 25 types** on `https://api.wavespeed.ai/api/v3/` (catalog fetched 2026-07-12). Coverage: image-to-video (195), image-to-image (131), text-to-image (124), text-to-video (95), text-to-audio (61), lora-support (59), digital-human (44), video-to-video (40), upscaler (22), video-extend (21), image-to-3d (19), ai-remover (15), audio-to-audio (15), image-to-text (14), training (13), motion-control (13), portrait-transfer (12), speech-to-text (10), video-dubbing (8), video-to-text (7), text-to-3d (6), llm (6), content-moderation (5), audio-to-video (3), video-effects (3). Cheapest tier ~$0.0002-0.005 per gen. Full catalog cached at `scripts/kilo-benchmarks/cache/wavespeed_catalog.json` (1.7 MB) + flat version at `scripts/kilo-benchmarks/cache/wavespeed_models_flat.json` (254 KB, one entry per model with `id/type/base_price/description`). Refetch: `curl -H "Authorization: Bearer $WAVESPEED_API_KEY" https://api.wavespeed.ai/api/v3/models`. Needs a `scrape_wavespeed_catalog.py` write to seed `agents` rows. |
| Kie AI | LLM (aggregator) | (none — no `kie` rows in DB yet) | env `KIE_API_KEY` (unset) | ❌ | needs signup | LLM inference aggregator (mixed vendors). OR is the incumbent; only signup if Kie routing is verified cheaper on a specific model. |
| ImageRouter | Image (aggregator) | (none — no `imagerouter` rows in DB yet) | env `IMAGEROUTER_API_KEY` (unset) | ❌ | needs signup | Image-gen aggregator (OR-for-images). Value proposition depends on whether it beats Replicate + Fal for the FLUX/SDXL SKUs we actually use. |
| Venice AI | LLM | (none — no `venice` rows in DB yet; 1 sub-route in `or:` gateway) | env `VENICE_API_KEY` (unset) | ❌ | needs signup | Uncensored LLM gateway (crypto-native, no-KYC). Already reachable as an OR sub-route (`cheapest_gateway LIKE '%venice%'` = 1 row). Direct signup only if content-moderation-free routing is a product requirement. |

## Web-only accounts — NOT accessible for CLI / API use

| Vendor | Category | DB provider(s) | Auth mechanism | Status | Credits/quota | Notes |
|---|---|---|---|---|---|---|
| Gemini (web) | LLM | (none) | web login only | ❌ | web-tier | UI-only account — do NOT recommend a `google/gemini-*` route on the assumption we can call it; use the OpenRouter route or DashScope. |
| GPT (web / ChatGPT Plus) | LLM | (none) | web login only | ❌ | Plus subscription | UI-only account — do NOT recommend a `openai/gpt-*` direct route on this basis; the OpenRouter route is the actual reachable one. |
| Perplexity (web) | LLM | (none) | web login only | ❌ | Pro subscription | UI-only account — no API is wired. |
