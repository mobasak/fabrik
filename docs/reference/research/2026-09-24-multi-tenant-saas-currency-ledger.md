# Research ledger — `saas/95-multi-tenant-saas.md` currency pass, 2026-09-24

Every fact the two research seats — PostgreSQL core (pg) and RLS practice (pr) — the Fable author-blind second-opinion seat (op), and the orchestrator's re-fetches for the review (rv) of file 30's turn returned, filed
whole with its disposition (`CLAUDE.md` § External Knowledge; checked by `scripts/check_research_ledger.py`). The
synthesis is the revision of `.windsurf/rules/saas/95-multi-tenant-saas.md` and its `CLAIMS.yaml` rows
(`pack: saas/95-multi-tenant-saas.md`).

| id | source | fact | url | disposition |
|---|---|---|---|---|
| pg-01 | Postgres seat (WebFetch + exa) | "Superusers and roles with the `BYPASSRLS` attribute always bypass the row security system when accessing a table. Table owners normally bypass row security as well, though a table owner can choose to be subject to row security with `ALTER TABLE ... FORCE ROW LEVEL SECURITY`." | https://www.postgresql.org/docs/current/ddl-rowsecurity.html | USED → `saas/95` § RLS Setup (`FORCE` binds the owner, never superusers or `BYPASSRLS`) and `.windsurf/rules/CLAIMS.yaml` row `pg-rls-bypass-and-force` |
| pg-02 | Postgres seat (WebFetch + exa) | FORCE is described only as making the owner subject to RLS; no exception is stated for superusers or BYPASSRLS | https://www.postgresql.org/docs/current/ddl-rowsecurity.html | DUPLICATE of pg-01 |
| pg-03 | Postgres seat (WebFetch + exa) | "Operations that apply to the whole table, such as `TRUNCATE` and `REFERENCES`, are not subject to row security." | https://www.postgresql.org/docs/current/ddl-rowsecurity.html | USED → `saas/95` § What RLS Does Not Cover and `.windsurf/rules/CLAIMS.yaml` row `pg-rls-gaps` |
| pg-04 | Postgres seat (WebFetch + exa) | "Referential integrity checks, such as unique or primary key constraints and foreign key references, always bypass row security … avoid 'covert channel' leaks of information through such referential integrity checks." | https://www.postgresql.org/docs/current/ddl-rowsecurity.html | USED → `saas/95` § What RLS Does Not Cover (tenant-scoped uniqueness, composite foreign keys, and — as the pack's inference from the same bypass — cascades from `tenants`) and `.windsurf/rules/CLAIMS.yaml` row `pg-rls-gaps` |
| pg-05 | Postgres seat (WebFetch + brave) | "If any of the underlying base relations has row-level security enabled, then by default, the row-level security policies of the view owner are applied" | https://www.postgresql.org/docs/current/sql-createview.html | USED → `saas/95` § What RLS Does Not Cover (views) and `.windsurf/rules/CLAIMS.yaml` row `pg-rls-gaps` |
| pg-06 | Postgres seat (WebFetch + brave) | "if the view has `security_invoker` set to `true`, then the policies and permissions of the invoking user are used instead" | https://www.postgresql.org/docs/current/sql-createview.html | USED → `saas/95` § What RLS Does Not Cover and § Banned Patterns (`security_invoker = true`) |
| pg-07 | Postgres seat (brave + WebSearch) | PostgreSQL 15 introduced `security_invoker` views | https://www.postgresql.org/about/news/postgresql-15-released-2526/ | REJECTED — read whole: the version floor is noise for a fleet whose databases all clear it, deleted under the D-062 literal ban |
| pg-08 | Postgres seat (WebFetch, two pages) | neither CREATE MATERIALIZED VIEW nor CREATE POLICY mentions RLS on materialized views; CREATE POLICY's target is defined as a table | https://www.postgresql.org/docs/current/sql-createpolicy.html | USED → `saas/95` § What RLS Does Not Cover (policies target tables; a materialized view holds its own copy) |
| pg-09 | Postgres seat (WebFetch) | `current_setting`: "throws an error unless _missing_ok_ is supplied and is `true` (in which case NULL is returned)" | https://www.postgresql.org/docs/current/functions-admin.html | USED → `saas/95` § Fail-Closed Default |
| pg-10 | Postgres seat (brave) | the 9.6 docs carry the same `missing_ok` semantics | https://www.postgresql.org/docs/9.6/functions-admin.html | DUPLICATE of pg-09 |
| pg-11 | Postgres seat (WebFetch) | `set_config`: "If _is_local_ is `true`, the new value will only apply during the current transaction … This function corresponds to the SQL command SET." | https://www.postgresql.org/docs/current/functions-admin.html | USED → `saas/95` § Tenant Context Propagation (the transaction-local form of `SET LOCAL`) and `.windsurf/rules/CLAIMS.yaml` row `set-local-no-bind-use-set-config` |
| pg-12 | Postgres seat (WebFetch + brave) | "A block containing an `EXCEPTION` clause is significantly more expensive to enter and exit than a block without one. Therefore, don't use `EXCEPTION` without need." | https://www.postgresql.org/docs/current/plpgsql-control-structures.html | USED → `saas/95` § Fail-Closed Default (the sub-select wrapper pays it once per query) |
| pg-13 | Postgres seat (WebFetch + exa + brave) | pg_dump: "By default, pg_dump will set row_security to off … If the user does not have sufficient privileges to bypass row security, then an error is thrown." | https://www.postgresql.org/docs/current/app-pgdump.html | USED → `saas/95` § What RLS Does Not Cover (backups run as `fabrik_admin` or a superuser) |
| pg-14 | Postgres seat (composition of two pages) | dumping every row therefore needs superuser, BYPASSRLS, or an unforced table owner | https://www.postgresql.org/docs/current/app-pgdump.html | DUPLICATE of pg-13 |
| pg-15 | Postgres seat (WebFetch) | "new rows that would be created via `INSERT` or `UPDATE` are checked against the expression specified in `WITH CHECK` … if false or null is returned then an error occurs" | https://www.postgresql.org/docs/current/sql-createpolicy.html | USED → `saas/95` § RLS Setup (`WITH CHECK` gates writes) |
| pg-16 | Postgres seat (WebFetch + brave) | "if no `WITH CHECK` expression is defined, then the `USING` expression will be used both to determine which rows are visible … and which new rows will be allowed to be added" | https://www.postgresql.org/docs/current/sql-createpolicy.html | USED → `saas/95` § RLS Setup (omitted `WITH CHECK` reuses `USING`) |
| pr-01 | practice seat (WebSearch + exa) | psycopg 3: server-side binding "doesn't work with SET or with NOTIFY" | https://www.psycopg.org/psycopg3/docs/basic/from_pg2.html | USED → `saas/95` § Tenant Context Propagation and `.windsurf/rules/CLAIMS.yaml` row `set-local-no-bind-use-set-config` |
| pr-02 | practice seat (WebSearch + exa) | psycopg: "Must use \"SELECT set_config('timezone', %s)\"" instead of `SET timezone to %s` | https://www.psycopg.org/articles/2020/11/24/psycopg3-adaptation/ | USED → `saas/95` § Tenant Context Propagation (`set_config` with a bound value) |
| pr-03 | practice seat (WebSearch) | asyncpg: `SET local statement_timeout = $1` fails with a syntax error — a PostgreSQL limitation, not the driver's | https://www.postgresql.org/message-id/837985448.725696.1732374064323%40sg2000-ap-3 | USED → `saas/95` § Tenant Context Propagation (in psycopg and asyncpg alike) |
| pr-04 | practice seat (exa) | protocol: parameters are only supported in SELECT, INSERT, UPDATE and DELETE | https://www.cybertec-postgresql.com/en/query-parameter-data-types-performance/ | USED → `saas/95` § Tenant Context Propagation (the protocol limit) and `.windsurf/rules/CLAIMS.yaml` row `set-local-no-bind-use-set-config` |
| pr-05 | practice seat (exa fetch) | Supabase's RLS guide writes the canonical policy as `using ( (select auth.uid()) = user_id )` | https://supabase.com/docs/guides/database/postgres/row-level-security | DUPLICATE of pr-06 |
| pr-06 | practice seat (exa fetch) | "Wrapping the function in some SQL causes an initPlan to be run by the optimizer which allows it to 'cache' the results versus calling the function on each row … You can only do this if the results … do not change based on the row data"; auth.uid()=user_id 179 ms → (select auth.uid())=user_id 9 ms | https://supabase.com/docs/guides/troubleshooting/rls-performance-and-best-practices-Z5Jjwv | USED → `saas/95` § RLS Setup (sub-select wrapper) and `.windsurf/rules/CLAIMS.yaml` row `rls-policy-performance` |
| pr-07 | practice seat (exa fetch) | "put an index on columns used in the RLS that are not primary keys or unique already … Improvement seen over 100x on large tables" | https://supabase.com/docs/guides/troubleshooting/rls-performance-and-best-practices-Z5Jjwv | USED → `saas/95` § RLS Setup (index every column a policy reads) and `.windsurf/rules/CLAIMS.yaml` row `rls-policy-performance` |
| pr-08 | practice seat (WebFetch) | PgBouncer's feature table lists SET/RESET as never compatible with transaction pooling | https://www.pgbouncer.org/features.html | USED → `saas/95` § Tenant Context Propagation (session-level `SET` is incompatible with transaction pooling) |
| pr-09 | practice seat (brave) | Heroku: "Never use SET SESSION or SET alone, which defaults to SET SESSION with transaction pooling" | https://devcenter.heroku.com/articles/best-practices-pgbouncer-configuration | DUPLICATE of pr-08 |
| pr-10 | practice seat (brave) | community issue: per-transaction context via `BEGIN; SELECT set_config('app.tenant_id', $1, true); …; COMMIT;` as the pooling-safe pattern | https://github.com/dockndevai/percona-pg-lab/issues/4 | DUPLICATE of pr-02 |
| pr-11 | practice seat (WebSearch + exa) | PostgREST sets `request.jwt.claims` and other request GUCs per request and switches to the JWT's role "for the duration of the HTTP request" | https://postgrest.org/en/v11/references/transactions.html | USED → `saas/95` § Dual-Mode RLS (compat keeps PostgREST's contract) |
| pr-12 | practice seat (WebSearch) | Supabase `auth.uid()` = `nullif(coalesce(current_setting('request.jwt.claim.sub', true), (current_setting('request.jwt.claims', true)::jsonb ->> 'sub')), '')::uuid` | https://github.com/supabase/auth/blob/master/migrations/20211202183645_update_auth_uid.up.sql | USED → `saas/95` § Dual-Mode RLS (Supabase's `auth.uid()` reads the claims' `sub`) |
| pr-13 | practice seat (WebFetch + WebSearch) | OWASP: "Treat client-supplied tenant identifiers as selectors only. Verify that the authenticated principal is authorized to act in the selected tenant." | https://cheatsheetseries.owasp.org/cheatsheets/Multi_Tenant_Security_Cheat_Sheet.html | USED → `saas/95` § Tenant Resolution and Membership and `.windsurf/rules/CLAIMS.yaml` row `owasp-tenant-selector-and-rls` |
| pr-14 | practice seat (WebFetch + WebSearch) — quote NOT verbatim; the second-opinion seat and the orchestrator re-fetched the page and the sentence "Row-level security should be enforced at the database for defence in depth" is absent | a paraphrase of OWASP on database-enforced RLS | https://cheatsheetseries.owasp.org/cheatsheets/Multi_Tenant_Security_Cheat_Sheet.html | REJECTED — read whole: the page carries no such sentence; the pack cites the verbatim lines in op-01 instead |
| op-01 | Fable second-opinion seat (WebFetch, two passes); re-fetched by the orchestrator (WebFetch) | OWASP: "This ORM helper is defense in depth, not complete enforcement"; "Connect the normal application request path with a least-privileged role that is neither a superuser nor a BYPASSRLS role"; "Do not serve ordinary tenant-scoped requests through a privileged connection"; "Re-establish the tenant context for every transaction"; "FORCE ROW LEVEL SECURITY applies policies to a table owner. It does not constrain a superuser or a role with the BYPASSRLS attribute." | https://cheatsheetseries.owasp.org/cheatsheets/Multi_Tenant_Security_Cheat_Sheet.html | USED → `saas/95` § Isolation Strategy, § Tenant Context Propagation and § Tenant Resolution and Membership (the `tenancy` warning), and `.windsurf/rules/CLAIMS.yaml` row `owasp-tenant-selector-and-rls` |
| op-02 | Fable second-opinion seat (WebFetch) | "The default is `PUBLIC`, which will apply the policy to all roles."; policies' target is a table | https://www.postgresql.org/docs/current/sql-createpolicy.html | REJECTED — read whole: confirms the pack's `TO PUBLIC` choice, which needed no change |
| op-03 | Fable second-opinion seat (WebFetch) | "the relevant `SELECT` policies will apply to `COPY table TO` statements. Currently, `COPY FROM` is not supported for tables with row-level security." | https://www.postgresql.org/docs/current/sql-copy.html | USED → `saas/95` § What RLS Does Not Cover (bulk loads) |
| op-04 | Fable second-opinion seat (WebFetch) | pg_dump `--role`: "causes pg_dump to issue a `SET ROLE` rolename command after connecting" | https://www.postgresql.org/docs/current/app-pgdump.html | USED → `saas/95` § What RLS Does Not Cover (`pg_dump --role=fabrik_admin`) |
| op-05 | Fable second-opinion seat (brave) | "After assigning a local value during a transaction and finishing the transaction, the setting becomes recognised, has an empty string value" | https://www.postgresql.org/message-id/CA+Q86ij0KDCB0G45G509-8q0DNR611gcKG-sSM83GA1EBL7boA@mail.gmail.com | USED → `saas/95` § Fail-Closed Default (it reads `''` after a transaction-local set ends) |
| op-06 | Fable second-opinion seat (brave) | bug #15646: "current behavior returns empty string instead of NULL (the initial value) after transaction is rolled back" | https://www.postgresql.org/message-id/15646-0d3caee9f6266f60@postgresql.org | DUPLICATE of op-05 |
| op-07 | Fable second-opinion seat (brave) | Tom Lane: "The GUC infrastructure doesn't recognize null as a value" | https://postgrespro.com/list/thread-id/2631762 | DUPLICATE of op-05 |
| op-08 | Fable second-opinion seat (WebFetch) | ddl-rowsecurity: "The only exceptions to this rule are `leakproof` functions … the optimizer may choose to apply such functions ahead of the row-security check" | https://www.postgresql.org/docs/current/ddl-rowsecurity.html | REJECTED — read whole: policy quals already run before ordinary user functions, so the pack needs no LEAKPROOF rule |
| op-09 | Fable second-opinion seat (brave) | asyncpg: `SET local statement_timeout = $1` → "syntax error at or near \"$1\""; "asyncpg does not do query argument interpolation in any form … unlike psycopg2" | https://github.com/sqlalchemy/sqlalchemy/discussions/7192 | USED → `saas/95` § Tenant Context Propagation (psycopg2 quotes client-side; psycopg 3 and asyncpg refuse) |
| op-10 | Fable second-opinion seat (exa) | the ALTER MATERIALIZED VIEW synopsis has no ENABLE/FORCE ROW LEVEL SECURITY action | https://www.postgresql.org/docs/current/sql-altermaterializedview.html | USED → `saas/95` § What RLS Does Not Cover (materialized views) |
| rv-01 | orchestrator (WebFetch), raised by the review | "For security, `search_path` should be set to exclude any schemas writable by untrusted users. This prevents malicious users from creating objects … that mask objects intended to be used by the function"; the docs set trusted schemas with `pg_temp` last | https://www.postgresql.org/docs/current/sql-createfunction.html | USED → `saas/95` § What RLS Does Not Cover (`SECURITY DEFINER`: pin `search_path`, `pg_temp` last) |
| rv-02 | orchestrator (WebFetch), raised by the review | "by default, execute privilege is granted to `PUBLIC` for newly created functions … you must revoke the default `PUBLIC` privileges and then grant execute privilege selectively" | https://www.postgresql.org/docs/current/sql-createfunction.html | USED → `saas/95` § What RLS Does Not Cover (`REVOKE EXECUTE … FROM PUBLIC`) |
| rv-03 | orchestrator (WebFetch), raised by the review | EXPLAIN ANALYZE reports "the number of rows removed by a filter condition" as a "Rows Removed by Filter" line | https://www.postgresql.org/docs/current/using-explain.html | USED → `saas/95` § What RLS Does Not Cover (side channels) |
| rv-04 | orchestrator (WebFetch), raised by the review | "By default, users cannot access any objects in schemas they do not own. To allow that, the owner of the schema must grant the `USAGE` privilege on the schema." | https://www.postgresql.org/docs/current/ddl-schemas.html | USED → `saas/95` § Dual-Mode RLS (`GRANT USAGE ON SCHEMA auth`) |
| rv-05 | orchestrator (WebFetch), raised by the review | RESET: "The default value is defined as the value that the parameter would have had, if no `SET` had ever been issued for it in the current session. The actual source of this value might be … per-database or per-user default settings." | https://www.postgresql.org/docs/current/sql-reset.html | USED → `saas/95` § Tenant Context Propagation (a role- or database-level default is what `RESET` restores) |
| rv-06 | orchestrator (WebFetch, first read for `core/app-audit-log.md`'s turn, ledger op-01 of 2026-09-23), raised by the review | "The right to modify or destroy an object is inherent in being the object's owner, and cannot be granted or revoked in itself … owners are always treated as holding all grant options" | https://www.postgresql.org/docs/current/ddl-priv.html | USED → `saas/95` § What RLS Does Not Cover (while the app role owns the tables it holds `TRUNCATE` and can alter or drop policies) |
| rv-07 | orchestrator (WebFetch), raised by the review | SET ROLE: "The current session user must have the `SET` option for the specified role_name, either directly or indirectly via a chain of memberships with the `SET` option." | https://www.postgresql.org/docs/current/sql-set-role.html | USED → `saas/95` § Dual-Mode RLS (`GRANT authenticated TO <request login role>` so the role drop can happen) and § Tenant Resolution and Membership (membership is what puts `service_role` within reach) |
