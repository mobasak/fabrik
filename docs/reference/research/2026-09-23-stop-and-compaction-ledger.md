# Research ledger — stop-and-compaction enforcement (2026-09-23)

Every fact the research seats of `/fabrik-spec` and its `/fabrik-spec-review` round 1 returned, filed whole before synthesis (D-352). The synthesis is `docs/superpowers/specs/2026-09-23-stop-and-compaction-enforcement-design.md`. Seats: `hooks` (Opus `fabrik-researcher`, Stop/SessionStart/PreCompact/prompt hooks), `compact` (Sonnet, compaction controls), `practice` (Sonnet, ask-vs-act field practice), `rv-c` and `rv-d` (round-1 researcher refuters, E1–E4/E7 and E5–E10). All fetched 2026-09-23.

| id | source | fact (verbatim as returned) | url | disposition |
|---|---|---|---|---|
| hooks-1 | hooks | Stop input fields: session_id, transcript_path, cwd, permission_mode, hook_event_name, stop_hook_active, last_assistant_message, background_tasks[], session_crons[] | https://code.claude.com/docs/en/hooks | USED → spec E1, § C1 Inputs |
| hooks-2 | hooks | "use this field rather than reading transcript_path: the transcript file isn't guaranteed to include the final message at Stop time on all versions." | https://code.claude.com/docs/en/hooks | USED → spec E1, § C1 Inputs |
| hooks-3 | hooks | "`decision` \| "block" prevents Claude from stopping. Omit to allow Claude to stop" · "`reason` \| Required when decision is "block". Tells Claude why it should continue" | https://code.claude.com/docs/en/hooks | USED → spec E1 |
| hooks-4 | hooks | "Check this value or process the transcript to avoid blocking on a condition that will never resolve. Claude Code overrides the hook and ends the turn after 8 consecutive blocks." | https://code.claude.com/docs/en/hooks | USED → spec E1, § What exists today (the warn-through) |
| hooks-5 | hooks | Stop `hookSpecificOutput.additionalContext` "keeps the conversation going through the same loop protections as decision: "block" … but the transcript labels it Stop hook feedback" | https://code.claude.com/docs/en/hooks | REJECTED — read whole: a softer continuation channel the design does not need because every deferral block must carry a reason |
| hooks-6 | hooks | SessionStart matcher values: startup, resume, clear, compact, fork; "\| compact \| Auto or manual compaction \|" | https://code.claude.com/docs/en/hooks | USED → spec E2 |
| hooks-7 | hooks | SessionStart input carries `source`; on resume also `seconds_since_last_response`, `context_tokens`, `prompt_cache_likely_expired` | https://code.claude.com/docs/en/hooks | USED → spec E2 (context_tokens scoped to resume; C5 uses preTokens instead) |
| hooks-8 | hooks | "SessionStart hooks that match the `compact` source \| Claude Code runs them and adds their output to the compacted context" · "Context that hooks added earlier \| Summarized with the rest of the conversation" | https://code.claude.com/docs/en/context-window | USED → spec E2, E4 |
| hooks-9 | hooks | "PreCompact hooks receive trigger and custom_instructions. For manual, custom_instructions contains what the user passes into /compact and is null when they pass nothing. For auto, custom_instructions is null." | https://code.claude.com/docs/en/hooks | USED → spec E3 |
| hooks-10 | hooks | "Exit with code 2 to block compaction." · "Claude Code discards a PreCompact hook's systemMessage and continue fields." | https://code.claude.com/docs/en/hooks | USED → spec E3; § Rejected alternatives (PreCompact block) |
| hooks-11 | hooks | "If compaction was triggered proactively before the context limit, Claude Code skips it and the conversation continues uncompacted. If compaction was triggered to recover from a context-limit error … the current request fails." | https://code.claude.com/docs/en/hooks | USED → spec § Rejected alternatives (PreCompact block) |
| hooks-12 | hooks | "PostCompact hooks receive trigger and compact_summary. The compact_summary field contains the conversation summary generated by the compact operation." | https://code.claude.com/docs/en/hooks | USED → spec E3 |
| hooks-13 | hooks | No hook output field or tool lets a hook or the model trigger compaction (not found in the PreCompact, PostCompact and Stop sections, the event table, context-window, hooks-guide) | https://code.claude.com/docs/en/hooks | USED → spec E3; Intake I1 |
| hooks-14 | hooks | UserPromptSubmit: "Plain stdout and the additionalContext value are each injected as a system reminder that starts with the hook's name; Claude reads both." | https://code.claude.com/docs/en/hooks | USED → spec § C3 (the prompt-time clear of an open DECISION block rides the existing UserPromptSubmit call) |
| hooks-15 | hooks | "Claude Code supports prompt-based hooks (type: "prompt") that use an LLM to evaluate whether to allow or block an action, and agent hooks (type: "agent") that spawn an agentic verifier with tool access." | https://code.claude.com/docs/en/hooks | USED → spec E7; judge-panel approach A |
| hooks-16 | hooks | Prompt hooks: "Send the hook input and your prompt to a Claude model, Haiku by default"; response `{"ok", "reason", "impossible"}`; "the reason is fed back to Claude as its next instruction and the turn continues, unless the response also sets impossible: true" | https://code.claude.com/docs/en/hooks | USED → spec E7 |
| hooks-17 | hooks | Prompt hook timeout default 30 s; agent hooks 60 s and "Agent hooks are experimental" | https://code.claude.com/docs/en/hooks | REJECTED — read whole: latency detail for approach A, which the panel ranked last on precision and spend |
| hooks-18 | hooks | "`/goal` is a session-scoped shortcut: you type a condition and it's active for the current session only. A Stop hook lives in your settings file, applies to every session in its scope" | https://code.claude.com/docs/en/goal | USED → spec E7; § Rejected alternatives (/goal) |
| hooks-19 | hooks | `/goal`: "If Claude keeps answering the evaluator without making progress (no tool use for several turns in a row), Claude Code stops the loop" · "One goal can be active per session." | https://code.claude.com/docs/en/goal | REJECTED — read whole: /goal is user-typed and session-scoped, so its loop details cannot shape a fleet rule |
| compact-1 | compact | `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`: "Set the percentage (1-100) of the auto-compact window at which auto-compaction triggers. Use lower values like `50` to compact earlier; the variable can't raise the threshold" | https://code.claude.com/docs/en/env-vars | USED → spec E6 |
| compact-2 | compact | `autoCompactEnabled` — "Turn automatic compaction off or on."; the window is set by `/autocompact <size>` (saves `autoCompactWindow`) or `CLAUDE_CODE_AUTO_COMPACT_WINDOW` | https://code.claude.com/docs/en/settings-reference | USED → spec E6, § C5 |
| compact-3 | compact | "Add custom compaction instructions: `/compact Focus on code samples and API usage` tells Claude what to preserve during summarization." + "# Compact instructions\n\nWhen you are using compact, please focus on test output and code changes" | https://code.claude.com/docs/en/costs | USED → spec E5, § C4 |
| compact-4 | compact | "Models running with a native 1M window, such as Sonnet 5, the Fable models, and Opus 4.7 and later on the Anthropic API, compact before the window fills, at about 967K tokens by default." | https://code.claude.com/docs/en/model-config | USED → spec E6 |
| compact-5 | compact | Re-injected after compaction: system prompt, project-root CLAUDE.md and unscoped rules, auto memory, invoked skill bodies ("capped at 5,000 tokens per skill and 25,000 tokens total"); the startup skill listing "is not re-injected after `/compact`" | https://code.claude.com/docs/en/context-window | USED → spec E4 |
| compact-6 | compact | the same seat also listed "fresh git status" and "up to 5 most-recently-modified files" as re-injected | https://code.claude.com/docs/en/context-window | REJECTED — read whole: the live "What survives compaction" table has no git-status or recent-files row (rv-c-2 re-fetched it), so the DRAFT's reliance on it was withdrawn |
| compact-7 | compact | "Use a `SessionStart` hook with a `compact` matcher to re-inject critical context after every compaction." | https://code.claude.com/docs/en/hooks-guide | DUPLICATE of hooks-8 |
| compact-8 | compact | "a claude-progress.txt file that keeps a log of what agents have done, and an initial git commit" · "an initializer agent that sets up the environment on the first run, and a coding agent that is tasked with making incremental progress in every session, while leaving clear artifacts for the next session" | https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents | USED → spec E8 |
| compact-9 | compact | "Effective context engineering for AI agents" frames context as a finite "attention budget" and covers compaction, tool-result clearing and memory | https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents | REJECTED — read whole: general framing that changes no step of this design, which rests on the harness facts instead |
| practice-1 | practice | "If the user's intent is unclear, infer the most useful likely action and proceed, using tools to discover any missing details instead of guessing." (the seat first returned a paraphrase; rv-d-1 fetched the verbatim) | https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices | USED → spec E8 |
| practice-2 | practice | "Claude stops when the work looks done. Without a check it can run, 'looks done' is the only signal available, and you become the verification loop" | https://code.claude.com/docs/en/best-practices | REJECTED — read whole: about verification loops for done-ness, not about asking the operator, so no step changes |
| practice-3 | practice | "For larger features, have Claude interview you first … Once the spec is complete, start a fresh session to execute it." | https://code.claude.com/docs/en/best-practices | REJECTED — read whole: its fresh-session advice contradicts the operator's standing ruling recorded in the spec, so it is not adopted |
| practice-4 | practice | "Never stop or hand back to the user when you encounter uncertainty — research or deduce the most reasonable approach and continue." | https://developers.openai.com/cookbook/examples/gpt-5/gpt-5_prompting_guide | USED → spec E8 |
| practice-5 | practice | "a Stop hook runs your check as a script and blocks the turn from ending until it passes. Claude Code overrides the hook and ends the turn after 8 consecutive blocks." | https://code.claude.com/docs/en/best-practices | DUPLICATE of hooks-4 |
| practice-6 | practice | Ralph plugin: "Always use `--max-iterations` as a safety net to prevent infinite loops on impossible tasks"; its README names the platform cap as 9 | https://github.com/anthropics/claude-code/blob/main/plugins/ralph-wiggum/README.md | USED → spec E9 and the recorded 8-vs-9 discrepancy |
| practice-7 | practice | "Stop hook (persistent-mode.mjs) never honors stop_hook_active — Claude Code's 9-block safety override fires on every persistence mode" | https://github.com/Yeachan-Heo/oh-my-claudecode/issues/3138 | USED → spec E9 |
| practice-8 | practice | "Three to four options with tradeoffs... Recommendation + counter-argument... The counter-argument is load-bearing. Without it, the recommendation is unfalsifiable." | https://github.com/ai-sdlc-framework/ai-sdlc/blob/main/ai-sdlc-plugin/skills/decision-rubric/SKILL.md | USED → spec E10, § C2 (Options carry what each changes) |
| practice-9 | practice | "The ADR must contain everything the agent needs to write correct code without asking follow-up questions... Decisions must be specific enough to act on." | https://github.com/vercel/ai/blob/08cdf6ae/skills/adr-skill/SKILL.md | REJECTED — read whole: an ADR-authoring rule for specs, already covered by the house decision ledger, so it adds no step here |
| rv-c-1 | rv-c | E1/E2/E3/E7 quotes located verbatim in the raw hooks.md (331,285 B) | https://code.claude.com/docs/en/hooks | DUPLICATE of hooks-4 |
| rv-c-2 | rv-c | live "What survives compaction" rows: System prompt/output style · Project-root CLAUDE.md and unscoped rules · Auto memory · Rules with paths: frontmatter (lost until re-read) · Nested CLAUDE.md (lost until re-read) · Invoked skill bodies · Hooks — no git-status row | https://code.claude.com/docs/en/context-window | USED → spec E4, § C3 item 4 (dirty files included) |
| rv-c-3 | rv-c | `context_tokens` appears only in the SessionStart resume input and PostModelSwitch; `preTokens` and `compact_boundary` are transcript terms, absent from hooks.md | https://code.claude.com/docs/en/hooks | USED → spec § C5, § V5 (preTokens) |
| rv-d-1 | rv-d | E8's first quote was not verbatim: the source reads "If the user's intent is unclear, infer the most useful likely action … instead of guessing." | https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices | DUPLICATE of practice-1 |
