# Research ledger — `core/cost-budget.md` currency pass, 2026-09-24

Every fact the two research seats and the Fable author-blind second-opinion seat (op) of file 29's turn returned — Claude Code (cc) and OpenRouter / confidence
calibration (or) — filed whole with its disposition (`CLAUDE.md` § External Knowledge; checked by
`scripts/check_research_ledger.py`). The synthesis is the revision of `.windsurf/rules/core/cost-budget.md`
and its `CLAIMS.yaml` rows (`pack: core/cost-budget.md`).

| id | source | fact | url | disposition |
|---|---|---|---|---|
| cc-01 | Claude Code seat (exa + WebSearch) | "With `--output-format json`, the response payload includes `total_cost_usd` and a per-model cost breakdown … Both figures are client-side estimates and can differ from your actual bill." | https://code.claude.com/docs/en/headless | USED → `core/cost-budget` § What the Ledger's Dollars Mean and `.windsurf/rules/CLAIMS.yaml` row `claude-code-total-cost-is-an-estimate` |
| cc-02 | Claude Code seat (exa) | the result message carries `duration_ms`, `num_turns`, `total_cost_usd`, `usage`, `modelUsage`, `session_id`, `is_error`, `result` | https://code.claude.com/docs/en/agent-sdk/typescript | USED → `core/cost-budget` § What the Ledger's Dollars Mean (`total_cost_usd`) and § Worked Example (`usage`) |
| cc-03 | Claude Code seat (exa) | a captured CLI JSON result shows `total_cost_usd` and `usage.input_tokens` / `output_tokens` / cache fields populated | https://github.com/anthropics/claude-code-sdk-python/pull/3 | DUPLICATE of cc-02 |
| cc-04 | Claude Code seat (exa fetch) | `usage` adds `cache_creation_input_tokens` and `cache_read_input_tokens` | https://code.claude.com/docs/en/agent-sdk/cost-tracking | REJECTED — read whole: the ledger records input and output tokens only, and the pack sets no rule on cache tokens |
| cc-05 | Claude Code seat (exa) | "Claude Max and Pro subscribers have usage included in their subscription, so the session cost figure isn't relevant for billing purposes" — "an estimate computed locally from token counts" | https://code.claude.com/docs/en/costs | USED → `core/cost-budget` § What the Ledger's Dollars Mean (an estimate, not money billed) |
| cc-06 | Claude Code seat (exa fetch) | "`total_cost_usd` and `costUSD` fields are client-side estimates, not authoritative billing data … computes them locally from a price table … Do not bill end users or trigger financial decisions from these fields." | https://code.claude.com/docs/en/agent-sdk/cost-tracking | USED → `core/cost-budget` § What the Ledger's Dollars Mean and `.windsurf/rules/CLAIMS.yaml` row `claude-code-total-cost-is-an-estimate` |
| cc-07 | Claude Code seat (exa) | field report: a Max-subscription `claude -p --max-budget-usd 1.00` run stopped with "Exceeded USD budget (1)" while `/usage` showed $0.0000 — the budget counts the list-price estimate | https://github.com/anthropics/claude-code/issues/85400 | USED → `core/cost-budget` § What the Ledger's Dollars Mean (it measures quota burn in API-dollar terms) |
| cc-08 | Claude Code seat (exa) | CLI reference: "`--max-budget-usd` — Maximum dollar amount to spend on API calls before stopping (print mode only). Spend from subagents counts toward the cap." | https://code.claude.com/docs/en/cli-reference | USED → `core/cost-budget` § The Watchdog's Caps (the flag the operator ruled out) and `.windsurf/rules/CLAIMS.yaml` row `claude-code-print-mode-caps` |
| cc-09 | Claude Code seat (exa) | CLI reference: "`--max-turns` — Limit the number of agentic turns (print mode only). Exits with an error when the limit is reached. No limit by default." | https://code.claude.com/docs/en/cli-reference | USED → `core/cost-budget` § The Watchdog's Caps and § Model Selection for Your Own LLM Callers (`--max-turns`), and `.windsurf/rules/CLAIMS.yaml` row `claude-code-print-mode-caps` |
| cc-10 | Claude Code seat (exa fetch) | model aliases: `default`, `best`, `fable`, `sonnet`, `opus`, `haiku`, `sonnet[1m]`, `opus[1m]`, `opusplan`; "On the Anthropic API, `opus` resolves to Opus 4.8 and `sonnet` resolves to Sonnet 4.6" | https://code.claude.com/docs/en/model-config | USED → `core/cost-budget` § The Watchdog's Caps (the `opus` alias — worded per op-05: the recommended Opus for your provider, not necessarily the newest) |
| cc-11 | Claude Code seat (WebFetch) | API list prices per MTok in/out: Opus 5 $5/$25, Opus 5.5 $4/$20, Sonnet 5 $2/$10, Haiku 4.5 $1/$5, Fable 5.1 $10/$50 | https://platform.claude.com/docs/en/about-claude/pricing | USED → `core/cost-budget` § The Watchdog's Caps (sizing: a $1 day covers a handful of Opus diagnoses) |
| or-01 | OpenRouter seat (exa fetch) | `anthropic/claude-3.5-haiku` is listed, "currently pointing to Claude 3.5 Haiku (2024-10-22)" | https://openrouter.ai/anthropic/claude-3.5-haiku | REJECTED — read whole: the page existing is not routability; the second-opinion probe found no OpenRouter endpoint and Anthropic lists the model retired (op-01, op-02) |
| or-02 | OpenRouter seat (exa fetch) | `anthropic/claude-opus-4` is still listed | https://openrouter.ai/anthropic/claude-opus-4 | REJECTED — read whole: the watchdog's expensive-model setting is documented as unused, so the pack sets no rule on it |
| or-03 | OpenRouter seat (exa fetch) | current Haiku id: `anthropic/claude-haiku-4.5` | https://openrouter.ai/anthropic/claude-haiku-4.5 | USED → `core/cost-budget` § The Watchdog's Caps (set the current Haiku id) |
| or-04 | OpenRouter seat (exa fetch) | current Sonnet id: `anthropic/claude-sonnet-5` | https://openrouter.ai/anthropic/claude-sonnet-5 | REJECTED — read whole: the pack names no Sonnet id and selects Claude by alias |
| or-05 | OpenRouter seat (exa fetch + brave) | current Opus id: `anthropic/claude-opus-5.5`, released 22 Sep 2026, "succeeding Claude Opus 5" | https://openrouter.ai/anthropic/claude-opus-5.5 | REJECTED — read whole: the pack names no Opus id and selects Claude by alias |
| or-06 | OpenRouter seat (exa fetch + WebSearch) | "OpenRouter automatically returns detailed usage information with every response, including … Prompt and completion token counts … Cost in credits … `usage: { include: true }` … deprecated and have no effect" | https://openrouter.ai/docs/cookbook/administration/usage-accounting | USED → `core/cost-budget` § What the Ledger's Dollars Mean (OpenRouter rows are real spend) |
| or-07 | OpenRouter seat (exa) | Tian et al. (EMNLP 2023): verbalized confidences of RLHF models are better calibrated than their token probabilities, "often reducing the expected calibration error by a relative 50%" | https://doi.org/10.18653/v1/2023.emnlp-main.330 | USED → `core/cost-budget` § Model Selection (relative gain only; see or-09) |
| or-08 | OpenRouter seat (brave) | Xiong et al.: "Vanilla verbalized confidence exhibits significant overconfidence and poor failure prediction" | https://arxiv.org/pdf/2306.13063 | USED → `core/cost-budget` § Model Selection (overconfident) and `.windsurf/rules/CLAIMS.yaml` row `llm-verbalized-confidence-overconfident` |
| or-09 | OpenRouter seat (exa + brave) | the disagreement is a prompting-method artifact; for LLMs of 70B+ parameters "the ECE is around 0.1" | https://arxiv.org/pdf/2412.14737v1.pdf | USED → `core/cost-budget` § Model Selection (ECE around 0.1, sensitive to how it is asked) and `.windsurf/rules/CLAIMS.yaml` row `llm-verbalized-confidence-overconfident` |
| or-10 | OpenRouter seat (WebFetch + WebSearch) | per-key credit limits: `limit`, `limit_remaining`, `limit_reset`; "Once a key reaches its limit, further requests on that key are rejected before they're sent to the provider"; a 402 is returned | https://openrouter.ai/docs/api_reference/limits | USED → `core/cost-budget` § What the Ledger's Dollars Mean (per-key limit, 402 on exhaustion — re-grounded on today's page wording, op-06) and `.windsurf/rules/CLAIMS.yaml` row `openrouter-usage-and-key-limits` |
| op-01 | Fable second-opinion seat (curl, public model list; not re-run by the orchestrator under the no-provider-calls rule) | `anthropic/claude-3.5-haiku` is absent from OpenRouter's 458-model list and its endpoints listing reports 0 endpoints (8 for `anthropic/claude-haiku-4.5`) | https://openrouter.ai/api/v1/models | USED → `core/cost-budget` § The Watchdog's Caps (the default fallback model is dead) |
| op-02 | Fable second-opinion seat (exa); re-fetched by the orchestrator (WebFetch) | pricing table: "Claude Haiku 3.5 (retired, except on Bedrock and Google Cloud)"; Opus 4.8 and Opus 5 both $5 / $25 per MTok; Haiku 4.5 $1 / $5 | https://platform.claude.com/docs/en/about-claude/pricing | USED → `core/cost-budget` § The Watchdog's Caps (retired on Anthropic's API) and `.windsurf/rules/CLAIMS.yaml` row `watchdog-default-fallback-model-retired` |
| op-03 | Fable second-opinion seat (WebFetch) | costs page: "Claude Code computes the dollar figure locally from token counts at list price" and "the multiplied figure also counts toward `--max-budget-usd`" | https://code.claude.com/docs/en/costs | USED → `core/cost-budget` § The Watchdog's Caps (why the operator ruled out a per-call dollar cap: session-init cache cost counts toward it) |
| op-04 | Fable second-opinion seat (WebFetch) | CLI reference: with `--continue` / `--resume`, "totals restored from earlier runs don't count toward it"; cap enforcement needs Claude Code v2.1.217 or later | https://code.claude.com/docs/en/cli-reference | REJECTED — read whole: the pack does not recommend `--max-budget-usd` for the watchdog, which the operator ruled out |
| op-05 | Fable second-opinion seat (exa) | model-config: "`opus` — Uses the latest Opus model" yet "On the Anthropic API, `opus` resolves to Opus 4.8"; "Aliases point to the recommended version for your provider and update over time" | https://code.claude.com/docs/en/model-config | USED → `core/cost-budget` § The Watchdog's Caps (the recommended Opus, not necessarily the newest) |
| op-06 | Fable second-opinion seat (exa + WebFetch) | limits page today: "Per-key credit limits — an optional spending cap configured on an individual API key" with `limit`, `limit_reset`, `limit_remaining`; "If `limit_remaining` on the key is exhausted, raise the key's credit limit or wait for it to reset"; the "before they're sent to the provider" sentence is not on the page | https://openrouter.ai/docs/api_reference/limits | USED → `core/cost-budget` § What the Ledger's Dollars Mean and `.windsurf/rules/CLAIMS.yaml` row `openrouter-usage-and-key-limits` |
| op-07 | Fable second-opinion seat (exa) | "Both success and error result messages include `usage` and `total_cost_usd`." | https://code.claude.com/docs/en/agent-sdk/cost-tracking | USED → `core/cost-budget` § Worked Example (record error results too) |
