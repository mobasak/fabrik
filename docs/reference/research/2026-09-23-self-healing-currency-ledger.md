# Research ledger — `core/self-healing.md` currency pass, 2026-09-23

Every fact the one research seat of file 24's turn returned, filed whole with its disposition (`CLAUDE.md` § External
Knowledge; checked by `scripts/check_research_ledger.py`). The pack's other claims are about the vendored fabrik-lib
modules and the hub's deploy path, and were grounded by executing that code, not by web research. The synthesis is
the rewrite of `.windsurf/rules/core/self-healing.md` and its `CLAIMS.yaml` rows.

| id | source | fact | url | disposition |
|---|---|---|---|---|
| sh-ext-01 | external facts (Sonnet researcher) | X1 \| PARTLY \| "Similar to `always`, except that when the container is stopped (manually or otherwise), it isn't restarted even after Docker daemon restarts." + moby restartmanager.go "backoffMultiplier = 2 / defaultTimeout = 100 * time.Millisecond / maxRestartTimeout = 1 * time.Minute" — an OOM kill (exit 137) is an exit, so unless-stopped restarts it; the backoff figures live in moby source, not docs.docker.com \| https://docs.docker.com/engine/containers/start-containers-automatically/ \| WebFetch, WebSearch, brave-search \| 2026-09-23 | https://docs.docker.com/engine/containers/start-containers-automatically/ | USED → `core/self-healing.md` ladder row 1 (unless-stopped restarts the OOM-killed container, with back-off); the back-off constants stay out of the pack |
| sh-ext-02 | external facts (Sonnet researcher) | X2 \| TRUE \| "Docker records OOM kills in the container's state as OOMKilled: true" / "check system logs for OOM killer activity with `dmesg \| grep -i \"killed process\"` or `journalctl -k`" — no source puts the kernel OOM line in the container's own `docker logs` stream \| https://www.netdata.cloud/guides/docker/docker-oomkilled/ \| WebSearch (two queries, multiple corroborating sources) \| 2026-09-23 | https://www.netdata.cloud/guides/docker/docker-oomkilled/ | USED → `core/self-healing.md` ladder row 1 symptom (OOMKilled in docker inspect; the oom-killer line is in the host kernel log, not docker logs) |
| sh-ext-03 | external facts (Sonnet researcher) | X3 \| TRUE \| "Return the current number of connections in this pool." (get_size); "Return the maximum allowed number of connections in this pool." (get_max_size); Pool.close "Wait until all pool connections are released, close them and shut down the pool."; acquire() accepts `timeout` — there is no `pool.size`/`pool.max` attribute \| https://magicstack.github.io/asyncpg/current/api/index.html \| WebSearch, WebFetch \| 2026-09-23 | https://magicstack.github.io/asyncpg/current/api/index.html | USED → `core/self-healing.md` ladder row 6 (get_size / get_max_size / acquire(timeout) / close) |
| sh-ext-04 | external facts (Sonnet researcher) | X4 \| TRUE \| "Random wait with exponentially widening window." (wait_random_exponential); "Wait strategy that applies exponential backoff." (wait_exponential, non-jittered); "Wait strategy that applies exponential backoff and jitter." (wait_exponential_jitter exists) \| https://tenacity.readthedocs.io/en/latest/api.html \| WebSearch, WebFetch \| 2026-09-23 | https://tenacity.readthedocs.io/en/latest/api.html | USED → `core/self-healing.md` anti-pattern 1 (wait_random_exponential or wait_exponential_jitter; bare wait_exponential banned) |
| sh-ext-05 | external facts (Sonnet researcher) | X5 \| TRUE \| "class TimeoutException(TransportError): The base class for timeout errors." and "class ConnectError(NetworkError): Failed to establish a connection." \| https://github.com/encode/httpx/blob/master/httpx/_exceptions.py \| WebFetch, exa \| 2026-09-23 | https://github.com/encode/httpx/blob/master/httpx/_exceptions.py | USED → `core/self-healing.md` ladder row 4 (httpx.TimeoutException / httpx.ConnectError) |
| sh-ext-06 | external facts (Sonnet researcher) | X6 \| TRUE \| "Retry-After = HTTP-date / delay-seconds" ; RFC 6585 429 "MAY include a Retry-After header indicating how long to wait before making a new request"; MDN: used with 503, 429 and redirects \| https://github.com/httpwg/http-core/blob/main/httpbis.abnf \| exa, WebFetch, WebSearch \| 2026-09-23 | https://github.com/httpwg/http-core/blob/main/httpbis.abnf | USED → `core/self-healing.md` ladder row 3 (Retry-After is delay-seconds or an HTTP-date) |
| sh-ext-07 | external facts (Sonnet researcher) | X7 \| TRUE \| "This document requests the allocation of an IPv4 /10 address block to be used as Shared Address Space to accommodate the needs of Carrier-Grade NAT (CGN) devices." \| https://www.rfc-editor.org/rfc/rfc6598.html \| WebSearch, WebFetch \| 2026-09-23 | https://www.rfc-editor.org/rfc/rfc6598.html | USED → `core/self-healing.md` worked example, closing paragraph (carrier-grade NAT shares one address across subscribers) |
