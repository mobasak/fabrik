# Decisions

Append-at-top. One row per decision; rows are IMMUTABLE — a changed decision gets a NEW row whose
what-cell opens "supersedes D-NNN:"; the old row is never edited. WHY ≤ 2 lines; the full rationale
lives at the WHERE links. Subagents and the pipeline never hold the pen — the dispatching session
appends. Query: grep this file first; fleet-wide: `python3 /opt/fabrik/scripts/decisions.py <term>`
(`--check` validates ledger integrity — its own `--help` is the current check inventory).

| id | when | who | what (the decision) | why | where |
|---|---|---|---|---|---|
| D-103 | 2026-09-03 | operator ("session limit passed 95% and it did not switch automatically? fix it … second time today") + fleet (hub) build | **The rotation tick trips on the PROJECTED reading — reading + the burn since the previous tick — on both legs** (5h at `ROTATE_THRESHOLD` 98, weekly at the account's `caps.json` cap); the previous tick's reading is remembered per account+window in `state/tick-last-reading.json` and a burn is inferred only for the same account, the same window (reset epoch unchanged) and a memory ≤ 15 min old; corrupt/unwritable memory degrades to the plain threshold. **And the board's 20 s fast path fires at the DRAIN line (85) whenever its probe is blind** — a fallback payload carries `probe_failed`; the tick then reads live. The 98 rule of the same day stands unchanged in intent. Reversible (one helper + one expression each). | Measured on the 19:50 episode: the tick logged ob@ at 89 → 93 → 96 ("below 98, no flip") and the next tick found it at 100 with the operator already switched by hand — at a 5-minute cadence and a 3–4% inter-tick burn there is no tick in [98, 100). A line checked every 5 minutes must be crossed BEFORE the wall. The board's fast path was blind for the whole crossing: `--status --json` timed out 7× in a row (60 s each) and the trigger only ever saw the last GOOD reading, 96. Rejected: a 1-minute cron (five times the usage probes; the crontab is operator-owned and classifier-blocked) and lowering the threshold back to 95 (a fixed number is wrong at every other burn rate). The 01:07 episode (can@ at 100%, NO successor) was the caps working: ob 92 ≥ 80, sarp 91 ≥ 90, mob at 95 — the pool was spent, not a defect. | `scripts/sysadmin/claude_rotate.py::_tick_burn` + `_fleet_flip_leg` (twin `scripts/aro-wake/claude_rotate.py`) · `tests/test_claude_rotate_v2.py` (4 tests, 2 red-first) · `scripts/sysadmin/quota_dashboard.py::_maybe_trigger_rotation` (+ `tests/test_quota_dashboard.py`, 3 tests) · `docs/workstation/claude-account-rotation.md` · `docs/workstation/quota-dashboard.md` · `docs/workstation/hooks-index.md` § 2c |
| D-101 | 2026-09-03 | intel (adversarial review of both orchestrator chains vs the assembled corpus — operator request "our commands have review loops and enforcement the mega epic commands don't") | **The orchestrator chains are PATCHED IN PLACE through the assembler — never moved into `commands/_sources/`.** The 17 chain docs stay canonical under `docs/orchestrator/**`; the fixes land as wrapper-renderer changes (`_render_orch_wrapper` attaches close-feedback + NEXT like `render()`/`_emit_skill`), block substitution for the hand-inlined fragment twins, and text edits — and the ettw chain is re-converged against its own checklist items 133–138 (0 of 13 docs carry `## Guardrails — never`; 00/03/04 have no self-audit). Rejected: assembling the 5,706 lines into `_sources` (it would strip the chains' own `Reads:`-budget / hollow-citation discipline, checklist mega 102 / ettw 132). | Why: measured, not assumed — the chains ALREADY carry converge loops (mega 04, ettw 01R/08/10), a NEXT line per doc, and the run-record block on every wrapper; what they lack is exactly five corpus standards that stop at the wrapper boundary or postdate their last convergence (2026-07-18), plus one BLOCKER: seven Does-NOT lines forbid `git commit`/`git push` while citing `CLAUDE.md` HARD STOPS as authority — the row at `CLAUDE.md:216` mandates the opposite, and the Stop hook enforces it. Fifteen findings (R1–R15), 32 finder rows refuted with quotes, 105 `path:line` anchors re-derived at close. | Where: `docs/development/reviews/2026-09-03-orchestrator-chains-corpus-review.md` (ledger, verdict, fix order R1 → R14 → R2/R3 → R5/R13 → rest; beat: infra for the chain, intel for the spec). Consequence for D-099: the CONVERGED multi-agent spec owes a re-freeze (SR1–SR6 in § 4) BEFORE the operator's approval — its loop step 6 collides with R1, its `--assign`-after-04 is circular (R9), and 07's merge target is main-checkout-shaped (R10). Reversible (a review artifact + a fix order; no surface changed yet). |
| D-100 | 2026-09-03 | operator ("can you fix the mesh" — infra busy) + fleet (hub) build | **The pane self-watch is a STANDING watch, not one-wake-then-exit** — one arm per session for its life, one wake per death record, a duplicate arm exits at once under a per-sid flock; the `rate_limit` wait re-asks the quota helper every slice (a manual account switch ends it); the ARM order reaches sync-excluded repos through a user-level SessionStart hook (`scripts/sysadmin/claude_selfwatch_orient.sh`) instead of a per-repo `session_orient.py`. The "RE-ARM after every delivered wake" order is RETIRED from both contracts. Reversible (script + hook + prose; the old one-shot script is in the DR store's history). | Measured on the 2026-09-03 429/529 storm (16 h transcript scan): 18 `/opt` sessions died on an API error and 17 carry a typed "proceed" / "switched the account" after one (a last-error-only read gives 8); 429 is in every one of the 17 — they slept toward the DEAD account's reset clock after the operator had already switched (the rotation never touches death markers); the 3 fabrik-lib sessions never armed a watch (no orient hook); at least one session died a second time before the agent re-armed. A waker whose continuity depends on agent discipline after every wake is a waker that fails exactly in a storm; a standing watch removes the discipline from the loop. Class fix over per-repo hooks: the user-level hook covers every future sync-excluded repo, not one. | `~/.claude/bin/claude-selfwatch.sh` (DR-mirrored; fixtures W6/W7/W8 in `claude-mesh-test.sh`) · `scripts/sysadmin/claude_selfwatch_orient.sh` + `tests/test_claude_selfwatch_orient.py` · `.claude/hooks/session_orient.py` · `CLAUDE.md` § Orient (a) · `templates/governance/CLAUDE.md` § Orient (a) · `docs/workstation/hooks-index.md` |
| D-099 | 2026-09-03 | intel (`/fabrik-spec` → `/fabrik-spec-review`, 7 passes; the operator's vision 2026-09-03 + "proceed") | **Multi-agent-per-repo operating model spec CONVERGED — N named sessions per project repo, N−1 of them in `claude --worktree` isolation, one main-checkout merge owner.** Three derived defaults stated for operator override, not asked: (a) the tail = § Pipeline from `5-certify`, run by agent-1 (`/fabrik-features` REFRESH → user-test \| service-test → `/fabrik-deploy-checklist` → `/fabrik-release` → Gate 2); (b) projects first — the hub's own three sessions stay as they are until the assemble-prune (CLAUDE.md:150) and sync-reads-main hazards close; (c) agent-1 merges in `epic_order` phase order, rebase-first, `--no-ff`, `rerere` on. NOT adoption: the approval gate that follows is the operator's half of the I9 joint decision. | Why: hunk-level absorption under the contract's strictest pathspec form (mail 01M1KM3WH173JVEWD8RDMB9S1P) — every existing protection is path-level or recovery; separate working trees plus Claude Code's isolation enforcement make the absorbing primitive unreachable by construction. Review: 7 passes, findings 18→1→2→4→2→1→0, md5 285bb8df no-op on a full read; the closing re-derivation corrected six of the author's own measurements, incl. "`--worktree` hidden from `--help`, count 0" (it is documented at help line 250 — a bounded measurement read as absence). | Where: `docs/superpowers/specs/2026-09-03-multi-agent-per-repo-design.md` (Status: CONVERGED); supersedes in part the 2026-08-12 roles spec's single-writer rationale (its `CLAUDE_AGENT` identity mechanism stands). Reversible (a design text; the spec's retirement path is reverting the four emitted artifacts). Next: operator approval → `/fabrik-plan-after-chat`. |
| D-098 | 2026-09-03 | operator directive (same mechanism for the DISTRIBUTED contract) | **THE PROJECT-FACING CONTRACT GETS THE SAME MAIL-SIZING AND SESSION-START MECHANISM — fleet-wide, 47 repos.** (Count corrected in place 2026-09-03 from "49": that was `ls /opt/*/CLAUDE.md`, which counts the HUB's own contract and sync-EXCLUDED `fabrik-lib` as targets. Measured post-distribution: 47 project copies, all 47 byte-identical to hub HEAD, 0 divergent.) `templates/governance/CLAUDE.md` gains, all edited INSIDE the clauses that already own the subject: (a) three session-start duties in § Orient — ARM the self-watch, PROBE the assigned MCPs, CATCH UP via `/fabrik-catchup` and/or session-recall on a NEW chat; (b) per-agent OWNERSHIP made explicit in § Behavior's shared-repo bullet — each agent owns a task/epic/plan and that ownership is DOCUMENTED via an `**Owner:**` line on the plan/epic plus `.fabrik/plan-locks/<plan>.json` `owned_paths`; locks stop path collisions, the Owner line names the person to defer to (measured: for PLANS this promotes an EXISTING convention — 27 `**Owner:**` uses across 10 of 117 project plans; for EPICS it is genuinely NEW — 0 of 10 carry one. Corrected in place 2026-09-03: the original row claimed "rather than inventing one" for both, a claim wider than its measurement — epics were never counted); (c) the § fabrik-mail HANDLE-NOW chain gains a mandatory SIZE step, with the operator's reassignment override. **SIZING has THREE outcomes here, not the hub's two** — a defect in a Fabrik-owned/synced surface is filed UPSTREAM, never planned or patched locally (editing the synced copy is a HARD STOP); only then SPEC/PLAN vs RIGHT-NOW, and every right-now fix ships with `/fabrik-review-scoped`. That third route was a review finding: the first draft copied the hub's two-way split and would have told 49 repos to plan a local fix for a synced-file defect, contradicting their own HARD STOP. **The hub/template asymmetry is deliberate and both are correct for their reader**: the hub OWNS synced sources ('editing one IS a fleet-wide change'), a project may never touch them. Hub `CLAUDE.md` also aligned to name `/fabrik-catchup`, so the two contracts do not drift. Cost: template +2758 bytes (+4.50%), hub +131 (+0.19%). CLASS: reversible — contract text; but note the template IS a governance-sync trigger, so this one distributes to 49 repos on the post-commit sync, unlike the hub file. | a project agent receiving mail had no instruction to decide whether it was a five-minute fix or a spec, and no session-start duty at all — while the self-watch that revives a dead pane is ordered by a hook that goes silent on exactly the compact-resume path | templates/governance/CLAUDE.md § Orient · § Behavior (shared repo) · § fabrik-mail (HANDLE-NOW) · CLAUDE.md § Orient · supersedes nothing; extends D-097 (the hub-side directive, itself renumbered from a colliding D-094) to the fleet |
| D-097 | 2026-09-03 | operator directive (mail ownership · triage-before-work · session-start duties) — minted as a SECOND `D-094` in 713846ec (id collision with the quota-rotation row); renumbered to the next free id 2026-09-03, content untouched, zero references outside this ledger (`grep -rn D-094` over *.md/*.py/*.yaml) | **MAIL IS SIZED BEFORE IT IS WORKED, AND THREE SESSION-START DUTIES ARE NOW CONTRACT.** RULED: (a) a hub message is worked by its ADDRESSEE — beats stay documented (§ Behavior's FEEDBACK-IS-RECIPROCAL bullet + `docs/reference/agents/`) — **except** when the operator hands one agent another's mail on an urgent turn, which is explicitly permitted provided the handler names whose beat it was and routes anything found beyond the fix back to them; (b) the handle-now chain gains a mandatory **SIZE it** step between validate and do — a validated request is either SPEC/PLAN work (new mechanism, vendored/synced surface, schema, auth, >5 files), said so in the reply and opened at its pipeline stage rather than half-built inline, or a RIGHT-NOW fix; (c) **every right-now fix ships with `/fabrik-review-scoped`** (heavy surface ⇒ full `/fabrik-review`), which § Completion Contract 1a already ordered BEFORE the reply — the mail clause now points at it instead of restating it; (d) § Orient gains three session-start duties: ARM the self-watch, PROBE the assigned MCPs, CATCH UP via session-recall. **The self-watch duty is the load-bearing one**: `session_orient.py:239-243` skips its arming line on `source=compact` and headless, so a session that never armed pre-compact is never told again, and the contract is the only backstop. Self-review of the edit caught three defects in it before commit — `$CLAUDE_SESSION_ID` does not exist on this box (an empty arg arms a watch that protects nothing; live watches carry a literal uuid), a pointer to a § Behavior 'routing table' that is inline prose, and a restatement of § Past sessions' own triggers. REJECTED: a new enforcement check for mail triage — fire rate unmeasured and the Stop hook already blocks record-less code edits (FIX DIRECTIVE 5). Cost: +6 lines on 395. CLASS: reversible — contract text, hub-local (`CLAUDE.md` is NOT in the governance-sync files filter, verified against `.pre-commit-config.yaml:134`, so this distributes nowhere). | mail was being worked without anyone deciding whether it was a five-minute fix or a spec, and the only revival channel a dead pane has was ordered by a hook that goes quiet on exactly the resume path this session took | CLAUDE.md § Orient (session start) · § Behavior (the mail HANDLED-NOW clause) · .claude/hooks/session_orient.py:239-243 · docs/workstation/hooks-index.md |
| D-096 | 2026-09-03 | operator ruling ("shouldn't it be before /fabrik-deploy-plan or /fabrik-release?" → "move it") + fleet (hub) grounding | **`/fabrik-deploy-checklist` moves to AFTER end-to-end certification, directly before `/fabrik-release`** — the parity contract freezes on the CERTIFIED build. Why: certification consumes nothing from the contract (0 mentions in `fabrik-user-test.md` / `fabrik-service-test.md`); its only consumers are the release precondition and the verify run; and certification produces fixes that move exactly what the contract freezes (routes, env set, migration head, compose services), so the D-091 position (freeze before the gauntlets) forced a version bump after every certification fix. Where: both CLAUDE.md contracts § Orient `6-release` row + § Pipeline flow (fleet-synced), the assembler NEXT map (features REFRESH → certification; user-test/service-test → checklist → release), the checklist's previous-line + `_fragments/deploy-chain.md`, `docs/reference/deployment-verification.md`, `docs/FEATURES.md`, the dashboard's `PIPELINE_ORDER` (+ test). Supersedes D-091's pipeline-position clause only; the rest of D-091 stands. Reversible (a text order; no artifact shape changes). |
| D-095 | 2026-09-03 | fleet (hub), on tryton-crm's findings 01M1J0QP8JW4W7J1XYXFXMYKGQ + 01M1J185FSQN43SZ75EP0CZCZJ from their v2/v3 re-run | **The parity contract DECLARES its container-leg service** (`CONTAINER_LEG_SERVICE`, read by the runner from `--header`; empty = the project's app service) — the runner never assumes "the app container": a DB-free bridge in front of a stateful backend is a common stack shape, not tryton-crm's special case, and the leg container must carry the comparator's runtime deps (`python-dotenv` at module level in `libs/health_probe`). **Comparators follow ONE test** — does this number change when a user does their job? — never an example list; derived stores that mirror a row set (filestore ↔ attachment rows, search index ↔ documents) take the same comparator and switch together. REJECTED: guarding the comparator's dotenv import on the hub's vendored copy — the module is vendored AS SHIPPED from fabrik-lib (D-086); the dependency is named in both commands instead, and the import guard is fabrik-lib's call. CLASS: reversible (a declaration with an empty default; command text). | the first project to re-run the re-rendered command found the runner's one remaining assumption (which container) and the rule text's one remaining example-list; both surfaced only by running the leg for real | templates/scaffold/scripts/verify_prod_parity.py (`CONTAINER_LEG_SERVICE`, `--header`) · commands/_sources/fabrik-deploy-verify.md § Phase 6 container leg · commands/_sources/fabrik-deploy-checklist.md § Phase 2 · docs/reference/deployment-verification.md · D-091 · D-086 |
| D-094 | 2026-09-03 | operator directive (three rules: wider board · probe every 20s · rotate at 98% on the 5h window) | **Quota rotation: the flip threshold is 98 on the session window (was 95) and the dashboard is the fast path.** The dashboard server probes every 20s WITHOUT a viewer (supersedes the 2026-08-18 probe-on-view design — the operator wants the board and the rotation decision current at 20s granularity; four usage probes per 20s is the accepted price) and invokes `claude_rotate.py --tick` the moment the active account's 5h window ≥ 98% or it is cap-walled (once per 120s cooldown; the tick keeps dwell/pause/successor validation/its own lock); the `*/5` cron tick remains the backstop. `ROTATE_THRESHOLD` default 98 through one helper (`_rotate_threshold()`); the weekly leg is decided SEPARATELY — at the account's `caps.json` cap when one exists (the cap IS the operator's weekly rule: can/mob 99, sarp 90, ob 80), at the threshold otherwise (the scoped review caught `min(threshold, cap)` tripping a cap of 99 at 98). The board's tick runs on its own thread under the cron's `flock` lock; `generate()` is serialized. Board width 1600px; rows in ROTATION order (active · next · then …, ineligible last — the read-only mirror of the picker). CLASS: reversible (env knobs: `ROTATE_THRESHOLD`, `QUOTA_DASH_PROBE_INTERVAL_S`, `QUOTA_DASH_REFRESH_S`, `QUOTA_DASH_TRIGGER_COOLDOWN_S`). | a 5-minute cron granularity cannot honour "as soon as"; the board already probes all four accounts, so it is the natural place to fire the tick — no second probe, no new cron line (crontab writes are classifier-blocked anyway) | scripts/sysadmin/quota_dashboard.py (`_start_probe_loop`, `_maybe_trigger_rotation`, `PROBE_INTERVAL_S`, `TRIGGER_*`) · scripts/sysadmin/claude_rotate.py (`_rotate_threshold`) · tests/test_quota_dashboard.py · tests/test_claude_rotate_v2.py · docs/workstation/quota-dashboard.md · docs/workstation/claude-account-rotation.md · D-083 (broadcast policy) |
| D-093 | 2026-09-03 | intel, `/fabrik-plan-review` (lifecycle branch) P22–P23 | **FLYWHEEL PLAN ARCHIVED — and its "48 vendored copies" blast radius was WRONG, in the one direction nothing can report.** `/fabrik-plan-review` re-invoked on a plan already at `Status: EXECUTED` takes the command's lifecycle branch (verify-done → `git mv` to `archived/`), not a re-convergence. Verify-done raised TWO defects, both fixed before the move: (a) Phase G's header still read `PARTIALLY EXECUTED — G1 and G3 reach the cross-repo gate ... Both need the operator's explicit word`, contradicting its own ✅ sub-steps and the plan's Status line — the operator gave that word and both shipped in canonical `a66c6e29` (re-derived at `pg_ledger.py:62`, `ledger.py:185`, `agent.py:207`); (b) **the population is 50 live vendored copies, not 48, and 2 are structurally unreachable by the sync** — `ai-model-catalog/engine/libs/subagents` and `whatsapp-agent/src/libs/subagents`. `sync_enforcement_to_projects.py:496` writes `project_dir / vendored_rel`, a FIXED path and never a search, and its `mkdir(parents=True)` creates a correct-but-UNUSED top-level copy beside the stale one each repo actually imports (`microbench_coding_direct.py:66` inserts `SCRIPT_DIR`; `check_imports_resolvable.py:529` inserts `ROOT/"src"`). Re-derived depth-unbounded: 63 total − 8 `/opt/archived/**` − 5 hub worktrees = 50 live, 2 without `failure_reason`; the sync glob `ls -d /opt/*/libs/subagents` returns 48 because a single `*` cannot descend. CONSEQUENCE: the C2 re-benchmark dispatched to `ai-model-catalog` would have run on the copy that cannot write `failure_reason` — the exact column C2 exists to produce — and would have looked successful while settling nothing. Sync blind spot filed to infra (`01M1J0HNBTQ2XPV5J0AR1ZFFFK`); C2 consequence routed to `ai-model-catalog` (`01M1J0K84DGPBTK68B9VKFWYFN`) — cross-repo, so routed, never edited. CLASS: reversible (a plan move + a record correction; the `git mv` undoes). | a completion claim was verified at the writer and at the store while the actual PRODUCER ran a third copy neither check looks at — the plan's own checklist row 9 (bounded search read as a total) firing on the plan's own completion claim | docs/development/plans/archived/2026-09-02-plan-1-flywheel-recording.md § Pass Ledger P22–P23 · § Context Ledger (corrected blast radius) · § Phase G · scripts/sync_enforcement_to_projects.py:496 · mail 01M1J0HNBTQ2XPV5J0AR1ZFFFK · 01M1J0K84DGPBTK68B9VKFWYFN |
| D-092 | 2026-09-02 | intel, `/fabrik-execute-plan` (row minted 2026-09-03 — the flip shipped without one) | **THE SUBAGENT LEDGER GAINED SIX COLUMNS, FLEET-WIDE — recorded late because an EXECUTED flip minted no row.** One additive migration on the shared `subagent_runs` table: `failure_reason` · `queue_s` · `tokens_out` · `tokens_in` · `run_label` · `corpus_id`, all nullable, applied to BOTH live databases (local `fabrik_analytics` as its `postgres` owner; postgres-main over SSH), idempotent, 13,014 existing rows unaffected. Before it the table's entire failure vocabulary was `''|capped|done|error|out_of_scope|scored`, so OUR price cap was indistinguishable from a model failure, OUR turn ceiling from a provider stall, and benchmark queueing from model latency — three wrong model verdicts came out of that gap in one day. Shipped via canonical `/opt/fabrik-lib/subagents` (`a66c6e29`, on `origin/main`) + re-vendor. ⚠️ Two hand-built row tuples that `_COLS` does NOT drive had to grow with `_INSERT`, and BOTH were caught by EXECUTION, never inspection: `record_run`'s (a placeholder-count check printed MATCH while every insert raised into the outbox) and `set_quality`'s (`_append_outbox` zips against `_COLS` with `strict=True`, so verdicts silently stopped outboxing). CLASS: **ONE-WAY** — a shipped schema change on a shared table distributed to 48 vendored copies and two databases. BUDGET: the migration is additive and nullable, so old-shape inserts keep working; cost of being wrong is a dead column, not a break. KILL: a column with no producer after 90 days is dropped (`tokens_in` is already in that state by construction and is recorded as such). CONFIDENCE: high on `failure_reason` (it reversed three verdicts immediately), medium on `corpus_id`/`run_label` (no consumer yet). COUNTER: the case against was six separate migrations or none — rejected, every column rides the same DDL. TRIPWIRE: `failure_reason` still NULL on >50% of non-`done` rows after two weeks means the derivation, not the column, is wrong. CLOSE-OUT: 2026-10-02, re-measure column coverage the way the plan measured it. | the flywheel could not record WHY a run failed, so it was measuring our own configuration and calling it model quality | docs/development/plans/archived/2026-09-02-plan-1-flywheel-recording.md § Phase F · /opt/fabrik-lib/subagents/subagents/pg_ledger.py:62 · ledger.py:185 · fabrik-lib a66c6e29 · hub 64e7f633 |
| D-091 | 2026-09-02 | operator ruling ("when we are ready to deploy we must run this command, that is all") + fleet (hub) `/fabrik-review` of tryton-crm's first real freeze | **`/fabrik-deploy-checklist` has ONE source — what the project ships; the A/B/C modes are RETIRED** (a project accumulates many specs and plans, none describes the whole deployed product; specs reach the contract only through `FEATURES.md`). **The parity contract is MULTI-SITE**: every row declares where it runs — `hub` (public surface, the project's checkout), `container` (inside the running app on the VPS: DB, redis, internal network), `host` (the VPS host: `docker ps`, volumes) — `/fabrik-deploy-verify` runs one leg per site, `docker cp`s the gitignored vendored comparator + the contract into the app container for its leg, emits an unreachable leg as UNVERIFIABLE (`--unreachable`, never dropped), and merges with `--verdict --rows-from`. MEASURED: executed from the hub, tryton-crm's v1 contract was 12 agree / 15 unresolved of 27 with `l3_postgres` DOWN — permanently, because 15 rows need the container or the host; and the VPS deploy is `git pull` + `COPY . .`, so a gitignored `libs/health_probe/` never reaches the image. REJECTED, do not re-propose: (a) tracking the vendored comparator in every project so it ships in the image — a sync-overwritten dir would become committed code in ~46 repos and still not solve the host leg; the runner ships it per run instead; (b) a hub-only execution model with SSH tunnels to postgres-main/redis-main — the rows would then measure through a tunnel the product never uses. Also RULED: `--self-check` is STATIC; `NOT_OBLIGATED` is a stub declaration wired into `--verdict`; the checklist forbids self-referential rows and snapshot fallbacks and requires floor comparators on data that grows after go-live. CLASS: reversible (a template + two command sources; older headers with `Mode:` still parse; tryton-crm re-freezes as v2 with sites). | the first real run of the command certified its rows red-first and still produced a contract that could never be CONFIRMED where the runner executes it — the defect was the execution model the subsystem never specified, not the authoring; and the modes were a convention copied from `/fabrik-data-contract` that the operator correctly could not make sense of | templates/scaffold/scripts/verify_prod_parity.py (`site`, `run_rows(site=, unreachable=)`, `--rows-from`) · commands/_sources/fabrik-deploy-verify.md § Phase 6 · commands/_sources/fabrik-deploy-checklist.md § Phase 0 + Phase 2 · docs/reference/deployment-verification.md · tests/test_scaffold_deploy_contract.py · D-089 · D-086 |
| D-090 | 2026-09-02 | intel, plan 2026-09-02-plan-1-flywheel-recording Phase B | **THE RANKING READS LOCAL POSTGRES ONLY, AND THAT IS DELIBERATE.** `fabrik apply` provisions a second `fabrik_analytics.subagent_runs` on **postgres-main** and injects a per-project `<id>_subagent_ins` writer DSN into every deployed project (`infrastructure.py:734-755`), while `rank_task_subagents.py:17` queries the LOCAL database — which reads as a split brain feeding `pick_models` from half the data. RULED: it is not. Measured 2026-09-02 over the fleet path — `ssh vps "sudo docker exec postgres-main psql -U postgres -d fabrik_analytics -tAc 'SELECT count(*), count(DISTINCT agent_id), min(ts), max(ts), count(DISTINCT project) FROM subagent_runs'"` → **`0|0|||0`**. The table exists, no deployed service has ever written a row, and the local database is therefore the WHOLE population; a union read would add no data and a second failure mode. The scope is now stated in the script itself so the next reader does not re-open it as a bug, with the instruction to RE-MEASURE before changing it — if that table ever holds rows, the ranking's input has become incomplete and that is the moment to widen it. **The underlying defect — a registrar provisioning a sink with NO writer and NO reader — is real and is FLEET's**, filed as `01M1H2XGV09Y78W9TGVG3G92TH`; this plan deliberately does not work around it. Consequence recorded: an earlier correction of mine that called this plan's figures "the dev-time half only" was itself wrong — the figures were totals all along. CLASS: reversible (a comment + this row; widening the read is a one-function change if the measurement ever changes). | a scope that looks like a bug gets "fixed" by the next reader, and widening this read would have coupled the fleet-wide ranking to a database that has never held a single row | scripts/kilo-benchmarks/rank_task_subagents.py:17-28 · docs/development/plans/archived/2026-09-02-plan-1-flywheel-recording.md § Phase B · mail 01M1H2XGV09Y78W9TGVG3G92TH |
| D-089 | 2026-09-02 | fleet (hub), `/fabrik-review` of the shipped deployment-verification surface (before its first real run on tryton-crm) | **The parity contract stub is PROJECT-OWNED and NEVER SYNCED.** It is seeded once, at scaffold time (`SCRIPT_FILES`, `src/fabrik/scaffold.py:494`); a project scaffolded before 2026-09-02 receives it at its first `/fabrik-deploy-checklist` run, which copies `templates/scaffold/scripts/verify_prod_parity.py` in and commits it as the project's own file, and ADDS the fleet-AI doc sections from the templates. REJECTED, do not re-propose: distributing the stub through `RUN_SCRIPTS`/the fleet sync — synced scripts are written into the projects' generated `.gitignore` block (`fabrik_synced_manifest.py:212-214`) and re-copied on every sync, so a synced contract could be neither committed nor kept (the hub template would overwrite every FROZEN contract on the next sync). Also RULED by the same review, as code: the stub puts the PROJECT ROOT on `sys.path` before its lazy import (run as documented, `sys.path[0]` was `scripts/` and `libs/` was invisible on every project); a FROZEN contract with no comparison row authored FAILS CLOSED ("0 of 0" never certifies — rows that exist but are all `not obligated` keep their reading, the exemption being explicit printed data); an unknown flag exits 64 with usage; one exit algebra (`_exit_code` delegates to `verdict()`). CLASS: reversible (a template + two command sources; the copy-in path is additive). | the whole-plan review had marked these classes CLEAN by reading; the operator's pre-run re-ask was answered by EXECUTING the documented invocation on a project-shaped layout, which is where all four defects lived — the test rig had injected `PYTHONPATH` and hidden the first | docs/development/reviews/2026-09-02-deployment-verification-shipped-surface-review.md · templates/scaffold/scripts/verify_prod_parity.py · commands/_sources/fabrik-deploy-checklist.md (Starting state + Phase 6) · commands/_sources/fabrik-deploy-verify.md (Phase 0 step 4) · D-086 (the build) |
| D-088 | 2026-09-02 | operator (re-invoked `/fabrik-plan-review`) + intel, P14–P21 | **FLYWHEEL PLAN RE-CONVERGED after an AUTHOR-BLIND pass over Amendment 1** (md5 `3f85e657`; P21 quiet; dispatched 1 / returned 1). **Supersedes D-087, whose CONVERGED claim was FALSE**: that flip used a bare `str.replace` with no match assertion inside a script that printed success unconditionally — the Pass Ledger read RE-CONVERGED while `Status:` read DRAFT, and `check_convergence.py` exited 0 because a DRAFT plan is not a convergence target (filed to infra, `01M1H7DYNW8HP2AJ8SB2R0YPBH`). RULED, each verified at source: (a) Phase F's migration gate named `db/schema.sql` + Alembic — **this repo has NEITHER**; the real contract is edit `SUBAGENT_RUNS_DDL` in canonical `/opt/fabrik-lib/subagents/subagents/pg_ledger.py` (one level deeper than assumed), grow `_INSERT` + `_COLS` or the column is never written, re-vendor to 48 copies, then hand-run `ALTER TABLE … ADD COLUMN IF NOT EXISTS` against **both** the local DB and postgres-main — `fabrik apply` propagates only to postgres-main, the table this plan measured EMPTY; (b) F1's cited token list was the wrong source (those 13 are `flush_outbox` reasons — a row whose flush failed is not IN the table); F1 stands on `AgentResult.error` via `_RESULT_FIELDS`; (c) `tokens_in` has **zero producers** across 20 files — F3 ships `tokens_out` only, or adds a producer first; (d) F6 reduces to the dedupe pass: extending the unique index silently inverts latest-wins or kills every `set_quality` delta, and the tie-break it also proposed already exists (`rank_task_subagents.py:178`) with a regression test; (e) the 46-vs-50 flush delta is EXPLAINED (4 duplicates absorbed by `ON CONFLICT DO NOTHING`, reproduced on a throwaway DB) and the equality reconciliation an earlier draft mandated is false by construction — it would have redded the daily refresh daily; **WITHDRAWN**; (f) Phase H's fix relocated to the dispatch close — the checker cannot `SELECT subagent_runs` and `set_quality` writes the SAME receipt shape as `record_agent_run`, so scored and dispatch receipts are indistinguishable; (g) C2 is CROSS-REPO — `microbench_review.py` left in `73bde59a` for `/opt/ai-model-catalog/engine/`, and `TASK_SUBAGENT_SELECTION.md` still cites it locally. REFUTED with re-run evidence: the blind pass called the 17× latency table and the 51% quality figure unreproducible/mixed — both reproduce exactly; it could only guess because **the plan published no query**, which is the real defect and is fixed. CLASS: reversible — a plan Status flip; execution stays gated on `/fabrik-execute-plan` approval. | seven author passes found numbers and citations; the author-blind pass found MECHANISMS — and the operator's re-ask found that the previous CONVERGED stamp had never been applied to the file at all | docs/development/plans/archived/2026-09-02-plan-1-flywheel-recording.md § Pass Ledger P14–P21 · § Coverage Checklist rows 17–29 · commits 0ccfa29f · c2812c57 · de60d901 · 11d516ee · supersedes D-087 |
| D-087 | 2026-09-02 | operator ("update the plan to include closing these gaps") + intel, `/fabrik-plan-review` P10–P13 | **FLYWHEEL PLAN AMENDMENT 1 — the MEASUREMENT gaps** (re-converged at md5 `24a6675f`; P13 quiet, checklist 23/23). **Supersedes D-084**, which converged a plan that fixed the flywheel's PLUMBING while leaving its INSTRUMENT unable to support the conclusions it is asked for. Measured coverage over 9,327 rows: cost 77% · turns 81% · latency 81% · provider 48% · session_id 31% · **quality 30%** · tool_calls 2%. RULED: Phase F is re-scoped from the `project` column alone to **ONE additive migration carrying six columns** — (F1) `failure_reason`, the decisive one: the DB's entire failure vocabulary is `''|capped|done|error|out_of_scope|scored`, so it cannot distinguish OUR price cap from a model failure, OUR turn ceiling from a provider stall, or benchmark queueing from model latency — three wrong conclusions were drawn from that in one session and each was reversed only by leaving the DB for the JSONL error text; (F2) `queue_s` split from `latency_s` (same model reads 1051s benchmarked vs 61s in production — 8 of 10 models 2–17× inflated when swept concurrently, so NO latency conclusion is available today); (F3) `tokens_in`/`tokens_out` so `$/run` stops confounding model price with task size; (F4) `project`=repo with the run label moved out (the original scope); (F5) `corpus_id`/`task_ref` so two runs are known to be comparable; (F6) the `scored`-row duplication the unique index exempts — `UNIQUE (agent_id) WHERE status <> 'scored'` means dispatch rows CANNOT duplicate (0 measured) but 120 agent_ids already carry duplicate scored rows. **NEW Phase H** closes the quality hole: `review` is 91% of volume and only **51% scored** (2,221 of 4,337), and `check_subagent_flywheel.py` WARNs on an UNRECORDED run (`:12`, `:313-314`) while a recorded-but-UNSCORED run passes silently — proven by enumeration, 2 of 2 grep hits (`:393`, `:414`) concern the DSN/driver path only. H ships ADVISORY with its fire rate measured for a week before any threshold blocks (FIX DIRECTIVE 5). ALSO CORRECTED: the plan's at-least-once flush warning overstated the risk — the dedup key its source says is missing already EXISTS as that partial unique index. REJECTED: six separate migrations (one additive change or none); disabling the 14 median->600s models (every one is benchmark-only, none is in any `pick_models` shortlist, and their latency measures our concurrency — the `max_price` error a third time). CLASS: reversible — a plan Status flip; execution stays gated on the operator's `/fabrik-execute-plan` approval. | a flywheel whose ranking cannot record WHY a run failed is not measuring models, it is measuring our own configuration — and it did, three times in one day | docs/development/plans/archived/2026-09-02-plan-1-flywheel-recording.md § Phase F (F1–F6) · § Phase H · § Pass Ledger P10–P13 · § Coverage Checklist rows 18–23 · supersedes D-084 |
| D-086 | 2026-09-02 | fleet (hub), `/fabrik-execute-plan` of plan 2026-09-01-plan-1 (approved by the operator this turn) | **BUILT: the deployment-verification subsystem** — commits `3b13aafa` (Phase C), `548ef44a` (Phases A+B, one commit) + the Phase D convergence commit. WHAT lives WHERE: the parity contract stub `templates/scaffold/scripts/verify_prod_parity.py` (seeded via `SCRIPT_FILES` into the 12 scaffoldable types — `SCAFFOLD_TYPES` registers 13, `wordpress` is refused, hub-rooted symlink at `scripts/`; `Status: DRAFT` → the contract run (`--json`/`--verdict`) exits 2; `--json`/`--verdict`/`--self-check`/`--header`; the verdict algebra as CODE — the vendored `_COMPARISON_KEYS` disjunction decides a parity row, `None` on one fails closed, a DOWN outranks a mismatch, `not obligated` alone shrinks the denominator, no FROZEN = UNVERIFIED); fabrik-lib `health-probe` VENDORED AS SHIPPED at `libs/health_probe/` (`e48ba19c`/`f21f2123`, `VENDORED_DIRS`, fleet-synced); `/fabrik-deploy-checklist` (Appendix A verbatim, rows lowercased); `/fabrik-deploy-verify` rewritten (Phase 1b identity, registrars DERIVED from `_REGISTRAR_ORDER` at run time, Phase 6 parity BLOCKING); `/fabrik-release` VPS precondition on `FROZEN`; the `6-release` stage row + flow line in both synced contracts; fleet-AI sections in the DEPLOYMENT/OPERATIONS templates; `docs/reference/deployment-verification.md`. EXECUTION RULINGS (each measured, each recorded in the plan file): (a) corpus predicate 3 does NOT resolve `templates/**` for command sources — the stub gets a hub-rooted SYMLINK (the seeded-scripts-exist-hub-rooted convention, one source), the check unchanged; (b) the vendored module is exempt from the sync-trigger and print-ban gates BY PATH like every other vendored lib (two one-line enforcement edits outside File Scope); (c) Phases A and B land in ONE commit because predicate 2 forbids naming a command before its source exists; (d) row functions are lowercase (ruff N802); (e) the doc-stub sentinel embeds `{PROJECT_NAME}`, the token the check recognises; (f) the FROZEN-header grader is a DEFERRED backlog row, not silently absent. REJECTED, do not re-propose: extending predicate 3's templates fallback to command sources (its test pins the opposite by design); a second hub copy of the stub (the `sync_extensions.sh` drift class); a hand-derived verdict in the runner's prose (the retired rule wearing a table). CLASS: reversible (commands + a template + a vendored dir; the synced surfaces distribute on commit and roll back the same way). | an empty-database certification passed every liveness check; the fix had to be a DECLARATION the deploy is certified against, executed by code rather than read by an agent — and every plan premise that had been inferred rather than run (the ordering rationale, the sentinel visibility, the gate exemptions) broke on contact with the gates | docs/reference/deployment-verification.md · docs/development/plans/archived/2026-09-01-plan-1-deployment-verification.md (Phase C/A/B markers + execution findings) · docs/development/reviews/2026-09-01-plan-1-deployment-verification-phase-{C,A,B}-review.md · D-082 (the plan) · D-077 (the design) |
| D-085 | 2026-09-02 | infra (hub), `/fabrik-review` pass 9 — Opus finder, re-derivation | supersedes D-081: COUNT CORRECTION ONLY — the fleet-index denominator in D-081 § where is **148** systems, not 150: `Supabase` and `Promtail` each carry a `###` block AND a § Retired row, so "143 blocks + 7 retired rows" double-counts two; every other clause of D-081 stands unchanged. CLASS: reversible (one header sentence). | the previous re-derivation proved each half (143 blocks, 7 rows) and never checked that the two sets were disjoint; the sum was then propagated verbatim into an immutable ledger row and the CHANGELOG | docs/reference/apis/EXTERNAL_SYSTEMS.md:4 · CHANGELOG.md (the external-services entry) · docs/development/reviews/2026-09-02-external-services-chain-review.md (AJ6) |
| D-084 | 2026-09-02 | intel, `/fabrik-plan-review` P1–P9 | FLYWHEEL RECORDING-REPAIR PLAN **CONVERGED** (Status DRAFT→CONVERGED at md5 `a43496d0`; P9 quiet — 0 raised, 0 edits, checklist 17/17 adjudicated, `check_convergence` 0). Nine passes: 5 pool finders + 1 native Opus author-blind, all dispatched returned. RULED: (a) the postgres-main split-brain is CLOSED by measurement — the deployed-side `fabrik_analytics.subagent_runs` is EMPTY (`0|0|||0`), so the local DB is the whole population and an earlier 'these figures are only the dev half' correction was itself wrong; Phase B collapses to recording that the registrar provisions a sink with no writer AND no reader (fleet's beat, filed `01M1H2XGV09Y78W9TGVG3G92TH`); (b) **`daily_refresh.sh` has NO ranking step** — `rank_task_subagents` appears once, in a comment — so the ranking driving `pick_models` fleet-wide runs on no schedule; A5 added to wire it, and Phase E's delivery mechanism depended on it; (c) the price cap was removed **2026-07-19** (`select.py:83`), not 07-21 — the earlier date came from `git log -S` matching the commit that added the PHRASE, not the one that removed the CODE; (d) C1 is rewritten because the DB has no error-text column: the '240' is a JSONL-ledger count, the DB holds 90, and the old post-apply gate could never return 0; (e) G2's minimum-distinct-DAYS floor is REPLACED by a distinct-INCIDENT rule (≥2 dispatch days ≥7 days apart) — the '2 days' were 46 days apart, i.e. a signal reproduced across two incidents, and the day-floor would have suppressed exactly that while passing a single-outage artefact. REJECTED: installing psycopg into 43 project venvs (the hub walker makes it unnecessary; 43 dependency files for nothing); writing `SUBAGENT_RUNS_DSN` into all 44 repos as part of Phase A (unargued churn — only trade-intelligence's actively-wrong DSN is required). CLASS: reversible — a plan Status flip; execution stays gated on the operator's `/fabrik-execute-plan` approval. | five author passes found only NUMBERS and CITATIONS; the author-blind pass found MECHANISMS (a step ordered against a step that does not exist, a walker that cannot satisfy its own contract, a predicate matching zero rows) — the asymmetry is the argument for the author-blind pass being mandatory | docs/development/plans/archived/2026-09-02-plan-1-flywheel-recording.md § Pass Ledger P1–P9 · § Coverage Checklist (17 rows) · § Self-audit · commits 553c32ee · f7213063 · 02e8ad25 |
| D-083 | 2026-09-02 | operator (directive: broadcast only when there is no account to rotate to and agents risk exhaustion; then all must stop gracefully without losing work, ready to be restarted) + fleet | QUOTA EXHAUSTION IS A FLEET-WIDE GRACEFUL STOP, DELIVERED AT THE TOOL CALL. RULED: (a) the broadcast condition STAYS as built — `_fleet_active_wall_advisory` fires once per wall episode ONLY when the active account is walled and the picker finds no successor (or rotation is paused); today's four broadcasts were the picker defect (D-082's sibling fix `72090672`/`c8d75574`), not the advisory; (b) the stop is the tick's `fleet-exhausted` stamp read by a NEW synced PreToolUse hook `quota_stop.py`: work tools denied with the checkpoint instruction, reads/git/`command_run.py`/`mail.py`/`thread_anchor.py` allowed, so the Stop hook's commit-and-push law is reachable and no work is lost; (c) FAIL-OPEN on a missing stamp, an unreadable state dir, or a tick log older than 900s — a dead cron never freezes the fleet; (d) the hold lifts by itself when the tick clears the stamp on relief; a session that already ended is restarted by the operator or the resume mesh. REJECTED: a mail-only stop (reaches a session at its next prompt, never mid-turn); killing sessions (loses in-flight edits); a Stop-hook-only stop (fires at turn end — too late). CLASS: reversible (one hook file + one settings entry; delete both to retire). | the operator watched the fleet broadcast four times in one afternoon and hit the wall twice; a warning agents cannot act on mid-turn is not a stop, and a stop that kills a session loses the work the warning was meant to protect | .claude/hooks/quota_stop.py · .claude/settings.json (PreToolUse) · scripts/fabrik_synced_manifest.py · tests/test_quota_stop_hook.py · docs/workstation/hooks-index.md · docs/workstation/claude-account-rotation.md § Rotation |
| D-082 | 2026-09-02 | fleet (hub), `/fabrik-plan-review` R6–R9, after the operator asked for the fabrik-lib mail to be checked and the review re-run | DEPLOYMENT-VERIFICATION PLAN **RE-CONVERGED against fabrik-lib's SHIPPED comparison axis** (Status DRAFT→CONVERGED at md5 `96e79a91`; R9 quiet — 33 anchors read, 19/19 probes verbatim, 11/11 digest quotes; `check_convergence` 0 · `check_stage_artifacts` 0 · `check_rule_grounding` the one cited Standing phantom). **Supersedes D-080**, whose stamp was written against their SPEC: `01M1GQR1R3TD9AE68YVSP0DT51` reports the build LANDED (their `e48ba19c`/`53c098c2`, D-029; 75 tests re-run at their HEAD, read-only). RULED: (a) the module is VENDORED AS SHIPPED — File Scope row 12 `libs/health_probe/` + `VENDORED_DIRS` in `fabrik_synced_manifest.py` (the entry both the scaffolder and the fleet sync read, so new AND existing projects get it; FLEET-SYNCED), lazy-imported by the parity stub, an absent copy = an `UNVERIFIABLE` row that still exits 2; the "implement the shape locally until their build lands" clause is DEAD scope, struck; (b) the runner's parity-row predicate is their `_COMPARISON_KEYS` DISJUNCTION (`health_probe.py:552`, `:639`) — the plan's `expected AND actual` was the exact fail-open they closed (a row carrying only `match` exited 0); (c) `run_all_checks() -> list[dict[str, object]]` is consumed by key presence + `str()` coercion, guarded by the executed test because the hub's mypy excludes `templates/`/`scripts/`/`tests/`; (d) `mismatch_exit` stays the default 2, validated 2..255 at their `:521-544`; (e) three more rule packs MATCH the vendored files (55-observability · 58-resilience · self-healing) — adjudicated and quoted. Sixteen findings R6–R8, all in text the author wrote; the Phase C gate I wrote in R6 cited a `sync --dry-run` listing that cannot witness a vendored dir (measured: 10,531 lines, none naming `libs/subagents`) and was replaced by asserting the manifest entry. CLASS: reversible — a plan Status flip; execution stays gated on the operator's `/fabrik-execute-plan` approval. | a plan converged against an interface SPEC binds to nothing an executor can copy; the moment the module shipped, every 'lands later' clause became wrong and the missing vendoring step became the first thing execution would have tripped on | docs/development/plans/archived/2026-09-01-plan-1-deployment-verification.md § Pass ledger R6–R9 · File Scope row 12 · Phase A steps 4–7 · Phase C step 5 · § Evidence (executed at fabrik-lib HEAD) · supersedes D-080 |
| D-081 | 2026-09-02 | operator ("we have developed an auto scan … is it working" → "fix them then review") + infra | THE EXTERNAL-SERVICES CHAIN LIVES ON THE ONE SCHEDULE THAT EXISTS: the chain is ONE script, `scripts/external_services_chain.sh` (gather_envs → classify, bounded `--max-per-run 10` and cursor-walked → gather_envs again → `registry_sync --fetch-credits` → `gen_dashboard`), run by BOTH `daily_refresh.sh` and `wsl_startup_hook.sh` because they share the daily lock; the dashboard — the liveness heartbeat — is written only when the DATA steps succeeded (a failed classify alerts but never ages it); `scripts/refresh_service_inventory.py` is RETIRED (never ran once — its cron line was "operator-installed" and never installed; its seen-set re-bill guard was already superseded by `--tombstone-unresolved`); the chain is a declared liveness surface (`external-services-chain`, evidence = the dashboard's mtime ≤30 h); `scripts/service_catalog.json` (the classify step's output) stays OFF the pipeline auto-commit stage list — RULED by the intel session 2026-09-02 (relayed by the operator): a blind auto-commit would push whatever the file holds at boot, including a session's staged-but-unreviewed lines; the daily classify write therefore leaves a dirty tracked catalog until a session commits it (pre-existing since 2026-07-26 — the commit is the responsibility of whoever runs or reviews the chain), and the hand-off mail `01M1GY9Q8YVVDRGG8WJW5CD5QX` is withdrawn. SECOND INPUT RULED: env keys are a proxy and `gather_envs` now also scans every `/opt` git repo's source for `https://<host>` literals, attributing by registrable domain and flagging the rest into the same NEEDS-TRIAGE queue; multi-service platform domains (`amazonaws`, `googleapis`, `azure`, `windows`, `cloudfront`) are never credited to one vendor. REJECTED: (a) installing the orchestrator's own hourly cron (crontab writes are classifier-blocked and a second schedule is a second source of truth); (b) keeping the orchestrator as a manual entry point (its docstring advertises a cron that does not exist); (c) feeding all unresolved code hosts (254 of 371 at 2026-09-02 14:30) to the classifier unbounded (pool spend; the cursor-walked 10/day window drains the queue (254 of 371 scanned hosts unresolved at 2026-09-02 14:30, before the first classify laps → ~25 days) at ≤$2/day worst case, ≈$0.02 measured, and a permanently-erroring provider is retried once per lap then tombstoned after 3). CLASS: reversible (two shell scripts, one JSON row, one schema column with a default, one script deletion recoverable from git). | the registry + dashboard froze on build day 2026-07-18 for 46 days while the scan ran every morning, invisible to the liveness audit; and the env-key proxy missed 239 of 495 code-referenced hosts, 8 of them fleet-used systems with no key anywhere | scripts/kilo-benchmarks/daily_refresh.sh · scripts/gather_envs.py::scan_code_hosts · scripts/classify_services.py::bound_flagged · .fabrik/liveness-registry.json · docs/reference/external-services-registry.md · tests/test_external_services_chain.py · docs/reference/apis/EXTERNAL_SYSTEMS.md (+8 blocks; header derived from the blocks: 150 systems / 116 fleet-used) |
| D-080 | 2026-09-02 | fleet (hub), `/fabrik-plan-review` R1–R5 | DEPLOYMENT-VERIFICATION PLAN **CONVERGED** (Status DRAFT→CONVERGED at md5 `51edd8a4`; R5 quiet, zero raised; `check_convergence` exit 0 with the plan as its listed target, `check_stage_artifacts` 0, `check_rule_grounding` one cited Standing phantom). **23 findings across R1–R4, every one in text written the same day by the plan's own author**, three classes worth keeping: (a) **prose-vs-source drift** — Phase B's prose still quoted the 1366-char description the renderer had REFUSED, said "via EXTRACT", and derived routes/schema by the rules the dry run had already overturned, while the embedded source (Appendix A) was correct; the R1 check was one-directional (source ⊇ prose) and passed — R5 checks both directions; (b) **execution order is C → A → B → D**, not A → C → B → D: corpus predicate 3 resolves the project-side parity script (`verify_prod_parity.py` under the project's `scripts/`, a forward reference seeded by Phase C) for BOTH the new command and the rewritten runner, so Phase C's template must exist before either gate; (c) **governance files were in File Scope** (`INDEX.md`, `docs/README.md`) though `check_plan_tickets.py::GOVERNANCE_FILES` keeps them out — they are Phase D doc-sync steps. Also added under review: § Constraints Digest (8 verbatim quotes, grader-verified), § Spec coverage map (14 spec elements → phases), § Execution discipline (per-phase FULL `/fabrik-review`, pool-default + native, parallelism with merge points), Behavior Contracts per phase (`wordpress` refused at `scaffold.py:6049` → 12 scaffold params + a refusal assertion, not 13), a toolchain preflight for the cross-repo READ-only dry run, `/fabrik-docs-review` as Phase D's last step. REJECTED, do not re-propose: shaping a digest header to dodge the grader's phantom — the grader is fixed instead (filed `01M1GSBZ9HYZP18QZSNR09W62G`). CLASS: reversible — a plan Status flip; execution stays gated on the operator's `/fabrik-execute-plan` approval. | a plan rewritten twice in one day cannot self-grade: its author's re-reads passed a one-directional check while the artifact that will actually be committed disagreed with the plan describing it — and the ordering defect would have reddened Phase A's own gate on its first run | docs/development/plans/archived/2026-09-01-plan-1-deployment-verification.md § Pass ledger R1–R5 + § Constraints Digest + § Execution discipline · extends D-077 |
| D-079 | 2026-09-02 | operator (directive) + infra (/fabrik-doc-converge) | `docs/reference/apis/EXTERNAL_SYSTEMS.md` ADOPTS THE FLEET-INDEX CONTRACT: it has no row in the doc-converge Convergence Contract table (hub-only reference), so the `docs/SERVICES.md` row is adopted for the hub — *every fleet-used external system has a dependency block, and no block cites a dead system* — with the block shape RULED as the 12-field Capability Profile (57) + the 58 resilience posture, one block per system, section = service type. DENOMINATOR RULED: the union of the doc's prior entries ∪ `service_catalog.json` ∪ vendor-shaped keys in `/opt/*/.env.example` + `specs/services/*.yaml` ∪ live `docker ps` on all three hosts ∪ `docs/reference/apis/*.md` (measured 2026-09-02 after alias merge: 158 systems, 115 fleet-used, 73 previously absent — 79 before merging aliases); entries with zero signal in that union are REMOVED with the denominator stated (29 Coolify/WordPress-era 'Referenced' entries), never silently. CELL TIERS RULED: a cell is grounded only when a NATIVE grounder fetched its source today (`_(src: …, date)_`); a pool-swept cell is `UNVERIFIED (pool …) — verify: <url>` when its URL is live and `UNKNOWN` when dead — measured this run: 83 of 216 pool-cited URLs were dead and the sweep cost $0.03, i.e. single-shot read_only units do not fetch; their output is a suggestion, never a fact. REJECTED: (a) writing the 12 fields into every `service_catalog.json` row — the catalog is a does-it-exist index and rows would rot (57 § Doc Sync); (b) one vendor doc per system before the index converges — 121 files with zero readers; the index carries the profile, a vendor doc is added when its depth is needed (5 exist); (c) treating pool cells as grounded because they cite a URL. CLASS: reversible (one doc, regenerable from `scratchpad/build_external_systems.py` inputs; the generator is not committed — the doc is the artifact). | the operator asked whether every external service type was named and addressed in every aspect; the honest answer was no on both axes — 79 fleet-used vendors absent, 0 of 77 entries carrying retry/recovery/cost, and 30 entries describing software the fleet never ran | docs/reference/apis/EXTERNAL_SYSTEMS.md · .windsurf/rules/CLAIMS.yaml (10 rows, pack = the doc) · .windsurf/rules/core/57-external-data-sourcing.md § The Capability Profile · this commit |
| D-078 | 2026-09-02 | operator (directive: "~/.config/fabrik/subagents.env must have all external ai suppliers api keys") + intel | THE SHARED POOL CONFIG IS THE CANONICAL HOME FOR EVERY AI-SUPPLIER KEY. `~/.config/fabrik/subagents.env` (mode 600) now carries OPENROUTER · NVIDIA · MISTRAL · GROQ · GEMINI · KILO · CONTEXT7 alongside the existing EXA/BRAVE/FIRECRAWL + DSN; it is the loader's FALLBACK (real env → `<repo>/.env` → shared), so a project may still override locally. Every key was VALIDATED LIVE against a gated endpoint with a bogus-key control BEFORE being copied — which found that `/opt/fabrik/.env`'s `KILO_API_KEY` was a byte-identical copy of `OPENROUTER_API_KEY` and failed Kilo auth exactly like a bogus key (replaced from `/opt/apidoccreator/.env`). CEREBRAS has a provider row (D-007) but no key exists on the box — that lane stays unavailable. CLASS: reversible (a config file, backed up before edit; project `.env` still wins). | a supplier key held in one repo's `.env` is unreachable from every other repo — the loader never crosses repo boundaries — so the free lanes were silently dead fleet-wide while looking wired in code; and `/v1/models` being PUBLIC on OpenRouter/NVIDIA/Kilo means a 200 there is not evidence a key works | ~/.config/fabrik/subagents.env · docs/CONFIGURATION.md § Shared fleet config · libs/subagents/_dotenv.py::load_env |
| D-077 | 2026-09-02 | operator ("great" — the design-approval gate `/fabrik-spec-review` stops at) | DEPLOYMENT-VERIFICATION CONTRACT DESIGN **APPROVED** — the CONVERGED spec `docs/superpowers/specs/2026-09-01-deployment-verification-contract-design.md` (J5 quiet at md5 `cdb48812`, D-075) is the approved design: Approach B (extend `/fabrik-deploy-verify`; project-run; contract-driven) + Amendment 1 (a NEW project-side authoring command, `/fabrik-deploy-checklist`, that emits the project-side parity script `verify_prod_parity.py` under the project's `scripts/` — a forward reference, seeded by the plan's Phase C — and refreshes DEPLOYMENT/OPERATIONS) + Amendments 2–3 (bind to fabrik-lib `health-probe.compare()`, tri-state `match` read by row shape, fail-closed on attempted-unresolved, exit ranking 1→2→0, `strict=True` always). Same turn the operator directed: *"study the structure, enforcement, loop, convergence and all aspects of our existing commands to use same approach and update the plan"* — so the plan is re-authored to the command-corpus conventions before `/fabrik-plan-review`, not executed as-was. CLASS: reversible — an approval; the build is gated behind plan review + execute-plan approval. | the spec-fed skip downstream cites exactly this row; an approval turn that ends row-less mints the project's largest scope decision nowhere | docs/superpowers/specs/2026-09-01-deployment-verification-contract-design.md · docs/development/plans/archived/2026-09-01-plan-1-deployment-verification.md · extends D-075 |
| D-076 | 2026-09-02 | operator (where-is question) + infra (hunt) | WHERE THE "external systems we can reach, and HOW — API / SDK / CLI / MCP" document lives: `docs/reference/ai-media-generation-provider-map.md` — the **AI Media-Generation Provider Reach Map**, committed `d1969b48` 2026-07-26 (the same day as pack 57 and the catalog auto-freshen), per provider: the lane (video/image/audio/3D/editing), whether we can automate it WITHOUT a human in a GUI, the exact reach path, and a legend (`❌ add-key` = unreachable without a new credential). Its § "What we actually hold" is the programmatic toolkit by name. Scope: generative-media providers ONLY. The two general registries are different documents: `docs/reference/apis/EXTERNAL_SYSTEMS.md` (every external system by category, with auth + usage — not classified by reach method) and `scripts/service_catalog.json` (the secret-free vendor registry, keyed by `category:`). Pack `57-external-data-sourcing` is the RULE distilled over all three (its reach-for ladder MCP → fabrik-lib → vendor API → CLI). RECORDED GAP, not fixed here: 57, the catalog, the commands and `agents-fabrik.md` do not link the reach map (`rg -l` → 0 of 4) — at 57's next touch, rung 3 should point at it as the per-lane reach registry beside the catalog. Found by hunt only after the ledger missed; this row exists so the next ask is a grep. | "where is the doc that states external source candidates (CLI, MCP, APIs, scraping)?" cost a multi-tool hunt: session recall had no match, the repo's own history never added a doc under a sourcing name, and a filename-with-spaces bug hid `docs/reference/research/` from the first scan | docs/reference/ai-media-generation-provider-map.md · docs/reference/apis/EXTERNAL_SYSTEMS.md · scripts/service_catalog.json · .windsurf/rules/core/57-external-data-sourcing.md · INDEX.md:587 |
| D-075 | 2026-09-02 | fleet (hub), `/fabrik-spec-review` run 6 over Amendment 3 | DEPLOYMENT-VERIFICATION CONTRACT SPEC RE-CONVERGED A THIRD TIME (Status DRAFT→CONVERGED, passes J1–J5, J5 quiet at md5 `cdb48812`; 21 passes across six runs) — AND THE METHOD CHANGED: the closing pass EXECUTED the verdict algebra (four row shapes + a critical-DOWN/mismatch co-occurrence, 13/13) with the RETIRED `None → not checked` rule executed beside it and reproducing the defect (all-`None` contract → green, exit 0), so the check is proven able to see what it exists to catch — fabrik-lib's D-026 applied to a hub design. Settled in the artifact: **(a) `match: None` is read by ROW SHAPE, one rule** — `expected`/`actual`+`None` = attempted-unresolved → FAIL CLOSED, denies `CONFIRMED`, exit 2; a row with no `expected`/`actual` is **not a parity row** (outside the parity denominator, judged under `UP`) — **NOT `not obligated`**, which stays reserved for a `shape:`-driven exemption (supersedes the liveness-row half of D-072's mapping, which was minted 40 minutes before being overturned; D-072 was never committed, so its text was corrected in place rather than superseded); **(b)** precedence `1 → 2 → 0` stated once, settled both sides; **(c)** the pass-label series **I is skipped** — `I1/I2/I3` collided with the intake-id namespace (`grep -c '^| I'` read 31 for 28), defect 16's class re-introduced by the review that fixed defect 16; **(d)** the companion plan gains Phase A step 7 + File Scope row 6: `tests/test_deploy_verify_verdict.py`, watched-fail-first, as a gate — a text-only CONVERGED on this spec has now been wrong five times. 8 defects this run, ALL introduced by earlier passes of this same review or by Amendment 3's first draft; 4 candidates refuted and recorded. CLASS: reversible — a spec Status flip + a plan gate. | five consecutive CONVERGED stamps by re-reading were each void within hours; the sixth is the first whose closing method could distinguish the correct rule from the retired one, because it ran both | docs/superpowers/specs/2026-09-01-deployment-verification-contract-design.md § Pass ledger J1–J5 + § Verdict algebra · docs/development/plans/archived/2026-09-01-plan-1-deployment-verification.md Phase A step 7 · scratch `verdict_algebra_check.py` (13 assertions, becomes the plan's test) · extends D-067, D-069, D-072 |
| D-074 | 2026-09-02 | operator (directive) + infra (rules pass file 12 re-audit) | THE CAPABILITY PROFILE IS THE VENDOR-SIDE HALF OF THE AUTORECOVERY CONTRACT: 7 → 12 fields in `core/57-external-data-sourcing.md`, each mapped to the `58-resilience` coverage-map row it makes detectable (health signal → rows 4/5 · credential lifecycle → a tripwire, never a retry · interface lifecycle → a `Deprecation`/`Sunset` watch · data contract → strict boundary validation + invalid-rate metric · push delivery → dedup + reconciliation poll). Division of labour RULED, not implied: 57 records what the VENDOR guarantees and how its failures become visible; 58 owns every retry/pause/alert; nothing in 57 retries. Human-gated classes (credit top-up, endpoint retired with no successor) are ALLOWED under "everything autorecovers" on one condition — the connector stays SAFE without a human (paused, escalated once, never hot-looping) and §2b names the class as human-gated. The `RESILIENCE.md` §2b card gains a `Profile:` line in the scaffold template because the obligation had no consumer (measured: 0/6 hub vendor docs, 0/32 command sources). REJECTED: (a) folding fields 8–12 into 58's map — vendor facts, not handling; 58 would carry a data axis it has no rules for; (b) widening 58's globs to `connectors/`/`ingest/`/`webhooks/`/`scrapers/` — already flagged overbroad; 57 tells the agent to read 58 instead; (c) a gate check for profile presence now — fire rate first (FIX verb 5); the template slot + plan-time rule are the measured first step. CLASS: reversible (two packs + one template line). | the operator's bar ("external service connectors … everything must autorecover themselves") cannot be met by 58 alone: the vendor failures that break a connector AFTER its first call — a retired endpoint, an expired key, a silently changed schema, a missed webhook — are invisible unless the vendor's contract records how each one shows up | .windsurf/rules/core/57-external-data-sourcing.md § The Capability Profile · .windsurf/rules/core/58-resilience.md § The coverage map · templates/scaffold/docs/RESILIENCE_TEMPLATE.md §2b · .windsurf/rules/CLAIMS.yaml (7 new + 2 superseded) · this commit |
| D-073 | 2026-09-02 | intel, from the /fabrik-review closing sweep on D-070's own fix | mcp_watch STALENESS SIGNAL = the roster's hashed MCP SLICE (`mcpServers` global + per-project), the repo `.mcp.json` mtime, and the account-pointer SYMLINK's lstat — the roster FILE's mtime is REJECTED as a signal | `~/.claude-fleet/active/.claude.json` is Claude Code's global STATE file (statsig gates, changelog fetch times, 121 project entries) which the harness rewrites every few seconds — measured advancing 3x in one minute — so mtime > session_start was unconditionally true and the banner fired on EVERY prompt in EVERY repo forever, printed directly above the liveness banner it discredits (wallpaper = the D-033 decay mode) | .claude/hooks/mcp_watch.py::tool_universe_fingerprint · roster_tools_changed · tests/test_mcp_watch.py::test_roster_mtime_alone_is_not_staleness |
| D-072 | 2026-09-02 | fleet (hub), answering fabrik-lib's direct scope question `01M1GGD0G6F09YAZMTAEE8KBYP` + absorbing their prototype finding `01M1GEPYN6WV6CQT9YYJ9ZMQ53` | **(a) THE PARITY-CONTRACT RUNNER STAYS HUB-SIDE — NO to upstreaming its generic half, with a NAMED REVISIT TRIGGER.** fabrik-lib asked directly (their operator's brief quotes this spec's original "🆕 fabrik-lib candidate" clause, which the ladder retired). Grounds: what remains after `compare()` is corpus orchestration in **fabrik-platform vocabulary** — Layers 1–4, `_REGISTRAR_ORDER`, `shape:` flags, `SCAFFOLD_TYPES`, `specs/services/*.yaml` — so it fails the GENERIC half of their bar, not the reuse half; and the verdict algebra is **demonstrably unstable**, having changed materially twice in one day. REVISIT when the hub build ships and the algebra survives ≥2 project types unchanged; the direction then is *vendor the hub's comparator upward*, not write a second one. Their ENHANCE unit proceeds alone. **(b) `match: None` SPLITS BY ROW SHAPE and one half FAILS CLOSED** — supersedes D-067's uniform `None → not checked`: no `expected`/`actual` + `None` = benign liveness row — NOT a parity row, outside the parity denominator (corrected in spec-review J1: `not obligated` is reserved for a `shape:`-driven exemption); `expected`/`actual` PRESENT + `None` = **comparison attempted and unresolved ⇒ non-zero exit, cannot reach `CONFIRMED`**. Payload is **FOUR** additive keys — `expected`/`actual`/`match`/**`compare_error`**. **(c) Exit `2` ratified as STANDING** (fabrik-lib had carried it as provisional three times), widened to *disagrees OR unresolved*; they adopted the hub's LIVENESS-WINS precedence verbatim (`270dce64`), so `1 → 2 → 0` is settled both sides. CLASS: reversible — an interface binding on unbuilt code plus a spec Status change. | fabrik-lib BUILT their design and executed its success criteria: a caller-supplied comparator that RAISES lost the system name to `_probe_label` (`"<lambda>"`, `health_probe.py:205-209`) and exited **0 with the system declared `critical`** — validated hub-side at `:423-427`. My algebra inherited the hole one layer up, where a systematically broken comparator reports `0 of N` and never FAILS; nine of their passes and four of mine were blind to it because nothing about the prose is false when you read it | docs/superpowers/specs/2026-09-01-deployment-verification-contract-design.md § Amendment 3 + § Verdict algebra · mail 01M1GEPYN6 · 01M1GGD0G6 · reply 01M1GGXP47 · /opt/fabrik-lib/health-probe/health_probe.py:205-209,423-427 · extends D-067, D-069 |
| D-071 | 2026-09-02 | operator (ruling) | THE RULE-PACK AUTO-LOAD SIZE CAP IS RETIRED FLEET-WIDE. the `check_rule_size.py` enforcement script DELETED (it lived under the synced enforcement dir), its `final_gate.py` wiring, liveness-audit canary, gate canary and unit test removed, and every doc row dropped (README, FINAL_GATE_WORKFLOW ×3, FABRIK_SCAFFOLD_WORKFLOW, regenerated capabilities.json + docs/CAPABILITIES.md). Operator: *"autoload cap was for windsurf but we have stopped using windsurf."* Distributes to ~46 projects via the post-commit governance-sync (`scripts/enforcement/` is synced recursively), so no project gate enforces a pack byte ceiling any more. **Correction to the record for whoever reads the old rationale:** the check's own docstring claimed it was NOT a Cascade limit but a self-imposed agent-context budget — so the cap outlived its stated purpose on a second count too: a 51 KB pack is ~1.3% of a 1M-token window, and packs only load on glob match. Supersedes the 12 KB→32 KB→50 KB threshold history entirely. CLASS: reversible — one file's deletion, recoverable from git; nothing depends on the cap existing. WHAT WE LOSE: no mechanical ceiling on a glob-activated pack, so a runaway pack is now caught by review, not by the gate. NOT retired: the real cost the cap only proxied for — overbroad pack globs — which stays open in STRATEGIC_BACKLOG. | Windsurf/Cascade is retired (see the Kilo+Cascade retirement), and the byte ceiling had just forced a comprehensive pack (58-resilience, D-068) to shed content for a constraint no live consumer imposes | scripts/final_gate.py · scripts/sysadmin/liveness_audit.py · tests/test_gate_check_canaries.py · docs/workflows/FINAL_GATE_WORKFLOW.md · docs/STRATEGIC_BACKLOG.md · this commit |
| D-070 | 2026-09-02 | operator (via wef 01M1GE3PWBPKZWETCANJXWGGRC) + intel | mcp_watch staleness clock = HARNESS PROCESS start (/proc ancestry → claude), transcript-head ts as fallback only; stat-time clock REJECTED (ctime moves on append → verdict raced the flush, silent on stale sessions) and first-line-ts-alone REJECTED (a resumed conversation keeps its file — live 2026-05-13 head on a 2026-09-02 session ⇒ banner fires forever, reload cannot clear it). Degraded-universe protocol: banner leads with CHECK-YOUR-ASSIGNED-MCPs + D-033 fix-first; window-reload is the remedy for the diagnosed restore-class only, said to the operator | the tool universe loads at process start, so only a process-start clock is both truthful when stale AND clearable by the remedy the banner names | .claude/hooks/mcp_watch.py · tests/test_mcp_watch.py · docs/workstation/hooks-index.md:17 |
| D-069 | 2026-09-02 | fleet (hub), `/fabrik-spec-review` re-run over Amendment 2 | DEPLOYMENT-VERIFICATION CONTRACT SPEC RE-CONVERGED (Status DRAFT→CONVERGED, passes H1–H4, H4 quiet at content md5 `d06d1150`; 16 passes across five review runs). The re-run was owed because Amendment 2 edited § Verdict algebra post-flip. **12 defects found, and only ONE was in Amendment 2 — the other eleven were pre-existing and had survived five prior CONVERGED stamps**, which is the finding worth keeping: (a) the check corpus **double-assigned ids** — Layer 3 ran 11–19, Layer 4 restarted at 18, so `#18`/`#19` each named two checks and the corpus held 25 checks with a highest number of 23 (now **29, numbered 1–29, no reuse**: L1 1–4 · L2 5–10 · L3 11–23 · L4 24–29); (b) **Layer 3 covered 6 of the 10 registrars it declares as its denominator** — `postgres`/`redis`/`meilisearch`/`watchdog` had no row, and `meilisearch` is the registrar the section's OWN note names as proof that hand-listing loses registrars (rows 20–23 added); (c) the I17 correction to **27** deployable repos never propagated — `37` still drove three live claims and the dependent "strands 86%" (now 27/43 = **63%**); (d) § Next still routed to the **epic chain** pass G1 overturned, as the document's last line with no marker; (e) the ladder basis *"nothing covers it"* went FALSE when `compare()` landed; (f) the companion plan's Phase-A gate **already passed before Phase A ran** (`grep -c _REGISTRAR_ORDER … ≥ 1` returns 1 from an incidental mention at `:137`) — the spec's own "a check that cannot fail is a defect", in its own plan. ALSO SETTLED HERE, not deferred to fabrik-lib: **exit-code PRECEDENCE when a critical DOWN and a mismatch co-occur → LIVENESS WINS (exit 1 outranks 2)**, runner-side and independent of their choice, since the runner branches on the `--json` payload; precedence can never upgrade a verdict because both conditions independently deny `CONFIRMED`. And `match: None` / `UNVERIFIABLE (<why>)` / `not checked` disambiguated (run-time result · authoring-time declaration · the bucket both report into) — only `not obligated` leaves the denominator. CLASS: reversible — a spec Status flip. | a spec whose central rule is denominator integrity was carrying a duplicated id space, a 6-of-10 denominator, and a superseded population count — all internally consistent, none survivable once every number was re-derived from primary source instead of re-cited | docs/superpowers/specs/2026-09-01-deployment-verification-contract-design.md § Pass ledger H1–H4 · docs/development/plans/archived/2026-09-01-plan-1-deployment-verification.md (Phase A gate) · supersedes nothing; extends D-067 |
| D-068 | 2026-09-02 | operator (directive) + infra (rules pass file 13) | RESILIENCE IS SCOPED BY A FAILURE-CLASS COVERAGE MAP, AND EVERY CLASS OWES A NO-HUMAN RECOVERY. Operator: *"resilience must address all types of possible errors in a production environment … everything must autorecover themselves."* Implemented in `core/58-resilience.md` as a 22-row map (failure class × bounding primitive × **autorecovery trigger** × owning pack); a design leaving a row blank for a class it can suffer is a gradeable DEFECT. **Classes owned by another pack are INDEXED, never restated** — the map is the completeness instrument, not a second copy of `75-workers-jobs`/`25-data-postgres`/`30-ops`. Newly ruled: a mechanism that detects and then waits for a person is monitoring, not resilience — so the sliding-TTL pause now carries a first-set timestamp and escalates once past N× TTL, and every Redis-gated guard declares fail-open or fail-closed. REJECTED, do not re-propose: (a) exempting the pack from the 50 KB auto-load cap (`SIZE_EXEMPT` is for packs that are NOT broadly auto-loaded; this one is, so the cap is doing real work); (b) raising the cap to fit the edit — a fleet-wide guard changed to make one commit pass. CLASS: reversible (one pack + one sibling correction). | the pack answered only the outbound-call slice: boot, shutdown, overload and its own Redis substrate were absent from all 56 rule files, and its "no permanent stuck state" promise was contradicted by the sliding TTL that re-stamps on every detection | .windsurf/rules/core/58-resilience.md § The coverage map · docs/reference/rules-currency-pass.md · CLAIMS.yaml (8 rows) · this commit |
| D-067 | 2026-09-02 | fleet (hub) ratifying fabrik-lib's converged interface, filing `01M1ESR5KJW5Z1EE2YE55MBTE8` | DEPLOYMENT-VERIFICATION BINDS TO fabrik-lib `health-probe`'s COMPARISON MODE; THE HUB FALLBACK IS RETIRED. Interface accepted as specced (their `2026-09-02-health-probe-comparison-mode-design.md`, CONVERGED 9 passes, `5f5b2e6f`): `compare(name, expected, actual, *, comparator=None)` returning `{system,status,detail}` + `expected`/`actual`/`match`; `status` stays LIVENESS-only and agreement lives in `match`; `cli(probes, critical=None, *, mismatch_exit=2, strict=False)`. **RATIFIED by us, since we consume the exit code: `2` for mismatch** (0 pass · 1 DOWN · 2 DISAGREES) — a distinct code is what lets the runner tell "prod is dead" from "prod is alive holding the wrong data". **`match` is TRI-STATE and `None` ≠ pass**: it maps to `not checked`, counts in the denominator, never in the numerator. **The runner ALWAYS passes `strict=True`** — `health_probe.py:448` (`critical = critical or set()`) with `:478`/`:481` means an undeclared `critical` makes the CLI `sys.exit(0)` while printing `DOWN:`, so their opt-in flag is load-bearing for the hub. REJECTED, do not re-propose: (a) the old hub-side FALLBACK (vendor as-is + implement the diff in the parity runner) — it would have invented a second comparison shape and then owed a migration; (b) a `MISMATCH` status value — it silently reclassifies a healthy-but-disagreeing provider as unhealthy and `live_chain` DROPS it from the routing chain, an outage caused by a verification feature; (c) a separate fabrik-lib comparison module — the capability needs only the injectable-probe seam and the result shape, both already health-probe's. CLASS: reversible — an interface binding on unbuilt code; both sides can renegotiate before any implementation lands. | our requirement "non-zero exit on mismatch, matching your existing critical-down semantics" was UNREACHABLE as worded, because those semantics already fail OPEN — matching them would have inherited the exact fail-open the requirement existed to prevent; and the boolean `match` we asked for forces a liveness-only probe to report `True`, a false all-clear in precisely the 0-of-760 shape this work exists to catch | docs/superpowers/specs/2026-09-01-deployment-verification-contract-design.md § Amendment 2 · docs/development/plans/archived/2026-09-01-plan-1-deployment-verification.md Phase A steps 5–6 · /opt/fabrik-lib/health-probe/health_probe.py:448,478,481 · mail 01M1G4SWQR · 01M1G798QB · 01M1G8N9DC |
| D-066 | 2026-09-01 | infra (deep review of 45acc263, operator-requested) | FINDING-SHAPE TRIAGE ADOPTED, ITS ESCALATION PATH REJECTED. `/fabrik-review` Phase 3 + the scoped twin now classify findings on a second axis — MECHANICAL (unambiguous, local: fix in-run, row-less) vs ARCHITECTURAL (the correct fix is a design choice moving a contract/boundary/data-model/auth posture ANOTHER module or repo depends on: fix in-run, name the rejected alternatives, mint ONE ledger row via the existing FIXED-bullet rule) — and the shape is recorded in the finding's disposition row (`FIXED(n) · MECHANICAL` / `· ARCHITECTURAL → D-NNN`). **REJECTED, do not re-propose: (a) the agentpatterns.ai human-click hand-off** (a human classifies, an agent fixes only "mechanical" ones) — our loop keeps the fix in-run and makes the decision visible instead; **(b) a "narrow escalation" bullet routing hard architectural fixes to `BLOCKED:`** — it shipped in 45acc263 and was removed the same day: two of its three triggers ("genuinely irreversible", "needs authority you do not have") were a FOURTH BLOCKED case in disguise and the third dropped the "unresolvable" guard word, it contradicted the FIXED+REFUTED ledger identity, execute-plan's enumerated halts and the just-updated 50-code-review pack, it instructed the menu shape the operator-decision-bar anchor forbids — and, decisively, it was MECHANICALLY UNREACHABLE: `check_review_coverage.py:293-304` counts a `## BLOCKED` section only with a 3-attempts phrase, so a zero-attempt escalation left the agent choosing between abandoning it and FORGING an attempts claim. CLASS: reversible (two command sources + a render). | the axis is the one importable idea from the review-then-implement pattern; the escalation was mine and was an unauthorized fourth exit that the graders could not represent — a new path that cannot be recorded honestly is worse than no path | commands/_sources/fabrik-review.md § Phase 3 · commands/_sources/fabrik-review-scoped.md · docs/development/reviews/2026-09-01-review-triage-review.md · 45acc263 (introduced) · ae152d54 (O-1) · this commit (escalation removed) |
| D-065 | 2026-09-01 | operator ("in every project we keep operations.md and deployment.md ... must be kept fully uptodate and ready for fleet ai") | OPERATIONS.md + DEPLOYMENT.md ARE FLEET-AI INTERFACES, NOT JUST DOCS: every project keeps both FULLY current and machine-consumable — the fleet AI reads them to know what and how to deploy and which VPS services to set up (workers, systemd units, cron/Beat jobs, companions). The bar is fleet-AI-readability; the Doc Sync Matrix rows (compose→OPERATIONS/SERVICES, deploy-config→DEPLOYMENT, jobs→RESILIENCE §7) gate updates but not consumability. Rules-corpus enforcement lands at the owning packs' currency-pass turns (40-documentation, 75-workers-jobs, deploy surfaces). Companion directive same turn: docs/infrastructure/ fleet docs are a mandated grounding source for pack evaluations — read AND live-verified. CLASS: reversible — a documentation-standard ruling. | the deploy control plane is hub-side; projects can't self-deploy, so their deploy knowledge TRAVELS ONLY through these two files — a stale OPERATIONS.md silently misdeploys | memory project_rules_goal_no_version_literals (bar items) · docs/STRATEGIC_BACKLOG.md § rules currency pass · Doc Sync Matrix (existing floor) |
| D-064 | 2026-09-01 | operator-dispatched (file-6 turn named the flip) + infra execution | BOOKWORM→TRIXIE EXECUTED as the D-062 class commit: `debian_codename: trixie` in versions.yaml, renderer re-injected every span (10-python, 12-node, 20-typescript, 30-ops), literal stragglers (50-code-review, 30-ops checklist/banned rows) reworded version-free. Grounds: Debian 12 regular security ENDED 2026-07-12 (LTS-only since — the fleet built on an EOL-full-support layer for 7 weeks); trixie stable 2025-08-09, full support to 2028-08; python:3.14-slim-trixie + node:24-trixie-slim live on Docker Hub amd64. CLASS: reversible (flip the yaml back; old tags remain pullable). Blast: NEW builds only — running containers unaffected until their next rebuild; scaffold emission still bookworm+3.12 (alignment backlogged, now two axes stale). | building on a distro past its full-security window is the exact drift D-062's machinery exists to catch; the flip was one yaml line + one render because the spans were laid file-by-file | .windsurf/rules/versions.yaml · claims row debian-pin-trixie · docs/STRATEGIC_BACKLOG.md § rules currency pass |
| D-063 | 2026-09-01 | operator ("in each evaluaton, you must call a subagent ... which is fable5. if not available due to quota, opus5") | RULES-PASS SECOND OPINION IS MANDATORY AND ITS MODEL IS PINNED: every file evaluation in the rules currency pass dispatches an author-blind subagent for an independent opinion, running on FABLE 5 — falling back to OPUS 5 only when Fable 5 is quota-unavailable. Supersedes the interim pool-default plan for this bar (the pool stays the default for other gradeable fan-outs per rule 62). CLASS: reversible — a dispatch policy. | file 1's backfilled opinion found 4 defects two prior lenses missed; file 2's found 9 more incl. one reversing the author's own deletion — fresh-context review earns its cost, and pinning the model ends per-file re-derivation | memory project_rules_goal_no_version_literals § done-bar · docs/STRATEGIC_BACKLOG.md § rules currency pass |
| D-062 | 2026-09-01 | operator (verbatim goal + "you are just bandating... if mention a version number in the file, it will become stale. we must solve this professionally") | THE RULES-CORPUS GOAL AND THE NO-VERSION-LITERAL RULING: the ruleset's target qualities are always-uptodate · correct · lean · efficient · low-maintenance · free · resilient · traceable · logged · fastest · agile · best-practice. Version literals are BANNED from rule files as a class — human-paging tripwires (D-061's layer) are triage, not the solve; the professional direction is a single machine-updated version source with render-time injection (pack SOURCES carry the policy + a placeholder; machinery supplies the current literal — the command corpus's _sources→rendered pipeline applied to rules), designed and landed during the pass. SCOPE: the currency pass runs core/ first then ALL rules folders to COMPLETION, each file against the manifesto + live web research. CLASS: one-way-leaning for the pipeline choice (it restructures how packs are authored) — the pipeline design gets its own spec-shaped proposal before landing; the ban itself is effective now. | the corpus is a months-deep curated asset (Gemini deep researches); any literal in prose rots by construction — the goal is machinery that cannot go stale, not reminders to humans | this row · memory project_rules_goal_no_version_literals · docs/STRATEGIC_BACKLOG.md § rules currency pass · D-061 (now triage-tier, not the solve) |
| D-061 | 2026-09-01 | operator pushback ("this very shallow solution") → agent design | RULES-CORPUS CURRENCY IS A CLAIMS REGISTER, NOT A VERSION REGEX: every external-world assertion a pack relies on becomes a dated row in `.windsurf/rules/CLAIMS.yaml` (claim · kind · verify hint · window_days · last_verified), grown file-by-file by the currency pass itself; `rules_currency_watch.py` (weekly cron rider) mails infra BOTH layers — version-pin drift within days via endoflife.date (auto rows) AND any claim past its re-verify window (the receiving session re-researches exactly the stale claims under handle-now). Refuted claim = fix the pack + supersede the row, mirroring ledger discipline. CLASS: reversible — a YAML + a rider. | today's 10-python findings proved the point: 2 of 3 real defects (a silently-false rationale, a cross-pack contradiction) are invisible to any string-matcher — claims drift, not just literals | .windsurf/rules/CLAIMS.yaml · scripts/sysadmin/rules_currency_watch.py · tests/sysadmin/test_rules_currency_watch.py · docs/STRATEGIC_BACKLOG.md rules-pass ledger |
| D-060 | 2026-09-01 | operator ("no we will not use this command against our rules, it wasnt designed for that") | /fabrik-rules-review IS A PROJECT-COMPLIANCE TOOL ONLY — never invoked against the `.windsurf/rules` packs themselves, despite its rendered HUB-mode text describing exactly that (the operator overrules the command's own § HUB mode; a rules-corpus evaluation, when it happens, is a separate instrument to be designed then). REJECTED same turn: my proposal to arm it with manifesto + live-currency lenses via $ARGUMENTS. CLASS: reversible — a scoping ruling. | the command was designed to audit a PROJECT's status against the synced packs; repurposing it against the packs stretches a compliance gauntlet into an authoring review it was never shaped for | commands/_sources/fabrik-rules-review.md (HUB-mode text now non-operative for pack-audits) · this row |
| D-059 | 2026-09-01 | operator ("there must be feedback section at this finishing message... then FEEDBACK:") | THE FINAL OUTPUT BLOCK IS 7 LINES — `FEEDBACK:` is the 7th, after `NEXT:`: the run-record close verdict made CHAT-VISIBLE (same bar: a filed claim names a durable artifact; bare "none" refused — `none — <surfaces>` or the filing). Enforced, not asked: `final_gate_stop.py::_FINAL_BLOCK_KEYS` now requires it, so a task-completing response missing the line blocks at the Stop hook, fleet-wide via the governance sync. Companion directive same turn: every run-record `start` now runs the assigned-vs-live MCP probe (`command_run.py::_mcp_probe_advisory` → `mcp_health.py`, advisory per D-033) — the ORIENT prose mandated the check and nothing ran it. CLASS: reversible — a block key + an advisory probe. | six prior asks produced persisted-but-invisible verdicts (D-055/D-056 made them recorded and agent-read; the operator still saw nothing in the chat itself); prose mandates without an executed check measurably do not happen | CLAUDE.md § FINAL OUTPUT · templates/governance/CLAUDE.md · .claude/hooks/final_gate_stop.py · scripts/command_run.py |
| D-058 | 2026-08-31 | agent (hub-infra, adopting wef upstream proposal 01M1CM6G — their ranked option 2 over their preferred option 1) | GATE VULTURE SUPPRESSION IS PER-REPO, NEVER GLOBAL: `final_gate.py` scans a repo-root `.vulture-whitelist.py` alongside `src/` when the file exists (vulture's documented whitelist idiom); a repo without the file gets a byte-identical argv. REJECTED: option 1 (fleet-global `--ignore-names sql,params,deep,timeout,ssl_context`) — common names, would blind all ~46 repos to genuinely dead locals; option 3 (demote vulture to WARN) — weakens a check with real finds (same run's bandit install caught a live B314). CLASS: reversible — remove the three lines. | 4/4 measured false positives were required-interface parameters vulture is structurally unable to see as used; the fix belongs in the CONFIG, scoped to the repo that owns the contract, auditable in its review | scripts/final_gate.py (`_vulture_argv`) · tests/enforcement/test_final_gate_vulture.py · /opt/web-ecommerce-factory/docs/reference/upstream-proposals/2026-08-31-vulture-fires-on-required-interface-signatures.md |
| D-057 | 2026-08-31 | agent (hub-infra, on the operator's word — "the wef D-023/024/025 sequencing" handed back after 01M1CBJWQS deferred it) | WEF DUP-ID REPAIR SEQUENCING RULED (mail 01M1CKAVPA → wef2): the class had grown 3→6 pairs (D-023/024/025/031/039/040, one still only staged); ONE lane (wef2) repairs ALL SIX in ONE commit, AFTER the in-flight mint lands (which re-derives its own id from pulled origin/HEAD first). Per-pair rule: the INBOUND-REFERENCED side keeps the id (reference stability is the ledger's contract — measured: plan-3→approval row, backlog→seo-briefs row, audit-spec→aspect-11 row), tiebreak first-committed-keeps; renumber = id cell only, fresh ids from post-pull max; every reference naming a MOVED row (incl. intra-ledger supersedes pointers) fixed in the same commit. The hub's synced duplicate-id gate check (their proposal, 4/day fire rate measured) lands WARN-first only AFTER wef2's repair-landed reply. CLASS: reversible — a renumbering commit, revertible; the gate is not yet built. | both sides of five pairs were committed, so wef's own "renumber the settled side" rule no longer selected a side; a deterministic successor criterion was owed, and the operator does not sequence what is derivable | /opt/fabrik-mail/web-ecommerce-factory/inbox/01M1CKAVPAHBDGA0Y4F13R501D.md · docs/STRATEGIC_BACKLOG.md (dup-id gate row) |
| D-056 | 2026-08-31 | operator ("i dont read anything, you read" — the 6th feedback ask, refining D-055) | THE FEEDBACK READER IS AN AGENT, NOT THE OPERATOR: `scripts/sysadmin/feedback_relay.py` rides the daily kaizen cron slot (a rider inside `weekly_catchup.sh`'s kaizen_collect_v2 case — no new crontab line, which stays operator-owned) and mails every not-yet-relayed close verdict (D-055 `feedback_text`, filed-verdicts expanded, nones counted) to the shared `fabrik` inbox `--to-agent infra` — where the handle-now law makes a hub session read, validate, fix-or-route, and ack each item. Watermarked at-most-once; a failed send retries next cadence; relay failure never unstamps the collector. Proven live at mint: 2 verdicts relayed (mail 01M1CG2R8Z), second run quiet. CLASS: reversible — remove the rider line. | five asks produced a recording fix (D-055) whose digest still assumed a human reader; the operator reads nothing — the loop closes only if the machinery delivers feedback to a session obliged to act on it | scripts/sysadmin/feedback_relay.py · scripts/sysadmin/weekly_catchup.sh (rider) · docs/workstation/kaizen.md |
| D-055 | 2026-08-31 | operator (5th ask: "i dont see any agent is using feedback: and reporting back about our commands/skills and rules") | FEEDBACK VERDICTS ARE PERSISTED AND OPERATOR-READABLE: `command_run.py` close now stores the full `--feedback` TEXT on the run record (`feedback_text`, capped 2000) alongside the token, and `check_feedback_duty.py --digest` prints every recent close's verdict text — the operator's one-command reading surface. ROOT CAUSE of the five inert asks, measured: the duty WAS firing (14 closes/14d on the hub, 10 substantive) but `command_run.py:1390` classified the prose into `filed|none|unstated` and DISCARDED the text by design ("the PROSE never is [stored]") — the reports existed only in chat scroll. Pre-D-055 records print `<token: text not persisted>` in the digest, never silently blank. CLASS: reversible — storage+display, revert = drop the field. | five operator asks produced zero visible feedback because the substance was thrown away at the only moment it was machine-captured | scripts/command_run.py (feedback_text) · scripts/enforcement/check_feedback_duty.py (--digest) · commands/_fragments/close-feedback.md · tests/enforcement/test_feedback_duty.py::test_d055_* |
| D-054 | 2026-08-31 | operator | `NEXT: operator decision` HAS A BAR — legitimate on exactly three named grounds (contractual human gate · materially-work-changing AND unresolvable from artifacts/code/ledger · a decision the operator already owns this turn); an (a)/(b) options menu and a self-reliability cite are NEVER legitimate; everything else is DISPATCHED, not offered. Recorded post-hoc for sibling commit d09e568e (the authoring session minted no row — the ledger duty its own text names); the § EXIT ad-hoc-branch disposition carve-out (ground 1, not a menu) added in the same change as this row. `operator-decision-bar` adopted as a UNIVERSAL governance marker. CLASS: reversible — a governance clause, revert = new ruling. | the operator, verbatim: "i dont want to decide that kind of things, the tasks and their order is obvious" — after an agent manufactured an (a)/(b) fork to transfer its own uncertainty; the phrase was the contract's only unguarded exit | CLAUDE.md § the operator-decision bar · templates/governance/CLAUDE.md (synced) · /opt/fabrik-lib/CLAUDE.md (author-applied, approved) · commit d09e568e |
| D-053 | 2026-08-31 | operator→infra (derived on the operator's finish-it directive) | LEDGER-GRAMMAR RE-GROUNDING: the char-window halves of the grader regexes are REMOVED — `QUIET_PASS` (was 40-char found→fixed gap) and `_REDERIVATION_ROW` ×2 lockstep (was 160-char pass→re-deriv gap) now bind SAME-LINE only, so ledger-row cell ORDERING and manifest length stop being load-bearing. The canonical row template (term-coverage) + the bidirectional corpus-vs-grader fixture tests stay as the alignment guard. This closes the /fabrik-review BLOCKED: NON-CONVERGENCE of the fresh corpus review (rounds 10-13 new: 2,2,3,5 — every late finding was a window-mismatch of the dual-specified grammar). Measured before landing: 554 review files fleet-wide, 8 flips, ALL fail→pass (2 QUIET, 6 REDERIV) — zero new reds; red→green regression test with a >160-char-gap compliant row. CLASS: reversible — two regexes, revert = restore the caps. | prose fragments and regex graders specified one grammar in two places; 13 review rounds proved prose editing cannot converge the pair — the windows were the fragility class itself | scripts/enforcement/check_convergence.py::QUIET_PASS,_REDERIVATION_ROW · check_review_coverage.py::_REDERIVATION_ROW · commands/_fragments/term-coverage.md canonical row · tests/enforcement/test_review_exit_contract.py::test_d053_* |
| D-052 | 2026-08-31 | operator | EVERY PROJECT GETS A WATCHDOG ("every project gets a watchdog"). The platform default was already `enabled=true` for kind ∈ {service, worker, wordpress}; the defect was that 15 REAL project specs carried an explicit `watchdog: {enabled: false}` override, so the default never applied. All 15 flipped to `true` this change. Scaffolder TEST FIXTURES (`test-*`, `guide-proj`, `noguide-proj`, `my-docs-site`, `image-broker`, `evolution-api`) are NOT projects and stay as-is — flipping them would break the scaffolder tests that assert the disabled path (`test-guide-disabled` asserts it by name). Retired `wpf` excluded. This ruling SUPERSEDES any per-spec opt-out comment, explicitly including zitadel's "a third-party image can't run the product-aware sidecar" — which was wrong for the default config: the sidecar builds from `/opt/fabrik-lib/watchdog/watchdog_sidecar` and needs the project's SOURCE only for the code-fix tiers (`propose_fix_prs` / `auto_code_fix`, both default `false`); ops-only monitoring watches the CONTAINER. COST, stated not hidden: each enabled watchdog carries `daily_budget_usd: 1.0` + `daily_invocations_cap: 200`, so the ceiling is ~$15/day IF all 15 deploy and saturate; only zitadel is deployed today, and tryton-crm is next. CLASS: reversible — a per-spec flag, revert = flip back. | the capability is live (`fabrik/watchdog` image + a running sidecar) and was being left on the table by inherited opt-outs nobody re-examined; a multitenant CRM system-of-record with a 24h RPO deploying with no watchdog is the case that surfaced it | 15 × `specs/services/*.yaml` `watchdog.enabled` · default-by-kind at `infrastructure.py`:322-335 · `spec_loader.py::WatchdogConfig`:410-418 · sidecar source `drivers/watchdog.py`:125 |
| D-051 | 2026-08-29 | operator | THE DESIGN-SYSTEM RESOLUTION LADDER (recorded 2026-08-31 by T29 of the manifesto pass — the ruling predates the ledger's adoption and lived in a commit body + rule pack): a GUI project's design system resolves BIC-first (the project's own `docs/design-system.md`, brand-identiy-creator-sourced); the house identities (`ocoron-design-system.md` / `tojlo-design-system.md`) apply ONLY on the project's explicit house-identity declaration — judging a BIC-branded product against ocoron tokens is itself a finding. CLASS: reversible — a resolution policy, revert = new ruling. | the ruling is load-bearing law in /fabrik-ui-design Phase 1 and /design-review's contract ladder but had zero DECISIONS.md rows — the D-050 unminted-standing-policy class, caught by T29's verifier | .windsurf/rules/saas/60-saas-ui.md (canonical) · commit f47146ce ("Operator ruling 2026-08-29:") · commands/_sources/fabrik-ui-design.md:44-49 |
| D-050 | 2026-08-26 | operator | RIVALS SCAN BUDGET RULING (recorded 2026-08-31 by T24 of the manifesto pass — the ruling predates the ledger's adoption and lived only in code comments): competitor-intel scans run with NO money ceiling ("no ceiling" spelled as a large number; `--budget 0` REJECTED; `truncated=True` is a LOUD partial-run finding, never a footnote), AND agents/`claude -p` are REJECTED for this purpose — the paid multi-leg engine is the sanctioned route ("why do we spend money while we have claude -p … I reject using any agents for this purpose"). CLASS: reversible — a budget policy, revert = new ruling. | the ruling is cited verbatim with its date in the driver (rivals_run.py:134, :258) and load-bearing in /fabrik-rivals' termination conditions, but had zero DECISIONS.md rows — the T20 unminted-standing-policy class, caught by T24's verifier | scripts/rivals_run.py:134,:258 · commands/_sources/fabrik-rivals.md:49-51,:98-99 |
| D-048 | 2026-08-31 | infra | REVIEW EXIT VOCABULARY RECONCILED (T22 of the manifesto pass; CLASS: reversible — prose contracts, revert = re-edit): the graders' quiet exit `found: 0 · fixed: 0` is CANONICAL; `/fabrik-review`'s `found:` counts only candidates NEEDING adjudication — a re-raise of an already-adjudicated STANDING row (DESIGN-GAP etc.) is CITED in its disposition-ledger row, never counted, so honest termination and QUIET_PASS/`check_review_coverage` final-row-quiet are simultaneously satisfiable; `new:` stays the stopped-learning signal. Unblocks the parked command_run done-round-check (STRATEGIC_BACKLOG). | counting re-raises made termination impossible (transdoc 2026-08-27: found never reaches 0 while a standing row is true); suppressing them hides a real observation; both graders demand the quiet pair (check_convergence.py QUIET_PASS :150; check_review_coverage.py :466-469) | commands/_sources/fabrik-review.md § Reporting + § Run record · docs/development/reviews/2026-08-31-plan-1-manifesto-command-pass-T22-fabrik-review-review.md |
| D-047 | 2026-08-31 | operator+infra | The commands/skills MANIFESTO PASS planned + CONVERGED (Status flip; CLASS: reversible — a plan document, revert = delete): 34-ticket serial set (T01 fragments baseline · T02–T33 one per command source, denominator grounded 32 · T34 integration receipt), each ticket = 63b six-intersection evaluation + minimal fixes + per-command /fabrik-review to new: 0, run without stopping under NO-POOL; converged through 8 review passes (16 findings fixed incl. a fired-and-re-grounded NON-CONVERGENCE breaker whose named foundation error — fix rounds introducing unverified claims — minted the in-round re-derivation rule), all flip gates green. Execution awaits the operator's word. | operator: "walk you through all commands/skills... evaluate and update each against docs/reference/operating-manifesto.md without stopping. one by one... a /fabrik-review so at least 31 tickets"; D-044 recorded this pass as next | docs/development/plans/2026-08-31-plan-1-manifesto-command-pass/ · checklist 63b (52278be3) |
| D-049 | 2026-08-31 | operator+fleet | SHARED GOTENBERG BASIC AUTH ARMED — `/opt/gotenberg/compose.yaml` gains `command: ["gotenberg", "--api-enable-basic-auth"]`. The env var `GOTENBERG_API_ENABLE_BASIC_AUTH=true` was INERT: gotenberg 8.x arms basic auth only via that CLI flag (its own `--help`), and the compose had no `command:`, so the image CMD `[gotenberg]` ran unauthenticated under entrypoint `[/usr/bin/tini --]`. Username/password were already configured and correct — only the flag was missing. Verified live both directions: unauthenticated convert 401, authenticated 200, /health 200. | measured, not suspected: the service is published at pdf.vps1.ocoron.com behind only gzip, and its own access log showed **2449 of 2450 conversions in 30 days arriving from PUBLIC internet IPs, every one HTTP 200**, alongside webshell scanning (wp_filemanager.php, /admin.php, /222.php). An unauthenticated Chromium renderer on the internet is compute abuse AND an SSRF vector into the fabrik network. Blast radius of arming it was zero: no compose, env, or hardcoded reference to it anywhere on the box, and 1 internal request in 30 days (a probe). | /opt/gotenberg/compose.yaml (backups: compose.yaml.backup.20260831-070710 + compose.yaml.bak.*) · docs/development/plans/2026-08-31-plan-deploy-tryton-crm.md § Self-audit |
| D-045 | 2026-08-30 | operator+fleet | ACCOUNT ROTATION RETIRED ON THE VPS FLEET — recorded now because the ruling was received during the quota-governance spec ("NO account rotation on the VPS (single-key ob@); the VPS fallback is a MODE fallback: ob@ subscription → OpenRouter metered pool, not account→account"), executed across 2026-08-29/30, and never given a row. Each host runs ONE `claude` as ob@ owning its OWN OAuth refresh chain (per-host `claude /login`); the WSL→VPS credential sync is DISABLED; quota pressure is handled by the governor (shed routine at `QUOTA_RESERVE_PCT=80`, incident always runs) instead of by rotating accounts. Rotation remains live on the WSL dev box only. The `manager-accounts/` snapshot dirs still on all 3 hosts (4 each, verified live) are INERT RESIDUE. `claude_rotate.py` stays on the fleet for `run_claude` + `--probe-current` telemetry, not for rotation. | the docs still carried an operator runbook to *enable* rotation prefaced "⚠️ Not live yet" — false in both halves (it shipped, then was superseded), which would have walked a future reader into re-enabling a retired mechanism on a single-key host | docs/infrastructure/vps-ai-sysadmin.md § Quota governance + § Rollout runbook (RETIRED) · docs/infrastructure/vps-fleet-architecture.md § Host-level mechanisms · canonical runbook docs/workstation/vps-claude-quota-governance.md |
| D-044 | 2026-08-30 | operator+infra | MANIFESTO BOUND INTO GOVERNANCE (settles the D-043 open question: bind fleet-wide, via pointer not copy): the hub + template decision-ledger duties gain classify-at-mint (reversible/ONE-WAY), the one-way § Binding field block (CLASS/BUDGET/KILL/CONFIDENCE/COUNTER/TRIPWIRE/CLOSE-OUT), and Invariant-3 most-reversible-default; template § UNIVERSAL corrected "seven"→eight anchors (decision-ledger was missing from its enumeration since the anchor landed); fabrik-lib mailed to adopt the ledger duty + seed docs/DECISIONS.md (their drift check already flags `missing: decision-ledger`; 0 DECISIONS mentions, no ledger file — the manifesto cannot bind there until it lands). Phases/gates NOT copied into any contract — the manifesto stays canonical; contracts point and bind. The commands/skills corpus gets its own manifesto pass next (operator). | evaluation this run: 0/3 contracts mentioned the manifesto, a kill criterion, or reversible/one-way; an agent can't obey a document it never learns exists, and a one-way decision minted a plain row with nothing for Phase 5 to measure | CLAUDE.md § the decision ledger · templates/governance/CLAUDE.md (same bullet + § UNIVERSAL) · docs/reference/operating-manifesto.md § Binding |
| D-046 | 2026-08-30 | operator+fleet | Grafana service-account token ROTATED (fleet beat, not infra's — operator: "grafana is installed in vps server(s) so it is your duty"). Old token id=1 (created 2026-04-18, no expiry) REVOKED and proven dead (401); new token `fabrik-fleet-20260830` id=2 live (200), sole token on SA id=2. Propagated to the two consumers that hold literal copies: /opt/fabrik/.env and the hub's gitignored .mcp.json; VPS fleet verified to hold NO copy (0 occurrences in .env.sysadmin ×3). Grafana MCP re-verified ✔ Connected. | the literal had lived in a git object + the working tree since 2026-04-18 (4.5 months, no expiry); rotation is the only part a history-clean cannot do. SYSTEMIC TRAP FOUND: emit_mcp_project_config.py's secret overlay takes ${VAR} values from the hub's OWN .mcp.json (chain position 2), so re-emission copies the stale secret from itself — a rotation can never propagate by re-emitting; the donor must be edited first. Rotation runbook + emitter both owe this. | scripts/sysadmin/emit_mcp_project_config.py::_load_defs (the donor chain) · .env + .mcp.json (both gitignored) · backups/.env.backup.20260830-223904 + .mcp.json.backup · probe scripts/probes/grafana_token_check.sh |
| D-043 | 2026-08-30 | operator | OPERATING MANIFESTO ADOPTED at docs/reference/operating-manifesto.md — the operator's phase framework (triage/5W1H/reasoning modes/systemic/execution/longevity, original text verbatim) + the five enforcement additions (decision budget in the Phase-0 gate, default-most-reversible under ambiguity, armed tripwires in Close the Loop, fired-kill-defaults-to-kill, WIP limit 3) + § Binding wiring the gates to EXISTING machinery: the decision ledger (one-way rows grow CLASS/BUDGET/KILL/CONFIDENCE/COUNTER/TRIPWIRE/CLOSE-OUT fields), Gatus/Alertmanager tripwires, STRATEGIC_BACKLOG parking; enforcement advisory-first per the rollout law | prose is enforced by memory and mood; the manifesto's gates bind only through committed artifacts and alerts — and the per-decision record it needed already exists since this morning (the ledger) | docs/reference/operating-manifesto.md |
| D-042 | 2026-08-30 | operator+infra | the cold-spawn HERD class KILLED: all fetch-at-spawn MCP servers converted to local pinned installs (npm -g x9, uv tool install serena + postgres-mcp with the mcp<2 pin baked); mcp_defs.json commands localized, 39 repos re-emitted, all 5 rosters + headless synced (md5=1), DR-backed; measured 15/15 Connected in 9.2s (was 60-90s with 4-8 timeouts) | burned three times in one day (morning wipe outage, both youtube restarts) — the roster's own recorded fix direction, now executed; caveat: bins are node-version-pinned (re-install after a node upgrade) | scripts/sysadmin/mcp_defs.json · docs/workstation/mcp-roster.md § fetch-at-spawn |
| D-041 | 2026-08-30 | operator | supersedes the ENFORCEMENT layer of D-032/D-033 (the rulings stand; the forcing was insufficient — operator: "i asked you to implement this properly and you failed"): a PER-MESSAGE UserPromptSubmit hook (mcp_watch.py, synced) now injects (1) a STALENESS banner when .mcp.json/roster changed after the session's transcript began — catches the resumed-conversation class measured live on the youtube window (the IDE resumes sessions, restoring dead rosters' tools) — and (2) a cached mcp_health dead-server banner (TTL 15m, detached refresh) until fixed; plus the grounding-order fragment makes a dead mandated research arm an explicit fix-first event | session-start steering + on-error rules cannot bind a session that never touches the dead server; the binding moment for "any message" is the prompt hook | .claude/hooks/mcp_watch.py · scripts/sysadmin/mcp_health.py --cache-out · commands/_fragments/grounding-research.md |
| D-040 | 2026-08-30 | fleet | Type-census RESCAFFOLD executed SELECTIVELY, not mechanically: backfilled only the two genuine gaps (tojlo-mail manifest.xml e37c722; whatsapp-agent extension/ skeleton 1c3d7cd) and deliberately did NOT write `server/{requirements.txt,Dockerfile,db/schema.sql}` into the 8 retyped saas-skeleton repos. Tooling: scripts/retype_project.py (dry-run default, never-overwrite, dirty-refuse, 7 tests, mutation-proven). | all 8 report the same 3 'missing' files because they already have WORKING backends at the REPO ROOT (verified: youtube/site-provisioner/brand-identiy-creator each carry Dockerfile+requirements/pyproject at root) — TYPE_REQUIRED_FILES encodes the greenfield `server/` layout, so for a mature repo it reports layout DIVERGENCE as absence; a mechanical sweep would have created phantom backends beside real ones in 8 repos incl. a production SaaS | scripts/retype_project.py · tests/test_retype_project.py · /opt/tojlo-mail e37c722 · /opt/whatsapp-agent 1c3d7cd · finding mailed to infra (checks-semantics class) |
| D-039 | 2026-08-30 | operator+fleet | SUPERSEDES D-037. `office-extension` ADOPTED as scaffold type 13 and tojlo-mail is its first instance. Operator ruling, verbatim: "tojlo-mail is not saas, it is outlook extension" + "it has frontend as outlook extension and backend will be installed in our vps fleet". Implementation follows the `static-site` precedent — `_scaffold_office_extension` REUSES `_scaffold_saas_skeleton` for the two genuinely-deployable halves (hosted taskpane web + backend API on the fleet) and adds `manifest.xml` (greppable `__HOST_URL__`/GUID placeholders, never a plausible-wrong URL). UI-BEARING: added to the hub CLAUDE.md pipeline list, so ui-design/user-test/design-review run; MCP set = the web-GUI set infra had already staged. | my D-037 rejection weighed DEPLOY SHAPE (taskpane+API == saas-skeleton) and ignored PRODUCT IDENTITY — the type is the semantic label every agent and pipeline reads, and an Outlook add-in typed saas-skeleton misroutes tooling and misinforms every future reader; the operator corrected it. Cost/fire-rate was the right lens for a mechanism, the wrong lens for an identity | src/fabrik/scaffold.py::SCAFFOLD_TYPES + _TYPE_SCAFFOLDERS + TYPE_REQUIRED_FILES + _scaffold_office_extension · tests/test_scaffold_office_extension.py (4 tests) · CLAUDE.md § Pipeline · /opt/tojlo-mail/project.yaml:16 · mails 01M19PSJN3 + 01M19ZRDT0 |
| D-038 | 2026-08-30 | fleet | specs/services/{trading-core,proposal-creator}.yaml are LEGACY-shaped — top-level `template: python-api` and NO `shape:` block at all; recorded as a PRE-DEPLOY BLOCKER rather than guessed-in, because a fabricated shape silently skips or wrongly creates registrars. Neither is deployed today (verified: no container on vps1), so the gap is dormant; the first `fabrik apply` for either MUST author the shape from the repo's real DB/cache/metrics usage first, and drop the now-stale `template: python-api` (both repos are saas-skeleton since D-023). | the shape block is what drives every registrar; a spec that lacks it deploys silently broken (CLAUDE.md § Spec contract awareness), and inventing one from a type name is exactly the un-grounded guess the FIX DIRECTIVE bans | specs/services/trading-core.yaml · specs/services/proposal-creator.yaml · found during the D-023 ripple duty (mail 01M19PKH30) |
| D-037 | 2026-08-30 | fleet | office-extension REJECTED as a new scaffold type (proposal 01M19PSJN3); tojlo-mail's correct type is **saas-skeleton**, not the interim chrome-extension. An Office add-in's DEPLOYABLE surfaces (hosted taskpane web app + backend API) are already exactly saas-skeleton's shape — proven by tojlo-mail's own spec (`kind: service`, needs_database + needs_cache + exposes_metrics + has_bearer_api) and its monorepo (`apps/web`, `apps/worker`, image proxies). What office-extension would add over saas-skeleton is a manifest.xml skeleton — a TEMPLATE FILE, not a registry type — while costing a SCAFFOLD_TYPES row, an emission path, an MCP mapping, two synced CLAUDE.md pipeline lists (fleet-wide blast radius) and permanent maintenance, at a measured fire rate of n=1 repo. Revisit at n≥3 office add-ins. UI-bearing classification is preserved either way (saas-skeleton runs ui-design/user-test/design-review), and the MCP set is identical (playwright + chrome-devtools). | FIX DIRECTIVE §5 (no overengineering — measure the fire rate before adding a mechanism): one repo cannot carry a fleet-wide type whose deployable shape is indistinguishable from an existing one; chrome-extension is nonetheless WRONG for it (no hosted half, no deploy spec) so the interim must not stand | scaffold.py::SCAFFOLD_TYPES (unchanged, deliberately) · /opt/tojlo-mail/project.yaml:16 (flip to saas-skeleton — CROSS-REPO, operator-gated) · mails 01M19PSJN3 + 01M19PKH30 |
| D-036 | 2026-08-30 | operator | FEEDBACK SUBSTANCE FLOOR, mandatory in all 31 commands: the close-out verdict is a MANDATORY step and a bare "none"/"n/a"/"nothing" is mechanically REFUSED by command_run.py (a none needs its surfaces-exercised clause; a filing needs its filing verb); the record stays running and the Stop hook blocks the turn until a substantive verdict lands | the operator asked 4 times in text and compliance measured 12/13 silent closes — text does not bind at output time (Lesson 116); this is the 5th ask made mechanical, calibrated against live fixtures (6 non-space chars) not vibed | scripts/command_run.py::_feedback_lacks_substance · close-feedback fragment (all 31 commands re-rendered) |
| D-035 | 2026-08-30 | operator | INTER-AGENT MAIL CONTRACT: substantive kinds (finding/request/upstream-feedback) carry 5W1H + FACTUAL root cause (WHY reproduced/measured, or an explicit unknown) + SYSTEMIC (the class, never the instance) as MANDATORY sections; ABDUCTIVE (alternatives ruled out) when WHY is inferred; INDUCTIVE/DEDUCTIVE/COUNTERFACTUAL where they carry weight; mail.py send WARNS on gaps (advisory tier), reply/relay exempt | a reader in another repo reconstructs the situation only from the text — structure is the transfer format; warn-first per the measured-rollout law | scripts/mail.py::_structure_gaps · docs/reference/fabrik-mail.md § The message contract · close-feedback fragment (all 32 commands) |
| D-034 | 2026-08-30 | operator+infra | session-identity forcing (advisory): ORIENT warns when a HUB session runs with CLAUDE_AGENT unset (window renames never reach hooks — the mis-signed-day class), and the commit-msg shim warns on Agent-Name ≠ CLAUDE_AGENT; both warn-only until fire rate earns promotion | identity binds only through the env var; a mis-signed session poisons attribution for a whole day (recorded incident) | .claude/hooks/session_orient.py::_identity_line · scripts/check_commit_trailers.py::_warn_agent_name_mismatch |
| D-033 | 2026-08-30 | operator+infra | the MCP forcing pair (advisory): mcp_health.py probes the ASSIGNED set (repo .mcp.json) for real liveness (initialize handshake / HTTP reach, parallel, exit 0 always) closing the invisible-startup-death gap; the failure-moment rule in both CLAUDE.md surfaces binds the response to a dead tool (fix-first + say-so, never a silent detour) | ORIENT (D-032) made breakage detectable, not enforced; the pair adds the mechanical diff + the output-moment duty, warn-first per the measured-rollout law | scripts/sysadmin/mcp_health.py · CLAUDE.md + templates/governance/CLAUDE.md § Behavior |
| D-032 | 2026-08-30 | operator | every agent, on any message, KNOWS its assigned MCPs + the full catalog + the fix-first duty ("if any mcp has any issue it must first fix it") — wired as a runtime ORIENT line (reads the repo's .mcp.json live, catalog = mcp-roster.md by absolute path, known fix classes named) in the synced session_orient.py | an agent that doesn't know its toolbox can't use it, ask for more, or notice a dead server; the SessionStart hook is the one surface every session reads | .claude/hooks/session_orient.py::_mcp_line (synced fleet-wide) |
| D-031 | 2026-08-30 | operator+infra | supersedes D-020's emission semantics only: postgres-pro is emitted PER-REPO and ONLY with a proven-connecting DATABASE_URL (probe at emission; absent-until-configured otherwise); the user-level roster carries the 5, not 6 — the GRANT stays universal, the presence follows a working DB | measured: postgres-mcp v1.29 blocks its MCP handshake ~30s on ANY non-connecting URI (DNS, auth, refused alike) and refuses to start env-less — every wrong shape reads as a dead server in Claude's 30s timeout; 4 repos have connecting DBs today, the rest gain the server by adding DATABASE_URL + re-running the emitter | scripts/sysadmin/emit_mcp_project_config.py::_uri_connects · docs/workstation/mcp-roster.md § postgres-pro |
| D-030 | 2026-08-30 | infra (plan-3 decision row 3, operator-approved via plan) | `enableAllProjectMcpServers: true` fleet-wide in the synced .claude/settings.json | every project .mcp.json on this box is hub-emitted, not third-party; single-operator threat model; the per-repo approval dialog would fire ~46 times for files we wrote ourselves | .claude/settings.json (synced) · plan-3 § Decisions |
| D-029 | 2026-08-30 | infra (plan-3 decision row 2, operator-approved via plan) | the emitter is a SANCTIONED cross-repo distribution path of the governance-sync class: writes exactly one gitignored file per repo, never commits in a target repo (write-set containment TESTED) | operator ordered the split; the CLAUDE.md cross-repo HARD STOP's exemption reasoning walked in the plan's digest row 12 | scripts/sysadmin/emit_mcp_project_config.py · plan-3 digest |
| D-028 | 2026-08-30 | infra (plan-3 decision row 1, operator-approved via plan) | emitted .mcp.json = gitignored + resolved-inline DATABASE_URI + hub-regenerated (never committed, never hand-edited) | `${VAR}` expansion reads the SHELL env, not the repo .env (grounded via official docs 2026-08-30), so per-repo DB URIs cannot use expansion; gitignore keeps the credential out of history | manifest "MCP config" gitignore group · emitter docstring |
| D-027 | 2026-08-30 | operator | census round 2: candle → saas-skeleton ("candle is saas" — domain plain saas, NO content overlays) · exam-coach → mobile-app ("mobile app to build apk" — a NEW correction beyond D-023: swaps the web-GUI MCP set for maestro/mobile-mcp) · untyped working dirs CONFIRMED correct as-is (universal 6 post-trim, by design); both repos committed+pushed with own ledger rows; candle had NO remote (created mobasak/candle, branch 'mobasak/candle' pushed to master) | closes the last unclassified repo — the census is now operator-confirmed over the entire /opt population | the 2 repos' project.yaml + their D-001 rows |
| D-026 | 2026-08-30 | operator | TYPE FIXES EXECUTED BY HUB — operator authorized cross-repo this turn ("correct the wrong project types yourself from here, i authorize you"): 10 repos committed+pushed (7 → saas-skeleton incl. calendar-orchestration-engine — UI-bearing wins, node packs stay glob-active; whatsapp-agent + tojlo-mail → chrome-extension, tojlo-mail INTERIM until office-extension adoption); each repo got its own D-001 ledger row + CHANGELOG same-change. Residue: proposal-creator + marketing-argumant-generator have NO git remote (commits local-only — off-box-unprotected until a repo exists); image-generation archival + office-extension registry adoption + spec-shape/catalog ripple stay with fleet | fleet busy; the split's B1 hold-point needed the corrected types; explicit this-turn authorization satisfies the cross-repo HARD STOP | the 10 repos' project.yaml + their DECISIONS.md D-001 rows · hub mail to fleet |
| D-025 | 2026-08-30 | operator+infra | D-022's health-overlay predicate applied to the EXISTING fleet (operator audit: "check all projects we have agreed will get correct mcps"): longephedia-vault + supplement-tracker-advisor get +citation-verifier +claim-validator(when built) +pubchem; office-extension per-type row staged PENDING fleet's type adoption (playwright + chrome-devtools); image-generation excluded from emission via the D-023 archival ordering | "future health projects" wording had made the two existing health repos invisible to the standing rule — a predicate is swept against the live fleet, not only forward | docs/workstation/mcp-roster.md § per-repo overlays + § per-type table |
| D-024 | 2026-08-30 | operator | supersedes D-023 (delegation clause only): the type-census execution goes to FLEET, not intel — operator corrected the mislabel; the census corrections themselves stand unchanged | scaffolding + types are fleet's beat (charter); the registry-gap call (outlook-extension type) lands on the same beat | mail 01M19PKH30 re-routed @fleet + correction note 01M19PQASH |
| D-023 | 2026-08-30 | operator | /opt TYPE CENSUS corrected (operator's own product words, 12 repos): brand-identiy-creator, proposal-creator, marketing-argumant-generator, site-provisioner, trading-core, youtube, longephedia-vault → saas-skeleton · calendar-orchestration-engine → UI-bearing (web GUI + API; node stack vs the one saas type = intel's mapping call) · tojlo-mail → OUTLOOK extension (no registry type exists — nearest chrome-extension; registry-gap flagged) · whatsapp-agent → chrome-extension (website extension for WhatsApp chat) · image-generation → ARCHIVE · ai-model-catalog confirmed saas-skeleton · rest confirmed correct; execution delegated to intel by mail | types drive rule packs, GUI pipeline stages and per-type MCP sets — wrong types starve repos of their machinery; a saas typed python-api never runs ui-design/user-test | project.yaml per repo · mail to intel (id in commit) |
| D-022 | 2026-08-30 | operator | the HEALTH PAIR cross-wired: fabrik-citation-verifier ↔ fabrik-claim-validator each get the OTHER's MCP, and BOTH get pubchem (pubchem stays on wef too); standing rule: future health projects get all three overlays at adoption; claim-validator's MCP server does not exist yet — build requested of the owning repo | both services feed the operator's upcoming health projects — claims need citations verified and substances resolved, citations need claims context; per-repo overlays, never fleet-wide | docs/workstation/mcp-roster.md § per-repo overlays + the planned row |
| D-021 | 2026-08-30 | operator | serena ADOPT-AND-WIRE, universal (all 12 types): corpus now names it for symbol-level grounding — plan-after-chat Phase 1 (find_symbol / find_referencing_symbols, Grep as fallback) + review's caller/impact adjudication | prior zero usage (2 of 57 repos) was zero corpus wiring, not quality — agents use what commands name; measured trial: still unused after wiring = a retirement case with evidence | docs/workstation/mcp-roster.md § serena · commands/_sources/{fabrik-plan-after-chat,fabrik-review}.md |
| D-020 | 2026-08-30 | operator | postgres-pro UNIVERSAL — ON all 12 scaffold types ("fix postgres-pro and wire it all type of projects"), overriding the shape-driven (`needs_database`-only) proposal; crash fixed same-change: `uvx --with 'mcp<2'` pin (mcp 2.x renamed FastMCP), synced to all 5 fleet rosters + DR-backed | every repo touches the shared PG sooner or later; a restricted read-only DB lens is cheap (light idle) and safer than raw psql; per-repo DATABASE_URI rides the split implementation (old URI targeted dead :15432) | docs/workstation/mcp-roster.md § universal set + § chronic non-connectors |
| D-019 | 2026-08-30 | operator+infra | chrome-devtools RIDES WITH playwright everywhere playwright is granted — the 6 UI-bearing types (D-015) AND every per-repo playwright overlay, so wef gets +chrome-devtools alongside its D-016 playwright | playwright drives the screen, chrome-devtools measures it (lighthouse_audit CWV, throttling, traces) — the corpus binds both by name in the same loops; CWV on a produced storefront is conversion tooling | docs/workstation/mcp-roster.md § chrome-devtools ruling in full |
| D-018 | 2026-08-30 | operator+infra | media-engine is CONTENT-driven, never type-driven: ON for the media producers (wef, brand-identiy-creator, youtube) + any future repo whose product/pipeline output is media; OFF elsewhere (hub + fabrik-lib keep it via hub-class) | media need follows what a repo PRODUCES, not its scaffold type; 295MB/4p per window is waste in ~40 non-producing repos; one-off design assets route through a producer/hub window or the engine's API | docs/workstation/mcp-roster.md § media-engine |
| D-017 | 2026-08-30 | operator+infra | shadcn + magicui ALSO on web-ecommerce-factory — supersedes the doc-recorded magicui boundary ("use on the SaaS UI, never on produced sites") for wef ONLY | wef's storefronts are React/Tailwind (Medusa direction); motion components are conversion tooling on a commercial site; the boundary stands for every other produced-site context | docs/workstation/mcp-roster.md § per-repo overlays |
| D-016 | 2026-08-30 | operator | playwright ALSO on web-ecommerce-factory (per-repo overlay on D-015's six UI types) | wef drives/verifies the ecommerce sites it produces — rendered-surface work in a repo whose own type would not grant it | docs/workstation/mcp-roster.md § playwright |
| D-015 | 2026-08-30 | operator+infra | playwright + chrome-devtools ON for the 6 UI-bearing types only (never crawling — the research chain is exa/brave/firecrawl); fabrik-lib is HUB-CLASS under the split: full roster, like /opt/fabrik | playwright drives OUR screens, not the web; fabrik-lib builds modules for every type and its agent needs the full toolbox | docs/workstation/mcp-roster.md § per-type sets + § the split |
| D-014 | 2026-08-30 | operator+infra | six MCP dispositions: github RETIRED (gh CLI covers it, 3 corpus refs swapped, removed from all 5 rosters) · grafana hub-only · shadcn saas (+conditional desktop/static) · magicui saas-only · mobile-mcp + maestro mobile-app only | 2 grounding refs vs 173MB/4p per window; grafana consumers run hub-side; the four UI/mobile servers are type-tooling (maestro alone 724MB/window) | 98a25cc7-era docs/workstation/mcp-roster.md · this commit |
| D-013 | 2026-08-30 | operator | session-recall + exa + brave-search + firecrawl are UNIVERSAL — in every project's roster, no exception; any future roster trim excludes these four | "must be used for all projects no exception" — kills the firecrawl->curl corpus-swap candidate; the firecrawl FSLegacyMainResolve crash (corrupted npx cache entry) fixed same-turn | docs/workstation/mcp-roster.md § the split |
| D-012 | 2026-08-30 | operator | Traycer is rarely used — direction: run its command chain natively in Claude Code; the command EVALUATION is sequenced AFTER the MCP evaluation and the rules evaluation finish | "we rarely use traycer instead, i want to use their commands here" | docs/orchestrator/ (the chain + _traycer-skills shims) · docs/STRATEGIC_BACKLOG.md row |
| D-011 | 2026-08-30 | operator+infra | the decision ledger BUILT and rolled out fleet-wide — 48 repos seeded on the first sync; helper + duties + corpus wiring live | plan-1 executed to done, all 3 phases reviewed to no-op | docs/development/plans/archived/2026-08-30-plan-1-decision-ledger.md · 0d2ca091 · 18e38d9c · b321ea39 |
| D-010 | 2026-08-30 | operator+infra | rule-grounding gate v2: MUST-READ pack set is COMPUTED (FLOOR + rubric-MATCHED), digest rows carry verbatim quote + file:line, check_rule_grounding grades plans | "partially, not the full contract" — an unfollowable 26-pack duty teaches skimming; a quote is greppable proof | 4b97f5e4 · 65c50e23 · commands/_fragments/grounding-rules.md |
| D-009 | 2026-08-30 | operator+infra | the interrogative floor: specs answer WHO/WHY/WHAT/HOW/WHEN/WHERE at named sections; Personas includes automated consumers; ## Lifecycle mandated; NO-PERSONAS/NO-LIFECYCLE graded | three approval dialogues each surfaced a question the artifact should have answered | 730a1af5 · commands/_sources/fabrik-spec.md |
| D-008 | 2026-08-30 | operator+infra | adoption-forces taxonomy frozen with citable AF-1..AF-32 ids; market-facing specs name the primary persona's forces by id | "we will use these while creating distribution channels"; channel choice follows the timing forces | 8760e000 · docs/reference/product-adoption-forces.md |
| D-007 | 2026-08-30 | operator+infra | decision ledger storage = per-repo docs/DECISIONS.md FILE; postgres, JSONL, per-decision ADR files, semantic memory all REJECTED | git gives who/when tamper-evident; grep is the query; field practice (decision-log scale rule, 8 cited sources) | docs/superpowers/specs/2026-08-30-decision-ledger-v2-design.md § Storage decision |
| D-006 | 2026-08-30 | operator+infra | 1c APPROACH floor: >=2 distinct cited sources from >=2 tools with a real search leg; "internal-only" never waives approach research; APPROACH-FLOOR check | a decision-ledger spec shipped on one summariser fetch and was operator-REJECTED; the mandated search overturned its semantics in minutes | 21f37809 · docs/archive/specs/2026-08-30-decision-ledger-design.md |
| D-005 | 2026-08-30 | operator+infra | ASK has a bar: intake dispositions derive from the frozen goal artifacts (FEATURES/rivals/flows/spec chain) before ANY question reaches the operator | "why are you asking this to me? dont you know our rules, commands, goals?" — the artifacts had every answer | 5f07370a · commands/_fragments/chat-intake.md |
| D-004 | 2026-08-30 | operator | docker volume deletion is NEVER offered as cleanup — volumes are DATA; classify read-only, fix the leak generator, delete only an explicit id list on the operator's word | among 852 "dangling" volumes sat a Tryton filestore, a live PG17 dev DB, and wpf's MariaDB; a past prune cost tryton translations | 025cbb20 · CLAUDE.md § HARD STOPS |
| D-003 | 2026-08-30 | operator+infra | context7 MCP retired from the window roster; corpus grounding re-armed on official-docs WebFetch | 45 lifetime calls vs 364MB/window across every window; WebFetch covers the need | 74ad8a06 · docs/workstation/mcp-roster.md |
| D-002 | 2026-08-30 | infra | cache-prune never wipes ~/.npm/_npx unconditionally — capped like every other cache | the 2026-08-30 mass MCP outage: npx-cache wipe + window restart = every npx-spawned server dead | 7862d8a2 · docs/workstation/cleanup-automation.md |
| D-001 | 2026-08-29 | operator+infra | governance-sync moved to POST-commit — it can no longer abort a commit; a sync failure prints loudly with the manual re-run command | the pre-commit sync was the widest concurrent-writer window and aborted innocent sibling commits | 188d8fc9 · CLAUDE.md (hub contract header) |
| D-000 | 2026-08-30 | operator | the decision ledger adopted fleet-wide: every agent records decisions (why/what/where/who/when) and ALWAYS queries the ledger before answering where-is/did-we-decide | "i dont want to struggle like this again … i want a full answer not struggling ais around to find it" — the crawling-MCP multi-hour hunt | docs/superpowers/specs/2026-08-30-decision-ledger-v2-design.md · docs/development/plans/archived/2026-08-30-plan-1-decision-ledger.md |
| D-102 | 2026-09-03 | operator direction ("drop traycer orchestrator … epic to ticket workflow will become redundant … mega to epic commands should be updated … 3 claude code windows over the epics") + intel `/fabrik-spec-review` r8 (5 passes, md5 no-op 4ac05ba5; pool 3 units scored 4/1/0 + native Opus) | **Multi-agent-per-repo spec CONVERGED r8, and three decisions inside it: (1) RETIRE `docs/orchestrator/epic-to-ticket-workflow/` (13 docs + checklist) — every step has a stronger, gated corpus twin (mapped step-for-step in the spec § Chain consolidation (a), incl. ettw 00's INFRA-CHECK → `/fabrik-spec`'s epic-file intake), and two capabilities are accepted LOSSES, not gaps (09's Done-but-affected matrix; 06's per-ticket DO-NOT prose); (2) RETIRE the Traycer layer (`_traycer-skills/`, `traycer_mirror.py`, the wiring doc) — its cockpit/driver were never built; (3) ASSEMBLE the four mega docs as three corpus commands `/fabrik-vision`, `/fabrik-epics`, `/fabrik-epics-review` (17 `/fab-*` → 3 `/fabrik-*`), the docs MOVING into the sources, `--assign` running inside the review's Step 1.5. Plus the two operator-named edits: an epic-file intake for `/fabrik-spec` and `owned_paths` carried epic → spine File Scope → plan-lock, with a `check_plan_tickets` rule and an `/fabrik-execute-plan` refusal.** This row supersedes D-101's patch-in-place verdict (its premise — both chains kept, 5,076 lines — no longer holds; the option is superseded, not contradicted on evidence). | Why: measured, not assumed — 12 of the audit's 15 findings live in ettw; retiring it closes R1/R2/R3/R5/R13/R14 by deletion; assembling four docs buys run-record, close-feedback, NEXT and question-bar fragments and deletes the whole wrapper render path (`ORCH_SOURCES`, `_render_orch_wrapper`, `check_command_corpus._orch_corpus`). The review re-derived every count (33 sources, 17 wrappers, 13 ettw docs, 4,297 / 5,076 / 2,132 lines), corrected the audit's phantom router path (it named a non-existent file under scripts/; the hook is `.claude/hooks/skill_router.py`) and its unmeasured "5,706", and found `claude --help` visibility of `--worktree` is account/flag-gated (4 mentions vs 0 on the same binary the same day) — the launch path is grounded by probe only (passed ×4 today under both states). | Where: `docs/superpowers/specs/2026-09-03-multi-agent-per-repo-design.md` (§ Chain consolidation (a)–(f), § Personas loop 12 + 9E GUI / 12 + 7E headless, budget 39 at E=3, residual R6). Supersedes D-101 (verdict only; the audit ledger stands). Reversible (a design + a retirement recoverable from git; no surface changed yet). Next: operator approval → `/fabrik-plan-after-chat` on this spec; the build (infra's beat, carried by intel under the 2026-09-03 authorisation). |
