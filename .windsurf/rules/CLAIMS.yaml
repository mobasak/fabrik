# Rules-corpus CLAIMS REGISTER — every external-world assertion the packs rely on,
# as a dated, re-verifiable row. (Rules pass 2026-09-01: the drift that matters is
# CLAIMS, not version strings — the Alpine rationale went silently false while its
# rule stayed right; no regex sees that.)
#
# CONTRACT
# - Grown file-by-file by the rules currency pass: evaluating a pack extracts its
#   external claims here, dated, with the source that grounded them.
# - `rules_currency_watch.py --claims` (weekly cron rider) mails infra every claim
#   whose last_verified is older than its window — the mail carries the verify
#   hints, so the receiving session re-researches EXACTLY the stale claims and
#   bumps the dates (or fixes the pack when the world moved).
# - Version LITERALS (python:X.Y, node:N) are additionally auto-watched against
#   endoflife.date by the same script — kind: version rows marked `auto: true`
#   page within a week of an upstream release, not at window expiry.
# - A claim REFUTED by re-verification = fix the pack in the same change and
#   supersede the row (new row, old one gets `superseded: <date>`), mirroring the
#   decision-ledger discipline.
#
# window_days guidance: 90 fast-moving (versions, tool landscape) · 180 practices
# and rationales · 365 stable standards/deprecations.

claims:
# pack: may name a reference DOC (docs/reference/apis/EXTERNAL_SYSTEMS.md) when the claim lives in the
# fleet index rather than a rule pack — the watcher keys on last_verified/window_days only (2026-09-02).
  - id: py-current-stable
    pack: core/10-python.md
    claim: "Python current stable is 3.14 (packs pin python:3.14)"
    kind: version
    auto: true            # endoflife.date watcher pages on release, not window
    verify: "https://endoflife.date/api/python.json"
    window_days: 90
    last_verified: 2026-09-01

  - id: uv-standard
    pack: core/10-python.md
    claim: "uv is the mandated package manager AND the industry-standard choice (1.0 shipped, broad production adoption; pip/poetry relegated)"
    kind: practice
    verify: "search: python package manager standard <year>; https://docs.astral.sh/uv/"
    window_days: 180
    last_verified: 2026-09-01

  - id: fastapi-lifespan
    pack: core/10-python.md
    claim: "@app.on_event is deprecated; lifespan context manager is the current FastAPI idiom"
    kind: deprecation
    verify: "https://fastapi.tiangolo.com/advanced/events/"
    window_days: 365
    last_verified: 2026-09-01

  - id: pydantic-settings-v2
    pack: core/10-python.md
    claim: "pydantic-settings BaseSettings + SettingsConfigDict is the current idiom; no pydantic v3 breaking change shipped"
    kind: version
    verify: "https://docs.pydantic.dev/latest/concepts/pydantic_settings/"
    window_days: 180
    last_verified: 2026-09-01

  - id: alpine-rationale
    pack: core/10-python.md
    claim: "Alpine ban rationale: musllinux wheels exist (PEP 656) but coverage partial, source builds ~50x slower, musl allocator/stack degrades CPython — the ban still pays"
    kind: rationale
    verify: "search: python alpine musllinux wheels coverage <year>; https://pythonspeed.com/articles/alpine-docker-python/"
    window_days: 180
    last_verified: 2026-09-01

  - id: sqlalchemy2-text
    pack: core/10-python.md
    claim: "SQLAlchemy 2.0 requires text() for raw SQL strings in execute()"
    kind: deprecation
    verify: "https://docs.sqlalchemy.org/en/20/changelog/migration_20.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: rfc9457-problem-details
    pack: core/10-python.md, core/15-api-contracts.md
    claim: "RFC 9457 ProblemDetails is the current HTTP error-payload standard (obsoletes 7807)"
    kind: standard
    verify: "https://www.rfc-editor.org/rfc/rfc9457"
    window_days: 365
    last_verified: 2026-09-01

  - id: debian-slim-variant
    pack: core/30-ops.md
    claim: "Fleet pins -slim-bookworm; Docker's bare -slim now defaults to trixie (Debian 13 stable) — flip is a 5-pack class commit owned by 30-ops"
    kind: version
    auto: true
    verify: "https://hub.docker.com/_/python/tags?name=slim"
    window_days: 90
    last_verified: 2026-09-01
    superseded: 2026-09-01   # flip EXECUTED (D-064) — and the bare-slim clause was over-broad: true for python, FALSE for node (24-slim digest == 24-bookworm-slim, proven 2026-09-01). Successor: debian-pin-trixie

  - id: uv-copy-pinned
    pack: core/30-ops.md
    claim: "Astral's documented Docker pattern is COPY --from=ghcr.io/astral-sh/uv:<pin> with an exact version pin (uv_version span, 0.12.8 at write); unpinned pip-install-uv re-resolves a build tool per build"
    kind: practice
    verify: "https://docs.astral.sh/uv/guides/integration/docker/; https://pypi.org/pypi/uv/json"
    window_days: 90
    last_verified: 2026-09-01

  - id: redis-fleet-major
    pack: core/30-ops.md
    claim: "redis-main runs redis:7-alpine (probed live 2026-09-01); redis 7.x active support ended 2025-05, security-only to 2029-12; current major is 8.x — deliberate fleet state, upgrade is a fleet infra decision with a 2029 horizon"
    kind: version
    verify: "https://endoflife.date/redis; ssh vps sudo docker inspect redis-main --format '{{.Config.Image}}'"
    window_days: 180
    last_verified: 2026-09-01

  - id: node-lts-line
    pack: core/12-node.md, core/20-typescript.md
    claim: "Packs pin node:24 via the node_lts span (sole Active LTS; 22 is Maintenance, EOL Apr 2027); node 26 becomes LTS 2026-10-28 — the auto-watcher pages then"
    kind: version
    auto: true
    verify: "https://endoflife.date/api/nodejs.json"
    window_days: 90
    last_verified: 2026-09-01

  - id: glitchtip-5xx-capture
    pack: core/55-observability.md
    claim: "VERIFIED IN SOURCE 2026-09-01 (55-observability's turn): sentry-sdk captures deliberately raised HTTPException via failed_request_status_codes, default _DEFAULT_FAILED_REQUEST_STATUS_CODES = frozenset(range(500,600)) — i.e. 5xx ONLY (4xx captured by nothing), matched DUCK-TYPED on .status_code (not HTTPException-typed), recorded as handled: True (not an unhandled crash). Stable since sentry-sdk 2.15; fleet pins >=2.18. Basis of 10-python's no-logger.exception rule."
    kind: rationale
    verify: "sentry_sdk/integrations/__init__.py::_DEFAULT_FAILED_REQUEST_STATUS_CODES + starlette.py::patch_exception_middleware; https://docs.sentry.io/platforms/python/integrations/fastapi/"
    window_days: 365
    last_verified: 2026-09-01

  - id: ts-strip-types-stable
    pack: core/12-node.md
    claim: "Node type stripping is stable and enabled by default (run .ts directly; ts-node/tsx obsolete for dev; opt-out flag exists)"
    kind: deprecation
    verify: "https://nodejs.org/api/typescript.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: express5-npm-latest
    pack: core/12-node.md
    claim: "Express current major (5.x line) is npm `latest` since 2025; legacy major maintenance-only — bare `npm i express` installs the current major"
    kind: version
    verify: "https://www.npmjs.com/package/express"
    window_days: 180
    last_verified: 2026-09-01

  - id: fastify-current-major
    pack: core/12-node.md
    claim: "Fastify 5.x is the current major (prior major EOL mid-2025); greenfield default stands"
    kind: version
    verify: "https://fastify.dev/docs/latest/Reference/LTS/"
    window_days: 180
    last_verified: 2026-09-01

  - id: fastify-throughput-4x
    pack: core/12-node.md
    claim: "Fastify sustains roughly 4x Express throughput on JSON APIs (order-of-magnitude decision aid, not a benchmark)"
    kind: rationale
    verify: "search: fastify express benchmark <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: npm-install-scripts-vector
    pack: core/12-node.md
    claim: "Install scripts are the dominant npm supply-chain vector (Shai-Hulud worm class; Mastra easy-day-js typosquat 2026-06: 140+ versions backdoored via postinstall); npm's current major blocks dependency install scripts by default; `npm ci --ignore-scripts` stays explicit"
    kind: practice
    verify: "https://github.com/lirantal/npm-security-best-practices; https://www.stepsecurity.io/blog/mastra-npm-packages-compromised-using-easy-day-js"
    window_days: 180
    last_verified: 2026-09-01

  - id: node-cve-trio-2026
    pack: core/12-node.md
    claim: "CVE-2026-21713 (HMAC non-constant-time compare), CVE-2026-21710 (crafted __proto__ header crash), CVE-2026-21637 (TLS SNI-callback DoS) — all fixed in Node's 2026-03-24 security release; basis of the patch-floor, timingSafeEqual, header-allowlist and TLS-at-Traefik rules"
    kind: rationale
    verify: "https://nodejs.org/en/blog/vulnerability/march-2026-security-releases"
    window_days: 365
    last_verified: 2026-09-01

  - id: als-contextframe-negligible
    pack: core/12-node.md
    claim: "AsyncContextFrame is the default AsyncLocalStorage implementation in current LTS and makes ALS overhead negligible (~2% vs manual passing; large gains vs async_hooks)"
    kind: rationale
    verify: "https://blog.platformatic.dev/the-hidden-cost-of-context"
    window_days: 180
    last_verified: 2026-09-01

  - id: esm-tsc-default
    pack: core/12-node.md
    claim: "Node TSC consensus: ESM is the definitive default; ESM-only package set keeps growing (chalk, execa, …)"
    kind: practice
    verify: "search: node esm default cjs <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: docker-stop-grace-default
    pack: core/12-node.md
    claim: "Docker's default stop grace period is 10s; the 20s drain backstop works only under the scaffold's stop_grace_period: 45s (scaffold.py emits it)"
    kind: rationale
    verify: "https://docs.docker.com/reference/cli/docker/container/stop/"
    window_days: 365
    last_verified: 2026-09-01

  - id: idempotency-key-header-standard
    pack: core/15-api-contracts.md
    claim: "Idempotency-Key (un-prefixed) is the IETF httpapi standards-track header name (draft-ietf-httpapi-idempotency-key-header, active at -07); not yet an RFC — re-verify for publication or abandonment"
    kind: standard
    verify: "https://datatracker.ietf.org/doc/draft-ietf-httpapi-idempotency-key-header/"
    window_days: 180
    last_verified: 2026-09-01
    superseded: 2026-09-01   # REFUTED same day: the draft EXPIRED at -07 (IESG state Expired) — caught by the file-3 second opinion's datatracker/history check

  - id: idempotency-key-header-consensus
    pack: core/15-api-contracts.md
    claim: "Idempotency-Key (un-prefixed) is the industry-CONSENSUS header name (Stripe/PayPal practice); the IETF httpapi draft defining it EXPIRED at -07 without becoming an RFC — re-verify for revival or publication"
    kind: standard
    verify: "https://datatracker.ietf.org/doc/draft-ietf-httpapi-idempotency-key-header/history/"
    window_days: 180
    last_verified: 2026-09-01

  - id: rfc9745-deprecation-header
    pack: core/15-api-contracts.md
    claim: "Deprecation header is RFC 9745 (2025-03) with a structured DATE value (@unix-timestamp), obsoleting the draft-era boolean form; Sunset is RFC 8594"
    kind: standard
    verify: "https://www.rfc-editor.org/info/rfc9745/"
    window_days: 365
    last_verified: 2026-09-01

  - id: hey-api-openapi-ts-current
    pack: core/15-api-contracts.md
    claim: "@hey-api/openapi-ts is the current OpenAPI→TS codegen (used by Vercel/PayPal, ~4.5M weekly); still pre-1.0 — 'pin exact version' is upstream's own guidance"
    kind: practice
    verify: "https://www.npmjs.com/package/@hey-api/openapi-ts"
    window_days: 180
    last_verified: 2026-09-01

  - id: oasdiff-current
    pack: core/15-api-contracts.md
    claim: "oasdiff is maintained (v1.26.x, 2026-07) and `oasdiff breaking --fail-on ERR` remains the right invocation; supports OpenAPI 3.0/3.1/3.2"
    kind: practice
    verify: "https://github.com/oasdiff/oasdiff/releases"
    window_days: 365
    last_verified: 2026-09-01

  - id: ts-native-compiler-current
    pack: core/20-typescript.md
    claim: "The current `typescript` npm major ships the native Go compiler as tsc (GA 2026-07, ~8-12x builds; same syntax, tsc --noEmit verbatim); programmatic API lands next minor — API-dependent toolchains (typescript-eslint, framework plugins) pin the previous major meanwhile"
    kind: version
    verify: "https://devblogs.microsoft.com/typescript/; https://www.npmjs.com/package/typescript"
    window_days: 90
    last_verified: 2026-09-01

  - id: zod4-stable
    pack: core/20-typescript.md
    claim: "zod 4 is the stable root export (4.5 current); z.object().parse(process.env) + z.coerce/default idioms unchanged from the pack's example"
    kind: version
    verify: "https://zod.dev/"
    window_days: 180
    last_verified: 2026-09-01

  - id: erasable-syntax-only-recommended
    pack: core/20-typescript.md
    claim: "erasableSyntaxOnly + verbatimModuleSyntax are in Node's officially recommended tsconfig for natively-run TS; native stripping refuses ALL enums/namespaces-with-code/parameter-properties"
    kind: practice
    verify: "https://nodejs.org/api/typescript.html"
    window_days: 180
    last_verified: 2026-09-01

  - id: baseurl-hard-error
    pack: core/20-typescript.md
    claim: "baseUrl is a hard error in the current TS major; paths works standalone with relative mappings"
    kind: deprecation
    verify: "https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/"
    window_days: 365
    last_verified: 2026-09-01

  - id: stdlib-uuid7
    pack: core/25-data-postgres.md
    claim: "uuid.uuid7() is stdlib as of Python 3.14 (RFC 9562; merged 2025-03) and returns plain uuid.UUID — the uuid_utils.compat idiom is legacy-python-only"
    kind: version
    verify: "https://docs.python.org/3/library/uuid.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: pg-fleet-major
    pack: core/25-data-postgres.md
    claim: "postgres-main runs major 16 (agents-fabrik.md:165; EOL 2028-11) — predates native uuidv7() (PG18, 2025-09-25), so app-side generation is the fleet default. TRIPWIRE: when postgres_major flips >=18, rewrite § Primary Keys (DEFAULT uuidv7() becomes the fleet default) — the 'predates it' sentence goes false on flip"
    kind: version
    verify: "https://endoflife.date/api/postgresql.json; grep 'PostgreSQL 16' /opt/fabrik/agents-fabrik.md"
    window_days: 180
    last_verified: 2026-09-01

  - id: pgbouncer-prepared-statements
    pack: core/25-data-postgres.md
    claim: "PgBouncer supports protocol-level named prepared statements in transaction mode via max_prepared_statements (since 1.21, late 2023; default 0/off); client-side the safe fallback is BOTH statement_cache_size=0 (asyncpg) and prepared_statement_cache_size=0 (SQLAlchemy dialect LRU)"
    kind: practice
    verify: "https://www.pgbouncer.org/config.html; https://docs.sqlalchemy.org/en/20/dialects/postgresql.html (asyncpg Prepared Statement Cache)"
    window_days: 180
    last_verified: 2026-09-01
    superseded: 2026-09-01   # 'default 0/off' REFUTED same day against pgbouncer.org primary: current releases default 200 (ON) — second-opinion catch

  - id: pgbouncer-prepared-statements-v2
    pack: core/25-data-postgres.md
    claim: "PgBouncer tracks protocol-level named prepared statements in transaction+statement pooling via max_prepared_statements, DEFAULT 200 (ON) in current releases (feature since 1.21); SQL-level PREPARE untracked; client-side fallback for non-tracking poolers = statement_cache_size=0 AND prepared_statement_cache_size=0"
    kind: practice
    verify: "https://www.pgbouncer.org/config.html; https://docs.sqlalchemy.org/en/20/dialects/postgresql.html (asyncpg Prepared Statement Cache)"
    window_days: 180
    last_verified: 2026-09-01

  - id: debian-pin-trixie
    pack: core/30-ops.md (+ debian_codename span users)
    claim: "Fleet Debian pin flipped bookworm->trixie 2026-09-01: bookworm regular security ENDED 2026-07-12 (LTS to 2028-06); trixie stable 2025-08-09, full support to 2028-08; python -slim-trixie + node trixie-slim images live on Docker Hub (amd64)"
    kind: version
    verify: "https://www.debian.org/releases/trixie/; https://hub.docker.com/_/python/tags?name=slim-trixie"
    window_days: 180
    last_verified: 2026-09-01

  - id: pgvector-not-installed
    pack: core/25-data-postgres.md
    claim: "postgres-main runs postgres:16-alpine with NO pgvector extension (probe: ssh vps sudo docker exec postgres-main psql -U postgres -tc 'select extname from pg_extension' -> plpgsql only); the pgvector/pgvector image is documented but not deployed — vector-needing projects must request the infra change"
    kind: rationale
    verify: "the probe command in the claim, run live"
    window_days: 180
    last_verified: 2026-09-01

  - id: jwt-alg-practice
    pack: core/35-security-auth.md
    claim: "HS256 with a 256-bit secret remains sound for issuer==verifier (Pattern A); 2026 consensus prefers EdDSA/ES256 over RSA for multi-verifier; algorithm pinning in the verifier (explicit allow-list, never header-dispatched) is the mandatory confusion-attack defense"
    kind: practice
    verify: "https://cheatsheetseries.owasp.org/cheatsheets/JSON_Web_Token_for_Java_Cheat_Sheet.html; search: jwt signing algorithm best practice <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: nextjs-middleware-cve
    pack: core/35-security-auth.md
    claim: "CVE-2025-29927 (x-middleware-subrequest header bypass, CVSS 9.1, patched 2025-03 in 12.3.5/13.5.9/14.2.25/15.2.3) is the evidence base for the never-middleware-only rule; the rule outlives the patch"
    kind: rationale
    verify: "https://securitylabs.datadoghq.com/articles/nextjs-middleware-auth-bypass/"
    window_days: 365
    last_verified: 2026-09-01

  - id: xfo-obsolete
    pack: core/35-security-auth.md
    claim: "X-Frame-Options is formally obsolete (MDN, OWASP Clickjacking cheat sheet); CSP frame-ancestors is the modern control and wins when both are present — ship both, frame-ancestors primary"
    kind: standard
    verify: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/X-Frame-Options; https://cheatsheetseries.owasp.org/cheatsheets/Clickjacking_Defense_Cheat_Sheet.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: nextjs-proxy-rename
    pack: core/35-security-auth.md
    claim: "Next.js 16 renamed middleware.ts to proxy.ts (request-shaping, not a security boundary) and SILENTLY IGNORES a leftover middleware.ts at build — nonce/redirect logic stops with no error; the pack's file references track this"
    kind: deprecation
    verify: "https://nextjs.org/docs/app/guides/content-security-policy; https://github.com/vercel/next.js/discussions/84842"
    window_days: 180
    last_verified: 2026-09-01

  - id: supabase-signing-keys
    pack: core/35-security-auth.md
    claim: "Supabase new projects default to asymmetric JWT signing (ES256) since 2025-10; getClaims() verifies locally via JWKS and falls back server-side for legacy HS256; JWKS endpoint EMPTY on HS256 projects (the pack's Pattern-B guidance rests on all three)"
    kind: version
    verify: "https://supabase.com/docs/guides/auth/signing-keys"
    window_days: 180
    last_verified: 2026-09-01

  - id: argon2id-lib-defaults
    pack: core/35-security-auth.md
    claim: "OWASP mandates Argon2id specifically (min m=19MiB/t=2/p=1); the vendored lib's argon2-cffi defaults (Argon2id, 64MiB/t=3/p=4) exceed the minimums — naming 'Argon2id + lib defaults' suffices, no cost table needed in the pack"
    kind: practice
    verify: "https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html; https://argon2-cffi.readthedocs.io/en/stable/api.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: agents-md-standard
    pack: core/40-documentation.md
    claim: "AGENTS.md is the cross-tool open convention (agents.md, Aug 2025; Linux Foundation Agentic AI Foundation stewardship; 60k+ repos, read by 20-30+ agent tools) — a project's AGENTS.md content bar is agent operating instructions, not just topology"
    kind: standard
    verify: "https://agents.md/"
    window_days: 365
    last_verified: 2026-09-01

  - id: ai-markdown-rag-rules
    pack: core/40-documentation.md
    claim: "Heading-hierarchy chunking with intact fenced blocks remains standard RAG practice; one-H1 codified in llms.txt; the pack's 10 AI-markdown rules stay consensus (2 are house style, honestly framed)"
    kind: practice
    verify: "https://llmstxt.org/; search: markdown chunking rag best practice <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: playwright-mv3-sw-fix
    pack: core/45-testing-strategy.md
    claim: "The MV3 service-worker-restart flake fix landed in @playwright/test 1.59 (PR #39476); current is 1.62.x, so every fresh install has it — the pack's floor was retired as noise, the claim carries the boundary"
    kind: version
    verify: "https://registry.npmjs.org/@playwright/test/latest; https://github.com/microsoft/playwright/pull/39476"
    window_days: 365
    last_verified: 2026-09-01

  - id: vitest-rsc-boundary
    pack: core/45-testing-strategy.md
    claim: "2026 consensus incl. official Next.js docs: Vitest+RTL is the legitimate unit lane for sync/props-only components + server-action logic; ASYNC Server Components cannot be rendered by any unit runner (by design) — Playwright E2E owns them; the pack's blanket Vitest ban was retired for the async-boundary rule"
    kind: practice
    verify: "https://nextjs.org/docs/app/guides/testing/vitest"
    window_days: 180
    last_verified: 2026-09-01

  - id: chrome-extension-sideload-flags
    pack: core/45-testing-strategy.md
    claim: "Stable Chrome/Edge removed --load-extension/--disable-extensions-except (Chrome 137/139) — extension E2E must use Playwright's bundled Chromium; the pack's channel guidance rests on this"
    kind: deprecation
    verify: "search: chrome load-extension flag removed <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: musl-allocator-perf
    pack: core/10-python.md
    claim: "musl's allocator/thread-stack defaults still degrade CPython vs glibc (half of the Alpine ban's residual rationale; the reviewer flags it as aging)"
    kind: rationale
    verify: "search: musl mallocng CPython performance <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: robots-txt-rfc9309-standard
    pack: core/57-external-data-sourcing.md
    claim: "robots.txt is a published IETF standard (RFC 9309), not merely a convention; it carries no direct enforcement but non-compliance is used as bad-faith evidence"
    kind: standard
    verify: "search: RFC 9309 robots exclusion protocol standard status"
    window_days: 365
    last_verified: 2026-09-01

  - id: ai-crawler-ua-directives
    pack: core/57-external-data-sourcing.md
    claim: "AI-specific robots.txt user-agent directives (GPTBot, CCBot, Google-Extended, anthropic-ai, PerplexityBot) are the de-facto channel for reserving text-and-data-mining; the named set churns"
    kind: practice
    verify: "search: AI crawler user-agent robots.txt directives TDM opt-out <year>"
    window_days: 90
    last_verified: 2026-09-01
    superseded: 2026-09-02

  - id: scraping-bypass-tooling-liability
    pack: core/57-external-data-sourcing.md
    claim: "The live litigation wave reframes scraping from access-authorisation (CFAA, largely settled for logged-out public pages) to anti-circumvention, and targets the TOOLING layer — CAPTCHA solvers, anti-bot bypass, proxy services — as trafficking in circumvention tech. UNSETTLED: no ruling yet. This is why fabrik-lib/captcha-solve and the catalog's proxy/captcha categories are capabilities, not defaults"
    kind: rationale
    verify: "search: DMCA 1201 anti-circumvention web scraping captcha solver proxy liability ruling <year> — has any court RULED?"
    window_days: 90
    last_verified: 2026-09-01
    superseded: 2026-09-02

  - id: browsewrap-vs-clickwrap-scraping
    pack: core/57-external-data-sourcing.md
    claim: "ToS shown without affirmative agreement is weak against a logged-out fetcher; clickwrap/account terms bind and make breach an ordinary contract claim — so holding an account changes the answer"
    kind: practice
    verify: "search: browsewrap clickwrap enforceability scraping public data <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: eu-tdm-model-training-regime
    pack: core/57-external-data-sourcing.md
    claim: "Scraping that feeds MODEL TRAINING is regulated separately and more strictly in the EU (GPAI/TDM copyright-reservation obligations) than the same fetch for internal use. DELIBERATELY DATE-FREE in the pack: sources disagreed on the in-force dates during the 2026-09-01 pass, so the rule states the asymmetry, not a timeline"
    kind: standard
    verify: "search: EU AI Act GPAI training data copyright TDM opt-out obligations in force date — reconcile conflicting dates before asserting one"
    window_days: 180
    last_verified: 2026-09-01

  - id: mcp-roster-is-split
    pack: core/57-external-data-sourcing.md
    claim: "The MCP roster is SPLIT (user-level + per-repo .mcp.json), so ~/.claude.json alone shows a subset; canonical is docs/workstation/mcp-roster.md + the repo .mcp.json, probed by scripts/sysadmin/mcp_health.py"
    kind: rationale
    verify: "read: /opt/fabrik/.mcp.json and ~/.claude.json — is the split still in force?"
    window_days: 180
    last_verified: 2026-09-01

  - id: jitter-required-multiprocess-retry
    pack: core/58-resilience.md
    claim: "tenacity's wait_exponential has NO jitter and its own docstring calls it 'not suitable for resolving contention between multiple processes for a shared resource' — hence wait_random_exponential/wait_exponential_jitter for any worker fleet"
    kind: practice
    verify: "read: .venv/lib/**/tenacity/wait.py wait_exponential docstring — is the no-jitter caveat still there? + search: exponential backoff full jitter thundering herd <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: retryable-status-set
    pack: core/58-resilience.md
    claim: "The retryable HTTP set is 408/429/5xx; 400/401/403/404/422 are permanent. 429 is the single most common retryable vendor response, and a flat 'never retry 4xx' rule inverts it"
    kind: practice
    verify: "search: which HTTP status codes are safe to retry 429 408 <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: retry-after-two-formats
    pack: core/58-resilience.md
    claim: "Retry-After has TWO legal formats — delay-seconds and HTTP-date — and both must be parsed; the value is vendor-controlled so it must be CAPPED before it becomes either a sleep or a pause TTL"
    kind: standard
    verify: "search: Retry-After header HTTP-date delay-seconds RFC 9110 semantics"
    window_days: 365
    last_verified: 2026-09-01

  - id: openrouter-pin-disables-failover
    pack: core/58-resilience.md
    claim: "On OpenRouter, setting `sort` or `order` DISABLES load balancing — and outage-aware routing is part of load balancing — so pinning a provider silently opts the caller out of automatic failover; a `models` request-body array falls back on any error including downtime"
    kind: standard
    verify: "https://openrouter.ai/docs/features/provider-routing — do sort/order still disable load balancing, and does the models array still fall back on any error?"
    window_days: 180
    last_verified: 2026-09-02

  - id: stop-grace-period-default
    pack: core/58-resilience.md
    claim: "Docker Compose stop_signal defaults to SIGTERM and stop_grace_period defaults to 10 SECONDS, after which the daemon sends SIGKILL — so an app whose own shutdown timeout is >= the grace period is SIGKILLed mid-drain. Clean SIGTERM exit is code 143; 137 is SIGKILL (blown grace period, or OOM)"
    kind: standard
    verify: "https://docs.docker.com/reference/compose-file/services/ — is the default still 10s? + search: docker exit code 143 vs 137 SIGTERM SIGKILL"
    window_days: 365
    last_verified: 2026-09-02

  - id: depends-on-is-not-readiness
    pack: core/58-resilience.md
    claim: "Compose depends_on is START ORDER, not readiness; condition: service_healthy gates only the FIRST start and guarantees nothing about a dependency that restarts later — so every process still needs its own bounded, jittered boot retry with a startup deadline"
    kind: standard
    verify: "https://docs.docker.com/reference/compose-file/services/ — read depends_on + condition semantics"
    window_days: 365
    last_verified: 2026-09-02

  - id: population-retry-budget
    pack: core/58-resilience.md
    claim: "A per-request attempt cap does NOT bound fleet retry traffic — 3 attempts still triples load when every request fails at once. The standard control is a POPULATION budget (token bucket or a ratio ceiling, ~10% of recent traffic), as implemented by gRPC retry throttling and the AWS SDK retry quota, and recommended by Google SRE's overload chapter"
    kind: practice
    verify: "search: retry budget token bucket gRPC retry throttling AWS SDK retry quota <year>; https://sre.google/sre-book/addressing-cascading-failures/"
    window_days: 365
    last_verified: 2026-09-02

  - id: deadline-propagation-over-per-hop-timeouts
    pack: core/58-resilience.md
    claim: "Per-hop timeouts compose into work that outlives the caller ('ghost work'); the control is ONE absolute deadline propagated down the chain, each hop deriving its timeout from what remains and short-circuiting when too little is left. gRPC implements this natively via deadlines/grpc-timeout"
    kind: practice
    verify: "https://sre.google/sre-book/addressing-cascading-failures/ § Deadline propagation; + search: deadline propagation absolute deadline microservices <year>"
    window_days: 365
    last_verified: 2026-09-02

  - id: guard-fail-open-default
    pack: core/58-resilience.md
    claim: "For a Redis-backed guard that OPTIMISES (rate limit, pause flag, dedup) the practiced default when Redis is unreachable is FAIL-OPEN — often with a degraded local bound — because failing closed turns a cache blip into a full outage and, if Redis died of load, removes the only thing still serving. FAIL-CLOSED is for guards protecting something a breach does not recover from (contractual vendor limits, spend caps)"
    kind: practice
    verify: "search: rate limiter fail open vs fail closed redis down best practice <year> — is fail-open still the practiced default for optimising guards?"
    window_days: 180
    last_verified: 2026-09-02

  - id: cache-stampede-coalescing
    pack: core/58-resilience.md
    claim: "A hot key's expiry makes every concurrent request miss at the same instant and stampede the origin; the two practiced fixes are request coalescing (single-flight) and probabilistic early expiration/refresh-ahead — this herd arrives from your own cache, not from a retry loop, so jitter and backoff do not touch it"
    kind: practice
    verify: "search: cache stampede thundering herd singleflight probabilistic early expiration <year>"
    window_days: 365
    last_verified: 2026-09-02

  - id: circuit-breaker-half-open-probe-cap
    pack: core/58-resilience.md
    claim: "A circuit breaker's half-open state must admit a LIMITED number of trial calls (a configurable halfOpenMaxRequests / permittedNumberOfCallsInHalfOpenState); a half-open that admits every caller dumps full traffic onto a dependency that has not recovered, re-killing it on a recovery_timeout cycle"
    kind: standard
    verify: "https://learn.microsoft.com/en-us/azure/architecture/patterns/circuit-breaker — does Half-Open still specify a limited number of trial requests?"
    window_days: 365
    last_verified: 2026-09-02

  - id: ai-crawler-ua-directives-v2
    pack: core/57-external-data-sourcing.md
    claim: "supersedes ai-crawler-ua-directives: the AI robots.txt token set churns and vendors run SEVERAL bots each. Anthropic's live tokens are ClaudeBot (training) / Claude-User (user fetch) / Claude-SearchBot (indexing); anthropic-ai and Claude-Web are RETIRED. OpenAI and Perplexity document their user-initiated fetchers (ChatGPT-User, Perplexity-User) as not governed by robots.txt; Anthropic says all three of its bots honour it. Tokens still current: GPTBot, CCBot, Google-Extended, PerplexityBot"
    kind: practice
    verify: "https://support.claude.com/en/articles/8896518 — token list unchanged? + search: AI crawler user-agent tokens robots.txt <year>"
    window_days: 90
    last_verified: 2026-09-02

  - id: aipref-content-usage-draft
    pack: core/57-external-data-sourcing.md
    claim: "The IETF AIPREF working group's draft-ietf-aipref-attach defines a machine-readable Content-Usage rule for robots.txt AND an HTTP Content-Usage header (e.g. train-ai=n), and UPDATES RFC 9309 if approved — Standards Track, still an Internet-Draft (rev -05 published 2026-08-19, expires 2027-02-20). Honour it where present; do not cite it as a standard yet"
    kind: standard
    verify: "https://datatracker.ietf.org/doc/draft-ietf-aipref-attach/ — still a draft, or published as an RFC? (then reword the pack: 'draft' → the RFC number)"
    window_days: 90
    last_verified: 2026-09-02

  - id: scraping-bypass-tooling-liability-v2
    pack: core/57-external-data-sourcing.md
    claim: "supersedes scraping-bypass-tooling-liability: the anti-circumvention (DMCA §1201) theory against scraping TOOLING now has a first ruling — Google v. SerpApi was dismissed with leave to amend (N.D. Cal., July 2026): the court accepted that browser-fingerprint spoofing, IP rotation and CAPTCHA-solving to defeat SearchGuard ARE 'circumvention', and dismissed only because a §1201 barrier must guard works the copyright holder authorised it to guard. 20+ §1201 scraping suits were on file by 2026-05 (Reddit v. Perplexity + data-scraping firms, 2025-10-22, among them). UNSETTLED in the defendants' favour on the narrow point, settled AGAINST them on what counts as circumvention"
    kind: rationale
    verify: "search: Google v SerpApi amended complaint DMCA 1201 ruling <year>; Reddit v Perplexity ruling — any appellate or final decision on tooling-layer liability?"
    window_days: 90
    last_verified: 2026-09-02

  - id: eu-ai-act-art53-tdm-reservation
    pack: core/57-external-data-sourcing.md
    claim: "EU AI Act Art. 53(1)(c) obliges providers of general-purpose AI models to identify and comply with Art. 4(3) DSM-Directive rights reservations 'including through state-of-the-art technologies' — applicable from 2025-08-02 (models released earlier: until 2027-08-02). The GPAI Code of Practice (Copyright chapter, Measure 1.3) commits signatories to robots.txt (RFC 9309) PLUS 'other appropriate machine-readable protocols' — the EC/EUIPO process to publish an agreed list (TDMRep, ai.txt, HTTP headers, C2PA asset-level are the candidates) is running, list reviewed at least every two years. Hamburg OLG (2025-12-10): a natural-language opt-out in terms of use is insufficient — reservations must be machine-readable. Whether robots.txt ALONE is a valid Art. 4(3) reservation is still contested"
    kind: standard
    verify: "https://digital-strategy.ec.europa.eu/en/faqs/stakeholder-consultation-ai-and-copyright-compliance — has the agreed opt-out protocol list been published? + search: Hamburg OLG TDM opt-out machine-readable ruling"
    window_days: 180
    last_verified: 2026-09-02

  - id: http-deprecation-sunset-headers
    pack: core/57-external-data-sourcing.md
    claim: "Two standards let a CONSUMER detect an API's retirement at runtime: RFC 9745 (Deprecation response header, Standards Track, published 2025-03; an Item Structured Field whose value MUST be a Date, e.g. `Deprecation: @1688169599`, plus a `deprecation` link relation) and RFC 8594 (Sunset header, the date the resource becomes unresponsive; `sunset` link relation). The Sunset timestamp MUST NOT be earlier than the Deprecation one. Both are hints, not guarantees — a client must still work without them"
    kind: standard
    verify: "https://www.rfc-editor.org/rfc/rfc9745.html + https://www.rfc-editor.org/rfc/rfc8594.html — still current, not obsoleted?"
    window_days: 365
    last_verified: 2026-09-02

  - id: statuspage-v2-status-api
    pack: core/57-external-data-sourcing.md
    claim: "The Statuspage convention exposes a public read-only status API at <page>/api/v2/ — status.json (indicator none/minor/major/critical), summary.json, components.json, incidents/unresolved.json, scheduled-maintenances/{upcoming,active}.json — widely copied by vendors' own status pages, which makes 'poll the vendor's status API before deciding a 5xx burst is yours' implementable for most SaaS dependencies. Private pages need an API key header"
    kind: practice
    verify: "https://support.atlassian.com/statuspage/docs/what-are-the-different-apis-under-statuspage/ — endpoints unchanged?"
    window_days: 365
    last_verified: 2026-09-02

  - id: credential-rotation-dual-key
    pack: core/57-external-data-sourcing.md
    claim: "Zero-downtime credential rotation is the dual-key OVERLAP pattern — new key issued, both valid for a grace window sized to the slowest consumer, old key revoked only after telemetry shows migration — backed by a vault, on a schedule shorter than expiry. Some vendors support only SINGLE-PHASE rotation (one active credential; the old one dies the instant the new one is issued), which forces a brief outage or a maintenance window — the profile must say which the vendor is"
    kind: practice
    verify: "search: API key rotation dual key overlap zero downtime <year>; is single-phase-only still a common vendor limitation?"
    window_days: 365
    last_verified: 2026-09-02

  - id: vendor-schema-drift-boundary-validation
    pack: core/57-external-data-sourcing.md
    claim: "A third-party API's response shape changes WITHOUT a version bump or notice (nullable flips, type changes, renamed/removed fields, restructured nesting) and the response still returns 200 with valid JSON — outages page you, drift corrupts silently. The practiced defence is strict validation at the integration boundary (pydantic strict=True / zod .strict(), unknown keys rejected deliberately), never silently dropping invalid records, and exporting the invalid-record RATE as a metric — a spike is the upstream change; a 'missing' cluster on one field names it"
    kind: practice
    verify: "search: third-party API schema drift detection boundary validation <year>"
    window_days: 365
    last_verified: 2026-09-02

  - id: paddle-rate-limit-240pm
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Paddle Billing API: 240 req/min per IP (1,000/min for preview endpoints); over the cap → 429 too_many_requests + Retry-After and the IP is locked out 60s; per-subscription immediate-charge caps 20/h and 100/24h; webhooks must be acked 200 within 5s; live webhook retries 60 over 3 days, replay via POST /notifications/{id}/replay (90-day window); API versions are not deprecated today"
    kind: standard
    verify: "https://developer.paddle.com/api-reference/about/rate-limiting + /webhooks/respond-to-webhooks + /api-reference/about/versioning"
    window_days: 180
    last_verified: 2026-09-02

  - id: resend-rate-limit-10rps
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Resend: 10 req/s per team (raisable); Free = 100 emails/day + 3,000/month with inbound counted; over-quota → 429 daily_quota_exceeded / monthly_quota_exceeded (hard block on Free, $0.90/1k overage on Pro); Idempotency-Key on POST /emails (24h); webhooks are Svix-signed, at-least-once, retried at 5s/5m/30m/2h/5h/10h, unordered"
    kind: standard
    verify: "https://resend.com/docs/api-reference/rate-limit + /docs/api-reference/errors + /docs/dashboard/webhooks/introduction"
    window_days: 180
    last_verified: 2026-09-02

  - id: cloudflare-api-1200-per-5min
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Cloudflare API: 1,200 requests per 5 minutes per user (all methods) → 429 and a 5-minute block; Ratelimit/Ratelimit-Policy/retry-after headers; DNS records per zone Free 200 (zones created ≥2024-09-01) / Pro+ 3,500; API token 'Roll' is a hard cut, Turnstile secret rotation keeps the old secret 2h; status = Statuspage /api/v2/status.json"
    kind: standard
    verify: "https://developers.cloudflare.com/fundamentals/api/reference/limits/ + https://www.cloudflarestatus.com/api/v2/status.json"
    window_days: 180
    last_verified: 2026-09-02

  - id: cloudflare-r2-limits
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "R2: REST API 1,200 req/5 min; 1 write/s per object key (429 above); multipart parts 5 MiB–5 GiB up to 10,000, incomplete uploads expire after 7 days; strongly consistent reads/lists; pricing $0.015/GB-mo, Class A $4.50/M, Class B $0.36/M, egress free; usage via GraphQL r2OperationsAdaptiveGroups; event notifications go to Queues, not webhooks"
    kind: standard
    verify: "https://developers.cloudflare.com/r2/platform/limits/ + /r2/pricing/ + /r2/reference/consistency/"
    window_days: 180
    last_verified: 2026-09-02

  - id: backblaze-b2-api-v4-lifecycle
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Backblaze B2 native API is at /b2api/v4/ and 'does not have any plans to stop supporting old versions' (≥1 year notice promised); 429 is per-account with no published rpm and no Retry-After; auth tokens expire after 24h (expired_auth_token → re-authorize); app keys may carry validDurationSeconds; Class A/B/C calls are free, only Class D (event notifications) is metered after 2,500/day; $6.95/TB-mo, egress free to 3× storage"
    kind: standard
    verify: "https://www.backblaze.com/docs/cloud-storage-native-api-versions + /apidocs/introduction-to-the-b2-native-api + /cloud-storage/pricing"
    window_days: 180
    last_verified: 2026-09-02

  - id: openrouter-limits-and-credits
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "OpenRouter: free models 20 req/min and 50 req/day (<$10 lifetime credits) or 1,000/day (≥$10); paid models have no published rpm ('capacity governed globally' — extra keys/accounts buy nothing); 429 carries X-RateLimit-* + Retry-After when the provider gave one; 402 when balance ≤0 or key limit exhausted; GET /api/v1/key and /api/v1/credits expose usage; 5.5% ($0.80 min) card fee on credit purchases; status.openrouter.ai is NOT Statuspage-shaped (/api/v2/status.json 404)"
    kind: standard
    verify: "https://openrouter.ai/docs/api-reference/limits (WebFetch — exa returns unrendered placeholders) + /docs/api-reference/errors + /docs/faq"
    window_days: 90
    last_verified: 2026-09-02

  - id: revenuecat-v2-rate-limits
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "RevenueCat API v2 per-minute limits by domain (Customer 480, Virtual Currencies 480, Project Config 60, Audiences 60, Charts 25); 429 body rate_limit_error + backoff_ms + RevenueCat-Rate-Limit-Current-Usage/-Limit + Retry-After; free to $2,500 MTR then 1% of MTR; webhooks retried 5 times (5/10/20/40/80 min), signature X-RevenueCat-Webhook-Signature t=,v1= HMAC-SHA256 over '<t>.<body>'; status = Statuspage"
    kind: standard
    verify: "https://www.revenuecat.com/docs/api-v2 + /docs/integrations/webhooks + https://status.revenuecat.com/api/v2/status.json"
    window_days: 180
    last_verified: 2026-09-02

  - id: iyzico-limits-and-webhook-shape
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "iyzico: per-method per-minute caps (initialize payment 50, retrieve 50, HPP 100, refund 50, cancel 50); over the cap → body {status:failure, errorCode:50000 'Request Limit Exceeded'} with NO documented HTTP code or Retry-After; 'majority of services designed non-idempotent' (no Idempotency-Key — reconcile via conversationId/paymentId + /payment/detail); webhooks retried every 15 min, stop after 3 attempts, X-IYZ-SIGNATURE-V3 has NO timestamp (replay window unbounded) and must be enabled by email; no public status page found"
    kind: standard
    verify: "https://docs.iyzico.com/en/getting-started/preliminaries/limiters + /en/advanced/webhook + /en/getting-started/preliminaries/idempotency"
    window_days: 180
    last_verified: 2026-09-02

  - id: telegram-bot-api-limits
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Telegram Bot API: ≤1 msg/s per chat, ≤20/min per group, ~30/s bulk (Paid Broadcasts up to 1000/s at 0.1 Stars/msg); 429 returns ok:false + parameters.retry_after seconds; webhook max_connections 1–100 (default 40), server-side retries until 2xx then gives up, undelivered updates kept 24h, dedup/order on sequential update_id; bot tokens never expire, /token regenerates with immediate invalidation (single-phase); no official status page"
    kind: standard
    verify: "https://core.telegram.org/bots/faq + https://core.telegram.org/bots/api#responseparameters (page too large for WebFetch — curl | grep)"
    window_days: 180
    last_verified: 2026-09-02

  - id: namecheap-api-limits-and-sethosts
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Namecheap API: 50 calls/min, 700/hour, 8,000/day per key (official FAQ); production access needs ≥20 domains OR ≥$50 balance OR ≥$50 spent in 2 years; IPv4 whitelist mandatory; single key, 'Reset' invalidates the old key immediately (no overlap); setHosts REPLACES the whole record set — omitted records are deleted; no documented over-limit HTTP code; status.namecheap.com did not resolve on 2026-09-02"
    kind: standard
    verify: "https://www.namecheap.com/support/knowledgebase/article.aspx/9739/63/api-faq/ (HEAD → 404, GET blocked for non-browser UAs — open in a browser or via the exa fetcher) + /support/api/methods/domains-dns/set-hosts/"
    window_days: 180
    last_verified: 2026-09-02

  - id: llm-self-review-agreeableness-bias
    pack: core/50-code-review.md
    claim: "LLM evaluators show agreeableness bias — they confirm correct feedback but fail to reject incorrect feedback, and recall is measurably worse when a model reviews its OWN output than another's. This is the evidence base for author-blind review (D-063) and for maker-checker separation"
    kind: rationale
    verify: "search: LLM self-review agreeableness bias recall maker-checker adversarial code review <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: finder-verifier-false-positive-control
    pack: core/50-code-review.md
    claim: "Finder/verifier separation (finders over-generate; independent refute-by-default verifiers kill a large share) is the architectural false-positive control; published single-pass AI review precision is reported as low enough that verifiers refute a large share of finder output (figures vary by tool and are not pinned here). Signal ratio erosion silences correct findings too"
    kind: practice
    verify: "search: AI code review false positive rate signal ratio finder verifier separation <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: review-loop-shape-alternatives
    pack: core/50-code-review.md
    claim: "The published review-then-implement pattern caps automated fixing at ONE pass and escalates to a human instead of looping, explicitly offering no convergence guarantee. (Our converge-to-quiet choice and its dedup/lens obligations are an internal design decision, not part of this external claim.)"
    kind: rationale
    verify: "read: https://agentpatterns.ai/code-review/review-then-implement-loop/ — has the one-pass cap changed? + search: actor-critic review convergence rounds <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: metric-cardinality-silent-undercount
    pack: core/55-observability.md
    claim: "Metric cardinality overflow is SILENT and corrupts answers: totals stay correct while filter/group-by queries undercount, so dashboards, SLOs and alerts keep rendering wrong numbers. A per-request-varying label value is never a metric label"
    kind: practice
    verify: "search: OpenTelemetry metric cardinality limit overflow undercount otel.metric.overflow <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: fleet-has-no-trace-backend
    pack: core/55-observability.md
    claim: "PROBED 2026-09-01 across ALL THREE fleet hosts (vps1/vps2/vps3, 0 matches each): Loki + Prometheus + Grafana (+ the EOL Promtail) and NO trace backend / OTel collector anywhere — so distributed tracing is over-engineering here; a request correlation ID is the correct ceiling. Field naming should still follow OTel convention (trace_id/span_id) to avoid a later fleet-wide rename"
    kind: rationale
    verify: >-
      probe ALL THREE hosts (vps, vps2, vps3) with `sudo docker ps` and grep -icE
      'tempo|jaeger|otel|collector'; also check Grafana datasources for a trace store.
      Has a trace backend appeared on ANY host?
    window_days: 90
    last_verified: 2026-09-01

  - id: llms-txt-null-for-visibility
    pack: core/40-documentation.md
    claim: "llms.txt has NO measured effect on AI search visibility — a ~300k-domain citation study found removing the variable improved model accuracy (noise, not signal), a ~137k-domain server-log study found 97% of files got zero requests, and Google states Search ignores it. It remains a community convention, not a ratified standard"
    kind: practice
    verify: "search: llms.txt adoption evidence server log study AI citation correlation <year> — has any frontier lab or search engine committed to reading it?"
    window_days: 180
    last_verified: 2026-09-01

  - id: llms-txt-coding-agents-are-the-audience
    pack: core/40-documentation.md
    claim: "The one measured non-null llms.txt audience is CODING AGENTS (Claude Code out-fetched every AI retrieval bot/assistant/training crawler in the 137k-domain log study), and Chrome files its llms.txt audit under 'agentic browsing' next to WebMCP — agent tooling, not SEO. Agents fetch it only when DIRECTED; zero AI-bot requests arrive for files that do not exist, so an unlinked file is unread"
    kind: practice
    verify: "search: which bots fetch llms.txt coding agents Claude Code server logs <year> + check Chrome Lighthouse audit category"
    window_days: 180
    last_verified: 2026-09-01

  - id: docusaurus-serve-not-for-production
    pack: core/42-docusaurus.md
    claim: "Docusaurus's own deployment docs state `docusaurus serve` is not the best option compared to a static host/CDN, and that Docusaurus's only responsibility is to emit static files in build/ — this is the upstream basis for the pack's ban on a Node runtime in a production container"
    kind: practice
    verify: "read https://docusaurus.io/docs/deployment — does it still steer away from `docusaurus serve` for production?"
    window_days: 365
    last_verified: 2026-09-01

  - id: pagefind-plugin-maintenance-risk
    pack: core/42-docusaurus.md
    claim: "⚠️ RE-VERIFY MUST REWRITE THIS TEXT, not just bump the date — the age below decays. `@getcanary/docusaurus-theme-search-pagefind` is functional and in production use (e.g. Redux Toolkit) but as of 2026-09-01 its newest release was ~23 months old (19 versions, last Oct 2024) and its peer range declares @docusaurus/core ^2||^3 and react ^17||^18 — so Pagefind is the mandate and this plugin is not; verify resolution before adopting"
    kind: version
    verify: "check npm for BOTH: @getcanary/docusaurus-theme-search-pagefind (any newer release? does its peer range cover the site's majors?) AND the alternative docusaurus-plugin-pagefind (still maintained? peers still current?). As of 2026-09-01: getcanary 1.0.2 (19 versions, last 2024-10-12, peers @docusaurus/core ^2||^3 + react ^17||^18); docusaurus-plugin-pagefind 0.2.2 (2026-07-24, peers @docusaurus/core >=3 + react >=18). The pack deliberately carries NO numbers — they live here."
    window_days: 180
    last_verified: 2026-09-01

  - id: pagefind-scale-ceiling
    pack: core/42-docusaurus.md
    claim: "Pagefind's OWN stated envelope is 'tens of thousands of pages', with a ~300kB total network payload on a 10,000-page site; past that the index fetch cost is what tells and a hosted engine becomes the better tool. (An earlier draft of this row cited ~25k pages as Pagefind's figure — that number is third-party, not the vendor's.) Algolia is Docusaurus's own first-class option — our ban is a constraint (external SaaS; free DocSearch requires a public site), not a quality judgement"
    kind: rationale
    verify: "search: Pagefind index size scaling limit pages vs hosted search <year> + check Docusaurus docs/search for its recommended options"
    window_days: 365
    last_verified: 2026-09-01

  - id: watchdog-tier-d-window-is-300-fleet-wide
    pack: core/60-watchdog.md
    claim: "The Tier-D approval window IS plumbed end-to-end: spec_loader.py WatchdogConfig.code_fix_window_sec (default 300, ge=60 le=3600) -> drivers/watchdog.py falls back to 300 and exports WATCHDOG_APPROVAL_WINDOW_SEC -> the module's control_plane/approval.py reads it (_DEFAULT_WINDOW = 300). Commit 2c5b56e1 raised the fleet default 300->1800; D-378 (2026-09-23) set it back to 300, so the hub and module defaults agree. Explicit spec values override it"
    kind: rationale
    verify: "trace the whole chain, not one repo: grep code_fix_window_sec in /opt/fabrik/src/fabrik/spec_loader.py AND drivers/watchdog.py, then grep WATCHDOG_APPROVAL_WINDOW_SEC in /opt/fabrik-lib/watchdog. ⚠️ The module never spells the SPEC field name — searching fabrik-lib for `code_fix_window_sec` alone returns zero and falsely reads as 'not implemented'"
    window_days: 180
    last_verified: 2026-09-24

  - id: watchdog-guard-bounds-verified
    pack: core/60-watchdog.md
    claim: "VERIFIED IN SOURCE 2026-09-01: the pack's numeric guards match watchdog_sidecar/actions.py — _FORBIDDEN_TARGETS is exactly 14 entries (postgres-main…site-provisioner), pause TTL bounds [5,3600], drop_queue_items row cap 1..10000, rotate_locks age [30,86400], and the 100_000-key wipe cap"
    kind: rationale
    verify: "re-read /opt/fabrik-lib/watchdog/watchdog_sidecar/actions.py — do the bounds and the forbidden-target count still match the pack's table?"
    window_days: 180
    last_verified: 2026-09-01

  - id: watchdog-db-writes-are-tier-c-gated
    pack: core/60-watchdog.md
    claim: "VERIFIED IN SOURCE 2026-09-01: drop_queue_items and rotate_locks are NOT Tier A — actions.py::TIER_C_WRITE_HANDLERS routes them through the approved-write lane, fail-closed behind WATCHDOG_ALLOW_DB_WRITES (default OFF) and a provisioned WATCHDOG_DB_URL_RW; absent either the handler returns 'skipped'"
    kind: rationale
    verify: "grep -n TIER_C_WRITE_HANDLERS -A6 /opt/fabrik-lib/watchdog/watchdog_sidecar/actions.py — are both still in the Tier-C lane and still double-gated?"
    window_days: 180
    last_verified: 2026-09-01

  - id: watchdog-cost-cap-not-flag-enforced
    pack: core/60-watchdog.md
    claim: "per_incident_budget_usd is NOT enforced by a --max-budget-usd flag: llm_client.py explicitly bans that flag per the operator directive that per-call $ caps break the diagnose loop, and per_incident_cap_usd is accepted for API compatibility without call-site enforcement. Spend is observed via total_cost_usd and bounded by DAILY caps. OpenRouter fallback timeout is 60s (the pack previously said 45s)"
    kind: rationale
    verify: "grep -n 'max-budget-usd\\|_OPENROUTER_TIMEOUT\\|per_incident_cap_usd' /opt/fabrik-lib/watchdog/watchdog_sidecar/llm_client.py"
    window_days: 180
    last_verified: 2026-09-01

  - id: autonomous-remediation-silence-semantics
    pack: core/60-watchdog.md
    claim: "Published autonomous-remediation designs predominantly use SILENCE = DENIAL (an approval request that times out resolves to rejection and never converts into consent); our Tier D uses the inverse (TIMEOUT routes to APPLY). The inversion is defensible for an unattended fleet but is only safe while the hard test gate, post-apply health verification with auto-rollback, and the STOP kill-switch all hold"
    kind: practice
    verify: "search (>=2 engines): autonomous remediation approval timeout semantics silence denial vs consent <year> — has the prevailing convention moved?"
    window_days: 180
    last_verified: 2026-09-01

  - id: watchdog-enabled-has-no-kind-defaulting
    pack: core/60-watchdog.md
    claim: "VERIFIED 2026-09-01: there is NO shape.kind watchdog defaulting anywhere, and `_register_watchdog` (cited 3x in spec_loader.py :414/:417/:812) DOES NOT EXIST. ⚠️ The enablement DEFAULT DIFFERS BY PATH: WatchdogConfig.enabled defaults FALSE (spec_loader.py:428) but the apply path reads raw YAML and defaults TRUE (infrastructure.py:344), so fabrik apply provisions a sidecar where fabrik plan/audit/destroy (model_dump) see enabled:false. The in-code comment at infrastructure.py:337 claims the class defaults True and is wrong"
    kind: rationale
    verify: "READ THE CLASS, not the comment: sed -n 428,430p src/fabrik/spec_loader.py (Field default) AND grep -n 'get(\"enabled\"' src/fabrik/orchestrator/infrastructure.py (raw-dict fallback) — do the two paths still disagree? Plus grep -rn 'def _register_watchdog' src/ (expect nothing)"
    window_days: 180
    last_verified: 2026-09-01

  - id: scalar-docusaurus-client-side-render
    pack: core/42-docusaurus.md
    claim: "@scalar/docusaurus renders an OpenAPI spec client-side at runtime (no build-time .mdx generation), which is why the pack prefers it over docusaurus-plugin-openapi-docs / Redocusaurus — those materialise many physical .mdx files at build, inflating commits and build duration"
    kind: practice
    verify: "check npm @scalar/docusaurus is alive + its docs: does it still render dynamically rather than generating files? And do the banned plugins still generate per-endpoint .mdx?"
    window_days: 180
    last_verified: 2026-09-02

  - id: posthog-ingest-never-429s-drops-over-quota
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "PostHog: public ingest has no request rate limit and never 429s — over a billing limit it returns 200 with quota_limited[] and DROPS the data; private endpoints 240/min + 1,200/h per team (shared bucket across personal keys) with 429 + Retry-After; dedup is eventual on event uuid"
    kind: standard
    verify: "https://posthog.com/docs/api + /docs/billing/limits-alerts + /docs/data/events"
    window_days: 180
    last_verified: 2026-09-02

  - id: axiom-three-limit-headers
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Axiom: every response carries X-RateLimit-*, X-QueryLimit-* (GB·ms) and X-IngestLimit-* (bytes/month); 429 {message: rate limit exceeded}; ingest is per-batch partial-accept (inspect `failed`, not the status); Personal plan 500 GB/mo hard max, 30-day retention"
    kind: standard
    verify: "https://axiom.co/docs/restapi/api-limits + /docs/reference/limits + /docs/restapi/endpoints/ingestToDataset"
    window_days: 180
    last_verified: 2026-09-02

  - id: slack-1rps-and-events-retry-3x
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Slack: posting (chat.postMessage / incoming webhook) is 1 per second per channel with short bursts; 429 + Retry-After on every HTTP API incl. webhooks; Events API must be 2xx-acked within 3 s, retried 3x (immediate / 1 min / 5 min) with x-slack-retry-num, >95% failures in 60 min disables the subscription; signature v0=HMAC-SHA256 over v0:ts:body with a 5-minute skew window"
    kind: standard
    verify: "https://docs.slack.dev/apis/web-api/rate-limits + /apis/events-api + /authentication/verifying-requests-from-slack"
    window_days: 180
    last_verified: 2026-09-02

  - id: vercel-per-endpoint-429-with-limit-block
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Vercel: rate limits are per endpoint; 429 body error.code=rate_limited with limit{remaining,reset,resetMs,total}; Hobby 100 deployments/day, deploy hooks 60/h per project, no overage on Hobby (feature pauses up to 30 days); webhooks x-vercel-signature = HMAC-SHA1 of raw body, retried with backoff up to 24 h, 2XX within 30 s"
    kind: standard
    verify: "https://vercel.com/docs/rest-api/errors + /docs/limits + /docs/webhooks"
    window_days: 180
    last_verified: 2026-09-02

  - id: cerebras-prepaid-hard-stop-and-versioned-header
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Cerebras: prepaid credits with a hard STOP at zero (no overage); org-level dual token buckets (uncached TPM + total TPM), 429 names the exhausted bucket; keys do not expire; breaking API changes ship as versions selected by X-Cerebras-Version-Patch with ≥6 months notice; per-model pricing/limits/deprecated readable unauthenticated at GET https://api.cerebras.ai/public/v1/models (docs page and endpoint DISAGREED on gemma-4-31b pricing 2026-09-02 — read the endpoint)"
    kind: standard
    verify: "https://inference-docs.cerebras.ai/support/rate-limits + /api-reference/versions + https://api.cerebras.ai/public/v1/models"
    window_days: 120
    last_verified: 2026-09-02

  - id: linkedin-scrape-tos-prohibited-999
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "LinkedIn: robots.txt header prohibits automated access without express permission; User Agreement §8.2 bans scraping/bots; hiQ v. LinkedIn ended 2022-12-08 in a consent judgment + permanent injunction against hiQ (contract claim won, not CFAA) — scraping is ToS-enforced; bot detection answers HTTP 999 / authwall with no Retry-After; official API = daily app+member quotas, 60-day access tokens, Linkedin-Version YYYYMM header with ≥1-year support"
    kind: standard
    verify: "https://www.linkedin.com/robots.txt + https://www.linkedin.com/legal/user-agreement + https://learn.microsoft.com/en-us/linkedin/shared/api-guide/concepts/rate-limits + /linkedin/marketing/versioning"
    window_days: 180
    last_verified: 2026-09-02

  - id: bls-api-v2-500-per-day-200-on-exhaustion
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "BLS Public Data API: v2 (registered) 500 queries/day, 50 series and 20 years per query; v1 25/day, 25 series, 10 years; 50 requests/10 s rate cap → 429; daily exhaustion is a HTTP 200 with status REQUEST_NOT_PROCESSED (enforce the budget client-side); registration keys must be renewed at least yearly; one-day lag between publication and API availability"
    kind: standard
    verify: "https://www.bls.gov/developers/api_faqs.htm + /developers/api_signature_v2.htm + /bls/api_features.htm (WebFetch is 403'd by the federal WAF — use exa or curl)"
    window_days: 365
    last_verified: 2026-09-02

  - id: gmail-api-quota-units-and-refresh-token-death
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Gmail API: 1,200,000 quota units/min per project, 6,000/min per user, messages.send = 100 units; 80M units/day billing threshold from 2026 (overage to be charged, ≥90 days notice); a per-user CONCURRENT request cap exists but is unpublished; 429 'User-rate limit exceeded (Mail sending)' can lag minutes and a 200 does not prove delivery; refresh tokens die on 6-month idle, password change with Gmail scopes, >100 tokens per client, or 7 days for Testing-status apps; watch() expires ≤7 days (renew daily), Pub/Sub is at-least-once unordered"
    kind: standard
    verify: "https://developers.google.com/workspace/gmail/api/reference/quota + /guides/handle-errors + /guides/push + https://developers.google.com/identity/protocols/oauth2"
    window_days: 120
    last_verified: 2026-09-02

  - id: ses-throttle-drops-and-sns-at-least-once
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Amazon SES: sandbox 200 recipients/24 h + 1/s per Region; over quota SES DROPS the message (ThrottlingException / SMTP 454, retry after up to 10 min); no idempotency key on SendEmail; feedback via SNS is at-least-once and unordered (dedup on feedbackId), HTTP retries default 3 @ 20 s within a 3,600 s cap; SMTP password is derived from the IAM secret so rotating the IAM key rotates SMTP"
    kind: standard
    verify: "https://docs.aws.amazon.com/ses/latest/dg/quotas.html + /dg/manage-sending-quotas-errors.html + /dg/smtp-credentials.html + https://docs.aws.amazon.com/sns/latest/dg/sns-message-delivery-retries.html"
    window_days: 180
    last_verified: 2026-09-02

  - id: msgraph-outlook-per-mailbox-limits-and-webhook-lifecycle
    pack: docs/reference/apis/EXTERNAL_SYSTEMS.md
    claim: "Microsoft Graph (Outlook): 10,000 requests per 10 min and 4 concurrent per app+mailbox, 150 MB upload per 5 min; global 130,000 req/10 s per app; 429 + Retry-After (batched sub-requests 429 individually, SDKs do not auto-retry them); client secrets ≤24 months (certs recommended), app-only flow returns no refresh token; GA APIs get ≥24 months deprecation notice; change-notification subscriptions on mail expire <7 days (rich: 1,440 min), 2xx within 3 s, retried up to 4 h, `missed`/`subscriptionRemoved` lifecycle events → delta resync"
    kind: standard
    verify: "https://learn.microsoft.com/en-us/graph/throttling-limits + /graph/throttling + /graph/change-notifications-delivery-webhooks + /entra/identity-platform/how-to-add-credentials"
    window_days: 180
    last_verified: 2026-09-02

  # --- core/75-workers-jobs.md — file 16 of the currency pass, 2026-09-22 ---
  - id: py314-forkserver-default
    pack: core/75-workers-jobs.md
    claim: "Since Python 3.14 multiprocessing's default start method on POSIX is forkserver; fork is no longer the default on any platform and must be requested via get_context('fork'); os.fork() warns in a multithreaded process since 3.12"
    kind: deprecation
    verify: "https://docs.python.org/3/library/multiprocessing.html (Changed in version 3.14 note; gh-84559)"
    window_days: 180
    last_verified: 2026-09-22

  - id: psycopg3-for-new-projects
    pack: core/75-workers-jobs.md
    claim: "New projects should use Psycopg 3 (package `psycopg`); psycopg2 is maintained for legacy applications and receives no new features"
    kind: deprecation
    verify: "https://www.psycopg.org/ ; https://pypi.org/project/psycopg/"
    window_days: 365
    last_verified: 2026-09-22

  - id: pgbouncer-txn-listen-never
    pack: core/75-workers-jobs.md
    claim: "PgBouncer transaction pooling: LISTEN is Never supported, NOTIFY is Yes — a listener needs a session-mode or direct connection"
    kind: rationale
    verify: "https://www.pgbouncer.org/features.html (feature-support table)"
    window_days: 365
    last_verified: 2026-09-22

  - id: compose-init-reaps
    pack: core/75-workers-jobs.md
    claim: "Compose `init: true` runs a platform-bundled init as PID 1 that forwards signals and reaps processes — equivalent to tini as ENTRYPOINT"
    kind: rationale
    verify: "https://docs.docker.com/reference/compose-file/services/#init"
    window_days: 365
    last_verified: 2026-09-22

  - id: pgqueuer-maintained
    pack: core/75-workers-jobs.md
    claim: "PgQueuer is maintained — 1.4.0 released 2026-09-14, requires Python >=3.10"
    kind: version
    verify: "https://pypi.org/project/pgqueuer/"
    window_days: 180
    last_verified: 2026-09-22

  - id: procrastinate-maintained
    pack: core/75-workers-jobs.md
    claim: "Procrastinate is maintained — 3.9.0 released 2026-06-20, requires Python >=3.10"
    kind: version
    verify: "https://pypi.org/project/procrastinate/"
    window_days: 180
    last_verified: 2026-09-22

  - id: cgroup-v2-cpu-max-format
    pack: core/75-workers-jobs.md
    claim: "cgroup v2 cpu.max reads '$MAX $PERIOD' with 'max' meaning no limit; memory.max defaults to 'max'"
    kind: standard
    verify: "https://docs.kernel.org/admin-guide/cgroup-v2.html (cpu.max, memory.max)"
    window_days: 365
    last_verified: 2026-09-22

  - id: asyncpg-add-listener
    pack: core/75-workers-jobs.md
    claim: "asyncpg Connection.add_listener(channel, callback) is the LISTEN API; the callback receives (connection, pid, channel, payload)"
    kind: standard
    verify: "https://magicstack.github.io/asyncpg/current/api/index.html (add_listener)"
    window_days: 365
    last_verified: 2026-09-22

  - id: os-process-cpu-count
    pack: core/75-workers-jobs.md
    claim: "os.process_cpu_count() (Python 3.13+) returns the logical CPUs usable by the calling thread — affinity/cgroup-aware — and is what concurrent.futures now sizes by; multiprocessing.cpu_count() reports the host"
    kind: standard
    verify: "https://docs.python.org/3/whatsnew/3.13.html (os: process_cpu_count, gh-109649)"
    window_days: 365
    last_verified: 2026-09-22

  - id: gpu-tgi-archived
    pack: core/76-gpu-workers.md
    claim: "HF text-generation-inference entered maintenance mode 2025-12-11 and the repo was archived 2026-03-21; Inference Endpoints docs recommend vLLM or SGLang (engine chosen per endpoint, no default)"
    kind: deprecation
    verify: "https://github.com/huggingface/text-generation-inference (archive banner); https://huggingface.co/docs/inference-endpoints/en/engines/tgi"
    window_days: 365
    last_verified: 2026-09-22

  - id: gpu-trtllm-pytorch-only
    pack: core/76-gpu-workers.md
    claim: "TensorRT-LLM >= 1.0: PyTorch is the sole execution backend; the TensorRT engine backend, trtllm-build and convert_checkpoint are removed — no compile step; published H100 runs put it single-digit to ~30% over vLLM, config-dependent"
    kind: deprecation
    verify: "https://nvidia.github.io/TensorRT-LLM/legacy/tensorrt-backend-removal.html ; https://github.com/NVIDIA/TensorRT-LLM/releases/tag/v1.0.0"
    window_days: 180
    last_verified: 2026-09-22

  - id: gpu-dcp-async-save
    pack: core/76-gpu-workers.md
    claim: "torch.distributed.checkpoint: dcp.save is synchronous; dcp.async_save returns a Future and overlaps the write; fully background staging (DefaultStager / async_stager) is new in torch 2.9"
    kind: practice
    verify: "https://docs.pytorch.org/tutorials/recipes/distributed_async_checkpoint_recipe.html"
    window_days: 365
    last_verified: 2026-09-22

  - id: gpu-expandable-segments
    pack: core/76-gpu-workers.md
    claim: "PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True reduces fragmentation via growable per-stream segments; it is off by default, documented experimental, and ignored under backend:cudaMallocAsync"
    kind: rationale
    verify: "https://docs.pytorch.org/docs/stable/notes/cuda.html (Memory management / environment variables)"
    window_days: 365
    last_verified: 2026-09-22

  - id: gpu-nvfp4-blackwell
    pack: core/76-gpu-workers.md
    claim: "NVFP4 is hardware-native on Blackwell (B200/GB200/B300): within ~1% of FP8 accuracy on large models, ~1.8x smaller than FP8, up to ~3x FP8 peak throughput; FP4-ready checkpoints published under the nvidia/ HF namespace"
    kind: practice
    verify: "https://developer.nvidia.com/blog/introducing-nvfp4-for-efficient-and-accurate-low-precision-inference/ ; https://developer.nvidia.com/blog/3-ways-nvfp4-accelerates-ai-training-and-inference/"
    window_days: 180
    last_verified: 2026-09-22

  - id: gpu-moe-quantizes-proportionally
    pack: core/76-gpu-workers.md
    claim: "Quantizing MoE expert weights cuts VRAM proportionally like dense models (Mixtral 8x7B ~94GB FP16 -> ~32GB AWQ; DeepSeek-V3 ~1.4TB BF16 -> ~700GB FP8); the MoE caveat is that ALL experts must be resident, not that quantization saves little"
    kind: rationale
    verify: "search: mixtral 8x7b vram awq gptq; deepseek v3 fp8 vram — any two independent sizing pages"
    window_days: 365
    last_verified: 2026-09-22

  - id: gpu-vllm-cold-start-measured
    pack: core/76-gpu-workers.md
    claim: "vLLM weight load into GPU memory is ~30s for a 7-8B model and minutes for 70B (excluding image pull); sub-10s starts need snapshots / NVMe caches, never a baseline"
    kind: rationale
    verify: "https://dudeperf3ct.github.io/posts/vllm_startup_load_time/ ; https://tensorfuse.io/docs/blogs/reducing_gpu_cold_start"
    window_days: 180
    last_verified: 2026-09-22

  - id: gpu-groq-self-serve-catalog
    pack: core/76-gpu-workers.md
    claim: "Groq self-serve chat catalog is openai/gpt-oss-120b ($0.15/$0.60 per 1M) and openai/gpt-oss-20b ($0.075/$0.30); llama-3.3-70b-versatile and llama-3.1-8b-instant left the free and developer tiers 2026-08-16 (enterprise-only); groq.com/pricing no longer publishes a table"
    kind: version
    verify: "https://console.groq.com/docs/deprecations ; https://console.groq.com/docs/models"
    window_days: 90
    last_verified: 2026-09-22

  - id: gpu-together-prices
    pack: core/76-gpu-workers.md
    claim: "Together AI: meta-llama/Llama-4-Maverick-17B-128E-Instruct-FP8 (the exact id) $0.27/$0.85 per 1M; meta-llama/Llama-3.3-70B-Instruct-Turbo $1.04 flat (was $0.88)"
    kind: version
    verify: "https://www.together.ai/models/llama-4-maverick ; https://www.together.ai/models/llama-3-3-70b"
    window_days: 90
    last_verified: 2026-09-22

  - id: gpu-70b-api-rate-spread
    pack: core/76-gpu-workers.md
    claim: "A 70B-class open-weight model costs $0.10/$0.32 (DeepInfra Llama-3.3-70B-Instruct-Turbo) to $0.90 flat (Fireworks) to $1.04 flat (Together) per 1M — a 10x provider spread, so the self-host break-even is provider-specific"
    kind: version
    verify: "https://deepinfra.com/meta-llama/Llama-3.3-70B-Instruct-Turbo/api ; https://fireworks.ai/pricing ; https://www.together.ai/models/llama-3-3-70b"
    window_days: 90
    last_verified: 2026-09-22

  - id: gpu-openai-compat-base-urls
    pack: core/76-gpu-workers.md
    claim: "OpenAI-compatible base URLs: Groq https://api.groq.com/openai/v1 ; Together https://api.together.ai/v1 ; Fireworks https://api.fireworks.ai/inference/v1 (no trailing slash, accounts/fireworks/models/… ids) ; RunPod serverless vLLM https://api.runpod.ai/v2/<endpoint-id>/openai/v1"
    kind: practice
    verify: "https://console.groq.com/docs/openai ; https://docs.fireworks.ai/tools-sdks/openai-compatibility ; https://docs.runpod.io/serverless/vllm/openai-compatibility"
    window_days: 365
    last_verified: 2026-09-22

  - id: gpu-runpod-prices
    pack: core/76-gpu-workers.md
    claim: "RunPod H100: serverless $4.79/hr flex, $9.98/hr active; Secure Cloud pods $2.89-3.49/hr by SKU (PCIe/SXM/NVL); Community ~$2.69/hr; FlashBoot (~0.5s best, 95% < 2.3s) free on every endpoint; $0 egress"
    kind: version
    verify: "https://www.runpod.io/pricing ; https://www.runpod.io/blog/introducing-flashboot-serverless-cold-start"
    window_days: 90
    last_verified: 2026-09-22

  - id: gpu-modal-prices
    pack: core/76-gpu-workers.md
    claim: "Modal: H100 SXM5 $0.001097/s (~$3.95/hr), B200 ~$6.25/hr, H200 ~$4.54/hr; CPU $0.0000131/core-s and memory $0.00000222/GiB-s billed separately; non-preemptible = 3x base price; region multiplier 1.15-1.75x; cold-start levers are Memory Snapshots, min_containers, scaledown_window"
    kind: version
    verify: "https://modal.com/pricing ; https://modal.com/docs/guide/cold-start"
    window_days: 90
    last_verified: 2026-09-22

  - id: gpu-marketplace-h100-rates
    pack: core/76-gpu-workers.md
    claim: "H100 $/hr on the marketplace tier: Vast.ai $1.30-2.50 verified hosts, interruptible spot ~50% lower (egress per byte host-set, no published range; hosts scored on uptime/DLPerf, no VRAM guarantee); TensorDock $1.91 spot / $2.25 on-demand; Spheron ~$2.0-2.1 spot SXM5 / ~$2.5-3.4 on-demand SXM5, PCIe on-demand from $2.01; Thunder Compute $3.20 PCIe (A100 80GB $1.09), $0 egress"
    kind: version
    verify: "https://docs.vast.ai/guides/instances/pricing ; https://www.tensordock.com/gpu-h100.html ; https://www.spheron.network/pricing/ ; https://www.thundercompute.com/pricing"
    window_days: 90
    last_verified: 2026-09-22

  - id: gpu-hardware-market-rates
    pack: core/76-gpu-workers.md
    claim: "Market $/hr ranges: B200 $3.75-6.79, H200 $2.09-4.44, H100 SXM $1.30-3.49, A100 80GB $0.40-1.85, L40S $0.55-1.47, RTX 4090 $0.33-0.49, L4 $0.31-0.90, T4 $0.15-0.64; RTX PRO 6000 Blackwell (96GB GDDR7 ECC, 1.79 TB/s, from ~$2.26) and RTX 5090 (32GB GDDR7, ~1.8 TB/s, ~$0.55-0.67) are now common rentals"
    kind: version
    verify: "https://getdeploying.com/gpus ; https://getdeploying.com/gpus/nvidia-h100"
    window_days: 90
    last_verified: 2026-09-22

  - id: gpu-roce-alternative
    pack: core/76-gpu-workers.md
    claim: "Multi-node training needs an RDMA fabric: InfiniBand is the default, RoCEv2 on 400GbE+ with a lossless (PFC/ECN) config is proven at scale (Meta's 24k-GPU Llama 3 RoCE cluster); 1-10 Gbps Ethernet is unusable above ~2 nodes"
    kind: practice
    verify: "https://www.runpod.io/blog/roce-vs-infiniband-multi-node-gpu-training ; search: llama 3 roce cluster 24k"
    window_days: 365
    last_verified: 2026-09-22

  - id: gpu-salad-consumer-only
    pack: core/76-gpu-workers.md
    claim: "Salad Cloud offers consumer RTX nodes plus an RTX PRO 6000 Blackwell dedicated tier — no H100/A100; nodes preempt (~30 min minimum runs) and no churn percentage is published; $0 egress"
    kind: rationale
    verify: "https://www.salad.com/pricing ; https://docs.salad.com"
    window_days: 180
    last_verified: 2026-09-22

  - id: gpu-moe-hardware-minimums
    pack: core/76-gpu-workers.md
    claim: "Frontier open-weight MoE minimum hardware: Llama 4 Maverick (400B total) ~4x H100 at INT4 / 8x H200 FP16; DeepSeek-V3/R1 (671B) ~8x H200 at FP8; gpt-oss-120b fits ONE 80GB GPU because its experts are MXFP4 by training"
    kind: rationale
    verify: "https://huggingface.co/openai/gpt-oss-120b ; search: llama 4 maverick system requirements; deepseek v3 hardware requirements"
    window_days: 180
    last_verified: 2026-09-22

  - id: gpu-vision-api-prices
    pack: core/76-gpu-workers.md
    claim: "Open-weight vision models on the managed tier: Qwen3-VL-30B-A3B-Instruct $0.15/$0.60 and Qwen3-VL-235B-A22B-Instruct $0.20/$0.88 per 1M (DeepInfra); Llama 4 Maverick is vision-capable at $0.27/$0.85 (Together)"
    kind: version
    verify: "https://deepinfra.com/pricing ; https://www.together.ai/models/llama-4-maverick"
    window_days: 90
    last_verified: 2026-09-22

  - id: gpu-replicate-billing
    pack: core/76-gpu-workers.md
    claim: "Replicate bills per second of hardware for most public/custom models (T4 through H100 tiers) and per token only for a set of popular LLMs — several times a dense per-token rate on an under-utilised GPU"
    kind: rationale
    verify: "https://replicate.com/pricing"
    window_days: 180
    last_verified: 2026-09-22

  - id: gpu-hardware-specs
    pack: core/76-gpu-workers.md
    claim: "Datasheet VRAM / memory bandwidth: B200 192GB HBM3e ~8TB/s; H200 141GB 4.8TB/s; H100 SXM 80GB 3.35TB/s; A100 80GB ~2TB/s; L40S 48GB 864GB/s; RTX 4090 24GB ~1TB/s; L4 24GB 300GB/s; T4 16GB 300GB/s"
    kind: rationale
    verify: "https://www.nvidia.com/en-us/data-center/h200/ ; https://www.nvidia.com/en-us/data-center/l40s/ ; the per-GPU NVIDIA datasheets"
    window_days: 365
    last_verified: 2026-09-22

  - id: cc-subagent-concurrency-cap
    pack: core/62-using-subagents.md
    claim: "Claude Code refuses to spawn a subagent when 20 are running in the session (CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS, default 20, per session, adjustable never disableable): 'Concurrent subagent limit reached … Do not retry'; spawning succeeds again when the running count drops"
    kind: version
    verify: "https://code.claude.com/docs/en/sub-agents#concurrent-subagent-limit ; https://code.claude.com/docs/en/env-vars"
    window_days: 90
    last_verified: 2026-09-22

  - id: cc-tool-use-concurrency
    pack: core/62-using-subagents.md
    claim: "CLAUDE_CODE_MAX_TOOL_USE_CONCURRENCY caps how many read-only tools AND subagents execute in parallel (default 10), so a dispatch wider than 10 runs 10-wide below the 20-seat cap"
    kind: version
    verify: "https://code.claude.com/docs/en/env-vars"
    window_days: 90
    last_verified: 2026-09-22

  - id: cc-subagent-spawn-depth
    pack: core/62-using-subagents.md
    claim: "A subagent may spawn subagents up to 3 layers below the main conversation by default (CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH; 1 turns nesting off); a type keeps a seat from spawning by omitting Agent from its tools list or adding it to disallowedTools; the per-session total cap CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION is removed"
    kind: version
    verify: "https://code.claude.com/docs/en/sub-agents#let-subagents-spawn-their-own-subagents ; https://code.claude.com/docs/en/env-vars"
    window_days: 90
    last_verified: 2026-09-22

  - id: cc-subagent-model-resolution
    pack: core/62-using-subagents.md
    claim: "Subagent model resolves per-invocation parameter → frontmatter model (inherit = parent's) → CLAUDE_CODE_SUBAGENT_MODEL → the main conversation's model; CLAUDE_CODE_SUBAGENT_MODEL_FORCE (any value, 0 included) discards every definition's model and the per-invocation parameter — seats then run on CLAUDE_CODE_SUBAGENT_MODEL, or on the parent's model when that is unset; a per-invocation model also applies when the seat is resumed by message"
    kind: version
    verify: "https://code.claude.com/docs/en/sub-agents (Model resolution)"
    window_days: 90
    last_verified: 2026-09-22

  - id: cc-subagent-frontmatter
    pack: core/62-using-subagents.md
    claim: "Documented subagent frontmatter keys: name, description, tools, disallowedTools, model (sonnet|opus|haiku|fable|<model id>|inherit), permissionMode, mcpServers, skills, hooks, memory, background, isolation, maxTurns, effort, initialPrompt, omitClaudeMd, color; omitted tools = the parent's full tool set"
    kind: version
    verify: "https://code.claude.com/docs/en/sub-agents#supported-frontmatter-fields"
    window_days: 90
    last_verified: 2026-09-22

  - id: cc-subagent-resume-and-compaction
    pack: core/62-using-subagents.md
    claim: "A subagent dispatched run_in_background is continued by SendMessage to its recorded agentId with its context intact (no agent-teams flag required) — a synchronous seat has no resumable id, and a resume is also refused for an oversized transcript or a worktree outside the session's isolation fences; subagents auto-compact under the main conversation's rules and CLAUDE_AUTOCOMPACT_PCT_OVERRIDE; per-seat transcripts live at <project>/<sessionId>/subagents/agent-<id>.jsonl"
    kind: practice
    verify: "https://code.claude.com/docs/en/sub-agents#resume-subagents ; https://code.claude.com/docs/en/env-vars"
    window_days: 180
    last_verified: 2026-09-22

  - id: multi-agent-token-cost
    pack: core/62-using-subagents.md
    claim: "Anthropic's multi-agent research post: agents use ~4x the tokens of chat and multi-agent systems ~15x; token usage explains ~80% of performance variance; an Opus lead with Sonnet subagents beat single-agent Opus by 90.2% on their eval — width is paid in tokens, and 'an independent reader catches what the author cannot' is Fabrik's own measured finding, not a claim in that post or in Cognition's 'Don't build multi-agents'"
    kind: rationale
    verify: "https://www.anthropic.com/engineering/multi-agent-research-system ; https://cognition.ai/blog/dont-build-multi-agents"
    window_days: 365
    last_verified: 2026-09-22

  - id: mcp-python-sdk-v2-stable
    pack: docs/reference/subagent-pool-contract.md
    claim: "The mcp Python SDK v2 is the current stable line (pip install mcp resolves to v2); v1.x is maintenance-only (security fixes) — the frozen pool rule 'pin v1, v2 is pre-release' is inverted"
    kind: deprecation
    verify: "https://github.com/modelcontextprotocol/python-sdk (README + releases)"
    window_days: 180
    last_verified: 2026-09-22

  - id: openrouter-credits-management-key
    pack: docs/reference/subagent-pool-contract.md
    claim: "GET https://openrouter.ai/api/v1/credits (data.total_credits / data.total_usage) requires a MANAGEMENT key — a normal inference key gets 403 'Only management keys can perform this operation'; GET /api/v1/key works with a regular key (limit_remaining); GET /api/v1/models is public"
    kind: version
    verify: "https://openrouter.ai/docs/api-reference/authentication ; https://openrouter.ai/docs/api/api-reference/credits/get-remaining-credits"
    window_days: 180
    last_verified: 2026-09-22

  - id: fleet-postgres-main-no-pgvector
    pack: core/65-rag-search.md
    claim: "postgres-main runs the plain postgres:<major>-alpine image (PostgreSQL 16.x, musl); pg_available_extensions lists pg_trgm 1.6 (installed only in glitchtip) and NO vector extension — pgvector cannot be created on any database there until the fleet changes the image"
    kind: rationale
    verify: "ssh vps 'sudo -n docker exec -i postgres-main psql -U postgres -tA' <<< \"select name, default_version from pg_available_extensions where name in ('vector','pg_trgm')\""
    window_days: 90
    last_verified: 2026-09-22

  - id: pgvector-hnsw-defaults
    pack: core/65-rag-search.md
    claim: "pgvector HNSW defaults are m=16, ef_construction=64 at build and hnsw.ef_search=40 (max 1000) at query; IVFFlat needs manual rebuilds to keep recall"
    kind: practice
    verify: "https://github.com/pgvector/pgvector/blob/master/README.md (HNSW / Query options)"
    window_days: 180
    last_verified: 2026-09-22

  - id: pgvector-iterative-scan-halfvec
    pack: core/65-rag-search.md
    claim: "pgvector 0.8.0 (2024-10-30) added hnsw.iterative_scan (strict_order|relaxed_order) so filtered approximate queries no longer starve (a 10% filter at ef_search=40 averaged 4 rows); 0.7.0 added halfvec (2-byte floats, up to 4,000 dims) and bit vectors; the 0.8.x line is current"
    kind: version
    verify: "https://github.com/pgvector/pgvector/blob/master/CHANGELOG.md"
    window_days: 180
    last_verified: 2026-09-22

  - id: pg-bm25-extensions
    pack: core/65-rag-search.md
    claim: "Native Postgres full-text has no BM25 (ts_rank_cd is coverage-density); maintained BM25-on-Postgres extensions are ParadeDB pg_search, VectorChord-bm25 (with pg_tokenizer.rs) and Tiger Data's pg_textsearch"
    kind: practice
    verify: "https://pgxn.org/dist/pg_search/ ; https://github.com/tensorchord/VectorChord-bm25 ; https://www.tigerdata.com/blog/pg-textsearch-bm25-full-text-search-postgres"
    window_days: 180
    last_verified: 2026-09-22

  - id: rrf-k60-origin
    pack: core/65-rag-search.md
    claim: "Reciprocal Rank Fusion score = sum 1/(k+rank), k=60 — Cormack, Clarke & Buettcher, SIGIR 2009; k=60 is the default in pgvector's own hybrid-search example"
    kind: rationale
    verify: "https://cormack.uwaterloo.ca/cormacksigir09-rrf.pdf ; https://github.com/pgvector/pgvector-python/blob/master/examples/hybrid_search/rrf.py"
    window_days: 365
    last_verified: 2026-09-22

  - id: meili-ranking-rules-seven
    pack: core/65-rag-search.md
    claim: "Meilisearch v1.36.0 (2026-03) split the attribute ranking rule into attributeRank + wordPosition, making the default seven rules (words, typo, proximity, attributeRank, sort, wordPosition, exactness); before v1.36 the default is six with attribute, and the two forms cannot be mixed; the fleet's meilisearch container is getmeili/meilisearch:v1.13 (probed 2026-09-22); changing searchable/filterable/sortable attributes triggers a full reindex; current stable is the v1.5x line"
    kind: version
    verify: "https://github.com/meilisearch/meilisearch/releases/tag/v1.36.0 ; https://www.meilisearch.com/docs/capabilities/full_text_search/relevancy/ranking_rules ; ssh vps 'sudo -n docker ps --format {{.Image}} | grep meili'"
    window_days: 90
    last_verified: 2026-09-22

  - id: meili-hybrid-embedders
    pack: core/65-rag-search.md
    claim: "Meilisearch ships native vector/hybrid search: per-index embedders (OpenAI, HuggingFace, Ollama, REST) and a per-search semanticRatio (0-1, default 0.5) — the pack keeps it OFF by rule (one vector store)"
    kind: practice
    verify: "https://www.meilisearch.com/docs/capabilities/hybrid_search/advanced/custom_hybrid_ranking"
    window_days: 180
    last_verified: 2026-09-22

  - id: chroma-chunking-eval
    pack: core/65-rag-search.md
    claim: "Chroma's chunking evaluation (2024) is the primary source on semantic vs recursive chunking: ClusterSemanticChunker IoU 8.0±6.0 vs RecursiveCharacterTextSplitter 7.0±5.6 — single-digit-point gains for an embedding call per split; no primary source states a '3–5%' figure"
    kind: rationale
    verify: "https://www.trychroma.com/research/evaluating-chunking"
    window_days: 365
    last_verified: 2026-09-22

  - id: rag-eval-metric-names
    pack: core/65-rag-search.md
    claim: "Ragas names its metrics Faithfulness and Context Precision; DeepEval's are Faithfulness and Contextual Precision (ContextualPrecisionMetric); both packages are maintained"
    kind: practice
    verify: "https://docs.ragas.io/en/stable/concepts/metrics/available_metrics/ ; https://deepeval.com/docs/metrics-contextual-precision"
    window_days: 180
    last_verified: 2026-09-22

  - id: openrouter-embeddings-rerank-endpoints
    pack: core/65-rag-search.md
    claim: "OpenRouter exposes POST /api/v1/embeddings (fields model, input, dimensions, encoding_format, input_type, provider), GET /api/v1/embeddings/models, and POST /api/v1/rerank (model, query, documents, top_n → results[].relevance_score); rerank models include cohere/rerank-v3.5 and the free nvidia/llama-nemotron-rerank-vl-1b-v2"
    kind: practice
    verify: "https://openrouter.ai/docs/api-reference/embeddings/create-embeddings ; https://openrouter.ai/docs/cookbook/evaluate-and-optimize/rag ; https://openrouter.ai/nvidia/llama-nemotron-rerank-vl-1b-v2"
    window_days: 180
    last_verified: 2026-09-22

  - id: qwen3-embedding-8b-on-openrouter
    pack: core/65-rag-search.md
    claim: "qwen/qwen3-embedding-8b on OpenRouter: $0.01/M input, 32,768-token context (shown as 33K), 4096 native dims with Matryoshka output 32–4096 (Qwen3-Embedding card, arXiv 2506.05176); 100+ languages; no primary source gives the accuracy at 1024 dims; per-provider honouring of `dimensions` through OpenRouter is not documented"
    kind: version
    verify: "https://openrouter.ai/qwen/qwen3-embedding-8b ; https://huggingface.co/Qwen/Qwen3-Embedding-8B"
    window_days: 90
    last_verified: 2026-09-22

  - id: embedding-alternatives-on-openrouter
    pack: core/65-rag-search.md
    claim: "On OpenRouter: openai/text-embedding-3-large $0.13/M 8k (13x the default); mistralai/codestral-embed-2505 $0.15/M 8k; openai/text-embedding-3-small IS listed at $0.02/M 8k (outside the roster floors); voyageai/voyage-4 $0.06/M 32k; google/gemini-embedding-001 $0.15/M 20k"
    kind: version
    verify: "https://openrouter.ai/openai/text-embedding-3-large ; https://openrouter.ai/mistralai/codestral-embed-2505 ; https://openrouter.ai/openai/text-embedding-3-small ; https://openrouter.ai/voyageai/voyage-4 ; https://openrouter.ai/google/gemini-embedding-001"
    window_days: 90
    last_verified: 2026-09-22

  - id: claude-count-tokens-and-1m-context
    pack: core/65-rag-search.md
    claim: "Anthropic token counting is client.messages.count_tokens() (POST /v1/messages/count_tokens; the bare client.count_tokens() is removed); the current Claude line (Sonnet 4.6, Opus 5, Fable/Mythos 5.x, Sonnet 5) has a 1M-token context by default with no beta header, older models such as Sonnet 4.5 have 200k"
    kind: version
    verify: "https://platform.claude.com/docs/en/api/messages/count_tokens ; https://platform.claude.com/docs/en/build-with-claude/context-windows"
    window_days: 90
    last_verified: 2026-09-22

  - id: openrouter-models-context-length
    pack: core/65-rag-search.md
    claim: "OpenRouter's public GET /api/v1/models returns each model's context_length — the source for per-model context limits at runtime instead of a hand-typed table (e.g. qwen/qwen3-32b 131,072 with YaRN on OpenRouter vs 32,768 native; qwen/qwen3-8b $0.117/$0.455 per M, 131k)"
    kind: practice
    verify: "https://openrouter.ai/api/v1/models (public) ; https://openrouter.ai/qwen/qwen3-8b"
    window_days: 180
    last_verified: 2026-09-22

  - id: tiktoken-encodings
    pack: core/65-rag-search.md
    claim: "tiktoken encodings: o200k_base for current OpenAI models (GPT-4o line onward), cl100k_base for the older GPT-4/3.5 and text-embedding-3 models — the module's chunker counts with cl100k_base as an approximation for non-OpenAI embedders"
    kind: practice
    verify: "https://github.com/openai/tiktoken (README model→encoding table)"
    window_days: 365
    last_verified: 2026-09-22

  - id: ollama-api-show-context-length
    pack: core/65-rag-search.md
    kind: practice
    claim: "Ollama's model-information endpoint is POST /api/show (JSON body naming the model) and its response carries the context length under model_info as <arch>.context_length, e.g. llama.context_length"
    verify: "https://github.com/ollama/ollama/blob/main/docs/api.md#show-model-information (WebFetch on the raw file, 2026-09-22)"
    window_days: 180
    last_verified: 2026-09-22

  - id: anthropic-contextual-retrieval
    pack: core/66-rag-chunking.md
    kind: practice
    claim: "Anthropic's Contextual Retrieval (2024): prepending a chunk-specific context sentence before embedding and BM25 cut the top-20-chunk retrieval failure rate by 35% (contextual embeddings alone, 5.7%→3.7%), 49% (with contextual BM25, →2.9%) and 67% with reranking (→1.9%); one-time cost about $1.02 per million document tokens with prompt caching; a knowledge base under ~200,000 tokens (~500 pages) goes in the prompt whole with no RAG"
    verify: "https://www.anthropic.com/news/contextual-retrieval (WebFetch, 2026-09-22)"
    window_days: 365
    last_verified: 2026-09-22

  - id: chroma-chunking-eval-sizes
    pack: core/66-rag-chunking.md
    kind: practice
    claim: "Chroma's Evaluating Chunking Strategies for Retrieval (Smith & Troynikov, July 2024) measures token-level recall/precision/IoU; RecursiveCharacterTextSplitter at 200 tokens with no overlap performs consistently well, OpenAI Assistants' default of 800 tokens with 400 overlap gives slightly below-average recall and the lowest scores on the other metrics, ClusterSemanticChunker at 400 tokens reaches 0.913 recall while 200 tokens gives the highest precision/IoU; reducing overlap improves IoU; recall spans 83.6%–91.9% across strategies"
    verify: "https://www.trychroma.com/research/evaluating-chunking (WebFetch + brave, 2026-09-22)"
    window_days: 365
    last_verified: 2026-09-22

  - id: chunk-size-multidataset-2025
    pack: core/66-rag-chunking.md
    kind: practice
    claim: "Rethinking Chunk Size For Long-Document Retrieval: A Multi-Dataset Analysis (Bhat, Rudat, Spiekermann, Flores-Herr, arXiv 2505.21700, May 2025): smaller chunks of 64–128 tokens perform better on datasets with concise fact-based answers, larger chunks of 512–1024 tokens on datasets needing broader context; the optimum also depends on the embedding model"
    verify: "https://arxiv.org/abs/2505.21700 (WebFetch, 2026-09-22)"
    window_days: 365
    last_verified: 2026-09-22

  - id: semantic-chunking-cost-2024
    pack: core/66-rag-chunking.md
    kind: practice
    claim: "Is Semantic Chunking Worth the Computational Cost? (Qu, Tu, Bao, arXiv 2410.13070, Oct 2024) evaluates document retrieval, evidence retrieval and answer generation and concludes the computational costs of semantic chunking are not justified by consistent performance gains over fixed-size chunking"
    verify: "https://arxiv.org/abs/2410.13070 (WebFetch, 2026-09-22)"
    window_days: 365
    last_verified: 2026-09-22

  - id: late-chunking-jina-2024
    pack: core/66-rag-chunking.md
    kind: practice
    claim: "Late Chunking (Günther et al., Jina AI, arXiv 2409.04701, Sept 2024): embed all tokens of a long document with a long-context mean-pooling embedding model, then pool per chunk; averaged over three models and four BeIR datasets it improves naive chunking's nDCG@10 by 1.5–1.9 points absolute (2.7–3.6% relative), more on long documents; overlapping chunks generally neither improves nor harms retrieval; it needs token-level embeddings, so it is not reachable through a one-vector-per-input embeddings API"
    verify: "https://arxiv.org/abs/2409.04701 ; https://github.com/jina-ai/late-chunking (exa, 2026-09-22)"
    window_days: 365
    last_verified: 2026-09-22

  - id: azure-chunking-overlap-guidance
    pack: core/66-rag-chunking.md
    kind: practice
    claim: "Azure AI Search's chunking guidance recommends starting at 512 tokens (about 2,000 characters) with a 25% initial overlap (128 tokens), 10–15% overlap for fixed-size chunks, and names Markdown/HTML heading syntax as a chunk-by-section boundary; it states each token is around four characters for common OpenAI models"
    verify: "https://learn.microsoft.com/en-us/azure/search/vector-search-how-to-chunk-documents (WebFetch, 2026-09-22)"
    window_days: 180
    last_verified: 2026-09-22

  - id: langchain-markdown-header-splitter
    pack: core/66-rag-chunking.md
    kind: practice
    claim: "LangChain's MarkdownHeaderTextSplitter(headers_to_split_on=[('#','Header 1'),('##','Header 2'),…], strip_headers=True by default, return_each_line=False) splits a Markdown file by the given headers and attaches the header values as per-chunk metadata; the documented pattern applies a size splitter such as RecursiveCharacterTextSplitter inside each header group afterwards, with overlap never crossing a section boundary"
    verify: "https://reference.langchain.com/python/langchain-text-splitters/markdown/MarkdownHeaderTextSplitter ; https://docs.langchain.com/oss/python/integrations/splitters/markdown_header_metadata_splitter (exa, 2026-09-22 — python.langchain.com URLs redirect)"
    window_days: 180
    last_verified: 2026-09-22

  - id: rag-module-chunker-bounds
    pack: core/66-rag-chunking.md
    kind: practice
    claim: "fabrik-lib rag/chunker.py is the plaintext path only (its docstring: no Markdown headings, tables or fences; heading_path is a caller-supplied breadcrumb): TOKEN_TARGET_MIN/MAX 512/800, TOKEN_HARD_MIN/MAX 120/1200, RAG_EMBED_TOKEN_CEILING default 6800 with a token-grid hard split, _add_overlap default 0.15 on sentence boundaries capped at the hard max, cl100k_base counting; Chunk.chunk_header is computed but ingest_text() embeds c.content without it; the two fleet copies (trade-intelligence, youtube) declare TOKEN_API_CEILING = 6800 but neither enforces it (trade-intelligence has no token-grid split; youtube splits a >1,200-token sentence at target_max)"
    verify: "grep -nE 'TOKEN_|overlap_ratio|get_encoding' /opt/fabrik-lib/rag/chunker.py ; grep -nE 'c.content|chunk_header' /opt/fabrik-lib/rag/ingest.py"
    window_days: 90
    last_verified: 2026-09-22

  - id: pgvector-control-not-trusted
    pack: core/65-rag-search.md
    claim: "pgvector's vector.control declares no `trusted = true` (upstream master and the on-box 0.6.0 copy), so CREATE EXTENSION vector needs a superuser — the fleet ask is the image AND the superuser step; if a release marks it trusted the ask shrinks to the image"
    kind: version
    verify: "https://raw.githubusercontent.com/pgvector/pgvector/master/vector.control"
    window_days: 180
    last_verified: 2026-09-22

  - id: node-require-esm
    pack: core/67-file-api.md
    kind: version
    claim: "Node require() of an ES module is unflagged since 22.12/20.19/23.0 and silent by default since 22.13/20.19/23.5; marked no-longer-experimental only from 25.4; it throws ERR_REQUIRE_ASYNC_MODULE on a module with top-level await \u2014 dynamic import() is the safe path in CJS"
    verify: "https://nodejs.org/api/modules.html (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: r2-s3-limits
    pack: core/67-file-api.md
    kind: practice
    claim: "Cloudflare R2: region is 'auto' (empty and us-east-1 alias to it); multipart parts 5 MiB-5 GiB, all non-final parts uniform in size, <=10,000 parts, 5 TiB max object; presigned URLs 1 s-7 days and only on <account>.r2.cloudflarestorage.com; default lifecycle expires incomplete multipart uploads 7 days after initiation, AbortIncompleteMultipartUpload rules supported; egress free"
    verify: "https://developers.cloudflare.com/r2/platform/limits/ ; https://developers.cloudflare.com/r2/buckets/object-lifecycles/ ; https://developers.cloudflare.com/r2/api/s3/presigned-urls/ ; https://developers.cloudflare.com/r2/pricing/ (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: b2-s3-addressing-limits
    pack: core/67-file-api.md
    kind: practice
    claim: "Backblaze B2 S3 API accepts both virtual-hosted (bucket.s3.<region>.backblazeb2.com) and path-style addressing; multipart parts 5 MB-5 GB (last part >=1 byte), <=10,000 parts; egress free up to 3x average stored bytes, unlimited via partner CDNs incl. Cloudflare"
    verify: "https://www.backblaze.com/docs/cloud-storage-call-the-s3-compatible-api ; https://www.backblaze.com/docs/cloud-storage-large-files ; https://www.backblaze.com/apidocs/s3-upload-part ; https://www.backblaze.com/cloud-storage/pricing (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: supabase-storage-s3-auth
    pack: core/67-file-api.md
    kind: practice
    claim: "Supabase Storage S3 session-token auth: accessKeyId = project_ref, secretAccessKey = anon key, sessionToken = user JWT, forcePathStyle true, endpoint https://<ref>.storage.supabase.co/storage/v1/s3; RLS respected for session tokens, generated S3 keys bypass RLS; x-amz-acl / x-amz-grant-* and versioning not implemented"
    verify: "https://supabase.com/docs/guides/storage/s3/authentication ; https://supabase.com/docs/guides/storage/s3/compatibility (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: aws-sdk-checksum-defaults
    pack: core/67-file-api.md
    kind: practice
    claim: "@aws-sdk/client-s3 from 3.729.0 (2025-01-16) defaults requestChecksumCalculation/responseChecksumValidation to WHEN_SUPPORTED (CRC32 on uploads); WHEN_REQUIRED disables it for incompatible third-party backends; Cloudflare R2 (checksum table) and Backblaze B2 (unsupported list no longer names checksum headers, updated 2025-12-10) accept the default per current docs"
    verify: "https://github.com/aws/aws-sdk-js-v3/issues/6810 ; https://docs.aws.amazon.com/sdkref/latest/guide/feature-dataintegrity.html ; https://developers.cloudflare.com/r2/api/s3/api/ ; https://www.backblaze.com/docs/cloud-storage-s3-compatible-api (exa + WebFetch, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: aws-sdk-undici-handler
    pack: core/67-file-api.md
    kind: practice
    claim: "@smithy/undici-http-handler (and @aws-sdk/undici-http-handler) is AWS-published and documented in aws-sdk-js-v3 CLIENTS.md as an optional alternative; the Node default handler uses node:http/https with keepAlive true, maxSockets 50; the README benchmark reports 35-50% less time in request handling on a local server and says to benchmark your own case"
    verify: "https://raw.githubusercontent.com/aws/aws-sdk-js-v3/main/supplemental-docs/CLIENTS.md ; https://github.com/smithy-lang/smithy-typescript/tree/main/packages/undici-http-handler ; https://registry.npmjs.org/@smithy/undici-http-handler/latest (exa + WebFetch, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: aws-sdk-retry-modes
    pack: core/67-file-api.md
    kind: practice
    claim: "ConfiguredRetryStrategy lives in @smithy/util-retry (@aws-sdk/util-retry is deprecated) and extends StandardRetryStrategy with a custom backoff \u2014 it is not adaptive; adaptive mode (AdaptiveRetryStrategy / retryMode adaptive) adds a per-client rate limiter that can delay first attempts, and AWS does not recommend it as a general default or for one client serving many tenants"
    verify: "https://raw.githubusercontent.com/smithy-lang/smithy-typescript/main/packages/util-retry/src/ConfiguredRetryStrategy.ts ; https://docs.aws.amazon.com/sdkref/latest/guide/feature-retry-behavior.html ; https://registry.npmjs.org/@aws-sdk/util-retry/latest (exa + WebFetch, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: presigned-url-expiry
    pack: core/67-file-api.md
    kind: rationale
    claim: "SigV4 presigned URLs expire at most 604,800 s (7 days); getSignedUrl defaults to 900 s; NIST SP 800-63B-4 contains no presigned-URL rule, no 'observation contract' and no 900 s cap (its 15-minute figure is the AAL3 inactivity timeout) \u2014 the 900 s cap is Fabrik house policy"
    verify: "https://raw.githubusercontent.com/smithy-lang/smithy-typescript/main/packages/signature-v4/src/constants.ts ; https://docs.aws.amazon.com/AmazonS3/latest/userguide/using-presigned-url.html ; https://pages.nist.gov/800-63-4/sp800-63b.html (exa + WebFetch + brave, 2026-09-23)"
    window_days: 365
    last_verified: 2026-09-23
  - id: presigner-signed-headers
    pack: core/67-file-api.md
    kind: practice
    claim: "@aws-sdk/s3-request-presigner adds content-type to unsignableHeaders, so ContentType binds only with signableHeaders: new Set(['content-type']); content-length is not unsignable and stays a signed header when set (source inference); only x-amz-* headers are hoisted to the query; R2 documents 403 on a mismatched signed Content-Type but not Content-Length enforcement"
    verify: "https://raw.githubusercontent.com/aws/aws-sdk-js-v3/main/packages/s3-request-presigner/src/presigner.ts ; https://developers.cloudflare.com/r2/api/s3/presigned-urls/ (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: busboy-multer-facts
    pack: core/67-file-api.md
    kind: version
    claim: "busboy latest 1.6.0 (2022, no v2, repo dormant since 2023); limits.fileSize truncates the stream (truncated=true + 'limit' event), it never errors; multer (latest 2.4.0) is built on busboy, memoryStorage holds the entire file as a Buffer, and 2.4.0 fixes the latest of its 2025-2026 advisories"
    verify: "https://registry.npmjs.org/busboy ; https://github.com/mscdex/busboy ; https://registry.npmjs.org/multer/latest ; https://github.com/expressjs/multer/security/advisories (WebFetch + exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: file-type-facts
    pack: core/67-file-api.md
    kind: version
    claim: "file-type latest 22.x is ESM-only (type module, Node >=22); exports fileTypeFromBuffer/FromStream(web ReadableStream only)/FromFile/FromBlob/FromTokenizer; default sampleSize 4100 bytes; detects OOXML docx/xlsx/pptx distinctly from zip, not legacy CFB .doc/.xls without @file-type/cfbf"
    verify: "https://registry.npmjs.org/file-type/latest ; https://github.com/sindresorhus/file-type (WebFetch + exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: blake3-security-level
    pack: core/67-file-api.md
    kind: rationale
    claim: "BLAKE3 with at least a 32-byte output targets a security level of at least 128 bits for all its goals (not 256); @noble/hashes 2.x is ESM-only and exports it at '@noble/hashes/blake3.js'"
    verify: "https://www.ietf.org/archive/id/draft-aumasson-blake3-00.html ; https://registry.npmjs.org/@noble/hashes/latest ; https://github.com/paulmillr/noble-hashes (exa + WebFetch, 2026-09-23)"
    window_days: 365
    last_verified: 2026-09-23
  - id: sharp-untrusted-input-defaults
    pack: core/67-file-api.md
    kind: practice
    claim: "sharp limitInputPixels defaults to 268402689 (0x3FFF squared); failOn 'warning' recommended for untrusted input; output strips all metadata (EXIF incl. orientation, ICC, XMP, IPTC) unless keepMetadata/withMetadata/keepExif etc. is called"
    verify: "https://sharp.pixelplumbing.com/api-constructor ; https://sharp.pixelplumbing.com/api-output (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: clamav-clamd-facts
    pack: core/67-file-api.md
    kind: practice
    claim: "clamd INSTREAM: z/n-prefixed command, chunks framed by a 4-byte network-order length, zero-length terminator; replies 'stream: OK' / '<sig> FOUND'; the whole stream is capped by StreamMaxLength (default 100M) and an overflow replies 'INSTREAM size limit exceeded'; MaxFileSize 100M, MaxScanSize 400M; stock clamd.conf TCPSocket off, the official clamav/clamav image enables 3310; docker docs: min 3 GiB, prefer 4 GiB \u2014 db load >1.2 GiB, doubled during reload unless ConcurrentDatabaseReload no; 1.4 is the LTS line (EOL 2027-08-15)"
    verify: "https://docs.clamav.net/manual/Usage/ClamdProtocol.html ; https://raw.githubusercontent.com/Cisco-Talos/clamav/main/etc/clamd.conf.sample ; https://docs.clamav.net/manual/Installing/Docker.html ; https://docs.clamav.net/faq/faq-eol.html (WebSearch + exa + WebFetch, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23
  - id: kvkk-deletion-regulation
    pack: core/67-file-api.md
    kind: rationale
    claim: "KVKK deletion regulation (under Law 6698 Art. 7(3)): Art. 11(2) periodic destruction interval set in the retention policy and 'her halde alt\u0131 ay\u0131 ge\xE7emez'; Art. 7(3) all deletion/destruction/anonymisation operations recorded and kept 'en az \xFC\xE7 y\u0131l'; the regulation names no hash chain or tamper-evidence mechanism \u2014 record-keeping plus 'gerekli her t\xFCrl\xFC teknik ve idari tedbirler'"
    verify: "https://www.kvkk.gov.tr/Icerik/5441 ; https://www.kvkk.gov.tr/Icerik/6649/Personal-Data-Protection-Law (exa + WebFetch, 2026-09-23)"
    window_days: 365
    last_verified: 2026-09-23

  - id: opossum-trip-rule
    pack: core/67-file-api.md
    kind: practice
    claim: "opossum opens when the rolling-window error percentage exceeds errorThresholdPercentage, counted only once fires >= volumeThreshold; volumeThreshold defaults to 0, so a single failure in a quiet window reads 100% and opens; half-open admits one probe and a success closes"
    verify: "https://github.com/nodeshift/opossum/blob/main/lib/circuit.js (volumeThreshold default + the errorRate check) ; https://registry.npmjs.org/opossum/latest (source read by the second-opinion seat, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23

  - id: smithy-request-timeout
    pack: core/67-file-api.md
    kind: practice
    claim: "@smithy/node-http-handler: when requestTimeout elapses it only logs a warning (logger.warn) unless throwOnRequestTimeout is true, in which case the request is destroyed and the promise rejects with a TimeoutError; the default handler sets no request timeout"
    verify: "https://github.com/smithy-lang/smithy-typescript/tree/main/packages/node-http-handler/src (set-request-timeout) ; executed against @smithy/node-http-handler 4.12.1 with a never-replying server by the review seat, 2026-09-23"
    window_days: 180
    last_verified: 2026-09-23

  - id: presign-empty-body-checksum
    pack: core/67-file-api.md
    kind: practice
    claim: "With the client default requestChecksumCalculation WHEN_SUPPORTED, getSignedUrl for a PutObjectCommand signs x-amz-checksum-crc32=AAAAAA== (the CRC32 of an empty body) plus x-amz-sdk-checksum-algorithm=CRC32 into the URL; a client built with WHEN_REQUIRED omits both"
    verify: "executed: getSignedUrl with @aws-sdk/client-s3 + s3-request-presigner 3.1138.0, default vs WHEN_REQUIRED client, by two review seats 2026-09-23 ; https://github.com/aws/aws-sdk-js-v3/tree/main/packages/s3-request-presigner"
    window_days: 180
    last_verified: 2026-09-23

  - id: iyzico-subscription-non3d
    pack: core/85-payments-billing.md
    kind: practice
    claim: "iyzico processes every subscription transaction as NON3D, the first included ('Abonelik işlemlerinde ilk işlem dahil, tüm işlemler NON3D olarak gerçekleştirilmektedir'); both start paths (Checkout Form POST /v2/subscription/checkoutform/initialize and the NON-3DS raw-card path) are documented; the English page omits the sentence; the subscription feature is a paid add-on enabled per account on request"
    verify: "https://docs.iyzico.com/urunler/abonelik/abonelik-entegrasyonu/abonelik-islemleri ; https://docs.iyzico.com/urunler/abonelik (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23

  - id: stripe-unsupported-turkey
    pack: core/85-payments-billing.md
    kind: rationale
    claim: "Turkey is not a Stripe-supported country, so a Turkey-resident entity cannot open a Stripe account"
    verify: "https://stripe.com/global (WebFetch; exa dropped the country list; WebSearch secondary sources agree, 2026-09-23)"
    window_days: 365
    last_verified: 2026-09-23

  - id: paddle-billing-api-basics
    pack: core/85-payments-billing.md
    kind: version
    claim: "The product is Paddle Billing (legacy: Paddle Classic); the REST API is version 1 (Paddle-Version: 1); @paddle/paddle-js is for Paddle.js v2 (Billing only); overlay and inline checkout are both documented, both via Checkout.open (inline adds displayMode inline), overlay described as quicker; portal sessions are POST /customers/{customer_id}/portal-sessions returning urls.general.overview and per-subscription URLs with a temporary token, lifetime undocumented, not to be stored, cached or iframed; sandbox and live are separate accounts (pdl_sdbx_/pdl_live_ API keys, test_/live_ client tokens, environment 'sandbox' | 'production')"
    verify: "https://developer.paddle.com/api-reference/about/versioning ; https://www.npmjs.com/package/@paddle/paddle-js ; https://developer.paddle.com/concepts/sell/self-serve-checkout ; https://developer.paddle.com/api-reference/customer-portals/create-customer-portal-session ; https://developer.paddle.com/build/tools/sandbox (exa + WebFetch, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23

  - id: paddle-webhook-signature
    pack: core/85-payments-billing.md
    kind: practice
    claim: "Paddle-Signature is ts=<unix>;h1=<hash>; the signed payload is the timestamp, a colon and the raw body, HMAC-SHA256 keyed with the destination's secret; checking ts age is optional and Paddle's SDKs default to a 5-second tolerance; at least one h1, more than one during a future secret rotation"
    verify: "https://developer.paddle.com/webhooks/signature-verification (exa + WebFetch, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23

  - id: paddle-webhook-delivery
    pack: core/85-payments-billing.md
    kind: practice
    claim: "Paddle expects a 200 within five seconds; live accounts retry 60 times within 3 days (20 in the first hour, 47 in the first day), sandbox 3 times within 15 minutes; delivery order is not guaranteed (use occurred_at); exhausted deliveries become failed and can be replayed"
    verify: "https://developer.paddle.com/webhooks/respond-to-webhooks ; https://developer.paddle.com/build/tools/sandbox (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23

  - id: paddle-adjustments
    pack: core/85-payments-billing.md
    kind: practice
    claim: "Paddle Billing has no transaction.refunded event; refunds and credits are POST /adjustments with action (refund, credit are merchant-created; chargeback, chargeback_reverse, chargeback_warning, chargeback_warning_reverse, credit_reverse are created by Paddle), type full|partial (partial needs items), transaction_id, reason; tax_mode defaults to internal (amounts tax-inclusive); most live refunds start pending_approval; events adjustment.created and adjustment.updated"
    verify: "https://developer.paddle.com/api-reference/adjustments/create-adjustment ; https://developer.paddle.com/webhooks/overview (exa + WebFetch, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23

  - id: iyzico-webhooks
    pack: core/85-payments-billing.md
    kind: practice
    claim: "iyzico webhooks: configured under Settings > Merchant Settings > Merchant Notifications (HTTPS); first notification 10-15 s after the first attempt, resent every 15 minutes until a 2xx, about three more times (TR says '3 defa tekrar', EN '3 attempts'); signature header X-IYZ-SIGNATURE-V3 (V1/V2 unsupported) is sent only once webhook signing is enabled on the account; HMAC-SHA256 hex keyed with the secret key over family-specific field concatenations that also include the secret key; no general refund webhook; the Checkout Form result is read back with its token and a subscription's status re-checked after a notification"
    verify: "https://docs.iyzico.com/ek-servisler/webhook ; https://docs.iyzico.com/en/advanced/webhook ; https://docs.iyzico.com/odeme-metotlari/odeme-formu/cf-entegrasyonu/cf-sorgulama (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23

  - id: paytr-iframe-callback
    pack: core/85-payments-billing.md
    kind: practice
    claim: "PayTR iFrame callback hash = base64(HMAC-SHA256(merchant_key, merchant_oid + merchant_salt + status + total_amount)); the merchant must answer the plain text OK only; without it PayTR treats the notification as failed and (per the English page) retries after a minute, so duplicates are de-duplicated on merchant_oid; test_mode=1 makes test transactions; recurring payments and non_3d need a separately granted Non3D permission (Direkt API)"
    verify: "https://dev.paytr.com/iframe-api/iframe-api-2-adim ; https://dev.paytr.com/en/iframe-api/iframe-api-2-adim ; https://dev.paytr.com/direkt-api/kart-saklama-api/kayitli-kart-tekrarlayan-odeme (exa, 2026-09-23)"
    window_days: 180
    last_verified: 2026-09-23

  - id: kdv-rate-and-export-exemption
    pack: core/85-payments-billing.md
    kind: rationale
    claim: "Turkish standard KDV rate is 20% (from 2023-07-10, Cumhurbaşkanı Kararı 7346); services performed for customers abroad are exempt (KDV Kanunu md. 11/1-a) only if performed for a customer abroad AND used abroad (md. 12/2)"
    verify: "https://www.mevzuat.gov.tr/anasayfa/MevzuatFihristDetayIframe?MevzuatTur=1&MevzuatNo=3065&MevzuatTertip=5 (exa) ; GİB KDV oranları PDF (exa search highlight, 2026-09-23)"
    window_days: 365
    last_verified: 2026-09-23

  - id: resend-pricing-and-limits
    pack: core/86-email-templates.md
    kind: practice
    claim: "Resend transactional: free 3,000 emails/mo capped at 100/day with 3 domains; Pro $20/mo for 50,000 ($0.90/1,000 overage); marketing (Broadcasts) priced by contacts — free to 1,000, $40/mo for 5,000; API default 10 requests/s per team; batch up to 100 (no attachments); 40 MB per email; Idempotency-Key kept 24 hours"
    verify: "https://resend.com/pricing ; https://resend.com/docs/api-reference/rate-limit ; https://resend.com/docs/dashboard/emails/idempotency-keys (WebFetch + exa + brave, 2026-09-23; ledger docs/reference/research/2026-09-23-email-templates-currency-ledger.md resend-01..19)"
    window_days: 90
    last_verified: 2026-09-23

  - id: postmark-pricing-and-streams
    pack: core/86-email-templates.md
    kind: practice
    claim: "Postmark: free Developer tier 100 emails/mo, no overage; Basic $15/mo from 10,000 ($1.80/1,000 overage, $1.30 Pro, $1.20 Platform); running since 2010; transactional and broadcast Message Streams on separate infrastructure; 10 MB per message, 50 MB per batch payload"
    verify: "https://postmarkapp.com/pricing ; https://postmarkapp.com/support/article/how-to-create-and-send-through-message-streams (WebFetch + exa + brave, 2026-09-23; ledger postmark rows resend-10..20)"
    window_days: 180
    last_verified: 2026-09-23

  - id: mailbox-bulk-sender-requirements
    pack: core/86-email-templates.md
    kind: standard
    claim: "Gmail and Yahoo bulk senders (~5,000+ messages/day): SPF and DKIM and DMARC at least p=none with From aligned to SPF or DKIM; spam rate below 0.3%; RFC 8058 one-click for marketing mail only, plus a visible body link, honoured within 48 hours; every sender needs valid forward and reverse DNS and TLS. Outlook.com applies the same 5,000/day authentication rule and has junked non-compliant mail since 2025-05-05, with rejection announced for later; it recommends but does not mandate one-click"
    verify: "https://support.google.com/mail/answer/81126 ; https://support.google.com/a/answer/14229414 ; https://senders.yahooinc.com/best-practices/ ; techcommunity.microsoft.com post 4399730 (WebFetch + exa + brave, 2026-09-23; ledger mailbox-01..12)"
    window_days: 180
    last_verified: 2026-09-23

  - id: rfc8058-one-click
    pack: core/86-email-templates.md
    kind: standard
    claim: "RFC 8058: List-Unsubscribe carries one HTTPS URI; List-Unsubscribe-Post carries exactly List-Unsubscribe=One-Click; a valid DKIM signature must cover both headers"
    verify: "https://www.rfc-editor.org/rfc/rfc8058 (WebFetch, 2026-09-23; ledger mailbox-07)"
    window_days: 365
    last_verified: 2026-09-23

  - id: gmail-clip-and-dmarc-rollout
    pack: core/86-email-templates.md
    kind: practice
    claim: "Gmail clips messages above ~102 KB of HTML/text (an ESP-measured figure; Google does not publish it); Google's DMARC rollout guidance is about a week at p=none, then quarantine on a small percentage ramped up, then reject — no fixed calendar"
    verify: "https://mailchimp.com/help/gmail-is-clipping-my-email/ ; https://knowledge.workspace.google.com/admin/security/recommended-dmarc-rollout (WebSearch + exa + WebFetch, 2026-09-23; ledger mailbox-05, mailbox-06)"
    window_days: 180
    last_verified: 2026-09-23

  - id: email-dark-mode-support
    pack: core/86-email-templates.md
    kind: practice
    claim: "Per caniemail: Apple Mail, Samsung Email and Thunderbird honour both the color-scheme meta and @media (prefers-color-scheme); Gmail honours only a light color-scheme and not the media query; classic Outlook for Windows honours neither; Outlook.com/new Outlook honour the meta but expose data-og* attributes instead of the query; Yahoo/AOL rewrite the query to a filtered no-op. New Outlook for Windows renders with WebView2"
    verify: "https://www.caniemail.com/features/html-meta-color-scheme/ ; https://www.caniemail.com/features/css-at-media-prefers-color-scheme/ (firecrawl og:description + WebFetch; WebFetch alone misreads the colour grid) ; https://learn.microsoft.com/en-us/microsoft-365-apps/outlook/overview-new-outlook-windows (2026-09-23; ledger mjml-06..13, mjml-18..19)"
    window_days: 180
    last_verified: 2026-09-23

  - id: mjml-behaviour-and-major
    pack: core/86-email-templates.md
    kind: practice
    claim: "MJML emits Outlook conditional tables and VML backgrounds itself; mj-style goes to <head> unless inline=\"inline\"; mj-raw is the templating pass-through and minification can break on template syntax; RTL is dir on the root <mjml> (direction=\"rtl\" on mj-section only reorders columns); the next major after the one the vendored build pins is breaking (includes opt-in, new minifiers, higher Node floor)"
    verify: "https://documentation.mjml.io/ ; https://github.com/mjmlio/mjml/releases (the release notes of the current major) (brave + WebFetch, 2026-09-23; ledger mjml-01..05, mjml-14..17)"
    window_days: 90
    last_verified: 2026-09-23

  - id: listmonk-one-click-and-features
    pack: core/86-email-templates.md
    kind: practice
    claim: "Listmonk natively sends List-Unsubscribe and List-Unsubscribe-Post: List-Unsubscribe=One-Click (Settings → Privacy) and its unsubscribe endpoint accepts POST; SES bounces arrive at /webhooks/service/ses via an SNS HTTPS subscription; core sends through SES SMTP only (listmonk-messenger adds the SES API); campaigns schedule one send instant, not per-subscriber local time; SMTP server selectable per campaign; AGPL; Go + Postgres"
    verify: "https://listmonk.app/docs/bounces/ ; https://listmonk.app/docs/concepts/ ; https://github.com/knadh/listmonk/commit/ec097909dbe84e8317f4def41806626c636512c2 ; https://github.com/knadh/listmonk/issues/2374 ; install doc names the minimum PostgreSQL (exa + WebSearch, 2026-09-23; ledger listmonk-01..19)"
    window_days: 180
    last_verified: 2026-09-23

  - id: ses-pricing-plans
    pack: core/86-email-templates.md
    kind: practice
    claim: "From 2026-07-21 new SES accounts (and account/region pairs idle since 2025-06-01) start on the Essentials plan at $0.16 per 1,000 emails for the first 10M/month; à la carte outbound is $0.10 per 1,000 plus $0.12/GB attachments; dedicated IP $24.95/mo; VDM $0.07/1,000 plus a query fee under à la carte, included on higher plans; AWS Turkey invoices in TRY with 20% KDV"
    verify: "https://aws.amazon.com/ses/pricing/ (exa + firecrawl query, 2026-09-23) ; https://aws.amazon.com/legal/awstr/ (ledger ses-01, ses-02, ses-08, ses-17, ses-20, self-01)"
    window_days: 90
    last_verified: 2026-09-23

  - id: ses-limits-and-enforcement
    pack: core/86-email-templates.md
    kind: practice
    claim: "SES sandbox: 200 messages/24h, 1/s, verified recipients only; AWS answers a production request within 24 hours; post-sandbox quota varies by use case (50,000/day common); one per-account sending rate for API and SMTP; review at 5% bounces or 0.1% complaints, possible pause at 10% or 0.5%; 40 MB per message (SMTP/current API), 50 recipients; SMTP STARTTLS on 587, TLS wrapper on 465; custom MAIL FROM MX feedback-smtp.<region>.amazonses.com priority 10 and SPF include:amazonses.com; Easy DKIM uses 3 CNAMEs; hold a dedicated IP at ~1,000 emails/day per provider"
    verify: "https://docs.aws.amazon.com/ses/latest/dg/request-production-access.html ; .../quotas.html ; .../faqs-enforcement.html ; .../smtp-connect.html ; .../mail-from.html ; .../dedicated-ip-warming.html (brave + exa + firecrawl; WebFetch times out on docs.aws.amazon.com, 2026-09-23; ledger ses-03..16, ses-19)"
    window_days: 180
    last_verified: 2026-09-23

  - id: iys-commercial-message-rules
    pack: core/86-email-templates.md
    kind: standard
    claim: "İYS: a consent not registered in İYS is invalid; outside-İYS consents registered within 3 business days; opt-outs stop sends within 3 business days; tacir/esnaf need no prior consent but must be registered and checked; ongoing-transaction notices need no consent or İYS check only while promoting nothing; records kept 3 years; under 250,000 addresses the İLETİ API is reached only via an integrator, now Ministry-of-Trade-authorised (2024 communiqué; unauthorised partners lapsed 2025-06-30); İLETİ is an annual tiered fee, KDV-exclusive, two smallest tiers free"
    verify: "https://www.mevzuat.gov.tr/mevzuat?MevzuatNo=20914&MevzuatTur=7&MevzuatTertip=5 (firecrawl; WebFetch fails TLS) ; https://iys.org.tr/hizmet-saglayici/kurumsal-hizmetler ; https://iys.org.tr/entegratorler (exa + firecrawl, 2026-09-23; ledger iys-01..08, iys-16..17)"
    window_days: 180
    last_verified: 2026-09-23

  - id: expo-push-limits
    pack: core/86-email-templates.md
    kind: practice
    claim: "Expo push: one service call reaches APNs and FCM; ≤100 messages per send request; 600 notifications/s per project; ≤1,000 receipt ids per request; receipts cleared after 24 hours; DeviceNotRegistered means drop the token; total payload ≤4 KiB; Android needs an FCM service-account (HTTP v1) credential uploaded to EAS"
    verify: "https://docs.expo.dev/push-notifications/sending-notifications/ ; https://docs.expo.dev/push-notifications/push-notifications-setup/ (firecrawl + WebFetch + exa, 2026-09-23; ledger iys-09..15, iys-18..19)"
    window_days: 180
    last_verified: 2026-09-23

  - id: docker-oom-restart-and-signal
    pack: core/self-healing.md
    kind: practice
    claim: "An OOM kill ends the container's main process (exit 137), so `restart: unless-stopped` restarts it, with a growing back-off between attempts (moby's restart manager); the kill shows as `State.OOMKilled: true` in `docker inspect` and in the host kernel log, NOT in the container's own `docker logs` stream"
    verify: "https://docs.docker.com/engine/containers/start-containers-automatically/ ; moby daemon/internal/restartmanager/restartmanager.go ; https://www.netdata.cloud/guides/docker/docker-oomkilled/ (WebFetch + WebSearch + brave, 2026-09-23; ledger docs/reference/research/2026-09-23-self-healing-currency-ledger.md sh-ext-01, sh-ext-02)"
    window_days: 180
    last_verified: 2026-09-23

  - id: asyncpg-pool-size-api
    pack: core/self-healing.md
    kind: practice
    claim: "asyncpg exposes pool occupancy as methods — get_size(), get_max_size(), get_idle_size() — not `pool.size`/`pool.max` attributes; `acquire()` takes a `timeout`; `Pool.close()` waits for released connections and shuts the pool down"
    verify: "https://magicstack.github.io/asyncpg/current/api/index.html (WebSearch + WebFetch, 2026-09-23; ledger sh-ext-03)"
    window_days: 180
    last_verified: 2026-09-23

  - id: httpx-transport-exceptions
    pack: core/self-healing.md
    kind: practice
    claim: "httpx.TimeoutException (base of Connect/Read/Write/PoolTimeout) and httpx.ConnectError (a NetworkError) are both TransportError subclasses under RequestError"
    verify: "https://github.com/encode/httpx/blob/master/httpx/_exceptions.py ; https://www.python-httpx.org/exceptions/ (WebFetch + exa, 2026-09-23; ledger sh-ext-05)"
    window_days: 180
    last_verified: 2026-09-23

  - id: cgnat-shared-address
    pack: core/self-healing.md
    kind: rationale
    claim: "Carrier-grade NAT puts many subscribers behind one public address (RFC 6598 reserves 100.64.0.0/10 as its shared space), so blocking one IP can block many unrelated users — why network-level IP blocks stay operator-only"
    verify: "https://www.rfc-editor.org/rfc/rfc6598.html (WebSearch + WebFetch, 2026-09-23; ledger sh-ext-07)"
    window_days: 180
    last_verified: 2026-09-23

  - id: claude-code-model-aliases
    pack: ai/00-ai-model-selection.md
    kind: practice
    claim: "Claude Code's `--model` aliases `haiku`, `sonnet`, `opus`, `fable` point to the recommended (newest) model of each family for the provider and update over time — code that selects by alias never goes stale; `best` = what `fable` resolves to where available, else `opus`; `fable` has resolved to Fable 5.1 since v2.1.257 (Fable 5 in Claude apps gateway sessions); no Fable model is ever an account default; on Pro/Max/Team/Enterprise/API the default is Opus 5.5; on Bedrock/Vertex/Foundry some aliases resolve to OLDER models (e.g. `sonnet` → Sonnet 4.5); `opusplan`, `sonnet[1m]` and `default` also exist"
    verify: "https://code.claude.com/docs/en/model-config.md ; https://code.claude.com/docs/en/cli-reference.md (claude-code-guide agent via WebFetch, 2026-09-23; ledger docs/reference/research/2026-09-23-ai-model-selection-currency-ledger.md cc-01..14, cc-17..19)"
    window_days: 90
    last_verified: 2026-09-23

  - id: claude-model-lineup-ids
    pack: ai/00-ai-model-selection.md
    kind: version
    claim: "Current Claude API ids: claude-fable-5-1, claude-opus-5-5, claude-sonnet-5, claude-haiku-4-5-20251001 (API alias claude-haiku-4-5); API list price per MTok input/output: Fable 10/50, Opus 4/20, Sonnet 2/10, Haiku 1/5 USD. Claude Mythos 5 (claude-mythos-5) shares Fable 5's capabilities without its safety classifiers, only via Project Glasswing, and has no Claude Code alias. The ids are mirrored in .windsurf/rules/versions.yaml (claude_*_id) — refresh both together"
    verify: "https://platform.claude.com/docs/en/about-claude/models/overview (firecrawl query + WebFetch, 2026-09-23) ; https://platform.claude.com/docs/en/models/fable-5/introducing-claude-fable-5-and-claude-mythos-5.md (ledger cc-15, cc-16, cc-26..30, self-01)"
    window_days: 90
    last_verified: 2026-09-23

  - id: claude-p-json-envelope
    pack: ai/00-ai-model-selection.md
    kind: practice
    claim: "`claude -p --output-format json` returns result, session_id, total_cost_usd and modelUsage (per-model; preferred for accounting — `usage` covers the main loop only); both cost figures are client-side estimates, not a bill; with `--json-schema` the conforming output is in `structured_output`"
    verify: "https://code.claude.com/docs/en/headless.md ; https://code.claude.com/docs/en/agent-sdk/typescript.md (WebFetch, 2026-09-23; ledger cc-20..25)"
    window_days: 180
    last_verified: 2026-09-23

  - id: claude-subscription-beats-metered
    pack: ai/00-ai-model-selection.md
    kind: rationale
    claim: "Per token, `claude -p` on the Max subscription costs less than nearly every metered model: the hub cost sidecar's amortized rate (fee ÷ tokens over a rolling 30-day window, 4 accounts) sat around 0.013 USD per MTok on 2026-09-23 — undercut only by free tiers and a priced micro-model (27 of 410 active catalog rows below it) — against Anthropic API list prices of 1–10 USD per MTok input; the operator's standing measurement"
    verify: "/opt/fabrik/scripts/kilo-benchmarks/claude_p_cost.json (`amortized_per_mtok`, `window_start`/`window_end`, rebuilt by scripts/claude_p_cost.py) + https://platform.claude.com/docs/en/about-claude/pricing — re-read both; the claim holds while the amortized rate stays below the cheapest metered route in the bake-off browser"
    window_days: 90
    last_verified: 2026-09-23

  - id: core-web-vitals-thresholds
    pack: saas/60-saas-ui.md
    kind: practice
    claim: "Core Web Vitals 'good' thresholds are LCP ≤ 2.5 s, INP ≤ 200 ms and CLS ≤ 0.1, assessed at the 75th percentile of field page loads, segmented mobile/desktop"
    verify: "https://web.dev/articles/vitals (WebFetch + brave, 2026-09-23; ledger ui-01..04)"
    window_days: 180
    last_verified: 2026-09-23

  - id: lighthouse-lab-has-no-inp
    pack: saas/60-saas-ui.md
    kind: practice
    claim: "A Lighthouse navigation run (what Lighthouse CI runs) cannot measure INP — it needs a user interaction; the lab responsiveness proxy is Total Blocking Time, and INP is measured only in a timespan run or a scripted user flow (or in the field)"
    verify: "https://developer.chrome.com/blog/inp-tools-2022 ; https://developer.chrome.com/docs/lighthouse/performance/lighthouse-total-blocking-time (WebFetch + exa, 2026-09-23; ledger ui-10..13)"
    window_days: 180
    last_verified: 2026-09-23

  - id: wcag22-aa-criteria
    pack: saas/60-saas-ui.md
    kind: standard
    claim: "WCAG 2.2 is the current W3C Recommendation (WCAG 3.0 is still a Working Draft); the pack cites 2.4.11 Focus Not Obscured (Minimum, AA), 2.5.8 Target Size (Minimum, AA, 24×24 CSS px or spacing), 3.3.8 Accessible Authentication (Minimum, AA) and 3.3.7 Redundant Entry (Level A)"
    verify: "https://www.w3.org/WAI/standards-guidelines/wcag/ ; https://www.w3.org/TR/wcag-3.0/ ; the Understanding pages under https://www.w3.org/WAI/WCAG22/Understanding/ ; https://www.w3.org/WAI/standards-guidelines/wcag/new-in-22/ (exa + brave + WebFetch, 2026-09-23; ledger ui-05..09, op-04)"
    window_days: 365
    last_verified: 2026-09-23

  - id: nextjs-font-self-hosting
    pack: saas/60-saas-ui.md
    kind: practice
    claim: "`next/font/google` downloads font CSS and files at build time and self-hosts them, so the browser sends no requests to Google (`display` defaults to 'swap'); App Router layouts and pages are Server Components by default"
    verify: "https://nextjs.org/docs/app/api-reference/components/font ; https://nextjs.org/docs/app/getting-started/server-and-client-components (WebFetch, 2026-09-23; ledger ui-14..16)"
    window_days: 180
    last_verified: 2026-09-23

  - id: tailwind-v4-css-first
    pack: saas/60-saas-ui.md
    kind: version
    claim: "Tailwind CSS v4 is configured CSS-first (`@theme`), with no auto-detected `tailwind.config.js` (a JS config loads only via `@config`); theme values are CSS variables; breakpoints ship `max-*` variants — the saas-skeleton scaffold still emits Tailwind v3 with a `tailwind.config.ts`"
    verify: "https://tailwindcss.com/docs/upgrade-guide ; https://tailwindcss.com/docs/responsive-design (WebFetch, 2026-09-23; ledger ui-17..20) + emit a saas-skeleton and read package.json"
    window_days: 90
    last_verified: 2026-09-23

  - id: aria-apg-dialog-tooltip
    pack: saas/60-saas-ui.md
    kind: standard
    claim: "WAI-ARIA APG: a modal dialog container has role=dialog and aria-modal=true, traps Tab focus and closes on Escape; a tooltip has role=tooltip, is referenced by its trigger's aria-describedby and dismisses on Escape"
    verify: "https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/ ; https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/ (WebFetch, 2026-09-23; ledger ui-21..25)"
    window_days: 365
    last_verified: 2026-09-23

  - id: paddle-customer-portal
    pack: saas/60-saas-ui.md
    kind: practice
    claim: "Paddle Billing includes a Paddle-hosted customer portal by default (invoices, subscription management, payment details); an authenticated deep link is minted per customer via POST /customers/{customer_id}/portal-sessions, and sessions are temporary and must not be cached"
    verify: "https://developer.paddle.com/concepts/sell/customer-portal/ ; https://developer.paddle.com/api-reference/customer-portals/create-customer-portal-session/ (exa + WebSearch, 2026-09-23; ledger ui-26..28)"
    window_days: 180
    last_verified: 2026-09-23

  - id: audit-retention-legal-periods
    pack: core/app-audit-log.md
    kind: standard
    claim: "TR commercial records (and the documents behind them, incl. invoices) are kept 10 years under TTK Art. 82 — VUK Art. 253's 5 years is only the tax floor; GDPR Art. 7(1) requires the controller to be able to demonstrate consent and Art. 5(2) to demonstrate compliance; KVKK Art. 7(1) requires personal data to be deleted, destroyed or anonymised once the reason for processing ends — so audit rows are kept for a legal period, never indefinitely"
    verify: "https://www.turkhukuksitesi.com/mevzuat.php?mid=11233 (TTK 82) ; TÜRMOB VUK 253 note (ledger al-04/al-07) ; https://gdpr-info.eu/art-7-gdpr/ ; https://www.mevzuat.gov.tr/MevzuatMetin/1.5.6698.pdf (exa + brave, 2026-09-23; ledger al-01..al-09)"
    window_days: 365
    last_verified: 2026-09-23

  - id: paddle-transaction-completed-shape
    pack: core/app-audit-log.md
    kind: practice
    claim: "Paddle `transaction.completed`: `subscription_id` is nullable (one-time purchases), `details.totals.total` is a string in the currency's lowest unit, the currency is `currency_code`, every envelope carries an `event_id` usable for idempotency, and live deliveries are retried up to 60 times over 3 days"
    verify: "https://developer.paddle.com/webhooks/transactions/transaction-completed ; https://developer.paddle.com/webhooks/about/respond-to-webhooks (exa + WebFetch, 2026-09-23; ledger al-10..al-14)"
    window_days: 180
    last_verified: 2026-09-23

  - id: audit-chain-advisory-lock
    pack: core/app-audit-log.md
    kind: standard
    claim: "`pg_advisory_xact_lock(bigint)` takes an exclusive transaction-level lock, released only at transaction end, with no table privilege required — so it serialises audit-chain writers on an append-only table where `SELECT … FOR UPDATE` (which needs UPDATE) cannot"
    verify: "https://www.postgresql.org/docs/current/functions-admin.html (WebFetch + brave, 2026-09-23; ledger al-17..al-18) + /opt/fabrik-lib/app-audit-log/audit_log.py record_event docstring"
    window_days: 365
    last_verified: 2026-09-23

  - id: audit-owner-role-cannot-be-append-only
    pack: core/app-audit-log.md
    kind: standard
    claim: "A PostgreSQL object's owner holds every privilege on it and can always re-grant them, so REVOKE UPDATE/DELETE cannot bind an owner role — and Fabrik's registrar makes each app's role the database owner, so the audit table is tamper-evident, not append-only, until a separate non-owning app role is provisioned"
    verify: "https://www.postgresql.org/docs/current/ddl-priv.html (WebFetch, 2026-09-23; ledger op-01) + /opt/fabrik/src/fabrik/drivers/postgres.py (the ALTER DATABASE … OWNER TO line)"
    window_days: 365
    last_verified: 2026-09-23

  - id: paddle-seller-refund-requirements
    pack: saas/88-saas-launch-checklist.md
    kind: practice
    claim: "Paddle requires sellers to show terms, refund policy and buyer-support contact on their website and to have the buyer accept terms and refund policy before purchase, and the Terms to carry the seller's legal name and the 'online reseller Paddle.com … Merchant of Record' text; account verification requires a pricing page, ToS, Privacy Policy and Refund Policy; Paddle's own buyer refund policy gives a 14-day window (statutory in EU/EEA, UK, Turkey and others; 7 days in some markets; the 5-day unconditional right is Singapore-only) — there is no universal Paddle '5-day unconditional refund' rule"
    verify: "https://www.paddle.com/seller-guides/seller-handbook ; https://www.paddle.com/legal/refund-policy ; https://www.paddle.com/paddle-101 (exa + WebFetch + brave, 2026-09-24; ledger pd-01..pd-09, op-01)"
    window_days: 180
    last_verified: 2026-09-24

  - id: paddle-dispute-rate-and-fee
    pack: saas/88-saas-launch-checklist.md
    kind: practice
    claim: "Under Paddle as merchant of record the seller bears the chargeback amount plus a fee of 20 USD/GBP/EUR or 40 CAD/AUD that Paddle keeps even when the dispute is won (help centre and handbook; MSA 10.4 caps it at 'up to' that); the help centre sets the acceptable rate below 0.65% of monthly transaction value with won disputes counted (the handbook: 0.1-0.3% average, above 0.75% unacceptable), and Paddle may act at its discretion on an excessive rate (MSA 10.3)"
    verify: "https://www.paddle.com/legal/terms ; https://www.paddle.com/seller-guides/seller-handbook ; https://www.paddle.com/help/manage/risk-prevention/understanding-chargebacks-with-paddle (exa + WebFetch, 2026-09-24; ledger pd-12..pd-14, op-03, op-05)"
    window_days: 180
    last_verified: 2026-09-24

  - id: ccpa-thresholds-and-optout-link
    pack: saas/88-saas-launch-checklist.md
    kind: standard
    claim: "CCPA applies to a business with revenue over $25M CPI-adjusted ($26,625,000 from 2025), OR that buys/sells/shares personal information of 100,000+ consumers or households, OR that earns 50%+ of revenue from selling/sharing it (Civ. Code 1798.140(d)); the 'Do Not Sell or Share' link is owed only by a business that sells or shares, one that does not says so in its privacy policy (11 CCR 7013(g)); a 'Your Privacy Choices' link or frictionless opt-out-signal processing may replace it (7013(d), 7015, 7025)"
    verify: "https://leginfo.legislature.ca.gov/faces/codes_displaySection.xhtml?lawCode=CIV&sectionNum=1798.140 ; https://cppa.ca.gov/regulations/cpi_adjustment.html ; https://www.law.cornell.edu/regulations/california/11-CCR-7013 (exa, 2026-09-24; ledger cc-01..cc-09). Next CPI adjustment due 2027"
    window_days: 365
    last_verified: 2026-09-24

  - id: eu-consent-cookies-and-fingerprinting
    pack: saas/88-saas-launch-checklist.md
    kind: standard
    claim: "Non-essential cookies and similar storage need prior opt-in consent and a pre-ticked box is not consent (CJEU C-673/17 Planet49); strictly necessary storage is exempt (ePrivacy Art. 5(3)); device fingerprinting falls within Art. 5(3) (EDPB Guidelines 2/2023, which leave the exemptions to member states); the open-source FingerprintJS library is MIT again since v5 (Oct 2025), after BSL in v4"
    verify: "https://www.edpb.europa.eu/system/files/2024-10/edpb_guidelines_202302_technical_scope_art_53_eprivacydirective_v2_en_0.pdf ; https://github.com/fingerprintjs/fingerprintjs/blob/master/LICENSE ; Planet49 C-673/17 (exa + WebSearch, 2026-09-24; ledger cc-10..cc-18)"
    window_days: 365
    last_verified: 2026-09-24

  - id: kvkk-verbis-exemption
    pack: saas/88-saas-launch-checklist.md
    kind: standard
    claim: "A Turkey-resident data controller is exempt from VERBIS registration with fewer than 50 employees AND an annual balance sheet under 100M TRY, if its main activity is not special-category processing (Board decisions 2018/87, 2023/1154, 2025/1572; non-balance-sheet bookkeepers on employee count alone, 2025/2393); controllers resident abroad register regardless of size; a controller that becomes obliged registers within 30 days"
    verify: "https://www.kvkk.gov.tr/Icerik/8577 ; https://www.kvkk.gov.tr/Icerik/7646 ; https://verbis.kvkk.gov.tr/sharedFolder/sorularla-verbis.pdf (exa + WebSearch + brave, 2026-09-24; ledger kv-01..kv-08)"
    window_days: 365
    last_verified: 2026-09-24

  - id: tr-kdv-teknokent-software
    pack: saas/88-saas-launch-checklist.md
    kind: standard
    claim: "KDV Kanunu Geçici 20/1 exempts deliveries and services of listed application software produced exclusively in a Teknoloji Geliştirme Bölgesi, to any buyer (domestic included), while the income/corporate-tax exemption runs (4691 Geçici 2: until 31/12/2028); hosted access counts when the IP stays with the TGB firm ('sanal ortamda paylaşım'), while maintenance/support other than updates is excluded (KDV Genel Uygulama Tebliği II/G-2); it is not an export regime (export is md. 11/1-a + 12/2); outside both, the standard rate is 20% (Decree 7346)"
    verify: "https://vergipedia.com/degerlendirme/teknoloji-gelistirme-bolgelerindeki-teslim-ve-hizmetlerde-kdv-uygulamasi/ ; https://www.resmigazete.gov.tr/eskiler/2023/07/20230707-11.pdf ; 4691 Geçici 2 text (WebSearch + brave + exa + WebFetch, 2026-09-24; ledger kd-01..kd-08, op-12). Re-check before 31/12/2028 — the exemption's end date"
    window_days: 365
    last_verified: 2026-09-24

  - id: claude-code-total-cost-is-an-estimate
    pack: core/cost-budget.md
    kind: standard
    claim: "Claude Code's `--output-format json` result carries `total_cost_usd` and `usage`; the cost is a client-side estimate computed from a bundled price table, not billed spend — on a Pro/Max subscription it is not a billing figure, and Anthropic says not to trigger financial decisions from it (it still measures quota burn at API list prices)"
    verify: "https://code.claude.com/docs/en/headless ; https://code.claude.com/docs/en/agent-sdk/cost-tracking ; https://code.claude.com/docs/en/costs (exa + WebSearch, 2026-09-24; ledger cc-01, cc-05..cc-07)"
    window_days: 180
    last_verified: 2026-09-24

  - id: claude-code-print-mode-caps
    pack: core/cost-budget.md
    kind: practice
    claim: "Claude Code print mode has `--max-budget-usd` (stop before spending more than N dollars on API calls, subagents included) and `--max-turns` (limit agentic turns, no limit by default)"
    verify: "https://code.claude.com/docs/en/cli-reference (exa, 2026-09-24; ledger cc-08, cc-09)"
    window_days: 180
    last_verified: 2026-09-24

  - id: openrouter-usage-and-key-limits
    pack: core/cost-budget.md
    kind: practice
    claim: "OpenRouter returns token counts and cost on every response (the `usage: {include: true}` flag is deprecated and has no effect), and an API key can carry a credit limit (`limit`, `limit_remaining`, `limit_reset`) — an exhausted key gets 402 until the limit is raised or resets"
    verify: "https://openrouter.ai/docs/cookbook/administration/usage-accounting ; https://openrouter.ai/docs/api_reference/limits (exa + WebFetch + WebSearch, 2026-09-24; ledger or-06, or-10, op-06)"
    window_days: 180
    last_verified: 2026-09-24

  - id: llm-verbalized-confidence-overconfident
    pack: core/cost-budget.md
    kind: rationale
    claim: "An LLM's verbalized (self-rated) confidence is overconfident in absolute terms — expected calibration error around 0.1 for 70B+ models — and depends on how it is elicited, even though it beats raw token probabilities; so a confidence threshold alone is not a safe escalation trigger"
    verify: "https://arxiv.org/pdf/2306.13063 ; https://arxiv.org/pdf/2412.14737v1.pdf ; https://doi.org/10.18653/v1/2023.emnlp-main.330 (exa + brave, 2026-09-24; ledger or-07..or-09)"
    window_days: 365
    last_verified: 2026-09-24

  - id: watchdog-default-fallback-model-retired
    pack: core/cost-budget.md
    kind: version
    claim: "Claude Haiku 3.5 is retired on Anthropic's API (still on Bedrock and Google Cloud) and OpenRouter lists no endpoint for `anthropic/claude-3.5-haiku`, so the watchdog's default OpenRouter fallback model cannot serve; `anthropic/claude-haiku-4.5` is the current Haiku"
    verify: "https://platform.claude.com/docs/en/about-claude/pricing (WebFetch, 2026-09-24) ; OpenRouter public model list + endpoints (second-opinion probe, 2026-09-24; ledger op-01, op-02)"
    window_days: 90
    last_verified: 2026-09-24
