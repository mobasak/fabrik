# Rules-corpus CLAIMS REGISTER — every external-world assertion the packs rely on,
# as a dated, re-verifiable row. (Rules pass 2026-09-01: the drift that matters is
# CLAIMS, not version strings — the Alpine rationale went silently false while its
# rule stayed right; no regex sees that.)
#
# CONTRACT
# - Grown file-by-file by the rules currency pass: evaluating a pack extracts its
#   external claims here, dated, with the source that grounded them.
# - `rules_currency_watch.py --claims` (weekly cron rider) mails infra every claim
#   whose last_verified is older than its window — the mail carries the verify
#   hints, so the receiving session re-researches EXACTLY the stale claims and
#   bumps the dates (or fixes the pack when the world moved).
# - Version LITERALS (python:X.Y, node:N) are additionally auto-watched against
#   endoflife.date by the same script — kind: version rows marked `auto: true`
#   page within a week of an upstream release, not at window expiry.
# - A claim REFUTED by re-verification = fix the pack in the same change and
#   supersede the row (new row, old one gets `superseded: <date>`), mirroring the
#   decision-ledger discipline.
#
# window_days guidance: 90 fast-moving (versions, tool landscape) · 180 practices
# and rationales · 365 stable standards/deprecations.

claims:
  - id: py-current-stable
    pack: core/10-python.md
    claim: "Python current stable is 3.14 (packs pin python:3.14)"
    kind: version
    auto: true            # endoflife.date watcher pages on release, not window
    verify: "https://endoflife.date/api/python.json"
    window_days: 90
    last_verified: 2026-09-01

  - id: uv-standard
    pack: core/10-python.md
    claim: "uv is the mandated package manager AND the industry-standard choice (1.0 shipped, broad production adoption; pip/poetry relegated)"
    kind: practice
    verify: "search: python package manager standard <year>; https://docs.astral.sh/uv/"
    window_days: 180
    last_verified: 2026-09-01

  - id: fastapi-lifespan
    pack: core/10-python.md
    claim: "@app.on_event is deprecated; lifespan context manager is the current FastAPI idiom"
    kind: deprecation
    verify: "https://fastapi.tiangolo.com/advanced/events/"
    window_days: 365
    last_verified: 2026-09-01

  - id: pydantic-settings-v2
    pack: core/10-python.md
    claim: "pydantic-settings BaseSettings + SettingsConfigDict is the current idiom; no pydantic v3 breaking change shipped"
    kind: version
    verify: "https://docs.pydantic.dev/latest/concepts/pydantic_settings/"
    window_days: 180
    last_verified: 2026-09-01

  - id: alpine-rationale
    pack: core/10-python.md
    claim: "Alpine ban rationale: musllinux wheels exist (PEP 656) but coverage partial, source builds ~50x slower, musl allocator/stack degrades CPython — the ban still pays"
    kind: rationale
    verify: "search: python alpine musllinux wheels coverage <year>; https://pythonspeed.com/articles/alpine-docker-python/"
    window_days: 180
    last_verified: 2026-09-01

  - id: sqlalchemy2-text
    pack: core/10-python.md
    claim: "SQLAlchemy 2.0 requires text() for raw SQL strings in execute()"
    kind: deprecation
    verify: "https://docs.sqlalchemy.org/en/20/changelog/migration_20.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: rfc9457-problem-details
    pack: core/10-python.md, core/15-api-contracts.md
    claim: "RFC 9457 ProblemDetails is the current HTTP error-payload standard (obsoletes 7807)"
    kind: standard
    verify: "https://www.rfc-editor.org/rfc/rfc9457"
    window_days: 365
    last_verified: 2026-09-01

  - id: debian-slim-variant
    pack: core/30-ops.md
    claim: "Fleet pins -slim-bookworm; Docker's bare -slim now defaults to trixie (Debian 13 stable) — flip is a 5-pack class commit owned by 30-ops"
    kind: version
    auto: true
    verify: "https://hub.docker.com/_/python/tags?name=slim"
    window_days: 90
    last_verified: 2026-09-01
    superseded: 2026-09-01   # flip EXECUTED (D-064) — and the bare-slim clause was over-broad: true for python, FALSE for node (24-slim digest == 24-bookworm-slim, proven 2026-09-01). Successor: debian-pin-trixie

  - id: uv-copy-pinned
    pack: core/30-ops.md
    claim: "Astral's documented Docker pattern is COPY --from=ghcr.io/astral-sh/uv:<pin> with an exact version pin (uv_version span, 0.12.8 at write); unpinned pip-install-uv re-resolves a build tool per build"
    kind: practice
    verify: "https://docs.astral.sh/uv/guides/integration/docker/; https://pypi.org/pypi/uv/json"
    window_days: 90
    last_verified: 2026-09-01

  - id: redis-fleet-major
    pack: core/30-ops.md
    claim: "redis-main runs redis:7-alpine (probed live 2026-09-01); redis 7.x active support ended 2025-05, security-only to 2029-12; current major is 8.x — deliberate fleet state, upgrade is a fleet infra decision with a 2029 horizon"
    kind: version
    verify: "https://endoflife.date/redis; ssh vps sudo docker inspect redis-main --format '{{.Config.Image}}'"
    window_days: 180
    last_verified: 2026-09-01

  - id: node-lts-line
    pack: core/12-node.md, core/20-typescript.md
    claim: "Packs pin node:24 via the node_lts span (sole Active LTS; 22 is Maintenance, EOL Apr 2027); node 26 becomes LTS 2026-10-28 — the auto-watcher pages then"
    kind: version
    auto: true
    verify: "https://endoflife.date/api/nodejs.json"
    window_days: 90
    last_verified: 2026-09-01

  - id: glitchtip-5xx-capture
    pack: core/55-observability.md
    claim: "VERIFIED IN SOURCE 2026-09-01 (55-observability's turn): sentry-sdk captures deliberately raised HTTPException via failed_request_status_codes, default _DEFAULT_FAILED_REQUEST_STATUS_CODES = frozenset(range(500,600)) — i.e. 5xx ONLY (4xx captured by nothing), matched DUCK-TYPED on .status_code (not HTTPException-typed), recorded as handled: True (not an unhandled crash). Stable since sentry-sdk 2.15; fleet pins >=2.18. Basis of 10-python's no-logger.exception rule."
    kind: rationale
    verify: "sentry_sdk/integrations/__init__.py::_DEFAULT_FAILED_REQUEST_STATUS_CODES + starlette.py::patch_exception_middleware; https://docs.sentry.io/platforms/python/integrations/fastapi/"
    window_days: 365
    last_verified: 2026-09-01

  - id: ts-strip-types-stable
    pack: core/12-node.md
    claim: "Node type stripping is stable and enabled by default (run .ts directly; ts-node/tsx obsolete for dev; opt-out flag exists)"
    kind: deprecation
    verify: "https://nodejs.org/api/typescript.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: express5-npm-latest
    pack: core/12-node.md
    claim: "Express current major (5.x line) is npm `latest` since 2025; legacy major maintenance-only — bare `npm i express` installs the current major"
    kind: version
    verify: "https://www.npmjs.com/package/express"
    window_days: 180
    last_verified: 2026-09-01

  - id: fastify-current-major
    pack: core/12-node.md
    claim: "Fastify 5.x is the current major (prior major EOL mid-2025); greenfield default stands"
    kind: version
    verify: "https://fastify.dev/docs/latest/Reference/LTS/"
    window_days: 180
    last_verified: 2026-09-01

  - id: fastify-throughput-4x
    pack: core/12-node.md
    claim: "Fastify sustains roughly 4x Express throughput on JSON APIs (order-of-magnitude decision aid, not a benchmark)"
    kind: rationale
    verify: "search: fastify express benchmark <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: npm-install-scripts-vector
    pack: core/12-node.md
    claim: "Install scripts are the dominant npm supply-chain vector (Shai-Hulud worm class; Mastra easy-day-js typosquat 2026-06: 140+ versions backdoored via postinstall); npm's current major blocks dependency install scripts by default; `npm ci --ignore-scripts` stays explicit"
    kind: practice
    verify: "https://github.com/lirantal/npm-security-best-practices; https://www.stepsecurity.io/blog/mastra-npm-packages-compromised-using-easy-day-js"
    window_days: 180
    last_verified: 2026-09-01

  - id: node-cve-trio-2026
    pack: core/12-node.md
    claim: "CVE-2026-21713 (HMAC non-constant-time compare), CVE-2026-21710 (crafted __proto__ header crash), CVE-2026-21637 (TLS SNI-callback DoS) — all fixed in Node's 2026-03-24 security release; basis of the patch-floor, timingSafeEqual, header-allowlist and TLS-at-Traefik rules"
    kind: rationale
    verify: "https://nodejs.org/en/blog/vulnerability/march-2026-security-releases"
    window_days: 365
    last_verified: 2026-09-01

  - id: als-contextframe-negligible
    pack: core/12-node.md
    claim: "AsyncContextFrame is the default AsyncLocalStorage implementation in current LTS and makes ALS overhead negligible (~2% vs manual passing; large gains vs async_hooks)"
    kind: rationale
    verify: "https://blog.platformatic.dev/the-hidden-cost-of-context"
    window_days: 180
    last_verified: 2026-09-01

  - id: esm-tsc-default
    pack: core/12-node.md
    claim: "Node TSC consensus: ESM is the definitive default; ESM-only package set keeps growing (chalk, execa, …)"
    kind: practice
    verify: "search: node esm default cjs <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: docker-stop-grace-default
    pack: core/12-node.md
    claim: "Docker's default stop grace period is 10s; the 20s drain backstop works only under the scaffold's stop_grace_period: 45s (scaffold.py emits it)"
    kind: rationale
    verify: "https://docs.docker.com/reference/cli/docker/container/stop/"
    window_days: 365
    last_verified: 2026-09-01

  - id: idempotency-key-header-standard
    pack: core/15-api-contracts.md
    claim: "Idempotency-Key (un-prefixed) is the IETF httpapi standards-track header name (draft-ietf-httpapi-idempotency-key-header, active at -07); not yet an RFC — re-verify for publication or abandonment"
    kind: standard
    verify: "https://datatracker.ietf.org/doc/draft-ietf-httpapi-idempotency-key-header/"
    window_days: 180
    last_verified: 2026-09-01
    superseded: 2026-09-01   # REFUTED same day: the draft EXPIRED at -07 (IESG state Expired) — caught by the file-3 second opinion's datatracker/history check

  - id: idempotency-key-header-consensus
    pack: core/15-api-contracts.md
    claim: "Idempotency-Key (un-prefixed) is the industry-CONSENSUS header name (Stripe/PayPal practice); the IETF httpapi draft defining it EXPIRED at -07 without becoming an RFC — re-verify for revival or publication"
    kind: standard
    verify: "https://datatracker.ietf.org/doc/draft-ietf-httpapi-idempotency-key-header/history/"
    window_days: 180
    last_verified: 2026-09-01

  - id: rfc9745-deprecation-header
    pack: core/15-api-contracts.md
    claim: "Deprecation header is RFC 9745 (2025-03) with a structured DATE value (@unix-timestamp), obsoleting the draft-era boolean form; Sunset is RFC 8594"
    kind: standard
    verify: "https://www.rfc-editor.org/info/rfc9745/"
    window_days: 365
    last_verified: 2026-09-01

  - id: hey-api-openapi-ts-current
    pack: core/15-api-contracts.md
    claim: "@hey-api/openapi-ts is the current OpenAPI→TS codegen (used by Vercel/PayPal, ~4.5M weekly); still pre-1.0 — 'pin exact version' is upstream's own guidance"
    kind: practice
    verify: "https://www.npmjs.com/package/@hey-api/openapi-ts"
    window_days: 180
    last_verified: 2026-09-01

  - id: oasdiff-current
    pack: core/15-api-contracts.md
    claim: "oasdiff is maintained (v1.26.x, 2026-07) and `oasdiff breaking --fail-on ERR` remains the right invocation; supports OpenAPI 3.0/3.1/3.2"
    kind: practice
    verify: "https://github.com/oasdiff/oasdiff/releases"
    window_days: 365
    last_verified: 2026-09-01

  - id: ts-native-compiler-current
    pack: core/20-typescript.md
    claim: "The current `typescript` npm major ships the native Go compiler as tsc (GA 2026-07, ~8-12x builds; same syntax, tsc --noEmit verbatim); programmatic API lands next minor — API-dependent toolchains (typescript-eslint, framework plugins) pin the previous major meanwhile"
    kind: version
    verify: "https://devblogs.microsoft.com/typescript/; https://www.npmjs.com/package/typescript"
    window_days: 90
    last_verified: 2026-09-01

  - id: zod4-stable
    pack: core/20-typescript.md
    claim: "zod 4 is the stable root export (4.5 current); z.object().parse(process.env) + z.coerce/default idioms unchanged from the pack's example"
    kind: version
    verify: "https://zod.dev/"
    window_days: 180
    last_verified: 2026-09-01

  - id: erasable-syntax-only-recommended
    pack: core/20-typescript.md
    claim: "erasableSyntaxOnly + verbatimModuleSyntax are in Node's officially recommended tsconfig for natively-run TS; native stripping refuses ALL enums/namespaces-with-code/parameter-properties"
    kind: practice
    verify: "https://nodejs.org/api/typescript.html"
    window_days: 180
    last_verified: 2026-09-01

  - id: baseurl-hard-error
    pack: core/20-typescript.md
    claim: "baseUrl is a hard error in the current TS major; paths works standalone with relative mappings"
    kind: deprecation
    verify: "https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/"
    window_days: 365
    last_verified: 2026-09-01

  - id: stdlib-uuid7
    pack: core/25-data-postgres.md
    claim: "uuid.uuid7() is stdlib as of Python 3.14 (RFC 9562; merged 2025-03) and returns plain uuid.UUID — the uuid_utils.compat idiom is legacy-python-only"
    kind: version
    verify: "https://docs.python.org/3/library/uuid.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: pg-fleet-major
    pack: core/25-data-postgres.md
    claim: "postgres-main runs major 16 (agents-fabrik.md:165; EOL 2028-11) — predates native uuidv7() (PG18, 2025-09-25), so app-side generation is the fleet default. TRIPWIRE: when postgres_major flips >=18, rewrite § Primary Keys (DEFAULT uuidv7() becomes the fleet default) — the 'predates it' sentence goes false on flip"
    kind: version
    verify: "https://endoflife.date/api/postgresql.json; grep 'PostgreSQL 16' /opt/fabrik/agents-fabrik.md"
    window_days: 180
    last_verified: 2026-09-01

  - id: pgbouncer-prepared-statements
    pack: core/25-data-postgres.md
    claim: "PgBouncer supports protocol-level named prepared statements in transaction mode via max_prepared_statements (since 1.21, late 2023; default 0/off); client-side the safe fallback is BOTH statement_cache_size=0 (asyncpg) and prepared_statement_cache_size=0 (SQLAlchemy dialect LRU)"
    kind: practice
    verify: "https://www.pgbouncer.org/config.html; https://docs.sqlalchemy.org/en/20/dialects/postgresql.html (asyncpg Prepared Statement Cache)"
    window_days: 180
    last_verified: 2026-09-01
    superseded: 2026-09-01   # 'default 0/off' REFUTED same day against pgbouncer.org primary: current releases default 200 (ON) — second-opinion catch

  - id: pgbouncer-prepared-statements-v2
    pack: core/25-data-postgres.md
    claim: "PgBouncer tracks protocol-level named prepared statements in transaction+statement pooling via max_prepared_statements, DEFAULT 200 (ON) in current releases (feature since 1.21); SQL-level PREPARE untracked; client-side fallback for non-tracking poolers = statement_cache_size=0 AND prepared_statement_cache_size=0"
    kind: practice
    verify: "https://www.pgbouncer.org/config.html; https://docs.sqlalchemy.org/en/20/dialects/postgresql.html (asyncpg Prepared Statement Cache)"
    window_days: 180
    last_verified: 2026-09-01

  - id: debian-pin-trixie
    pack: core/30-ops.md (+ debian_codename span users)
    claim: "Fleet Debian pin flipped bookworm->trixie 2026-09-01: bookworm regular security ENDED 2026-07-12 (LTS to 2028-06); trixie stable 2025-08-09, full support to 2028-08; python -slim-trixie + node trixie-slim images live on Docker Hub (amd64)"
    kind: version
    verify: "https://www.debian.org/releases/trixie/; https://hub.docker.com/_/python/tags?name=slim-trixie"
    window_days: 180
    last_verified: 2026-09-01

  - id: pgvector-not-installed
    pack: core/25-data-postgres.md
    claim: "postgres-main runs postgres:16-alpine with NO pgvector extension (probe: ssh vps sudo docker exec postgres-main psql -U postgres -tc 'select extname from pg_extension' -> plpgsql only); the pgvector/pgvector image is documented but not deployed — vector-needing projects must request the infra change"
    kind: rationale
    verify: "the probe command in the claim, run live"
    window_days: 180
    last_verified: 2026-09-01

  - id: jwt-alg-practice
    pack: core/35-security-auth.md
    claim: "HS256 with a 256-bit secret remains sound for issuer==verifier (Pattern A); 2026 consensus prefers EdDSA/ES256 over RSA for multi-verifier; algorithm pinning in the verifier (explicit allow-list, never header-dispatched) is the mandatory confusion-attack defense"
    kind: practice
    verify: "https://cheatsheetseries.owasp.org/cheatsheets/JSON_Web_Token_for_Java_Cheat_Sheet.html; search: jwt signing algorithm best practice <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: nextjs-middleware-cve
    pack: core/35-security-auth.md
    claim: "CVE-2025-29927 (x-middleware-subrequest header bypass, CVSS 9.1, patched 2025-03 in 12.3.5/13.5.9/14.2.25/15.2.3) is the evidence base for the never-middleware-only rule; the rule outlives the patch"
    kind: rationale
    verify: "https://securitylabs.datadoghq.com/articles/nextjs-middleware-auth-bypass/"
    window_days: 365
    last_verified: 2026-09-01

  - id: xfo-obsolete
    pack: core/35-security-auth.md
    claim: "X-Frame-Options is formally obsolete (MDN, OWASP Clickjacking cheat sheet); CSP frame-ancestors is the modern control and wins when both are present — ship both, frame-ancestors primary"
    kind: standard
    verify: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/X-Frame-Options; https://cheatsheetseries.owasp.org/cheatsheets/Clickjacking_Defense_Cheat_Sheet.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: nextjs-proxy-rename
    pack: core/35-security-auth.md
    claim: "Next.js 16 renamed middleware.ts to proxy.ts (request-shaping, not a security boundary) and SILENTLY IGNORES a leftover middleware.ts at build — nonce/redirect logic stops with no error; the pack's file references track this"
    kind: deprecation
    verify: "https://nextjs.org/docs/app/guides/content-security-policy; https://github.com/vercel/next.js/discussions/84842"
    window_days: 180
    last_verified: 2026-09-01

  - id: supabase-signing-keys
    pack: core/35-security-auth.md
    claim: "Supabase new projects default to asymmetric JWT signing (ES256) since 2025-10; getClaims() verifies locally via JWKS and falls back server-side for legacy HS256; JWKS endpoint EMPTY on HS256 projects (the pack's Pattern-B guidance rests on all three)"
    kind: version
    verify: "https://supabase.com/docs/guides/auth/signing-keys"
    window_days: 180
    last_verified: 2026-09-01

  - id: argon2id-lib-defaults
    pack: core/35-security-auth.md
    claim: "OWASP mandates Argon2id specifically (min m=19MiB/t=2/p=1); the vendored lib's argon2-cffi defaults (Argon2id, 64MiB/t=3/p=4) exceed the minimums — naming 'Argon2id + lib defaults' suffices, no cost table needed in the pack"
    kind: practice
    verify: "https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html; https://argon2-cffi.readthedocs.io/en/stable/api.html"
    window_days: 365
    last_verified: 2026-09-01

  - id: agents-md-standard
    pack: core/40-documentation.md
    claim: "AGENTS.md is the cross-tool open convention (agents.md, Aug 2025; Linux Foundation Agentic AI Foundation stewardship; 60k+ repos, read by 20-30+ agent tools) — a project's AGENTS.md content bar is agent operating instructions, not just topology"
    kind: standard
    verify: "https://agents.md/"
    window_days: 365
    last_verified: 2026-09-01

  - id: ai-markdown-rag-rules
    pack: core/40-documentation.md
    claim: "Heading-hierarchy chunking with intact fenced blocks remains standard RAG practice; one-H1 codified in llms.txt; the pack's 10 AI-markdown rules stay consensus (2 are house style, honestly framed)"
    kind: practice
    verify: "https://llmstxt.org/; search: markdown chunking rag best practice <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: playwright-mv3-sw-fix
    pack: core/45-testing-strategy.md
    claim: "The MV3 service-worker-restart flake fix landed in @playwright/test 1.59 (PR #39476); current is 1.62.x, so every fresh install has it — the pack's floor was retired as noise, the claim carries the boundary"
    kind: version
    verify: "https://registry.npmjs.org/@playwright/test/latest; https://github.com/microsoft/playwright/pull/39476"
    window_days: 365
    last_verified: 2026-09-01

  - id: vitest-rsc-boundary
    pack: core/45-testing-strategy.md
    claim: "2026 consensus incl. official Next.js docs: Vitest+RTL is the legitimate unit lane for sync/props-only components + server-action logic; ASYNC Server Components cannot be rendered by any unit runner (by design) — Playwright E2E owns them; the pack's blanket Vitest ban was retired for the async-boundary rule"
    kind: practice
    verify: "https://nextjs.org/docs/app/guides/testing/vitest"
    window_days: 180
    last_verified: 2026-09-01

  - id: chrome-extension-sideload-flags
    pack: core/45-testing-strategy.md
    claim: "Stable Chrome/Edge removed --load-extension/--disable-extensions-except (Chrome 137/139) — extension E2E must use Playwright's bundled Chromium; the pack's channel guidance rests on this"
    kind: deprecation
    verify: "search: chrome load-extension flag removed <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: musl-allocator-perf
    pack: core/10-python.md
    claim: "musl's allocator/thread-stack defaults still degrade CPython vs glibc (half of the Alpine ban's residual rationale; the reviewer flags it as aging)"
    kind: rationale
    verify: "search: musl mallocng CPython performance <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: robots-txt-rfc9309-standard
    pack: core/57-external-data-sourcing.md
    claim: "robots.txt is a published IETF standard (RFC 9309), not merely a convention; it carries no direct enforcement but non-compliance is used as bad-faith evidence"
    kind: standard
    verify: "search: RFC 9309 robots exclusion protocol standard status"
    window_days: 365
    last_verified: 2026-09-01

  - id: ai-crawler-ua-directives
    pack: core/57-external-data-sourcing.md
    claim: "AI-specific robots.txt user-agent directives (GPTBot, CCBot, Google-Extended, anthropic-ai, PerplexityBot) are the de-facto channel for reserving text-and-data-mining; the named set churns"
    kind: practice
    verify: "search: AI crawler user-agent robots.txt directives TDM opt-out <year>"
    window_days: 90
    last_verified: 2026-09-01

  - id: scraping-bypass-tooling-liability
    pack: core/57-external-data-sourcing.md
    claim: "The live litigation wave reframes scraping from access-authorisation (CFAA, largely settled for logged-out public pages) to anti-circumvention, and targets the TOOLING layer — CAPTCHA solvers, anti-bot bypass, proxy services — as trafficking in circumvention tech. UNSETTLED: no ruling yet. This is why fabrik-lib/captcha-solve and the catalog's proxy/captcha categories are capabilities, not defaults"
    kind: rationale
    verify: "search: DMCA 1201 anti-circumvention web scraping captcha solver proxy liability ruling <year> — has any court RULED?"
    window_days: 90
    last_verified: 2026-09-01

  - id: browsewrap-vs-clickwrap-scraping
    pack: core/57-external-data-sourcing.md
    claim: "ToS shown without affirmative agreement is weak against a logged-out fetcher; clickwrap/account terms bind and make breach an ordinary contract claim — so holding an account changes the answer"
    kind: practice
    verify: "search: browsewrap clickwrap enforceability scraping public data <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: eu-tdm-model-training-regime
    pack: core/57-external-data-sourcing.md
    claim: "Scraping that feeds MODEL TRAINING is regulated separately and more strictly in the EU (GPAI/TDM copyright-reservation obligations) than the same fetch for internal use. DELIBERATELY DATE-FREE in the pack: sources disagreed on the in-force dates during the 2026-09-01 pass, so the rule states the asymmetry, not a timeline"
    kind: standard
    verify: "search: EU AI Act GPAI training data copyright TDM opt-out obligations in force date — reconcile conflicting dates before asserting one"
    window_days: 180
    last_verified: 2026-09-01

  - id: mcp-roster-is-split
    pack: core/57-external-data-sourcing.md
    claim: "The MCP roster is SPLIT (user-level + per-repo .mcp.json), so ~/.claude.json alone shows a subset; canonical is docs/workstation/mcp-roster.md + the repo .mcp.json, probed by scripts/sysadmin/mcp_health.py"
    kind: rationale
    verify: "read: /opt/fabrik/.mcp.json and ~/.claude.json — is the split still in force?"
    window_days: 180
    last_verified: 2026-09-01

  - id: jitter-required-multiprocess-retry
    pack: core/58-resilience.md
    claim: "tenacity's wait_exponential has NO jitter and its own docstring calls it 'not suitable for resolving contention between multiple processes for a shared resource' — hence wait_random_exponential/wait_exponential_jitter for any worker fleet"
    kind: practice
    verify: "read: .venv/lib/**/tenacity/wait.py wait_exponential docstring — is the no-jitter caveat still there? + search: exponential backoff full jitter thundering herd <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: retryable-status-set
    pack: core/58-resilience.md
    claim: "The retryable HTTP set is 408/429/5xx; 400/401/403/404/422 are permanent. 429 is the single most common retryable vendor response, and a flat 'never retry 4xx' rule inverts it"
    kind: practice
    verify: "search: which HTTP status codes are safe to retry 429 408 <year>"
    window_days: 365
    last_verified: 2026-09-01

  - id: retry-after-two-formats
    pack: core/58-resilience.md
    claim: "Retry-After has TWO legal formats — delay-seconds and HTTP-date — and both must be parsed; the value is vendor-controlled so it must be CAPPED before it becomes either a sleep or a pause TTL"
    kind: standard
    verify: "search: Retry-After header HTTP-date delay-seconds RFC 9110 semantics"
    window_days: 365
    last_verified: 2026-09-01

  - id: openrouter-pin-disables-failover
    pack: core/58-resilience.md
    claim: "On OpenRouter, setting `sort` or `order` DISABLES load balancing — and outage-aware routing is part of load balancing — so pinning a provider silently opts the caller out of automatic failover; a `models` request-body array falls back on any error including downtime"
    kind: standard
    verify: "https://openrouter.ai/docs/features/provider-routing — do sort/order still disable load balancing, and does the models array still fall back on any error?"
    window_days: 180
    last_verified: 2026-09-02

  - id: stop-grace-period-default
    pack: core/58-resilience.md
    claim: "Docker Compose stop_signal defaults to SIGTERM and stop_grace_period defaults to 10 SECONDS, after which the daemon sends SIGKILL — so an app whose own shutdown timeout is >= the grace period is SIGKILLed mid-drain. Clean SIGTERM exit is code 143; 137 is SIGKILL (blown grace period, or OOM)"
    kind: standard
    verify: "https://docs.docker.com/reference/compose-file/services/ — is the default still 10s? + search: docker exit code 143 vs 137 SIGTERM SIGKILL"
    window_days: 365
    last_verified: 2026-09-02

  - id: depends-on-is-not-readiness
    pack: core/58-resilience.md
    claim: "Compose depends_on is START ORDER, not readiness; condition: service_healthy gates only the FIRST start and guarantees nothing about a dependency that restarts later — so every process still needs its own bounded, jittered boot retry with a startup deadline"
    kind: standard
    verify: "https://docs.docker.com/reference/compose-file/services/ — read depends_on + condition semantics"
    window_days: 365
    last_verified: 2026-09-02

  - id: population-retry-budget
    pack: core/58-resilience.md
    claim: "A per-request attempt cap does NOT bound fleet retry traffic — 3 attempts still triples load when every request fails at once. The standard control is a POPULATION budget (token bucket or a ratio ceiling, ~10% of recent traffic), as implemented by gRPC retry throttling and the AWS SDK retry quota, and recommended by Google SRE's overload chapter"
    kind: practice
    verify: "search: retry budget token bucket gRPC retry throttling AWS SDK retry quota <year>; https://sre.google/sre-book/addressing-cascading-failures/"
    window_days: 365
    last_verified: 2026-09-02

  - id: deadline-propagation-over-per-hop-timeouts
    pack: core/58-resilience.md
    claim: "Per-hop timeouts compose into work that outlives the caller ('ghost work'); the control is ONE absolute deadline propagated down the chain, each hop deriving its timeout from what remains and short-circuiting when too little is left. gRPC implements this natively via deadlines/grpc-timeout"
    kind: practice
    verify: "https://sre.google/sre-book/addressing-cascading-failures/ § Deadline propagation; + search: deadline propagation absolute deadline microservices <year>"
    window_days: 365
    last_verified: 2026-09-02

  - id: guard-fail-open-default
    pack: core/58-resilience.md
    claim: "For a Redis-backed guard that OPTIMISES (rate limit, pause flag, dedup) the practiced default when Redis is unreachable is FAIL-OPEN — often with a degraded local bound — because failing closed turns a cache blip into a full outage and, if Redis died of load, removes the only thing still serving. FAIL-CLOSED is for guards protecting something a breach does not recover from (contractual vendor limits, spend caps)"
    kind: practice
    verify: "search: rate limiter fail open vs fail closed redis down best practice <year> — is fail-open still the practiced default for optimising guards?"
    window_days: 180
    last_verified: 2026-09-02

  - id: cache-stampede-coalescing
    pack: core/58-resilience.md
    claim: "A hot key's expiry makes every concurrent request miss at the same instant and stampede the origin; the two practiced fixes are request coalescing (single-flight) and probabilistic early expiration/refresh-ahead — this herd arrives from your own cache, not from a retry loop, so jitter and backoff do not touch it"
    kind: practice
    verify: "search: cache stampede thundering herd singleflight probabilistic early expiration <year>"
    window_days: 365
    last_verified: 2026-09-02

  - id: circuit-breaker-half-open-probe-cap
    pack: core/58-resilience.md
    claim: "A circuit breaker's half-open state must admit a LIMITED number of trial calls (a configurable halfOpenMaxRequests / permittedNumberOfCallsInHalfOpenState); a half-open that admits every caller dumps full traffic onto a dependency that has not recovered, re-killing it on a recovery_timeout cycle"
    kind: standard
    verify: "https://learn.microsoft.com/en-us/azure/architecture/patterns/circuit-breaker — does Half-Open still specify a limited number of trial requests?"
    window_days: 365
    last_verified: 2026-09-02

  - id: llm-self-review-agreeableness-bias
    pack: core/50-code-review.md
    claim: "LLM evaluators show agreeableness bias — they confirm correct feedback but fail to reject incorrect feedback, and recall is measurably worse when a model reviews its OWN output than another's. This is the evidence base for author-blind review (D-063) and for maker-checker separation"
    kind: rationale
    verify: "search: LLM self-review agreeableness bias recall maker-checker adversarial code review <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: finder-verifier-false-positive-control
    pack: core/50-code-review.md
    claim: "Finder/verifier separation (finders over-generate; independent refute-by-default verifiers kill a large share) is the architectural false-positive control; published single-pass AI review precision is reported as low enough that verifiers refute a large share of finder output (figures vary by tool and are not pinned here). Signal ratio erosion silences correct findings too"
    kind: practice
    verify: "search: AI code review false positive rate signal ratio finder verifier separation <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: review-loop-shape-alternatives
    pack: core/50-code-review.md
    claim: "The published review-then-implement pattern caps automated fixing at ONE pass and escalates to a human instead of looping, explicitly offering no convergence guarantee. (Our converge-to-quiet choice and its dedup/lens obligations are an internal design decision, not part of this external claim.)"
    kind: rationale
    verify: "read: https://agentpatterns.ai/code-review/review-then-implement-loop/ — has the one-pass cap changed? + search: actor-critic review convergence rounds <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: metric-cardinality-silent-undercount
    pack: core/55-observability.md
    claim: "Metric cardinality overflow is SILENT and corrupts answers: totals stay correct while filter/group-by queries undercount, so dashboards, SLOs and alerts keep rendering wrong numbers. A per-request-varying label value is never a metric label"
    kind: practice
    verify: "search: OpenTelemetry metric cardinality limit overflow undercount otel.metric.overflow <year>"
    window_days: 180
    last_verified: 2026-09-01

  - id: fleet-has-no-trace-backend
    pack: core/55-observability.md
    claim: "PROBED 2026-09-01 across ALL THREE fleet hosts (vps1/vps2/vps3, 0 matches each): Loki + Prometheus + Grafana (+ the EOL Promtail) and NO trace backend / OTel collector anywhere — so distributed tracing is over-engineering here; a request correlation ID is the correct ceiling. Field naming should still follow OTel convention (trace_id/span_id) to avoid a later fleet-wide rename"
    kind: rationale
    verify: >-
      probe ALL THREE hosts (vps, vps2, vps3) with `sudo docker ps` and grep -icE
      'tempo|jaeger|otel|collector'; also check Grafana datasources for a trace store.
      Has a trace backend appeared on ANY host?
    window_days: 90
    last_verified: 2026-09-01

  - id: llms-txt-null-for-visibility
    pack: core/40-documentation.md
    claim: "llms.txt has NO measured effect on AI search visibility — a ~300k-domain citation study found removing the variable improved model accuracy (noise, not signal), a ~137k-domain server-log study found 97% of files got zero requests, and Google states Search ignores it. It remains a community convention, not a ratified standard"
    kind: practice
    verify: "search: llms.txt adoption evidence server log study AI citation correlation <year> — has any frontier lab or search engine committed to reading it?"
    window_days: 180
    last_verified: 2026-09-01

  - id: llms-txt-coding-agents-are-the-audience
    pack: core/40-documentation.md
    claim: "The one measured non-null llms.txt audience is CODING AGENTS (Claude Code out-fetched every AI retrieval bot/assistant/training crawler in the 137k-domain log study), and Chrome files its llms.txt audit under 'agentic browsing' next to WebMCP — agent tooling, not SEO. Agents fetch it only when DIRECTED; zero AI-bot requests arrive for files that do not exist, so an unlinked file is unread"
    kind: practice
    verify: "search: which bots fetch llms.txt coding agents Claude Code server logs <year> + check Chrome Lighthouse audit category"
    window_days: 180
    last_verified: 2026-09-01

  - id: docusaurus-serve-not-for-production
    pack: core/42-docusaurus.md
    claim: "Docusaurus's own deployment docs state `docusaurus serve` is not the best option compared to a static host/CDN, and that Docusaurus's only responsibility is to emit static files in build/ — this is the upstream basis for the pack's ban on a Node runtime in a production container"
    kind: practice
    verify: "read https://docusaurus.io/docs/deployment — does it still steer away from `docusaurus serve` for production?"
    window_days: 365
    last_verified: 2026-09-01

  - id: pagefind-plugin-maintenance-risk
    pack: core/42-docusaurus.md
    claim: "⚠️ RE-VERIFY MUST REWRITE THIS TEXT, not just bump the date — the age below decays. `@getcanary/docusaurus-theme-search-pagefind` is functional and in production use (e.g. Redux Toolkit) but as of 2026-09-01 its newest release was ~23 months old (19 versions, last Oct 2024) and its peer range declares @docusaurus/core ^2||^3 and react ^17||^18 — so Pagefind is the mandate and this plugin is not; verify resolution before adopting"
    kind: version
    verify: "check npm for BOTH: @getcanary/docusaurus-theme-search-pagefind (any newer release? does its peer range cover the site's majors?) AND the alternative docusaurus-plugin-pagefind (still maintained? peers still current?). As of 2026-09-01: getcanary 1.0.2 (19 versions, last 2024-10-12, peers @docusaurus/core ^2||^3 + react ^17||^18); docusaurus-plugin-pagefind 0.2.2 (2026-07-24, peers @docusaurus/core >=3 + react >=18). The pack deliberately carries NO numbers — they live here."
    window_days: 180
    last_verified: 2026-09-01

  - id: pagefind-scale-ceiling
    pack: core/42-docusaurus.md
    claim: "Pagefind's OWN stated envelope is 'tens of thousands of pages', with a ~300kB total network payload on a 10,000-page site; past that the index fetch cost is what tells and a hosted engine becomes the better tool. (An earlier draft of this row cited ~25k pages as Pagefind's figure — that number is third-party, not the vendor's.) Algolia is Docusaurus's own first-class option — our ban is a constraint (external SaaS; free DocSearch requires a public site), not a quality judgement"
    kind: rationale
    verify: "search: Pagefind index size scaling limit pages vs hosted search <year> + check Docusaurus docs/search for its recommended options"
    window_days: 365
    last_verified: 2026-09-01

  - id: watchdog-tier-d-window-is-1800-fleet-wide
    pack: core/60-watchdog.md
    claim: "The Tier-D approval window IS plumbed end-to-end: spec_loader.py WatchdogConfig.code_fix_window_sec (default 1800, ge=60 le=3600) -> drivers/watchdog.py exports WATCHDOG_APPROVAL_WINDOW_SEC -> the module's control_plane/approval.py reads it. Commit 2c5b56e1 raised the fleet default 300->1800 deliberately. The bare 300 in coordinator.py is an unreachable fallback for STANDALONE/vendored use only"
    kind: rationale
    verify: "trace the whole chain, not one repo: grep code_fix_window_sec in /opt/fabrik/src/fabrik/spec_loader.py AND drivers/watchdog.py, then grep WATCHDOG_APPROVAL_WINDOW_SEC in /opt/fabrik-lib/watchdog. ⚠️ The module never spells the SPEC field name — searching fabrik-lib for `code_fix_window_sec` alone returns zero and falsely reads as 'not implemented'"
    window_days: 180
    last_verified: 2026-09-01

  - id: watchdog-guard-bounds-verified
    pack: core/60-watchdog.md
    claim: "VERIFIED IN SOURCE 2026-09-01: the pack's numeric guards match watchdog_sidecar/actions.py — _FORBIDDEN_TARGETS is exactly 14 entries (postgres-main…site-provisioner), pause TTL bounds [5,3600], drop_queue_items row cap 1..10000, rotate_locks age [30,86400], and the 100_000-key wipe cap"
    kind: rationale
    verify: "re-read /opt/fabrik-lib/watchdog/watchdog_sidecar/actions.py — do the bounds and the forbidden-target count still match the pack's table?"
    window_days: 180
    last_verified: 2026-09-01

  - id: watchdog-db-writes-are-tier-c-gated
    pack: core/60-watchdog.md
    claim: "VERIFIED IN SOURCE 2026-09-01: drop_queue_items and rotate_locks are NOT Tier A — actions.py::TIER_C_WRITE_HANDLERS routes them through the approved-write lane, fail-closed behind WATCHDOG_ALLOW_DB_WRITES (default OFF) and a provisioned WATCHDOG_DB_URL_RW; absent either the handler returns 'skipped'"
    kind: rationale
    verify: "grep -n TIER_C_WRITE_HANDLERS -A6 /opt/fabrik-lib/watchdog/watchdog_sidecar/actions.py — are both still in the Tier-C lane and still double-gated?"
    window_days: 180
    last_verified: 2026-09-01

  - id: watchdog-cost-cap-not-flag-enforced
    pack: core/60-watchdog.md
    claim: "per_incident_budget_usd is NOT enforced by a --max-budget-usd flag: llm_client.py explicitly bans that flag per the operator directive that per-call $ caps break the diagnose loop, and per_incident_cap_usd is accepted for API compatibility without call-site enforcement. Spend is observed via total_cost_usd and bounded by DAILY caps. OpenRouter fallback timeout is 60s (the pack previously said 45s)"
    kind: rationale
    verify: "grep -n 'max-budget-usd\\|_OPENROUTER_TIMEOUT\\|per_incident_cap_usd' /opt/fabrik-lib/watchdog/watchdog_sidecar/llm_client.py"
    window_days: 180
    last_verified: 2026-09-01

  - id: autonomous-remediation-silence-semantics
    pack: core/60-watchdog.md
    claim: "Published autonomous-remediation designs predominantly use SILENCE = DENIAL (an approval request that times out resolves to rejection and never converts into consent); our Tier D uses the inverse (TIMEOUT routes to APPLY). The inversion is defensible for an unattended fleet but is only safe while the hard test gate, post-apply health verification with auto-rollback, and the STOP kill-switch all hold"
    kind: practice
    verify: "search (>=2 engines): autonomous remediation approval timeout semantics silence denial vs consent <year> — has the prevailing convention moved?"
    window_days: 180
    last_verified: 2026-09-01

  - id: watchdog-enabled-has-no-kind-defaulting
    pack: core/60-watchdog.md
    claim: "VERIFIED 2026-09-01: there is NO shape.kind watchdog defaulting anywhere, and `_register_watchdog` (cited 3x in spec_loader.py :414/:417/:812) DOES NOT EXIST. ⚠️ The enablement DEFAULT DIFFERS BY PATH: WatchdogConfig.enabled defaults FALSE (spec_loader.py:428) but the apply path reads raw YAML and defaults TRUE (infrastructure.py:344), so fabrik apply provisions a sidecar where fabrik plan/audit/destroy (model_dump) see enabled:false. The in-code comment at infrastructure.py:337 claims the class defaults True and is wrong"
    kind: rationale
    verify: "READ THE CLASS, not the comment: sed -n 428,430p src/fabrik/spec_loader.py (Field default) AND grep -n 'get(\"enabled\"' src/fabrik/orchestrator/infrastructure.py (raw-dict fallback) — do the two paths still disagree? Plus grep -rn 'def _register_watchdog' src/ (expect nothing)"
    window_days: 180
    last_verified: 2026-09-01

  - id: scalar-docusaurus-client-side-render
    pack: core/42-docusaurus.md
    claim: "@scalar/docusaurus renders an OpenAPI spec client-side at runtime (no build-time .mdx generation), which is why the pack prefers it over docusaurus-plugin-openapi-docs / Redocusaurus — those materialise many physical .mdx files at build, inflating commits and build duration"
    kind: practice
    verify: "check npm @scalar/docusaurus is alive + its docs: does it still render dynamically rather than generating files? And do the banned plugins still generate per-endpoint .mdx?"
    window_days: 180
    last_verified: 2026-09-02
