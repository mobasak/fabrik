<!-- Read by Windsurf Cascade. Cascade silent-truncates at 6,000 chars per file. This file is currently OVER budget (Phase 2 reshape pending) — do not add to it without compressing first. -->
# Cascade Agent Contract

**Read by:** Windsurf Cascade only. Kilo CLI reads `AGENTS-compact.md` (via `opencode.json`); Traycer reads `AGENTS.md`.
**Auto-loaded with this:** glob-matched and `model_decision` packs in `.windsurf/rules/**/*.md` (recursive — includes subdirectories). Don't duplicate their content here.

## ⚠️ FIRST OUTPUT (every response)
```
RULES ACTIVE: CASCADE | <3 rules from this file you applied>
PACKS: <pack codes whose globs match this turn's scope, or "none">
```
`PACKS` line: declare which `.windsurf/rules/*.md` packs are scope-relevant by matching their frontmatter `globs:` against files you will touch this turn. Listing a pack implies you read it. Pure conversation/planning turns with no file scope = `PACKS: none`.

## Orient (every task)
1. Read `project.yaml` (`type`, `ports`, `has_user_guide`), `README.md`, `INDEX.md`, `compose.yaml`, `Dockerfile`, `.env.example`, `pyproject.toml`/`package.json`.
2. If `AFCL.md` exists, append friction findings as encountered.
3. Rule packs in `.windsurf/rules/` self-activate via frontmatter — trust the frontmatter; the table below is for human discovery.
4. Plans in `docs/development/plans/` need: Key Invariants, Failure Modes, 5–10 testable Acceptance Criteria.

## Behavior
- **Check before create:** verify file does not exist before write. Exists = STOP.
- **Present before execute:** plan → approval → execute. Read-only IDE calls (`read_file`, `grep_search`, `list_dir`, `find_references`) are exempt.
- **Stay on task:** no unsolicited advice or process commentary.
- **Conflict resolution:** rule pack > ticket. Surface conflict before proceeding.
- **State conflict:** task contradicts existing state → stop, report. Never silently overwrite.

## Completion Contract
1. **IMPLEMENT** — Strict ticket Scope. Adjacent fixes only inside files already in Scope. 1 test for core path (skip docs-only).
2. **GATE** — Run literal command from ticket's `Final Gate Instruction`; fix until `status: "success"`. Defaults:
   - Standard: `python scripts/final_gate.py --lean --json`
   - Milestone / multi-component / schema / auth: `python scripts/final_gate.py --json`
   - Epic closure: `python scripts/final_gate.py --systemic --json`
3. **CHANGELOG** — One entry under `## [Unreleased]` (gate-enforced).
4. **LESSONS LEARNT** — Fill ticket `Lessons Learnt:` line with `none` OR structured entry in `docs/LESSONS_LEARNT.md`. Silence = failure.
5. **EXIT** — Gate auto-stages on success. STOP. Do NOT run `git commit` / `git push` unless user said "commit" or "push" this turn. Manual `git add` is allowed.

## HARD STOPS — Cascade mechanics
| Rule | Instead |
|:--|:--|
| `git commit` / `git push` (unless user said so this turn) | gate auto-stages — task ends there |
| `fabrik redeploy` without `git push` first (git-sourced apps) | always: `git commit` → `git push` → `fabrik redeploy`; Coolify pulls from GitHub, not local `/opt/` — skipping push redeploys stale code |
| compose without `deploy.resources.limits.memory` on a Coolify-deployed service | Coolify v4.0.0-beta.459 ignores its `limits_memory` UI field for both `build_pack=dockercompose` Applications and one-click Services. The compose itself must carry `deploy.resources.limits` or Docker sees no cap (HostConfig.Memory=0). Scaffolder auto-emits this in `_write_canonical_compose`. For Coolify Services, mutate via `PATCH /api/v1/services/<uuid>` with **base64-encoded** `docker_compose_raw` — never edit `/data/coolify/services/<uuid>/docker-compose.yaml` directly (regenerated from DB on redeploy). F5 fix 2026-05-16 / Lesson 62. |
| `DB_HOST=localhost` / `DATABASE_URL=...@localhost:` in `.env` or Coolify env vars | always use Docker service names: `postgres-main:5432`, `redis-main:6379` — `localhost` inside a container is the container itself, not the host |
| Authelia config reload via SIGHUP | Authelia exits on SIGHUP, does NOT hot-reload — always `docker restart <authelia-container>` after config edits |
| Adding a new Gatus endpoint using hardcoded container name | always use stable Docker DNS name: compose service name for Service stacks, or registered stable alias for single-image Applications. Never use UUID container name — it changes on every redeploy. See `scripts/vps_apply_limits.sh` alias section for registered pairs. |
| Gatus/internal URL using UUID container name (e.g. `tcp://vckgs8c00o40o884k48cgow8-220643454460:3000`) | Coolify single-image Applications have no stable DNS alias by default. After deploying one: (1) add stable alias to `networks.coolify.aliases` in its compose at `/data/coolify/applications/<uuid>/docker-compose.yaml`, (2) run `docker network disconnect coolify <uuid-name> && docker network connect --alias <stable-name> --alias <uuid-name> coolify <uuid-name>`, (3) update `/opt/monitoring/configs/gatus/` to use the stable name, (4) add the alias pair to `vps_apply_limits.sh` for reboot persistence |
| Health check `/health` behind auth | Authelia bypass rule covers `*.vps1.ocoron.com → /health` automatically — never protect `/health` |
| Binding container ports to host directly | all services must be on `coolify` Docker network only; let Traefik route — never expose raw container ports |
| Not wiring `gzip@docker` middleware on router | scaffold emits it automatically; admin UIs: `authelia-forward@docker,gzip@docker`; API services: `gzip@docker`; public: none |
| Running a foreground command that may take >30s (build, install, deploy, test, sync, `fabrik *`, `docker *`, `pytest`, `npm i`) | use `scripts/rund [--timeout S] -- <cmd>` (argv) or `scripts/rundsh '<pipe>'` (shell), then `scripts/runwait $(scripts/runlast) <secs>` to bound wait, then `scripts/runc $(scripts/runlast)` to read result. Never block on foreground long commands — Cascade has no timeout. Full doc: `docs/reference/long-command-monitoring.md`. |
| edit files outside ticket Scope | strict Scope only |
| modify `pyproject.toml` / `requirements.txt` / `package.json` / `uv.lock` / `package-lock.json` | only if ticket explicitly authorises deps change |
| create files outside project tree | local project paths only |
| recreate `.venv` or replace existing Docker config | extend, don't replace |

Technical hard stops (Alpine ban, raw `pip install`, Docker port exposure, Authelia, M2M `X-Internal-Token` / `internal_auth.py` canonical pattern, kebab-case, `/tmp/` ban, port ranges, etc.) → auto-loaded packs are authoritative.

## External Knowledge — Search, Don't Guess
Training data is stale. For any 3rd-party API/SDK/vendor (Coolify, Paddle, Traefik, Authelia, Stripe, Supabase, Cloudflare, n8n):
1. Search repo first: `grep_search` `docs/`, `docs/reference/`, `AFCL.md`, `docs/LESSONS_LEARNT.md`.
2. If absent: try in order — `search_web` → `*web_search_exa` → `*firecrawl_search`. Cite source URL in code comment.
3. If 3 calls don't resolve it, output `BLOCKED: <vendor> — <searched> — <missing>` and stop.

Skip for: stdlib, language syntax, internal Fabrik conventions.

## Pointers (content lives in linked packs)
- **Backup secrets before edit** (`.env`, `*.key`, `*.pem`, `secrets/`, `.ssh/`) → `35-security-auth.md` § Sensitive Data Protection.
- **Password policy** (32 chars, `[a-zA-Z0-9]`, `secrets.choice()`, banned: `postgres`/`admin`/`password123`) → `35-security-auth.md` § Password Policy.
- **Before new scripts:** `grep_search` `scripts/` and `scripts/enforcement/`. Extend, don't duplicate.

## Environment
WSL Ubuntu only. Linux paths, never Windows tooling. Code must work in WSL dev (PG localhost, `.env`), VPS Docker (`postgres-main`, `compose.yaml`), Supabase (env vars) without modification.

## Rule Pack Pointers (navigation only — frontmatter governs loading)
All in `.windsurf/rules/` subdirectories. Frontmatter globs activate packs — paths below are for human discovery.

| Task involves | Path |
|:--|:--|
| Python | `core/10-python.md` |
| API endpoints / schemas | `core/15-api-contracts.md` |
| TypeScript | `core/20-typescript.md` |
| PostgreSQL queries / migrations | `core/25-data-postgres.md` |
| Docker / compose / ops | `core/30-ops.md` |
| Auth / sessions / secrets | `core/35-security-auth.md` |
| Doc generation | `core/40-documentation.md` |
| Docusaurus | `core/42-docusaurus.md` |
| Testing / One-Test Rule | `core/45-testing-strategy.md` |
| Code review / gates | `core/50-code-review.md` |
| Logging / health / monitoring | `core/55-observability.md` |
| Resilience / external calls | `core/58-resilience.md` |
| RAG / vector search | `core/65-rag-search.md` |
| RAG chunking | `core/66-rag-chunking.md` |
| Background workers / jobs | `core/75-workers-jobs.md` |
| GPU workers | `core/76-gpu-workers.md` |
| Paddle / iyzico billing | `core/85-payments-billing.md` |
| Email templates | `core/86-email-templates.md` |
| SaaS UI | `saas/60-saas-ui.md` |
| SaaS launch checklist | `saas/88-saas-launch-checklist.md` |
| Multi-tenant / RLS | `saas/95-multi-tenant-saas.md` |
| React Native / mobile | `mobile-app/80-mobile.md` |
| Mobile billing (RevenueCat) | `mobile-app/81-mobile-billing.md` |
| Mobile launch checklist | `mobile-app/89-mobile-launch-checklist.md` |
| Chrome extensions (MV3) | `chrome-ext/70-chrome-ext.md` |
| Design system (Ocoron) | `core/ocoron-design-system.md` |
| Design system (Tojlo) | `core/tojlo-design-system.md` |

## ⚠️ FINAL OUTPUT (last 4 lines, every response)
```
GATE: <command run> → success|failure
DOCS UPDATED: <files | none>
CHANGELOG: <entry title | n/a>
LESSONS LEARNT: <none | docs/LESSONS_LEARNT.md entry title>
```
Missing any line = task failure. Run gate; if `failure`, fix and re-run until `success`, then output the 4 lines.

## Spec contract awareness

Every Fabrik project has `specs/services/<id>.yaml` with a `shape:` block that drives:

- Which Postgres DB / Redis index / Backrest plan / Gatus endpoint / Prometheus job / GlitchTip project / Authelia rule / Meilisearch index get auto-created on `fabrik apply`
- The shape contract is canonical: code MUST match it, not the other way around

If your code:

- Adds a database call → `shape.needs_database` MUST be `true` in the spec
- Adds a Redis cache → `shape.needs_cache` MUST be `true`
- Exposes `/metrics` → `shape.exposes_metrics` MUST be `true`
- Adds Meilisearch indexes → `shape.has_search_feature` MUST be `true`
- Adds an admin UI behind auth → `shape.is_admin_dashboard` MUST be `true`

If you change code in a way that affects any of the above, ALSO update `specs/services/<id>.yaml`.
Don't ship code that contradicts the spec — `fabrik apply` will skip the registrar and you'll have a silently broken deploy.

To preview what the spec will trigger: `fabrik plan specs/services/<id>.yaml`
