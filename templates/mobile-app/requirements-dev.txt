# Backend test/dev dependencies (not shipped in the runtime image — the
# Dockerfile COPYs only requirements.txt). Install for `pytest server/tests/`:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
httpx>=0.28.0
