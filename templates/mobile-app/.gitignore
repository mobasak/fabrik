# Expo / React Native
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native (regenerated by `expo prebuild` / EAS)
/ios/
/android/

# Secrets & signing
.env*.local
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro / logs
*.log
.metro-health-check*
