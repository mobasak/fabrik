; Force PHP-FPM to listen on IPv4 — prevents 502 when Docker DNS
; resolves service names to IPv6 but FPM binds to [::] only (Gap 18).
; Prefix zz- ensures this loads last and overrides zz-docker.conf.
[www]
listen = 0.0.0.0:9000
