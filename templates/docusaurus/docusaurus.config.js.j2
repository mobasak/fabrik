// @ts-check
import {themes as prismThemes} from 'prism-react-renderer';

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: '{{ site_title | default(name + " Documentation") }}',
  tagline: '{{ site_tagline | default("Developer documentation") }}',
  favicon: 'img/favicon.ico',

  url: 'https://{{ domain }}',
  baseUrl: '/',

  organizationName: '{{ org_name | default("ocoron") }}',
  projectName: '{{ name }}',

  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'warn',

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          sidebarPath: './sidebars.js',
          docItemComponent: "@theme/ApiItem",
        },
        blog: {{ 'true' if features.blog else 'false' }},
        theme: {
          customCss: './src/css/custom.css',
        },
      }),
    ],
  ],

  plugins: [
    [
      'docusaurus-plugin-openapi-docs',
      {
        id: 'api',
        docsPluginId: 'classic',
        config: {
          api: {
            specPath: '{{ openapi.spec_file | default("./openapi.yaml") }}',
            outputDir: 'docs/api',
            sidebarOptions: {
              groupPathsBy: 'tag',
            },
          },
        },
      },
    ],
  ],

  themes: ['docusaurus-theme-openapi-docs'],

  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      navbar: {
        title: '{{ site_title | default(name) }}',
        logo: {
          alt: '{{ name }} Logo',
          src: 'img/logo.svg',
        },
        items: [
          {
            type: 'docSidebar',
            sidebarId: 'guideSidebar',
            position: 'left',
            label: 'Guides',
          },
          {
            type: 'docSidebar',
            sidebarId: 'apiSidebar',
            position: 'left',
            label: 'API Reference',
          },
          {
            href: 'https://{{ main_domain | default(domain | replace("docs.", "")) }}',
            label: 'Website',
            position: 'right',
          },
        ],
      },
      footer: {
        style: 'dark',
        links: [
          {
            title: 'Docs',
            items: [
              { label: 'Getting Started', to: '/docs/getting-started' },
              { label: 'API Reference', to: '/docs/api' },
            ],
          },
          {
            title: 'Product',
            items: [
              { label: 'Website', href: 'https://{{ main_domain | default(domain | replace("docs.", "")) }}' },
              { label: 'Pricing', href: 'https://{{ main_domain | default(domain | replace("docs.", "")) }}/pricing' },
            ],
          },
        ],
        copyright: `Copyright © ${new Date().getFullYear()} {{ org_name | default("Ocoron") }}.`,
      },
      prism: {
        theme: prismThemes.github,
        darkTheme: prismThemes.dracula,
      },
    }),
};

export default config;
