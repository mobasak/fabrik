# Agentic Friction & Constraint Log (AFCL)
**Date:** {{date}} | **System:** Fabrik / Zero-Dollar Enterprise Stack

## 1. Executive Summary
> *Short summary of what the AI "thinks" is slowing down the development cycle.*

## 2. Identified Constraints
| Category | Issue Description | Severity | Impact on Kilo/Traycer |
| :--- | :--- | :--- | :--- |
| Context Drift | Forgot PostgreSQL 16 naming conventions | Med | Syntax Errors |
| Logic Gap | Attempted to use Client-side hooks in Server Components | High | Build Failure |
| Operator Discipline | Re-ran `bootstrap-vps.sh` as `root@<ip>` after `step_01` had disabled root SSH login, triggering fail2ban after 3 failed retries — locked dev WSL out for 10 min. Mitigated 2026-06-07 by adding preflight auto-detection that suggests `ozgur@<ip>` BEFORE the third retry triggers the ban. New rule pack `.windsurf/rules/core/90-bootstrap-scripts.md` codifies the SSH-user-transition trap so any future AI agent reads the rule before walking into it. | High | Lost 10 min recovering from a 1-line operator mistake; would have wasted the entire DR drill window |
| Quote Escaping | Nested `$(...)` inside `echo "..."` inside `remote '...'` survives local bash parsing but explodes on the remote bash ("syntax error near unexpected token"). Discovered 2026-06-07 in step_14b (`python-telegram-bot` version-print). Mitigated by always capturing version-strings into a variable first OR dropping the cosmetic version-print entirely. Rule encoded in `.windsurf/rules/core/90-bootstrap-scripts.md` Rule 2. | High | Script aborts with cryptic remote-bash error mid-bootstrap; only caught by actual ssh execution, NOT by `bash -n` |
| Subagent Pool | `run_agents` on a review finder with a **large inlined prompt** (~10KB plan doc, `tools_enabled=False`+`allow_ungrounded=True`) hung in `alive_waiting` >10 min for both `deepseek/deepseek-v4-pro` and `minimax/minimax-m3` — `pick_models` + dispatch worked, the model response just never streamed back within a 200–260s wall-clock. Discovered 2026-07-11 reviewing `plan-1-converging-doc-maintenance`. Worked around by falling back to the native authoritative review (the skill's documented fallback). Open question: is the latency the OpenRouter gateway, the model, or the big-prompt worktree round-trip? For plan-TEXT review, prefer native finders or chunk the doc; keep the pool for code fan-out where per-unit prompts are smaller. | Med | A pool breadth layer that hangs forces a native-only review (zero flywheel rows) — the exact all-native outcome `check_subagent_flywheel.py` warns against; not a blocker (native decide is authoritative) but the flywheel learned nothing this pass |

## 3. The "Silicon Ceiling" Event
**Scenario:** [Describe the specific coding task where the AI failed]
**Constraint:** [e.g., "The context window was too small to see the FastAPI dependency injection logic."]

## 4. Guardrail Recommendations
- [ ] Update `.kilo.json` to exclude large `/node_modules` from search.
- [ ] Add "Never use 'any' type" to the global TypeScript ruleset.
- [ ] Simplify the `auth.py` logic; it's currently too complex for the agent to map in one pass.

---
*Generated by: Cascade SWE Agent*

## Spec contract awareness

Every Fabrik project has `specs/services/<id>.yaml` with a `shape:` block that drives:

- Which Postgres DB / Redis index / Backrest plan / Gatus endpoint / Prometheus job / GlitchTip project / Authelia rule / Meilisearch index get auto-created on `fabrik apply`
- The shape contract is canonical: code MUST match it, not the other way around

If your code:

- Adds a database call → `shape.needs_database` MUST be `true` in the spec
- Adds a Redis cache → `shape.needs_cache` MUST be `true`
- Exposes `/metrics` → `shape.exposes_metrics` MUST be `true`
- Adds Meilisearch indexes → `shape.has_search_feature` MUST be `true`
- Adds an admin UI behind auth → `shape.is_admin_dashboard` MUST be `true`

If you change code in a way that affects any of the above, ALSO update `specs/services/<id>.yaml`.
Don't ship code that contradicts the spec — `fabrik apply` will skip the registrar and you'll have a silently broken deploy.

To preview what the spec will trigger: `fabrik plan specs/services/<id>.yaml`

---

## Friction log

- **2026-07-11 — OpenRouter subagent pool hangs at `pick_models()` (BROKEN, needs operator fix).** During plan-1 (converging-doc-maintenance) execution, every attempt to dispatch a pool review finder (`from libs.subagents import pick_models, run_agents`) hung with **zero output** — `pick_models("review"/"docs", n=1)` never returned (killed at 140–170s, 3× this session). Because the pool was non-functional, all `/fabrik-review` fan-out for this plan ran on **native `fabrik-reviewer`** (Opus/Sonnet) instead, which records no flywheel rows → the `check_subagent_flywheel.py` "pool-or-declare" gate blocks, resolved via a `NO-POOL:` commit declaration. **Impact:** zero flywheel rows land while the pool hangs; the flywheel can't learn. **Fix needed:** investigate why `pick_models`/`run_agents` hang in this environment (likely a live OpenRouter/DB call with no timeout in `libs/subagents/select.py`) — a bounded timeout + fail-fast would let the pool degrade to native instead of hanging the whole review.

- **2026-08-04 — LCB `--difficulty` filter forces the multi-GB fetch the harness exists to avoid (BROKEN as shipped).** `microbench_coding_direct.py --difficulty hard` filters from the WHOLE `livecodebench/code_generation_lite` release because streaming `.take(N)` reads only the shard head (head-of-window is saturated: 3 models at pass@1=1.00, 7 at 0.98 — filtering is genuinely needed). But the whole-release path downloads ~3GB+ (713M shard OK, next 623M shard died with `httpx.ReadTimeout` at 456kB/s), and a standalone `python -c` probe cached 713M to `~/.cache/huggingface` because only the harness sets `HF_HOME` to `.lcb-hf-cache`. **Ticket-sized fix:** build a one-time difficulty manifest — stream the release ONCE (or resume the download via the harness so `HF_HOME` is set), write `cache/lcb_difficulty_manifest.json` mapping `question_id → difficulty` (+ release tag), then make `--difficulty` a two-step: load manifest → stream with `.take(N*k)` and keep matches until `limit` is hit, failing loud if the manifest lacks the release. Never re-download per run; manifest is ~KB and commits to the repo.

| Subagent Pool (consumer-side) | `fanout()` returns a TUPLE `(results, results_table)` — iterating the return value directly walks the 2-tuple, whose items have no `.text`/`.agent_id`, presenting as "models streamed+completed but returned empty output". Cost on 2026-08-12: FOUR dispatches misread as empty (spec-review trio, two review pairs), 5 flywheel rows falsely scored 0, and a bogus "systemic pool failure" narrative almost shipped upstream — the module was fine all along. Rule: ALWAYS `results, table = fanout(...)`; if every result reads empty AND attrs are missing, suspect YOUR unpacking before the module. | High | False 0-scores poison rankings; breadth layer silently lost |
