# Direct-vendor pricing registry — drives fetch_direct_vendor_prices.py.
#
# Per docs/development/plans/2026-06-29-plan-direct-vendor-pricing.md
# (Phase 1 architecture). Each vendor entry MAY contain:
#
#   pricing_url:    URL the orchestrator fetches
#   fetch_method:   "static" | "rendered" | "stealth" (chooses WebScraper method)
#   parser_module:  Python import path; the orchestrator does
#                   `importlib.import_module(parser_module).extract(payload, url)`
#                   and expects list[ParsedRow] back. Pure function, no I/O.
#   models:         dict mapping DB id → page-side slug + expected pricing_unit.
#                   The orchestrator joins ParsedRow.model_slug to the DB
#                   row via the page-side slug. Unit mismatch REFUSES to write.
#   consecutive_fetch_failures: bumped on HTTP error / parser exception
#                               (handled by orchestrator, not stored in YAML for
#                               Phase 1 — kept in memory + audit log; can be
#                               persisted in YAML in a later phase).
#
# Phase 1 ships parsers for 5 vendors only (the static-HTML audio set).
# The remaining 22 vendors are stubbed with `parser_module: null` so they
# show up in the audit log + scraper output as "not yet ground".

# ============================================================
# Phase 1 — populated, parsers ship in this commit
# ============================================================

assemblyai:
  pricing_url: https://www.assemblyai.com/pricing
  fetch_method: static
  parser_module: direct_vendor_parsers.assemblyai
  models:
    assemblyai/universal-2:
      slug_on_page: universal-2
      expected_unit: audio-min

deepgram:
  pricing_url: https://deepgram.com/pricing
  fetch_method: static
  parser_module: direct_vendor_parsers.deepgram
  models:
    deepgram/nova-2:
      slug_on_page: nova-2
      expected_unit: audio-min
    deepgram/nova-3:
      slug_on_page: nova-3
      expected_unit: audio-min

soniox:
  pricing_url: https://soniox.com/pricing/
  fetch_method: static
  parser_module: direct_vendor_parsers.soniox
  models:
    soniox/stt-async-v4:
      slug_on_page: stt-async-v4
      expected_unit: audio-min
    soniox/stt-realtime-v4:
      slug_on_page: stt-realtime-v4
      expected_unit: audio-min

cartesia:
  pricing_url: https://cartesia.ai/pricing
  fetch_method: static
  parser_module: direct_vendor_parsers.cartesia
  models:
    cartesia/sonic-2:
      slug_on_page: sonic-2
      expected_unit: audio-min

speechmatics:
  pricing_url: https://www.speechmatics.com/pricing
  fetch_method: static
  parser_module: direct_vendor_parsers.speechmatics
  models:
    speechmatics/enhanced:
      slug_on_page: enhanced
      expected_unit: audio-min

# ============================================================
# Phase 2 / 3 / 4 — stubs (parsers TBD); orchestrator skips them
# but reports them in the audit log so coverage is auditable.
# ============================================================

anthropic:
  pricing_url: https://docs.anthropic.com/en/docs/about-claude/pricing  # docs subdomain works rendered (no Cloudflare wall, unlike anthropic.com/pricing)
  fetch_method: rendered
  parser_module: direct_vendor_parsers.anthropic
  models:
    anthropic/claude-opus-4.8:
      slug_on_page: "Claude Opus 4.8"
      expected_unit: M-tokens
    anthropic/claude-sonnet-4.6:
      slug_on_page: "Claude Sonnet 4.6"
      expected_unit: M-tokens
    anthropic/claude-haiku-4.5:
      slug_on_page: "Claude Haiku 4.5"
      expected_unit: M-tokens
    anthropic/claude-fable-5:
      slug_on_page: "Claude Fable 5"
      expected_unit: M-tokens

aws:
  pricing_url: https://aws.amazon.com/transcribe/pricing/
  fetch_method: rendered     # Phase 2 reality-check: raw HTML has no inline $/min anchored to a DB row
  parser_module: null

azure:
  pricing_url: https://azure.microsoft.com/en-us/pricing/details/cognitive-services/speech-services/
  fetch_method: rendered     # Phase 2 reality-check: SSR HTML omits the actual $/X numbers
  parser_module: null

bfl:
  pricing_url: https://bfl.ai/pricing   # Pass-7+ resolved from blackforestlabs.ai
  fetch_method: static
  parser_module: null

coqui:
  pricing_url: https://huggingface.co/coqui/XTTS-v2
  # Phase 3 (2026-06-30): live-probed 144KB rendered, 0 prices total. Coqui XTTS-v2
  # is an HF model card (self-hosted; NOT commercial — no pricing to scrape).
  # Parser stays null; seed values reflect self-host cost estimate. Operator audit
  # quarterly only — vendor will never publish per-call prices for this model.
  fetch_method: rendered
  parser_module: null

deepl:
  pricing_url: https://www.deepl.com/pro
  # Phase 3 (2026-06-30): live-probed rendered HTML — 493KB, 0 per-call patterns.
  # Per-char rate is hidden behind subscription tier prices (`{{pricePerCharacter}}`
  # template placeholder, never populated). Marketing page shows only €X/mo tiers.
  # Wired to subscription_monitor: scans daily for per-call pattern emergence.
  fetch_method: rendered
  parser_module: direct_vendor_parsers.subscription_monitor

elevenlabs:
  pricing_url: https://elevenlabs.io/pricing
  # Phase 3 (2026-06-30): live-probed 700KB, 0 per-call patterns. Confirmed
  # subscription-only — 147 $-patterns on the page are all tier-card values
  # ($0/mo, $22/mo, $99/mo, etc.) and effective per-minute rates per tier
  # (which assume multilingual-v2; per-model rate differentials documented
  # but not priced). Wired to subscription_monitor.
  fetch_method: rendered
  parser_module: direct_vendor_parsers.subscription_monitor

google-cloud:
  pricing_url: https://cloud.google.com/speech-to-text/pricing
  fetch_method: rendered     # Phase 2 reality-check: SSR HTML omits the actual $/X numbers
  parser_module: null

heygen:
  pricing_url: https://www.heygen.com/pricing
  # Phase 3 (2026-06-30): live-probed 771KB rendered, 0 per-call patterns.
  # All 122 $-patterns are subscription tier prices ($0, $24, $69, $89, $349 per mo).
  # Earlier "CF bot-wall" classification was wrong — plain rendered works fine.
  # Wired to subscription_monitor.
  fetch_method: rendered
  parser_module: direct_vendor_parsers.subscription_monitor

ideogram:
  pricing_url: https://ideogram.ai/manage/api   # Pass-7+ resolved: needs stealth
  fetch_method: stealth
  parser_module: null

kling:
  pricing_url: https://klingai.com/pricing
  fetch_method: rendered     # Phase 2 reality-check: SSR HTML omits the actual $/X numbers
  parser_module: null

llamaindex:
  pricing_url: https://www.llamaindex.ai/pricing
  # Phase 3 (2026-06-30): live-probed 934KB rendered, 0 per-call patterns.
  # 6 $-patterns are subscription tier prices ($0, $50, $500) + credit notation
  # ("1,000 credits = $1.25" — credit not per-call). Earlier "CF protection"
  # classification was wrong — plain rendered works. Wired to subscription_monitor.
  fetch_method: rendered
  parser_module: direct_vendor_parsers.subscription_monitor

luma:
  pricing_url: https://lumalabs.ai/dream-machine/pricing
  fetch_method: rendered     # Phase 2 reality-check: SSR HTML omits the actual $/X numbers
  parser_module: null

mistral:
  pricing_url: https://mistral.ai/pricing
  # Pass-9 (2026-06-30): probed 3 URLs (mistral.ai/pricing, docs.mistral.ai,
  # products/la-plateforme) — all return 312-899KB rendered HTML with ZERO
  # $ or € patterns. Same client-render-bound-to-API-fetch wall as qwen.
  # Only 1 DB row (mistral/ocr) — low ROI. Operator quarterly check.
  fetch_method: rendered
  parser_module: null

openai:
  pricing_url: https://platform.openai.com/docs/pricing  # platform subdomain (no CF wall, unlike openai.com/api/pricing)
  fetch_method: rendered
  parser_module: direct_vendor_parsers.openai
  models:
    openai/whisper-large-v3:
      slug_on_page: "Whisper"
      expected_unit: audio-min
    openai/gpt-4o-transcribe:
      slug_on_page: "gpt-4o-transcribe"
      expected_unit: audio-min
    openai/gpt-4o-mini-transcribe:
      slug_on_page: "gpt-4o-mini-transcribe"
      expected_unit: audio-min
    openai/tts-1:
      slug_on_page: "tts-1"
      expected_unit: M-chars
    openai/tts-1-hd:
      slug_on_page: "tts-1-hd"
      expected_unit: M-chars
    openai/gpt-4o-mini-tts:
      slug_on_page: "gpt-4o-mini-tts"
      expected_unit: M-chars

pika:
  pricing_url: https://pika.art/pricing
  fetch_method: rendered     # Phase 2 reality-check: credit-based pricing; subscription-only verification in Phase 3
  parser_module: null

# playht: REMOVED — vendor permanently shut down 2025-12-31 (Meta acquisition);
# see CHANGELOG entry + seed_direct_vendors.py comment block.

qwen:
  pricing_url: https://www.alibabacloud.com/help/en/model-studio/model-pricing
  # Pass-9 (2026-06-30): probed 4 URLs (alibabacloud.com, help.aliyun.com,
  # bailian.console, qwenlm.ai) — all return 300-900KB rendered HTML with
  # ZERO $ or ¥ patterns. Pricing tables are bound to React state populated
  # by client-side API fetches that browserless v2 doesn't deep-execute.
  # Same wall as mistral. 12 DB rows would benefit; not worth a Playwright
  # parser given Qwen prices change ~quarterly. Operator quarterly check.
  fetch_method: rendered
  parser_module: null

recraft:
  pricing_url: https://www.recraft.ai/pricing
  fetch_method: rendered     # Phase 2 reality-check: credit-based pricing; subscription-only verification in Phase 3
  parser_module: null

runway:
  pricing_url: https://runwayml.com/pricing
  fetch_method: static         # Pass-1 found subscription-only; Phase 3 status-only verification
  parser_module: null

stability:
  pricing_url: https://stability.ai/pricing
  fetch_method: rendered     # Phase 2 reality-check: SSR HTML omits the actual $/X numbers
  parser_module: null

suno:
  pricing_url: https://suno.com/pricing
  # Phase 3 (2026-06-30): live-probed 393KB rendered, 0 per-call patterns, 0 per-unit
  # qualifiers (just bare $-amounts = subscription tier prices). Pure subscription.
  # Earlier "CF bot-wall" classification was wrong — plain rendered works.
  fetch_method: rendered
  parser_module: direct_vendor_parsers.subscription_monitor

udio:
  pricing_url: https://www.udio.com/pricing
  # Phase 3 (2026-06-30): live-probed 189KB rendered, 0 per-call patterns. 12
  # $-patterns are subscription tier prices (Free $0, Standard $10, Pro $30).
  # Earlier "CF bot-wall" classification was wrong — plain rendered works.
  fetch_method: rendered
  parser_module: direct_vendor_parsers.subscription_monitor
