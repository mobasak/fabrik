# Operator-curated mapping: our agents.id  →  fal.ai model id
#
# Verified 2026-06-29 (Phase 2 of plan-2-aggregator-pricing.md): every entry
# was confirmed present in `GET https://fal.ai/api/models` paginated catalog.
# Pricing is parsed from each model's `pricingInfoOverride` Markdown field
# (or returns NULL when the vendor hasn't filled it in — flagged confidence=0).
#
# Adding a row: confirm the fal id by searching the catalog, then verify
# its pricingInfoOverride is parseable by fetch_fal_prices.py --dry-run.

mirrors:
  # ---------- BFL FLUX (6 entries) ----------
  bfl/flux-pro-1.1: fal-ai/flux-pro/v1.1
  bfl/flux-pro-1.1-ultra: fal-ai/flux-pro/v1.1-ultra
  bfl/flux-dev: fal-ai/flux/dev
  bfl/flux-schnell: fal-ai/flux/schnell

  # ---------- Stability (1 entry) ----------
  stability/sdxl: fal-ai/fast-sdxl

  # ---------- ElevenLabs TTS / STT / SFX / music (6 entries) ----------
  elevenlabs/multilingual-v2: fal-ai/elevenlabs/tts/multilingual-v2
  elevenlabs/turbo-v2.5: fal-ai/elevenlabs/tts/turbo-v2.5
  elevenlabs/eleven-v3-alpha: fal-ai/elevenlabs/tts/eleven-v3
  elevenlabs/sound-effects: fal-ai/elevenlabs/sound-effects/v2

# ---------- WISHLIST (no fal.ai mirror as of 2026-06-29) ----------
#   recraft/v4.1               → Recraft API only (fal has only recraft/upscale/*)
#   recraft/nano-banana        → fal has fal-ai/nano-banana-2 but billing differs;
#                                 Recraft Nano Banana spec we encoded is direct-only
#   soniox/stt-async-v4        → direct only
#   openai/dall-e-3            → OpenAI direct only
#   google/imagen-*, veo-*     → Google direct only
#   bfl/flux-pro                → fal has flux-pro/v1.1 + ultra but no v1.0
#   bfl/flux-fill              → fal has flux-pro/v1/fill (different model)
#   bfl/flux-redux             → no fal equivalent
#   stability/sd3.5-*          → no fal mirror
#   stability/stable-audio-2   → no fal mirror (eleven music is closest)
#   openai/whisper-large-v3    → fal has elevenlabs/speech-to-text/scribe-v2 not whisper
