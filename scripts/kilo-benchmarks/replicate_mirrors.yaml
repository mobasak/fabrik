# Operator-curated mapping: our agents.id  →  Replicate model slug
#
# Verified 2026-06-29 (Pass 3a of plan-2-aggregator-pricing.md): every entry
# returns HTTP 200 from `GET https://api.replicate.com/v1/models/{slug}` AND
# the model's public page (`https://replicate.com/{slug}`) carries the
# `billingConfig.current_tiers[0].prices[]` payload in a
# `<script id="react-component-props-*">` block.
#
# Adding a row: verify both checks pass before commit.
# Removing a row: leave the slug here as a comment with a date + reason
# (Phase 2 fal.ai mirror map mirrors the format).

mirrors:
  # ---------- BFL FLUX (7 entries) ----------
  bfl/flux-pro-1.1: black-forest-labs/flux-1.1-pro
  bfl/flux-pro-1.1-ultra: black-forest-labs/flux-1.1-pro-ultra
  bfl/flux-pro: black-forest-labs/flux-pro
  bfl/flux-dev: black-forest-labs/flux-dev
  bfl/flux-schnell: black-forest-labs/flux-schnell
  bfl/flux-fill: black-forest-labs/flux-fill-pro
  bfl/flux-redux: black-forest-labs/flux-redux-schnell

  # ---------- Stability (3 entries) ----------
  stability/sdxl: stability-ai/sdxl
  stability/sd3.5-large: stability-ai/stable-diffusion-3.5-large
  stability/sd3.5-large-turbo: stability-ai/stable-diffusion-3.5-large-turbo

  # ---------- OpenAI Whisper (1 entry) ----------
  openai/whisper-large-v3: openai/whisper

  # ---------- Stability Audio (1 entry, community port) ----------
  stability/stable-audio-2: stackadoc/stable-audio-open-1.0

# ---------- WISHLIST (not on Replicate; direct-only) ----------
# Documented for the next operator who tries to add them:
#   recraft/v4.1            → Recraft API only (direct)
#   runway/gen-*            → Runway API only
#   kling/v*                → Kling API only (operator-checked 2026-06-29)
#   luma/*                  → Luma API only
#   suno/v*, udio/v1        → no Replicate mirror
#   elevenlabs/*            → direct only
#   google/imagen-*, veo-*  → Vertex AI exclusive
#   openai/dall-e-*         → OpenAI direct API only
#   openai/sora-turbo       → OpenAI direct only
#   ideogram/v*             → Ideogram API only
